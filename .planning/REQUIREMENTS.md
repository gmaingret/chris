# v2.3 Test Data Infrastructure — Requirements

**Milestone:** v2.3 (single phase)
**Spec:** `M008.5_Test_Data_Infrastructure.md` (repo root)
**Goal:** Build the primed-fixture pipeline (organic base fetched from prod + synthetic delta generated on demand) so every downstream milestone can be validated immediately — no real-calendar-time data-accumulation gates.

---

## Active Requirements

### Organic data fetch (FETCH)

- [ ] **FETCH-01**: Operator can run `npx tsx scripts/fetch-prod-data.ts` to dump live prod data into a timestamped snapshot directory under `tests/fixtures/prod-snapshot/<ISO8601>/` and exit 0 on success.
- [ ] **FETCH-02**: The snapshot contains JSONL exports of: `pensieve_entries` (restricted to `source='telegram'`), `pensieve_embeddings` (scoped to those telegram-source ids), `episodic_summaries`, `decisions`, `decision_events`, `decision_capture_state`, `contradictions`, `proactive_state`, `memories`. Each file has stable row ordering for diffability.
- [ ] **FETCH-03**: Fetch script uses SSH to Proxmox (192.168.1.50) with the existing operator auth — no new credentials introduced, no read-write operations on prod.
- [ ] **FETCH-04**: `tests/fixtures/prod-snapshot/LATEST` symlink points to the newest snapshot directory after each fetch run.
- [ ] **FETCH-05**: `tests/fixtures/prod-snapshot/` is gitignored (added to `.gitignore` if not already excluded) — snapshots stay local per developer.

### Synthetic delta (SYNTH)

- [x] **SYNTH-01
**: Operator can run `npx tsx scripts/synthesize-delta.ts --organic <stamp> --target-days N --seed NN --milestone <name>` to produce a fused organic+synthetic fixture under `tests/fixtures/primed/<milestone>-<N>days/`.
- [x] **SYNTH-02
**: Synthetic pensieve entries are generated by a Haiku call per synthetic day that uses randomly-sampled organic telegram entries as few-shot examples; output entries carry realistic UTC+1/+2 timestamps and `source='telegram'`.
- [x] **SYNTH-03
**: Synthetic episodic summaries are produced by running the real `runConsolidate(date)` from `src/episodic/consolidate.ts` against each synthetic day — the engine under test produces the fixture's summaries.
- [x] **SYNTH-04
**: Synthetic decisions carry realistic `resolve_by` spread. If the consuming milestone requires resolutions, synthesize resolution replies and run real `handleResolution`.
- [x] **SYNTH-05
**: Synthetic contradiction pairs are deterministic: N pre-written adversarial pairs inserted with `status='DETECTED'`.
- [x] **SYNTH-06
**: Synthetic `wellbeing_snapshots` rows are generated with realistic 1–5 distributions (for M009 forward use; schema may be added by M009 itself — v2.3 codes against whatever table exists).
- [x] **SYNTH-07
**: Re-running `synthesize-delta.ts` with the same `--seed` against the same organic base produces byte-identical non-LLM output. Anthropic outputs are captured VCR-style on first run and replayed on subsequent runs.

### Test harness integration (HARN)

- [ ] **HARN-01**: `src/__tests__/fixtures/load-primed.ts` exports `loadPrimedFixture(name)` that seeds the Docker Postgres test DB from a primed fixture directory.
- [ ] **HARN-02**: `loadPrimedFixture` clears target tables in FK-safe order before load; repeated calls within a test suite are idempotent and collision-safe.
- [ ] **HARN-03**: A sanity-check Vitest integration test under `fileParallelism: false` loads an `m008-14days` primed fixture and asserts ≥ 7 days of summaries exist, ≥ 200 pensieve entries, and the episodic-tier contract holds (UNIQUE(summary_date), no Immich source leakage).

### Freshness + reproducibility (FRESH)

- [ ] **FRESH-01**: When `tests/fixtures/prod-snapshot/LATEST` is older than 24 hours, `synthesize-delta.ts` and `regenerate-primed.ts` silently invoke `fetch-prod-data.ts` before proceeding; no warning prompts, no half-stale runs.
- [x] **FRESH-02
**: A `--no-refresh` flag on `synthesize-delta.ts` / `regenerate-primed.ts` skips the auto-fetch and uses whatever `LATEST` points to (for offline / sandbox / air-gapped workflows only).
- [ ] **FRESH-03**: `npx tsx scripts/regenerate-primed.ts --milestone <name> --force` refreshes everything from scratch: fetch → synthesize → VCR cache rebuild, independent of any stamp age.

### Documentation (DOC)

- [ ] **DOC-01**: `.planning/codebase/TESTING.md` is updated with a new section explaining the organic+synthetic primed-fixture pattern, `loadPrimedFixture(name)` usage, and the 24h freshness policy.
- [ ] **DOC-02**: A new project convention is added to `PROJECT.md` and/or `.planning/codebase/CONVENTIONS.md`: *"no milestone may gate on real calendar time for data accumulation — use the primed-fixture pipeline instead."*

---

## Future Requirements (deferred, in scope for later milestones)

- **Synthetic image generation for Immich-path tests** — defer until a milestone specifically exercises vision/photo paths.
- **Synthetic voice transcripts** — defer until voice-upload lands.
- **Cross-environment snapshot migration** (e.g., prod → staging) — no staging environment currently exists.
- **Automatic regression detection on snapshot diff** — nice-to-have observability; defer unless a concrete milestone needs it.

---

## Out of Scope (explicit exclusions)

- **PII scrubbing / anonymization** — Greg is the sole user of Chris. If a second user ever joins, revisit before any new snapshot is taken.
- **Real-time prod → test DB replication** — snapshots are pull-based on demand, not streaming.
- **Writing back to prod from tests** — the fetch script is read-only pg_dump; no write path exists.
- **Synthetic sources other than `source='telegram'`** — Immich/Gmail/Gdrive are excluded from v2.3 snapshots per M008.1 consolidation-contract alignment. Add in a later milestone if/when ambient-retrieval tests need them.

---

## Traceability

| REQ-ID | Phase | Plan | Status |
|--------|-------|------|--------|
| FETCH-01 | 24 | 24-01 | complete |
| FETCH-02 | 24 | 24-01 | complete |
| FETCH-03 | 24 | 24-01 | complete |
| FETCH-04 | 24 | 24-01 | complete |
| FETCH-05 | 24 | 24-01 | complete |
| SYNTH-01 | 24 | 24-02 | complete |
| SYNTH-02 | 24 | 24-02 | complete |
| SYNTH-03 | 24 | 24-03 | complete |
| SYNTH-04 | 24 | 24-02 | complete |
| SYNTH-05 | 24 | 24-02 | complete |
| SYNTH-06 | 24 | 24-02 | complete |
| SYNTH-07 | 24 | 24-02 | complete |
| HARN-01 | 24 | 24-04 | pending |
| HARN-02 | 24 | 24-04 | pending |
| HARN-03 | 24 | 24-04 | pending |
| FRESH-01 | 24 | 24-01 | complete |
| FRESH-02 | 24 | 24-02 | complete |
| FRESH-03 | 24 | 24-04 | pending |
| DOC-01 | 24 | 24-04 | pending |
| DOC-02 | 24 | 24-04 | pending |

**Total:** 20 requirements across 5 categories. All mapped to Phase 24 (single-phase milestone), distributed across 4 plans (24-01: 6 reqs, 24-02: 7 reqs, 24-03: 1 req, 24-04: 6 reqs).
