# v2.2 M008 Episodic Consolidation — Integration Check

**Milestone:** M008 Episodic Consolidation (PRD v2.2)
**Phases in scope:** 20 (schema/tech-debt) · 21 (consolidation engine) · 22 (cron + retrieval routing) · 23 (test suite + backfill + /summary)
**Date:** 2026-04-18
**Method:** Cross-phase wiring audit (export → import → call-site verification) and E2E flow tracing of committed source files. NOT a runtime test execution — tests are inspected for wiring correctness only; pass/fail at runtime depends on Docker gate (`bash scripts/test.sh`).

---

## Executive Summary

| Metric | Value |
|---|---|
| Total requirements checked | 35 / 35 |
| WIRED | 32 |
| PARTIALLY WIRED | 2 |
| UNWIRED | 1 |
| E2E flows verified | 6 / 6 |
| Flows COMPLETE | 5 |
| Flows DEGRADED | 1 |
| Flows BROKEN | 0 |
| Boundary violations (RETR-05/06) | 0 |
| Orphaned exports | 1 |

**One material integration gap:** `retrieveContext` (RETR-02/03) is exported from `src/pensieve/routing.ts` and exercised by tests, but no production engine handler calls it. The routing decision (recency / verbatim-keyword / high-importance descent) is therefore live as a tested function but never invoked at runtime by chat-mode handlers. RETR-04 (INTERROGATE date-anchored injection) bypasses `retrieveContext` and calls `getEpisodicSummary` directly — that path is fully wired.

The remaining 32 requirements are wired end-to-end through committed code. Drizzle snapshot lineage (TD-01) is contiguous 0000..0005 with no orphaned snapshots. The D031 boundary (RETR-05/06) is enforced by `src/chris/__tests__/boundary-audit.test.ts` against `personality.ts`, `ground-truth.ts`, and `embeddings.ts` — all three contain zero `episodic_summaries` references.

---

## E2E Flow Verification

### Flow 1 — Daily cron consolidation — COMPLETE

```
src/index.ts:89  cron.schedule(config.episodicCron, runConsolidateYesterday, { timezone: config.proactiveTimezone })
  ↓ src/index.ts:11  import { runConsolidateYesterday } from './episodic/cron.js'
src/episodic/cron.ts:90  runConsolidateYesterday(now = new Date())
  ↓ computeYesterday(now, config.proactiveTimezone)  // Intl.DateTimeFormat 'en-CA' → UTC midnight − 86_400_000
src/episodic/cron.ts:104  await runConsolidate(yesterday)
  ↓ src/episodic/cron.ts:45  import { runConsolidate } from './consolidate.js'
src/episodic/consolidate.ts:202  runConsolidate(date)
  ├─ Step 2: pre-flight SELECT episodicSummaries WHERE summaryDate = localDateStr  (CONS-03)
  ├─ Step 3: getPensieveEntriesForDay(date, tz)  (CONS-02 entry-count gate)
  │    ↓ src/episodic/sources.ts:91  → dayBoundaryUtc(date, tz) via Luxon
  ├─ Step 4: Promise.all([getContradictionsForDay, getDecisionsForDay])  (CONS-08, CONS-09)
  ├─ Step 5: assembleConsolidationPrompt({...})  (prompts.ts:106)
  │    ↓ injects CONSTITUTIONAL_PREAMBLE (CONS-04, prompts.ts:117)
  │    ↓ injects importance rubric (CONS-05), verbatim clause (CONS-10),
  │      sparse guard (CONS-11), floor hints (CONS-06/07)
  ├─ Step 6: anthropic.messages.parse(...) with zodOutputFormat(EpisodicSummarySonnetOutputSchemaV4)
  ├─ Step 7: runtime importance floors (CONS-06: ≥6 if real decision; CONS-07: ≥7 if contradiction)
  ├─ Step 8: parseEpisodicSummary(...)  (Zod re-validation)
  ├─ Step 9: db.insert(episodicSummaries).onConflictDoNothing({ target: summaryDate })  (CONS-03)
  └─ Step 10 catch: notifyConsolidationError(date, err)  (CONS-12)
       ↓ src/episodic/notify.ts:40  → bot.api.sendMessage(config.telegramAuthorizedUserId, ...)
```

Cross-phase wiring: index.ts (P22) → cron.ts (P22) → consolidate.ts (P21) → sources.ts (P21) + prompts.ts (P21) + notify.ts (P21) → schema.ts (P20) + types.ts (P20) + config.ts (P20). Every link is a real `import` + call site, not just an export.

**Status:** COMPLETE. All eight CONS-* + CRON-* + EPI-04 + CONSTITUTIONAL_PREAMBLE wiring points present.

### Flow 2 — Retrieval routing (recency + verbatim + descent) — DEGRADED

```
test (only)  →  retrieveContext({ query, queryDate, rawLimit })
                  ↓ src/pensieve/routing.ts:129
                  ├─ if hasVerbatimKeyword(query) → hybridSearch  (RETR-03)
                  ├─ if queryAge ≤ 7 → hybridSearch  (RETR-02 raw branch)
                  ├─ else getEpisodicSummary(queryDate)
                  │    ├─ if null → hybridSearch (no-summary-fallback)
                  │    ├─ if importance ≥ 8 → loadEntriesByIds(summary.sourceEntryIds)  (RETR-03 descent)
                  │    └─ else summary-only
                  └─ catch → hybridSearch fallback
```

**Verified test coverage:**
- `src/pensieve/__tests__/routing.test.ts` — 30+ unit tests against the export.
- `src/episodic/__tests__/synthetic-fixture.test.ts:608` — TEST-17a/b/c/d exercises all four routing branches against the real `retrieveContext` orchestrator with mocked `hybridSearch` + `getEpisodicSummary` and (TEST-17d) real Drizzle round-trip for `loadEntriesByIds`.

**Production wiring:** NONE. `grep -rn "retrieveContext\b" src/` outside tests finds zero call sites. Mode handlers (`journal.ts`, `interrogate.ts`, `reflect.ts`, `coach.ts`, `psychology.ts`, `produce.ts`) all call `hybridSearch` or `searchPensieve` directly with mode-specific `*_SEARCH_OPTIONS` — none route through `retrieveContext`.

**Implication:** RETR-02 (recency routing) and RETR-03 (verbatim fast-path + high-importance descent) exist as a tested library but have no live runtime path. A user query about a date 30 days ago does NOT pass through `retrieveContext` — it goes through the regular `hybridSearch` cosine-similarity path (or the INTERROGATE-specific date-extraction path in Flow 3). The episodic-summary tier IS reachable via Flow 3 (date-anchored INTERROGATE) and Flow 5 (/summary), but the general "old query → summary" routing is dead code at the entry level.

**Status:** DEGRADED. Tests pass, contracts hold, but the orchestrator function is orphaned at runtime.

### Flow 3 — INTERROGATE date-anchored injection (RETR-04) — COMPLETE

```
chat message → engine → handleInterrogate(chatId, text, language, declinedTopics)
  ↓ src/chris/modes/interrogate.ts:53
  ├─ extractQueryDate(text, language)  (date-extraction.ts:226)
  │    ├─ matchIsoDate (regex, no Anthropic call)
  │    ├─ matchRelativeAgo (EN/FR/RU "N units ago")
  │    ├─ matchMonthDay (EN/FR/RU)
  │    └─ if no fast-path AND hasDateHeuristic → Haiku fallback
  ├─ if queryDate AND ageDays > 7
  │    └─ getEpisodicSummary(queryDate)  (retrieve.ts:343)
  │         └─ if found → episodicBlock = formatEpisodicBlock(summary)
  │              "## Recent Episode Context (interpretation, not fact)"  (D031 marker)
  ├─ searchPensieve(text, 10)  (retrieve.ts:50)
  ├─ pensieveContext = episodicBlock + buildPensieveContext(searchResults)
  └─ buildSystemPrompt('INTERROGATE', pensieveContext, ...)
       ↓ prepends CONSTITUTIONAL_PREAMBLE
       ↓ appends Known Facts block (from GROUND_TRUTH only)
```

Cross-phase wiring: P22 `extractQueryDate` + `getEpisodicSummary` consumed by P21-era `handleInterrogate`. `episodicBlock` is prepended (interrogate.ts:101) so D031 marker appears before raw citations.

**Status:** COMPLETE. RETR-04 fully wired. Date extraction has regex fast-path (no LLM cost on query without date heuristic — date-extraction.ts:245) per spec.

### Flow 4 — Operator backfill — COMPLETE

```
$ npx tsx scripts/backfill-episodic.ts --from 2026-04-13 --to 2026-04-17
  ↓ scripts/backfill-episodic.ts:243  main() → parseCliArgs + runBackfill
  ↓ scripts/backfill-episodic.ts:158  runBackfill(from, to, opts?)
    for each dateStr in iterateDates(from, to):
      ├─ 2_000ms inter-day delay (OPS-01 verbatim)
      ├─ runConsolidate(new Date(`${dateStr}T00:00:00Z`))
      │    ↓ scripts/backfill-episodic.ts:37  import { runConsolidate } from '../src/episodic/consolidate.js'
      │  ─ engine normalizes UTC midnight → config.proactiveTimezone localDateStr (consolidate.ts:205)
      │  ─ CONS-03 idempotency: pre-flight SELECT + ON CONFLICT DO NOTHING
      └─ classify result: 'inserted' | 'skipped' (existing|no-entries) | 'failed' (D-22 continue-on-error)
  ↓ logger.info(totals, 'backfill.complete')
  ↓ console.log("backfill complete: ${inserted} inserted, ${skipped} skipped, ${errored} errored ...")
  ↓ process.exit(0)
```

Cross-phase wiring: P23 script → P21 `runConsolidate`. The script bypasses P22's `runConsolidateYesterday` wrapper (correct — backfill needs explicit historical dates, not "yesterday relative to now"). Argument validation rejects future dates (line 115) and span > 365 days (line 121). Same `ConsolidateResult` discriminated-union shape consumed correctly (lines 187/193/200).

**Status:** COMPLETE. OPS-01 fully wired with safety valves and idempotency reuse.

### Flow 5 — `/summary` Telegram command (CMD-01) — COMPLETE

```
Telegram message "/summary" or "/summary 2026-04-15"
  ↓ src/bot/bot.ts:32  bot.command('summary', handleSummaryCommand)  (registered BEFORE generic text handler at line 74)
  ↓ src/bot/handlers/summary.ts:156  handleSummaryCommand(ctx)
  ├─ langOf(getLastUserLanguage(chatId))
  ├─ parse "after" segment with /^\/summary(?:@\w+)?\s*/i
  ├─ if empty → targetDate = yesterdayInTz(config.proactiveTimezone)
  ├─ elif ISO_DATE.test(after) → targetDate = after
  ├─ else → reply MSG.usage[lang]  (D-27 no apology, no flattery)
  ├─ if isFutureDate(targetDate) → reply MSG.noRowFuture[lang]  (D-32 short-circuit)
  ├─ row = await getEpisodicSummary(new Date(`${targetDate}T00:00:00Z`))  (RETR-01)
  ├─ if row === null → MSG.noRowPast[lang]  (D-30 "not an error")
  └─ else → ctx.reply(formatSummary(row, lang))   plain text, no parse_mode (D-31)
  catch → logger.warn + MSG.genericError[lang]
```

Cross-phase wiring: P23 handler → P22 `getEpisodicSummary` → P20 schema. `bot.ts:32` registers the command before the generic `bot.on('message:text')` handler (line 74) — order discipline correct, /summary will not be swallowed by the JOURNAL pipeline. Inherits single-user `auth` middleware (bot.ts:20).

**Status:** COMPLETE.

### Flow 6 — Boundary preservation (RETR-05/06) — COMPLETE

```
Static-source audit at src/chris/__tests__/boundary-audit.test.ts:
  ├─ src/chris/personality.ts (Known Facts builder) — 0 episodic_summaries refs  (RETR-05)
  ├─ src/pensieve/ground-truth.ts (KF data source) — 0 episodic_summaries refs  (RETR-05)
  ├─ src/pensieve/embeddings.ts (embed INSERT path) — 0 episodic_summaries refs  (RETR-06)
  └─ db.insert(pensieveEmbeddings) regex match — no /episodic/i in any INSERT block  (RETR-06)
```

Verified: `Grep "episodic_summaries|episodicSummaries"` against the three target files returns no matches. The audit test is committed and asserts `[]` against any future regression.

**Status:** COMPLETE. D031 boundary enforced by source-text scanning, not relying on review discipline.

---

## Per-REQ-ID Coverage Table (35 / 35)

| REQ-ID | Phase | Integration Path | Status | Issue |
|---|---|---|---|---|
| TD-01 | 20 | drizzle/meta/_journal.json contiguous 0000..0005, snapshot files match | WIRED | — |
| EPI-01 | 20 | `episodicSummaries` pgTable in db/schema.ts:319-340 (8 cols + 3 indexes); migration 0005_episodic_summaries.sql committed | WIRED | — |
| EPI-02 | 20 | `unique('summary_date')` + `index('topics').using('gin')` + `index('importance')` in schema.ts:334-336 | WIRED | — |
| EPI-03 | 20 | 3-layer Zod chain in episodic/types.ts (Sonnet/Insert/full row) + parseEpisodicSummary; consumed at consolidate.ts:272 | WIRED | — |
| EPI-04 | 20 | `config.episodicCron` defined at config.ts:48; consumed at index.ts:89 (cron registration) and notify.ts:45 (date rendering tz) | WIRED | — |
| CONS-01 | 21 | runConsolidate orchestrator at consolidate.ts:202; called by cron.ts:104 (live) and backfill-episodic.ts:185 (operator) | WIRED | — |
| CONS-02 | 21 | Entry-count gate at consolidate.ts:230-237 (zero entries → skipped:no-entries; no Sonnet call) | WIRED | — |
| CONS-03 | 21 | Pre-flight SELECT at consolidate.ts:215-227 + ON CONFLICT DO NOTHING at consolidate.ts:295 (UNIQUE summary_date) | WIRED | — |
| CONS-04 | 21 | CONSTITUTIONAL_PREAMBLE exported at personality.ts:29; imported at prompts.ts:27, injected at prompts.ts:117 | WIRED | — |
| CONS-05 | 21 | buildImportanceRubric (4 bands + frequency + chain-of-thought) at prompts.ts:165-186 | WIRED | — |
| CONS-06 | 21 | Floor hint in prompt (prompts.ts:241-247) + runtime clamp at consolidate.ts:258-263 (max 6 if real decision in REAL_DECISION_STATES) | WIRED | — |
| CONS-07 | 21 | Floor hint in prompt (prompts.ts:250-256) + runtime clamp at consolidate.ts:264 (max 7 if any contradiction) | WIRED | — |
| CONS-08 | 21 | getDecisionsForDay (sources.ts:180) read-only via direct Drizzle (no decisions API call); excludes 'open-draft'; consumed at consolidate.ts:241 | WIRED | — |
| CONS-09 | 21 | getContradictionsForDay (sources.ts:135) read-only via direct Drizzle, status='DETECTED' filter; consumed at consolidate.ts:241; verbatim entryA/B preserved at prompts.ts:204-208 | WIRED | — |
| CONS-10 | 21 | buildVerbatimQuoteClause at prompts.ts:188-193 ("verbatim substring … empty array is acceptable") | WIRED | — |
| CONS-11 | 21 | isSparseDay heuristic (prompts.ts:83) + buildSparseEntryGuard (prompts.ts:262-267) emitted only when entries<3 OR totalChars<400 | WIRED | — |
| CONS-12 | 21 | notifyConsolidationError at notify.ts:40; called at consolidate.ts:210 (invalid date) and consolidate.ts:327 (top-level catch) | WIRED | — |
| CRON-01 | 22 | Independent cron.schedule at index.ts:89 — PEER to proactive sweep at line 73, NOT nested; calls runConsolidateYesterday(); double-catch (handler at index.ts:92 + wrapper at cron.ts:103) | WIRED | — |
| CRON-02 | 22 | `{ timezone: config.proactiveTimezone }` at index.ts:95; computeYesterday uses Intl.DateTimeFormat('en-CA') with timeZone option (cron.ts:62-71); DST tests in src/episodic/__tests__/cron.test.ts:133 | WIRED | — |
| RETR-01 | 22 | getEpisodicSummary (retrieve.ts:343) + getEpisodicSummariesRange (retrieve.ts:385); both timezone-aware via formatLocalDate (retrieve.ts:32). Consumed by interrogate.ts:78 (single) and bot/handlers/summary.ts:188 (single). getEpisodicSummariesRange has tests but no production caller (M009 future use). | PARTIALLY WIRED | `getEpisodicSummariesRange` exported and tested but no production consumer yet — documented in 22-01-SUMMARY as M009 substrate. Not a defect for M008 since no requirement consumes the range path; flagged as a forward dependency. |
| RETR-02 | 22 | retrieveContext at routing.ts:129 with recency boundary (≤7d raw, >7d summary); RECENCY_BOUNDARY_DAYS=7 const exported. Tested in routing.test.ts + synthetic-fixture.test.ts TEST-17a/b. | UNWIRED at runtime | **No production caller of `retrieveContext`.** Mode handlers call hybridSearch directly with mode-specific options. The recency-routing decision exists as a tested library but is never invoked by the chat engine. INTERROGATE has its own ad-hoc routing (Flow 3) that calls getEpisodicSummary directly without going through retrieveContext. |
| RETR-03 | 22 | hasVerbatimKeyword at routing.ts:72 with VERBATIM_KEYWORDS (EN/FR/RU); HIGH_IMPORTANCE_THRESHOLD=8 + loadEntriesByIds descent at routing.ts:182-194. Pure regex — no Haiku call (verified by no anthropic import in routing.ts). | UNWIRED at runtime | Same root cause as RETR-02 — `retrieveContext` orphaned. The verbatim fast-path AND the high-importance descent both live inside retrieveContext and are not reachable from any production code path. Tests pass. |
| RETR-04 | 22 | extractQueryDate (date-extraction.ts:226) regex fast-path → `getEpisodicSummary` injection at interrogate.ts:78; "## Recent Episode Context (interpretation, not fact)" D031 marker at interrogate.ts:29. Bypasses retrieveContext intentionally. | WIRED | — |
| RETR-05 | 22 | Boundary audit at chris/__tests__/boundary-audit.test.ts:69-93 enforces zero episodic_summaries refs in personality.ts and ground-truth.ts; Known Facts block (personality.ts:46-59) sources only from GROUND_TRUTH. Verified by direct grep. | WIRED | — |
| RETR-06 | 22 | Boundary audit at chris/__tests__/boundary-audit.test.ts:96-126 enforces zero episodic_summaries refs in embeddings.ts; db.insert(pensieveEmbeddings) bodies asserted free of /episodic/i. Verified by direct grep. | WIRED | — |
| TEST-15 | 23 | 14-day fixture skeleton in src/episodic/__tests__/synthetic-fixture.test.ts (vi.setSystemTime, real Postgres, mocked Anthropic, FIXTURE_TZ='Europe/Paris') | WIRED | — |
| TEST-16 | 23 | Pearson r > 0.7 correlation between Sonnet importance and GROUND_TRUTH_LABELS — labels at synthetic-fixture.test.ts:138 cover bands 1-3/4-6/7-9/10 with both tails | WIRED | — |
| TEST-17 | 23 | Routing assertions at synthetic-fixture.test.ts:608 (4 sub-cases a/b/c/d) — calls `retrieveContext` directly with mocked dependencies. Note: this is the only production call site in the codebase. | WIRED | — |
| TEST-18 | 23 | DST spring-forward fixture at FIXTURE_DATE='2026-03-08' / TZ='America/Los_Angeles' in synthetic-fixture.test.ts; cron-level DST tests in src/episodic/__tests__/cron.test.ts:133 | WIRED | — |
| TEST-19 | 23 | Idempotency assertion: second runConsolidate returns `{ skipped: 'existing' }` (discriminated string, matching consolidate.ts contract). Header note synthetic-fixture.test.ts:24-29 explicitly reconciles plan vs actual shape. | WIRED | — |
| TEST-20 | 23 | Decision-day floor (CONS-06) test in synthetic-fixture.test.ts asserts importance ≥6 when REAL_DECISION_STATES row present | WIRED | — |
| TEST-21 | 23 | Contradiction-day dual-position + CONS-07 floor in synthetic-fixture.test.ts | WIRED | — |
| TEST-22 | 23 | Live anti-flattery test at src/episodic/__tests__/live-anti-flattery.test.ts; ADVERSARIAL_DATE='2026-02-14'; 3-of-3 against real Sonnet; describe.skipIf gate on ANTHROPIC_API_KEY. Calls runConsolidate end-to-end. | WIRED | — |
| OPS-01 | 23 | scripts/backfill-episodic.ts: parseCliArgs + iterateDates + runBackfill; 2-second inter-day delay (line 40); MAX_SPAN_DAYS=365 (line 44); future-date rejection (line 115); reuses CONS-03 idempotency via runConsolidate. | WIRED | — |
| CMD-01 | 23 | bot.command('summary', handleSummaryCommand) at bot.ts:32 (registered before bot.on('message:text') at line 74); handler at bot/handlers/summary.ts:156 calls getEpisodicSummary (RETR-01). Localized EN/FR/RU; D-27/D-30/D-31/D-32 edge cases handled. | WIRED | — |

---

## Detailed Findings

### Orphaned Exports (1)

| Export | From | Reason |
|---|---|---|
| `retrieveContext` (+ `RECENCY_BOUNDARY_DAYS`, `HIGH_IMPORTANCE_THRESHOLD`, `VERBATIM_KEYWORDS`) | `src/pensieve/routing.ts` | Only test files import these. No production engine handler routes through retrieveContext. RETR-02 + RETR-03 logic exists as a tested library but is dead code at runtime. |

**Note:** `getEpisodicSummariesRange` is exported and tested but currently has no production consumer. This is acknowledged in 22-01-SUMMARY as forward substrate for M009 weekly review. Not flagged as orphan because no v2.2 requirement consumes the range path.

### Missing Connections (1)

| Expected | From | To | Reason |
|---|---|---|---|
| `retrieveContext` invocation in mode handlers | `src/pensieve/routing.ts` (P22) | `src/chris/modes/*.ts` (mode handlers) | The PRD intent (RETR-02: "≤7 days raw, >7 summary") is implemented in retrieveContext but no engine path calls it. INTERROGATE has its own ad-hoc routing (date-extraction → getEpisodicSummary) that bypasses the orchestrator. JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE call hybridSearch directly with their `*_SEARCH_OPTIONS` constants and never see episodic summaries. Result: a 30-day-old query in JOURNAL mode never escalates to the summary tier. |

### Broken Flows (0)

None. Six flows traced; one (Flow 2) is DEGRADED but no flow is broken in the sense of "user-facing feature fails".

### Unprotected Routes (0)

`/summary` inherits the single-user `auth` middleware via `bot.use(auth)` at bot.ts:20 before the command registration at bot.ts:32. No new HTTP routes added by M008 — `/health` is the only Express endpoint and intentionally unauthenticated.

### Boundary Violations (0)

Verified by direct grep + the committed boundary-audit test suite: zero `episodic_summaries`/`episodicSummaries` references in personality.ts (KF builder), ground-truth.ts (KF data), or embeddings.ts (vector INSERT path).

### Drizzle Snapshot Lineage (TD-01)

`src/db/migrations/meta/_journal.json` lists 6 contiguous entries idx 0..5 with monotonically-increasing `when` timestamps. All 6 snapshot files (`0000_snapshot.json` … `0005_snapshot.json`) present. No orphan snapshots, no gaps. Migration file `0005_episodic_summaries.sql` (15 lines) creates the table, the UNIQUE/GIN/btree indexes, and the importance CHECK constraint — matches schema.ts:319-340 exactly.

---

## Cross-Phase Connection Matrix

| Producer (Phase) | Export | Consumer (Phase) | Call Site | Status |
|---|---|---|---|---|
| 20 | `episodicSummaries` table | 21 | consolidate.ts (insert), sources.ts (none — different tables) | WIRED |
| 20 | `episodicSummaries` table | 22 | retrieve.ts (select), routing.ts (loadEntriesByIds joins pensieveEntries only) | WIRED |
| 20 | `episodicSummaries` table | 23 | summary.ts (via RETR-01), backfill (via runConsolidate), all tests | WIRED |
| 20 | `parseEpisodicSummary` (Zod) | 21 | consolidate.ts:272 (re-validate before insert) | WIRED |
| 20 | `EpisodicSummarySonnetOutputSchema` | 21 | Used as v3 contract surface; v4 mirror in consolidate.ts:75 for SDK | WIRED |
| 20 | `config.episodicCron` | 22 | index.ts:89 (cron registration) | WIRED |
| 20 | `config.proactiveTimezone` (existing) | 21, 22 | consolidate.ts, sources.ts, retrieve.ts, cron.ts, summary.ts, notify.ts | WIRED |
| 21 | `CONSTITUTIONAL_PREAMBLE` | (already in personality.ts; CONS-04 just verifies export) | prompts.ts:27 imports + 117 injects | WIRED |
| 21 | `runConsolidate` | 22 (cron path) | cron.ts:45 → cron.ts:104 | WIRED |
| 21 | `runConsolidate` | 23 (operator) | scripts/backfill-episodic.ts:37 → 185 | WIRED |
| 21 | `assembleConsolidationPrompt` | 21 (internal) | consolidate.ts:39 → 246 | WIRED |
| 21 | `getPensieveEntriesForDay`/`getContradictionsForDay`/`getDecisionsForDay` | 21 (internal) | consolidate.ts:40-44 → 230, 241 | WIRED |
| 21 | `notifyConsolidationError` | 21 (internal) | consolidate.ts:50 → 210, 327 | WIRED |
| 22 | `runConsolidateYesterday` | 22 (cron registration) | index.ts:11 → 91 | WIRED |
| 22 | `getEpisodicSummary` | 22 (INTERROGATE path) | interrogate.ts:2 → 78 | WIRED |
| 22 | `getEpisodicSummary` | 23 (/summary handler) | summary.ts:35 → 188 | WIRED |
| 22 | `getEpisodicSummary` | 22 (routing internal) | routing.ts:4 → 166 — but routing.ts itself has no production caller | WIRED→ORPHANED |
| 22 | `getEpisodicSummariesRange` | none in M008 scope | — | EXPORTED-ONLY (forward dependency for M009) |
| 22 | `retrieveContext` | none in production | — | ORPHANED at runtime; exercised only by tests |
| 22 | `extractQueryDate` | 22 (INTERROGATE) | interrogate.ts:7 → 69 | WIRED |
| 23 | `handleSummaryCommand` | bot registration | bot.ts:10 → 32 | WIRED |
| 23 | `runBackfill` (programmatic entry) | tests | backfill.test.ts | WIRED |

---

## Recommendations (informational — not part of the integration check)

1. **RETR-02/03 wiring decision required.** Either wire `retrieveContext` into the JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE handlers (one-line replacement of `hybridSearch(query, OPTS)` with `retrieveContext({ query, queryDate: ?, rawLimit: OPTS.limit })` + extraction of `queryDate` from the user message), OR document explicitly that RETR-02/03 are forward-deferred to a follow-up milestone and adjust REQUIREMENTS.md accordingly. The current state — implemented + tested + unreachable — is the worst of both worlds: code is owned and maintained without delivering user-facing value, and the milestone audit will continue to report it as wired.
2. **Optional:** Consider a `retrieveContextForMode(mode, query, queryDate)` adapter that bridges mode-specific `*_SEARCH_OPTIONS` (tags, recencyBias, limit, minScore) with the routing decision. The current options shape (typed retrieval with tags filter) does not map cleanly onto retrieveContext (which only accepts a query/queryDate/rawLimit) — this mismatch may be why the wiring step was skipped.

---

## Appendix — Files Examined

Source files (read in full or relevant sections):
- `/home/claude/chris/src/index.ts`
- `/home/claude/chris/src/episodic/cron.ts`
- `/home/claude/chris/src/episodic/consolidate.ts`
- `/home/claude/chris/src/episodic/prompts.ts`
- `/home/claude/chris/src/episodic/sources.ts`
- `/home/claude/chris/src/episodic/notify.ts`
- `/home/claude/chris/src/episodic/types.ts`
- `/home/claude/chris/src/pensieve/retrieve.ts`
- `/home/claude/chris/src/pensieve/routing.ts`
- `/home/claude/chris/src/pensieve/embeddings.ts`
- `/home/claude/chris/src/pensieve/ground-truth.ts`
- `/home/claude/chris/src/chris/personality.ts`
- `/home/claude/chris/src/chris/modes/interrogate.ts`
- `/home/claude/chris/src/chris/modes/date-extraction.ts`
- `/home/claude/chris/src/chris/__tests__/boundary-audit.test.ts`
- `/home/claude/chris/src/bot/bot.ts`
- `/home/claude/chris/src/bot/handlers/summary.ts`
- `/home/claude/chris/scripts/backfill-episodic.ts`
- `/home/claude/chris/src/db/schema.ts`
- `/home/claude/chris/src/db/migrations/0005_episodic_summaries.sql`
- `/home/claude/chris/src/db/migrations/meta/_journal.json`
- `/home/claude/chris/src/config.ts`

Test files (consulted for wiring confirmation, not run):
- `/home/claude/chris/src/episodic/__tests__/synthetic-fixture.test.ts`
- `/home/claude/chris/src/episodic/__tests__/live-anti-flattery.test.ts`
- `/home/claude/chris/src/episodic/__tests__/schema.test.ts`
- `/home/claude/chris/src/episodic/__tests__/cron.test.ts`
- `/home/claude/chris/src/pensieve/__tests__/routing.test.ts`
- `/home/claude/chris/src/pensieve/__tests__/retrieve.episodic.test.ts`

Greps performed (key results):
- `Grep "episodic_summaries|episodicSummaries"` across `src/` → 18 files, all expected (no boundary leakage into KF/embeddings paths)
- `Grep "CONSTITUTIONAL_PREAMBLE"` → 6 files including prompts.ts (CONS-04 wiring)
- `grep -rn "retrieveContext\b"` outside `__tests__` → 0 production callers (orphan finding)
- `grep -rn "runConsolidate\b"` outside `__tests__` → cron.ts and backfill-episodic.ts (correct Flow 1 + Flow 4)
- `grep -rn "from.*pensieve/retrieve"` outside `__tests__` → 9 files (5 mode handlers, 1 contradiction.ts, interrogate.ts, summary.ts, resolution.ts) — all expected
