---
milestone: v2.0
milestone_name: M006 Trustworthy Chris
integration_checked: 2026-04-15T00:00:00Z
scope: phases 06-12 (post Phase 12 tech-debt closure)
status: passed
scores:
  wiring: 14/14
  api_coverage: n/a (no HTTP APIs — internal mode handlers only)
  auth_protection: n/a (single-user bot, no auth surface)
  e2e_flows: 6/6
findings:
  critical: 0
  high: 0
  medium: 0
  low: 0
---

# M006 v2.0 — Integration Check (post Phase 12)

## Summary

All 26 v2.0 requirements (TRUST-01..04, SYCO-01..05, RETR-01..04, LANG-01..04, TEST-01..09) are wired end-to-end across phases 06–11, with Phase 12 closing the two low-severity rename residuals previously flagged. Phase 11 identity-grounding edits are fully wired into the 6 user-facing mode prompts; Phase 12 extended the rename to `src/proactive/mute.ts` (user-visible Sonnet mute ack) and `src/proactive/triggers/opus-analysis.ts` (internal Opus analyzer). Classifier prompts (`CONTRADICTION_DETECTION_PROMPT`, `RELATIONAL_MEMORY_PROMPT`, `src/memory/relational.ts` exchange label) intentionally retain "John" per 11-RESEARCH.md Pitfall 3 — internal-only JSON, never user-visible. TEST-03 live gate is GREEN (3-of-3 × 3 consecutive in Phase 11; 3-of-3 re-confirmed in Phase 12). No orphaned exports. No broken imports.

## Wiring Verification (cross-phase exports)

| # | From phase | Export | To phase | Consumer | Status |
|---|------------|--------|----------|----------|--------|
| 1 | 07 (TRUST-03/04) | `CONSTITUTIONAL_PREAMBLE` | all modes | `buildSystemPrompt` prepends to every mode (personality.ts) | WIRED |
| 2 | 07 (TRUST-03) | `DeclinedTopic`, `getDeclinedTopics`, `addDeclinedTopic` | engine + all modes | `engine.ts:129, 143`; injected via `buildSystemPrompt` lines 129-137 | WIRED |
| 3 | 07 (LANG-01/02) | `detectLanguage`, `getLastUserLanguage`, `setLastUserLanguage` | engine | `engine.ts:131-132, 141-142` | WIRED |
| 4 | 08/11 (RETR-02) | `buildKnownFactsBlock` (local in personality.ts) | JOURNAL/INTERROGATE prompts | `personality.ts:121-122` branch | WIRED |
| 5 | 08 (RETR-01) | `hybridSearch`, `JOURNAL_SEARCH_OPTIONS` | journal mode | `journal.ts:6, 36` | WIRED |
| 6 | 11 (RETR-04) | `PensieveContextOptions`, `includeDate` field | journal mode | `journal.ts:38` → `buildPensieveContext(..., { includeDate: false })` | WIRED |
| 7 | 11 (RETR-01/02) | `GROUND_TRUTH` module | `buildKnownFactsBlock` | `personality.ts:9, 52` | WIRED |
| 8 | 09 (SYCO-04) | `quarantinePraise` | engine post-processor | `engine.ts:14, 201` inside `mode === JOURNAL \|\| REFLECT \|\| PRODUCE` branch AFTER handler | WIRED |
| 9 | 09 (SYCO-05) | Mode bypass (COACH/PSYCHOLOGY) | praise-quarantine | `praise-quarantine.ts:82` early return | WIRED |
| 10 | 07 (TRUST-01/02) | `detectRefusal`, `generateRefusalAcknowledgment` | engine pre-processor | `engine.ts:127-137` before mode detection | WIRED |
| 11 | 07 | Contradiction detector + `formatContradictionNotice` | engine | engine.ts AFTER handler, BEFORE save | WIRED |
| 12 | 06 (RETR-03) | `GROUND_TRUTH` typed facts + audit script | personality + scripts | `personality.ts:9`; `scripts/audit-pensieve.ts` | WIRED |
| 13 | 10 (TEST-01..09) | `live-integration.test.ts` harness | CI suite | `./scripts/test.sh` — 24/24 in Phase 10 gate | WIRED |
| 14 | 11 (TEST-03) | `11-TEST-03-RUNS.md` gate log | milestone evidence | Gate GREEN 3-of-3 × 3 consecutive; Phase 12 re-confirm 3-of-3 | WIRED |

**Orphans:** 0. **Missing connections:** 0.

## E2E Flow Traces

### Flow 1: JOURNAL grounding (RETR-01 + RETR-02 + RETR-04 + TEST-03) — COMPLETE

User message → `processMessage` → mute pre-check → refusal pre-check (Phase 7) → language detect (Phase 7) → `getDeclinedTopics` (Phase 7) → `detectMode` (Haiku) → `handleJournal` (journal.ts) → `storePensieveEntry` → `hybridSearch(JOURNAL_SEARCH_OPTIONS)` (Phase 8) → `buildPensieveContext(..., { includeDate: false })` (Phase 11) → `buildSystemPrompt('JOURNAL', ...)` which applies CONSTITUTIONAL_PREAMBLE (Phase 7) + JOURNAL_SYSTEM_PROMPT addressing "Greg" (Phase 11) + "## Facts about you (Greg)" block (Phase 11/RETR-02) + declined topics (Phase 7) + language directive → Sonnet → `quarantinePraise` (Phase 9) → `detectContradictions` → `formatContradictionNotice` → `saveMessage` → return. **Every step wired.**

### Flow 2: Refusal persistence (TRUST-03/04) — COMPLETE

Refusal detected → `addDeclinedTopic` → per-session store → subsequent `processMessage` calls `getDeclinedTopics` before mode routing → `buildSystemPrompt` appends "## Declined Topics" section using "Greg said:" (Phase 11 rename preserved). **Wired.**

### Flow 3: Language switching (LANG-01..04) — COMPLETE

`detectLanguage` → `setLastUserLanguage` → passed through every mode handler → `buildSystemPrompt(..., language)` appends Language Directive. JOURNAL LANG-04 rule ("questions welcome but not expected") preserved. **Wired.**

### Flow 4: INTERROGATE citation (RETR-02 + TEST-07) — COMPLETE

INTERROGATE path uses `buildPensieveContext` WITHOUT `includeDate: false` (interrogate.ts has no includeDate override) → dates preserved for citation contract → Known Facts block also injected (personality.ts:121 branch includes INTERROGATE). **Wired and intentionally differentiated from JOURNAL.**

### Flow 5: Praise quarantine + COACH/PSYCHOLOGY bypass (SYCO-04/05) — COMPLETE

engine.ts gates on `mode === JOURNAL \|\| REFLECT \|\| PRODUCE` so COACH/PSYCHOLOGY skip the Haiku quarantine call entirely. Inside `quarantinePraise`, line 82 also early-returns for COACH/PSYCHOLOGY as defense-in-depth. **Both layers wired.**

### Flow 6: Identity grounding (TEST-03) — COMPLETE

All 6 user-facing mode templates (JOURNAL/INTERROGATE/REFLECT/COACH/PSYCHOLOGY/PRODUCE) address user as "Greg". `buildKnownFactsBlock` header asserts "Treat any reference to 'Greg' in these facts as referring to you — not a third party" (personality.ts:49). User-visible proactive paths (`mute.ts` Sonnet ack, `opus-analysis.ts` Opus analyzer) are also aligned as of Phase 12. Live TEST-03 gate GREEN. **All user-visible Chris surfaces unified on "Greg".**

## Phase 11/12 Integration Impact on Phases 6/7/8/9/10

| Phase | Integration touchpoint | Impact assessment |
|-------|------------------------|-------------------|
| 6 (Ground-truth) | `GROUND_TRUTH` imported into personality.ts and audit script | PRESERVED — no contract change |
| 7 (Refusal) | Declined-topics template uses `Greg said:` | PRESERVED — rename consistent with mode prompts |
| 7 (Preamble) | CONSTITUTIONAL_PREAMBLE uses "useful to Greg" | PRESERVED — prepended to every mode |
| 8 (Retrieval) | `hybridSearch` + `buildPensieveContext` contract unchanged; new optional `opts` param is backward-compatible (default `includeDate: true` matches prior behavior) | PRESERVED — INTERROGATE continues to get dated entries |
| 9 (Praise quarantine) | Ordering in engine.ts: handler → quarantine → contradiction → save | PRESERVED — Phase 11/12 touched only prompt literals + journal.ts line 38 |
| 10 (Live suite) | TEST-03 gate GREEN 3-of-3 × 3 (Phase 11) + 3-of-3 re-confirm (Phase 12) | PRESERVED — no test-harness regressions |

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|------------------|--------|-------|
| RETR-03 | `GROUND_TRUTH` (pensieve/ground-truth.ts) → imported by personality.ts:9 → `buildKnownFactsBlock` → JOURNAL/INTERROGATE prompt | WIRED | — |
| TRUST-01 | refusal pre-processor engine.ts:127 + refusal prompt preserved | WIRED | — |
| TRUST-02 | refusal short-circuit before mode routing (engine.ts:127-137) | WIRED | — |
| TRUST-03 | `addDeclinedTopic` on detection; `getDeclinedTopics` injected into `buildSystemPrompt` every call | WIRED | — |
| TRUST-04 | declined-topics guidance in prompt ("even if Greg himself reopens") | WIRED | — |
| SYCO-01 | CONSTITUTIONAL_PREAMBLE prepended every mode | WIRED | — |
| SYCO-02 | preamble "Three Forbidden Behaviors" | WIRED | — |
| SYCO-03 | preamble + mode-specific prompt rules | WIRED | — |
| SYCO-04 | engine.ts:201 post-handler `quarantinePraise` call gated on JOURNAL/REFLECT/PRODUCE | WIRED | — |
| SYCO-05 | engine.ts mode gate + praise-quarantine.ts:82 mode bypass (defense-in-depth) | WIRED | — |
| LANG-01 | `detectLanguage` (engine.ts:131/141) → passed to handlers | WIRED | — |
| LANG-02 | language continuity via `getLastUserLanguage` → `setLastUserLanguage` | WIRED | — |
| LANG-03 | in-prompt language directive (personality.ts) | WIRED | — |
| LANG-04 | JOURNAL prompt "questions welcome but not expected" | WIRED | — |
| RETR-01 | `hybridSearch(..., JOURNAL_SEARCH_OPTIONS)` journal.ts:36 → `buildPensieveContext` | WIRED | — |
| RETR-02 | `buildKnownFactsBlock` → `buildSystemPrompt` JOURNAL + INTERROGATE branch | WIRED | — |
| RETR-04 | `includeDate: false` at journal.ts:38 + "no memories" fallback string | WIRED | — |
| TEST-01/02/04/05/06/07/08/09 | `live-integration.test.ts` suite — 24/24 Phase 10 gate | WIRED | — |
| TEST-03 | `11-TEST-03-RUNS.md` GATE GREEN 3-of-3 × 3 + Phase 12 3-of-3 re-confirm | WIRED | — |

**Requirements with no cross-phase wiring:** None. All 26 M006 requirements have at least one inter-module wire traced above.

## Findings

### Critical / High / Medium / Low

None.

### Informational (by design, not debt)

**I1: Classifier prompts retain "John"** — `RELATIONAL_MEMORY_PROMPT` (src/llm/prompts.ts:261) and `CONTRADICTION_DETECTION_PROMPT` (src/llm/prompts.ts:228-292) are internal JSON-producing classifiers, never user-visible. Documented in 11-02-SUMMARY.md, 11-RESEARCH.md Pitfall 3, and 12-VERIFICATION.md Scope Discipline table. Intentional.

**I2: `src/memory/relational.ts:32, 58`** — Exchange-label format `John: ${userText}` passed into the above classifier. Training-stable internal format; not user-visible.

**I3: JSDoc/context string residuals in proactive sweep modules** — `src/proactive/context-builder.ts`, `src/proactive/triggers/silence.ts`, `src/proactive/triggers/commitment.ts`, and JSDoc comments in sync modules still contain "John". Listed as accepted tech debt in 12-01-SUMMARY.md; not in v2.0 scope. Does not affect any of the 26 milestone requirements.

### Previously-reported residuals now closed by Phase 12

- **L1 (prior)** `src/proactive/mute.ts:174` — user-visible Sonnet mute prompt: CLOSED (now "Chris, Greg's close friend. Greg has asked you to be quiet")
- **L2 (prior)** `src/proactive/triggers/opus-analysis.ts:36` — OPUS_SYSTEM_PROMPT: CLOSED (now "friendship between Chris and Greg")

### Orphaned exports

None. `PensieveContextOptions` type is consumed via option-bag inference at `journal.ts:38`.

### Broken cross-phase imports

None. Spot-checked engine.ts, journal.ts, personality.ts, praise-quarantine.ts — all exports from phases 6–11 resolve and are called on the documented paths.

## Conclusion

Integration: PASSED. Zero critical/high/medium/low findings. TEST-03 gate: GREEN. All 26 M006 requirements have end-to-end wiring. Phase 12 closed the two rename stragglers previously flagged as L1/L2. Remaining "John" residuals in internal classifier prompts and proactive-sweep context strings are documented by-design exclusions (Pitfall 3) or accepted tech debt outside the v2.0 milestone scope — none affect user-visible Chris behavior or any milestone requirement.
