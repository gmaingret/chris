---
phase: 22.1-wire-retrieve-context
fixed_at: 2026-04-18T00:00:00Z
review_path: .planning/phases/22.1-wire-retrieve-context/22.1-REVIEW.md
iteration: 1
findings_in_scope: 1
fixed: 2
skipped: 4
status: all_fixed
---

# Phase 22.1: Code Review Fix Report

**Fixed at:** 2026-04-18
**Source review:** `.planning/phases/22.1-wire-retrieve-context/22.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- In-scope findings (warnings): 1
- Fixed: 2 (WR-01 + IN-04 trivial comment)
- Skipped: 4 info (non-trivial or stylistic)

## Fixed Issues

### WR-01: No direct unit tests for `summaryToSearchResult` or `hybridOptions` passthrough

**Files modified:** `src/pensieve/__tests__/routing.test.ts`
**Commit:** `658febc`
**Applied fix:** Added two new describe blocks at the end of routing.test.ts:
- `describe('summaryToSearchResult')` — 6 tests anchoring the inline header format, score=1.0 sentinel, topics=none fallback, `episodic-` prefix id namespace, UTC-midnight `createdAt` derivation, and `source='episodic-summary'` marker.
- `describe('retrieveContext hybridOptions passthrough')` — 6 tests covering all 3 hybridSearch-invoking branches (verbatim / recent / no-summary-fallback), the `hybridOptions.limit ?? rawLimit` precedence rule, rawLimit fallback when hybridOptions.limit is absent, and the default rawLimit=10 when neither is set.

Also imported `summaryToSearchResult` from routing.ts.

Verified: 34 routing tests pass (22 existing + 12 new). 105 mode tests still pass unchanged.

### IN-04: Clarify `includeDate: false` scope in journal.ts

**Files modified:** `src/chris/modes/journal.ts`
**Commit:** `177c012`
**Applied fix:** Added an NOTE comment to the `buildPensieveContext(...)` call in `journal.ts` explaining that `{ includeDate: false }` suppresses only the builder's citation date prefix on RAW entries — synthetic episodic-summary entries carry their episode date INSIDE content, which is semantically meaningful and intended to be visible. Prevents future-reader confusion; no behavior change. Reviewer explicitly recommended a comment over a code change.

## Skipped Issues

### IN-01: Redundant `rawLimit` argument at 5 mode handler call sites

**Reason:** Reviewer: "not a bug... creates two minor hazards". Dropping `rawLimit` is a behavior change across 5 handlers — the current redundancy keeps call-site intent explicit. Out of scope for warning-only pass.

### IN-02: Inconsistent `vi.mock` factory reference pattern between test files

**Reason:** Stylistic consistency nit — 5 files to update, no functional change. Reviewer: "Both patterns work." Out of scope.

### IN-03: `chris.<mode>.routing` log omits `citationCount`

**Reason:** New log field across 5 handlers — additive change, but still a behavior change to the structured log schema. Reviewer: "minor observability noise, not a bug." Out of scope.

### IN-05: Mode-handler docstring drift across 5 copies

**Reason:** Updating 5 docstrings in 5 files is not a "trivial one-line" fix. Reviewer frames this as a drift hazard inherent to the duplicate-by-design pattern — a refactor, not a bug fix. Out of scope.

---

_Fixed: 2026-04-18_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
