---
phase: 28-skip-tracking-adjustment-dialogue
plan: 04
type: execute
wave: 4
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - src/rituals/skip-tracking.ts
  - src/rituals/adjustment-dialogue.ts
  - src/rituals/scheduler.ts
  - src/rituals/__tests__/refusal-pre-check.integration.test.ts
  - src/rituals/__tests__/self-protective-pause.test.ts
autonomous: true
requirements: [SKIP-06, SKIP-07]
tags: [rituals, skip-tracking, refusal, self-protective-pause, audit-trail, m006-refusal, m009]

must_haves:
  truths:
    - "handleAdjustmentReply runs detectRefusal(text) BEFORE any anthropic.messages.parse call — refusals NEVER reach Haiku, NEVER count as evasive (load-bearing for SKIP-06 invariant per RESEARCH Pitfall 2)"
    - "Refusal patterns (drop it / disable / not now / drop / pause) route to manual_disable (drop it / disable) OR adjustment_mute_until 7-day deferral (not now) — distinguished by phrase classifier"
    - "hasReachedEvasiveTrigger(ritualId) queries ritual_responses for metadata.kind='adjustment_dialogue_response' AND metadata.classification='evasive' within 14-day window; returns true on count >= 2"
    - "On 2 evasive responses within 14 days: rituals.enabled = false AND rituals.config.mute_until = now + 30 days (atomic update); ritual_config_events row written with patch.kind='auto_pause'"
    - "autoReEnableExpiredMutes runs at top of runRitualSweep BEFORE the channel-cap check; for each ritual where enabled=false AND config.mute_until <= now: set enabled=true, clear mute_until, write ritual_config_events patch.kind='auto_re_enable'"
    - "M006 refusal-disable does NOT auto-re-enable (manual_disable is permanent until Greg manually re-enables); only the EVASIVE-triggered 30-day pause auto-re-enables"
    - "ritual_config_events writes use the discriminated envelope inside `patch` jsonb per RESEARCH Landmine 1 — actor varchar(32) ∈ {'system'|'user'|'auto_apply_on_timeout'|'adjustment_dialogue_refusal'}; patch.kind ∈ {'apply'|'abort'|'auto_pause'|'auto_re_enable'|'manual_disable'}"
    - "Cumulative mockAnthropicParse.not.toHaveBeenCalled() integration test proves refusal pre-check short-circuits BEFORE the Haiku call (load-bearing SKIP-06 contract)"
  artifacts:
    - path: "src/rituals/skip-tracking.ts"
      provides: "extended with hasReachedEvasiveTrigger predicate + autoReEnableExpiredMutes helper"
      contains: "hasReachedEvasiveTrigger"
    - path: "src/rituals/adjustment-dialogue.ts"
      provides: "extended handleAdjustmentReply with detectRefusal pre-check BEFORE any Haiku call + routeRefusal helper + extended evasive-classification path with hasReachedEvasiveTrigger check"
      contains: "detectRefusal"
    - path: "src/rituals/scheduler.ts"
      provides: "autoReEnableExpiredMutes invocation at top of runRitualSweep BEFORE STEP 0 channel-cap check"
      contains: "autoReEnableExpiredMutes"
    - path: "src/rituals/__tests__/refusal-pre-check.integration.test.ts"
      provides: "real-DB integration test asserting cumulative mockAnthropicParse.not.toHaveBeenCalled() for refusal-text replies + ritual_config_events writes + ritual NOT auto-paused via evasive trigger"
      min_lines: 100
    - path: "src/rituals/__tests__/self-protective-pause.test.ts"
      provides: "real-DB integration test for hasReachedEvasiveTrigger + 2-evasive-in-14d → 30-day pause + auto-re-enable on mute_until expiry + manual_disable does NOT auto-re-enable"
      min_lines: 120
  key_links:
    - from: "src/rituals/adjustment-dialogue.ts handleAdjustmentReply"
      to: "src/chris/refusal.ts detectRefusal"
      via: "called BEFORE anthropic.messages.parse — refusal short-circuits to routeRefusal helper"
      pattern: "detectRefusal\\("
    - from: "src/rituals/scheduler.ts runRitualSweep"
      to: "src/rituals/skip-tracking.ts autoReEnableExpiredMutes"
      via: "called BEFORE the ritualResponseWindowSweep call from Plan 28-01 — auto-re-enable runs first so re-enabled rituals participate in this tick's normal flow"
      pattern: "autoReEnableExpiredMutes\\("
    - from: "src/rituals/adjustment-dialogue.ts evasive branch"
      to: "src/rituals/skip-tracking.ts hasReachedEvasiveTrigger"
      via: "after writing the evasive-classification ritual_responses row, call hasReachedEvasiveTrigger(ritualId); on true, apply the 30-day auto-pause atomically"
      pattern: "hasReachedEvasiveTrigger\\("
---

<objective>
Phase 28 Wave 4 closing plan. Add the M006 refusal pre-check BEFORE any Haiku call in handleAdjustmentReply (load-bearing for SKIP-06 — refusals must never count as evasive). Add the self-protective 30-day pause on 2 evasive responses within 14 days, plus auto-re-enable when mute_until expires. Add ritual_config_events writes for every config mutation (using the envelope-in-patch shape per RESEARCH Landmine 1).

Per RESEARCH Pitfall 2: this plan's refusal pre-check is the load-bearing invariant for SKIP-06. The integration test in src/rituals/__tests__/refusal-pre-check.integration.test.ts asserts cumulative mockAnthropicParse.not.toHaveBeenCalled() across all refusal-text test cases — proving Haiku is never reached for refusal inputs.

Purpose: SKIP-06 (self-protective 30-day pause after 2 evasive in 14d + auto-re-enable) + SKIP-07 (M006 refusal handling honored INSIDE the dialogue + ritual_config_events audit trail).

Output: Extended skip-tracking.ts (hasReachedEvasiveTrigger + autoReEnableExpiredMutes); extended adjustment-dialogue.ts handleAdjustmentReply with refusal pre-check + evasive-trigger check; scheduler.ts autoReEnableExpiredMutes invocation; 2 integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-VALIDATION.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-01-SUMMARY.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-02-SUMMARY.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-03-SUMMARY.md
@src/chris/refusal.ts
@src/rituals/types.ts
@src/rituals/skip-tracking.ts
@src/rituals/adjustment-dialogue.ts
@src/rituals/scheduler.ts
@src/db/schema.ts

<interfaces>
From src/chris/refusal.ts:131-156 (detectRefusal — read-only reuse):
  detectRefusal(text: string): RefusalResult
  RefusalResult = { isRefusal: false } | { isRefusal: true; topic: string; originalSentence: string }
  Patterns cover EN+FR+RU; 'drop it' + 'not now' + 'leave it' + 'stop' (variant phrasing) all match. The detector returns isRefusal: true on FIRST matching pattern; topic is extracted from capture group when present, else originalSentence.

  Coverage status (per RESEARCH §"Specific Ideas verified at refusal.ts:24-55"):
    - 'drop it' (EN_PATTERNS line 34) ✓
    - 'not now' (line 48 — standalone-only regex) ✓
    - 'stop' covered via 'arrête' / 'прекрати' etc. (FR/RU lines 71/108)
    - 'disable' is NOT in EN_PATTERNS — Plan 28-04 adds adjustment-specific extension INSIDE adjustment-dialogue.ts (preferred per PATTERNS.md §C — keeps general refusal detector unchanged for non-adjustment contexts where 'disable' might mean something else)

From src/rituals/skip-tracking.ts (post Plan 28-02):
  Exports: cadenceDefaultThreshold, computeSkipCount, shouldFireAdjustmentDialogue
  Plan 28-04 ADDS: hasReachedEvasiveTrigger, autoReEnableExpiredMutes

From src/rituals/adjustment-dialogue.ts (post Plan 28-03):
  Exports: fireAdjustmentDialogue, handleAdjustmentReply, handleConfirmationReply, confirmConfigPatch, ritualConfirmationSweep
  Plan 28-04 EXTENDS handleAdjustmentReply with:
    Step 1.5: detectRefusal pre-check BEFORE the existing classification step
    Step 5 evasive branch: after writing the evasive-classification ritual_responses row, call hasReachedEvasiveTrigger and apply 30-day pause if true

From src/db/schema.ts:471-477 ritualConfigEvents (RESEARCH Landmine 1 — discriminated envelope inside patch jsonb):
  Columns: id, ritualId, actor varchar(32) NOT NULL, patch jsonb NOT NULL, createdAt
  Plan 28-04 actor values: 'adjustment_dialogue_refusal' (27 chars — fits) | 'system' (auto_pause / auto_re_enable)
  Plan 28-04 patch.kind values: 'manual_disable' (refusal "drop it"/"disable") | 'auto_pause' (2-evasive-in-14d) | 'auto_re_enable' (mute_until expired)

From CONTEXT.md D-28-08 routing:
  - "drop it" / "disable" → UPDATE rituals SET enabled = false (paused indefinitely; manual re-enable only). ritual_config_events with patch.kind='manual_disable' + source='adjustment_dialogue_refusal'.
  - "not now" → defer adjustment dialogue 7 days. Set rituals.config.adjustment_mute_until = now + 7 days (Plan 28-01 already added this field to RitualConfigSchema). Skip-counting continues; the dialogue just won't fire for 7 days.
  - **Refusals do NOT count as evasive.**

From CONTEXT.md D-28-07:
  - 2 evasive responses within 14 days → rituals.enabled = false + rituals.config.mute_until = now + 30 days
  - Auto-re-enable when mute_until expires
  - Manual override always allowed (no Chris-side block)

From src/rituals/scheduler.ts post Plan 28-03 (lines 82-93 — runRitualSweep entry):
  Order:
    1. logger.info('rituals.sweep.start')
    2. await ritualResponseWindowSweep(now)  ← Plan 28-01
    3. STEP 0: channel-cap check
    4. ... existing flow with Plan 28-02 + Plan 28-03 additions

  Plan 28-04 INSERTS autoReEnableExpiredMutes BEFORE step 2 (the response-window sweep) — re-enabled rituals must participate in THIS tick's flow, not wait for the next tick.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend skip-tracking.ts with hasReachedEvasiveTrigger + autoReEnableExpiredMutes helpers</name>
  <files>src/rituals/skip-tracking.ts, src/rituals/__tests__/self-protective-pause.test.ts</files>
  <read_first>
    - src/rituals/skip-tracking.ts (post Plan 28-02 — read in full to understand existing exports + JSDoc style)
    - src/db/schema.ts:432-450 (ritualResponses shape — metadata jsonb is where evasive markers live)
    - src/db/schema.ts:367-389 (rituals shape — enabled boolean + config jsonb where mute_until lives)
    - src/db/schema.ts:471-477 (ritualConfigEvents shape — actor varchar(32) + patch jsonb per RESEARCH Landmine 1)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/skip-tracking.ts" §hasReachedEvasiveTrigger (lines 239-249)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md §SC-3 (lines 477-481) — the falsifiable test plan
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md D-28-07 + D-28-08
  </read_first>
  <behavior>
    Self-protective-pause.test.ts:
    - Test 1 (hasReachedEvasiveTrigger, 0 evasive): seed ritual; insert 0 ritual_responses rows. hasReachedEvasiveTrigger returns false.
    - Test 2 (1 evasive within 14d): seed 1 ritual_responses row with metadata.kind='adjustment_dialogue_response' AND metadata.classification='evasive', created_at = now-7d. Returns false (need >= 2).
    - Test 3 (2 evasive within 14d): seed 2 such rows at now-7d and now-1d. Returns TRUE.
    - Test 4 (2 evasive but >14d apart): seed 2 such rows at now-20d and now-1d. Returns false (rolling window starts now-14d; the older row is outside).
    - Test 5 (autoReEnableExpiredMutes, no expired): seed ritual with enabled=false, config.mute_until = now+30d. autoReEnableExpiredMutes returns 0 (no work).
    - Test 6 (autoReEnableExpiredMutes, expired): seed ritual with enabled=false, config.mute_until = now-1m. autoReEnableExpiredMutes returns 1; ritual.enabled flips to true; config.mute_until cleared (set to null); ritual_config_events row written with actor='system' + patch.kind='auto_re_enable'.
    - Test 7 (autoReEnableExpiredMutes, manual_disable does NOT auto-re-enable): seed ritual with enabled=false but config.mute_until = NULL (manual_disable case — Plan 28-04 Task 2 sets enabled=false WITHOUT setting mute_until). autoReEnableExpiredMutes returns 0; ritual stays disabled.
    - Test 8 (idempotency under repeat): with one expired mute, call autoReEnableExpiredMutes twice. Second call returns 0 (ritual already enabled=true, mute_until=null); no second ritual_config_events row written.
  </behavior>
  <action>
    Edit src/rituals/skip-tracking.ts:

    (a) Extend imports:
       - drizzle-orm: ADD `gte` (already there from Plan 28-02) and `isNotNull` to existing imports
       - schema: ADD `ritualResponses` and `ritualConfigEvents` to existing { rituals, ritualFireEvents } imports
       - logger: import { logger } from '../utils/logger.js' (NEW import — Plan 28-02 likely didn't need it, but Plan 28-04 logs auto-re-enable events)

    (b) Add new export hasReachedEvasiveTrigger(ritualId): Promise<boolean>:

       JSDoc: "Per CONTEXT.md D-28-07: returns true if the ritual has accumulated >= 2 evasive responses within a rolling 14-day window. Caller (handleAdjustmentReply evasive branch in adjustment-dialogue.ts) applies the 30-day pause on true return. Refusals are NOT in this query — Plan 28-04 routes refusals to ritual_config_events.manual_disable WITHOUT writing to ritual_responses, so they cannot accidentally trigger the evasive count."

       Body:
       1. const fourteenDaysAgo = new Date(Date.now() - 14 * 24 * 3600 * 1000)
       2. const result = await db
            .select({ count: sql<number>\`COUNT(*)::int\` })
            .from(ritualResponses)
            .where(
              and(
                eq(ritualResponses.ritualId, ritualId),
                sql\`\${ritualResponses.metadata}->>'kind' = 'adjustment_dialogue_response'\`,
                sql\`\${ritualResponses.metadata}->>'classification' = 'evasive'\`,
                gte(ritualResponses.createdAt, fourteenDaysAgo),
              ),
            )
       3. const count = result[0]?.count ?? 0; return count >= 2;

    (c) Add new export autoReEnableExpiredMutes(now: Date = new Date()): Promise<number>:

       JSDoc: "Per CONTEXT.md D-28-07: scans rituals where enabled=false AND config.mute_until is set AND mute_until <= now. For each: set enabled=true, clear mute_until, write ritual_config_events with actor='system' and patch.kind='auto_re_enable'. Returns the count of re-enabled rituals.

       Critical: only operates on rituals with config.mute_until SET (not null). Manual_disable refusals (D-28-08 'drop it'/'disable' branch) set enabled=false WITHOUT setting mute_until, so they are NOT auto-re-enabled — they require manual operator intervention. This separation is the load-bearing invariant for SKIP-06's 'manual override always allowed but no auto-re-enable for refusals'."

       Body:
       1. SELECT id, config FROM rituals WHERE enabled = false AND (config->>'mute_until')::timestamptz <= now (use sql template literal for the expression — drizzle eq() doesn't support jsonb path-then-cast). Filter out rows where config.mute_until is null (the SQL cast `(config->>'mute_until')::timestamptz` returns NULL for absent/null mute_until, and the <= now comparison is NULL → row excluded).
       2. If empty, return 0.
       3. For each row: in sequence (NOT a transaction — each ritual is independent; if one fails, others still process):
          a. UPDATE rituals SET enabled = true, config = jsonb_set(config, '{mute_until}', 'null'::jsonb) WHERE id = row.id (use jsonb_set with the literal 'null'::jsonb to clear the field; postgres-js jsonb-binding works for the bare 'null'::jsonb literal).
          b. INSERT INTO ritualConfigEvents { ritualId: row.id, actor: 'system', patch: { kind: 'auto_re_enable', source: 'mute_until_expired', mute_until_was: row.config?.mute_until } } (RESEARCH Landmine 1 envelope-in-patch shape).
          c. logger.info({ ritualId: row.id }, 'rituals.auto_re_enable.applied').
       4. Return processedCount.

    (d) Update file header JSDoc to mention the two new exports:
       "Phase 28 Plan 04 (SKIP-06 + SKIP-07) — extends Plan 28-02's predicate module with self-protective-pause + audit-trail helpers."

    Create src/rituals/__tests__/self-protective-pause.test.ts:
       - Mirror src/rituals/__tests__/should-fire-adjustment.test.ts structure
       - vi.hoisted mocks for mockSendMessage (handlers may invoke bot)
       - Use the seedRitualWithFireEvents fixture from Plan 28-01 for setup, OR write inline ritual + ritualResponses inserts (whichever is cleaner)
       - Top-level describe: 'Phase 28 Plan 04 SKIP-06 — self-protective pause'
       - Implement Tests 1-8 from the behavior list above
       - For Test 6: assert ritual_config_events row exists by selecting WHERE ritualId = $1 ORDER BY createdAt DESC LIMIT 1; assert row.actor = 'system' AND row.patch (cast as object) has kind = 'auto_re_enable'
       - For Test 7 (manual_disable does NOT auto-re-enable): seed ritual with enabled=false AND config WITHOUT mute_until field (or with mute_until=null); call autoReEnableExpiredMutes; assert returned count is 0 AND the ritual stays enabled=false (no UPDATE was applied)
       - afterAll: cleanup; cumulative mockAnthropicCreate.not.toHaveBeenCalled() (defensive — these helpers should never reach Anthropic)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "skip-tracking|self-protective" | head -10 && bash scripts/test.sh src/rituals/__tests__/self-protective-pause.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to skip-tracking.ts or self-protective-pause.test.ts
    - bash scripts/test.sh src/rituals/__tests__/self-protective-pause.test.ts reports all 8 tests passing
    - File src/rituals/skip-tracking.ts now exports 5 functions (Plan 28-02's 3 + Plan 28-04's 2): grep -E "^export (async )?function" src/rituals/skip-tracking.ts | wc -l reports 5
    - grep -c "hasReachedEvasiveTrigger" src/rituals/skip-tracking.ts >= 2 (1 export + 1 JSDoc reference)
    - grep -c "autoReEnableExpiredMutes" src/rituals/skip-tracking.ts >= 2
    - grep -c "ritualConfigEvents" src/rituals/skip-tracking.ts >= 2 (1 import + 1 insert in autoReEnableExpiredMutes)
    - grep -E "actor: 'system'" src/rituals/skip-tracking.ts | wc -l >= 1 (the auto_re_enable insert)
    - grep -E "patch: \{ kind: 'auto_re_enable'" src/rituals/skip-tracking.ts | wc -l >= 1
  </acceptance_criteria>
  <done>
    skip-tracking.ts now has the full SKIP-06 read+write surface. Two new exports + JSDoc citing D-28-07 + RESEARCH Landmine 1. Tests cover the 8 boundary cases including the manual_disable separation invariant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend adjustment-dialogue.ts with detectRefusal pre-check + routeRefusal helper + evasive-trigger handling</name>
  <files>src/rituals/adjustment-dialogue.ts, src/rituals/__tests__/refusal-pre-check.integration.test.ts</files>
  <read_first>
    - src/rituals/adjustment-dialogue.ts (post Plan 28-03 — read in full; understand the existing handleAdjustmentReply structure with its STEP 1 atomic-consume + STEP 2 classify + STEP 4 branch dispatch)
    - src/chris/refusal.ts:1-156 in full (detectRefusal patterns + RefusalResult shape)
    - src/rituals/skip-tracking.ts (post Task 1 — hasReachedEvasiveTrigger newly exported)
    - src/db/schema.ts:471-477 (ritualConfigEvents shape — RESEARCH Landmine 1)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/adjustment-dialogue.ts" §C (lines 424-466) + §F (lines 558-588)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Pitfall 2 (lines 270-275) — load-bearing for SKIP-06 invariant
    - src/chris/__tests__/engine-pp5.test.ts (cumulative not-called pattern — analog for the new test file)
  </read_first>
  <behavior>
    Refusal-pre-check.integration.test.ts:
    - Test 1 ("drop it" → manual_disable): Insert pending row metadata.kind='adjustment_dialogue'. Inject text "drop it". Assert: rituals.enabled flipped to false; ritual_config_events row with actor='adjustment_dialogue_refusal' + patch.kind='manual_disable'. mockAnthropicParse.mock.calls.length === 0 (Haiku NOT reached).
    - Test 2 ("disable" → manual_disable): Inject text "disable this ritual". Assert: same as Test 1. (Plan 28-04 adds 'disable' to the adjustment-specific extension since detectRefusal does NOT cover it — see action step (a)).
    - Test 3 ("not now" → 7-day adjustment_mute_until): Inject text "not now please". Assert: rituals.enabled UNCHANGED (still true); rituals.config.adjustment_mute_until = approximately now + 7 days; ritual_config_events row with actor='adjustment_dialogue_refusal' + patch.kind='apply' + patch.field='adjustment_mute_until'. mockAnthropicParse NOT called.
    - Test 4 (refusal does NOT count as evasive): After Test 1's "drop it" reply, run hasReachedEvasiveTrigger(ritualId) and assert it returns false (refusal did NOT write a metadata.classification='evasive' row to ritual_responses). Send a SECOND "drop it" reply 7 days later (vi.setSystemTime); assert: ritual STILL enabled=false AND ritual NOT auto-paused via evasive trigger (no extra ritual_config_events row with patch.kind='auto_pause').
    - Test 5 (non-refusal text reaches Haiku as before): Inject text "change to 19:30" (clearly a change_requested intent). Assert: detectRefusal returned isRefusal: false; mockAnthropicParse.mock.calls.length === 1 (Haiku WAS reached); standard change_requested flow proceeds (confirmation pending row created).
    - Test 6 (evasive-trigger applies 30-day pause): Mock Haiku to return classification='evasive' twice (within 14 days). Inject 2 text replies that don't match refusal patterns (so detectRefusal returns isRefusal: false and Haiku IS reached). After the 2nd reply: assert rituals.enabled=false, rituals.config.mute_until = approximately now + 30 days, ritual_config_events row with actor='system' + patch.kind='auto_pause'.
    - Cumulative afterAll for the refusal-tests describe: mockAnthropicParse.not.toHaveBeenCalled() — but ONLY for the refusal test cases (Test 5 + Test 6 explicitly do call Haiku, so Tests 1-4 are in a separate describe block with its own afterAll cumulative assertion).
  </behavior>
  <action>
    Edit src/rituals/adjustment-dialogue.ts:

    (a) Add imports at top of file:
       - From '../chris/refusal.js': `import { detectRefusal, type RefusalResult } from '../chris/refusal.js';`
       - From './skip-tracking.js' (already imported with shouldFireAdjustmentDialogue per Plan 28-03 if needed): ADD `hasReachedEvasiveTrigger` to the existing import list

    (b) Define an adjustment-specific refusal extension at module top (per PATTERNS.md §C line 466 recommendation):
        ```typescript
        // Adjustment-specific refusal patterns NOT in the general detectRefusal
        // (kept local to the adjustment dialogue context — 'disable' in a
        // general conversation might mean something else; here it explicitly
        // means "disable this ritual").
        const ADJUSTMENT_DISABLE_PATTERN = /\b(disable|deactivate|turn\s+off)\b/i;
        ```
       And a helper:
        ```typescript
        function isAdjustmentRefusal(text: string): { isRefusal: boolean; topic: string; isHardDisable: boolean; isNotNow: boolean } {
          // Check general detectRefusal first
          const general = detectRefusal(text);
          if (general.isRefusal) {
            // Distinguish 'not now' (deferral) from 'drop it' (hard disable)
            const isNotNow = /\bnot\s+(?:now|today|right\s+now)\b/i.test(text);
            return { isRefusal: true, topic: general.topic, isHardDisable: !isNotNow, isNotNow };
          }
          // Adjustment-specific: 'disable' / 'deactivate' / 'turn off'
          if (ADJUSTMENT_DISABLE_PATTERN.test(text)) {
            return { isRefusal: true, topic: text.trim(), isHardDisable: true, isNotNow: false };
          }
          return { isRefusal: false, topic: '', isHardDisable: false, isNotNow: false };
        }
        ```

    (c) Add new internal helper async routeRefusal(ritualId, refusal, text): Promise<void>:

        Body branches on refusal.isHardDisable vs refusal.isNotNow:
        - **Hard disable ("drop it" / "disable" / etc.)**:
          1. db.update(rituals).set({ enabled: false }).where(eq(rituals.id, ritualId))
          2. db.insert(ritualConfigEvents).values({ ritualId, actor: 'adjustment_dialogue_refusal', patch: { kind: 'manual_disable', source: 'user_drop_it_or_disable', user_text: text.slice(0, 200) } })
          3. logger.info({ ritualId, originalText: text }, 'chris.adjustment.refused.manual_disable')
          4. Send Telegram echo: "OK, disabling this ritual. You can re-enable it manually anytime."

        - **"not now" deferral**:
          1. const sevenDaysFromNow = new Date(Date.now() + 7 * 24 * 3600 * 1000)
          2. db.update(rituals).set({ config: sql\`jsonb_set(config, '{adjustment_mute_until}', \${JSON.stringify(sevenDaysFromNow.toISOString())}::jsonb)\` }).where(eq(rituals.id, ritualId)) — note the JSON.stringify(...) wraps the ISO string in JSON quotes so jsonb_set sees it as a JSON string value
          3. db.insert(ritualConfigEvents).values({ ritualId, actor: 'adjustment_dialogue_refusal', patch: { kind: 'apply', field: 'adjustment_mute_until', new_value: sevenDaysFromNow.toISOString(), source: 'user_not_now' } })
          4. logger.info({ ritualId, deferUntil: sevenDaysFromNow.toISOString() }, 'chris.adjustment.refused.not_now')
          5. Send Telegram echo: "OK, I'll skip the adjustment dialogue for 7 days. Skip-tracking continues."

    (d) MODIFY existing handleAdjustmentReply (added in Plan 28-03 Task 3) — INSERT the refusal pre-check BETWEEN the atomic-consume STEP 1 and the classification STEP 2:

        ```typescript
        export async function handleAdjustmentReply(pending, chatId, text) {
          // STEP 1: Atomic-consume the pending row (existing — Plan 28-03)
          const [consumed] = await db.update(...).returning({...});
          if (!consumed) throw new StorageError('ritual.adjustment.race_lost');

          // STEP 1.5 (Plan 28-04): M006 refusal pre-check — load-bearing for SKIP-06.
          // RESEARCH Pitfall 2: refusals MUST short-circuit BEFORE the Haiku call to
          // prevent classifier-mis-classifying refusal-as-evasive → spurious 30-day pause.
          // Refusals NEVER reach Haiku, NEVER count as evasive, NEVER write the
          // metadata.classification='evasive' marker that hasReachedEvasiveTrigger reads.
          const refusal = isAdjustmentRefusal(text);
          if (refusal.isRefusal) {
            await routeRefusal(pending.ritualId, refusal, text);
            return ''; // IN-02 silent-skip
          }

          // STEP 2: Classification path (existing — Plan 28-03)
          const parsed = await classifyAdjustmentReply(text);
          // ... existing branches
        }
        ```

    (e) MODIFY the existing 'evasive' branch in handleAdjustmentReply (Plan 28-03 Task 3) — AFTER the existing INSERT INTO ritual_responses with metadata.kind='adjustment_dialogue_response' and metadata.classification='evasive', ADD an evasive-trigger check:

        ```typescript
        // Plan 28-04 SKIP-06 — after writing the evasive marker, check whether
        // hasReachedEvasiveTrigger fires (>= 2 evasive in 14d → 30-day pause).
        if (await hasReachedEvasiveTrigger(pending.ritualId)) {
          const thirtyDaysFromNow = new Date(Date.now() + 30 * 24 * 3600 * 1000);
          await db
            .update(rituals)
            .set({
              enabled: false,
              config: sql`jsonb_set(config, '{mute_until}', ${JSON.stringify(thirtyDaysFromNow.toISOString())}::jsonb)`,
            })
            .where(eq(rituals.id, pending.ritualId));
          await db.insert(ritualConfigEvents).values({
            ritualId: pending.ritualId,
            actor: 'system',
            patch: {
              kind: 'auto_pause',
              source: '2_evasive_in_14d',
              mute_until: thirtyDaysFromNow.toISOString(),
            },
          });
          logger.info(
            { ritualId: pending.ritualId, muteUntil: thirtyDaysFromNow.toISOString() },
            'chris.adjustment.auto_paused',
          );
          // Send Telegram echo
          await bot.api.sendMessage(
            config.telegramAuthorizedUserId,
            `Pausing this ritual for 30 days — feels like the timing isn't right. It will auto-re-enable on ${thirtyDaysFromNow.toISOString().slice(0, 10)}.`,
          );
        }
        ```

    Create src/rituals/__tests__/refusal-pre-check.integration.test.ts:
       - Mirror src/chris/__tests__/engine-pp5.test.ts cumulative-not-called pattern (lines 23-95)
       - vi.hoisted mocks: mockAnthropicCreate, mockAnthropicParse, mockSendMessage
       - vi.mock '../../llm/client.js' + vi.mock '../../bot/bot.js'
       - TWO top-level describe blocks:
         - 'Phase 28 Plan 04 SKIP-07 — refusal pre-check (Pitfall 2 invariant)' — Tests 1-4. afterAll: cumulative `expect(mockAnthropicParse).not.toHaveBeenCalled()` (LOAD-BEARING — proves Haiku is NEVER reached for refusal text inputs).
         - 'Phase 28 Plan 04 SKIP-06 — non-refusal Haiku path (positive control)' — Tests 5-6. afterAll: cumulative mockAnthropicCreate.not.toHaveBeenCalled() (Sonnet still NEVER reached; Haiku IS reached this time, which is correct).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "adjustment-dialogue|refusal-pre-check" | head -10 && bash scripts/test.sh src/rituals/__tests__/refusal-pre-check.integration.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to adjustment-dialogue.ts or refusal-pre-check.integration.test.ts
    - bash scripts/test.sh src/rituals/__tests__/refusal-pre-check.integration.test.ts reports all 6 tests passing
    - The refusal-pre-check describe block's cumulative `mockAnthropicParse.not.toHaveBeenCalled()` afterAll assertion PASSES (the load-bearing SKIP-06 invariant)
    - grep -c "detectRefusal" src/rituals/adjustment-dialogue.ts >= 2 (1 import + 1 invocation)
    - grep -c "hasReachedEvasiveTrigger" src/rituals/adjustment-dialogue.ts >= 2 (1 import + 1 invocation)
    - grep -c "isAdjustmentRefusal" src/rituals/adjustment-dialogue.ts >= 2 (1 declaration + 1 invocation)
    - grep -c "routeRefusal" src/rituals/adjustment-dialogue.ts >= 2 (1 declaration + 1 invocation)
    - grep -E "actor: 'adjustment_dialogue_refusal'" src/rituals/adjustment-dialogue.ts | wc -l >= 1 (the manual_disable + not_now writes)
    - grep -E "actor: 'system'" src/rituals/adjustment-dialogue.ts | wc -l >= 1 (the auto_pause write)
    - grep -E "patch: \{ kind: 'manual_disable'" src/rituals/adjustment-dialogue.ts | wc -l >= 1
    - grep -E "patch: \{ kind: 'auto_pause'" src/rituals/adjustment-dialogue.ts | wc -l >= 1
    - Plan 28-03's adjustment-dialogue.integration.test.ts STILL passes — Plan 28-04 changes don't break the existing classification branches (defense-in-depth assertion: refusal-pre-check returns early, then existing flow proceeds untouched for non-refusal inputs)
  </acceptance_criteria>
  <done>
    handleAdjustmentReply now does refusal pre-check BEFORE Haiku (load-bearing SKIP-06 invariant). routeRefusal handles 'drop it'/'disable' (manual_disable) vs 'not now' (7-day adjustment_mute_until). Evasive branch now checks hasReachedEvasiveTrigger and applies the 30-day pause. ritual_config_events writes use envelope-in-patch shape per RESEARCH Landmine 1. Cumulative not-called assertion proves Haiku is never reached for refusal text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire autoReEnableExpiredMutes into runRitualSweep at the very top</name>
  <files>src/rituals/scheduler.ts</files>
  <read_first>
    - src/rituals/scheduler.ts (post Plan 28-03 — read in full; understand the runRitualSweep entry block at lines 82-93 with the Plan 28-01 ritualResponseWindowSweep call already inserted)
    - src/rituals/skip-tracking.ts (post Task 1 — autoReEnableExpiredMutes signature)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/scheduler.ts" §"Phase 28 NEW steps to inject" (lines 264-274)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md "Integration Points" (line 207-211) — runRitualSweep order documented
  </read_first>
  <action>
    Edit src/rituals/scheduler.ts:

    (a) Extend the existing import from skip-tracking.js to include autoReEnableExpiredMutes:
        From: `import { shouldFireAdjustmentDialogue } from './skip-tracking.js';`
        To: `import { autoReEnableExpiredMutes, shouldFireAdjustmentDialogue } from './skip-tracking.js';`

    (b) INSIDE runRitualSweep, IMMEDIATELY AFTER `logger.info({ timestamp: now.toISOString() }, 'rituals.sweep.start');` and BEFORE the existing Plan 28-01 `try { await ritualResponseWindowSweep(now); }` block, INSERT a new try/catch:

        ```typescript
        // Phase 28 Plan 04 SKIP-06 — auto-re-enable expired mutes BEFORE the
        // response-window sweep. Re-enabled rituals must participate in this
        // tick's normal flow (next_run_at calculation, channel-cap, dispatch),
        // not wait for the next tick. Wrapped in try/catch (CRON-01 belt-and-
        // suspenders) so a failure does NOT block the dispatch path.
        try {
          await autoReEnableExpiredMutes(now);
        } catch (err) {
          logger.error({ err }, 'rituals.auto_re_enable.error');
        }
        ```

    The final order at the top of runRitualSweep is now:
       1. logger.info('rituals.sweep.start')
       2. autoReEnableExpiredMutes(now)         ← NEW Plan 28-04
       3. ritualResponseWindowSweep(now)        ← Plan 28-01
       4. STEP 0: hasReachedRitualDailyCap channel-cap check
       5. ... existing flow

    No other changes to scheduler.ts in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep scheduler.ts | head -5 && grep -c autoReEnableExpiredMutes src/rituals/scheduler.ts && bash scripts/test.sh src/rituals/__tests__/scheduler.test.ts 2>&1 | tail -15 && bash scripts/test.sh src/rituals/__tests__/should-fire-adjustment.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to scheduler.ts
    - grep -c "autoReEnableExpiredMutes" src/rituals/scheduler.ts reports >= 2 (1 import + 1 invocation)
    - grep -E "rituals.auto_re_enable.error" src/rituals/scheduler.ts | wc -l >= 1 (the new error log line)
    - bash scripts/test.sh src/rituals/__tests__/scheduler.test.ts (existing Phase 25/26/29 test) still passes — Plan 28-04 changes do NOT break the scheduler regression suite
    - bash scripts/test.sh src/rituals/__tests__/should-fire-adjustment.test.ts (Plan 28-02's test) still passes
    - bash scripts/test.sh src/rituals/__tests__/self-protective-pause.test.ts (Task 1's test) still passes — the autoReEnableExpiredMutes wiring exists but unit tests still hit the function directly via import (not via runRitualSweep), so they are independent of this wiring
  </acceptance_criteria>
  <done>
    runRitualSweep now invokes autoReEnableExpiredMutes BEFORE the response-window sweep. Re-enabled rituals participate in the same tick. Existing scheduler tests still pass. Plan 28-02 + 28-03 + 28-04 sweep-side helpers all wired in the documented order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Greg → Telegram → engine PP#5 → handleAdjustmentReply → detectRefusal | adversarial Greg-text could attempt to bypass refusal detection by clever phrasing; detector is regex-based with EN+FR+RU patterns; meta-reference guard (negative lookahead `told|said|mentioned`) prevents 'I told her to drop it' false-positive |
| handleAdjustmentReply STEP 1.5 ordering | LOAD-BEARING: refusal pre-check MUST run BEFORE classifyAdjustmentReply; reordered = refusal classified-as-evasive → spurious 30-day pause (RESEARCH Pitfall 2). Cumulative mockAnthropicParse.not.toHaveBeenCalled() asserts the ordering |
| ritualConfigEvents.actor varchar(32) | bounded; valid Plan 28-04 actors: 'adjustment_dialogue_refusal' (27 chars — fits) | 'system' (6 chars) |
| autoReEnableExpiredMutes config.mute_until SQL cast | cast `(config->>'mute_until')::timestamptz` returns NULL for unset/NULL field; NULL <= now is NULL (falsy); manual_disable rituals (config.mute_until=null) correctly excluded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-28-04** | Spoofing | Refusal bypass — Greg's "drop it" reaches Haiku before refusal pre-check | mitigate | refusal pre-check in handleAdjustmentReply BEFORE any LLM call (Task 2 step (d) — STEP 1.5 between atomic-consume and classification). Tested in refusal-pre-check.integration.test.ts (Tests 1-4 with cumulative `mockAnthropicParse.not.toHaveBeenCalled()` afterAll assertion). |
| T-28-S6 | Spoofing | autoReEnableExpiredMutes operating on adversarially-set mute_until in the past | accept | mute_until is operator-set via direct DB or via Plan 28-04 evasive-trigger (server-only). No Telegram-surface mutation of mute_until. Operator with DB access can already do anything; same trust level. |
| T-28-T4 | Tampering | metadata.classification='evasive' counted by hasReachedEvasiveTrigger but written by another path bypassing handleAdjustmentReply | accept | Defense-in-depth: ritual_responses.metadata is server-side only; no Telegram-surface writes mutate metadata directly. Future code paths writing classification='evasive' to ritual_responses for non-adjustment-dialogue contexts WOULD trigger the count — accepted because ritual_responses.metadata.kind='adjustment_dialogue_response' qualifier scopes the query. |
| T-28-R3 | Repudiation | refusal events not auditable | mitigate | Every refusal writes a ritualConfigEvents row with actor='adjustment_dialogue_refusal' + patch.user_text (truncated 200 chars). Replayable by audit query. |
| T-28-D4 | DoS | autoReEnableExpiredMutes scanning rituals on every tick | mitigate | Query is indexed-friendly: `WHERE enabled = false AND (config->>'mute_until')::timestamptz <= now()`. The rituals table has at most ~5 rows in M009 scope; scan cost is constant and small. Future scaling concern (M013+) addressed by adding a partial index `WHERE enabled = false` if needed. |
| T-28-E4 | Elevation of privilege | "disable" pattern in ADJUSTMENT_DISABLE_PATTERN matches in non-refusal contexts | accept | Pattern is scoped to handleAdjustmentReply call site only (isAdjustmentRefusal helper local to adjustment-dialogue.ts). General detectRefusal is NOT extended. Pattern requires word-boundary `\b` so 'disabled' would also match — accepted (semantic intent of 'disabled' is the same as 'disable'). |

</threat_model>

<verification>
Phase 28 Plan 04 acceptance:

1. **TS clean**: npx tsc --noEmit zero errors attributable to changed files (skip-tracking.ts, adjustment-dialogue.ts, scheduler.ts, the two new test files).
2. **Refusal-pre-check integration test green** with cumulative `mockAnthropicParse.not.toHaveBeenCalled()` afterAll passing — proves Pitfall 2 invariant (refusals never reach Haiku) is enforced.
3. **Self-protective-pause test green** covering all 8 boundary cases including Test 7 (manual_disable does NOT auto-re-enable).
4. **No regression on Plan 28-01/02/03 tests**: scheduler.test.ts, should-fire-adjustment.test.ts, adjustment-dialogue.integration.test.ts, confirmation-window.test.ts, engine-pp5-adjustment.test.ts, engine-pp5.test.ts ALL still pass.
5. **Full Docker gate**: bash scripts/test.sh src/rituals/ src/chris/ reports the rituals + chris suites green; substrate gates (0006-0010) all pass; anchor-bias regression guard still passes.
6. **Envelope-in-patch shape consistently used**: grep -E "actor: '(system|user|adjustment_dialogue_refusal|auto_apply_on_timeout)'" src/rituals/ -r | wc -l shows >= 5 matches across adjustment-dialogue.ts + skip-tracking.ts (Plan 28-03 + 28-04 writes); zero matches for the obsolete CONTEXT.md D-28-09 column-style writes (`change_kind:`, `old_value:` as top-level columns — those words may appear inside the patch envelope but NOT as drizzle .values({change_kind: ...}) keys).
</verification>

<success_criteria>
ROADMAP success criterion 3 + 4 full coverage:
- Criterion 3 — "After 2 evasive responses within 14 days on the same ritual, the ritual auto-disables for 30 days then auto-re-enables — verifiable by inserting two synthetic evasive responses 7 days apart and confirming enabled = false + mute_until = +30d after the 2nd" — proven by Test 6 in refusal-pre-check.integration.test.ts + Tests 5-7 in self-protective-pause.test.ts.
- Criterion 4 — "M006 refusal handling is honored — Greg replying 'drop it / disable / not now' routes to the refusal path (ritual paused / disabled per choice), NOT counted as evasive; conversation does not loop" — proven by Tests 1-4 in refusal-pre-check.integration.test.ts (the cumulative `mockAnthropicParse.not.toHaveBeenCalled()` assertion is the load-bearing proof).
- SKIP-07 audit trail — every config mutation writes ritual_config_events: refusal-disable, not-now-defer, auto-pause, auto-re-enable, plus Plan 28-03's apply/abort. Verifiable by SUMMARY listing the 5 actor / patch.kind combinations and grep counts in adjustment-dialogue.ts + skip-tracking.ts.
</success_criteria>

<output>
After completion, create .planning/phases/28-skip-tracking-adjustment-dialogue/28-04-SUMMARY.md. SUMMARY frontmatter MUST include `requirements-completed: [SKIP-06, SKIP-07]` per PROC-02 upstream verdict.

This SUMMARY also closes the phase — Plan 28-04 is the last plan. The Phase 28 verifier (`/gsd-verify-work`) reads all 4 SUMMARYs and asserts the 7 SKIP requirements + 4 ROADMAP success criteria are all covered.
</output>
