---
phase: 28-skip-tracking-adjustment-dialogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rituals/types.ts
  - src/rituals/voice-note.ts
  - src/rituals/wellbeing.ts
  - src/rituals/weekly-review.ts
  - src/rituals/scheduler.ts
  - src/rituals/__tests__/types.test.ts
  - src/rituals/__tests__/skip-tracking.integration.test.ts
  - src/rituals/__tests__/fixtures/skip-tracking.ts
autonomous: true
requirements: [SKIP-01, SKIP-02]
tags: [rituals, skip-tracking, fire-events, outcome-union, m009]

must_haves:
  truths:
    - "RitualFireOutcome union has 12 variants — 7 existing + 'wellbeing_completed' + 'wellbeing_skipped' + 'responded' + 'window_missed' + 'fired_no_response' (per RESEARCH Landmine 3)"
    - "Every fire path of voice-note / wellbeing / weekly-review writes a ritual_fire_events row with outcome from the typed union"
    - "Every response-handling path that resets skip_count writes a 'responded' (or 'wellbeing_completed') ritual_fire_events row AND updates rituals.skip_count = 0 atomically"
    - "ritualResponseWindowSweep helper detects expired ritual_pending_responses rows and emits BOTH window_missed AND fired_no_response events plus increments rituals.skip_count for the latter"
    - "rituals.skip_count is rebuildable by replay of ritual_fire_events from the most recent reset event (per D-28-03)"
  artifacts:
    - path: "src/rituals/types.ts"
      provides: "12-variant RitualFireOutcome union + RITUAL_OUTCOME const map + RitualConfigSchema with 9th adjustment_mute_until field"
      contains: "fired_no_response"
    - path: "src/rituals/voice-note.ts"
      provides: "fire-side + suppression + response-side ritual_fire_events writes; skip_count reset on responded"
      contains: "ritualFireEvents"
    - path: "src/rituals/wellbeing.ts"
      provides: "homogenized OUTCOME refs to RITUAL_OUTCOME const map; fire-side ritual_fire_events write added; skip_count reset in completeSnapshot"
      contains: "RITUAL_OUTCOME.WELLBEING_COMPLETED"
    - path: "src/rituals/weekly-review.ts"
      provides: "fire-side + sparse-data short-circuit ritual_fire_events writes"
      contains: "ritualFireEvents"
    - path: "src/rituals/scheduler.ts"
      provides: "ritualResponseWindowSweep helper + injection at top of runRitualSweep before STEP 0"
      contains: "ritualResponseWindowSweep"
    - path: "src/rituals/__tests__/skip-tracking.integration.test.ts"
      provides: "real-DB integration tests covering SKIP-01 outcome-filter rules + SKIP-02 replay invariant"
      min_lines: 200
    - path: "src/rituals/__tests__/fixtures/skip-tracking.ts"
      provides: "seedRitualWithFireEvents(ritualName, outcomes[]) helper"
      min_lines: 30
  key_links:
    - from: "src/rituals/voice-note.ts"
      to: "src/db/schema.ts ritualFireEvents"
      via: "db.insert(ritualFireEvents).values(...)"
      pattern: "db\\.insert\\(ritualFireEvents\\)"
    - from: "src/rituals/scheduler.ts"
      to: "src/rituals/scheduler.ts ritualResponseWindowSweep"
      via: "called at top of runRitualSweep before STEP 0 channel-cap check"
      pattern: "await\\s+ritualResponseWindowSweep\\("
    - from: "src/rituals/types.ts RITUAL_OUTCOME"
      to: "all 3 ritual handler files"
      via: "imported and used as the source-of-truth for ritual_fire_events.outcome strings"
      pattern: "RITUAL_OUTCOME\\."
---

<objective>
Phase 28 Wave 1 substrate. Extend RitualFireOutcome to 12 variants (per RESEARCH Landmine 3 — current union is 7 variants, NOT the 10 CONTEXT.md D-28-02 claimed; we ADD 3 new variants AND homogenize wellbeing's 2 free-form strings into the union). Wire ritual_fire_events writes into ALL 3 ritual handler fire paths (per RESEARCH Landmine 8 — only wellbeing.ts writes today, and only on completion/skip, not on initial fire). Add ritualResponseWindowSweep helper that closes the response-window loop by emitting window_missed + fired_no_response events when expires_at passes with consumed_at IS NULL. Add 'responded' reset on PP#5 voice-note deposit + skip_count = 0 update. Plan 28-02 builds the cadence-aware threshold predicate ON TOP OF this substrate.

Purpose: SKIP-01 (discriminated outcome union; only fired_no_response increments skip_count) + SKIP-02 (append-only ritual_fire_events; rituals.skip_count rebuildable by replay). Without this plan, Plan 28-02's computeSkipCount has nothing to count — ritual_fire_events is sparsely populated today.

Output: Extended union + RITUAL_OUTCOME const map; instrumented voice-note.ts + wellbeing.ts + weekly-review.ts; ritualResponseWindowSweep helper in scheduler.ts injected before STEP 0; integration test scaffold proving SKIP-01/02 acceptance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-VALIDATION.md
@src/rituals/types.ts
@src/rituals/voice-note.ts
@src/rituals/wellbeing.ts
@src/rituals/weekly-review.ts
@src/rituals/scheduler.ts
@src/db/schema.ts

<interfaces>
Existing contracts the executor must preserve verbatim.

From src/rituals/types.ts:42-103 (current state — 7-variant union, RitualConfigSchema 8 fields):
The current type is the ONLY pre-existing variants source. Plan 28-01 ADDS 5 strings ('wellbeing_completed', 'wellbeing_skipped', 'responded', 'window_missed', 'fired_no_response') WITHOUT removing any existing ones. The const map RITUAL_OUTCOME is NEW.

From src/db/schema.ts:457-464 (ritualFireEvents shape — pre-existing, not modified by Plan 28-01):
Columns: id (uuid pk), ritualId (uuid notnull FK rituals.id), firedAt (timestamptz notnull), outcome (text notnull — free-form, NOT enum), metadata (jsonb nullable), createdAt (timestamptz notnull default now).

From src/db/schema.ts:485-505 (ritualPendingResponses — NO metadata column today):
Columns: id, ritualId, chatId, firedAt, expiresAt, consumedAt, promptText, createdAt. The metadata column is NOT YET present — added in Plan 28-03 migration 0010. Plan 28-01 queries this table by (consumedAt IS NULL, expiresAt) ONLY — must NOT depend on metadata.

From src/rituals/wellbeing.ts:51,65-67 (current outcome-string state — to be homogenized):
Local consts OUTCOME_COMPLETED='wellbeing_completed' and OUTCOME_SKIPPED='wellbeing_skipped' written to ritualFireEvents.outcome. Plan 28-01 deletes the local consts and replaces references with RITUAL_OUTCOME.WELLBEING_COMPLETED / .WELLBEING_SKIPPED.

From src/rituals/wellbeing.ts:293-298 (the ONLY current ritual_fire_events write site):
db.insert(ritualFireEvents).values({ ritualId: openRow.ritualId, firedAt: new Date(), outcome: OUTCOME_COMPLETED, metadata: { fireRowId, snapshotDate } }) — pattern to copy verbatim, just substitute RITUAL_OUTCOME.* references and add similar inserts to the missing emit sites.

From src/rituals/voice-note.ts:179-230 (recordRitualVoiceResponse — NO ritual_fire_events write today):
3-step flow: atomic-consume pending row, storePensieveEntry with epistemicTag RITUAL_RESPONSE, db.insert(ritualResponses). ABSENT: ritual_fire_events 'responded' write + rituals.skip_count = 0 update. Plan 28-01 ADDS both at the end of the success path.

From src/rituals/scheduler.ts:82-93 (runRitualSweep entry — NEW ritualResponseWindowSweep injects here):
Order: logger.info('rituals.sweep.start') -> Plan 28-01 INSERTS ritualResponseWindowSweep call HERE -> STEP 0 channel-cap check -> remainder of sweep. Wrapped in try/catch so a sweep-helper failure does not block the dispatch path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RitualFireOutcome to 12 variants + add RITUAL_OUTCOME const map + add adjustment_mute_until to RitualConfigSchema</name>
  <files>src/rituals/types.ts, src/rituals/__tests__/types.test.ts</files>
  <read_first>
    - src/rituals/types.ts:1-103 in full (file is 104 LoC; understand the current 7-variant union shape and JSDoc commentary)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 3 (lines 522-525) and Open Question 2 (lines 573-576)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/types.ts" section (lines 30-92) — the 12-variant final shape and RITUAL_OUTCOME const map are pre-specified verbatim there
    - src/rituals/__tests__/cadence.test.ts as analog for unit-test framing (no DB needed, vitest pure-Zod tests)
  </read_first>
  <behavior>
    - Test 1: RitualFireOutcome union has exactly 12 string-literal members (TS type-level — write a const all RitualFireOutcome[] array literal exhausting the 12 strings; build must pass with no errors).
    - Test 2: RITUAL_OUTCOME const map has 12 keys; Object.values(RITUAL_OUTCOME) includes 'fired_no_response', 'responded', 'window_missed', 'wellbeing_completed', 'wellbeing_skipped'.
    - Test 3: At TS compile time, RITUAL_OUTCOME satisfies Record<string, RitualFireOutcome> — proves no string drift (verified by tsc, no runtime test needed).
    - Test 4: RitualConfigSchema parses successfully when adjustment_mute_until is set to '2026-05-15T20:00:00.000Z'; parses without the field; rejects 'not-iso' with ZodError.
    - Test 5: RitualConfigSchema still rejects unknown fields (Unrecognized key) — strict-mode preserved.
  </behavior>
  <action>
    Edit src/rituals/types.ts:

    (a) REPLACE the existing union at lines 88-95 with the 12-variant shape from PATTERNS.md lines 49-63. Preserve the existing JSDoc comment on system_suppressed; add comments for the 5 new variants. Each comment cites its origin (Phase 27 homogenization vs Phase 28 SKIP-01).

    (b) Add the RITUAL_OUTCOME const map IMMEDIATELY AFTER the union (before the RitualFireResult interface at line 97). Use the exact shape from PATTERNS.md lines 70-83. Include the JSDoc explaining Pitfall 4 mitigation. The closing line MUST be: as const satisfies Record<string, RitualFireOutcome>;

    (c) Add adjustment_mute_until as the 9th optional field in RitualConfigSchema. Insert AFTER the mute_until line (line 50) and BEFORE the time_zone line (line 51). Type: z.string().datetime().nullable().optional(). Per RESEARCH Open Question 2: NO schema_version bump needed (strict-mode invariant unchanged — adjustment_mute_until is a NEW recognized field, not unknown). Add a JSDoc note distinguishing it from mute_until (suppresses ALL fires) — adjustment_mute_until only suppresses the adjustment dialogue itself.

    (d) Update the JSDoc above the union (lines 71-87) to document the 5 new variants:
        - 'wellbeing_completed' — Phase 27 (homogenized in 28). Wellbeing snapshot completed via 3-tap. Resets skip_count via D-28-03.
        - 'wellbeing_skipped' — Phase 27 (homogenized). Greg tapped Skip button. Does NOT increment skip_count.
        - 'responded' — Phase 28 SKIP-01. Greg replied to a ritual prompt within window. Resets skip_count.
        - 'window_missed' — Phase 28 SKIP-01. ritual_pending_responses.expires_at passed with consumed_at IS NULL. Paired emit (with fired_no_response) by ritualResponseWindowSweep.
        - 'fired_no_response' — Phase 28 SKIP-01. THE skip-counting outcome. Increments rituals.skip_count.

    Create or extend src/rituals/__tests__/types.test.ts with the 5 behaviors above. Use vitest describe/it style. Mirror src/rituals/__tests__/cadence.test.ts framing (no DB needed — pure-type/Zod tests).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'rituals/types\.ts|types\.test\.ts' | grep -v warning | head -20 && npx vitest run src/rituals/__tests__/types.test.ts --reporter=basic 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to src/rituals/types.ts or the new test file
    - npx vitest run src/rituals/__tests__/types.test.ts reports all 5 tests passing
    - grep -c "fired_no_response" src/rituals/types.ts reports >= 2 (one in union, one in const map)
    - grep -c "'wellbeing_completed'" src/rituals/types.ts reports >= 2 (one in union, one in const map)
    - grep -c "RITUAL_OUTCOME" src/rituals/types.ts reports >= 13 (one declaration line plus 12 keys)
    - grep -n "adjustment_mute_until" src/rituals/types.ts reports a line between line 47 and line 55 (inside RitualConfigSchema before .strict())
    - grep -E "as const satisfies Record<string, RitualFireOutcome>" src/rituals/types.ts matches exactly 1 line
  </acceptance_criteria>
  <done>
    types.ts exports the 12-variant union, RITUAL_OUTCOME const map (TS-proven exhaustive), and RitualConfigSchema with adjustment_mute_until 9th optional field. Unit tests cover all 5 behaviors. tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ritual_fire_events writes + skip_count reset into voice-note.ts (fire + suppression + response paths)</name>
  <files>src/rituals/voice-note.ts</files>
  <read_first>
    - src/rituals/voice-note.ts:1-365 in full; understand the THREE emit sites (fireVoiceNote success at line 360-365 'return fired'; shouldSuppressVoiceNoteFire success at line 320 'return system_suppressed'; recordRitualVoiceResponse success at line 229 'return pensieveEntryId+consumedAt')
    - src/rituals/wellbeing.ts:51,293-298 (the canonical ritual_fire_events.values() shape — copy this pattern verbatim)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/voice-note.ts" section (lines 95-122) — the 3 instrumentation points are pre-specified
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 8 (lines 553-565) — response-side outcome split table
  </read_first>
  <behavior>
    - Test 1: After successful fireVoiceNote (mocked sendMessage + mocked DB), exactly ONE ritual_fire_events row exists with outcome='fired' for that ritualId; metadata contains promptIdx and prompt.
    - Test 2: When shouldSuppressVoiceNoteFire returns true, fireVoiceNote writes a ritual_fire_events row with outcome='system_suppressed' and metadata { reason: 'heavy_deposit_day', deposit_threshold: 5 }.
    - Test 3: After recordRitualVoiceResponse succeeds, exactly ONE ritual_fire_events row with outcome='responded' exists for the ritualId; rituals.skip_count for that ritualId is 0 after the call (regardless of pre-call value).
    - Test 4: All three emit sites use RITUAL_OUTCOME const-map references, not string literals — verifiable by grep returning ZERO matches for outcome string literals.
  </behavior>
  <action>
    Edit src/rituals/voice-note.ts:

    (a) Extend imports at line 32 (the existing schema import block) to add ritualFireEvents to the named imports list. Extend the types.js import at line 39 to include RITUAL_OUTCOME alongside the existing RitualConfig + RitualFireOutcome types.

    (b) Suppression-side emit — INSIDE fireVoiceNote, IMMEDIATELY BEFORE the return 'system_suppressed'; statement at line 320, ADD a db.insert(ritualFireEvents).values({...}) call with outcome RITUAL_OUTCOME.SYSTEM_SUPPRESSED and metadata { reason: 'heavy_deposit_day', deposit_threshold: RITUAL_SUPPRESS_DEPOSIT_THRESHOLD }.

    (c) Fire-side emit — INSIDE fireVoiceNote, AFTER the existing logger.info('rituals.voice_note.fired') block at lines 360-363, BEFORE the final return 'fired'; at line 364, ADD a db.insert(ritualFireEvents).values({...}) call with outcome RITUAL_OUTCOME.FIRED, firedAt: firedAt (the local var already in scope), and metadata { promptIdx, prompt }.

    (d) Response-side emit + skip_count reset — INSIDE recordRitualVoiceResponse, AFTER the db.insert(ritualResponses).values(...) block at lines 221-227, BEFORE the return statement at line 229, ADD TWO writes in sequence (NOT a transaction — both are idempotent under retry; D-28-03 documents tradeoff):
       (i) db.insert(ritualFireEvents).values({ ritualId: pending.ritualId, firedAt: consumed.consumedAt, outcome: RITUAL_OUTCOME.RESPONDED, metadata: { pendingResponseId: pending.id, pensieveEntryId: entry.id } })
       (ii) db.update(rituals).set({ skipCount: 0 }).where(eq(rituals.id, pending.ritualId))

    Note: eq is already imported from drizzle-orm at line 26; rituals is already imported at line 31; ritualFireEvents added in step (a). No additional imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep voice-note.ts | head -10 && grep -c ritualFireEvents src/rituals/voice-note.ts && grep -c "RITUAL_OUTCOME\." src/rituals/voice-note.ts && grep -E "skipCount: 0" src/rituals/voice-note.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to voice-note.ts
    - grep -c "ritualFireEvents" src/rituals/voice-note.ts reports >= 4 (1 import + 3 inserts)
    - grep -c "RITUAL_OUTCOME\." src/rituals/voice-note.ts reports >= 4 (1 import + 3 outcome refs)
    - grep -E "outcome: '(fired|system_suppressed|responded)'" src/rituals/voice-note.ts | wc -l reports 0 (no string literals — only RITUAL_OUTCOME refs)
    - grep -E "skipCount: 0" src/rituals/voice-note.ts | wc -l reports >= 1 (responded reset)
    - grep -E "rituals.voice_note.suppressed" src/rituals/voice-note.ts still matches the existing logger line (existing suppression branch preserved)
  </acceptance_criteria>
  <done>
    voice-note.ts emits ritual_fire_events on fire, suppression, and response paths using RITUAL_OUTCOME const-map refs. Response path also resets rituals.skip_count = 0 per D-28-03. Tests for these behaviors exist in skip-tracking.integration.test.ts (Task 6).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Homogenize wellbeing.ts outcomes to RITUAL_OUTCOME const map + add fire-side emit + add skip_count reset</name>
  <files>src/rituals/wellbeing.ts</files>
  <read_first>
    - src/rituals/wellbeing.ts:1-435 in full; already has TWO ritual_fire_events emit sites at lines 293-298 (completed) and 335-340 (skipped); fire-side at lines 130-159 has NO emit today
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/wellbeing.ts" section (lines 124-137) — exact instrumentation points pre-specified
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 3 (lines 522-525) for the homogenization rationale
  </read_first>
  <behavior>
    - Test 1: After fireWellbeing succeeds (mocked sendMessage), exactly ONE ritual_fire_events row exists with outcome='fired' for that ritualId; metadata contains fireRowId + prompt: WELLBEING_PROMPT.
    - Test 2: After completeSnapshot (3rd dim tap) runs successfully, ritual_fire_events has outcome='wellbeing_completed' AND rituals.skip_count for that ritualId is 0.
    - Test 3: After handleSkip (Greg taps skip button) runs, ritual_fire_events has outcome='wellbeing_skipped' AND rituals.skip_count is UNCHANGED (skip is NOT a reset event per D-28-02).
    - Test 4: Anchor-bias contract preserved — wellbeing.ts has zero SELECT/FROM patterns against wellbeing_snapshots (D-27-04 prong 1).
  </behavior>
  <action>
    Edit src/rituals/wellbeing.ts:

    (a) Replace local outcome constants (lines 65-67) with RITUAL_OUTCOME imports. CHANGE the existing types import at line 53 from `import type { RitualConfig, RitualFireOutcome } from './types.js';` to `import { RITUAL_OUTCOME, type RitualConfig, type RitualFireOutcome } from './types.js';`. Then DELETE lines 65-67 (the OUTCOME_COMPLETED / OUTCOME_SKIPPED local consts). Then in the existing emits at lines 293-298 and 335-340, REPLACE outcome: OUTCOME_COMPLETED with outcome: RITUAL_OUTCOME.WELLBEING_COMPLETED and outcome: OUTCOME_SKIPPED with outcome: RITUAL_OUTCOME.WELLBEING_SKIPPED.

    (b) Add fire-side emit — INSIDE fireWellbeing (lines 106-159), AFTER the existing logger.info('rituals.wellbeing.fired') block (lines 153-156) and BEFORE return 'fired'; at line 158, ADD a db.insert(ritualFireEvents).values({...}) call with outcome RITUAL_OUTCOME.FIRED and metadata { fireRowId: fireRow.id, prompt: WELLBEING_PROMPT }. Per RESEARCH Landmine 8: this fire-side emit was MISSING pre-Phase-28; only completion/skip writes existed.

    (c) Add skip_count reset in completeSnapshot — Per PATTERNS.md note: keep wellbeing_completed as the response signal (no separate 'responded' emit needed). Per D-28-03, completion DOES reset skip_count. INSIDE completeSnapshot (lines 257-310), AFTER the existing db.insert(ritualFireEvents).values(WELLBEING_COMPLETED) block (was lines 293-298) and BEFORE the await ctx.editMessageText(...) at line 301, ADD: db.update(rituals).set({ skipCount: 0 }).where(eq(rituals.id, openRow.ritualId)).

    Note: eq is already imported at line 45 from drizzle-orm; rituals is already imported at line 51. No additional imports needed.

    (d) Update the WellbeingMetadata jsdoc at lines 39-43 to clarify outcome semantics post-Phase-28 — replace the speculative wording with: "'wellbeing_completed' resets skip_count via D-28-03; 'wellbeing_skipped' is neither reset nor increment; 'fired_no_response' emitted by ritualResponseWindowSweep on 18h window expiry."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep wellbeing.ts | head -10 && grep -c "RITUAL_OUTCOME\." src/rituals/wellbeing.ts && grep -c "OUTCOME_COMPLETED\|OUTCOME_SKIPPED" src/rituals/wellbeing.ts && grep -c "ritualFireEvents" src/rituals/wellbeing.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to wellbeing.ts
    - grep -c "RITUAL_OUTCOME\." src/rituals/wellbeing.ts reports >= 4 (1 import + 3 outcome refs — fire + completed + skipped)
    - grep -c "OUTCOME_COMPLETED\|OUTCOME_SKIPPED" src/rituals/wellbeing.ts reports 0 (local consts deleted)
    - grep -c "ritualFireEvents" src/rituals/wellbeing.ts reports >= 4 (1 import + 3 inserts)
    - grep -E "outcome: '(fired|wellbeing_completed|wellbeing_skipped)'" src/rituals/wellbeing.ts | wc -l reports 0 (string literals removed)
    - Anchor-bias guard (scripts/test.sh:168) still passes — grep -E "select.*wellbeingSnapshots|from.*wellbeingSnapshots" src/rituals/wellbeing.ts returns nothing
  </acceptance_criteria>
  <done>
    wellbeing.ts homogenized to RITUAL_OUTCOME const map. Fire-side emit added. Skip_count reset on completion. Anchor-bias contract preserved. Two pre-existing emit sites updated; one new emit site added (fire).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add ritual_fire_events writes to weekly-review.ts (success + sparse-data short-circuit)</name>
  <files>src/rituals/weekly-review.ts</files>
  <read_first>
    - src/rituals/weekly-review.ts:517-652 (fireWeeklyReview's main success/sparse-data branches; understand the two return-'fired' sites and local var names: fireRow, result.isFallback, weekStartIso, weekEndIso)
    - src/rituals/weekly-review.ts:1-50 (imports — verify ritualFireEvents NOT yet imported)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/weekly-review.ts" section (lines 139-149) — note: weekly review has NO user-reply window; will NEVER emit 'responded' or 'fired_no_response' from this file
  </read_first>
  <behavior>
    - Test 1: Successful weekly review fire (mocked Sonnet+Haiku) writes exactly ONE ritual_fire_events row with outcome='fired' and metadata containing weekStart, weekEnd, isFallback: false.
    - Test 2: Sparse-data short-circuit (substrate has <4 wellbeing snapshots OR no episodic summaries) writes ritual_fire_events with outcome='fired' and metadata { reason: 'no_data_short_circuit' }.
    - Test 3: Successful templated-fallback fire (Sonnet/Haiku retry-cap-2 exhausted) writes ritual_fire_events with outcome='fired' and metadata { isFallback: true }.
  </behavior>
  <action>
    Edit src/rituals/weekly-review.ts:

    (a) Add ritualFireEvents to the schema imports. Find the existing import block from '../db/schema.js' near the top of the file and ADD ritualFireEvents to the named imports list. Also add RITUAL_OUTCOME to the existing types.js import at the top of the file.

    (b) Sparse-data short-circuit emit — Per PATTERNS.md line 148: emit outcome RITUAL_OUTCOME.FIRED with metadata { reason: 'no_data_short_circuit', weekStart: weekStartIso, weekEnd: weekEndIso } IMMEDIATELY BEFORE the sparse-data return 'fired'; statement (around weekly-review.ts:546-547 per PATTERNS.md). Read lines 530-560 first to find the exact return 'fired'; site for the sparse-data path; insert the db.insert(ritualFireEvents).values(...) call directly above that return.

    (c) Main fire-side emit — IMMEDIATELY AFTER the await bot.api.sendMessage(...) call near line 639 and BEFORE the function ultimately returns 'fired'. Insert db.insert(ritualFireEvents).values({...}) with outcome RITUAL_OUTCOME.FIRED and metadata { ritualResponseId: fireRow.id, isFallback: result.isFallback, weekStart: weekStartIso, weekEnd: weekEndIso }. The variable names fireRow, result.isFallback, weekStartIso, weekEndIso are read from the function's existing local scope. If any local variable is named differently in the actual code (verify by reading the function body), use the existing name — do NOT rename existing variables.

    (d) Add a JSDoc note at the top of fireWeeklyReview's docstring explaining: "Weekly review has NO user-reply window. ritual_fire_events from this handler emits ONLY outcome:'fired'. The 'fired_no_response' (skip-counting) outcome for weekly review is emitted by ritualResponseWindowSweep on virtual response window expiry — but per Plan 28-02 / RESEARCH OQ#5: weekly review skip-tracking treats 2 consecutive 'fired' events without intermediate 'responded' as the skip-threshold trigger. Implementation lives in Plan 28-02 computeSkipCount."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep weekly-review.ts | head -10 && grep -c ritualFireEvents src/rituals/weekly-review.ts && grep -c "RITUAL_OUTCOME\.FIRED" src/rituals/weekly-review.ts && grep -E "no_data_short_circuit" src/rituals/weekly-review.ts | head -2</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to weekly-review.ts
    - grep -c "ritualFireEvents" src/rituals/weekly-review.ts reports >= 3 (1 import + 2 inserts)
    - grep -c "RITUAL_OUTCOME\.FIRED" src/rituals/weekly-review.ts reports >= 2 (the two emit sites)
    - grep -E "no_data_short_circuit" src/rituals/weekly-review.ts returns at least 1 match
    - grep -E "isFallback:" src/rituals/weekly-review.ts returns at least 1 match (depending on local var name)
  </acceptance_criteria>
  <done>
    weekly-review.ts emits ritual_fire_events on both success branches (main fire + sparse-data short-circuit). RITUAL_OUTCOME.FIRED used. The fired_no_response weekly-review semantics deferred to Plan 28-02 / Task 5 ritualResponseWindowSweep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add ritualResponseWindowSweep helper to scheduler.ts and wire into runRitualSweep entry</name>
  <files>src/rituals/scheduler.ts</files>
  <read_first>
    - src/rituals/scheduler.ts:1-292 in full; understand runRitualSweep entry block at lines 82-93 where the new helper is invoked, and the existing imports at lines 25-44
    - src/db/schema.ts:485-505 (ritualPendingResponses shape — NO metadata column today; Plan 28-03 adds it; Plan 28-01 query MUST NOT depend on metadata)
    - src/rituals/voice-note.ts:184-204 (atomic-consume UPDATE...RETURNING pattern to copy verbatim for race-safety)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/scheduler.ts" section (lines 253-274) — predicate-injection layout
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 8 table (lines 559-565) — emit semantics
  </read_first>
  <behavior>
    - Test 1: When zero ritual_pending_responses rows exist with expires_at < now() AND consumed_at IS NULL, ritualResponseWindowSweep returns 0 immediately (sub-millisecond hot path).
    - Test 2: With ONE expired pending row (expires_at = now() - 60s, consumed_at IS NULL), ritualResponseWindowSweep emits TWO ritual_fire_events rows for that ritualId — one outcome='window_missed' and one outcome='fired_no_response' — both with metadata referencing pendingResponseId; the row's consumed_at is set to now (atomic-consume); rituals.skip_count for that ritualId is incremented by exactly 1.
    - Test 3: With TWO expired pending rows for the SAME ritualId (catch-up scenario), skip_count is incremented by EXACTLY 2 (one increment per fired_no_response event).
    - Test 4: After ritualResponseWindowSweep, the pending row is consumed (consumed_at NOT NULL) and will not be re-processed on subsequent calls.
    - Test 5: runRitualSweep with now argument calls ritualResponseWindowSweep(now) BEFORE STEP 0 channel-cap check — verifiable via spy on the helper.
  </behavior>
  <action>
    Edit src/rituals/scheduler.ts:

    (a) Extend imports at the top of the file:
       - drizzle-orm import at line 25: ADD isNull and sql to the existing { and, asc, eq, lte } list
       - schema import at line 28: ADD ritualFireEvents and ritualPendingResponses to the existing { rituals } list
       - types.js import at lines 36-41: ADD RITUAL_OUTCOME alongside parseRitualConfig

    (b) Add new EXPORTED helper ritualResponseWindowSweep AFTER cadencePeriodMs (after line 256) and BEFORE dispatchRitualHandler. Function signature:

        export async function ritualResponseWindowSweep(now: Date = new Date()): Promise<number>

    Body:
       Step 1: SELECT id, ritualId, expiresAt FROM ritualPendingResponses WHERE consumedAt IS NULL AND expiresAt <= now LIMIT 50. If result is empty, return 0 immediately.
       Step 2: For each expired row: atomic-consume via UPDATE ritualPendingResponses SET consumedAt = now WHERE id = row.id AND consumedAt IS NULL RETURNING id (mirrors voice-note.ts:184-204 pattern). If no row returned (race lost — PP#5 deposit consumed first), continue to next iteration.
       Step 3: After successful consume, perform PAIRED EMIT — db.insert(ritualFireEvents).values({ ritualId, firedAt: now, outcome: RITUAL_OUTCOME.WINDOW_MISSED, metadata: { pendingResponseId, expiresAt: row.expiresAt.toISOString() } }) THEN db.insert(ritualFireEvents).values({ ritualId, firedAt: now, outcome: RITUAL_OUTCOME.FIRED_NO_RESPONSE, metadata: same shape }).
       Step 4: Increment denormalized skip_count: db.update(rituals).set({ skipCount: sql`${rituals.skipCount} + 1` }).where(eq(rituals.id, row.ritualId)). Idempotent: each ritualFireEvents insert above is unique (id PK), so increment count matches event count after replay.
       Step 5: After the for-loop, log emittedCount + scanned and return emittedCount.

    Add a thorough JSDoc explaining: (i) Per RESEARCH Landmine 8, fired_no_response is emitted from a SWEEP-time scan, NOT from a handler. (ii) Paired with window_missed (the fact: window passed) so projection consumers can distinguish the policy classification (fired_no_response is the skip-counting outcome) from the underlying window-passed fact. (iii) Atomic-consume race-safety: same UPDATE-RETURNING pattern as voice-note.ts:184-204 (RIT-10 idempotency precedent). (iv) Per-call cap of 50 protects against backlog of expired rows triggering unbounded work in a single tick.

    (c) Wire helper into runRitualSweep — INSIDE runRitualSweep at line 84 (immediately AFTER `logger.info({ timestamp: now.toISOString() }, 'rituals.sweep.start');` and BEFORE STEP 0 channel-cap check at line 90), ADD a try/catch block that calls ritualResponseWindowSweep(now). On failure, log 'rituals.window_sweep.error' and continue (best-effort accounting; do NOT block dispatch path).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep scheduler.ts | head -10 && grep -c ritualResponseWindowSweep src/rituals/scheduler.ts && grep -c "FIRED_NO_RESPONSE\|WINDOW_MISSED" src/rituals/scheduler.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to scheduler.ts
    - grep -c "ritualResponseWindowSweep" src/rituals/scheduler.ts reports >= 3 (1 export declaration + 1 invocation in runRitualSweep + 1 in JSDoc reference)
    - grep -c "FIRED_NO_RESPONSE\|WINDOW_MISSED" src/rituals/scheduler.ts reports >= 2 (one each in the paired emit)
    - grep -E "skipCount: sql" src/rituals/scheduler.ts | wc -l reports >= 1 (the increment)
    - grep -E "await ritualResponseWindowSweep\(now\)" src/rituals/scheduler.ts reports exactly 1 line (the runRitualSweep call site)
    - The ritualResponseWindowSweep function is exported (grep -E "^export async function ritualResponseWindowSweep" matches)
  </acceptance_criteria>
  <done>
    scheduler.ts exports ritualResponseWindowSweep helper. runRitualSweep invokes it before STEP 0. Atomic-consume race-safe per RIT-10 precedent. Idempotent skip_count increment. Test 5 verified by integration test in Task 6.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create skip-tracking integration test fixture + integration test file</name>
  <files>src/rituals/__tests__/fixtures/skip-tracking.ts, src/rituals/__tests__/skip-tracking.integration.test.ts</files>
  <read_first>
    - src/rituals/__tests__/wellbeing.test.ts:44-120 (real-DB harness shape — fixture lifecycle, vi.hoisted mocks for bot.api.sendMessage, FK-ordered cleanup)
    - src/chris/__tests__/engine-pp5.test.ts:23-95 (mockAnthropicCreate vi.hoisted pattern — used as a model even though Plan 28-01 does NOT touch the engine)
    - src/rituals/wellbeing.ts (the seed-fallback fixture pattern — read in full if not already in context)
    - src/db/schema.ts (ritualFireEvents shape; ritualPendingResponses shape — for setup)
    - src/rituals/types.ts post-Task-1 changes (RITUAL_OUTCOME const map for assertions)
  </read_first>
  <behavior>
    - Test fixture seedRitualWithFireEvents(ritualName, outcomes[]): given an ordered array of outcome strings, inserts a rituals row (or finds existing seed) and inserts ritualFireEvents rows with sequential firedAt timestamps. Returns { ritualId, eventIds }.
    - Test 1 — voice-note fire emit: After fireVoiceNote (mocked sendMessage), exactly one ritualFireEvents row with outcome='fired' exists for the test ritual.
    - Test 2 — voice-note suppression emit: shouldSuppressVoiceNoteFire stub returns true; calling fireVoiceNote emits outcome='system_suppressed' and does NOT increment skip_count.
    - Test 3 — voice-note response resets: After recordRitualVoiceResponse (mocked storePensieveEntry), one outcome='responded' row exists AND rituals.skip_count is 0 even after pre-seeding skip_count=5.
    - Test 4 — wellbeing skip preserves count: pre-seed skip_count=2; trigger handleSkip; outcome='wellbeing_skipped' written; skip_count UNCHANGED at 2.
    - Test 5 — wellbeing complete resets count: pre-seed skip_count=3; trigger 3-tap completion; outcome='wellbeing_completed' written; skip_count=0.
    - Test 6 — ritualResponseWindowSweep emits paired window_missed + fired_no_response: insert one expired pending row; call helper; assert 2 ritualFireEvents rows for the ritualId (one each outcome) and skip_count=1.
    - Test 7 — ritualResponseWindowSweep is idempotent: call twice with same expired row pre-seeded; second call emits 0 (already consumed); skip_count remains 1.
    - Test 8 — replay invariant (SKIP-02): seed 5 fired_no_response events + 1 responded event (most recent) interleaved; sum-by-replay since most-recent reset = 0; verifies the projection rule baseline that Plan 28-02 will compute.
    - Cumulative-not-called assertion: afterAll() asserts mockAnthropicCreate.not.toHaveBeenCalled() across the whole describe — proves Plan 28-01 changes do NOT introduce LLM calls anywhere in the skip-tracking substrate (Pitfall 6 carry-over invariant).
  </behavior>
  <action>
    Create src/rituals/__tests__/fixtures/skip-tracking.ts:

    Export an async function seedRitualWithFireEvents(opts: { ritualName: string; cadence: 'daily'|'weekly'; outcomes: RitualFireOutcome[]; baseline?: Date }) that:
       - Looks up rituals row by name; if missing, inserts with sane defaults (next_run_at: tomorrow 21:00, enabled: true, config: { fire_at: '21:00', skip_threshold: 3, mute_until: null, time_zone: 'Europe/Paris', prompt_set_version: 'v1', schema_version: 1 })
       - For each outcome in outcomes[]: inserts a ritualFireEvents row with firedAt = baseline + (i * 1 minute) (deterministic ordering)
       - Returns { ritualId, eventIds: string[] }

    Create src/rituals/__tests__/skip-tracking.integration.test.ts:

    Mirror src/rituals/__tests__/wellbeing.test.ts structure:
       - vi.hoisted mocks for bot.api.sendMessage (mocked to return { message_id: 12345 })
       - vi.mock '../../bot/bot.js' to inject the mocked api
       - vi.hoisted mocks for mockAnthropicCreate, mockAnthropicParse (used in cumulative not-called assertion)
       - vi.mock '../../llm/client.js' to inject mocked anthropic client
       - vi.mock '../../pensieve/store.js' so storePensieveEntry returns { id: 'mock-pensieve-id' } without writing to embeddings/tagger pipelines

    Top-level describe block: 'Phase 28 Plan 01 — skip-tracking substrate (SKIP-01 + SKIP-02)'.
    afterAll: cumulative expect(mockAnthropicCreate).not.toHaveBeenCalled() + cleanup all rituals/ritualFireEvents/ritualPendingResponses/ritualResponses fixture rows + sql.end().
    beforeEach: cleanup; mockReset.

    Implement Tests 1-8 from the behavior list above. Each test uses seedRitualWithFireEvents (or the ritual handlers directly) for setup, executes the handler-under-test, and asserts on db.select(ritualFireEvents) row counts + outcome strings + rituals.skip_count values.

    For Test 8 (replay invariant): import RITUAL_OUTCOME from src/rituals/types.js and seed via seedRitualWithFireEvents([FIRED_NO_RESPONSE, FIRED_NO_RESPONSE, FIRED_NO_RESPONSE, RESPONDED, FIRED_NO_RESPONSE, FIRED_NO_RESPONSE]). Query ritualFireEvents ordered by firedAt DESC, find the most recent RESPONDED, count FIRED_NO_RESPONSE since that timestamp. Assert count === 2 (the two events after the responded reset).

    All test files use the canonical Docker postgres harness — invocation is `bash scripts/test.sh src/rituals/__tests__/skip-tracking.integration.test.ts`.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/rituals/__tests__/skip-tracking.integration.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/test.sh src/rituals/__tests__/skip-tracking.integration.test.ts reports all 8 tests passing in under 30 seconds
    - The afterAll cumulative mockAnthropicCreate.not.toHaveBeenCalled() assertion passes (zero LLM calls leaked into the substrate)
    - grep -c "RITUAL_OUTCOME\." src/rituals/__tests__/skip-tracking.integration.test.ts reports >= 6 (used in setup + assertions)
    - File src/rituals/__tests__/fixtures/skip-tracking.ts exists and exports seedRitualWithFireEvents
    - The fixture file is self-contained (no test-runner-specific imports) — verifiable by `node --check src/rituals/__tests__/fixtures/skip-tracking.ts` not erroring (note: ESM .js suffix imports must work)
  </acceptance_criteria>
  <done>
    Real-DB integration tests cover all 8 Plan 28-01 behaviors. Cumulative not-called assertion guards against LLM-leak regressions. The fixture helper is reusable by Plans 28-02 + 28-03 + 28-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron sweep tick → DB | scheduler.ts ritualResponseWindowSweep writes append-only events; race with concurrent sweep ticks (10:00 + 21:00 + script-driven manual) |
| voice-note PP#5 deposit → DB | recordRitualVoiceResponse writes ritual_fire_events 'responded' + skip_count=0 reset; race with ritualResponseWindowSweep (PP#5 path consumes pending row first; window-sweep loses race silently) |
| ritual_fire_events.outcome string | text NOT NULL column (free-form, NOT enum); typo at write site causes silent skip-count miscount |
| handler `db.insert(ritualFireEvents)` writes | append-only; no FK from ritual_fire_events back to ritual_pending_responses (just metadata.pendingResponseId text reference) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-S1 | Spoofing | ritual_fire_events.outcome string | mitigate | RITUAL_OUTCOME const map with `as const satisfies Record<string, RitualFireOutcome>` (Task 1) — TS compile-time proof every write-site reference is a union member; grep guard at acceptance criterion forbids string-literal outcomes in handler files |
| T-28-T1 | Tampering | rituals.skip_count denormalization | mitigate | D-28-03 — rituals.skip_count is rebuildable from ritual_fire_events by replay. Drift detected by Plan 28-02's computeSkipCount integration test. Plan 28-01 increments skip_count in the SAME function call path as the event insert (sequential, not transactional — accepted tradeoff per D-28-03) |
| T-28-R1 | Repudiation | ritualResponseWindowSweep race-loss | mitigate | Atomic-consume UPDATE-RETURNING pattern (mirrors voice-note.ts:184-204 RIT-10 precedent). Each ritual_pending_responses row consumed at most once. Concurrent sweep + PP#5 calls cannot double-emit fired_no_response |
| T-28-I1 | Information disclosure | metadata.pendingResponseId / pensieveEntryId in ritual_fire_events.metadata | accept | Already operational metadata; not user-facing PII. Existing wellbeing.ts emits already write similar metadata (lines 297, 339) — Plan 28-01 follows precedent |
| T-28-D1 | DoS | unbounded backlog of expired pending rows on cron tick | mitigate | Per-tick LIMIT 50 in ritualResponseWindowSweep (Task 5); backlog drains over subsequent ticks; cron handler wrapped in try/catch so a sweep failure does NOT crash the timer |
| T-28-E1 | Elevation of privilege | none | accept | Plan 28-01 is pure backend (no engine/PP#5 changes; no LLM calls); cumulative mockAnthropicCreate.not.toHaveBeenCalled() asserts no LLM-elevation path |

</threat_model>

<verification>
Phase 28 Plan 01 acceptance:

1. **TS clean**: `npx tsc --noEmit` zero errors attributable to changed files (types.ts, voice-note.ts, wellbeing.ts, weekly-review.ts, scheduler.ts, the new test files).
2. **All 6 tasks' targeted vitest runs green**: types.test.ts (Task 1), skip-tracking.integration.test.ts (Task 6) covers Tasks 2-5 behaviors via real-DB integration.
3. **Full Docker gate**: `bash scripts/test.sh src/rituals/` reports the rituals suite green; substrate gates (0006 + 0007 + 0008 + 0009) all pass; anchor-bias regression guard (scripts/test.sh:168) does NOT fire.
4. **No LLM-leak regression**: The afterAll cumulative `expect(mockAnthropicCreate).not.toHaveBeenCalled()` assertion in skip-tracking.integration.test.ts passes (Pitfall 6 carry-over).
5. **Const-map coverage**: All 3 ritual handler files reference RITUAL_OUTCOME.* (not string literals). Verifiable by grep returning ZERO matches for string literal outcomes in voice-note.ts / wellbeing.ts / weekly-review.ts handler bodies.
6. **Existing behavior preserved**: Phase 27 anchor-bias defeat (D-27-04) regression guard at scripts/test.sh:168 still passes; Phase 26 PP#5 invariant in engine-pp5.test.ts still passes; Phase 25 substrate gate (6|1|3 tables/enum/indexes) still passes.
</verification>

<success_criteria>
ROADMAP success criterion 1 partial coverage:
- "rituals.skip_count is rebuildable from ritual_fire_events by replay" — proven by Test 8 in skip-tracking.integration.test.ts
- "system_suppressed and window_missed outcomes do NOT count toward the threshold" — Plan 28-01 emits both correctly; the threshold filter logic ships in Plan 28-02
- "After 3 consecutive fired_no_response outcomes on a daily ritual..." — Plan 28-01 ships the substrate (events + skip_count increment); the predicate ships in Plan 28-02
</success_criteria>

<output>
After completion, create `.planning/phases/28-skip-tracking-adjustment-dialogue/28-01-SUMMARY.md`. SUMMARY frontmatter MUST include `requirements-completed: [SKIP-01, SKIP-02]` (per PROC-02 upstream verdict in STATE.md) so the milestone audit cross-references correctly.
</output>
