---
context: phase
phase: 32-substrate-hardening
task: null
total_tasks: 6
status: scoped_not_started
last_updated: 2026-05-10T06:56:21.060Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure during the 2026-05-05/10 incident-response cascade. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Test-only workarounds can mask production bugs.** The Plan 30-02 synthetic-fixture test reset `lastRunAt: null` between mock-clock iterations. The executor flagged this as "test-only mitigation; production race-lock contract preserved" — but the contract was actually broken (commit c76cb86 fix). Mitigation: when ANY plan adds a per-iteration reset that mutates state the production code path would never mutate, treat it as a candidate production bug and write the regression test that proves production behavior, not a test-only workaround.
- [ ] CONSTRAINT: **Transitive imports of test files activate their describe blocks.** `live-weekly-review.test.ts` imports `VALIDATION_MARKERS` from `live-integration.test.js` per the D-10 verbatim-import contract. With ANTHROPIC_API_KEY set, vitest evaluates the imported file's `describe.skipIf` gate and runs ALL of live-integration's tests — fanning out to multi-test-file Sonnet spend (~$5+ instead of the budgeted ~$0.45). Mitigation: ALWAYS use `-t <pattern>` filter when invoking live tests with a real API key. Pattern lives in the file's describe block name.
- [ ] CONSTRAINT: **Verifier must check for working-around vs working-with deviations.** The Phase 30 verifier reviewed the 4 Wave-2 deviations but didn't catch that the lastRunAt:null reset was working AROUND the production bug rather than working WITH the contract. Mitigation: when a deviation introduces a state reset between iterations, the verifier MUST trace whether production code performs the same reset; if no, the test is hiding a bug.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| First-fire celebration blindness | Phase 25-30 celebrated rituals firing once in prod (May 5 daily_journal, May 6 daily_wellbeing) without exercising the second-fire path. The bug only manifested after the FIRST fire — but no test, no verification step, no production check covered "fires N times in a row". | blocking | New regression test in idempotency.test.ts ("successive fires at production cadence all succeed — 10 sequential fires, each observing the previous lastRunAt"). Future ritual-related plans MUST include at least one second-fire assertion. |
| Test workaround as production bug mask | See blocking constraint above. The synthetic-fixture's `lastRunAt: null` reset between iterations was the hint that production was broken. | blocking | When reviewing executor deviations, the verifier MUST trace any per-iteration state reset to its production-code analog. If production doesn't perform the reset, the test is hiding a bug. |
| Transitive import activation of live tests | Importing constants from a `live-*.test.ts` file activates that file's describe.skipIf gate when env vars are set. | advisory | Use `-t <describe-pattern>` whenever invoking live tests with real API key. Document this in TESTING.md "Live Tests" subsection. |
| Verbatim duplicate proactive openers under similar trigger contexts | Sonnet produced byte-identical opener "Alors, t'es à Batumi là ? Comment c'est ?" on 2026-05-05 (silence trigger) and 2026-05-07 (commitment trigger), 48h apart. Same prompt + similar context → same low-temperature sample. | advisory | Item #1 of Phase 32 scope addresses this — anti-repetition guard checking last 7d ASSISTANT openers OR explicit instruction to vary surface phrasing. |

<current_state>
**Phase 32 scope decided, NO code work started yet.** Greg invoked /gsd-pause-work immediately after I proposed the 6-item scope and asked "go?" — they want to pause and resume cleanly later, not abandon the plan.

The proposed scope, sequence, and rationale are all captured in HANDOFF.json `decisions[]` and `remaining_tasks[]`. Nothing has been committed for Phase 32 itself yet; the only Phase 32-related artifact in git so far is the ROADMAP entry (commit 9f2b70a + extension in 196f605 + 49280dc).
</current_state>

<completed_work>

Recent session deliverables (M009 final stretch + Phase 30 close + production firefight):

- `be22af0` — drizzle journal `when` typo fix
- `28432bd` — proactive abstention guard (P0 hotfix for the meta-commentary leak)
- `9f2b70a` — Phase 32 ROADMAP entry initial scope (5 items)
- `196f605` — Phase 32 ROADMAP scope extension (5 → 8 items) + cosmetic cleanup
- `49280dc` — TEST-31 first live run failed; captured as Phase 32 item #9 (5 → 9 items)
- `c76cb86` — **production-blocking** tryFireRitualAtomic predicate `lt → lte` fix
- Plus all of Phase 30 wave 1 + 2 (5 + 7 + 1 = 13 commits) — see git log for the chain

All deployed to prod. Container HEAD = c76cb86 as of 2026-05-10 04:13 Paris. All three rituals unstuck via post-deploy catch-up.
</completed_work>

<remaining_work>

Phase 32 scope (in execution order, with deadlines):

1. **#9 weekly_review prompt tightening** (~2h) — DEADLINE 2026-05-10 20:00 Paris (= 18:00 UTC). After this, tonight's first-ever weekly_review fire produces a real Sonnet observation instead of templated fallback. **Deploy after this single item.**
2. **#1 proactive prompt restructure** (~2h) — anti-repetition + section-name avoidance. Addresses the verbatim-duplicate Batumi opener issue.
3. **#2 skip-when-no-USER-in-48h guard in runReflectiveChannel** (~30min) — root-cause fix for the meta-commentary edge case (commit 28432bd was symptom-patch).
4. **#8 src/rituals/journal.ts:357 lastIdx bag-empty branch fix** (~15min) — closes the ~17% consecutive-duplicate window at cycle boundaries.
5. **#3 CI monotonic-`when` guardrail** (~30min) — assert `src/db/migrations/meta/_journal.json` entries' `when` values are monotonically increasing. Prevents the year-stale typo class of bug.
6. **#4 Boot-time __drizzle_migrations drift warning** (~30min) — log warn at boot if `count(*) FROM drizzle.__drizzle_migrations != journal.entries.length`. Surfaces row-loss incidents weeks earlier.

After #6: **second deploy** with all remaining fixes. Then close v2.4 / open v2.5 / start M010.

Total estimated: ~6h focused work.
</remaining_work>

<deferred_work>

Three items dropped from Phase 32 scope per Greg's "fix what needs to be fixed in M009 once and for all" instruction:

- **#5 Forensic investigation of __drizzle_migrations row loss** — cold trail (Postgres logs only show checkpoints), no actionable next step. Defer indefinitely.
- **#6 synth-episodic.ts:288 organic+synth fusion** — test-data substrate, not user-visible M009. Defer to v2.5.1 substrate cleanup OR fold into M010 if M010 touches the synth pipeline.
- **#7 synth-delta.ts wellbeing-per-fused-day generation** — same as #6.

When (6) and (7) eventually ship, the relaxed thresholds in `src/__tests__/fixtures/primed-sanity.test.ts` (`MIN_EPISODIC_SUMMARIES = 4`, wellbeing ≥ 4) revert to the original literal HARN-04/06 spec values (≥ 21, ≥ 14). The TODO(phase-32) markers in that file + `scripts/validate-primed-manifest.ts` + the "Known gap" subsection in `.planning/codebase/TESTING.md` all currently point at Phase 32; if (6)/(7) move to a later phase, those TODO labels should be re-targeted (e.g., `TODO(phase-33)` or `TODO(v2.5.1-test-substrate)`).
</deferred_work>

<decisions_made>

- **Drop items 5, 6, 7 from Phase 32 scope.** Greg's framing was M009 user-visible fixes — these three don't qualify.
- **Sequence #9 first because of tonight's 20:00 Paris weekly_review deadline.** First-ever fire; current prompt fails TEST-31 with 2/3 templated fallbacks on adversarial input.
- **Inline execution, no subagent ceremony.** Greg is tired of process; each item is small.
- **Two deploy points** (after #9 + after all 6) instead of one big-bang or six per-item deploys.
</decisions_made>

<blockers>

None code-blocking. Two soft deadlines:
- 2026-05-10 09:00 Paris (in <5 min from this handoff) — daily_wellbeing first second-fire test in production. Watch the logs to confirm the c76cb86 fix held.
- 2026-05-10 20:00 Paris (~11h from this handoff) — first-ever weekly_review fire. Deploy item #9 before this OR accept that tonight's fire produces the templated fallback.
- 2026-05-10 21:00 Paris (~12h from this handoff) — daily_journal next fire.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state for the resuming agent
2. `.planning/ROADMAP.md` lines 21 (Phase 32 entry, current 9-item scope) — note that scope NARROWED to 6 items but ROADMAP not yet updated to reflect the drops; either update ROADMAP early in resumption OR leave it as upper-bound scope and deliver subset
3. `.planning/phases/30-test-infrastructure-harn-03-refresh/30-VERIFICATION.md` Postscript section — TEST-31 diagnostic (Stage-1 violation + observation length violation → 2/3 fallback) is the spec for item #9
4. `src/rituals/weekly-review-prompt.ts` — the file that needs tightening for item #9
5. `src/rituals/__tests__/live-weekly-review.test.ts` — the test that must pass 3/3 after item #9
6. `src/rituals/__tests__/idempotency.test.ts` — the new regression-test pattern that exercises N successive fires; mirror this approach for any other ritual-related changes
7. `~/.claude/projects/-home-claude-chris/memory/project_m009_first_fire_pending.md` — full incident-response narrative as of 2026-05-10 04:14 Paris

## Critical Anti-Patterns (do NOT repeat these)

- **First-fire celebration blindness** — see Anti-Patterns table above. Always include second-fire+ assertions for any ritual-related plan.
- **Test workaround as production bug mask** — see Anti-Patterns table above.
- **Transitive import activation of live tests** — always use `-t <pattern>` when running live tests with a real API key.

## Infrastructure State

- **chris-chris-1 container** on 192.168.1.50: HEAD = c76cb86, started 2026-05-10 04:11 Paris, healthy. Per-minute sweep cron active. All three rituals unstuck and scheduled correctly.
- **chris-postgres-1 container**: 12 migrations applied (0011 finally applied via drizzle this session). pg_stat shows healthy.
- **rituals state**:
  - daily_wellbeing: last_run_at = 2026-05-10 04:14 Paris (catch-up advance), next_run_at = 2026-05-10 09:00 Paris
  - daily_journal: last_run_at = 2026-05-10 04:13 Paris (catch-up advance), next_run_at = 2026-05-10 21:00 Paris
  - weekly_review: last_run_at = NULL (never fired), next_run_at = 2026-05-10 20:00 Paris (first ever)
- **Local sandbox**: working tree clean. Local postgres NOT running (cleaned up after Phase 30 + idempotency tests). Bring up via `docker compose -f docker-compose.local.yml up -d postgres` when needed.
- **Anthropic API key**: prod key at `ssh root@192.168.1.50 "grep '^ANTHROPIC_API_KEY=' /root/chris/.env | cut -d= -f2-"`. Local `.env` has an EXPIRED key (401s). Always pull the prod key for live tests; always use `-t <pattern>` filter to avoid transitive-test fanout.

## Pre-Execution Critique Required

Not applicable — Phase 32 doesn't have a design artifact, it's direct code fixes from a clear spec.

<context>

Greg's emotional state when /gsd-pause-work fired: tired of M009 dragging out, frustrated about delaying M010, wants the chapter CLOSED. Don't add ceremony when resuming. Don't propose scope re-discussion. Don't run /gsd-plan-phase or /gsd-discuss-phase. Just start item #9 and execute.

The big lift is item #9 (weekly_review prompt tightening). Plan: study the existing prompt + the two failure modes (Stage-1 multi-question + observation >800 chars), write the fix, validate by running TEST-31 with `-t live-weekly-review` and the prod API key, iterate until 3/3 pass, deploy.

Total session time should be 6-8h focused. If interrupted again, the per-item commits leave clean resumption points.
</context>

<next_action>
**Start with item #9 — weekly_review prompt tightening.**

Concrete first commands:
```bash
# Read the current state
sed -n '1,250p' src/rituals/weekly-review-prompt.ts
grep -nE "single.question|≤800|max\\(800\\)|stage1" src/rituals/weekly-review.ts | head -10
# Then study the failure modes
grep -A3 "Stage-1 violation\\|Too big" .planning/phases/30-test-infrastructure-harn-03-refresh/30-VERIFICATION.md
```

Then design + apply the prompt fix. Iterate against TEST-31:
```bash
PROD_KEY=$(ssh root@192.168.1.50 "grep '^ANTHROPIC_API_KEY=' /root/chris/.env | cut -d= -f2-")
RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY="$PROD_KEY" \
  bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts -t live-weekly-review
```

Pass criterion: `expect(fallbacks).toBe(0)` across 3-of-3 atomic iterations.

Commit per the existing convention: `fix(32): tighten weekly_review prompt to prevent fallback under adversarial input`. Then push + deploy via:
```bash
git push origin main
ssh root@192.168.1.50 "cd /root/chris && git pull && docker compose build chris && docker compose up -d chris"
```

After deploy: verify TEST-31 passes again against prod (optional — local pass is the gate). Then proceed to item #1 (proactive prompt restructure).

**Hard deadline for item #9 deploy: 2026-05-10 18:00 UTC = 20:00 Paris.**
</next_action>
