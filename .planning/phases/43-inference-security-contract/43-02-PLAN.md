---
phase: 43-inference-security-contract
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/memory/profiles/shared.ts
  - src/memory/profiles.ts
  - src/memory/profiles/jurisdictional.ts
  - src/memory/profiles/capital.ts
  - src/memory/profiles/health.ts
  - src/memory/profiles/family.ts
  - src/memory/profiles/psychological-shared.ts
  - src/db/schema.ts
  - src/db/migrations/0014_psychological_data_consistency_column.sql
  - src/db/migrations/meta/0014_snapshot.json
  - src/db/migrations/meta/_journal.json
  - scripts/test.sh
  - src/memory/__tests__/profiles.test.ts
  - src/memory/__tests__/profile-prompt.test.ts
  - src/memory/__tests__/psychological-profile-updater.integration.test.ts
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-03]
must_haves:
  truths:
    - "stripMetadataColumns in src/memory/profiles/shared.ts:321-337 destructures and discards `dataConsistency: _dataConsistency` alongside the existing `confidence: _confidence` (D-09)"
    - "stripMetadataColumns in src/memory/profiles.ts:215-231 — the symmetry copy — applies the IDENTICAL edit (CONTRACT-01 requires BOTH sites)"
    - "extractJurisdictionalPrevState (jurisdictional.ts:62-67), extractCapitalPrevState (capital.ts:40-44), extractHealthPrevState (health.ts:37-41), extractFamilyPrevState (family.ts:38-42) ALL return null when row.substrateHash === '' (Phase 33 D-11 seed-row sentinel; D-10)"
    - "assembleProfilePrompt for any of the 4 operational dimensions called with prevState=null OMITS the '## CURRENT PROFILE STATE' block entirely (already structurally implemented; CONTRACT-02 leverages this existing behavior)"
    - "profile_hexaco, profile_schwartz, profile_attachment tables each have a new `data_consistency` real column with NOT NULL DEFAULT 0 + CHECK (data_consistency >= 0 AND data_consistency <= 1) (D-12)"
    - "Migration `src/db/migrations/0014_psychological_data_consistency_column.sql` exists and applies cleanly against a fresh postgres (D-15)"
    - "scripts/test.sh has MIGRATION_14_SQL defined and applied in the migration sequence (HARD CO-LOC discipline)"
    - "src/memory/profiles/psychological-shared.ts:619-628 upsertValues includes `dataConsistency: sonnetOut.data_consistency` (D-14) — Sonnet's emission persists per fire"
    - "profile_history snapshot picks up the new column automatically (verified by inspection — the full-row copy at shared.ts:495-501 is polymorphic)"
    - "drizzle-kit meta snapshot 0014_snapshot.json + _journal.json entry are present and consistent with schema.ts (HARD CO-LOC #M11-1 lineage pattern)"
  artifacts:
    - path: "src/memory/profiles/shared.ts"
      provides: "stripMetadataColumns with dataConsistency in discard list"
      contains: "dataConsistency: _dataConsistency"
    - path: "src/memory/profiles.ts"
      provides: "stripMetadataColumns symmetry copy with dataConsistency in discard list"
      contains: "dataConsistency"
    - path: "src/memory/profiles/jurisdictional.ts"
      provides: "extractJurisdictionalPrevState returns null on seed-row sentinel"
      contains: "substrateHash === ''"
    - path: "src/memory/profiles/capital.ts"
      provides: "extractCapitalPrevState returns null on seed-row sentinel"
      contains: "substrateHash === ''"
    - path: "src/memory/profiles/health.ts"
      provides: "extractHealthPrevState returns null on seed-row sentinel"
      contains: "substrateHash === ''"
    - path: "src/memory/profiles/family.ts"
      provides: "extractFamilyPrevState returns null on seed-row sentinel"
      contains: "substrateHash === ''"
    - path: "src/db/schema.ts"
      provides: "profileHexaco + profileSchwartz + profileAttachment each have dataConsistency column + CHECK constraint"
      contains: "profile_hexaco_data_consistency_bounds"
    - path: "src/db/migrations/0014_psychological_data_consistency_column.sql"
      provides: "ALTER TABLE ADD COLUMN + ADD CONSTRAINT for 3 psych tables"
      min_lines: 15
      contains: "ALTER TABLE \"profile_hexaco\""
    - path: "src/db/migrations/meta/0014_snapshot.json"
      provides: "drizzle-kit meta snapshot consistent with schema.ts edits"
    - path: "scripts/test.sh"
      provides: "MIGRATION_14_SQL definition + psql apply line"
      contains: "MIGRATION_14_SQL"
    - path: "src/memory/profiles/psychological-shared.ts"
      provides: "upsertValues includes dataConsistency: sonnetOut.data_consistency"
      contains: "dataConsistency: sonnetOut.data_consistency"
  key_links:
    - from: "src/db/migrations/0014_psychological_data_consistency_column.sql"
      to: "src/db/schema.ts"
      via: "drizzle-kit generate consistency — schema.ts column adds match the SQL ALTER TABLE adds"
      pattern: "data_consistency.*real.*notNull.*default\\(0\\)"
    - from: "src/memory/profiles/psychological-shared.ts"
      to: "src/db/schema.ts"
      via: "Drizzle ORM — dataConsistency key in upsertValues maps to the new data_consistency column"
      pattern: "dataConsistency:.*sonnetOut\\.data_consistency"
    - from: "src/db/migrations/meta/_journal.json"
      to: "src/db/migrations/0014_psychological_data_consistency_column.sql"
      via: "appended entry with idx: 14, tag: 0014_psychological_data_consistency_column"
      pattern: "0014_psychological_data_consistency_column"
---

<objective>
Close the three CONTRACT-class defects: (CONTRACT-01) strip `dataConsistency` from prevState before Sonnet sees it; (CONTRACT-02) omit the `## CURRENT PROFILE STATE` block entirely on first-fire-after-deploy across all 4 operational dimensions; (CONTRACT-03) persist Sonnet's emitted `data_consistency` to a new column on each psychological profile table so future CONS-01 host-side consistency math has a queryable historical signal.

Purpose: Restore the "host computes, you don't emit" contract that Phase 34's hybrid confidence model depends on. Eliminate the worst-case anchoring for the M010-03 profile-drift threat (Sonnet sees empty fields + anti-drift directive on first fire → drifts toward the empty seed). Unblock CONS-01 / CONS-02 v2.7 backlog by giving them a queryable per-row signal.

Output: 2 stripMetadataColumns edits (operational shared.ts + profiles.ts symmetry copy), 4 extract<X>PrevState edits (1 line each), 3 schema.ts column additions + 3 CHECK constraints, 1 NEW SQL migration (`0014_psychological_data_consistency_column.sql`) + drizzle-kit meta regen, 1 scripts/test.sh apply line, 1 upsertValues edit (psychological-shared.ts), test extensions covering all three contracts.

Satisfies: CONTRACT-01 (stripMetadataColumns) + CONTRACT-02 (extract<X>PrevState null-on-seed) + CONTRACT-03 (data_consistency persistence + migration 0014).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-inference-security-contract/43-CONTEXT.md
@.planning/phases/43-inference-security-contract/43-PATTERNS.md
@.planning/phases/43-inference-security-contract/43-01-PLAN.md
@.planning/milestones/v2.5-phases/34-inference-engine/34-REVIEW.md
@.planning/milestones/v2.6-phases/38-psychological-inference-engine/38-REVIEW.md

# Implementation surfaces (pre-existing — verify before editing)
@src/memory/profiles/shared.ts
@src/memory/profiles.ts
@src/memory/profiles/jurisdictional.ts
@src/memory/profiles/capital.ts
@src/memory/profiles/health.ts
@src/memory/profiles/family.ts
@src/memory/profiles/psychological-shared.ts
@src/db/schema.ts
@src/db/migrations/0013_psychological_profiles.sql
@scripts/test.sh
@scripts/regen-snapshots.sh
@src/memory/__tests__/profiles.test.ts
@src/memory/__tests__/profile-prompt.test.ts
@src/memory/__tests__/psychological-profile-updater.integration.test.ts

<interfaces>
<!-- Contracts this plan locks: -->

// CONTRACT-01: stripMetadataColumns (both copies) returns Record<string, unknown>
// that does NOT contain the keys `confidence` or `data_consistency` (snake_case
// post-conversion) regardless of input row shape.

// CONTRACT-02: extract<X>PrevState returns null when:
//   - row is null/undefined (existing behavior), OR
//   - row.substrateHash === '' (new — Phase 33 D-11 seed-row sentinel)
// When null is returned, assembleProfilePrompt(dim, substrate, null, n) omits
// the '## CURRENT PROFILE STATE' block per Phase 34 D-07 (already implemented).

// CONTRACT-03: psychological_shared upsertValues object includes:
//   dataConsistency: sonnetOut.data_consistency
// where sonnetOut.data_consistency is the number Sonnet emitted (boundary v4
// schema; already present in the parsed output — line 643 already logs it).
// The new schema.ts column is `dataConsistency: real('data_consistency')
// .notNull().default(0)` on profileHexaco, profileSchwartz, profileAttachment;
// the CHECK constraint is `${table.dataConsistency} >= 0 AND ${table.dataConsistency} <= 1`.

// Migration 0014 HARD CO-LOC: schema.ts edit + .sql file + meta/0014_snapshot.json
// + meta/_journal.json entry + scripts/test.sh MIGRATION_14_SQL line ALL ship in
// this single plan atomically (Phase 25 D-03 lineage pattern; HARD CO-LOC #M11-1
// established in Phase 37 Plan 01 — repeating here for migration 0014).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dataConsistency to stripMetadataColumns discard list — both call sites (CONTRACT-01)</name>
  <read_first>
    - src/memory/profiles/shared.ts (lines 321-337 — current stripMetadataColumns)
    - src/memory/profiles.ts (lines 215-231 — symmetry copy of stripMetadataColumns)
    - .planning/phases/43-inference-security-contract/43-CONTEXT.md §D-09 (locked edit pattern: add `dataConsistency: _dataConsistency` to destructure + `void _dataConsistency` alongside the existing `_confidence`)
    - .planning/phases/43-inference-security-contract/43-PATTERNS.md §`src/memory/profiles/shared.ts — add dataConsistency to stripMetadataColumns` (target pattern verbatim)
  </read_first>
  <files>
    - src/memory/profiles/shared.ts
    - src/memory/profiles.ts
    - src/memory/__tests__/profiles.test.ts
  </files>
  <action>
    1. Edit `src/memory/profiles/shared.ts` `stripMetadataColumns` (lines 321-337):

       Change the destructure block from:
       ```typescript
       const {
         id, name, schemaVersion, substrateHash, confidence: _confidence,
         lastUpdated, createdAt,
         ...rest
       } = row;
       void id; void name; void schemaVersion; void substrateHash;
       void _confidence; void lastUpdated; void createdAt;
       ```
       To:
       ```typescript
       const {
         id, name, schemaVersion, substrateHash,
         confidence: _confidence, dataConsistency: _dataConsistency,
         lastUpdated, createdAt,
         ...rest
       } = row;
       void id; void name; void schemaVersion; void substrateHash;
       void _confidence; void _dataConsistency; void lastUpdated; void createdAt;
       ```

    2. Apply the identical edit to `src/memory/profiles.ts:215-231` (the symmetry copy).
       Note that this copy currently uses `confidence` (no `_` prefix) — adjust to `confidence: _confidence, dataConsistency: _dataConsistency` for consistency with shared.ts. Update the matching `void confidence;` to `void _confidence; void _dataConsistency;` block.

    3. In `src/memory/__tests__/profiles.test.ts`, ADD a new `describe('CONTRACT-01: stripMetadataColumns discards dataConsistency', ...)` block. Since `stripMetadataColumns` is module-private in both files, test indirectly:
       - For the `src/memory/profiles.ts` site: call `getOperationalProfiles()` against a fixture row that has `dataConsistency: 0.55` and `confidence: 0.42` on the DB side. Verify the returned profile data (the snake_case jsonb shape after strip) does NOT contain a `data_consistency` key.
       - For the `src/memory/profiles/shared.ts` site: this strip is called inside `runProfileGenerator` before the prompt is assembled. Add an assertion in an EXISTING profile-prompt.test.ts test (or in profiles.test.ts via a direct call to `extractJurisdictionalPrevState` — which is exported from `jurisdictional.ts` via the config, accessible by importing JURISDICTIONAL_PROFILE_CONFIG). Verify a row with both `confidence` and `dataConsistency` set returns a stripped object containing NEITHER key (snake_case form).

       Tag the new test with the comment `// CONTRACT-01 / D-09 regression — Phase 34 BL-02 closure` to make the test searchable.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/__tests__/profiles.test.ts src/memory/__tests__/profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dataConsistency: _dataConsistency" src/memory/profiles/shared.ts` returns 1
    - `grep -c "dataConsistency" src/memory/profiles.ts` returns >= 1 (in the stripMetadataColumns destructure)
    - `grep -c "void _dataConsistency" src/memory/profiles/shared.ts` returns 1
    - `grep -c "CONTRACT-01" src/memory/__tests__/profiles.test.ts` returns >= 1
    - `bash scripts/test.sh src/memory/__tests__/profiles.test.ts` exits 0 (existing tests still pass; new regression covers the discard)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0 (no regression to Plan 43-01 work)
  </acceptance_criteria>
  <done>
    Both stripMetadataColumns sites discard dataConsistency. Regression test green. Commit message: `fix(43-02): CONTRACT-01 — strip dataConsistency from prevState (D-09)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add substrateHash === '' null-return to all 4 extract<X>PrevState functions (CONTRACT-02)</name>
  <read_first>
    - src/memory/profiles/jurisdictional.ts (lines 56-67 — current extractJurisdictionalPrevState)
    - src/memory/profiles/capital.ts (lines 36-45 — extractCapitalPrevState; verify line range before editing)
    - src/memory/profiles/health.ts (lines 33-42 — extractHealthPrevState)
    - src/memory/profiles/family.ts (lines 34-43 — extractFamilyPrevState)
    - .planning/phases/43-inference-security-contract/43-CONTEXT.md §D-10 (one-line edit pattern + comment update) + §D-11 (test additions per dimension)
    - .planning/phases/43-inference-security-contract/43-PATTERNS.md §`src/memory/profiles/{jurisdictional,capital,health,family}.ts — extract<X>PrevState null-on-seed`
    - src/memory/profile-prompt.ts (line 282-303 — buildPreviousStateBlock; verify the assembler already omits the block when prevState is null — this is the structural test invariant at profile-prompt.test.ts line 155-159)
  </read_first>
  <files>
    - src/memory/profiles/jurisdictional.ts
    - src/memory/profiles/capital.ts
    - src/memory/profiles/health.ts
    - src/memory/profiles/family.ts
    - src/memory/__tests__/profile-prompt.test.ts
  </files>
  <action>
    For EACH of the 4 files (`jurisdictional.ts`, `capital.ts`, `health.ts`, `family.ts`), apply the same edit to `extract<X>PrevState`:

    Change from (jurisdictional.ts example):
    ```typescript
    function extractJurisdictionalPrevState(
      row: Record<string, unknown> | null,
    ): unknown | null {
      if (!row) return null;
      return stripMetadataColumns(row);
    }
    ```

    To:
    ```typescript
    /**
     * Map the camelCase DB row → snake_case jsonb subset for the prompt's
     * previous-state block. Returns null when:
     *   - the row is missing entirely (table never seeded), OR
     *   - row.substrateHash === '' (Phase 33 D-11 seed-row sentinel — first
     *     fire ever; M010-03 anti-drift defense: assembleProfilePrompt omits
     *     the ## CURRENT PROFILE STATE block when prevState is null,
     *     avoiding the empty-fields + anti-drift directive collision).
     */
    function extractJurisdictionalPrevState(
      row: Record<string, unknown> | null,
    ): unknown | null {
      if (!row) return null;
      if (row.substrateHash === '') return null;
      return stripMetadataColumns(row);
    }
    ```

    Replace the misleading existing comment ("Phase 33 seed rows are returned non-null so Sonnet sees 'insufficient data' markers from the seed") — the new behavior is OPPOSITE.

    In `src/memory/__tests__/profile-prompt.test.ts`, ADD a `describe('CONTRACT-02: seed-row first-fire omits ## CURRENT PROFILE STATE', ...)` block parametrized over all 4 dimensions (D-11 mandates `.every()` not `.some()` — every dimension must independently pass):

    ```typescript
    const SEED_ROW_SHAPE = {
      id: 'uuid', name: 'primary', schemaVersion: 1,
      substrateHash: '',  // Phase 33 D-11 sentinel
      confidence: 0, dataConsistency: 0,
      lastUpdated: new Date(), createdAt: new Date(),
      // dim-specific jsonb fields with seed values:
      currentCountry: null, physicalLocation: null, residencyStatus: [], /* etc */
    };

    describe.each([
      ['jurisdictional', JURISDICTIONAL_PROFILE_CONFIG],
      ['capital', CAPITAL_PROFILE_CONFIG],
      ['health', HEALTH_PROFILE_CONFIG],
      ['family', FAMILY_PROFILE_CONFIG],
    ] as const)('CONTRACT-02: seed-row first-fire — %s', (dim, config) => {
      it('extract<X>PrevState returns null for substrateHash === ""', () => {
        const result = config.extractPrevState(SEED_ROW_SHAPE);
        expect(result).toBeNull();
      });

      it('assembled prompt omits ## CURRENT PROFILE STATE on seed-row first fire', () => {
        const substrate = buildFixture();  // standard substrate
        const prevState = config.extractPrevState(SEED_ROW_SHAPE);  // null
        const result = assembleProfilePrompt(dim, substrate, prevState, substrate.pensieveEntries.length);
        expect(result.system).not.toContain('## CURRENT PROFILE STATE');
      });
    });
    ```

    Note: each per-dimension config object (JURISDICTIONAL_PROFILE_CONFIG etc.) exports `extractPrevState` via the `ProfileGeneratorConfig` shape — verify the export is reachable in the test file. The dim-specific JSONB seed fields must match the table schema (e.g., capital uses `fiPhase`, health uses `openHypotheses`); the test fixture for each dimension may need per-dim shape adjustments — use the existing Phase 33 seed-row pattern from `src/db/migrations/0012_operational_profiles.sql` lines for inserts.

    Run `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` after the source edits. All 4 dimensions × 2 assertions = 8 new tests should be GREEN; existing tests untouched.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files contain `if (row.substrateHash === '') return null;` — verify with: `grep -c "row.substrateHash === ''" src/memory/profiles/jurisdictional.ts src/memory/profiles/capital.ts src/memory/profiles/health.ts src/memory/profiles/family.ts | awk -F: '{n+=$2} END{print (n==4?"OK":"FAIL")}'` prints OK
    - None of the 4 files retain the old misleading comment "Phase 33 seed rows are returned non-null" — `! grep -l "Phase 33 seed rows are returned non-null" src/memory/profiles/*.ts`
    - `grep -c "CONTRACT-02" src/memory/__tests__/profile-prompt.test.ts` returns >= 1
    - `grep -E "describe\\.each.*jurisdictional.*capital.*health.*family" src/memory/__tests__/profile-prompt.test.ts` matches OR equivalent parametrization present (the `.every()` discipline from D-11 — all 4 dimensions independently asserted, NOT a weak `.some()`)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0 (8+ new tests pass GREEN; no existing test regression)
  </acceptance_criteria>
  <done>
    All 4 dimensions omit ## CURRENT PROFILE STATE on seed-row first fire. M010-03 anti-drift anchoring defense verified at unit level for every dimension. Commit message: `fix(43-02): CONTRACT-02 — extract<X>PrevState null on seed-row sentinel (D-10)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Schema.ts column adds + SQL migration 0014 + drizzle-kit meta regen + test.sh apply line (CONTRACT-03 migration — HARD CO-LOC)</name>
  <read_first>
    - src/db/schema.ts (lines 540-738 — current operational + psychological profile table defs; the operational dataConsistency pattern at line 549, 563 is the template)
    - src/db/migrations/0013_psychological_profiles.sql (full file — the CREATE TABLE pattern for the 3 psych tables this migration alters; useful for the column-name and quoting convention)
    - src/db/migrations/0006_rituals_wellbeing.sql (search for ALTER TABLE ADD COLUMN pattern — Phase 25 introduced the column-add form used here)
    - scripts/test.sh (lines 1-90 — migration apply pattern; lines 21 + 86 are the MIGRATION_13_SQL definition + psql apply line that this task mirrors for MIGRATION_14_SQL)
    - scripts/regen-snapshots.sh (lines 1-50 + 250-280 — drizzle-kit meta regen flow; this is the BLOCKING step that writes 0014_snapshot.json + appends _journal.json)
    - src/db/migrations/meta/_journal.json (current tail — verify the format of the appended entry: idx=14, version="7", tag="0014_psychological_data_consistency_column", breakpoints: true)
    - .planning/phases/43-inference-security-contract/43-CONTEXT.md §D-12 + §D-15 + §D-16 (locked column shape + migration numbering)
    - .planning/phases/43-inference-security-contract/43-PATTERNS.md §`src/db/migrations/0014_psychological_data_consistency_column.sql` (target SQL verbatim)
  </read_first>
  <files>
    - src/db/schema.ts
    - src/db/migrations/0014_psychological_data_consistency_column.sql (NEW)
    - src/db/migrations/meta/0014_snapshot.json (NEW — regen output)
    - src/db/migrations/meta/_journal.json (appended)
    - scripts/test.sh
  </files>
  <action>
    This task is an ATOMIC HARD CO-LOC bundle. All 5 file edits MUST land in a single commit. Splitting any of them reproduces the M010 PITFALL M010-11 / TECH-DEBT-19-01 lineage breakage.

    1. **schema.ts edits** — for each of `profileHexaco` (lines 662-684), `profileSchwartz` (lines 686-712), `profileAttachment` (lines 714-738):

       a. Add the column AFTER `overallConfidence` (so the column ordering in the migration journal matches schema.ts order):
       ```typescript
       overallConfidence: real('overall_confidence').notNull().default(0),
       dataConsistency: real('data_consistency').notNull().default(0),  // CONTRACT-03 / D-12
       ```

       b. Add the CHECK constraint inside the table-constraints array (where the existing `profile_<table>_overall_confidence_bounds` check lives):
       ```typescript
       check('profile_hexaco_data_consistency_bounds',
         sql`${table.dataConsistency} >= 0 AND ${table.dataConsistency} <= 1`),
       ```
       (substitute table name for each of the 3 tables: `profile_hexaco_data_consistency_bounds`, `profile_schwartz_data_consistency_bounds`, `profile_attachment_data_consistency_bounds`).

    2. **Create `src/db/migrations/0014_psychological_data_consistency_column.sql`** with the verbatim SQL from 43-PATTERNS.md (3 ALTER TABLE ADD COLUMN + 3 ADD CONSTRAINT statements). Include the leading SQL comment block citing D-15 + D-16 + the operational symmetry.

    3. **Run `bash scripts/regen-snapshots.sh`** — this regenerates `src/db/migrations/meta/0014_snapshot.json` and appends an entry to `_journal.json` with `idx: 14, version: "7", tag: "0014_psychological_data_consistency_column", breakpoints: true`. Verify both files updated by inspecting the output of `git status src/db/migrations/meta/`.

    4. **Edit scripts/test.sh** — add 2 new lines:
       - After line 21 (`MIGRATION_13_SQL=...`), add:
         ```bash
         MIGRATION_14_SQL="src/db/migrations/0014_psychological_data_consistency_column.sql"
         ```
       - After line 86 (the `psql ... < "$MIGRATION_13_SQL"` block), add the symmetric apply line:
         ```bash
         docker compose -f "$COMPOSE_FILE" exec -T postgres \
           psql -U chris -d chris -v ON_ERROR_STOP=1 -q < "$MIGRATION_14_SQL"
         ```

    5. Verify the migration applies cleanly by running `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` — this triggers the full Docker bring-up + migration apply path. If the migration fails (syntax error, constraint conflict), this run will surface it BEFORE any test runs.

    Do NOT proceed to Task 4 if `regen-snapshots.sh` reports inconsistency between schema.ts and the generated snapshot — the lineage MUST be clean for Plan 43-02 to ship.
  </action>
  <verify>
    <automated>bash scripts/regen-snapshots.sh --check-only &amp;&amp; bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dataConsistency: real" src/db/schema.ts | awk '$1>=7 {print "OK"}'` prints OK (4 existing operational + 3 new psychological = 7+ matches)
    - `grep -c "profile_hexaco_data_consistency_bounds\|profile_schwartz_data_consistency_bounds\|profile_attachment_data_consistency_bounds" src/db/schema.ts | awk '$1>=3 {print "OK"}'` prints OK
    - `test -f src/db/migrations/0014_psychological_data_consistency_column.sql` (file exists)
    - `grep -c "ALTER TABLE \"profile_hexaco\"\|ALTER TABLE \"profile_schwartz\"\|ALTER TABLE \"profile_attachment\"" src/db/migrations/0014_psychological_data_consistency_column.sql | awk '$1>=6 {print "OK"}'` prints OK (each table gets ADD COLUMN + ADD CONSTRAINT = 2 statements × 3 tables = 6)
    - `test -f src/db/migrations/meta/0014_snapshot.json` (meta snapshot regenerated)
    - `grep -c "0014_psychological_data_consistency_column" src/db/migrations/meta/_journal.json` returns 1 (journal entry appended)
    - `grep -c "MIGRATION_14_SQL" scripts/test.sh` returns >= 2 (definition + apply line)
    - `bash scripts/regen-snapshots.sh --check-only` exits 0 (schema.ts ↔ meta snapshot consistency verified)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0 (migration applies cleanly in fresh Docker postgres + no test regression)
  </acceptance_criteria>
  <done>
    Migration 0014 + schema.ts + drizzle meta + test.sh ALL ship atomically. HARD CO-LOC #M11-1 discipline satisfied. Commit message: `feat(43-02): CONTRACT-03 — migration 0014_psychological_data_consistency_column (D-15)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire dataConsistency: sonnetOut.data_consistency into psychological-shared.ts upsertValues (CONTRACT-03 persistence)</name>
  <read_first>
    - src/memory/profiles/psychological-shared.ts (lines 600-648 — the upsert block; verify line 619-628 upsertValues object shape matches CONTEXT.md §D-14 description; verify `sonnetOut.data_consistency` is already in scope on line 643)
    - .planning/phases/43-inference-security-contract/43-CONTEXT.md §D-14 (locked: add one key to upsertValues, sonnetOut.data_consistency is already a parsed boundary v4 field)
    - .planning/phases/43-inference-security-contract/43-PATTERNS.md §`src/memory/profiles/psychological-shared.ts — persist data_consistency in upsert`
    - src/memory/__tests__/psychological-profile-updater.integration.test.ts (existing integration test — pattern for how the upsert is exercised against real Docker postgres; this task EXTENDS this test)
  </read_first>
  <files>
    - src/memory/profiles/psychological-shared.ts
    - src/memory/__tests__/psychological-profile-updater.integration.test.ts
  </files>
  <action>
    1. Edit `src/memory/profiles/psychological-shared.ts` (around line 619-628):

       Insert the `dataConsistency` key into upsertValues immediately AFTER `overallConfidence`:
       ```typescript
       const upsertValues: Record<string, unknown> = {
         name: 'primary',
         schemaVersion: (currentRow.schemaVersion as number | undefined) ?? 1,
         substrateHash: computedHash,
         overallConfidence: sonnetOut.overall_confidence,
         dataConsistency: sonnetOut.data_consistency,  // CONTRACT-03 / D-14 — persists Sonnet's emission
         wordCount: substrate.wordCount,
         wordCountAtLastRun: substrate.wordCount,
         ...flatEncoded,
         lastUpdated: new Date(),
       };
       ```

       `sonnetOut.data_consistency` is already a typed number on the boundary v4 parsed output (line 643 already logs `dataConsistency: sonnetOut.data_consistency`). No additional Zod / type change needed.

    2. In `src/memory/__tests__/psychological-profile-updater.integration.test.ts`, EXTEND the existing 3-cycle integration test (or add a new test if a 3-cycle pattern doesn't exist for this assertion class) to assert post-fire:

       ```typescript
       // CONTRACT-03 / D-14 regression — Phase 38 WR-02 closure
       const profileRow = await db.select().from(profileHexaco).where(eq(profileHexaco.name, 'primary')).limit(1);
       expect(profileRow[0]).toBeDefined();
       expect(typeof profileRow[0].dataConsistency).toBe('number');
       expect(profileRow[0].dataConsistency).toBeGreaterThanOrEqual(0);
       expect(profileRow[0].dataConsistency).toBeLessThanOrEqual(1);
       ```

       (Same assertion shape parametrized over profile_hexaco + profile_schwartz + profile_attachment if all three are exercised in the integration test; if only hexaco is in scope of this test file's current fixtures, hexaco is sufficient — schwartz + attachment columns are verified by the migration-applies-cleanly gate in Task 3.)

       Tag the new block with `// CONTRACT-03 / D-14 — Phase 38 WR-02 closure`.

    Run `bash scripts/test.sh src/memory/__tests__/psychological-profile-updater.integration.test.ts` after the edits to verify the integration test still passes against the now-migrated DB.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/__tests__/psychological-profile-updater.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dataConsistency: sonnetOut.data_consistency" src/memory/profiles/psychological-shared.ts` returns 1
    - `grep -c "CONTRACT-03" src/memory/__tests__/psychological-profile-updater.integration.test.ts` returns >= 1
    - `grep -E "expect.*dataConsistency.*toBeGreaterThanOrEqual\(0\)" src/memory/__tests__/psychological-profile-updater.integration.test.ts` matches (CHECK-constraint-equivalent bound assertion present)
    - `bash scripts/test.sh src/memory/__tests__/psychological-profile-updater.integration.test.ts` exits 0 (integration cycle still green after column add + upsert change)
  </acceptance_criteria>
  <done>
    Sonnet's data_consistency emission persists to the new column on every psychological fire. Integration test confirms. Commit message: `fix(43-02): CONTRACT-03 — persist data_consistency in psychological upsert (D-14)`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Plan-level Docker test gate — full suite green confirms migration + 3 CONTRACT defenses + INJ from Plan 43-01 all coexist cleanly</name>
  <read_first>
    - scripts/test.sh (verify all 14 migrations apply in sequence: 0000..0013 + new 0014)
    - .planning/phases/43-inference-security-contract/43-CONTEXT.md §D-17 + §D-18 (real postgres, no live Sonnet calls)
  </read_first>
  <files>(no files written — verification gate only)</files>
  <action>
    Run `bash scripts/test.sh` (FULL suite — not scoped to src/memory/ this time, because the schema change ripples into any test that touches the psych profile tables). Per user memory `feedback_always_run_docker_tests.md`: never skip the full Docker test pass.

    Expected outcome: full suite GREEN at the v2.6 baseline (1412+ passed / 53 skipped from Phase 33 carry-forward, plus this phase's new tests). Specifically verify:
    - Migration 0014 applies cleanly during `docker compose ... psql ... < "$MIGRATION_14_SQL"` in scripts/test.sh
    - No type errors against the new schema.ts columns
    - The psychological upsert path persists `dataConsistency` (integration test confirms)
    - All Plan 43-01 INJ-01 + INJ-02 tests still GREEN (no cross-contamination with the CONTRACT edits)
    - No legacy test breaks because of the new `data_consistency` column (the `strict()` parse path may need a Zod schema update if the boundary v4 schema doesn't already include the field; check by inspecting the test output for `schema_mismatch` warns — if any appear on `data_consistency`, surface the issue and add a follow-up task to update `psychological-schemas.ts` v3 boundary to include `data_consistency` in `.strict()` allowed keys)

    On any unexpected failure, do NOT commit. Surface the failure and decide whether it's a real regression (fix in this task) or a baseline flakiness (skip with a note + verify the failing test is unrelated).
  </action>
  <verify>
    <automated>bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` exits 0 (full suite GREEN against fresh Docker postgres + all 14 migrations)
    - `psql ... -c "SELECT data_consistency FROM profile_hexaco WHERE name='primary'"` returns a numeric value in [0, 1] (verified by integration test, but the schema is live)
    - `git status` shows the expected files modified (no unrelated drift): src/memory/profiles/shared.ts, src/memory/profiles.ts, src/memory/profiles/{jurisdictional,capital,health,family}.ts, src/memory/profiles/psychological-shared.ts, src/db/schema.ts, src/db/migrations/0014_psychological_data_consistency_column.sql, src/db/migrations/meta/0014_snapshot.json, src/db/migrations/meta/_journal.json, scripts/test.sh, src/memory/__tests__/profiles.test.ts, src/memory/__tests__/profile-prompt.test.ts, src/memory/__tests__/psychological-profile-updater.integration.test.ts
    - No new test failures vs. the post-Plan-43-01 baseline
  </acceptance_criteria>
  <done>
    Full Docker suite green. All 5 v2.6.1 Phase 43 requirements (INJ-01, INJ-02, CONTRACT-01, CONTRACT-02, CONTRACT-03) implemented. Plan 43-02 ready to commit. Phase 43 ready for code-review. Commit message: `chore(43-02): full Docker test gate green — CONTRACT-01 + CONTRACT-02 + CONTRACT-03 ship`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row → prompt prevState | `extract<X>PrevState` reads the current DB row and returns the snake_case jsonb subset for the prompt. CONTRACT-01 ensures `data_consistency` is stripped (host-only field). CONTRACT-02 ensures seed-row first-fire returns null (no anchor-collision with anti-drift directive). |
| Sonnet emission → DB | Sonnet's `data_consistency` field is parsed at the boundary v4 schema. CONTRACT-03 persists it to a new column with a CHECK 0..1 constraint (defense-in-depth behind the Zod parse). |
| Migration apply path | `scripts/test.sh` brings up fresh Docker postgres and applies migrations 0000..0014 in order. Phase 43 adds 0014; HARD CO-LOC mandates schema.ts + .sql + meta + test.sh apply line all ship in one commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02-01 | Information Disclosure | stripMetadataColumns leaks dataConsistency back to Sonnet | mitigate | Two destructure edits (shared.ts + profiles.ts). Regression test asserts the snake_case `data_consistency` key is absent from stripped output. |
| T-43-02-02 | Tampering | Seed-row first fire anchors Sonnet to empty fields under anti-drift directive (M010-03) | mitigate | `extract<X>PrevState` returns null on `substrateHash === ''`. assembleProfilePrompt omits the prevState block when null. 4 unit tests + 4 integration tests assert per-dimension. |
| T-43-02-03 | Tampering | Migration 0014 fails to apply (constraint conflict, name collision) | mitigate | `regen-snapshots.sh --check-only` verifies schema.ts ↔ meta lineage. Full Docker test gate (Task 5) re-applies all 14 migrations in fresh DB. Idempotent ADD COLUMN IF NOT EXISTS handles re-apply path (constraint ADD is not idempotent per PostgreSQL — accepted per Phase 25 MD-02 pattern). |
| T-43-02-04 | Information Disclosure | dataConsistency column added but Sonnet's emission still discarded at upsert | mitigate | upsertValues edit + integration test asserts the persisted value is in [0, 1] after a fire. |
| T-43-02-05 | Repudiation | Audit trail loses dataConsistency history because profile_history snapshot doesn't capture it | accept | profile_history is a polymorphic full-row copy (shared.ts:495-501). Once the column exists on profile_hexaco / profile_schwartz / profile_attachment, snapshots include it automatically — no schema change to history table needed. |
| T-43-02-06 | Denial of Service | Migration 0014 against a populated production DB blocks writes for ALTER TABLE duration | accept | Phase 38 deploy was 2026-05-13; psych profile tables have 0 rows on production today (cron-fired-but-no-row baseline). ALTER TABLE ADD COLUMN with DEFAULT on empty tables is sub-millisecond. The first psych cron fire (Sun 2026-05-17 22:00 Paris per project memory `project_m010_phase34_deployed.md`) will be the first INSERT. Migration applies before any rows exist. |
| T-43-02-07 | Tampering | scripts/test.sh MIGRATION_14_SQL apply line missing → fresh test runs fail because schema.ts expects the column but DB doesn't have it | mitigate | Task 3 acceptance criteria explicitly grep for `MIGRATION_14_SQL` in scripts/test.sh; Task 5 full-suite gate exercises this path against fresh Docker postgres. |
</threat_model>

<verification>
**Phase-level checks at end of Plan 43-02 (and end of Phase 43 overall):**

1. `bash scripts/test.sh` exits 0 — full Docker suite green at fresh postgres + all 14 migrations applied
2. `psql -c "\d profile_hexaco"` shows `data_consistency real NOT NULL DEFAULT 0` column + `profile_hexaco_data_consistency_bounds` CHECK constraint (same for profile_schwartz, profile_attachment)
3. `grep -rE "stripMetadataColumns" src/memory/` shows both shared.ts + profiles.ts call sites both strip dataConsistency (no drift between the two)
4. `grep -rE "substrateHash === ''" src/memory/profiles/{jurisdictional,capital,health,family}.ts` returns 4 matches (one per file)
5. Live verification path: the next M011 cron fire (Sun 2026-05-17 22:00 Paris) will INSERT the first non-zero `dataConsistency` value into profile_hexaco; M010 cron fire (Sun 2026-05-17 same day) will verify CONTRACT-01 + CONTRACT-02 against real production substrate. No deferred live-test risk — both gates fire within 4 days of Phase 43 deploy.
6. `git log --oneline -10` shows the atomic Phase 43 commits in expected order: 43-01 commits (INJ) before 43-02 commits (CONTRACT + migration).
</verification>

<success_criteria>
- CONTRACT-01 closed: both `stripMetadataColumns` sites strip `dataConsistency`. The host-computes-not-emits contract is restored on every non-first fire.
- CONTRACT-02 closed: all 4 `extract<X>PrevState` functions return null on `substrateHash === ''`. The seed-row first-fire-after-deploy no longer presents Sonnet with empty fields + anti-drift directive (M010-03 anchoring defense). Verified by 4 unit tests (one per dim) + 1 integration test per dim (`.every()` discipline per D-11).
- CONTRACT-03 closed: migration 0014 adds `data_consistency real NOT NULL DEFAULT 0 CHECK 0..1` to profile_hexaco + profile_schwartz + profile_attachment. The psychological upsert writes Sonnet's emission to the new column. profile_history snapshot picks it up automatically (full-row copy).
- HARD CO-LOC #M11-1 lineage discipline: schema.ts + .sql + meta/0014_snapshot.json + meta/_journal.json entry + scripts/test.sh MIGRATION_14_SQL line ALL shipped in Plan 43-02 commits atomically.
- Full Docker test gate GREEN — no regression from Plan 43-01 INJ-01/INJ-02 work, no broken existing tests, integration test confirms data_consistency persistence in [0, 1].
- All 5 v2.6.1 Phase 43 requirements (INJ-01 + INJ-02 + CONTRACT-01 + CONTRACT-02 + CONTRACT-03) implemented and verified before ship.
</success_criteria>

<output>
After completion, create `.planning/phases/43-inference-security-contract/43-02-SUMMARY.md` recording:
- Per-file edit summary (line counts changed, key identifier adds)
- Verbatim migration SQL as shipped
- Drizzle meta snapshot regen confirmation (`bash scripts/regen-snapshots.sh --check-only` output)
- Per-test-file count of new CONTRACT-01/CONTRACT-02/CONTRACT-03 assertions
- Full Docker test suite pass/fail count vs. baseline
- The first M010 / M011 cron fire dates (Sunday 2026-05-17 22:00 Paris) — these are the live verification gates for the Phase 43 changes
- Any deviations from the plan (executor's discretion observations)
</output>
