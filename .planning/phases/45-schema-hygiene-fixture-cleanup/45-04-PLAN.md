---
phase: 45
plan: 04
type: execute
wave: 3
depends_on: [45-02, 45-03]
files_modified:
  - tests/fixtures/primed/m010-30days/MANIFEST.json
  - tests/fixtures/primed/m010-30days/pensieve_entries.jsonl
  - tests/fixtures/primed/m010-30days/pensieve_embeddings.jsonl
  - tests/fixtures/primed/m010-30days/wellbeing_snapshots.jsonl
  - tests/fixtures/primed/m010-30days/operational_profiles_snapshot.jsonl
  - tests/fixtures/primed/m010-5days/MANIFEST.json
  - tests/fixtures/primed/m010-5days/pensieve_entries.jsonl
  - tests/fixtures/primed/m010-5days/pensieve_embeddings.jsonl
  - tests/fixtures/primed/m010-5days/wellbeing_snapshots.jsonl
  - tests/fixtures/primed/m010-5days/operational_profiles_snapshot.jsonl
  - tests/fixtures/primed/m010-anti-hallucination/MANIFEST.json
  - tests/fixtures/primed/m010-anti-hallucination/pensieve_entries.jsonl
  - tests/fixtures/primed/m010-anti-hallucination/pensieve_embeddings.jsonl
  - tests/fixtures/primed/m010-anti-hallucination/wellbeing_snapshots.jsonl
  - tests/fixtures/primed/m010-anti-hallucination/operational_profiles_snapshot.jsonl
autonomous: true
requirements: [FIX-06]
requirements_addressed: [FIX-06]
tags: [fixture-refresh, m010, operational-profiles, regenerate-primed, schema_mismatch-elimination, anti-hallucination-gate]
must_haves:
  truths:
    - "FIX-06 operational fixture refresh: `npx tsx scripts/regenerate-primed.ts --milestone m010 --force` runs end-to-end successfully on a clean throwaway DB that has migrations 0000..0016 applied (in particular 0016 from Plan 45-03 backfills the seed defaults)"
    - "The regen output lands fresh JSONL files under `tests/fixtures/primed/m010-30days/`, `tests/fixtures/primed/m010-5days/`, and `tests/fixtures/primed/m010-anti-hallucination/` (exact dirs depend on current m010 fixture layout — verify before regen; if other m010 dirs exist, regenerate all of them)"
    - "Each fixture dir's `MANIFEST.json` has updated `synthesized_at` timestamp + `commit_sha` referencing Plan 45-03's SCHEMA-02 commit SHA (D-13)"
    - "operational_profiles_snapshot.jsonl files in each fixture dir contain `wellbeing_trend` jsonb objects with the v3-Zod-required nullable fields (`energy_30d_mean`, `mood_30d_mean`, `anxiety_30d_mean`) AND `parent_care_responsibilities` jsonb objects with `notes` + `dependents` keys — NO longer the bare `'{}'` shape from pre-Plan-45-03"
    - "PMT-06 anti-hallucination test (`src/memory/profiles/__tests__/live-anti-hallucination.test.ts`) runs against the refreshed fixtures and emits ZERO `family.parent_care_responsibilities` or `health.wellbeing_trend` schema_mismatch warns (D-14) — assertion log capture is silent on these two paths"
    - "All other M010 milestone-gate tests (integration-m010-30days.test.ts, integration-m010-5days.test.ts, primed-sanity-m010.test.ts) continue to pass against the refreshed fixtures — no regression in fixture row counts, ID stability, or substrate hashes that downstream tests depend on"
    - "The fixture-refresh commit message references Plan 45-03's SCHEMA-02 commit SHA verbatim per D-13 — e.g., `chore(45-04): refresh m010 primed fixtures against SCHEMA-02 backfill (commit <sha>)`. The lineage tracking lets future operators reconstruct WHY the fixtures shifted and WHEN."
    - "`bash scripts/test.sh` exits 0 with M010 milestone-gate tests passing"
    - "`npx vitest run src/memory/profiles/__tests__/live-anti-hallucination.test.ts` exits 0 with zero schema_mismatch warns in test output"
  artifacts:
    - path: "tests/fixtures/primed/m010-30days/operational_profiles_snapshot.jsonl"
      provides: "Refreshed M010 30-day fixture's operational_profiles snapshot with v3-Zod-conformant jsonb shapes on wellbeing_trend + parent_care_responsibilities"
      contains: "energy_30d_mean"
    - path: "tests/fixtures/primed/m010-5days/operational_profiles_snapshot.jsonl"
      provides: "Refreshed M010 5-day fixture's operational_profiles snapshot"
      contains: "energy_30d_mean"
    - path: "tests/fixtures/primed/m010-anti-hallucination/operational_profiles_snapshot.jsonl"
      provides: "Refreshed M010 anti-hallucination fixture's operational_profiles snapshot — feeds PMT-06 gate"
      contains: "energy_30d_mean"
    - path: "tests/fixtures/primed/m010-30days/MANIFEST.json"
      provides: "Updated synthesized_at timestamp + commit_sha pointing to Plan 45-03's SCHEMA-02 SHA"
      contains: "synthesized_at"
    - path: "tests/fixtures/primed/m010-5days/MANIFEST.json"
      provides: "Updated synthesized_at timestamp + commit_sha"
      contains: "synthesized_at"
    - path: "tests/fixtures/primed/m010-anti-hallucination/MANIFEST.json"
      provides: "Updated synthesized_at timestamp + commit_sha"
      contains: "synthesized_at"
  key_links:
    - from: "tests/fixtures/primed/m010-anti-hallucination/operational_profiles_snapshot.jsonl"
      to: "src/memory/profiles/__tests__/live-anti-hallucination.test.ts (PMT-06 gate)"
      via: "loadPrimedFixture loads the snapshot; test reads wellbeing_trend + parent_care_responsibilities through the v3 Zod schemas without strict-parse rejection"
      pattern: "loadPrimedFixture.*m010-anti-hallucination"
    - from: "Plan 45-03 migration 0016"
      to: "this regen output"
      via: "regenerate-primed.ts's throwaway DB (synthesize-episodic.ts) runs migrations through 0016 including the backfill UPDATE; operational_profiles_snapshot inherits the backfilled shapes"
      pattern: "MIGRATIONS includes 0016_phase33_seed_defaults_backfill"
    - from: "Plan 45-02 FIX-03 + FIX-08"
      to: "this regen run"
      via: "synthesize-episodic.ts uses the dynamic migration glob (FIX-03) so 0016 is included; AbortController SIGINT (FIX-08) means a Ctrl-C mid-regen unwinds the throwaway DB cleanly"
      pattern: "depends_on: [45-02] in frontmatter"
---

<objective>
Ship Phase 45 FIX-06: regenerate the M010 operational primed fixtures against the post-SCHEMA-02 schema so the PMT-06 anti-hallucination gate stops emitting `family.parent_care_responsibilities` + `health.wellbeing_trend` schema_mismatch warns at the assertion level.

Purpose: FIX-06 — fixture refresh is the operator step that lands the SCHEMA-02 backfill effects into the test fixtures. Without this regen, the existing committed fixtures still ship the pre-backfill `'{}'::jsonb` shapes (because they were synthesized against the old defaults), and the PMT-06 assertions continue to log the warns even though the runtime DB has been fixed. CONTEXT D-13 captures the operator command + commit-message-lineage protocol; D-14 captures the success criterion (PMT-06 warns gone).

This plan is Wave C of CONTEXT D-02's wave DAG: it **depends on Plan 45-03** (migration 0016 must exist before regen) AND **depends on Plan 45-02** (regenerate-primed.ts SIGINT + synthesize-episodic.ts migration-glob must work correctly for the regen run to complete deterministically). Within this plan, the work is a single operator command followed by a commit — no code edits.

Output: refreshed JSONL files under `tests/fixtures/primed/m010-*/`, updated MANIFEST.json files, and a commit message that references the SCHEMA-02 commit SHA per D-13.

Satisfies: FIX-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md

# Plans this plan depends on (read SUMMARYs after they ship)
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-02-PLAN.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-03-PLAN.md

# Root-cause review files
@.planning/milestones/v2.5-phases/34-inference-engine/34-REVIEW.md

# The regen pipeline scripts (used as-is in this plan, no edits)
@scripts/regenerate-primed.ts
@scripts/fetch-prod-data.ts
@scripts/synthesize-episodic.ts
@scripts/synthesize-delta.ts
@src/__tests__/fixtures/load-primed.ts

# The PMT-06 gate test that consumes the refreshed fixtures
@src/memory/profiles/__tests__/live-anti-hallucination.test.ts

# M010 other milestone-gate tests (regression surface)
@src/memory/profiles/__tests__/integration-m010-30days.test.ts
@src/memory/profiles/__tests__/integration-m010-5days.test.ts
@src/__tests__/fixtures/primed-sanity-m010.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory current M010 fixture layout + confirm dependencies</name>
  <files>(read-only inventory)</files>
  <read_first>
    - tests/fixtures/primed/m010-30days/MANIFEST.json (if exists)
    - tests/fixtures/primed/m010-5days/MANIFEST.json (if exists)
    - tests/fixtures/primed/m010-anti-hallucination/MANIFEST.json (if exists)
    - scripts/regenerate-primed.ts (full file — confirm CLI flag shape for `--milestone m010 --force` and how it iterates over M010 sub-fixtures)
    - src/memory/profiles/__tests__/live-anti-hallucination.test.ts:130-180 (loadPrimedFixture call site — confirm WHICH fixture path it consumes; that's the critical regen target)
  </read_first>
  <action>
    Discovery + confirmation step. NO file writes in this task.

    1. `ls -la tests/fixtures/primed/m010-*` — list all M010 fixture directories that currently exist. Record the list in the SUMMARY for this plan.
    2. For each directory, `cat <dir>/MANIFEST.json` to capture the current `synthesized_at`, `commit_sha`, and `table_counts`. This is the pre-regen baseline for post-regen diff.
    3. Confirm Plan 45-02 SIGINT/SSH fixes have shipped: `git log --oneline | head -10` should show the 45-02 commit; `grep -c "abortController" scripts/regenerate-primed.ts` should return `>= 1` (FIX-08 pattern present).
    4. Confirm Plan 45-03 migration 0016 has shipped: `ls src/db/migrations/0016_phase33_seed_defaults_backfill.sql` should exist; `node -e "const j=require('./src/db/migrations/meta/_journal.json'); console.log(j.entries.map(e=>e.tag).slice(-2))"` should include `0016_phase33_seed_defaults_backfill`.
    5. Confirm the test DB has 0016 applied: `docker compose -f docker-compose.local.yml exec -T postgres psql -U chris -d chris -tAc "SELECT wellbeing_trend FROM profile_health WHERE substrate_hash = '' LIMIT 1"` should return `'{"energy_30d_mean":null,"mood_30d_mean":null,"anxiety_30d_mean":null}'` (or similar — keys present).
    6. Record the Plan 45-03 SCHEMA-02 commit SHA: `git log --oneline --grep="0016\\|SCHEMA-02\\|45-03" | head -1` → capture the SHA for the regen commit message.

    **Anti-shallow-execution:**
    - If any of steps 3-5 FAIL, STOP. Do NOT run the regen — the wave dependency is not satisfied. Report failure with the specific check that failed.
    - The PMT-06 test's actual fixture path determines the canonical regen target. If `live-anti-hallucination.test.ts` reads `tests/fixtures/primed/m010-anti-hallucination/`, that's the most-critical dir; if it reads `tests/fixtures/primed/m010-30days/`, prioritize that. Both should be regenerated regardless, but the assertion-level proof depends on the test's actual consumer path.
  </action>
  <verify>
    <automated>ls src/db/migrations/0016_phase33_seed_defaults_backfill.sql && node -e "const j=require('./src/db/migrations/meta/_journal.json'); const tags=j.entries.map(e=>e.tag); if (!tags.includes('0016_phase33_seed_defaults_backfill')) { console.error('0016 not in journal'); process.exit(1); } console.log('OK')" && grep -c "AbortController" scripts/regenerate-primed.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/0016_phase33_seed_defaults_backfill.sql` exists (Plan 45-03 dependency satisfied)
    - `_journal.json` contains the 0016 entry (Plan 45-03 fully landed)
    - `scripts/regenerate-primed.ts` contains `AbortController` (Plan 45-02 FIX-08 landed)
    - Current M010 fixture inventory recorded in plan 45-04 working notes
    - Plan 45-03 SCHEMA-02 commit SHA captured for use in Task 3's commit message
  </acceptance_criteria>
  <done>
    Wave dependencies confirmed (45-02 + 45-03 shipped); current M010 fixture inventory captured; SCHEMA-02 commit SHA noted for commit-message lineage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run regenerate-primed.ts for milestone m010 with --force</name>
  <files>tests/fixtures/primed/m010-30days/* (REGENERATED), tests/fixtures/primed/m010-5days/* (REGENERATED), tests/fixtures/primed/m010-anti-hallucination/* (REGENERATED — verify exact dir list from Task 1)</files>
  <read_first>
    - scripts/regenerate-primed.ts (full file — confirm CLI argument shape: `--milestone m010 --force` per CONTEXT D-13; verify any other required flags like `--prod-data-source` or `--no-fetch` if the script supports them)
    - scripts/fetch-prod-data.ts (if regenerate-primed delegates to fetch-prod-data, confirm the SSH path now uses accept-new + repo-vetted known_hosts from Plan 45-02; if the operator's local prod data is stale, this fetches fresh data)
    - scripts/synthesize-episodic.ts (the dynamic migration glob from Plan 45-02 FIX-03 ensures 0016 is applied to the throwaway DB)
    - Task 1 working notes (the current fixture inventory + SCHEMA-02 commit SHA)
  </read_first>
  <action>
    Operator-style execution: run the regen command, capture stdout/stderr, verify the output.

    **Command (per CONTEXT D-13):**
    ```bash
    npx tsx scripts/regenerate-primed.ts --milestone m010 --force
    ```

    **Pre-flight:**
    - Ensure the local Docker test stack is NOT running on the regen's throwaway port (default 5435) — `docker ps | grep 5435` should be empty, or the regen script handles port cleanup itself per its existing logic.
    - Ensure operator's `~/.ssh/known_hosts` OR `scripts/.ssh-known-hosts` has an entry for `192.168.1.50` if the regen needs to fetch fresh prod data (per Plan 45-02 FIX-04). If the operator already has recent prod data cached locally (e.g., from a prior regen), the regen may skip the SSH fetch.

    **Execution:**
    - Run the command. Expect a multi-stage pipeline:
      1. (Optional) SSH-tunneled prod fetch via fetch-prod-data.ts (Plan 45-02 FIX-04 hardening applied).
      2. synthesize-episodic.ts spawns a throwaway postgres on port 5435, applies migrations 0000..0016 via the dynamic glob (Plan 45-02 FIX-03), loads prod data into the throwaway DB.
      3. synthesize-delta.ts synthesizes the additional fixture rows including the contradictions FK pre-filter (Plan 45-02 FIX-01).
      4. Final dump → JSONL files written to `tests/fixtures/primed/m010-*/`.
      5. MANIFEST.json files updated with new `synthesized_at` timestamps.

    **Post-run verification:**
    - For each refreshed dir, `cat MANIFEST.json` and confirm `synthesized_at` is today (or the regen day).
    - `jq -r '.wellbeing_trend' tests/fixtures/primed/m010-*/operational_profiles_snapshot.jsonl` — should now contain `energy_30d_mean`, `mood_30d_mean`, `anxiety_30d_mean` keys (where the seed row has substrate_hash=''; if the M010 fixtures use post-inference rows, the keys may have non-null numeric values). The key invariant is: NO bare `'{}'` shapes for these two columns.
    - `jq -r '.parent_care_responsibilities' tests/fixtures/primed/m010-*/operational_profiles_snapshot.jsonl` — should contain `notes` + `dependents` keys.

    **Anti-shallow-execution:**
    - If the regen command fails partway, capture the error message and report. Common failure modes:
      - SSH tunnel failure → check `scripts/.ssh-known-hosts` has the prod host key (Plan 45-02 FIX-04 bootstrap).
      - Migration 0016 not in the dynamic glob → verify Plan 45-02 FIX-03's loadMigrations() function returns 0016 in its array.
      - pgvector coercion failure → Plan 45-02 FIX-05's staging-table CAST should be active in load-primed.ts.
      - SIGINT during regen → Plan 45-02 FIX-08's AbortController should unwind cleanly; check for leaked docker projects via `docker ps -a | grep synthesize-episodic`.
    - Do NOT manually hand-edit the generated JSONL files to "fix" specific values. The regen output is the contract; if the output is wrong, the bug is in the pipeline (which Plan 45-02 + 45-03 should have addressed).
    - Do NOT skip MANIFEST.json updates — the synthesized_at timestamp + commit_sha are the lineage markers for future operators.
    - If the operator's environment doesn't have a fresh PROD fetch (e.g., 192.168.1.50 is offline), use the most recent local cache via a `--no-fetch` flag if the script supports it; otherwise STOP and report (CONTEXT D-13 assumes a working regen pipeline).
  </action>
  <verify>
    <automated>jq -r '.wellbeing_trend // empty' tests/fixtures/primed/m010-*/operational_profiles_snapshot.jsonl 2>/dev/null | head -5 | grep -c "energy_30d_mean" | xargs -I{} test {} -ge 1 && for dir in tests/fixtures/primed/m010-*; do test -f "$dir/MANIFEST.json" || { echo "missing $dir/MANIFEST.json"; exit 1; }; done && echo "all MANIFESTs present"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/regenerate-primed.ts --milestone m010 --force` exits 0
    - All M010 fixture dirs under `tests/fixtures/primed/` have updated `synthesized_at` in their MANIFEST.json (timestamp newer than pre-regen)
    - `jq -r '.wellbeing_trend' tests/fixtures/primed/m010-*/operational_profiles_snapshot.jsonl | grep -c "energy_30d_mean"` returns `>= 1` (the v3-Zod-required key is present in at least one snapshot)
    - `jq -r '.parent_care_responsibilities' tests/fixtures/primed/m010-*/operational_profiles_snapshot.jsonl | grep -c "dependents"` returns `>= 1`
    - No leaked docker projects: `docker ps -a | grep synthesize-episodic` returns nothing (Plan 45-02 FIX-08 SIGINT cleanup verified)
    - No leaked postgres clients: `lsof -i :5435` returns nothing (regen's throwaway port closed cleanly)
    - The pre-regen MANIFEST.json snapshots from Task 1 + the post-regen MANIFEST.json snapshots differ on `synthesized_at` (lineage updated)
  </acceptance_criteria>
  <done>
    M010 primed fixtures regenerated; operational_profiles_snapshot.jsonl files have v3-Zod-conformant jsonb shapes; MANIFEST.json updated; no resource leaks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify PMT-06 anti-hallucination gate emits zero schema_mismatch warns + commit refreshed fixtures</name>
  <files>(git commit step — no new files; commits the Task 2 outputs)</files>
  <read_first>
    - src/memory/profiles/__tests__/live-anti-hallucination.test.ts (full file — locate the schema_mismatch warn assertion + log-capture mechanism; understand what "zero warns" means in test-output terms)
    - Task 1 working notes (Plan 45-03 SCHEMA-02 commit SHA — needed for the commit message)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-13 (commit message format) + D-14 (PMT-06 verification)
  </read_first>
  <action>
    Two operations: run PMT-06 to verify the warns are gone, then commit the refreshed fixtures.

    **Operation 1: PMT-06 anti-hallucination gate verification (D-14).**

    Run the gate test against the freshly refreshed fixtures:
    ```bash
    npx vitest run src/memory/profiles/__tests__/live-anti-hallucination.test.ts 2>&1 | tee /tmp/pmt06-output.log
    ```

    Then grep the log for the two schema_mismatch warn signatures:
    ```bash
    grep -c "family.parent_care_responsibilities.*schema_mismatch" /tmp/pmt06-output.log
    grep -c "health.wellbeing_trend.*schema_mismatch" /tmp/pmt06-output.log
    ```

    BOTH must return `0`. If either returns `>= 1`, the regen did not fully eliminate the warns — STOP and diagnose:
    - Check that the throwaway DB had 0016 applied (synthesize-episodic.ts log output should mention the 0016 migration file).
    - Check the operational_profiles_snapshot.jsonl rows directly with `jq` to confirm the jsonb shapes match v3.
    - If the prod data fetched by fetch-prod-data.ts ALREADY had non-empty wellbeing_trend / parent_care_responsibilities (i.e., the prod DB has run inference cycles and substrate_hash != ''), the seed-row backfill predicate of 0016 won't fire (substrate_hash != '' rows are skipped per D-05). In that case the fixture rows reflect real inference output, NOT seed defaults — verify the v3 Zod schemas accept those shapes by running the regenerated rows through `wellbeingTrendSchema.strict().parse(...)` in a one-off node REPL.

    **Operation 2: Commit the refreshed fixtures (D-13).**

    Stage the changed fixture files:
    ```bash
    git add tests/fixtures/primed/m010-*/
    ```

    Verify what's staged with `git diff --cached --stat` — should show the JSONL files + MANIFEST.json files for the M010 fixture dirs.

    Commit with the SCHEMA-02-referencing message per CONTEXT D-13:
    ```bash
    git commit -m "chore(45-04): refresh m010 primed fixtures against SCHEMA-02 backfill (commit <SCHEMA-02-sha>)

    Re-run \`npx tsx scripts/regenerate-primed.ts --milestone m010 --force\` against
    a throwaway DB with migrations 0000..0016 applied. The 0016 backfill UPDATEs the
    cold-start seed row's wellbeing_trend + parent_care_responsibilities jsonb columns
    to match the v3 Zod schemas' required-nullable shape — refreshed fixtures now
    reflect this state.

    Verification: PMT-06 anti-hallucination gate (live-anti-hallucination.test.ts)
    emits zero \`family.parent_care_responsibilities\` or \`health.wellbeing_trend\`
    schema_mismatch warns. M010 milestone-gate tests pass against refreshed fixtures.

    Closes FIX-06. Lineage: Plan 45-03 SCHEMA-02 (commit <SCHEMA-02-sha>).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    Replace `<SCHEMA-02-sha>` with the actual commit SHA captured in Task 1.

    **NOTE per phase orchestration:** The parent orchestrator may batch-commit at the end of the phase rather than letting individual plan executions commit. If so, this Task's commit-step is REPLACED with "stage the changes" only; the parent's batch-commit captures everything. Verify with the orchestrator's commit policy before deciding which path to take. The task prompt for Phase 45 from the user/orchestrator may specify "DO NOT commit — parent will batch-commit at the end" — in that case skip the `git commit` invocation and only stage.

    **Anti-shallow-execution:**
    - Run PMT-06 verification BEFORE committing — if it fails, the fixtures shouldn't ship.
    - The commit message MUST include the SCHEMA-02 SHA verbatim — that's the lineage marker per D-13.
    - Do NOT git-add anything outside `tests/fixtures/primed/m010-*/` — operator scripts or migration files were touched by 45-02/45-03 in their own commits, not this plan's.
    - If the PMT-06 test ALSO depends on Plan 45-02's HARN test changes (FIX-07 — calendar-month-windowed wordCount), confirm those changes shipped before claiming Plan 45-04 success.
    - Run the full M010 milestone-gate test suite as a regression check before committing: `npx vitest run src/memory/profiles/__tests__/integration-m010-30days.test.ts src/memory/profiles/__tests__/integration-m010-5days.test.ts src/__tests__/fixtures/primed-sanity-m010.test.ts` — all should pass.
  </action>
  <verify>
    <automated>npx vitest run src/memory/profiles/__tests__/live-anti-hallucination.test.ts 2>&1 | tee /tmp/pmt06-output.log | tail -10 && grep -c "family.parent_care_responsibilities.*schema_mismatch\\|health.wellbeing_trend.*schema_mismatch" /tmp/pmt06-output.log | xargs -I{} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/memory/profiles/__tests__/live-anti-hallucination.test.ts` exits 0
    - `grep -c "family.parent_care_responsibilities.*schema_mismatch" /tmp/pmt06-output.log` returns `0`
    - `grep -c "health.wellbeing_trend.*schema_mismatch" /tmp/pmt06-output.log` returns `0`
    - `npx vitest run src/memory/profiles/__tests__/integration-m010-30days.test.ts src/memory/profiles/__tests__/integration-m010-5days.test.ts src/__tests__/fixtures/primed-sanity-m010.test.ts` exits 0 (M010 regression-free)
    - `git status` shows the M010 fixture files staged OR committed (depending on phase orchestrator policy)
    - The commit message (or the parent batch-commit message) references the SCHEMA-02 commit SHA
  </acceptance_criteria>
  <done>
    PMT-06 anti-hallucination gate emits zero target-warns; M010 milestone-gate suite regression-free; refreshed fixtures staged/committed with SCHEMA-02 lineage in the message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-machine ↔ Proxmox prod (SSH tunnel, if fetch invoked) | The regen may pull fresh prod data via fetch-prod-data.ts. Plan 45-02 FIX-04 hardens this (accept-new + repo-vetted known_hosts). |
| throwaway DB ↔ committed test fixtures | Fixture data is committed to the repo — any sensitive data in prod ends up in JSONL files. The synthesize-delta.ts pipeline scrubs/synthesizes most user-identifiable content but the operational_profiles_snapshot may contain real profile state. Reviewer must verify no PII leaks into the commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Information Disclosure (I) | M010 operational_profiles_snapshot.jsonl may contain real user profile data | mitigate | The M010 fixtures derive from synthesized/scrubbed data per the regen pipeline contract. Pre-commit review must confirm no real PII (names, addresses, etc.) leaks into the JSONL. The wellbeing_trend numeric fields (mood/energy/anxiety) are derived statistics, not raw data; the parent_care_responsibilities text may contain user-authored notes — verify these are synthetic before committing. |
| T-45-04-02 | Tampering (T) | Refreshed fixtures could be tampered with mid-commit | accept | Standard git commit process applies; PR review (if Phase 45 ships via PR) verifies the diff. Lineage marker (SCHEMA-02 SHA in commit message) lets future operators verify the regen path. |
| T-45-04-03 | Denial of Service (D) | Regen failure mid-pipeline leaves stale fixtures | mitigate | The regen runs in a throwaway DB; failure does NOT corrupt the test DB or committed fixtures. The operator re-runs after fixing the root cause. Plan 45-02 FIX-08's AbortController ensures Ctrl-C doesn't leak resources between attempts. |
</threat_model>

<verification>
- `npx vitest run src/memory/profiles/__tests__/live-anti-hallucination.test.ts` exits 0 with zero target-shape schema_mismatch warns in log output (D-14 satisfied).
- `npx vitest run src/memory/profiles/__tests__/integration-m010-30days.test.ts` exits 0 (no regression).
- `npx vitest run src/memory/profiles/__tests__/integration-m010-5days.test.ts` exits 0 (no regression).
- `npx vitest run src/__tests__/fixtures/primed-sanity-m010.test.ts` exits 0 (no regression).
- `bash scripts/test.sh` exits 0 — full Docker test suite green (per CLAUDE.md memory "always run full Docker tests").
- Refreshed fixture MANIFEST.json files have `synthesized_at` timestamps newer than the pre-regen baseline captured in Task 1.
- The commit (or stage) for the fixture refresh includes the SCHEMA-02 commit SHA in its message per D-13.
- No leaked docker projects + no leaked postgres clients on port 5435 (regen pipeline cleaned up).
</verification>

<success_criteria>
- M010 primed fixtures regenerated against post-SCHEMA-02 schema; operational_profiles_snapshot.jsonl files have v3-Zod-conformant jsonb shapes on wellbeing_trend + parent_care_responsibilities.
- PMT-06 anti-hallucination test emits zero `family.parent_care_responsibilities` or `health.wellbeing_trend` schema_mismatch warns (D-14).
- All M010 milestone-gate tests pass against the refreshed fixtures (no regression in row counts, IDs, or hashes).
- Fixture-refresh commit references Plan 45-03's SCHEMA-02 commit SHA per D-13 — lineage traceable.
- Regen run completed without resource leaks (no orphan docker projects, no orphan postgres clients).
- Phase 45 milestone-level FIX-06 closed; SCHEMA-02 → FIX-06 dependency chain end-to-end verified.
</success_criteria>

<output>
After completion, create `.planning/phases/45-schema-hygiene-fixture-cleanup/45-04-SUMMARY.md`
</output>
