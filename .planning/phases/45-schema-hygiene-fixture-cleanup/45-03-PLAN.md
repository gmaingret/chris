---
phase: 45
plan: 03
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - src/db/migrations/0016_phase33_seed_defaults_backfill.sql
  - src/db/migrations/meta/0016_snapshot.json
  - src/db/migrations/meta/_journal.json
  - scripts/test.sh
  - scripts/regen-snapshots.sh
  - src/__tests__/migrations/0016-seed-defaults-backfill.test.ts
autonomous: true
requirements: [SCHEMA-02]
requirements_addressed: [SCHEMA-02]
tags: [migration, drizzle, seed-backfill, operational-profiles, schema-mismatch, m010-root-cause]
must_haves:
  truths:
    - "Migration 0016 file exists at src/db/migrations/0016_phase33_seed_defaults_backfill.sql; filename ordering puts it AFTER Plan 45-01's 0015 (lexicographic — D-18)"
    - "Migration is idempotent: re-running against an already-backfilled DB returns exit 0 (the WHERE clauses `substrate_hash = '' AND <col> = '{}'::jsonb` make the UPDATE a no-op on rows already updated; the ALTER COLUMN SET DEFAULT is naturally idempotent)"
    - "UPDATE Operation 1: `UPDATE profile_health SET wellbeing_trend = '{\"energy_30d_mean\":null,\"mood_30d_mean\":null,\"anxiety_30d_mean\":null}'::jsonb WHERE substrate_hash = '' AND wellbeing_trend = '{}'::jsonb` runs — cold-start seed row gets the v3-Zod-required nullable fields"
    - "UPDATE Operation 2: `UPDATE profile_family SET parent_care_responsibilities = '{\"notes\":null,\"dependents\":[]}'::jsonb WHERE substrate_hash = '' AND parent_care_responsibilities = '{}'::jsonb` runs — cold-start seed row gets the v3-Zod-required nullable fields"
    - "ALTER COLUMN SET DEFAULT for profile_health.wellbeing_trend points to the new shape `{\"energy_30d_mean\":null,\"mood_30d_mean\":null,\"anxiety_30d_mean\":null}` — future fresh DBs ship the correct seed shape"
    - "ALTER COLUMN SET DEFAULT for profile_family.parent_care_responsibilities points to the new shape `{\"notes\":null,\"dependents\":[]}` — future fresh DBs ship the correct seed shape"
    - "Read-time strict-parse no longer rejects: `SELECT wellbeing_trend FROM profile_health WHERE substrate_hash = ''` returns a jsonb object with `energy_30d_mean`, `mood_30d_mean`, `anxiety_30d_mean` keys all set to null (not missing) — feeds Zod v3 schema's required-nullable shape"
    - "`src/db/migrations/meta/_journal.json` has a new entry with idx > Plan 45-01's idx, tag=`0016_phase33_seed_defaults_backfill`, when value strictly greater than 0015's when (Phase 32 #3 monotonic-when guardrail)"
    - "`src/db/migrations/meta/0016_snapshot.json` exists, generated by `npx drizzle-kit generate` from the SQL file (drizzle-kit detects DEFAULT change as schema-diff state)"
    - "`scripts/test.sh` has a new `MIGRATION_16_SQL=\"src/db/migrations/0016_phase33_seed_defaults_backfill.sql\"` variable + psql apply line + post-apply smoke gate verifying both backfill effects (SELECT confirms the jsonb shape of the seed rows post-migration)"
    - "`scripts/regen-snapshots.sh` cleanup-flag sentinels bumped to include 0016 (mirror Plan 45-01's 0015 bump)"
    - "Integration test `src/__tests__/migrations/0016-seed-defaults-backfill.test.ts` exercises 4 cases: (a) post-migration seed rows have required nullable fields, (b) ALTER DEFAULT works on fresh INSERT, (c) idempotency under re-run, (d) Zod v3 .strict() parse no longer rejects the seed row"
    - "`npx tsc --noEmit` clean; `bash scripts/test.sh` exits 0 with new smoke gate green and 0016 vitest file passing"
  artifacts:
    - path: "src/db/migrations/0016_phase33_seed_defaults_backfill.sql"
      provides: "2 UPDATE statements + 2 ALTER COLUMN SET DEFAULT statements + header comment block with HARD CO-LOC #M11-45b + reference to 34-REVIEW.md §Schema-Drift Origins (D-05)"
      min_lines: 70
      contains: "0016_phase33_seed_defaults_backfill"
    - path: "src/db/migrations/meta/0016_snapshot.json"
      provides: "drizzle-kit generated snapshot reflecting the new DEFAULT shapes"
      contains: "0016_phase33_seed_defaults_backfill"
    - path: "src/db/migrations/meta/_journal.json"
      provides: "Appended idx entry for 0016 with monotonically-increasing when value"
      contains: "0016_phase33_seed_defaults_backfill"
    - path: "scripts/test.sh"
      provides: "MIGRATION_16_SQL variable + apply line + 0016 smoke gate"
      contains: "MIGRATION_16_SQL"
    - path: "scripts/regen-snapshots.sh"
      provides: "Cleanup sentinel bump to include 0016"
      contains: "0016"
    - path: "src/__tests__/migrations/0016-seed-defaults-backfill.test.ts"
      provides: "4 integration tests for the backfill UPDATE + DEFAULT change + idempotency + Zod-parse acceptance"
      min_lines: 80
      contains: "describe('migration 0016"
  key_links:
    - from: "src/db/migrations/0016_phase33_seed_defaults_backfill.sql"
      to: "src/db/migrations/0013_psychological_profiles.sql + 0008_wellbeing_seed.sql"
      via: "UPDATE references the seed rows created by 0013 (profile_health.name='primary', profile_family.name='primary' with substrate_hash='')"
      pattern: "UPDATE \"profile_health\" SET \"wellbeing_trend\""
    - from: "src/db/migrations/meta/_journal.json"
      to: "src/db/migrations/0016_phase33_seed_defaults_backfill.sql"
      via: "drizzle-kit journal entry maps idx→tag; when strictly > prior entry (Plan 45-01's 0015) per Phase 32 #3 guardrail"
      pattern: "\"tag\": \"0016_phase33_seed_defaults_backfill\""
    - from: "scripts/test.sh"
      to: "src/db/migrations/0016_phase33_seed_defaults_backfill.sql"
      via: "CI test bootstrap applies the migration; smoke gate SELECT verifies seed row jsonb shape"
      pattern: "MIGRATION_16_SQL=\"src/db/migrations/0016_phase33_seed_defaults_backfill.sql\""
    - from: "Plan 45-04 (FIX-06 fixture refresh)"
      to: "this migration"
      via: "FIX-06 regen runs `regenerate-primed.ts` AFTER 0016 has applied; the throwaway DB created by synthesize-episodic.ts inherits the backfilled defaults"
      pattern: "depends_on: [45-03] in 45-04-PLAN.md"
---

<objective>
Ship Phase 45 SCHEMA-02: migration `0016_phase33_seed_defaults_backfill` that root-causes the M010 `schema_mismatch` warns by populating jsonb seed defaults with the v3-Zod-required nullable fields (`energy_30d_mean`, `mood_30d_mean`, `anxiety_30d_mean` on `profile_health.wellbeing_trend`; `notes`, `dependents` on `profile_family.parent_care_responsibilities`) AND updating the column DEFAULTs so future fresh DBs ship the correct shape.

Purpose: SCHEMA-02 — root cause from Phase 34 review §Schema-Drift Origins Confirmed (lines 361-390 of `34-REVIEW.md`). The Phase 33 seed defaults shipped `'{}'::jsonb` while the v3 Zod schemas declare those keys as `nullable` REQUIRED → first read-time parse rejects → `schema_mismatch` warn fires. The fix is two-fold: (1) UPDATE existing cold-start seed rows so they match the v3 shape, (2) ALTER the column DEFAULTs so this never recurs on a fresh deploy.

This plan is HARD CO-LOCATION #M11-45b per the pattern in 0013_psychological_profiles.sql:1-15: the SQL migration + drizzle journal entry + snapshot + `scripts/test.sh` apply line + smoke gate + `scripts/regen-snapshots.sh` sentinel bump ALL ship in one atomic commit. Same pattern as Plan 45-01 (#M11-45a) — see 45-01-PLAN.md's HARD CO-LOC discipline.

This plan **depends on Plan 45-01** for migration-number sequencing only (0015 must exist before 0016's drizzle-kit generate runs). It does NOT depend on 45-01 semantically — SCHEMA-01 constrains psychological tables, SCHEMA-02 backfills operational tables.

Output: 1 new migration SQL file + 1 new snapshot json (drizzle-kit generated) + 1 journal entry append + `scripts/test.sh` apply line + `scripts/regen-snapshots.sh` sentinel bump + 1 new integration test.

Satisfies: SCHEMA-02. Unblocks: Plan 45-04 (FIX-06 — operational primed fixtures get regenerated against the backfilled schema, eliminating the PMT-06 schema_mismatch warns at the assertion level).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md

# Plan 45-01 SUMMARY — for HARD CO-LOCATION pattern reference (read after 45-01 ships)
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-01-PLAN.md

# Root-cause review files (per-REQ specifics)
@.planning/milestones/v2.5-phases/34-inference-engine/34-REVIEW.md

# Direct in-repo analogs
@src/db/migrations/0013_psychological_profiles.sql
@src/db/migrations/0008_wellbeing_seed.sql
@src/db/migrations/meta/_journal.json

# Schema source (table + column types)
@src/db/schema.ts

# Test.sh + regen-snapshots.sh patterns to mirror (same files Plan 45-01 touched; sequential apply)
@scripts/test.sh
@scripts/regen-snapshots.sh
@scripts/validate-journal-monotonic.ts

# Zod schemas that consume the backfilled rows (read-time strict-parse path)
@src/memory/profile-schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 0016_phase33_seed_defaults_backfill.sql migration</name>
  <files>src/db/migrations/0016_phase33_seed_defaults_backfill.sql</files>
  <read_first>
    - src/db/migrations/0013_psychological_profiles.sql:1-38 (header comment pattern) + :48 (jsonb-default precedent that 0016 mirrors-via-ALTER) — for HARD CO-LOC pattern
    - src/db/migrations/0008_wellbeing_seed.sql (closest analog: INSERT-shaped seed; 0016 is the UPDATE-shaped sibling for already-seeded rows)
    - src/db/schema.ts — locate profile_health.wellbeing_trend + profile_family.parent_care_responsibilities columns; confirm column types are jsonb and the seed row pattern (name='primary', substrate_hash='')
    - .planning/milestones/v2.5-phases/34-inference-engine/34-REVIEW.md §Schema-Drift Origins Confirmed lines 361-390 — verbatim shape of the required nullable fields per profile column
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-05 (two operations: UPDATE + ALTER COLUMN SET DEFAULT) + D-18 (migration ordering)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"src/db/migrations/0016_phase33_seed_defaults_backfill.sql" — verbatim SQL body
    - src/memory/profile-schemas.ts (locate the `wellbeingTrendSchema` + `parentCareResponsibilitiesSchema` Zod definitions — confirm that the required-nullable keys match the backfill JSON shape exactly: `energy_30d_mean: z.number().nullable()`, etc.)
  </read_first>
  <action>
    Create `src/db/migrations/0016_phase33_seed_defaults_backfill.sql`. File structure:

    **Header comment block** (mirror 0013_psychological_profiles.sql:1-38 style + verbatim from PATTERNS §0016 header section):

    - Lines 1-9: MD-02 idempotency-guard rationale — the UPDATE is idempotent under the `WHERE substrate_hash = '' AND <col> = '{}'::jsonb` clause; re-running on already-updated rows is a no-op. The ALTER COLUMN SET DEFAULT is naturally idempotent (re-setting the same default is a no-op in PostgreSQL).
    - Blank line
    - Lines 11-13: Attribution — `-- Phase 45 (v2.6.1) — Phase 33 seed defaults backfill (SCHEMA-02).\n-- HARD CO-LOCATION #M11-45b: this SQL + meta/0016_snapshot.json + _journal.json idx-N entry +\n-- scripts/test.sh apply line + smoke gate + scripts/regen-snapshots.sh sentinel bump ALL ship in Plan 45-03 atomically.`
    - Blank line
    - Lines 15-17: Cross-phase coordination — `-- Migration slot 0014 is owned by Phase 43 (CONTRACT-03); Plan 45-01 owns slot 0015\n-- (psychological CHECK constraints); this Plan owns slot 0016. Lexicographic apply order\n-- means 0015 lands before 0016 — see CONTEXT D-18.`
    - Blank line
    - Lines 19-30: Root-cause statement — verbatim from PATTERNS §0016 lines 105-112:
      ```
      -- Root cause per Phase 34 REVIEW §Schema-Drift Origins Confirmed lines 361-390:
      -- - profile_health.wellbeing_trend default is '{}'::jsonb, missing v3-Zod-required
      --   nullable fields energy_30d_mean / mood_30d_mean / anxiety_30d_mean.
      --   Read-time .strict() parse rejects → schema_mismatch warn.
      -- - profile_family.parent_care_responsibilities default is '{}'::jsonb, missing
      --   required-nullable fields notes / dependents. Same failure mode.
      --
      -- Two operations in this migration:
      --   (1) UPDATE existing cold-start seed rows (substrate_hash = '' AND col = '{}'::jsonb)
      --       so the v3 Zod parse passes on next read.
      --   (2) ALTER COLUMN SET DEFAULT so future fresh DBs ship the correct shape.
      --
      -- Downstream: Plan 45-04 (FIX-06) regenerates M010 operational primed fixtures
      -- against the backfilled schema; PMT-06 anti-hallucination gate stops emitting
      -- family.parent_care_responsibilities + health.wellbeing_trend schema_mismatch warns.
      ```

    **Body — 2 UPDATE statements + 2 ALTER COLUMN statements, verbatim from PATTERNS §0016 lines 113-133:**

    ```sql
    -- Operation 1a: Backfill wellbeing_trend on cold-start seed rows.
    UPDATE "profile_health"
    SET "wellbeing_trend" = '{"energy_30d_mean":null,"mood_30d_mean":null,"anxiety_30d_mean":null}'::jsonb
    WHERE "substrate_hash" = '' AND "wellbeing_trend" = '{}'::jsonb;
    --> statement-breakpoint

    -- Operation 1b: Backfill parent_care_responsibilities on cold-start seed rows.
    UPDATE "profile_family"
    SET "parent_care_responsibilities" = '{"notes":null,"dependents":[]}'::jsonb
    WHERE "substrate_hash" = '' AND "parent_care_responsibilities" = '{}'::jsonb;
    --> statement-breakpoint

    -- Operation 2a: Update wellbeing_trend column DEFAULT for future fresh DBs.
    ALTER TABLE "profile_health"
      ALTER COLUMN "wellbeing_trend"
      SET DEFAULT '{"energy_30d_mean":null,"mood_30d_mean":null,"anxiety_30d_mean":null}'::jsonb;
    --> statement-breakpoint

    -- Operation 2b: Update parent_care_responsibilities column DEFAULT for future fresh DBs.
    ALTER TABLE "profile_family"
      ALTER COLUMN "parent_care_responsibilities"
      SET DEFAULT '{"notes":null,"dependents":[]}'::jsonb;
    --> statement-breakpoint
    ```

    **Anti-shallow-execution requirements:**
    - Do NOT introduce additional UPDATEs beyond the two listed. SCHEMA-02 scope per REQUIREMENTS.md + CONTEXT D-05 is exactly these two columns.
    - Do NOT touch profile rows where `substrate_hash != ''` — those have already run a real inference cycle and their jsonb shapes are set by the inference output, not by the seed default. The WHERE clause `substrate_hash = '' AND <col> = '{}'::jsonb` is the cold-start sentinel.
    - JSON shape MUST be EXACTLY what the v3 Zod schemas require — verify against `src/memory/profile-schemas.ts` before authoring (the field names energy_30d_mean / mood_30d_mean / anxiety_30d_mean / notes / dependents are spelled exactly per the source artifact references; cross-check spelling).
    - Use double-quoted SQL identifiers (`"profile_health"`, `"wellbeing_trend"`) — drizzle convention preserved.
    - Each statement followed by `--> statement-breakpoint` (drizzle convention, matches 0013 + 0015 conventions).
    - Do NOT modify the schema.ts default declarations — drizzle-kit will detect the ALTER COLUMN SET DEFAULT as a schema-diff and update its snapshot accordingly; schema.ts edits are NOT in scope for SCHEMA-02 (the migration owns the runtime truth).
    - If profile_family has multiple seed rows (e.g., `name='primary'` plus other variants), the UPDATE applies to ALL of them since the predicate is on `substrate_hash` not `name`. Confirm seed-row count via `SELECT COUNT(*) FROM profile_family WHERE substrate_hash = ''` on the test DB BEFORE assuming 1 row.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.local.yml exec -T postgres psql -U chris -d chris -v ON_ERROR_STOP=1 -q < src/db/migrations/0016_phase33_seed_defaults_backfill.sql && docker compose -f docker-compose.local.yml exec -T postgres psql -U chris -d chris -tAc "SELECT wellbeing_trend FROM profile_health WHERE substrate_hash = '' LIMIT 1" | grep -c "energy_30d_mean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^UPDATE "profile_health"' src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `1`
    - `grep -c '^UPDATE "profile_family"' src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `1`
    - `grep -c 'ALTER COLUMN "wellbeing_trend"' src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `1`
    - `grep -c 'ALTER COLUMN "parent_care_responsibilities"' src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `1`
    - `grep -c "energy_30d_mean" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 2` (UPDATE + ALTER DEFAULT)
    - `grep -c "anxiety_30d_mean" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 2`
    - `grep -c "mood_30d_mean" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 2`
    - `grep -c "dependents" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 2`
    - `grep -c "notes" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 2`
    - `grep -c "substrate_hash = ''" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `2` (one per UPDATE — idempotency predicate)
    - `grep -c -- "--> statement-breakpoint" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `>= 4`
    - `grep -c "HARD CO-LOCATION #M11-45b" src/db/migrations/0016_phase33_seed_defaults_backfill.sql` returns `1`
    - Applying the SQL to a fresh DB returns exit 0; the post-apply SELECT confirms wellbeing_trend has the three keys
    - Re-applying to the same DB returns exit 0 (idempotency — UPDATE's WHERE clause makes it a no-op on already-backfilled rows; ALTER DEFAULT is naturally idempotent)
  </acceptance_criteria>
  <done>
    SQL file authored with 2 UPDATEs + 2 ALTER COLUMN DEFAULTs, idempotent under re-run, header references HARD CO-LOC #M11-45b and CONTEXT D-05/D-18, JSON shapes match v3 Zod schemas exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate drizzle snapshot + append _journal.json entry for 0016</name>
  <files>src/db/migrations/meta/0016_snapshot.json, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - src/db/migrations/meta/_journal.json (current state — after Plan 45-01 lands, the latest entry should be 0015 with monotonic when; this Task appends 0016 with strictly-greater when)
    - src/db/migrations/meta/0015_snapshot.json (Plan 45-01 output — reference for the drizzle-kit snapshot format if drizzle decides DEFAULT changes are state-diffs)
    - scripts/validate-journal-monotonic.ts (Phase 32 #3 guardrail)
    - drizzle.config.ts (drizzle-kit config)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-01-PLAN.md Task 2 (snapshot generation pattern — mirror exactly with 0016 instead of 0015)
  </read_first>
  <action>
    Same pattern as Plan 45-01 Task 2 with 0016 in place of 0015.

    **(a) Run `npx drizzle-kit generate`** from project root with the 0016 SQL file already present. Drizzle-kit detects the manually-authored migration AND the schema-diff (DEFAULT changes on the two jsonb columns are diffable state in drizzle-kit's model) and produces:
    - `src/db/migrations/meta/0016_snapshot.json` — a fresh snapshot of the schema AFTER applying 0016 (the column DEFAULTs in the snapshot reflect the new shapes).
    - An appended entry in `_journal.json` with `idx` = next sequential (after Plan 45-01's 0015 entry's idx), `tag="0016_phase33_seed_defaults_backfill"`, `when=<Date.now()>`, `version="7"`, `breakpoints=true`.

    If drizzle-kit prompts for the migration name, use `--name 0016_phase33_seed_defaults_backfill` as a CLI flag if the version supports it; otherwise the existing filename should be auto-picked.

    **(b) Verify monotonic-when invariant**: open the regenerated `_journal.json`, confirm the new 0016 entry has `when` strictly greater than 0015's `when` (set by Plan 45-01). If the new when is NOT strictly greater (clock skew), manually edit the entry's `when` to be `<0015_when> + 1`.

    **Anti-shallow-execution requirements:**
    - Do NOT hand-edit `0016_snapshot.json`.
    - Do NOT modify any existing journal entries (0..N-1 where N is the new idx). Only append.
    - The drizzle-kit snapshot diff should show the two DEFAULT-changed columns. If drizzle-kit does NOT detect DEFAULT changes (sometimes drizzle-kit's drift detection is narrow), the snapshot will be identical to 0015's modulo metadata. In that case the snapshot file may be a near-copy of 0015_snapshot.json — that's expected and acceptable; the journal entry append is the primary artifact.
    - If Plan 45-01 has not yet been merged when this plan executes, that's a wave-ordering violation. The execute-phase orchestrator enforces Wave 1 (45-01) completes before Wave 2 (45-03) starts. The depends_on: [45-01] frontmatter is the gate.
  </action>
  <verify>
    <automated>npx tsx scripts/validate-journal-monotonic.ts && node -e "const j=require('./src/db/migrations/meta/_journal.json'); const last=j.entries[j.entries.length-1]; if (last.tag !== '0016_phase33_seed_defaults_backfill') { console.error('expected last tag 0016_phase33_seed_defaults_backfill, got '+last.tag); process.exit(1); } const prev=j.entries[j.entries.length-2]; if (last.when <= prev.when) { console.error('when not strictly greater than prior'); process.exit(1); } console.log('journal OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/meta/0016_snapshot.json` exists, non-empty, valid JSON
    - `node -e "const j=require('./src/db/migrations/meta/_journal.json'); console.log(j.entries[j.entries.length-1].tag)"` outputs `0016_phase33_seed_defaults_backfill`
    - The new entry's `when` strictly > Plan 45-01's 0015 entry's `when`
    - `npx tsx scripts/validate-journal-monotonic.ts` exits 0
    - No hand-edits to existing entries — `git diff src/db/migrations/meta/_journal.json` shows only an APPEND change
  </acceptance_criteria>
  <done>
    Drizzle journal has new entry tagging 0016 with monotonic when; 0016_snapshot.json exists and is drizzle-kit-generated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire 0016 into scripts/test.sh + add post-apply smoke gate + bump regen-snapshots.sh sentinel</name>
  <files>scripts/test.sh, scripts/regen-snapshots.sh</files>
  <read_first>
    - scripts/test.sh (full file — note Plan 45-01 added MIGRATION_15_SQL + apply + smoke gate; this Task adds the 0016 sibling AFTER 0015's block)
    - scripts/regen-snapshots.sh (full file — Plan 45-01 added 0015 sentinels; this Task adds 0016)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-01-PLAN.md Task 3 + Task 4 (mirror exactly with 0016)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"scripts/test.sh" — pattern reference
  </read_first>
  <action>
    Three sub-edits across two files.

    **Edit (a) — `scripts/test.sh` MIGRATION_16_SQL variable:**

    Insert a new line AFTER the `MIGRATION_15_SQL=...` line that Plan 45-01 added (top of file, near line 22+):
    ```bash
    MIGRATION_16_SQL="src/db/migrations/0016_phase33_seed_defaults_backfill.sql"
    ```

    **Edit (b) — `scripts/test.sh` psql apply line + post-apply smoke gate:**

    Insert AFTER the 0015 apply block (which Plan 45-01 added):
    ```bash
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -q < "$MIGRATION_16_SQL"

    # Phase 45 SCHEMA-02 — post-migration backfill smoke gate.
    # Per HARD CO-LOC #M11-45b: SQL migration + journal entry + this assertion all
    # ship together. Validates that the cold-start seed rows now have the v3-Zod-required
    # nullable fields (energy_30d_mean / mood_30d_mean / anxiety_30d_mean on
    # profile_health.wellbeing_trend; notes / dependents on profile_family.parent_care_responsibilities).
    echo "🔍 Verifying migration 0016 seed-defaults backfill..."
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -tAq -c "
        SELECT wellbeing_trend ? 'energy_30d_mean'
           AND wellbeing_trend ? 'mood_30d_mean'
           AND wellbeing_trend ? 'anxiety_30d_mean'
        FROM profile_health WHERE substrate_hash = '' LIMIT 1;
      " | grep -q '^t$' || { echo '❌ Expected wellbeing_trend to have energy_30d_mean + mood_30d_mean + anxiety_30d_mean keys after migration 0016'; exit 1; }
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -tAq -c "
        SELECT parent_care_responsibilities ? 'notes'
           AND parent_care_responsibilities ? 'dependents'
        FROM profile_family WHERE substrate_hash = '' LIMIT 1;
      " | grep -q '^t$' || { echo '❌ Expected parent_care_responsibilities to have notes + dependents keys after migration 0016'; exit 1; }
    echo "✅ Migration 0016 seed-defaults backfill verified"
    ```

    The `?` operator is PostgreSQL's jsonb existence-check operator — returns true if the top-level key exists in the jsonb value. The smoke gate verifies post-UPDATE state.

    **Edit (c) — `scripts/regen-snapshots.sh` cleanup sentinels bump:**

    Mirror Plan 45-01 Task 4's pattern, this time adding 0016 alongside 0015. At each sentinel block (around lines 67, 118-122, 140-153 — exact line numbers shift after Plan 45-01's edits), add the matching 0016 line. The script's pattern is consistent — add 0016 wherever 0015 appears.

    Also update any "latest migration" comment in the script to point to 0016.

    **Anti-shallow-execution requirements:**
    - Place the 0016 apply line + smoke gate AFTER the 0015 apply block from Plan 45-01 — lexicographic apply order is the contract per D-18.
    - The smoke gate must NOT depend on Plan 45-04's fixture refresh — it queries the seed-row state immediately after migration apply.
    - Do NOT remove the 0015 smoke gate or any prior sentinel from Plan 45-01.
    - Do NOT introduce a new `MIGRATION_15B_SQL` or similar — 0016 is its own slot per D-03/D-05.
    - The jsonb-existence `?` operator MUST be quoted carefully in the bash heredoc/SQL string (it can clash with shell or psql parameter expansion in some contexts). The `-c "..."` quoting around the whole SQL string keeps it safe.
  </action>
  <verify>
    <automated>bash scripts/test.sh 2>&1 | grep -E "Migration 0016 seed-defaults backfill verified|0016 backfill failed" && grep -c "MIGRATION_16_SQL=" scripts/test.sh | xargs -I{} test {} -eq 1 && grep -c "0016" scripts/regen-snapshots.sh | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MIGRATION_16_SQL=" scripts/test.sh` returns `1`
    - `grep -c "Migration 0016 seed-defaults backfill" scripts/test.sh` returns `>= 1` (the smoke-gate echo line)
    - `grep -c "wellbeing_trend ? 'energy_30d_mean'" scripts/test.sh` returns `1`
    - `grep -c "parent_care_responsibilities ? 'notes'" scripts/test.sh` returns `1`
    - The apply-line `psql … < "$MIGRATION_16_SQL"` appears AFTER the `MIGRATION_15_SQL` apply line — `awk '/MIGRATION_15_SQL\"$/{got15=NR} /MIGRATION_16_SQL\"$/{got16=NR} END{exit !(got16>got15)}' scripts/test.sh` returns 0
    - `grep -c "0016" scripts/regen-snapshots.sh` returns `>= 1` (sentinels added)
    - `grep -c "0015" scripts/regen-snapshots.sh` unchanged from Plan 45-01's count (no removal of 0015 sentinels)
    - `bash -n scripts/regen-snapshots.sh` exits 0
    - `bash scripts/test.sh` exits 0 with the new 0016 smoke gate green AND the existing 0015 smoke gate (from Plan 45-01) still green
  </acceptance_criteria>
  <done>
    test.sh applies 0016 after 0015, runs the post-backfill smoke gate verifying jsonb key existence on the seed rows, and regen-snapshots.sh sentinels include 0016 alongside 0015. Full Docker suite green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 0016-seed-defaults-backfill integration test</name>
  <files>src/__tests__/migrations/0016-seed-defaults-backfill.test.ts</files>
  <read_first>
    - src/db/migrations/0016_phase33_seed_defaults_backfill.sql (Task 1 output — the migration under test)
    - src/__tests__/migrations/0015-check-constraints.test.ts (Plan 45-01 sibling test — mirror its style: vitest describe + postgres client setup + UPDATE + assertion)
    - src/memory/profile-schemas.ts (locate wellbeingTrendSchema + parentCareResponsibilitiesSchema — used by test case 4 to assert Zod .strict() parse acceptance)
    - src/db/schema.ts (profile_health + profile_family table types + seed row pattern with `name='primary'` and substrate_hash sentinels)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-05 (backfill semantics) + D-13/D-14 (Plan 45-04 dependency note — this test does NOT need fixture refresh; it operates on the seed rows directly)
  </read_first>
  <action>
    Create `src/__tests__/migrations/0016-seed-defaults-backfill.test.ts`. Test shape mirrors Plan 45-01's 0015-check-constraints.test.ts.

    **Test fixture state:** Tests assume the test DB has had migrations applied through 0016 (the `scripts/test.sh` apply chain runs this; vitest runs after). The cold-start seed rows (substrate_hash='') exist from 0013_psychological_profiles.sql.

    **Test cases under `describe('migration 0016: seed defaults backfill', () => { ... })`:**

    1. **`it('post-migration seed row has wellbeing_trend with energy_30d_mean, mood_30d_mean, anxiety_30d_mean keys', async () => { ... })`** — SELECT wellbeing_trend FROM profile_health WHERE substrate_hash = '' LIMIT 1. Assert the returned jsonb object has all 3 keys (`expect(result.wellbeing_trend).toHaveProperty('energy_30d_mean'); ...`). All 3 values should be `null` (the Zod-required-nullable shape).

    2. **`it('post-migration seed row has parent_care_responsibilities with notes + dependents keys', async () => { ... })`** — SELECT parent_care_responsibilities FROM profile_family WHERE substrate_hash = '' LIMIT 1. Assert `notes` exists and is `null`; assert `dependents` exists and is `[]` (empty array).

    3. **`it('fresh INSERT to profile_health uses the new wellbeing_trend DEFAULT', async () => { ... })`** — INSERT a new test row with NO wellbeing_trend column specified (let DEFAULT fire). SELECT the inserted row. Assert the inserted wellbeing_trend has the 3 nullable keys. Then DELETE the test row (cleanup).

    4. **`it('Zod v3 wellbeingTrendSchema.strict().parse() accepts the backfilled seed row', async () => { ... })`** — SELECT wellbeing_trend from a seed row; pass it through `wellbeingTrendSchema.strict().parse(...)`. Assert no throw. (This is the user-visible regression closure for SCHEMA-02: the schema_mismatch warn was Zod's strict() rejecting the seed row.)

    5. **`it('migration is idempotent — re-running on already-backfilled row is a no-op', async () => { ... })`** — Apply the SQL file again via psql; SELECT the seed row; assert unchanged. (Optional case if test infra supports re-apply within a vitest test; otherwise this is covered by Task 1's acceptance_criteria.)

    **Anti-shallow-execution requirements:**
    - Use the SAME postgres client setup pattern as Plan 45-01's 0015 test (verify against the file created in that plan).
    - For test case 3, use a unique `name` value (e.g., `'profile_health_0016_test_' + Date.now()`) for the INSERT so the test doesn't collide with the seed row.
    - For test case 4, the Zod schema's exact import path must be verified (`src/memory/profile-schemas.ts` exports — locate the actual export name and path).
    - Tests run in serial (no `test.concurrent`) — they share DB state.
    - Cleanup: any inserted test row from case 3 MUST be DELETEd in an `afterEach` or at the end of the test.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/0016-seed-defaults-backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/migrations/0016-seed-defaults-backfill.test.ts` exits 0
    - Test output shows 4-5 passing `it()` blocks
    - `grep -c "energy_30d_mean" src/__tests__/migrations/0016-seed-defaults-backfill.test.ts` returns `>= 2`
    - `grep -c "wellbeingTrendSchema\\|parent_care_responsibilities" src/__tests__/migrations/0016-seed-defaults-backfill.test.ts` returns `>= 2`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Integration test asserts post-migration state on cold-start seed rows (3 cases minimum: wellbeing_trend keys, parent_care_responsibilities keys, fresh-INSERT-uses-DEFAULT). Optional Zod .strict() acceptance + idempotency cases improve coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration apply ↔ existing prod data | UPDATE statements modify existing rows. WHERE clause `substrate_hash = '' AND <col> = '{}'::jsonb` scopes the change to cold-start seed rows only — rows that have run a real inference cycle (substrate_hash != '') are untouched. |
| schema ALTER ↔ subsequent inserts | ALTER COLUMN SET DEFAULT changes the runtime default for future INSERTs. No data is lost; the change is forward-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Tampering (T) | profile_health.wellbeing_trend rows | accept | Backfill scope is intentional + reviewed (per CONTEXT D-05). The UPDATE replaces `'{}'::jsonb` with a fully-populated nullable shape; no information is destroyed (`{}` had no information to begin with) and no rows outside the seed-row predicate are touched. |
| T-45-03-02 | Tampering (T) | profile_family.parent_care_responsibilities rows | accept | Same as T-45-03-01 — scoped, reviewed, no information loss. |
| T-45-03-03 | Denial of Service (D) | Migration apply failure mid-statement | mitigate | Each statement followed by `--> statement-breakpoint` (drizzle convention); ON_ERROR_STOP=1 in psql apply means the migration aborts cleanly on the first failure. The two UPDATEs are independent (different tables), and the two ALTER DEFAULTs are independent — partial-apply is harmless (the migration is idempotent under re-run; the journal won't mark it complete until all statements pass). |
| T-45-03-04 | Information Disclosure (I) | New column DEFAULTs leak schema shape to attacker | accept | The shape `{energy_30d_mean, mood_30d_mean, anxiety_30d_mean}` is already public knowledge via the Zod schema in `src/memory/profile-schemas.ts` (open-source repo) and the inference output in `psychological-shared.ts`. No new disclosure. |
</threat_model>

<verification>
- `bash scripts/test.sh` exits 0 — full Docker test suite green (per CLAUDE.md memory "always run full Docker tests"). Both the new 0016 smoke gate AND Plan 45-01's 0015 smoke gate pass.
- `npx tsc --noEmit` exits 0 (new vitest test file compiles).
- `npx tsx scripts/validate-journal-monotonic.ts` exits 0 (0016 entry has when > 0015's when).
- `npx vitest run src/__tests__/migrations/0016-seed-defaults-backfill.test.ts` exits 0 with 4+ passing tests.
- After migration applies on a populated DB: `psql -c "SELECT wellbeing_trend FROM profile_health WHERE substrate_hash = '' LIMIT 1"` returns a row with all 3 v3-Zod-required nullable keys present.
- After migration applies on a fresh DB: a new INSERT into profile_health with no wellbeing_trend column specified gets the new DEFAULT shape (3 nullable keys).
- The migration is idempotent: re-applying returns exit 0 with no state change.
</verification>

<success_criteria>
- `src/db/migrations/0016_phase33_seed_defaults_backfill.sql` exists with 2 UPDATEs + 2 ALTER COLUMN SET DEFAULTs targeting `profile_health.wellbeing_trend` + `profile_family.parent_care_responsibilities`, scoped to cold-start seed rows (substrate_hash='') with the v3-Zod-required nullable shape.
- Drizzle journal `_journal.json` has a new entry tagging `0016_phase33_seed_defaults_backfill` with monotonically-increasing when (strictly > Plan 45-01's 0015 entry).
- `0016_snapshot.json` exists (drizzle-kit generated).
- `scripts/test.sh` applies 0016 in the migration loop AFTER 0015 and runs a smoke gate verifying the seed-row jsonb shape post-apply.
- `scripts/regen-snapshots.sh` cleanup sentinels bumped to include 0016 (mirroring the 0015 bump from Plan 45-01).
- Integration test asserts the backfill effect on seed rows + DEFAULT change on fresh INSERTs + Zod .strict() parse acceptance.
- The plan ships ALL artifacts atomically per HARD CO-LOC #M11-45b (no half-merges).
- Plan 45-04 (FIX-06) is now unblocked: `regenerate-primed.ts` run against a DB that has 0016 applied will land fixtures whose jsonb shapes match the v3 Zod schemas, eliminating the PMT-06 schema_mismatch warns at the assertion level.
</success_criteria>

<output>
After completion, create `.planning/phases/45-schema-hygiene-fixture-cleanup/45-03-SUMMARY.md`
</output>