---
phase: 45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/0015_psychological_check_constraints.sql
  - src/db/migrations/meta/0015_snapshot.json
  - src/db/migrations/meta/_journal.json
  - scripts/test.sh
  - scripts/regen-snapshots.sh
  - src/__tests__/migrations/0015-check-constraints.test.ts
autonomous: true
requirements: [SCHEMA-01]
requirements_addressed: [SCHEMA-01]
tags: [migration, drizzle, check-constraint, defense-in-depth, psychological-profile, hexaco, schwartz, attachment]
must_haves:
  truths:
    - "Migration 0015 file exists at src/db/migrations/0015_psychological_check_constraints.sql; filename ordering puts it after Phase 43's 0014 and before Plan 45-03's 0016 (D-04, D-18)"
    - "Migration applies cleanly to a fresh DB (no rows yet) AND to a populated DB (existing rows with null-literal jsonb dim columns) — idempotent under re-run per the MD-02 guard pattern at 0013_psychological_profiles.sql:1-15"
    - "CHECK constraints on profile_hexaco add `score BETWEEN 1.0 AND 5.0 AND confidence BETWEEN 0.0 AND 1.0` for each of 6 dim columns (honesty_humility, emotionality, extraversion, agreeableness, conscientiousness, openness); each constraint tolerates `value = 'null'::jsonb` (uninitialized state)"
    - "CHECK constraints on profile_schwartz add `score BETWEEN 0.0 AND 7.0 AND confidence BETWEEN 0.0 AND 1.0` for each of 10 dim columns (self_direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism)"
    - "CHECK constraints on profile_attachment add `score BETWEEN 1.0 AND 5.0 AND confidence BETWEEN 0.0 AND 1.0` for each of 3 dim columns (anxious, avoidant, secure) — applied even though attachment is below D028 activation today (defense-in-depth for when it activates)"
    - "Out-of-range UPDATE rejected at DB layer: `UPDATE profile_hexaco SET honesty_humility='{\"score\":5.5,\"confidence\":0.8}'::jsonb WHERE name='primary'` returns 23514 (check_violation), even when bypassing Zod"
    - "Out-of-range confidence UPDATE rejected: `UPDATE profile_schwartz SET self_direction='{\"score\":3.0,\"confidence\":1.5}'::jsonb WHERE name='primary'` returns 23514"
    - "Null-literal jsonb continues to pass: `UPDATE profile_hexaco SET honesty_humility='null'::jsonb WHERE name='primary'` succeeds (preserves uninitialized seed state per 0013_psychological_profiles.sql:48 default)"
    - "`src/db/migrations/meta/_journal.json` has a new entry with idx=15 (or idx=14 + idx=15 if Phase 43's 0014 has not yet shipped — coordinate with current state), tag=`0015_psychological_check_constraints`, when value strictly greater than the prior entry's when (Phase 32 #3 monotonic guardrail)"
    - "`src/db/migrations/meta/0015_snapshot.json` exists, generated by `npx drizzle-kit generate` from the SQL file"
    - "`scripts/test.sh` has a new `MIGRATION_15_SQL=\"src/db/migrations/0015_psychological_check_constraints.sql\"` variable declared at the top + a `psql … < $MIGRATION_15_SQL` apply line in the migration block + a post-apply smoke gate verifying the 19 new constraints exist (`SELECT COUNT(*) FROM information_schema.check_constraints WHERE constraint_name LIKE 'profile_%_bounds'` returns 6 + 10 + 3 = 19 plus the 3 pre-existing overall_confidence_bounds)"
    - "`scripts/regen-snapshots.sh` cleanup-flag sentinels bumped from MIGRATION_13→MIGRATION_15 + matching 0015 cleanup sentinels added (mirror existing 0013 bump pattern at lines 67, 118-122, 140-153)"
    - "Integration test `src/__tests__/migrations/0015-check-constraints.test.ts` exercises 6 cases: (a) in-range HEXACO UPDATE succeeds, (b) out-of-range HEXACO score (5.5) rejected with 23514, (c) out-of-range Schwartz score (-0.5) rejected, (d) out-of-range confidence (1.1) rejected, (e) null-literal jsonb still accepted, (f) all-19-bounds-constraints exist (introspection query)"
    - "`npx tsc --noEmit` clean; `bash scripts/test.sh` exits 0 with the new smoke gate green and the new vitest file passing"
  artifacts:
    - path: "src/db/migrations/0015_psychological_check_constraints.sql"
      provides: "19 ALTER TABLE … ADD CONSTRAINT statements + header comment block with HARD CO-LOC #M11-45a + cross-phase note about Phase 43 owning slot 0014 (D-04 + D-18)"
      min_lines: 90
      contains: "profile_hexaco_honesty_humility_bounds"
    - path: "src/db/migrations/meta/0015_snapshot.json"
      provides: "drizzle-kit generated snapshot reflecting the 19 new constraints"
      min_lines: 10
      contains: "0015_psychological_check_constraints"
    - path: "src/db/migrations/meta/_journal.json"
      provides: "Appended idx=15 entry with monotonically-increasing when value"
      min_lines: 105
      contains: "0015_psychological_check_constraints"
    - path: "scripts/test.sh"
      provides: "MIGRATION_15_SQL variable + apply line + 0015 smoke gate"
      contains: "MIGRATION_15_SQL"
    - path: "src/__tests__/migrations/0015-check-constraints.test.ts"
      provides: "6 integration-test cases for the new CHECK constraints"
      min_lines: 100
      contains: "describe('migration 0015"
  key_links:
    - from: "src/db/migrations/0015_psychological_check_constraints.sql"
      to: "src/db/migrations/0013_psychological_profiles.sql"
      via: "ALTER TABLE references the tables created in 0013; CHECK syntax mirrors 0013's overall_confidence_bounds (line 57, 82)"
      pattern: "ALTER TABLE \"profile_hexaco\" ADD CONSTRAINT"
    - from: "src/db/migrations/meta/_journal.json"
      to: "src/db/migrations/0015_psychological_check_constraints.sql"
      via: "drizzle-kit journal entry maps idx→tag; when strictly > prior entry per Phase 32 #3 guardrail"
      pattern: "\"tag\": \"0015_psychological_check_constraints\""
    - from: "scripts/test.sh"
      to: "src/db/migrations/0015_psychological_check_constraints.sql"
      via: "CI test bootstrap applies the migration; smoke gate verifies constraint existence"
      pattern: "MIGRATION_15_SQL=\"src/db/migrations/0015_psychological_check_constraints.sql\""
---

<objective>
Ship Phase 45 SCHEMA-01: add 19 defense-in-depth DB CHECK constraints to the psychological profile jsonb per-dim score/confidence columns. The constraints enforce HEXACO score ∈ [1.0, 5.0], Schwartz score ∈ [0.0, 7.0], and confidence ∈ [0.0, 1.0] across `profile_hexaco`, `profile_schwartz`, `profile_attachment`. A non-Zod-validated UPDATE can no longer slip out-of-range scores past the DB.

Purpose: SCHEMA-01 — defense-in-depth behind the existing Zod read-time parse (`psychological-schemas.ts` v3 schemas). Root cause from `37-REVIEW.md` WR-01 lines 45-50: per-dim ranges are part of the Never-Retrofit Checklist intent but were not encoded as DB-level CHECK constraints.

This plan is HARD CO-LOCATION #M11-45a per the pattern in 0013_psychological_profiles.sql:1-15: the SQL migration + drizzle journal entry + snapshot + `scripts/test.sh` apply line + smoke gate + `scripts/regen-snapshots.sh` cleanup-flag bump ALL ship in one atomic commit. Splitting any of these reproduces the M010 PITFALL M010-11 lineage break.

Output: 1 new migration SQL file + 1 new snapshot json (drizzle-kit generated) + journal entry append + `scripts/test.sh` apply line + `scripts/regen-snapshots.sh` sentinel bump + 1 new integration test file (`src/__tests__/migrations/0015-check-constraints.test.ts`).

Satisfies: SCHEMA-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md

# Root-cause reviews
@.planning/milestones/v2.6-phases/37-psychological-substrate/37-REVIEW.md

# Direct in-repo analog: Phase 37 migration that created the tables this plan constrains
@src/db/migrations/0013_psychological_profiles.sql
@src/db/migrations/meta/_journal.json
@src/db/migrations/meta/0013_snapshot.json

# Existing test.sh + regen-snapshots.sh patterns (mirror exactly)
@scripts/test.sh
@scripts/regen-snapshots.sh
@scripts/validate-journal-monotonic.ts

# Drizzle config (no edits expected; for reference)
@drizzle.config.ts

# Phase 32 #3 monotonic-when guardrail context (already enforced; just verify we honor it)
@.planning/milestones/v2.5-phases/32-test-rerun-bug/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 0015_psychological_check_constraints.sql migration</name>
  <files>src/db/migrations/0015_psychological_check_constraints.sql</files>
  <read_first>
    - src/db/migrations/0013_psychological_profiles.sql (full file — header pattern, idempotency guard, existing overall_confidence_bounds CHECK constraint syntax at lines 57, 82, 100)
    - src/db/migrations/0012_operational_profiles.sql (lines 32-45 for existing CHECK-constraint style)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md (D-04 — exact ranges + constraint scope; D-18 — slot 0015 because Phase 43 owns 0014)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"src/db/migrations/0015_psychological_check_constraints.sql" — verbatim ALTER TABLE template
    - .planning/milestones/v2.6-phases/37-psychological-substrate/37-REVIEW.md §WR-01 (lines 45-50 — recommended fix wording)
    - src/db/schema.ts:672-740 (psychological profile table definitions — confirm column names match exactly: HEXACO `honesty_humility, emotionality, extraversion, agreeableness, conscientiousness, openness`; Schwartz `self_direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism`; Attachment `anxious, avoidant, secure`)
  </read_first>
  <action>
    Create `src/db/migrations/0015_psychological_check_constraints.sql`. File structure (top to bottom):

    **Header comment block** (mirror style of `0013_psychological_profiles.sql:1-38`):
    - Lines 1-9: MD-02 idempotency-guard rationale (re-runnable migration safety) — use the exact 5-line preamble from `0013_psychological_profiles.sql:1-5`
    - Blank line
    - Lines 11-13: Attribution — `-- Phase 45 (v2.6.1) — Psychological profile defense-in-depth CHECK constraints.\n-- HARD CO-LOCATION #M11-45a: this SQL + src/db/schema.ts (no edits — schema.ts only declares\n-- top-level overall_confidence CHECK; per-dim CHECKs intentionally migration-only per CONTEXT D-04) +`
    - Lines 14-16: `-- migrations/meta/0015_snapshot.json + _journal.json entry (idx 15) +\n-- scripts/test.sh psql apply line + 0015 smoke gate +\n-- scripts/regen-snapshots.sh cleanup-flag bump ALL ship in Plan 45-01 atomically.`
    - Blank line
    - Lines 18-20: Cross-phase coordination — `-- Migration slot 0014 is owned by Phase 43 (CONTRACT-03 column addition); Phase 45's two\n-- migrations occupy slots 0015 + 0016 per CONTEXT.md D-18.`
    - Blank line
    - Lines 22-27: Scope statement — `-- Constraint scope (per CONTEXT D-04 + 37-REVIEW.md §WR-01 lines 45-50):\n--   HEXACO dims (6): score ∈ [1.0, 5.0], confidence ∈ [0.0, 1.0]\n--   Schwartz dims (10): score ∈ [0.0, 7.0], confidence ∈ [0.0, 1.0]\n--   Attachment dims (3): score ∈ [1.0, 5.0], confidence ∈ [0.0, 1.0]\n-- Each constraint tolerates value = 'null'::jsonb (uninitialized seed state per\n-- 0013_psychological_profiles.sql:48 default) via OR-branch.`
    - Blank line
    - Lines 29-32: Anti-Zod-bypass rationale — `-- Defense-in-depth: psychological-schemas.ts Zod v3 validates these ranges at\n-- the READ boundary, but a non-Zod-validated UPDATE could slip out-of-range scores\n-- past the DB. These constraints close that gap per 37-REVIEW.md WR-01 Fix-(b).`

    **Body — ALTER TABLE statements, 19 total** (use `ADD CONSTRAINT IF NOT EXISTS` if PostgreSQL version supports it — verify against `docker-compose.local.yml`'s pgvector/pgvector:pg16 image; pg16 supports `ADD CONSTRAINT IF NOT EXISTS`. If targeting pg<16, use `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` wrapper):

    For each of the 19 (dim, table, score_low, score_high) tuples below, emit:
    ```sql
    ALTER TABLE "<table>" ADD CONSTRAINT IF NOT EXISTS "<table>_<dim>_bounds"
      CHECK (
        "<dim>" = 'null'::jsonb
        OR (
          ("<dim>"->>'score')::numeric BETWEEN <low> AND <high>
          AND ("<dim>"->>'confidence')::numeric BETWEEN 0.0 AND 1.0
        )
      );
    --> statement-breakpoint
    ```

    Tuples (verify column names from `src/db/schema.ts:672-740`):
    - profile_hexaco: honesty_humility 1.0-5.0, emotionality 1.0-5.0, extraversion 1.0-5.0, agreeableness 1.0-5.0, conscientiousness 1.0-5.0, openness 1.0-5.0
    - profile_schwartz: self_direction 0.0-7.0, stimulation 0.0-7.0, hedonism 0.0-7.0, achievement 0.0-7.0, power 0.0-7.0, security 0.0-7.0, conformity 0.0-7.0, tradition 0.0-7.0, benevolence 0.0-7.0, universalism 0.0-7.0
    - profile_attachment: anxious 1.0-5.0, avoidant 1.0-5.0, secure 1.0-5.0

    Total 19 ALTER TABLE statements, each followed by the drizzle `--> statement-breakpoint` marker (mirroring `0013_psychological_profiles.sql:59, 84, 102`).

    **Anti-shallow-execution requirements:**
    - Do NOT add CHECK constraints to schema.ts — CONTEXT D-04 + PATTERNS §0015 explicitly say the per-dim CHECKs are migration-only (schema.ts already declares only the top-level overall_confidence CHECK).
    - Do NOT introduce additional ALTER TABLE statements beyond the 19 listed. The scope is fixed by CONTEXT D-04.
    - Do NOT remove or modify the existing `overall_confidence_bounds` constraints from 0013 — they remain in force; this migration only adds NEW per-dim constraints.
    - Each constraint name follows the project convention `<table>_<column>_bounds` exactly (mirror existing `profile_hexaco_overall_confidence_bounds` at 0013:57).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.local.yml exec -T postgres psql -U chris -d chris -v ON_ERROR_STOP=1 -q < src/db/migrations/0015_psychological_check_constraints.sql && docker compose -f docker-compose.local.yml exec -T postgres psql -U chris -d chris -tAc "SELECT COUNT(*) FROM information_schema.check_constraints WHERE constraint_name LIKE 'profile_%_bounds';"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^ALTER TABLE "profile_hexaco" ADD CONSTRAINT' src/db/migrations/0015_psychological_check_constraints.sql` returns `6`
    - `grep -c '^ALTER TABLE "profile_schwartz" ADD CONSTRAINT' src/db/migrations/0015_psychological_check_constraints.sql` returns `10`
    - `grep -c '^ALTER TABLE "profile_attachment" ADD CONSTRAINT' src/db/migrations/0015_psychological_check_constraints.sql` returns `3`
    - `grep -c "= 'null'::jsonb" src/db/migrations/0015_psychological_check_constraints.sql` returns `19` (every constraint has the null-literal OR-branch)
    - `grep -cE "BETWEEN 1.0 AND 5.0" src/db/migrations/0015_psychological_check_constraints.sql` returns `9` (6 HEXACO + 3 attachment)
    - `grep -cE "BETWEEN 0.0 AND 7.0" src/db/migrations/0015_psychological_check_constraints.sql` returns `10` (Schwartz)
    - `grep -cE "BETWEEN 0.0 AND 1.0" src/db/migrations/0015_psychological_check_constraints.sql` returns `19` (every confidence check)
    - `grep -c -- "--> statement-breakpoint" src/db/migrations/0015_psychological_check_constraints.sql` returns `19`
    - `grep -c "HARD CO-LOCATION #M11-45a" src/db/migrations/0015_psychological_check_constraints.sql` returns `1`
    - Applying the SQL to a fresh DB via `psql -v ON_ERROR_STOP=1` returns exit 0
    - Re-applying to the same DB (idempotency) returns exit 0 (the `ADD CONSTRAINT IF NOT EXISTS` clause OR the DO-block wrapper guards against duplicate-constraint errors)
  </acceptance_criteria>
  <done>
    SQL file authored with 19 CHECK constraints across 3 tables, header references HARD CO-LOC #M11-45a and CONTEXT D-04/D-18, idempotency guard works, and constraint syntax mirrors 0013_psychological_profiles.sql:57 exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate drizzle snapshot + append _journal.json entry</name>
  <files>src/db/migrations/meta/0015_snapshot.json, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - src/db/migrations/meta/_journal.json (full file — note current latest entry: idx 13, when=1778699398922, tag=0013_psychological_profiles)
    - src/db/migrations/meta/0013_snapshot.json (existing snapshot for reference — drizzle-kit output format)
    - scripts/regen-snapshots.sh (full file — pattern for invoking drizzle-kit + cleanup; we will NOT run regen-snapshots.sh as part of execution because it does broad cleanup; instead we invoke `npx drizzle-kit generate` directly with the existing 0015 SQL in place and then accept the snapshot output)
    - scripts/validate-journal-monotonic.ts (Phase 32 #3 guardrail — verifies entries[i].when < entries[i+1].when; new 0015 entry must have when strictly greater than 0014's [or 0013's if 0014 not yet shipped])
    - drizzle.config.ts (drizzle-kit config — confirms migration directory + schema path)
  </read_first>
  <action>
    Two operations, in order:

    **(a) Run `npx drizzle-kit generate`** with the 0015 SQL file already present in `src/db/migrations/`. Drizzle-kit detects the manually-authored migration and produces:
    - `src/db/migrations/meta/0015_snapshot.json` — a fresh snapshot of the schema AFTER applying 0015 (CHECK constraints are diffable schema state in drizzle-kit's model)
    - An appended entry in `src/db/migrations/meta/_journal.json` with `idx=15` (or whatever the next idx is — drizzle-kit auto-increments), `tag="0015_psychological_check_constraints"`, `when=<Date.now()>`, `version="7"`, `breakpoints=true`.

    Execution command (from project root):
    ```bash
    npx drizzle-kit generate
    ```

    If drizzle-kit prompts for input (e.g., "name the migration"), use `--name 0015_psychological_check_constraints` as a CLI flag if the version supports it; otherwise check whether drizzle-kit picked up the existing 0015 filename automatically and skipped the prompt.

    **(b) Verify monotonic-when invariant**: open the regenerated `_journal.json`, confirm the new idx=15 entry has `when` strictly greater than the prior entry (idx=14 if Phase 43 has shipped; else idx=13 with when=1778699398922). If the new when is NOT strictly greater (could happen if system clock is wrong), manually edit the entry's `when` to be `<prior_when> + 1` to satisfy `scripts/validate-journal-monotonic.ts`. The validator is invoked in `scripts/test.sh:51` BEFORE migrations apply.

    **Anti-shallow-execution requirements:**
    - Do NOT hand-edit `0015_snapshot.json` — that's drizzle-kit's job. Hand-editing breaks the drizzle migrator's invariants and future `regen-snapshots.sh` runs.
    - Do NOT modify any existing journal entries (idx 0..13 or 14). Only append.
    - If Phase 43 (`0014_*`) is not yet on disk when this plan executes, that's fine — drizzle-kit's `generate` will not create a 0014 phantom; it will just process whatever SQL files exist in the migrations directory. The 0015 entry will land at idx=14 in that case. CONTEXT D-18 acknowledges this cross-phase coordination — Phase 43 will fix up the ordering when it lands.
    - The new `when` value MUST be strictly greater than every existing `when`. If running with a clock-skewed environment, hand-bump as documented above.
  </action>
  <verify>
    <automated>npx tsx scripts/validate-journal-monotonic.ts && node -e "const j=require('./src/db/migrations/meta/_journal.json'); const last=j.entries[j.entries.length-1]; if (last.tag !== '0015_psychological_check_constraints') { console.error('expected last tag 0015_psychological_check_constraints, got '+last.tag); process.exit(1); } if (typeof last.when !== 'number') { console.error('when is not a number'); process.exit(1); } console.log('journal OK: idx='+last.idx+' tag='+last.tag+' when='+last.when);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/meta/0015_snapshot.json` exists, non-empty, valid JSON
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('src/db/migrations/meta/_journal.json','utf8')).entries.find(e=>e.tag==='0015_psychological_check_constraints')?.when ?? 'MISSING')"` outputs a number strictly greater than `1778699398922`
    - `npx tsx scripts/validate-journal-monotonic.ts` exits 0
    - No hand-edits to existing entries 0..13 (or 0..14) — verifiable by `git diff src/db/migrations/meta/_journal.json` showing only an APPEND change, no modifications to prior lines
  </acceptance_criteria>
  <done>
    Drizzle journal has a new idx-N entry with tag=0015_psychological_check_constraints and monotonic when; 0015_snapshot.json exists and is drizzle-kit-generated (not hand-authored).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire migration into scripts/test.sh + add post-apply smoke gate</name>
  <files>scripts/test.sh</files>
  <read_first>
    - scripts/test.sh (full file — observe the existing pattern: line 21 declares MIGRATION_13_SQL, line 86 applies it via psql, lines 88-122 are the existing post-0006 smoke gate that demonstrates the smoke-gate pattern)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"scripts/test.sh" — exact pattern to mirror
  </read_first>
  <action>
    Edit `scripts/test.sh`. Two changes:

    **(a) Add MIGRATION_15_SQL variable** — insert a new line AFTER line 21 (after `MIGRATION_13_SQL=...`):
    ```bash
    MIGRATION_15_SQL="src/db/migrations/0015_psychological_check_constraints.sql"
    ```

    If Phase 43's 0014 has already shipped and added `MIGRATION_14_SQL=...`, insert 0015's line AFTER that. If not, leave a gap (the variable name 0015 stands on its own; the apply order is determined by lexicographic filename sort in the actual `psql` block, not by variable name order).

    **(b) Add psql apply line** — insert a new block AFTER line 86 (after the existing `psql … < "$MIGRATION_13_SQL"` block):
    ```bash
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -q < "$MIGRATION_15_SQL"
    ```

    Again, if Phase 43's 0014 has shipped, insert AFTER 0014's apply block to preserve lexicographic order.

    **(c) Add post-apply smoke gate** (mirror the 0006 smoke gate at lines 88-122). Insert after the new 0015 apply block:
    ```bash
    # Phase 45 SCHEMA-01 — post-migration CHECK-constraint smoke gate.
    # Per HARD CO-LOC #M11-45a: SQL migration + journal entry + this assertion
    # all ship together. Validates that all 19 new per-dim bounds constraints
    # (6 HEXACO + 10 Schwartz + 3 attachment) exist after migration apply.
    echo "🔍 Verifying migration 0015 CHECK constraints..."
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -tAq -c "
        SELECT COUNT(*) FROM information_schema.check_constraints
        WHERE constraint_name LIKE 'profile_hexaco_%_bounds'
          AND constraint_name <> 'profile_hexaco_overall_confidence_bounds';
      " | grep -q '^6$' || { echo '❌ Expected 6 profile_hexaco per-dim bounds constraints (HEXACO 6 dims); see migration 0015'; exit 1; }
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -tAq -c "
        SELECT COUNT(*) FROM information_schema.check_constraints
        WHERE constraint_name LIKE 'profile_schwartz_%_bounds'
          AND constraint_name <> 'profile_schwartz_overall_confidence_bounds';
      " | grep -q '^10$' || { echo '❌ Expected 10 profile_schwartz per-dim bounds constraints (Schwartz 10 dims); see migration 0015'; exit 1; }
    docker compose -f "$COMPOSE_FILE" exec -T postgres \
      psql -U chris -d chris -v ON_ERROR_STOP=1 -tAq -c "
        SELECT COUNT(*) FROM information_schema.check_constraints
        WHERE constraint_name LIKE 'profile_attachment_%_bounds'
          AND constraint_name <> 'profile_attachment_overall_confidence_bounds';
      " | grep -q '^3$' || { echo '❌ Expected 3 profile_attachment per-dim bounds constraints (attachment 3 dims); see migration 0015'; exit 1; }
    echo "✅ Migration 0015 CHECK constraints verified (6+10+3 = 19 per-dim bounds)"
    ```

    **Anti-shallow-execution requirements:**
    - Do NOT touch existing `MIGRATION_*_SQL` variables or apply lines.
    - Do NOT change the order of existing apply lines.
    - The smoke gate MUST run AFTER the 0015 apply (and after 0014 if Phase 43 has landed it) but BEFORE the existing 0013-and-prior smoke gates that come after. Place it immediately after the 0015 apply block.
    - The smoke gate must NOT depend on Phase 43's 0014 column being present — 0015 only adds CHECK constraints on jsonb columns that already exist from 0013.
  </action>
  <verify>
    <automated>bash scripts/test.sh 2>&1 | grep -E "Migration 0015 CHECK constraints verified|❌.*0015"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MIGRATION_15_SQL=" scripts/test.sh` returns `1`
    - `grep -c "MIGRATION_15_SQL" scripts/test.sh` returns `>= 2` (variable declaration + apply line reference)
    - `grep -c 'Migration 0015 CHECK constraints' scripts/test.sh` returns `1`
    - `grep -c "profile_hexaco_%_bounds" scripts/test.sh` returns `>= 1`
    - The apply-line `psql … < "$MIGRATION_15_SQL"` appears AFTER the `MIGRATION_13_SQL` apply line in line order — `awk '/MIGRATION_13_SQL\"$/{got13=NR} /MIGRATION_15_SQL\"$/{got15=NR} END{exit !(got15>got13)}' scripts/test.sh` returns 0
    - `bash scripts/test.sh` exits 0 (full Docker test suite — per CLAUDE.md memory "always run full Docker tests")
    - The smoke gate emits `✅ Migration 0015 CHECK constraints verified` line on success
  </acceptance_criteria>
  <done>
    test.sh applies 0015 via psql, runs the 19-constraint smoke gate, and the full Docker test suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Bump scripts/regen-snapshots.sh cleanup sentinels</name>
  <files>scripts/regen-snapshots.sh</files>
  <read_first>
    - scripts/regen-snapshots.sh (full file — note the existing bump pattern at lines 67, 118-122, 140-153 where MIGRATION_12→MIGRATION_13 was already applied; mirror that for MIGRATION_13→MIGRATION_15 — skipping 14 because Phase 43 owns that bump and Phase 45 only owns 0015 in THIS plan)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"scripts/regen-snapshots.sh cleanup sentinels"
  </read_first>
  <action>
    Edit `scripts/regen-snapshots.sh` to add MIGRATION_15 sentinels. The script's pattern is: each new migration adds (i) a reference variable in the cleanup-sentinel block, (ii) a guarded `find -delete` cleanup line, (iii) a journal-entry reference.

    Mirror exactly what was done for MIGRATION_13 (the most recent existing bump):

    **(a)** At the existing MIGRATION_13 reference variable (line ~67), add a MIGRATION_15 sibling. If Phase 43's plans have already added MIGRATION_14, insert MIGRATION_15 after it; else insert after MIGRATION_13.

    **(b)** At each cleanup-sentinel guard (lines 118-122, 140-153), add the matching 0015 line.

    **(c)** Update any "latest migration" pointer or comment that says "0013" to say "0015" (preserve 0014 reference if Phase 43 has landed it; else leave a comment "0014 owned by Phase 43, will be added on its merge").

    The exact line numbers will shift if Phase 43 has landed first; the executor should diff against the most recent commit on `main` to identify the current 0013 references and add 0015 siblings (and 0014 if missing, with a TODO comment to coordinate with Phase 43).

    **Anti-shallow-execution requirements:**
    - Do NOT remove the 0013 or earlier sentinels — they remain in the script (the cleanup guards multiple migrations, not just the latest).
    - Do NOT regenerate snapshots by running `regen-snapshots.sh` as part of this plan — Task 2 already generated 0015_snapshot.json via `drizzle-kit generate` (the lightweight path); regen-snapshots.sh is the broader sweep used for full re-baselining and is out of scope for this plan.
  </action>
  <verify>
    <automated>grep -c "0015" scripts/regen-snapshots.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0015" scripts/regen-snapshots.sh` returns `>= 2` (matches the count pattern established for 0013)
    - `grep -c "0015_psychological_check_constraints" scripts/regen-snapshots.sh` returns `>= 1`
    - All existing 0013 references remain (`grep -c "0013" scripts/regen-snapshots.sh` returns >= the pre-edit count)
    - `bash -n scripts/regen-snapshots.sh` exits 0 (shell syntax valid)
  </acceptance_criteria>
  <done>
    regen-snapshots.sh cleanup sentinels bumped to include 0015, syntax valid, no removals of prior sentinels.
  </done>
</task>

<task type="auto">
  <name>Task 5: Author 0015-check-constraints integration test</name>
  <files>src/__tests__/migrations/0015-check-constraints.test.ts</files>
  <read_first>
    - src/db/migrations/0015_psychological_check_constraints.sql (Task 1 output — the constraints under test)
    - src/__tests__/migrations/ (directory — note: this directory may not yet exist; if not, mkdir and create. If a similar prior test exists, mirror its style)
    - src/db/schema.ts:672-740 (psychological profile table types — for fixture row construction)
    - src/__tests__/fixtures/load-primed.ts:46-65 (postgres client setup pattern, for reference)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"Plan 45-01" — test cases enumerated
  </read_first>
  <action>
    Create `src/__tests__/migrations/0015-check-constraints.test.ts` (mkdir parent if needed). The test exercises the new CHECK constraints against a running test DB (Docker postgres from `scripts/test.sh` — see existing integration-test pattern in the repo). Pattern: vitest `describe`/`it`, opens a postgres connection, runs UPDATE statements, asserts on the error code returned.

    **Test fixture:** The migration 0013 has already INSERTed seed rows with `name='primary'` and `substrate_hash=''` for each table. Tests UPDATE the seed row directly.

    **Test cases (organize under `describe('migration 0015: psychological CHECK constraints', () => { ... })`):**

    1. **`it('rejects out-of-range HEXACO score (>5.0)', async () => { ... })`** — `UPDATE profile_hexaco SET honesty_humility='{"score":5.5,"confidence":0.8,"last_updated":"2026-05-14T12:00:00Z"}'::jsonb WHERE name='primary'` — expect postgres error with `code === '23514'` (check_violation) and `constraint === 'profile_hexaco_honesty_humility_bounds'`.

    2. **`it('rejects out-of-range HEXACO score (<1.0)', async () => { ... })`** — same shape, score=0.5 — expect 23514.

    3. **`it('rejects out-of-range Schwartz score (>7.0)', async () => { ... })`** — `UPDATE profile_schwartz SET self_direction='{"score":7.5,"confidence":0.6,"last_updated":"..."}'::jsonb WHERE name='primary'` — expect 23514, constraint=`profile_schwartz_self_direction_bounds`.

    4. **`it('rejects out-of-range confidence (>1.0)', async () => { ... })`** — `UPDATE profile_hexaco SET emotionality='{"score":3.0,"confidence":1.5,"last_updated":"..."}'::jsonb WHERE name='primary'` — expect 23514.

    5. **`it('accepts in-range HEXACO update', async () => { ... })`** — `UPDATE profile_hexaco SET extraversion='{"score":4.2,"confidence":0.75,"last_updated":"..."}'::jsonb WHERE name='primary'` — succeeds; verify with `SELECT extraversion FROM profile_hexaco WHERE name='primary'` returns the inserted shape.

    6. **`it('accepts null-literal jsonb (uninitialized seed state)', async () => { ... })`** — `UPDATE profile_hexaco SET conscientiousness='null'::jsonb WHERE name='primary'` — succeeds (defense for the 0013 seed row default).

    7. **`it('all 19 per-dim bounds constraints exist', async () => { ... })`** — `SELECT constraint_name FROM information_schema.check_constraints WHERE constraint_name LIKE 'profile_%_bounds' AND constraint_name NOT LIKE '%_overall_confidence_bounds'` — expect array of length 19 (6 HEXACO + 10 Schwartz + 3 attachment), each name follows `profile_<table>_<dim>_bounds` pattern.

    **Anti-shallow-execution requirements:**
    - Tests must use the existing DB connection pattern (verify against an in-repo migration-test if one exists; else use direct `postgres()` client per `load-primed.ts` style). Do NOT use Drizzle's typed builder for UPDATEs — postgres.js's `client.unsafe` with parameterized SQL is required because Drizzle won't let you craft an intentionally-invalid jsonb shape.
    - Each test that asserts a CHECK violation must catch the error and assert on `err.code === '23514'` AND `err.constraint_name === '<expected>'` — not just on whether the UPDATE threw.
    - Tests run in serial (avoid parallel UPDATEs against the same row) — use vitest's `describe.sequential` or set `test.concurrent` to false.
    - Cleanup: after each test, reset the row to its 0013 default (`UPDATE profile_hexaco SET honesty_humility='null'::jsonb WHERE name='primary'`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/0015-check-constraints.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/migrations/0015-check-constraints.test.ts` exits 0
    - Test output shows 7 passing `it()` blocks
    - `grep -c "23514" src/__tests__/migrations/0015-check-constraints.test.ts` >= 4 (the 4 violation cases each assert on the code)
    - `grep -c "constraint" src/__tests__/migrations/0015-check-constraints.test.ts` >= 4 (each violation case asserts on the constraint name)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Integration test runs against a real DB, exercises all 4 violation paths + 1 acceptance path + 1 null-literal path + 1 introspection assertion, and passes when 0015 is applied.
  </done>
</task>

</tasks>

<verification>
- `bash scripts/test.sh` exits 0 (full Docker integration suite — per CLAUDE.md memory "always run full Docker tests"). The new MIGRATION_15 apply block + smoke gate run automatically and gate the rest of the suite.
- `npx tsc --noEmit` exits 0 (no new TS errors from the test file).
- `npx tsx scripts/validate-journal-monotonic.ts` exits 0 (new 0015 journal entry has monotonic when value).
- `npx vitest run src/__tests__/migrations/0015-check-constraints.test.ts` exits 0 with 7 passing tests.
- A direct `UPDATE` bypassing Zod against the test DB rejects out-of-range scores: `psql -c "UPDATE profile_hexaco SET honesty_humility='{\"score\":5.5,\"confidence\":0.8}'::jsonb WHERE name='primary'"` returns exit 1 with SQLSTATE 23514.
</verification>

<success_criteria>
- `src/db/migrations/0015_psychological_check_constraints.sql` exists with 19 ALTER TABLE … ADD CONSTRAINT statements covering all per-dim jsonb columns of profile_hexaco / profile_schwartz / profile_attachment, each tolerating `'null'::jsonb` for the uninitialized seed state.
- Drizzle journal `_journal.json` has a new entry tagging `0015_psychological_check_constraints` with monotonically-increasing when; `0015_snapshot.json` exists.
- `scripts/test.sh` applies 0015 in the migration loop and runs a 19-constraint smoke gate that fails the suite if any constraint is missing.
- `scripts/regen-snapshots.sh` cleanup sentinels bumped to include 0015 (mirroring the existing 0013 bump pattern).
- Integration test asserts that out-of-range HEXACO/Schwartz scores and out-of-range confidence values are rejected with SQLSTATE 23514 (check_violation), bypassing Zod via direct SQL UPDATE.
- The plan ships ALL artefacts atomically per HARD CO-LOC #M11-45a (no half-merges that leave migration without journal, or vice versa).
</success_criteria>

<output>
After completion, create `.planning/phases/45-schema-hygiene-fixture-cleanup/45-01-SUMMARY.md`
</output>
