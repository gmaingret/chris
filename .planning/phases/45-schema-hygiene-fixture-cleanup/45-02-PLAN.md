---
phase: 45
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/synthesize-delta.ts
  - scripts/synthesize-episodic.ts
  - scripts/fetch-prod-data.ts
  - scripts/regenerate-primed.ts
  - scripts/.ssh-known-hosts
  - src/__tests__/fixtures/load-primed.ts
  - src/__tests__/fixtures/primed-sanity-m011.test.ts
  - src/memory/profiles/__tests__/integration-m011-1000words.test.ts
  - src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl
  - src/__tests__/fixtures/__fixtures__/pgvector-smoke/MANIFEST.json
  - src/__tests__/fixtures/load-primed-pgvector.test.ts
  - .planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-07, FIX-08]
requirements_addressed: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-07, FIX-08]
tags: [fixture-pipeline, operator-scripts, ssh-hardening, pgvector, sigint, abort-controller, harn, word-count, calendar-month-window, synth-delta]
must_haves:
  truths:
    - "FIX-01: synthesize-delta.ts:934 pre-filters fusedContradictions against Set(fusedPensieve.map(p=>String(p.id))); drops orphan FK references silently; emits one summary log event `synth.contradictions.dropped` with droppedCount + totalCount when droppedCount > 0 (D-06)"
    - "FIX-02a: synthesize-delta.ts phrasesClause append at lines 584-594 is independent of dimensionHint truthiness — phrasesClause is appended in its own `if (phrasesClause)` branch outside the `if (dimensionHint)` branch (D-07a)"
    - "FIX-02b: HARN + integration test FIXTURE_PATH constants updated to `m011-1000words-5days` (matching synthesize-delta.ts:937 `${milestone}-${targetDays}days` naming) — primed-sanity-m011.test.ts, integration-m011-1000words.test.ts, and deferred-items.md docs all consistent (D-07b)"
    - "FIX-03: synthesize-episodic.ts MIGRATIONS list is glob-derived from `src/db/migrations/*.sql` at runtime via readdir + filter(endsWith('.sql')) + sort — NOT hardcoded `0000..0005`; meta/ subdirectory naturally excluded by .sql filter (D-08)"
    - "FIX-04: fetch-prod-data.ts SSH argv adds `-o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=scripts/.ssh-known-hosts` BEFORE the SSH_TARGET position (D-09, D-10); first-connect populates known_hosts; second-connect with rotated key is rejected (MITM-after-rotation window closed)"
    - "FIX-04 (cont): scripts/.ssh-known-hosts exists as a new committed repo-config file with header comment block documenting (a) the Phase 45 FIX-04 rationale, (b) the bootstrap procedure (first operator pre-commits `ssh-keyscan -H 192.168.1.50` output), (c) the rotation procedure (explicit PR replacing the line), (d) reference to 24-REVIEW.md §BL-03"
    - "FIX-05: load-primed.ts pensieve_embeddings load path uses staging-table CAST pattern (24-REVIEW.md:63 verbatim): TEMP staging table with embedding column ALTERed to TEXT → jsonb_populate_recordset into staging → INSERT INTO pensieve_embeddings SELECT *, embedding::vector FROM staging → DROP staging (D-11)"
    - "FIX-05 (smoke): src/__tests__/fixtures/__fixtures__/pgvector-smoke/ fixture exists with 1 row × 1024-float embedding + MANIFEST.json; load-primed-pgvector.test.ts asserts loadPrimedFixture('pgvector-smoke') returns rowCount=1 and the row's embedding column type is vector (regression closes on first non-empty regen, D-12)"
    - "FIX-07: primed-sanity-m011.test.ts totalTelegramWordCount(now) is window-filtered via previousMonthStart/End in Europe/Paris mirroring psychological-shared.ts:248-273 exactly; HARN test calls pass a pinned NOW constant matching downstream integration tests' NOW (D-15)"
    - "FIX-07 (cont): MIN_SPEECH_WORDS = 5000 is imported from existing src/memory/confidence.ts (verified to exist per CONTEXT line 89); HARN populated path uses >= MIN_SPEECH_WORDS, sparse path uses < MIN_SPEECH_WORDS — wordCount === 5000 is below substrate's gate, not above (D-16)"
    - "FIX-08: All 3 operator scripts (synthesize-episodic.ts, fetch-prod-data.ts, regenerate-primed.ts) replace `process.exit(130)` signal-handlers with `AbortController + process.exitCode = 130` pattern (D-17); existing finally blocks run naturally as event-loop drains; regenerate-primed.ts additionally awaits active child SIGTERM up to 5s then SIGKILL (24-REVIEW.md WR-05 fix line 91-95)"
    - "`npx tsc --noEmit` exits 0 across all modified files"
    - "`bash scripts/test.sh` exits 0 — full Docker test suite green per CLAUDE.md `always run full Docker tests`; new pgvector smoke test passes; primed-sanity-m011 HARN assertions hold for the new windowed wordCount"
    - "No regression in existing `synth.contradictions.dropped`-shaped logs (the event is NEW; no prior caller exists to break)"
  artifacts:
    - path: "scripts/synthesize-delta.ts"
      provides: "FIX-01 inline pre-filter at line 934 + FIX-02a phrasesClause decoupled-append at lines 584-594; both edits are inline (no new exports)"
      contains: "synth.contradictions.dropped"
    - path: "scripts/synthesize-episodic.ts"
      provides: "FIX-03 loadMigrations() async helper + dynamic MIGRATIONS array; FIX-08 AbortController-driven SIGINT handler replacing process.exit(130)"
      contains: "loadMigrations"
    - path: "scripts/fetch-prod-data.ts"
      provides: "FIX-04 SSH argv flags `-o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=scripts/.ssh-known-hosts` + header comment block; FIX-08 AbortController SIGINT pattern"
      contains: "StrictHostKeyChecking=accept-new"
    - path: "scripts/regenerate-primed.ts"
      provides: "FIX-08 AbortController SIGINT + child SIGTERM-then-SIGKILL-after-5s pattern at lines 222-228"
      contains: "AbortController"
    - path: "scripts/.ssh-known-hosts"
      provides: "Repo-vetted SSH known_hosts file with header documenting bootstrap + rotation procedures; initial contents are header-only (operator populates host line on first connect)"
      min_lines: 15
      contains: "Phase 45 FIX-04"
    - path: "src/__tests__/fixtures/load-primed.ts"
      provides: "FIX-05 insertPensieveEmbeddings local helper using staging-table CAST pattern; replaces the existing insertTable('pensieve_embeddings.jsonl', ...) call at line 274"
      contains: "pensieve_embeddings_staging"
    - path: "src/__tests__/fixtures/primed-sanity-m011.test.ts"
      provides: "FIX-02b M1K_NAME constant updated to `m011-1000words-5days`; FIX-07 calendar-month-windowed totalTelegramWordCount + MIN_SPEECH_WORDS import + WR-01 >=/< boundary alignment"
      contains: "m011-1000words-5days"
    - path: "src/memory/profiles/__tests__/integration-m011-1000words.test.ts"
      provides: "FIX-02b FIXTURE_PATH constant updated to `m011-1000words-5days`"
      contains: "m011-1000words-5days"
    - path: "src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl"
      provides: "Single row × 1024-float embedding + sibling tables (pensieve_entries minimum) — drives FIX-05 smoke regression"
      min_lines: 1
    - path: "src/__tests__/fixtures/__fixtures__/pgvector-smoke/MANIFEST.json"
      provides: "Fixture manifest pointing to the JSONL files + recording row counts"
    - path: "src/__tests__/fixtures/load-primed-pgvector.test.ts"
      provides: "Vitest test loading the pgvector-smoke fixture and asserting the staging-table CAST landed the row with a vector column (FIX-05 D-12 regression gate)"
      min_lines: 30
      contains: "loadPrimedFixture"
  key_links:
    - from: "scripts/synthesize-delta.ts"
      to: "syntheticPensieve fused id set"
      via: "Set membership check on entry_a_id + entry_b_id before insert"
      pattern: "pensieveIds.has\\(String\\(c.entry_a_id\\)\\)"
    - from: "scripts/synthesize-episodic.ts"
      to: "src/db/migrations directory"
      via: "readdir + filter .sql + sort"
      pattern: "readdir\\('src/db/migrations'\\)"
    - from: "scripts/fetch-prod-data.ts"
      to: "scripts/.ssh-known-hosts"
      via: "ssh -o UserKnownHostsFile=scripts/.ssh-known-hosts"
      pattern: "UserKnownHostsFile=scripts/\\.ssh-known-hosts"
    - from: "src/__tests__/fixtures/load-primed.ts"
      to: "pensieve_embeddings.embedding vector(1024) column"
      via: "INSERT … SELECT … embedding::vector FROM pensieve_embeddings_staging"
      pattern: "embedding::vector"
    - from: "src/__tests__/fixtures/primed-sanity-m011.test.ts"
      to: "src/memory/profiles/psychological-shared.ts calendar-month-window"
      via: "previousMonthStart(now, 'Europe/Paris') AND previousMonthEnd(now, 'Europe/Paris') applied to pensieve_entries.created_at filter"
      pattern: "previousMonthStart"
    - from: "src/__tests__/fixtures/primed-sanity-m011.test.ts"
      to: "src/memory/confidence.ts MIN_SPEECH_WORDS"
      via: "import { MIN_SPEECH_WORDS } from '../../memory/confidence.js'"
      pattern: "MIN_SPEECH_WORDS"
---

<objective>
Ship Phase 45 Wave A fixture-pipeline residuals: the 7 FIX-0x requirements that live in operator scripts + fixture-load + HARN test surface. All edits are line-precise (`file:line` cited in each task), all changes are local to the fixture-regen path (no app runtime touched), and the plan is **parallel-shippable with Plan 45-01** (no shared files; Wave A under CONTEXT D-02).

Purpose: Eliminate the 14-phase code-review BLOCKERs + WARNINGs that prevent deterministic fixture regen. Specifically:
- **FIX-01** (Phase 40 §Tech-Debt #2) — silently-dropped contradictions FK violation on sparse-pensieve fixtures.
- **FIX-02** (Phase 40 §Tech-Debt #3 + §BL-01) — bias-prompt precedence-rule coupling + the m011-1000words output-dir path collision.
- **FIX-03** (Phase 24 §BL-01) — hardcoded migration list silently drops `wellbeing_snapshots.jsonl` when the schema evolves past slot 0005.
- **FIX-04** (Phase 24 §BL-03) — SSH tunnel auto-trusts unknown hosts → PROD_PG_PASSWORD MITM exposure on fresh runners.
- **FIX-05** (Phase 24 §BL-06) — pgvector vector(1024) coercion fails through `jsonb_populate_recordset` on first non-empty embeddings JSONL regen.
- **FIX-07** (Phase 40 §BL-05 + §WR-01) — HARN word-count gate uses an unfiltered total (~4-month accumulation) while substrate sees only the previous calendar month → 1000-word fixture spuriously passes when substrate would fail.
- **FIX-08** (Phase 24 §BL-04 + §WR-05) — SIGINT handlers call `process.exit(130)` synchronously, skipping `finally` cleanup → SSH tunnels + postgres clients + docker projects leak.

Output: 4 modified scripts + 2 modified test files + 1 new repo-config file + 1 new smoke-fixture set + 1 new vitest file + 1 docs note update. All under Wave A (parallel with Plan 45-01).

Satisfies: FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-07, FIX-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md
@.planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md

# Root-cause review files (per-REQ specifics)
@.planning/milestones/v2.3-phases/24-primed-fixture-pipeline/24-REVIEW.md
@.planning/milestones/v2.6-phases/40-psychological-milestone-tests/40-REVIEW.md

# Source files modified by this plan
@scripts/synthesize-delta.ts
@scripts/synthesize-episodic.ts
@scripts/fetch-prod-data.ts
@scripts/regenerate-primed.ts
@src/__tests__/fixtures/load-primed.ts
@src/__tests__/fixtures/primed-sanity-m011.test.ts
@src/memory/profiles/__tests__/integration-m011-1000words.test.ts

# Pattern references (read-only; mirror exactly)
@src/memory/profiles/psychological-shared.ts
@src/memory/confidence.ts
@scripts/test.sh

# Docs file updated by this plan (path constant string update)
@.planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: synthesize-delta.ts — FIX-01 contradictions FK pre-filter + FIX-02a phrasesClause decoupling</name>
  <files>scripts/synthesize-delta.ts</files>
  <read_first>
    - scripts/synthesize-delta.ts:580-600 (current bias-prompt construction — phrasesClause append currently inside `if (dimensionHint)` block)
    - scripts/synthesize-delta.ts:925-950 (current contradictions concat at line ~934 + downstream INSERT call site)
    - scripts/synthesize-delta.ts:1-30 (imports + logger setup — confirm pino logger import path for the new event)
    - .planning/milestones/v2.6-phases/40-psychological-milestone-tests/40-REVIEW.md §Tech-Debt #2 lines 125-132 (verbatim pre-filter snippet)
    - .planning/milestones/v2.6-phases/40-psychological-milestone-tests/40-REVIEW.md §Tech-Debt #3 lines 134-136 (phrasesClause decoupling root cause)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"scripts/synthesize-delta.ts" — full mirror-verbatim snippets for both edits
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-06 (silent drop + summary log line semantics) + D-07a (phrasesClause hoist)
  </read_first>
  <action>
    Two independent inline edits in `scripts/synthesize-delta.ts`. Apply both in one commit (same file, atomic).

    **Edit (a) — FIX-01 contradictions FK pre-filter at line ~934:**

    Locate the current concatenation `const fusedContradictions = [...organicContradictions, ...synthContradictions];` (PATTERNS doc identifies it at line 934; verify exact line via Grep).

    Replace with the verbatim snippet from CONTEXT D-06 / PATTERNS §synthesize-delta.ts:

    - Build `pensieveIds = new Set(fusedPensieve.map((p) => String(p.id)))` from the upstream fused pensieve array (verify variable name `fusedPensieve` exists in scope at this line; if named differently, mirror the actual variable).
    - Hold the raw-concat result in `rawContradictions`.
    - Build `fusedContradictions` as `rawContradictions.filter((c) => pensieveIds.has(String(c.entry_a_id)) && pensieveIds.has(String(c.entry_b_id)))`.
    - Compute `droppedCount = rawContradictions.length - fusedContradictions.length`.
    - If `droppedCount > 0`, emit ONE summary log call: `logger.info({ droppedCount, totalCount: rawContradictions.length }, 'synth.contradictions.dropped');` (event name verbatim per D-06).

    **Anti-shallow-execution:**
    - The pre-filter must use String coercion on BOTH sides (pensieveIds set has String(p.id); filter does String(c.entry_a_id) AND String(c.entry_b_id)) — ID types may be number-vs-string across organic/synthetic sources.
    - Do NOT throw on droppedCount > 0; D-06 explicitly chose silent drop + log over fail-fast (sparse-fixture path would otherwise break).
    - Do NOT log per-row drops — only ONE summary event per synth run.
    - Do NOT touch the downstream INSERT call site; the filtered `fusedContradictions` flows through unchanged.

    **Edit (b) — FIX-02a phrasesClause decoupling at lines 584-594:**

    Locate the current bias-prompt builder where `phrasesClause` is appended inside the `if (dimensionHint)` branch only. Restructure to:

    - Compute `phrasesClause` (unchanged: signalPhrases-derived suffix or empty string).
    - Initialize `let result = base;`.
    - If `dimensionHint`, append `\n\nFocus today's entries on ${dimensionHint}.` to `result`.
    - If `phrasesClause` (truthy), append `phrasesClause` to `result`.
    - Return `result`.

    Per PATTERNS doc verbatim "BEFORE / AFTER" snippets. The key invariant: phrasesClause append condition is `if (phrasesClause)`, NOT `if (dimensionHint)` — they decouple.

    **Anti-shallow-execution:**
    - Do NOT change the format/wording of `phrasesClause` itself or of the `Focus today's entries on ...` sentence — only the control-flow structure changes.
    - Do NOT introduce new exports or new top-level helpers; this is an in-function structural edit.
    - The `return base;` fallthrough is REPLACED by the new `return result;` — verify no other return paths in the function need updating.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scripts/synthesize-delta\\.ts" | grep -v "^$" | wc -l | xargs -I{} test {} -eq 0 && grep -c "synth.contradictions.dropped" scripts/synthesize-delta.ts | xargs -I{} test {} -eq 1 && grep -c "if (phrasesClause)" scripts/synthesize-delta.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pensieveIds = new Set" scripts/synthesize-delta.ts` returns `1`
    - `grep -c "rawContradictions" scripts/synthesize-delta.ts` returns `>= 2` (declaration + filter input + droppedCount calc)
    - `grep -c "synth.contradictions.dropped" scripts/synthesize-delta.ts` returns `1` (single summary event)
    - `grep -c "droppedCount > 0" scripts/synthesize-delta.ts` returns `1`
    - `grep -c "if (phrasesClause)" scripts/synthesize-delta.ts` returns `>= 1` (the new decoupled branch)
    - `awk '/let result = base/{got_init=NR} /if \\(dimensionHint\\)/{got_dim=NR} /if \\(phrasesClause\\)/{got_phrases=NR} END{exit !(got_init < got_dim && got_dim < got_phrases)}' scripts/synthesize-delta.ts` exits 0 (ordering check)
    - `npx tsc --noEmit` exits 0 with no new errors in this file
    - `grep -c "process.exit\\|process\\.exitCode" scripts/synthesize-delta.ts` unchanged from pre-edit (this task does not touch SIGINT — that's Task 2's regenerate-primed scope; synthesize-delta is not on the FIX-08 list per D-17)
  </acceptance_criteria>
  <done>
    synthesize-delta.ts: contradictions FK pre-filter dropping orphans silently with one summary log event; phrasesClause appended independently of dimensionHint truthiness. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Operator scripts — FIX-03 (migration glob), FIX-04 (SSH hardening + .ssh-known-hosts), FIX-08 (AbortController SIGINT across 3 scripts)</name>
  <files>scripts/synthesize-episodic.ts, scripts/fetch-prod-data.ts, scripts/regenerate-primed.ts, scripts/.ssh-known-hosts</files>
  <read_first>
    - scripts/synthesize-episodic.ts:40-60 (current MIGRATIONS hardcoded array) + :470-490 (current SIGINT handler with process.exit(130))
    - scripts/fetch-prod-data.ts:60-100 (current SSH spawn argv — locate the option array passed to spawn) + :260-285 (current sigHandler calling closeTunnel().then(process.exit))
    - scripts/regenerate-primed.ts:215-235 (current sigHandler) + :240-260 (child-process spawn + on-exit handlers — activeChild reference)
    - scripts/test.sh:43-47 (filter-`.sql` pattern that FIX-03 mirrors)
    - .planning/milestones/v2.3-phases/24-primed-fixture-pipeline/24-REVIEW.md §BL-01 (FIX-03), §BL-03 (FIX-04 SSH hardening), §BL-04 (FIX-08 SIGINT pattern), §WR-05 (FIX-08 child-await + SIGKILL fallback at regenerate-primed)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-08 (glob source) + D-09/D-10 (SSH options) + D-17 (SIGINT pattern)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"scripts/synthesize-episodic.ts", §"scripts/fetch-prod-data.ts", §"scripts/regenerate-primed.ts", §"scripts/.ssh-known-hosts" — verbatim snippets for every edit
  </read_first>
  <action>
    Four sub-edits across three scripts + one new repo-config file. They share the AbortController SIGINT pattern across the 3 scripts and are best applied as one task because the SIGINT shape is identical and PR-review is cleaner with one diff.

    **Edit (a) — `scripts/synthesize-episodic.ts` FIX-03 dynamic migration list (lines ~40-60):**

    Add `import { readdir } from 'node:fs/promises';` at the top imports block (verify it's not already imported).

    Add an async helper near the top of the file (before main()):
    ```typescript
    async function loadMigrations(): Promise<string[]> {
      const files = await readdir('src/db/migrations');
      return files
        .filter((f) => f.endsWith('.sql'))
        .sort()
        .map((f) => `src/db/migrations/${f}`);
    }
    ```

    Replace the existing `const MIGRATIONS = [/* 0000..0005 hardcoded */]` array with a runtime call at the top of main(): `const MIGRATIONS = await loadMigrations();`. The `.sql` filter naturally excludes the `meta/` subdirectory (readdir returns it as an entry whose name is `meta` — no `.sql` extension).

    **Anti-shallow-execution:**
    - The `.sort()` is mandatory — lexicographic sort is the apply-order contract per D-18.
    - Do NOT use `glob` or `fast-glob` packages; `readdir + filter` is the established pattern in this file's ESM context.
    - If the file's main() is not an async wrapper, wrap the migration-apply loop in one (top-level await is allowed in this script's ESM mode per CONTEXT line 215; verify with grep of existing await calls).

    **Edit (b) — `scripts/synthesize-episodic.ts` FIX-08 SIGINT (lines ~470-490):**

    Replace the existing sigHandler (which calls `process.exit(130)` synchronously) with the AbortController pattern per CONTEXT D-17 + PATTERNS verbatim:

    ```typescript
    const abortController = new AbortController();
    const sigHandler = (): void => {
      process.exitCode = 130;
      abortController.abort();
    };
    process.on('SIGINT', sigHandler);
    process.on('SIGTERM', sigHandler);
    ```

    Thread `abortController.signal` into the docker-up/down chain and any long postgres query loops if a `signal` option is available; if not, the existing `try { ... } finally { downDocker(); await dbOverride?.end(); }` block already runs naturally as the event loop drains after the `abort()`.

    **Edit (c) — `scripts/fetch-prod-data.ts` FIX-04 SSH hardening (lines ~73-88):**

    Locate the SSH spawn argv array. Insert TWO new options BEFORE the existing `SSH_TARGET` position (preserving the existing `-o ExitOnForwardFailure=yes`, `-o ConnectTimeout=10`, `-o ServerAliveInterval=30`):
    - `'-o', 'StrictHostKeyChecking=accept-new'`
    - `'-o', 'UserKnownHostsFile=scripts/.ssh-known-hosts'`

    Add a header comment block above the `openTunnel` function (or wherever the SSH spawn lives) documenting the bootstrap procedure verbatim from PATTERNS §scripts/fetch-prod-data.ts:
    ```
    // FIX-04 SSH hardening (Phase 45 v2.6.1):
    //   StrictHostKeyChecking=accept-new — auto-trust the host key on first
    //   contact (so fresh runners + new operators work without manual
    //   `ssh-keyscan`), but refuse on key MISMATCH (closing the
    //   MITM-after-rotation window).
    //   UserKnownHostsFile=scripts/.ssh-known-hosts — vetted, repo-committed.
    //   Bootstrap: if scripts/.ssh-known-hosts has no host entry for
    //   192.168.1.50, the first connect populates it; operator MUST commit
    //   the line back to the repo so subsequent runs see the known key.
    //   To rotate: explicit PR replacing the line.
    //   Reference: 24-REVIEW.md §BL-03 lines 41-45.
    ```

    **Anti-shallow-execution:**
    - `accept-new` (NOT `yes` or `no`) — D-10 explicitly chose this so first-clone-and-bootstrap still works.
    - The UserKnownHostsFile path is RELATIVE to repo root (`scripts/.ssh-known-hosts`) — the script must be invoked from repo root; this is the existing convention for all `scripts/*.ts`.
    - Do NOT replace the existing `ConnectTimeout` / `ServerAliveInterval` options; ADD the two new options.

    **Edit (d) — `scripts/fetch-prod-data.ts` FIX-08 SIGINT (lines ~273-277):**

    Replace the sigHandler `void closeTunnel(ssh).then(() => process.exit(130));` with the AbortController shape:
    ```typescript
    const abortController = new AbortController();
    const sigHandler = (): void => {
      process.exitCode = 130;
      abortController.abort();
    };
    ```

    Thread `abortController.signal` into the table-dump loop body (the `for table of TABLES` body around lines 290+). The existing `finally { await closeTunnel(ssh); await client?.end({ timeout: 5 }); }` block runs naturally.

    **Edit (e) — `scripts/regenerate-primed.ts` FIX-08 SIGINT + child-await with SIGKILL fallback (lines ~222-228):**

    Replace the sigHandler with the AbortController + child-kill shape per CONTEXT D-17 + 24-REVIEW WR-05 line 91-95 + PATTERNS verbatim:
    ```typescript
    const abortController = new AbortController();
    const sigHandler = (): void => {
      process.exitCode = 130;
      abortController.abort();
      if (activeChild) {
        activeChild.kill('SIGTERM');
        const killTimer = setTimeout(() => {
          if (activeChild && activeChild.exitCode === null) {
            activeChild.kill('SIGKILL');
          }
        }, 5000);
        activeChild.on('exit', () => clearTimeout(killTimer));
      }
    };
    process.on('SIGINT', sigHandler);
    process.on('SIGTERM', sigHandler);
    ```

    Verify `activeChild` is a module-scope or closure-scope variable already set by the child-spawn call (PATTERNS line 280-285 confirms this exists in the current code).

    **Edit (f) — NEW file `scripts/.ssh-known-hosts`:**

    Create with the header from PATTERNS §scripts/.ssh-known-hosts verbatim (15-line header block, no host lines yet — the first operator to run fetch-prod-data.ts populates the host line via `accept-new` and then commits it back). The header documents:
    - Phase 45 FIX-04 attribution + date.
    - Bootstrap procedure.
    - Rotation procedure (operator pre-commits new key + PR review verifies fingerprint).
    - Host: 192.168.1.50 (Proxmox prod node).

    **Anti-shallow-execution:**
    - File MUST end with a newline (OpenSSH known_hosts parser).
    - Do NOT pre-populate the host key in this commit — that's the operator's bootstrap step per D-10. The empty-but-headered file commits the placeholder.
    - Each comment line MUST start with `# ` (OpenSSH parser comment convention).

    **Cross-edit anti-shallow-execution:**
    - The 3 SIGINT pattern edits are IDENTICAL in shape — verify with `grep -c "abortController.abort()" scripts/*.ts` returns `>= 3`.
    - regenerate-primed.ts is the ONLY script that needs the child-await with SIGKILL fallback; the other two have no child processes to await.
    - Do NOT add `process.on('SIGINT', ...)` calls if one already exists — REPLACE the existing handler body, don't append duplicates.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scripts/(synthesize-episodic|fetch-prod-data|regenerate-primed)\\.ts" | grep -v "^$" | wc -l | xargs -I{} test {} -eq 0 && grep -c "abortController" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{exit !(s>=6)}' && grep -c "StrictHostKeyChecking=accept-new" scripts/fetch-prod-data.ts | xargs -I{} test {} -eq 1 && test -f scripts/.ssh-known-hosts && grep -c "Phase 45 FIX-04" scripts/.ssh-known-hosts | xargs -I{} test {} -ge 1 && grep -c "readdir('src/db/migrations')" scripts/synthesize-episodic.ts | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readdir('src/db/migrations')" scripts/synthesize-episodic.ts` returns `1`
    - `grep -c "endsWith('.sql')" scripts/synthesize-episodic.ts` returns `>= 1`
    - `grep -c "MIGRATIONS = await loadMigrations" scripts/synthesize-episodic.ts` returns `1`
    - `grep -c "StrictHostKeyChecking=accept-new" scripts/fetch-prod-data.ts` returns `1`
    - `grep -c "UserKnownHostsFile=scripts/\\.ssh-known-hosts" scripts/fetch-prod-data.ts` returns `1`
    - `grep -c "FIX-04 SSH hardening" scripts/fetch-prod-data.ts` returns `1` (header comment present)
    - `test -f scripts/.ssh-known-hosts` (file exists)
    - `grep -c "Phase 45 FIX-04" scripts/.ssh-known-hosts` returns `1`
    - `grep -c "AbortController()" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{print s}'` returns `>= 3`
    - `grep -c "process.exitCode = 130" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{print s}'` returns `>= 3`
    - `grep -c "process.exit(130)" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{print s}'` returns `0` (all old-style synchronous-exit handlers removed)
    - `grep -c "kill('SIGKILL')" scripts/regenerate-primed.ts` returns `1` (only regenerate-primed has child-kill)
    - `npx tsc --noEmit` exits 0 with no new errors in the 3 scripts
    - `bash -n scripts/synthesize-episodic.ts` not applicable (TS not bash); equivalent check is tsc exit 0
  </acceptance_criteria>
  <done>
    3 operator scripts share the AbortController SIGINT pattern (process.exitCode = 130 + abort() instead of synchronous exit); finally cleanup runs naturally on Ctrl-C. fetch-prod-data uses accept-new + repo-vetted known_hosts. synthesize-episodic globs migrations dynamically. .ssh-known-hosts header-only repo-config file commits placeholder for first-operator bootstrap.
  </done>
</task>

<task type="auto">
  <name>Task 3: FIX-05 — load-primed.ts pgvector staging-table CAST + smoke fixture + regression test</name>
  <files>src/__tests__/fixtures/load-primed.ts, src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl, src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_entries.jsonl, src/__tests__/fixtures/__fixtures__/pgvector-smoke/MANIFEST.json, src/__tests__/fixtures/load-primed-pgvector.test.ts</files>
  <read_first>
    - src/__tests__/fixtures/load-primed.ts (full file — especially :46-65 client setup, :219-241 to_regclass feature-detect pattern, :270-280 the current `insertTable('pensieve_embeddings.jsonl', 'pensieve_embeddings')` call site, plus the `loadJsonl<T>` helper and `insertTable` signature)
    - src/db/schema.ts (locate the `pensieve_embeddings` table definition — confirm column names: id, pensieve_entry_id, embedding vector(1024), model, created_at)
    - .planning/milestones/v2.3-phases/24-primed-fixture-pipeline/24-REVIEW.md §BL-06 lines 59-63 (verbatim staging-table CAST recommendation)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-11 (staging-table approach) + D-12 (smoke fixture requirement)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"src/__tests__/fixtures/load-primed.ts" — verbatim `insertPensieveEmbeddings` function snippet
    - scripts/test.sh:43 (`CREATE EXTENSION IF NOT EXISTS vector;` — confirms test DB has pgvector enabled)
    - An existing primed fixture directory (e.g., `tests/fixtures/primed/m010-30days/` if present, or any directory under `tests/fixtures/primed/`) to mirror MANIFEST.json shape
  </read_first>
  <action>
    Three sub-edits: rewrite the pensieve_embeddings load path in load-primed.ts; add a small dedicated smoke fixture under `src/__tests__/fixtures/__fixtures__/pgvector-smoke/`; add a vitest regression test.

    **Edit (a) — `src/__tests__/fixtures/load-primed.ts` FIX-05 staging-table CAST:**

    Add a new local helper function `insertPensieveEmbeddings(client, fixtureDir)` per PATTERNS verbatim snippet (CONTEXT D-11; PATTERNS §load-primed.ts lines 346-393). The helper:
    1. Reads pensieve_embeddings.jsonl via existing `loadJsonl<Record<string, unknown>>(...)`.
    2. If no rows, returns 0.
    3. Creates TEMP table `pensieve_embeddings_staging` via `CREATE TEMP TABLE IF NOT EXISTS pensieve_embeddings_staging (LIKE pensieve_embeddings INCLUDING ALL EXCLUDING CONSTRAINTS);`.
    4. Alters the staging embedding column to TEXT via `ALTER TABLE pensieve_embeddings_staging ALTER COLUMN embedding TYPE text USING embedding::text;`.
    5. Bulk-INSERTs into staging via `INSERT INTO pensieve_embeddings_staging SELECT * FROM jsonb_populate_recordset(NULL::pensieve_embeddings_staging, $1::jsonb)` with `JSON.stringify(rows)`.
    6. Final INSERT with explicit cast: `INSERT INTO pensieve_embeddings SELECT id, pensieve_entry_id, embedding::vector AS embedding, model, created_at FROM pensieve_embeddings_staging;` — column names mirror schema.ts.
    7. Explicit `DROP TABLE pensieve_embeddings_staging` cleanup (TEMP auto-drops at session end but explicit for clarity).
    8. Returns rows.length for the counts map.

    Replace the existing line ~274 `await insertTable('pensieve_embeddings.jsonl', 'pensieve_embeddings');` with:
    ```typescript
    const embeddingsCount = await insertPensieveEmbeddings(client, fixtureDir);
    counts['pensieve_embeddings'] = embeddingsCount;
    ```

    Verify the `counts` variable shape matches the existing pattern at the top of the function (other counts entries follow `counts['<table>'] = <number>`).

    **Anti-shallow-execution:**
    - Helper lives LOCAL to load-primed.ts (per CONTEXT line 92 "Claude's Discretion" — chose local for minimal-surface-area, matching PATTERNS §load-primed.ts comment).
    - The column-projection in the final INSERT MUST list columns explicitly (id, pensieve_entry_id, embedding::vector, model, created_at) — NOT `SELECT *` — because the staging table's embedding column is TEXT, not VECTOR; only the explicit cast in the projection makes the types align with the real table.
    - Do NOT remove the `loadJsonl` import or the `insertTable` general helper — those remain for the other tables.
    - Use `client.unsafe(...)` (or whichever postgres.js parametrized-SQL primitive is in use elsewhere in this file) for the jsonb_populate_recordset call, mirroring the existing insertTable pattern.

    **Edit (b) — NEW smoke fixture under `src/__tests__/fixtures/__fixtures__/pgvector-smoke/`:**

    Create 3 files:

    1. `src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_entries.jsonl` — 1 row (parent for the embeddings FK):
       ```json
       {"id":"00000000-0000-4000-8000-000000000001","user_id":"smoke","content":"pgvector smoke fixture sentence.","source":"smoke","created_at":"2026-05-14T00:00:00Z","deleted_at":null,"metadata":null}
       ```
       (verify column names against schema.ts pensieveEntries table; adjust if columns differ. The content + UUID values are arbitrary.)

    2. `src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl` — 1 row with a real-shaped 1024-float embedding:
       ```json
       {"id":"<uuid>","pensieve_entry_id":"00000000-0000-4000-8000-000000000001","embedding":[0.001,0.002,...×1024],"model":"smoke","created_at":"2026-05-14T00:00:00Z"}
       ```
       The 1024 floats can be deterministic — e.g., `Array.from({length:1024}, (_,i) => i / 1024)` — but they MUST be EXACTLY 1024 values (verify with jq `length` or wc -w post-emit).

    3. `src/__tests__/fixtures/__fixtures__/pgvector-smoke/MANIFEST.json`:
       ```json
       {
         "fixture_name": "pgvector-smoke",
         "purpose": "FIX-05 regression smoke — single 1024-dim embedding round-trips via staging-table CAST.",
         "table_counts": {
           "pensieve_entries": 1,
           "pensieve_embeddings": 1
         }
       }
       ```
       Match the MANIFEST shape of an existing primed fixture under `tests/fixtures/primed/` for any other required keys (e.g., `synthesized_at`, `commit_sha` — verify against the closest existing fixture; if those are required by load-primed.ts's manifest-loader, populate them with placeholder values).

    **Anti-shallow-execution:**
    - The embedding array MUST contain EXACTLY 1024 numbers — verify with `jq '.embedding | length' src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl` returns `1024`.
    - The pensieve_entry_id in pensieve_embeddings.jsonl MUST match the id in pensieve_entries.jsonl (FK invariant).
    - Use new-format UUIDs (v4) for any uuid columns — verify schema.ts expects uuid type; if schema uses a different ID type, mirror.

    **Edit (c) — NEW `src/__tests__/fixtures/load-primed-pgvector.test.ts`:**

    Create the file. Test shape:
    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { loadPrimedFixture } from './load-primed.js';
    // ... postgres client setup matching existing migration-test pattern ...

    describe('load-primed FIX-05: pgvector staging-table CAST', () => {
      // beforeAll: set up test DB (or expect the global test-bootstrap to have done so)
      // afterAll: cleanup

      it('loads a 1024-dim embedding row via staging-table CAST without coercion error', async () => {
        const counts = await loadPrimedFixture('src/__tests__/fixtures/__fixtures__/pgvector-smoke', client);
        expect(counts['pensieve_embeddings']).toBe(1);
      });

      it('the loaded embedding row has vector type (introspectable)', async () => {
        const result = await client`
          SELECT pg_typeof(embedding) AS coltype FROM pensieve_embeddings
          WHERE pensieve_entry_id = '00000000-0000-4000-8000-000000000001' LIMIT 1
        `;
        expect(result[0]?.coltype).toBe('vector');
      });

      it('embedding has dimension 1024', async () => {
        const result = await client`
          SELECT array_length(embedding::real[], 1) AS dim FROM pensieve_embeddings
          WHERE pensieve_entry_id = '00000000-0000-4000-8000-000000000001' LIMIT 1
        `;
        expect(result[0]?.dim).toBe(1024);
      });
    });
    ```

    **Anti-shallow-execution:**
    - Test must clean up the loaded rows (DELETE in afterAll or use a TX-rollback pattern if available) — otherwise it pollutes the test DB for subsequent tests.
    - Use the SAME postgres client setup pattern as existing migration tests (e.g., the 0015 test created in Plan 45-01, or pre-existing tests under `src/__tests__/migrations/` or `src/__tests__/fixtures/`). Do NOT roll a new client setup.
    - If `loadPrimedFixture` signature is `(fixtureDir, client)` vs `(fixtureDir)` with internal client, mirror the existing call sites' shape.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/fixtures/load-primed-pgvector.test.ts 2>&1 | tail -20 | grep -E "(passed|failed)" && jq '.embedding | length' src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl | xargs -I{} test {} -eq 1024 && grep -c "pensieve_embeddings_staging" src/__tests__/fixtures/load-primed.ts | xargs -I{} test {} -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "insertPensieveEmbeddings" src/__tests__/fixtures/load-primed.ts` returns `>= 2` (declaration + call site)
    - `grep -c "pensieve_embeddings_staging" src/__tests__/fixtures/load-primed.ts` returns `>= 3` (CREATE + ALTER + INSERT + DROP — at least 3 references)
    - `grep -c "embedding::vector" src/__tests__/fixtures/load-primed.ts` returns `>= 1` (final INSERT projection cast)
    - `test -f src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl`
    - `test -f src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_entries.jsonl`
    - `test -f src/__tests__/fixtures/__fixtures__/pgvector-smoke/MANIFEST.json`
    - `jq '.embedding | length' src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl` outputs `1024`
    - `npx vitest run src/__tests__/fixtures/load-primed-pgvector.test.ts` exits 0 with all `it()` blocks passing
    - `npx tsc --noEmit` exits 0 with no new errors
  </acceptance_criteria>
  <done>
    load-primed.ts pensieve_embeddings load uses staging-table CAST; 1-row 1024-dim smoke fixture exists; vitest regression test passes confirming vector coercion no longer fails on non-empty regen.
  </done>
</task>

<task type="auto">
  <name>Task 4: FIX-02b + FIX-07 — HARN test calendar-month-window + MIN_SPEECH_WORDS consolidation + path constant updates</name>
  <files>src/__tests__/fixtures/primed-sanity-m011.test.ts, src/memory/profiles/__tests__/integration-m011-1000words.test.ts, .planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md</files>
  <read_first>
    - src/__tests__/fixtures/primed-sanity-m011.test.ts (full file — especially :89-100 current totalTelegramWordCount, :120-141 MIN_WORDS_30D + populated-path assertion, :163-175 FIXTURE constants, :188 sparse-path assertion)
    - src/memory/profiles/__tests__/integration-m011-1000words.test.ts:120-130 (FIXTURE_PATH constant location)
    - src/memory/profiles/psychological-shared.ts:248-275 (canonical calendar-month-window SQL — FIX-07 mirrors this verbatim) + :284 (the `< 5000` substrate-gate FIX-07 WR-01 aligns with)
    - src/memory/confidence.ts (full file — confirm `MIN_SPEECH_WORDS = 5000` export exists per CONTEXT line 89; locate its declaration)
    - .planning/milestones/v2.6-phases/40-psychological-milestone-tests/40-REVIEW.md §BL-01 lines 34-42 (FIX-02b path-bug Option-b — update test constants, not synthesize-delta) + §BL-05 lines 62-72 (FIX-07 window root cause + fix code) + §WR-01 lines 76-79 (strict-inequality + MIN_SPEECH_WORDS)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-CONTEXT.md D-07b (path constant update Option-b) + D-15 (window filter) + D-16 (MIN_SPEECH_WORDS + >=/<)
    - .planning/phases/45-schema-hygiene-fixture-cleanup/45-PATTERNS.md §"src/__tests__/fixtures/primed-sanity-m011.test.ts" — verbatim before/after snippets
    - .planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md:31-32 (current path-constant references in docs)
  </read_first>
  <action>
    Three coordinated edits across 2 test files + 1 docs file. All address the same set of constants (the m011-1000words fixture path naming + the 5000-word substrate gate).

    **Edit (a) — `src/__tests__/fixtures/primed-sanity-m011.test.ts` FIX-02b path constant + FIX-07 window + MIN_SPEECH_WORDS + WR-01 boundary:**

    1. At the top imports block, add:
       ```typescript
       import { DateTime } from 'luxon';
       import { MIN_SPEECH_WORDS } from '../../memory/confidence.js';
       import { sql as drizzleSql } from 'drizzle-orm';
       ```
       Verify `luxon` is already imported elsewhere in the file or add it; verify `confidence.js` relative path matches the file's location; verify drizzleSql alias matches the file's existing convention if any.

    2. Replace lines 163-164 (M1K_NAME constant):
       ```typescript
       const M1K_NAME = 'm011-1000words-5days'; // FIX-02b D-07b: matches synthesize-delta.ts:937 `${milestone}-${targetDays}days` naming
       const M1K_PATH = `tests/fixtures/primed/${M1K_NAME}/MANIFEST.json`;
       ```

    3. Replace the current `totalTelegramWordCount` function at lines 89-100 with a NOW-driven, calendar-month-windowed version per PATTERNS verbatim. The function signature becomes `async function totalTelegramWordCount(now: Date): Promise<number>`; body uses `DateTime.fromJSDate(now, { zone: 'Europe/Paris' })` → `.startOf('month').minus({ months: 1 })` to derive monthStart + .endOf('month') for monthEnd, then a SQL query against `pensieve_entries` filtering `source='telegram' AND deleted_at IS NULL AND created_at >= windowStart AND created_at <= windowEnd`. Use `db.execute<{word_count: number}>(...).rows[0]?.word_count ?? 0` accessor pattern (drizzle node-postgres returns `{rows: T[]}` envelope per WR-02).

    4. Update HARN test call sites to pass a pinned `NOW` constant matching the downstream integration test's NOW. For m011-30days populated path: `const NOW = new Date('2026-05-01T00:00:00Z')` (substrate window = April 2026). For m011-1000words sparse path: same `NOW = new Date('2026-05-01T00:00:00Z')` per PATTERNS line 439.

    5. Update line 120 (MIN_WORDS_30D constant): replace `const MIN_WORDS_30D = 5000;` with `const MIN_WORDS_30D = MIN_SPEECH_WORDS;`. Likewise replace line 167's `const MAX_WORDS_1K = 5000;` with `const MAX_WORDS_1K = MIN_SPEECH_WORDS;`.

    6. Update line 141 assertion: replace `expect(wordCount).toBeGreaterThan(MIN_WORDS_30D)` with `expect(wordCount).toBeGreaterThanOrEqual(MIN_WORDS_30D)` — populated path mirrors substrate's `>= MIN_SPEECH_WORDS` gate (D-16).

    7. Update line 188 assertion: confirm `expect(wordCount).toBeLessThan(MAX_WORDS_1K)` is `toBeLessThan` (strict <) — sparse path mirrors substrate's `< MIN_SPEECH_WORDS` gate. Per PATTERNS line 461 the existing line is already strict-< but verify and align if not.

    **Anti-shallow-execution:**
    - The window filter MUST use `created_at >= windowStart AND created_at <= windowEnd` — inclusive on BOTH ends matches `psychological-shared.ts:259-273` substrate semantics (verify the substrate file's exact operators before writing).
    - The `regexp_split_to_array(content, E'\\\\s+')` whitespace pattern MUST match substrate's exact pattern — verify against psychological-shared.ts:259-273.
    - The `.rows[0]?.word_count ?? 0` accessor is required because drizzle node-postgres returns the envelope; do not use `result[0]` (that's the postgres.js direct-client shape, not drizzle's).
    - Do NOT introduce a NEW shared constants module — `MIN_SPEECH_WORDS` already lives in `src/memory/confidence.ts` per CONTEXT D-16 line 89; import from there.
    - The `MIN_SPEECH_WORDS` import must use the `.js` extension in the import path (ESM convention used throughout this repo — verify against other imports in this file).

    **Edit (b) — `src/memory/profiles/__tests__/integration-m011-1000words.test.ts` FIXTURE_PATH constant:**

    At line ~124, replace the FIXTURE_PATH constant to use `m011-1000words-5days` (matching synthesize-delta's actual output naming). Per CONTEXT D-07b + PATTERNS line 411.

    **Edit (c) — `.planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md` docs note:**

    At lines 31-32, update the references from `m011-1000words` to `m011-1000words-5days` to reflect the new canonical name. Per CONTEXT D-07b. Add a brief inline note "(Phase 45 FIX-02b — name aligned with synthesize-delta's `${milestone}-${targetDays}days` output)".

    **Anti-shallow-execution (cross-edit):**
    - The path string `m011-1000words-5days` MUST be identical across all three files — single-source-of-truth via `M1K_NAME` constant where possible.
    - Do NOT touch `synthesize-delta.ts:937` — D-07b explicitly chose Option-b (update test constants, NOT the script).
    - The deferred-items.md note is a docs-only edit; do not change requirement IDs or status flags.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "primed-sanity-m011|integration-m011-1000words" | grep -v "^$" | wc -l | xargs -I{} test {} -eq 0 && grep -c "m011-1000words-5days" src/__tests__/fixtures/primed-sanity-m011.test.ts src/memory/profiles/__tests__/integration-m011-1000words.test.ts | awk -F: '{s+=$2} END{exit !(s>=2)}' && grep -c "MIN_SPEECH_WORDS" src/__tests__/fixtures/primed-sanity-m011.test.ts | xargs -I{} test {} -ge 3 && grep -c "previousMonth\\|startOf('month').minus" src/__tests__/fixtures/primed-sanity-m011.test.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "m011-1000words-5days" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `grep -c "m011-1000words-5days" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` returns `>= 1`
    - `grep -c "m011-1000words-5days" .planning/milestones/v2.6-phases/40-psychological-milestone-tests/deferred-items.md` returns `>= 1`
    - `grep -c "MIN_SPEECH_WORDS" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 3` (import + 2 constant assignments)
    - `grep -c "from '../../memory/confidence" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `grep -c "toBeGreaterThanOrEqual(MIN_WORDS_30D)" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `grep -c "toBeLessThan(MAX_WORDS_1K)" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `grep -cE "startOf\\('month'\\)|previousMonthStart" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `grep -c "Europe/Paris" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 1`
    - `npx tsc --noEmit` exits 0 with no new errors in the modified test files
    - `npx vitest run src/__tests__/fixtures/primed-sanity-m011.test.ts` exits 0 (NOTE: may require Plan 45-04 fixture refresh to fully pass — if so, the test file changes still compile and the test shape is correct; full-green is gated on Plan 45-04. Mark as "compiles + structural-pass" if Plan 45-04 not yet applied)
  </acceptance_criteria>
  <done>
    HARN test uses calendar-month-window-filtered wordCount mirroring substrate semantics; FIXTURE_PATH constants aligned across test files + docs; MIN_SPEECH_WORDS imported from existing confidence.ts; >=/< boundary aligned with substrate's strict-< gate. tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-machine ↔ Proxmox prod (SSH tunnel) | Operator runs `fetch-prod-data.ts`; SSH carries `PROD_PG_PASSWORD` over the tunnel. Pre-FIX-04, an attacker on the path could MITM on first connect (StrictHostKeyChecking=accept-new default behavior was auto-accept silently for THIS script's spawn invocation per Phase 24 review §BL-03). |
| fixture-regen pipeline ↔ test DB | Synthetic data flowing in; trust boundary is internal (no external input crosses here in this plan's edits). |
| signal handler ↔ async cleanup | SIGINT crossed by operator; pre-FIX-08, synchronous `process.exit(130)` skipped the `finally` block → leaked SSH tunnel, leaked postgres client. FIX-08 closes this gap by deferring exit via `process.exitCode + AbortController.abort()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Spoofing (S) | scripts/fetch-prod-data.ts SSH tunnel | mitigate | FIX-04: `StrictHostKeyChecking=accept-new` + repo-committed `scripts/.ssh-known-hosts` rejects MITM-after-rotation. First-connect bootstrap relies on initial out-of-band trust (operator on a known network during initial commit); subsequent connects validate against the committed key. |
| T-45-02-02 | Information Disclosure (I) | PROD_PG_PASSWORD over SSH | mitigate | T-45-02-01's mitigation directly covers this — the MITM attack vector against the password was the threat; closing the auto-trust window closes the disclosure path. |
| T-45-02-03 | Denial of Service (D) | Operator scripts SIGINT leaks resources | mitigate | FIX-08: AbortController + process.exitCode pattern allows `finally` blocks to run (closes SSH + postgres + docker children). Without this, Ctrl-C accumulates leaked file descriptors / connections / containers across operator iterations during fixture-regen cycles. |
| T-45-02-04 | Tampering (T) | Synth contradictions silently dropped | accept | FIX-01 explicitly chose silent drop + summary log over fail-fast per CONTEXT D-06; the alternative would break the sparse-fixture path. Risk: an attacker WHO HAS WRITE ACCESS to scripts/synthesize-delta.ts input fixtures could silently exclude contradictions, but they already have arbitrary code-edit access; no privilege escalation here. |
| T-45-02-05 | Tampering (T) | scripts/.ssh-known-hosts repo-vetted key rotation | mitigate | Rotation requires explicit PR (D-09); reviewer verifies fingerprint against out-of-band channel per the file's header documentation. Bypass requires both a malicious commit AND merge approval — covered by existing PR review. |
| T-45-02-06 | Information Disclosure (I) | pgvector smoke fixture leaks user content | accept | Fixture content (`"pgvector smoke fixture sentence."`) is synthetic, no real user data. Smoke fixture is checked into the repo (`src/__tests__/fixtures/__fixtures__/pgvector-smoke/`); review-checked at commit time. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (no new TS errors across the 4 modified scripts + 2 test files + 1 new test file).
- `bash scripts/test.sh` exits 0 (full Docker test suite — per CLAUDE.md memory `always run full Docker tests`). The new pgvector-smoke vitest passes; the HARN test compiles and either passes (if Plan 45-04 fixture refresh applied) or fails on the assertion content (gated, expected before Plan 45-04).
- `npx vitest run src/__tests__/fixtures/load-primed-pgvector.test.ts` exits 0.
- `grep -c "process.exit(130)" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{print s}'` returns `0` — all old-style synchronous-exit signal handlers removed.
- `grep -c "AbortController()" scripts/synthesize-episodic.ts scripts/fetch-prod-data.ts scripts/regenerate-primed.ts | awk -F: '{s+=$2} END{print s}'` returns `>= 3` — new pattern applied across all 3 scripts.
- `scripts/.ssh-known-hosts` exists and contains the Phase 45 FIX-04 header.
- `jq '.embedding | length' src/__tests__/fixtures/__fixtures__/pgvector-smoke/pensieve_embeddings.jsonl` returns `1024` (real-shaped dim).
- `grep -c "synth.contradictions.dropped" scripts/synthesize-delta.ts` returns `1` (single summary event per D-06).
- `grep -c "MIN_SPEECH_WORDS" src/__tests__/fixtures/primed-sanity-m011.test.ts` returns `>= 3` (import + 2 callsites consolidated).
</verification>

<success_criteria>
- All 7 requirements (FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-07, FIX-08) addressed at line-precise locations cited in their review references.
- No app-runtime code touched — only operator scripts + fixture-load + HARN test + repo-config + new smoke fixture + new test file.
- AbortController SIGINT pattern uniform across the 3 scripts (synthesize-episodic, fetch-prod-data, regenerate-primed); finally blocks run on Ctrl-C.
- SSH tunnel hardened with accept-new + repo-vetted known_hosts; MITM-after-rotation window closed.
- pgvector vector(1024) coercion works on the smoke fixture via staging-table CAST; regression closes on first non-empty regen.
- HARN word-count test uses calendar-month-windowed query mirroring substrate exactly; MIN_SPEECH_WORDS consolidated; >=/< boundary aligned.
- m011-1000words-5days path string consistent across primed-sanity-m011.test.ts + integration-m011-1000words.test.ts + deferred-items.md docs.
- Full Docker test suite green (modulo Plan 45-04 fixture-refresh dependency for the HARN populated-path assertion).
</success_criteria>

<output>
After completion, create `.planning/phases/45-schema-hygiene-fixture-cleanup/45-02-SUMMARY.md`
</output>
