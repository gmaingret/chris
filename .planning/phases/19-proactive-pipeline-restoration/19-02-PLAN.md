---
phase: 19-proactive-pipeline-restoration
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/proactive/prompts.ts
autonomous: true
requirements: [SWEEP-02]
requirements_addressed: [RES-02-enabler, RES-06-enabler, SWEEP-02]
gap_closure: true
canonical_commit: 4c156c3

must_haves:
  truths:
    - "src/proactive/prompts.ts exports `PROACTIVE_SYSTEM_PROMPT` (existing, unchanged)"
    - "src/proactive/prompts.ts exports `ACCOUNTABILITY_SYSTEM_PROMPT` with `{triggerContext}` placeholder, flattery guard, condemnation guard, and explicit prohibition of The Hard Rule (D027)"
    - "src/proactive/prompts.ts exports `ACCOUNTABILITY_FOLLOWUP_PROMPT` with natural 'couple days ago' phrasing for 48h escalation"
    - "Restored file is byte-exact from canonical commit 4c156c3"
    - "`npx tsc --noEmit` remains clean post-restoration"
  artifacts:
    - path: "src/proactive/prompts.ts"
      provides: "PROACTIVE_SYSTEM_PROMPT + ACCOUNTABILITY_SYSTEM_PROMPT + ACCOUNTABILITY_FOLLOWUP_PROMPT constants"
      exports:
        - "PROACTIVE_SYSTEM_PROMPT"
        - "ACCOUNTABILITY_SYSTEM_PROMPT"
        - "ACCOUNTABILITY_FOLLOWUP_PROMPT"
      min_lines: 75
      contains: "{triggerContext}"
  key_links:
    - from: "src/proactive/sweep.ts (restored in Plan 19-03)"
      to: "src/proactive/prompts.ts"
      via: "import { ACCOUNTABILITY_SYSTEM_PROMPT, ACCOUNTABILITY_FOLLOWUP_PROMPT } from './prompts.js'"
      pattern: "from ['\"]\\.\\/prompts"
    - from: "prompts.ts ACCOUNTABILITY_SYSTEM_PROMPT"
      to: "runtime {triggerContext} replacement"
      via: ".replace('{triggerContext}', ...)"
      pattern: "\\{triggerContext\\}"
---

<objective>
Restore `src/proactive/prompts.ts` byte-exact from canonical commit `4c156c3`. This adds the two ACCOUNTABILITY-channel LLM prompts that Plan 19-03's restored sweep.ts imports: `ACCOUNTABILITY_SYSTEM_PROMPT` (initial deadline-triggered outreach; neutral-factual with flattery/condemnation guards and explicit Hard Rule prohibition) and `ACCOUNTABILITY_FOLLOWUP_PROMPT` (48h escalation; natural "couple days ago" phrasing).

Purpose: Unblock Plan 19-03's sweep.ts restoration (which imports both prompts) and provide the prompt contract that Phase 15 VERIFICATION §Required Artifacts (line 37) and Phase 16 VERIFICATION §Required Artifacts (line 52) both attested as passing.

Output:
  - `src/proactive/prompts.ts` restored (29 → 77 lines; preserves PROACTIVE_SYSTEM_PROMPT verbatim, adds 2 new exports)
  - `npx tsc --noEmit` clean
  - Wave 2 Docker Postgres gate GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/19-proactive-pipeline-restoration/19-RESEARCH.md
@.planning/phases/19-proactive-pipeline-restoration/19-VALIDATION.md
@.planning/phases/15-deadline-trigger-sweep-integration/15-VERIFICATION.md
@.planning/phases/16-resolution-post-mortem-accountability-mode/16-VERIFICATION.md
@.planning/phases/19-proactive-pipeline-restoration/19-01-SUMMARY.md

<restoration_policy>
This plan is a byte-exact restoration from canonical commit `4c156c3`, identical in shape to Plan 19-01. Do not re-derive the prompt text. Do not substitute alternative flattery/condemnation guards. Do not change the `{triggerContext}` placeholder name (sweep.ts calls `.replace('{triggerContext}', ...)` by exact literal).

The canonical prompt text reflects decisions D-05 (channel separation), D-18 (48h escalation cadence), D-25 (praise-quarantine bypass at prompt level), D-27 (The Hard Rule is forbidden). These are locked; restoration preserves them.
</restoration_policy>

<interfaces>
<!-- Canonical prompts.ts export signatures. Executor MUST match these exactly. -->

Existing export (preserved verbatim from current 29-line file):
```typescript
export const PROACTIVE_SYSTEM_PROMPT = `...`;  // reflective-channel prompt; unchanged
```

New exports added by restoration:
```typescript
// ACCOUNTABILITY_SYSTEM_PROMPT: neutral-factual; has {triggerContext} placeholder;
// includes explicit "NEVER say: impressive, good job..." (flattery guard) and
// "NEVER say: I'm disappointed, you failed..." (condemnation guard);
// includes "The Hard Rule is explicitly forbidden" (D-27 compliance).
export const ACCOUNTABILITY_SYSTEM_PROMPT = `...`;

// ACCOUNTABILITY_FOLLOWUP_PROMPT: for 48h escalation; uses natural phrasing like
// "A couple of days ago I asked you about..." rather than robotic repeat;
// has {triggerContext} placeholder; same D-25/D-27 guards.
export const ACCOUNTABILITY_FOLLOWUP_PROMPT = `...`;
```
</interfaces>

<user_preference>
Per user MEMORY.md: Always run full Docker tests. Task 2 (wave-end gate) runs `bash scripts/test.sh`. Do not skip.
</user_preference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore prompts.ts with ACCOUNTABILITY constants</name>
  <files>src/proactive/prompts.ts</files>
  <read_first>
    - src/proactive/prompts.ts (current 29 lines — confirm `PROACTIVE_SYSTEM_PROMPT` exists and is the only export)
    - `git show 4c156c3:src/proactive/prompts.ts` (canonical 77 lines — target; inspect the flattery/condemnation guard text and Hard Rule prohibition)
    - .planning/phases/15-deadline-trigger-sweep-integration/15-VERIFICATION.md line 37 (attests ACCOUNTABILITY_SYSTEM_PROMPT existed with {triggerContext} placeholder, flattery guard, condemnation guard)
    - .planning/phases/16-resolution-post-mortem-accountability-mode/16-VERIFICATION.md line 52 (attests ACCOUNTABILITY_FOLLOWUP_PROMPT existed with "couple" phrasing for D-18)
    - .planning/phases/19-proactive-pipeline-restoration/19-RESEARCH.md §"src/proactive/prompts.ts (77 lines)"
  </read_first>
  <action>
Restore `src/proactive/prompts.ts` byte-exact from canonical commit `4c156c3`:

```bash
git show 4c156c3:src/proactive/prompts.ts > src/proactive/prompts.ts
```

**Expected post-restoration state:**

File must be 77 lines (was 29). It MUST preserve the existing `PROACTIVE_SYSTEM_PROMPT` export verbatim (used by the reflective channel) and add these two new `export const` statements:

1. `ACCOUNTABILITY_SYSTEM_PROMPT` — initial accountability outreach:
   - Contains `{triggerContext}` literal placeholder (sweep.ts performs `.replace('{triggerContext}', ctx)`)
   - Contains flattery guard language (e.g., phrases like "NEVER say:" followed by praise vocabulary such as "impressive", "good job")
   - Contains condemnation guard language (e.g., "NEVER say:" followed by "I'm disappointed", "you failed")
   - Contains explicit prohibition of The Hard Rule (D-27) — look for a phrase like "Hard Rule" or "you should have" in the forbidden list

2. `ACCOUNTABILITY_FOLLOWUP_PROMPT` — 48h escalation follow-up:
   - Contains `{triggerContext}` literal placeholder
   - Uses natural phrasing like "a couple of days ago" / "A couple days ago" — NOT a robotic repeat of the initial prompt
   - Same flattery/condemnation/Hard-Rule guards apply

**Verify byte-exact match:**
```bash
diff <(git show 4c156c3:src/proactive/prompts.ts) src/proactive/prompts.ts
# Expected: no output
```

**Verify required exports exist:**
```bash
grep -E "^export const (PROACTIVE_SYSTEM_PROMPT|ACCOUNTABILITY_SYSTEM_PROMPT|ACCOUNTABILITY_FOLLOWUP_PROMPT)" src/proactive/prompts.ts
# Expected: 3 matching lines
```

**Verify required guards/placeholders:**
```bash
grep -c "{triggerContext}" src/proactive/prompts.ts
# Expected: >= 2 (once in each ACCOUNTABILITY prompt)

grep -iE "couple" src/proactive/prompts.ts
# Expected: >= 1 (natural follow-up phrasing)

grep -iE "Hard Rule|should have|never say" src/proactive/prompts.ts
# Expected: >= 1 matching line for each of: "Hard Rule" OR "should have"; AND for "NEVER say" or equivalent
```

**Verify no schema / logic changes leaked in:**
```bash
# prompts.ts is pure string constants — should have zero runtime imports
grep -cE "^import" src/proactive/prompts.ts
# Expected: 0
```

Commit atomically:
```bash
git add src/proactive/prompts.ts
git commit -m "feat(19-02): restore ACCOUNTABILITY proactive prompts (RES-02, RES-06 enabler)

Restores src/proactive/prompts.ts from canonical commit 4c156c3 (29 -> 77 lines).

Preserves PROACTIVE_SYSTEM_PROMPT verbatim (reflective channel unchanged).

Adds two new exports required by Plan 19-03's sweep.ts restoration:

- ACCOUNTABILITY_SYSTEM_PROMPT: neutral-factual initial accountability prompt
  with {triggerContext} placeholder, flattery guard (D-25 praise-quarantine
  bypass at prompt level), condemnation guard, and explicit prohibition of
  The Hard Rule (D-27).

- ACCOUNTABILITY_FOLLOWUP_PROMPT: 48h escalation follow-up with natural
  'a couple of days ago' phrasing per D-18. Same D-25/D-27 guards.

Phase 15 VERIFICATION.md line 37 and Phase 16 VERIFICATION.md line 52 both
attested these prompts existed with the expected guard language — this is a
byte-exact restoration of that verified state.

Restored byte-exact from commit 4c156c3.

Refs: RES-02 enabler, RES-06 enabler, SWEEP-02 completion dependency,
Phase 19 gap closure."
```
  </action>
  <verify>
    <automated>diff <(git show 4c156c3:src/proactive/prompts.ts) src/proactive/prompts.ts</automated>
    <automated>grep -cE "^export const (PROACTIVE_SYSTEM_PROMPT|ACCOUNTABILITY_SYSTEM_PROMPT|ACCOUNTABILITY_FOLLOWUP_PROMPT)" src/proactive/prompts.ts</automated>
    <automated>grep -c "{triggerContext}" src/proactive/prompts.ts</automated>
    <automated>grep -ciE "couple" src/proactive/prompts.ts</automated>
    <automated>wc -l src/proactive/prompts.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `diff` against canonical returns empty (byte-exact)
    - 3 exports found: PROACTIVE_SYSTEM_PROMPT, ACCOUNTABILITY_SYSTEM_PROMPT, ACCOUNTABILITY_FOLLOWUP_PROMPT
    - `{triggerContext}` appears at least twice (once per ACCOUNTABILITY prompt)
    - "couple" or equivalent natural-stale phrasing appears at least once
    - `npx tsc --noEmit` exits 0
    - File is 77 lines
    - Atomic commit in place citing RES-02/RES-06 enabler
  </acceptance_criteria>
  <done>
    prompts.ts restored byte-exact from canonical; PROACTIVE preserved; 2 ACCOUNTABILITY exports added; typecheck clean; commit in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Wave 2 Docker Postgres gate</name>
  <files></files>
  <read_first>
    - scripts/test.sh (current 38 lines — same caveat as Plan 19-01 Task 4: migrations limited to 0000+0001; Plan 19-04 restores)
    - user MEMORY.md: "Always run full Docker tests; never skip integration tests"
  </read_first>
  <action>
Run the FULL Docker Postgres test suite as the Wave 2 gate:

```bash
bash scripts/test.sh 2>&1 | tee /tmp/wave2-gate.log
echo "EXIT CODE: $?"
```

**Expected results:**
  - `npx tsc --noEmit` still clean (prompts.ts is pure string constants; no type changes)
  - All Wave 1 green tests still green (state.test.ts, Phase 13-17 tests)
  - `sweep.test.ts` still in the same state as end of Wave 1 (awaiting Plan 19-03 replacement) — do NOT regress
  - No new test failures introduced by prompts.ts restoration (prompts.ts has no test of its own in canonical; canonical did not add one)

**Hard failure modes (MUST block the wave):**
  - `tsc --noEmit` non-zero (should not happen — prompts.ts is strings)
  - Regression in any previously-green test
  - Docker Postgres startup failure

**Known-acceptable failure mode:**
  - sweep.test.ts failing the same way it did at end of Wave 1 (pre-existing due to restoration-staging)

Do NOT skip this gate. Do NOT add `.skip` anywhere.

Commit wave-boundary marker:
```bash
git commit --allow-empty -m "chore(19-02): Wave 2 gate green — prompts.ts restored

Full Docker Postgres test suite ran:
- npx tsc --noEmit: 0 errors
- state.test.ts (Wave 1): green
- Phase 13-17 regression suite: no regressions
- sweep.test.ts: [same status as end of Wave 1; awaiting Plan 19-03]

ACCOUNTABILITY_SYSTEM_PROMPT and ACCOUNTABILITY_FOLLOWUP_PROMPT now
importable by sweep.ts (to be restored in Plan 19-03 Wave 3).

Refs: RES-02 + RES-06 enabler complete."
```
  </action>
  <verify>
    <automated>bash scripts/test.sh</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "ACCOUNTABILITY_SYSTEM_PROMPT" src/proactive/prompts.ts && grep -q "ACCOUNTABILITY_FOLLOWUP_PROMPT" src/proactive/prompts.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` exits 0 (or documents ONLY the acceptable sweep.test.ts pre-existing drift carried over from Wave 1)
    - `npx tsc --noEmit` exits 0
    - No Phase 13-18 regression beyond the carried-over Wave 1 sweep.test.ts state
    - Wave 2 boundary commit in place
    - If gate fails with new regression: SURFACE and PAUSE
  </acceptance_criteria>
  <done>
    Wave 2 Docker Postgres gate GREEN. Plan 19-02 complete — prompts.ts byte-exact from canonical; both ACCOUNTABILITY constants available for Plan 19-03 import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt string → LLM (Anthropic API) | Runtime `{triggerContext}` replacement interpolates decision data (prediction, falsification criterion, resolve_by) into the system prompt sent to Sonnet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Injection (prompt injection) | `{triggerContext}` replacement in ACCOUNTABILITY prompts — decision content (e.g., user-supplied prediction text) is interpolated into the system prompt | accept | Single-tenant system — `TELEGRAM_AUTHORIZED_USER_ID` gate enforced at bot layer; only Greg can write predictions. Prompt injection would be self-directed. No additional validation required beyond what Phase 14 capture flow already enforces. Risk inherited from Phase 15 and unchanged by restoration. |
| T-19-06 | Tampering | Restoration of prompts.ts overwrites any post-5582442 content | accept | 19-RESEARCH.md §Sources verified only commit `5582442` touched prompts.ts post-Phase-15; restoring from `4c156c3` recovers the verified state. |
| T-19-07 | Information disclosure | Prompts may contain guard language that leaks business rules (D-25, D-27) to Sonnet | accept | Intentional — the prompts exist explicitly to enforce D-25/D-27 at LLM level. This is by design. |
</threat_model>

<verification>
Plan 19-02 is verified complete when:

1. `diff <(git show 4c156c3:src/proactive/prompts.ts) src/proactive/prompts.ts` → empty
2. `grep` confirms all 3 exports present (PROACTIVE + 2 ACCOUNTABILITY)
3. `{triggerContext}` appears ≥ 2 times
4. "couple" (natural follow-up phrasing) appears ≥ 1 time
5. `npx tsc --noEmit` exits 0
6. `bash scripts/test.sh` exits 0 (wave gate)
7. No regression vs end of Wave 1
8. 2 commits: prompts.ts restoration, wave-2 gate marker
</verification>

<success_criteria>
- prompts.ts exports PROACTIVE_SYSTEM_PROMPT (unchanged), ACCOUNTABILITY_SYSTEM_PROMPT, ACCOUNTABILITY_FOLLOWUP_PROMPT
- Byte-exact match against canonical 4c156c3
- D-25 (flattery/condemnation guard) and D-27 (Hard Rule prohibition) compliance language present in restored text
- D-18 natural "couple days ago" phrasing present in follow-up prompt
- Wave 2 Docker Postgres gate GREEN
- Ready for Plan 19-03 sweep.ts import of both ACCOUNTABILITY_* constants
</success_criteria>

<output>
After completion, create `.planning/phases/19-proactive-pipeline-restoration/19-02-SUMMARY.md` documenting:
  - Diff-confirmation that prompts.ts is byte-exact from `4c156c3`
  - Exports present (3 total: PROACTIVE + 2 ACCOUNTABILITY)
  - Guard-language presence confirmed via grep (flattery, condemnation, Hard Rule, couple-days-ago)
  - Tsc and Docker gate results
  - Note for Plan 19-03: sweep.ts import line `import { ACCOUNTABILITY_SYSTEM_PROMPT, ACCOUNTABILITY_FOLLOWUP_PROMPT } from './prompts.js'` is now satisfiable; both constants resolve to the canonical strings
</output>
