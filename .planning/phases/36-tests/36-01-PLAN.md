---
phase: 36-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/synthesize-delta.ts
  - scripts/__tests__/synthesize-delta-profile-bias.test.ts
  - src/__tests__/fixtures/chat-ids.ts
  - src/__tests__/fixtures/seed-profile-rows.ts
  - src/__tests__/fixtures/primed-sanity-m010.test.ts
  - src/memory/profiles/__tests__/integration-m010-30days.test.ts
  - src/memory/profiles/__tests__/integration-m010-5days.test.ts
autonomous: false
requirements: [PTEST-01, PTEST-02, PTEST-03, PTEST-04]
tags: [m010, profiles, primed-fixture, integration-test, harn-sanity, idempotency]

must_haves:
  truths:
    - "`scripts/synthesize-delta.ts` accepts repeatable `--profile-bias <dim>` flag for dim ∈ {jurisdictional, capital, health, family}; unknown dim throws UsageError"
    - "When `--profile-bias` is provided, the per-day Haiku style-transfer prompt includes a per-dimension keyword hint sourced from `PROFILE_BIAS_KEYWORDS`, rotated round-robin via `PROFILE_BIAS_ROTATION` (D-09)"
    - "When `--profile-bias` is omitted, the Haiku prompt is byte-identical to the pre-change shape (legacy m009-21days regeneration unaffected)"
    - "Operator can regenerate `m010-30days` and `m010-5days` primed fixtures locally via documented `npx tsx scripts/regenerate-primed.ts ...` command (Pitfall P-36-01 Option A — fixtures gitignored)"
    - "HARN sanity gate `primed-sanity-m010.test.ts` skips cleanly when fixtures absent, asserts ≥12 keyword-classified entries per dimension on `m010-30days`, and <10 per dimension AND ≥1 per dimension on `m010-5days`"
    - "Shared helper `seedProfileRows()` idempotently re-applies migration 0012 seed-row values via `ON CONFLICT (name) DO UPDATE SET ...` for all 4 profile_* tables and deletes `profile_history` rows (Pitfall P-36-02 mitigation)"
    - "PTEST-02: loading `m010-30days` + running `updateAllOperationalProfiles()` produces 4 profile rows with confidence > 0, advancing `last_updated`, 64-hex `substrate_hash`, and 4 `profile_history` rows"
    - "PTEST-03 Cycle 1: `mockAnthropicParse` invoked exactly 4 times; Cycle 1's system prompt contains `CURRENT PROFILE STATE` (M010-10 prev-state injection verified per D-20)"
    - "PTEST-03 Cycle 2 (identical substrate, +7 days): cumulative `mockAnthropicParse.toHaveBeenCalledTimes(4)` (NOT 8); 4× `'chris.profile.profile_skipped_no_change'` log entries; `profile_history` still at 4 rows"
    - "PTEST-03 Cycle 3 (mutated substrate via new pensieve entry, +14 days): cumulative `mockAnthropicParse.toHaveBeenCalledTimes(8)` (4 new calls, NOT 5 — per D-19 corrected); all 4 outcomes `'profile_updated'`; `profile_history` has 8 rows"
    - "PTEST-04: loading `m010-5days` + running orchestrator → `mockAnthropicParse` NEVER called; all 4 outcomes `'profile_below_threshold'`; 4× `'chris.profile.threshold.below_minimum'` log entries with dimensions sorted [capital, family, health, jurisdictional]; profile rows byte-identical before/after"
  artifacts:
    - path: "scripts/synthesize-delta.ts"
      provides: "--profile-bias repeatable flag + PROFILE_BIAS_KEYWORDS + PROFILE_BIAS_ROTATION + dimensionHintFor helper"
      contains: "PROFILE_BIAS_KEYWORDS"
    - path: "scripts/__tests__/synthesize-delta-profile-bias.test.ts"
      provides: "Unit tests for --profile-bias flag acceptance, rejection, rotation, hint injection, VCR-cache invalidation"
      exports: []
    - path: "src/__tests__/fixtures/chat-ids.ts"
      provides: "CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922) constant for Plan 36-02 import"
      contains: "CHAT_ID_LIVE_ANTI_HALLUCINATION"
    - path: "src/__tests__/fixtures/seed-profile-rows.ts"
      provides: "seedProfileRows({ dbOverride? }) — idempotent UPSERT of migration-0012 seed rows for all 4 profile_* tables + profile_history wipe"
      exports: ["seedProfileRows"]
    - path: "src/__tests__/fixtures/primed-sanity-m010.test.ts"
      provides: "HARN gate for m010-30days (≥12 per dim) and m010-5days (<10 per dim, ≥1 per dim); skip-when-fixture-absent"
    - path: "src/memory/profiles/__tests__/integration-m010-30days.test.ts"
      provides: "PTEST-02 populated-case + PTEST-03 three-cycle idempotency (Cycle 1/2/3 = 4/4/8 cumulative calls)"
    - path: "src/memory/profiles/__tests__/integration-m010-5days.test.ts"
      provides: "PTEST-04 sparse threshold-enforcement (zero Sonnet calls, 4× below_minimum log)"
  key_links:
    - from: "scripts/synthesize-delta.ts"
      to: "Haiku style-transfer prompt (per-day call site lines 521-527)"
      via: "dimensionHintFor(dayIndex, profileBias) appended to buildHaikuSystemPrompt"
      pattern: "PROFILE_BIAS_ROTATION\\[d % PROFILE_BIAS_ROTATION\\.length\\]"
    - from: "src/memory/profiles/__tests__/integration-m010-30days.test.ts"
      to: "src/memory/profile-updater.ts (updateAllOperationalProfiles)"
      via: "direct invocation in describe block after fixture load + profile seed"
      pattern: "updateAllOperationalProfiles\\("
    - from: "src/memory/profiles/__tests__/integration-m010-5days.test.ts"
      to: "src/__tests__/fixtures/seed-profile-rows.ts (seedProfileRows)"
      via: "beforeEach reset to migration-0012 seed state (Pitfall P-36-02 mitigation)"
      pattern: "seedProfileRows\\(\\)"
    - from: "src/__tests__/fixtures/primed-sanity-m010.test.ts"
      to: "tests/fixtures/primed/m010-{30days,5days}/MANIFEST.json"
      via: "existsSync skip-when-absent gate (Pitfall P-36-01 mitigation)"
      pattern: "describe\\.skip|skipIfAbsent"
---

<objective>
Ship the M010 fixture-driven integration test surface: extend `scripts/synthesize-delta.ts` with the `--profile-bias` repeatable flag, document the regeneration command for the two new primed fixtures (m010-30days, m010-5days), create the shared `seedProfileRows()` helper to mitigate Pitfall P-36-02, and write 4 test files covering PTEST-01 (unit + HARN), PTEST-02 (populated), PTEST-03 (3-cycle idempotency), PTEST-04 (sparse threshold).

Purpose: HARD CO-LOC #M10-6 — the flag, the fixtures, the HARN gate, and the 4 integration tests ALL ship in one atomic plan. Splitting them across plans creates synth-pipeline/test mismatch (a bias-keyword change in one plan + the test that depends on the resulting fixture content in another plan).

Output:
- 1 modified CLI script (`scripts/synthesize-delta.ts`)
- 1 new unit test file (`scripts/__tests__/synthesize-delta-profile-bias.test.ts`)
- 1 modified chat-ids registry (`src/__tests__/fixtures/chat-ids.ts`)
- 1 new fixture helper (`src/__tests__/fixtures/seed-profile-rows.ts`)
- 1 new HARN sanity test (`src/__tests__/fixtures/primed-sanity-m010.test.ts`)
- 2 new integration test files (`integration-m010-30days.test.ts`, `integration-m010-5days.test.ts`)
- 1 operator checkpoint: regenerate fixtures locally (Pitfall P-36-01 Option A — gitignored; manual command)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-tests/36-CONTEXT.md
@.planning/phases/36-tests/36-RESEARCH.md
@.planning/phases/36-tests/36-PATTERNS.md
@.planning/phases/36-tests/36-VALIDATION.md
@.planning/phases/33-profile-substrate/33-SUMMARY.md
@.planning/phases/34-inference-engine/34-CONTEXT.md
@.planning/phases/34-inference-engine/34-02-SUMMARY.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From `src/__tests__/fixtures/chat-ids.ts` (existing exports — append new one after 99921):
```typescript
export const CHAT_ID_CHRIS_LIVE = BigInt(99901);
export const CHAT_ID_SYNTHETIC_FIXTURE = BigInt(99918);
export const CHAT_ID_LIVE_ACCOUNTABILITY = BigInt(99919);
export const CHAT_ID_VAGUE_VALIDATOR_LIVE = BigInt(99920);
export const CHAT_ID_M009_SYNTHETIC_FIXTURE = BigInt(99921);
// NEW: export const CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922);
```

From `src/db/schema.ts` (profile tables):
- `profileJurisdictional`, `profileCapital`, `profileHealth`, `profileFamily` — each with `name`, `schemaVersion`, `substrateHash`, `confidence`, `dataConsistency`, dimension-specific jsonb columns, `lastUpdated`
- `profileHistory` — polymorphic snapshot table; cleared in `beforeEach`

From `src/memory/profile-updater.ts`:
```typescript
export async function updateAllOperationalProfiles(opts?: { now?: Date }): Promise<readonly ProfileOutcome[]>;
// Outcomes: 'profile_updated' | 'profile_skipped_no_change' | 'profile_below_threshold' | 'profile_error'
```

From `src/memory/profiles/shared.ts`:
```typescript
export async function loadProfileSubstrate(): Promise<ProfileSubstrate>;
export function computeSubstrateHash(substrate: ProfileSubstrate, prevStateMeta?: { schema_version: number }): string;
// Single shared substrate across all 4 generators (Phase 34 D-14 — per-dimension filtering is v2.5.1)
```

From `src/__tests__/fixtures/load-primed.ts`:
```typescript
export async function loadPrimedFixture(name: string, opts?: { dbOverride?: postgres.Sql }): Promise<void>;
// FK-safe loader; clears + re-inserts 10 tables; does NOT touch profile_* (Pitfall P-36-02 — see seed-profile-rows.ts)
```

From `src/__tests__/fixtures/primed-sanity.test.ts:55-90` (scaffold to lift):
```typescript
const FIXTURE_PATH = `tests/fixtures/primed/${FIXTURE_NAME}/MANIFEST.json`;
const FIXTURE_PRESENT = existsSync(FIXTURE_PATH);
const skipIfAbsent = FIXTURE_PRESENT ? describe : describe.skip;
if (!FIXTURE_PRESENT) {
  console.log(`[primed-sanity] SKIP: ${FIXTURE_PATH} not found. Regenerate with: npx tsx scripts/regenerate-primed.ts --milestone m010 ...`);
}
skipIfAbsent('primed-sanity: ...', () => { beforeAll(async () => { await loadPrimedFixture(FIXTURE_NAME); }); /* ... */ });
```

From `src/memory/profiles/__tests__/generators.two-cycle.test.ts` (scaffold to lift verbatim for integration-m010-30days.test.ts):
```typescript
const { mockAnthropicParse, mockLoggerInfo, mockLoggerWarn, mockLoggerError } = vi.hoisted(() => ({
  mockAnthropicParse: vi.fn(),
  mockLoggerInfo: vi.fn(),
  mockLoggerWarn: vi.fn(),
  mockLoggerError: vi.fn(),
}));
vi.mock('../../../llm/client.js', async (importOriginal) => {
  const orig = await importOriginal<typeof import('../../../llm/client.js')>();
  return { ...orig, anthropic: { messages: { parse: mockAnthropicParse, create: vi.fn() } } };
});
vi.mock('../../../utils/logger.js', () => ({
  logger: { info: mockLoggerInfo, warn: mockLoggerWarn, error: mockLoggerError, debug: vi.fn() },
}));
// Dimension-routing mock via primeAllDimensionsValid() — system text contains 'Dimension Focus — Jurisdictional|Capital|Health|Family'
```

From `src/db/migrations/0012_operational_profiles.sql:132-204` (seed-row INSERT shapes for `seedProfileRows()` helper):
- jurisdictional seed: confidence 0.3, substrate_hash '', current_country '"Russia"'::jsonb, physical_location '"Saint Petersburg"'::jsonb, residency_status [{type, value}], active_legal_entities [{name, jurisdiction}], next_planned_move {destination, from_date}, planned_move_date '"2026-04-28"'::jsonb, passport_citizenships '["French"]'::jsonb
- capital seed: confidence 0.2, fi_target 1500000 + MAINGRET LLC + Georgian IE entities
- health seed: confidence 0, all jsonb arrays empty, case_file_narrative '"insufficient data"'::jsonb, wellbeing_trend '{}'::jsonb
- family seed: confidence 0, relationship_status '"insufficient data"'::jsonb, children_plans '"insufficient data"'::jsonb, active_dating_context '"insufficient data"'::jsonb

From `src/memory/profiles.ts`:
```typescript
export const PROFILE_INJECTION_HEADER = '## Operational Profile (grounded context — not interpretation)';
// Phase 35 D-08 / SURF-02
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --profile-bias flag + PROFILE_BIAS_KEYWORDS + PROFILE_BIAS_ROTATION to synthesize-delta.ts</name>
  <files>scripts/synthesize-delta.ts, scripts/__tests__/synthesize-delta-profile-bias.test.ts</files>
  <read_first>
    - scripts/synthesize-delta.ts (lines 1-300 for parseArgs block, then 400-600 for per-day call site; the file is ~670 lines so two ranged reads cover it)
    - scripts/__tests__/synthesize-delta.test.ts (existing scaffold — extend or sibling per PATTERNS.md decision; the file at scripts/__tests__/synthesize-delta-profile-bias.test.ts is NEW; lift the mock setup from synthesize-delta.test.ts:25-117)
    - .planning/phases/36-tests/36-CONTEXT.md §"Implementation Decisions" D-03..D-06 (flag shape, soft keyword hint, PROFILE_BIAS_KEYWORDS per-dimension list, VCR-cache invalidation)
    - .planning/phases/36-tests/36-PATTERNS.md §"scripts/synthesize-delta.ts" (existing parseArgs block at 140-201, Haiku prompt builder at 264-276, per-day call site at 521-527 — three additive injection points already located)
    - .planning/phases/36-tests/36-RESEARCH.md §"Pattern 2 — soft-hint keyword injection" (preserves stylistic variability; Haiku CHOOSES whether to incorporate)
  </read_first>
  <action>
    Per D-03: add parseArgs option `'profile-bias': { type: 'string', multiple: true, default: [] as string[] }` to the existing parseArgs block (around line 149-161). Per D-04 + D-05: declare a `Dimension` type union and two constants near `ENTRIES_PER_DAY` (line 76): `PROFILE_BIAS_KEYWORDS: Record<Dimension, readonly string[]>` (jurisdictional / capital / health / family keyword lists verbatim from D-05) and `PROFILE_BIAS_ROTATION: readonly Dimension[] = ['jurisdictional', 'capital', 'health', 'family'] as const` (D-09 round-robin order). Validate each `--profile-bias` value is in `DIMENSIONS` — reject with UsageError (T-36-V5 mitigation: input validation at the CLI entry boundary). Export a `dimensionHintFor(dayIndex: number, biases: readonly Dimension[]): string | undefined` helper that returns `PROFILE_BIAS_KEYWORDS[PROFILE_BIAS_ROTATION[dayIndex % 4]].join(', ')` IF the rotated dim is included in `biases`, else `undefined`. Extend `buildHaikuSystemPrompt(fewShot, dateIso, nEntries, dimensionHint?)` to append "Focus today's entries on {dim}-relevant facts ({keywords})." when the hint is provided. Plumb `opts.profileBias` from parseCliArgs → synthesize() into the per-day call site at line 521-527 so each day picks up its rotated hint. When `profileBias.length === 0`, the legacy prompt is preserved byte-identical (verify via unit test). Write `scripts/__tests__/synthesize-delta-profile-bias.test.ts` covering 6 cases per PATTERNS.md "Add cases" block at lines 622-686: (a) repeatable flag accepted single + multi, (b) unknown dim throws UsageError, (c) omitted → empty array, (d) rotation jurisdictional→capital→health→family→wrap, (e) hint appears in Haiku prompt for biased day, (f) no hint when omitted. Use the existing mock setup from synthesize-delta.test.ts:25-117 (mocks `cachedMessagesParse`, `autoRefreshIfStale`, `anthropic`, `logger`, `postgres`).
  </action>
  <acceptance_criteria>
    - `scripts/synthesize-delta.ts` exports `PROFILE_BIAS_KEYWORDS` and `PROFILE_BIAS_ROTATION` constants — verify via `grep -n "^export const PROFILE_BIAS" scripts/synthesize-delta.ts | grep -c PROFILE_BIAS` ≥ 2
    - `scripts/synthesize-delta.ts` exports `dimensionHintFor` helper — verify via `grep -nE "^export function dimensionHintFor" scripts/synthesize-delta.ts | wc -l` = 1
    - `npx tsx scripts/synthesize-delta.ts --help` (or invocation with no organic) lists `--profile-bias` in usage
    - `bash scripts/test.sh scripts/__tests__/synthesize-delta-profile-bias.test.ts` exits 0 with all 6+ cases green
    - `bash scripts/test.sh scripts/__tests__/synthesize-delta.test.ts` still exits 0 (legacy behavior preserved)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh scripts/__tests__/synthesize-delta-profile-bias.test.ts scripts/__tests__/synthesize-delta.test.ts</automated>
  </verify>
  <done>Flag accepted as repeatable; per-day round-robin rotation correct; hint appears in Haiku prompt iff `--profile-bias` provided AND the rotated dim is in the bias list; legacy invocations (m009 regen) unaffected; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate CHAT_ID_LIVE_ANTI_HALLUCINATION + create seedProfileRows helper</name>
  <files>src/__tests__/fixtures/chat-ids.ts, src/__tests__/fixtures/seed-profile-rows.ts</files>
  <read_first>
    - src/__tests__/fixtures/chat-ids.ts (current ID assignments — last value is BigInt(99921))
    - src/__tests__/fixtures/load-primed.ts (FK-safe idempotent loader shape — model for seedProfileRows public API + optional dbOverride + logger.info('fixture.seed_profile_rows.done') pattern; specifically lines 165-282)
    - src/db/migrations/0012_operational_profiles.sql lines 132-204 (verbatim seed-row INSERT statements for all 4 profile tables — these are the source-of-truth values to UPSERT)
    - src/db/connection.ts (db + sql exports)
    - src/db/schema.ts (profileJurisdictional, profileCapital, profileHealth, profileFamily, profileHistory tables — confirm column names for ON CONFLICT UPDATE SET clauses)
    - .planning/phases/36-tests/36-PATTERNS.md §"src/__tests__/fixtures/seed-profile-rows.ts" (lines 117-191 — full helper shape with the critical `ON CONFLICT (name) DO UPDATE SET …` pattern, NOT `DO NOTHING`)
    - .planning/phases/36-tests/36-RESEARCH.md §"Pitfall P-36-02" (lines 536-555 — full rationale; loader does not touch profile_* tables; tests must reset to seed state in beforeEach)
    - .planning/codebase/TESTING.md line 105 (chat-id allocation rule: "every test file that writes chat-scoped rows MUST import its chat ID from here. When adding a new test file, allocate the next ID.")
  </read_first>
  <action>
    Append `export const CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922);` to `src/__tests__/fixtures/chat-ids.ts` directly after `CHAT_ID_M009_SYNTHETIC_FIXTURE = BigInt(99921)` (TESTING.md:105 sequential convention; planning context explicitly locks 99922 — NOT the VALIDATION.md 99936 typo). Add a one-line JSDoc above pointing to Phase 36 Plan 36-02. Create `src/__tests__/fixtures/seed-profile-rows.ts` exporting `export async function seedProfileRows(opts?: { dbOverride?: postgres.Sql }): Promise<void>` — the helper performs 4 INSERT statements (one per profile table) using `ON CONFLICT (name) DO UPDATE SET ...` (mutation-resetting, NOT migration's DO NOTHING) with the verbatim values from migration 0012:132-204; ALSO performs `await db.delete(profileHistory)` so two-cycle/sparse tests start with zero history rows. Use `client.unsafe(...)` or `db.execute(sql\`...\`)` matching load-primed.ts conventions. Log `'fixture.seed_profile_rows.done'` with row counts on completion (per load-primed.ts pattern). The function MUST be idempotent — calling it twice in a row produces the same end state and writes 0 net new history rows. CRITICAL: the helper sets every mutable column back to seed via UPDATE SET (substrate_hash='', confidence=0.3/0.2/0/0, data_consistency=0, jsonb columns to seed values, last_updated=NOW()). This protects PTEST-02/03/04 from cross-test pollution per P-36-02.
  </action>
  <acceptance_criteria>
    - `grep -nE '^export const CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt\(99922\)' src/__tests__/fixtures/chat-ids.ts | wc -l` = 1
    - `src/__tests__/fixtures/seed-profile-rows.ts` exists and exports `seedProfileRows` — verify via `grep -nE '^export async function seedProfileRows' src/__tests__/fixtures/seed-profile-rows.ts | wc -l` = 1
    - File contains 4 ON CONFLICT DO UPDATE SET clauses (one per dimension) — verify via `grep -c 'ON CONFLICT.*DO UPDATE SET' src/__tests__/fixtures/seed-profile-rows.ts` ≥ 4
    - File contains profile_history wipe — verify via `grep -nE 'delete\(profileHistory\)|TRUNCATE.*profile_history' src/__tests__/fixtures/seed-profile-rows.ts | wc -l` ≥ 1
    - TypeScript compiles — verify via `npx tsc --noEmit` or `bash scripts/test.sh src/__tests__/fixtures/` (no isolated test for the helper yet — consumed by Tasks 5/6/7)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "seed-profile-rows|chat-ids" || echo "ok"</automated>
  </verify>
  <done>Chat-id registry has 99922 allocated for Plan 36-02; seedProfileRows helper compiles and is callable from beforeEach hooks; ON CONFLICT DO UPDATE SET resets all mutable columns to seed values (mutation-resetting); profile_history cleared.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator regenerates m010-30days + m010-5days primed fixtures locally</name>
  <what-built>
    The `--profile-bias` flag now exists in `scripts/synthesize-delta.ts` (Task 1). The two new primed fixtures (`tests/fixtures/primed/m010-30days/` and `tests/fixtures/primed/m010-5days/`) are required by Tasks 4-7 (HARN gate + 2 integration tests). Per Pitfall P-36-01 Option A, both directories are gitignored (`.gitignore` lines 23-25) and MUST be regenerated locally on every fresh checkout. The VCR cache (`tests/fixtures/.vcr/`) is also gitignored. First run hits the live Anthropic API (~$0.11 for ~35 Haiku calls combined); subsequent runs use VCR cache (free).
  </what-built>
  <how-to-verify>
    Run these two commands in order from the repo root, ensuring `ANTHROPIC_API_KEY` is in `.env` (already configured per prod):

    **Generate m010-30days (~30 Haiku calls, ~$0.10):**
    ```bash
    cd /home/claude/chris && npx tsx scripts/regenerate-primed.ts --milestone m010 --target-days 30 --profile-bias jurisdictional --profile-bias capital --profile-bias health --profile-bias family --force --seed 42
    ```

    **Generate m010-5days (~5 Haiku calls, ~$0.02):**
    ```bash
    cd /home/claude/chris && npx tsx scripts/regenerate-primed.ts --milestone m010 --target-days 5 --profile-bias jurisdictional --profile-bias capital --profile-bias health --profile-bias family --force --seed 42
    ```

    **Verify both succeeded:**
    ```bash
    ls tests/fixtures/primed/m010-30days/MANIFEST.json tests/fixtures/primed/m010-5days/MANIFEST.json && wc -l tests/fixtures/primed/m010-30days/pensieve_entries.jsonl tests/fixtures/primed/m010-5days/pensieve_entries.jsonl
    ```

    Expected: both MANIFEST.json files exist; m010-30days has ~90 entries (30 days × 3 entries/day); m010-5days has ~15 entries (5 × 3).

    **Note on regeneration determinism (Pitfall P-36-03):** seed=42 + 4-way rotation is deterministic given the same Haiku VCR cache. The keywords are NUDGES, not guarantees — Haiku may emit entries that fail the HARN gate. If HARN fails in Task 4, the operator may need to (a) tune `PROFILE_BIAS_KEYWORDS` in Task 1, (b) increase `ENTRIES_PER_DAY` from 3 to 4, or (c) extend `target-days` to 32 — per OQ-4 reconfirmation in CONTEXT.md `plan_hints`. The planner-recommended starting point is the locked D-09 4-way rotation; tune only if HARN fails.
  </how-to-verify>
  <resume-signal>Type "fixtures regenerated" and paste the `wc -l` output, OR describe issues encountered (auth errors, HARN-threshold concerns, VCR cache misses)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: HARN sanity gate test — primed-sanity-m010.test.ts (PTEST-01 enabler)</name>
  <files>src/__tests__/fixtures/primed-sanity-m010.test.ts</files>
  <read_first>
    - src/__tests__/fixtures/primed-sanity.test.ts (analog — full file; D-34 mandates verbatim scaffold; specifically the `FIXTURE_PRESENT` / `skipIfAbsent` pattern at lines 82-90 + the per-pensieve-count assertion shape at lines 104-109)
    - .planning/phases/36-tests/36-PATTERNS.md §"src/__tests__/fixtures/primed-sanity-m010.test.ts" (lines 194-262 — full scaffold to lift + the per-dimension keyword-grep assertion shape)
    - .planning/phases/36-tests/36-CONTEXT.md D-10 (≥12 per dim for m010-30days), D-11 (keyword-grep classification — NOT tag inspection), D-13 (<10 per dim for m010-5days), D-34 (use primed-sanity.test.ts scaffold verbatim)
    - .planning/phases/36-tests/36-RESEARCH.md §"Pitfall P-36-01" (Option A skip-when-absent pattern) AND §"Pitfall P-36-04" (keyword-grep false-positive double-counting — accept v1, doc in file header)
    - tests/fixtures/primed/m010-30days/MANIFEST.json (created by Task 3)
    - src/db/schema.ts (pensieveEntries table for the count query)
    - scripts/synthesize-delta.ts (PROFILE_BIAS_KEYWORDS — import or duplicate the constant in the test file; the constants are co-located in the script per Task 1)
  </read_first>
  <action>
    Create `src/__tests__/fixtures/primed-sanity-m010.test.ts` mirroring `primed-sanity.test.ts` verbatim per D-34. Define two `describe` blocks — one for `m010-30days`, one for `m010-5days` — each with its own `FIXTURE_PRESENT = existsSync('tests/fixtures/primed/{name}/MANIFEST.json')` gate and `skipIfAbsent = FIXTURE_PRESENT ? describe : describe.skip` pattern (P-36-01 mitigation). Print an operator instruction comment via `console.log` when absent: "Regenerate via npx tsx scripts/regenerate-primed.ts --milestone m010 --target-days {N} --profile-bias jurisdictional ...". Each describe block runs `beforeAll(async () => { await loadPrimedFixture(FIXTURE_NAME); })`. Import `PROFILE_BIAS_KEYWORDS` from `scripts/synthesize-delta.ts` (single source of truth — NOT duplicated). For m010-30days, loop over the 4 dimensions and assert per-dimension keyword-grep count ≥ 12 via `db.execute(sql\`SELECT COUNT(*)::int AS n FROM pensieve_entries WHERE content ~* ${keywordPattern}\`)`. For m010-5days, assert per-dimension count < 10 AND ≥ 1 (anti-zero trip-wire per test_strategy item 2.b). Add a file-header JSDoc per TESTING.md:114-138 with phase + plan + task ID, test enumeration, and `Run:` example using `bash scripts/test.sh ...`. Add an inline comment near the keyword-grep block documenting Pitfall P-36-04 (entries may match multiple dimensions; double-counting accepted in v1; HARN is a coverage signal, not a precision claim).
  </action>
  <acceptance_criteria>
    - `src/__tests__/fixtures/primed-sanity-m010.test.ts` exists and contains both `'m010-30days'` and `'m010-5days'` describe blocks — verify via `grep -cE "describe\.|skipIfAbsent.*m010-(30days|5days)" src/__tests__/fixtures/primed-sanity-m010.test.ts | grep -v '^#'` ≥ 2
    - File imports `PROFILE_BIAS_KEYWORDS` from synthesize-delta (single source of truth) — verify via `grep -nE "from '.*synthesize-delta" src/__tests__/fixtures/primed-sanity-m010.test.ts | wc -l` ≥ 1
    - File contains skip-when-absent pattern — verify via `grep -nE "existsSync|FIXTURE_PRESENT" src/__tests__/fixtures/primed-sanity-m010.test.ts | wc -l` ≥ 2
    - `bash scripts/test.sh src/__tests__/fixtures/primed-sanity-m010.test.ts` exits 0 (when fixtures present, all 8 per-dim assertions pass; when absent, test is skipped cleanly with stdout instruction)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/__tests__/fixtures/primed-sanity-m010.test.ts</automated>
  </verify>
  <done>HARN gate enforces ≥12 entries per dimension on m010-30days and <10 per dim on m010-5days (with ≥1 anti-zero floor); skip-when-absent pattern matches primed-sanity.test.ts:82-90 precedent; double-counting risk documented in file header.</done>
</task>

<task type="auto">
  <name>Task 5: Integration test m010-30days — PTEST-02 populated + PTEST-03 three-cycle idempotency</name>
  <files>src/memory/profiles/__tests__/integration-m010-30days.test.ts</files>
  <read_first>
    - src/memory/profiles/__tests__/generators.two-cycle.test.ts (FULL file — analog per D-33; scaffold to lift verbatim: vi.hoisted block lines 48-79, valid-response factories 114-174, primeAllDimensionsValid 187-205, cycle-by-cycle assertions 226-352)
    - src/memory/profile-updater.ts (updateAllOperationalProfiles signature + accepted opts.now param)
    - src/memory/profiles/shared.ts (loadProfileSubstrate, computeSubstrateHash — Phase 34 D-14 single-shared-substrate rationale governs why Cycle 3 = 8 calls not 5)
    - src/__tests__/fixtures/load-primed.ts (loadPrimedFixture signature)
    - src/__tests__/fixtures/seed-profile-rows.ts (created in Task 2 — imported in beforeEach)
    - .planning/phases/36-tests/36-PATTERNS.md §"src/memory/profiles/__tests__/integration-m010-30days.test.ts" (lines 266-429 — full file scaffold including the 3-cycle assertion shape and the M010-10 prev-state injection check)
    - .planning/phases/36-tests/36-CONTEXT.md D-15..D-20 (test file location, mock SDK boundary, assertion shape, three-cycle structure — D-19 CORRECTED 2026-05-13: Cycle 3 cumulative = **8** not 5)
    - .planning/phases/36-tests/36-RESEARCH.md §"PTEST-03 Cycle 3 Call Count Discrepancy" lines 428 (8 not 5 — per Phase 34 D-14 single shared substrate)
    - .planning/phases/34-inference-engine/34-CONTEXT.md D-13, D-14, D-15, D-17, D-29 (substrate-hash policy, single shared substrate, write-before-upsert profile_history)
  </read_first>
  <action>
    Create `src/memory/profiles/__tests__/integration-m010-30days.test.ts`. Lift verbatim from `generators.two-cycle.test.ts`: (a) the vi.hoisted block at lines 48-79 (mockAnthropicParse + mockLoggerInfo/Warn/Error), (b) the vi.mock calls for `../../../llm/client.js` and `../../../utils/logger.js`, (c) the four `valid{Jurisdictional|Capital|Health|Family}Response()` factories at lines 114-174 with seed-row-consistent values, (d) `primeAllDimensionsValid()` mock router at lines 187-205 (dispatches on `'Dimension Focus — Jurisdictional|Capital|Health|Family'` in the system prompt). Add a skip-when-absent gate: `const FIXTURE_PRESENT = existsSync('tests/fixtures/primed/m010-30days/MANIFEST.json'); const skipIfAbsent = FIXTURE_PRESENT ? describe : describe.skip;`. Structure: ONE outer `skipIfAbsent('PTEST-02 + PTEST-03 — m010-30days', () => { ... })` block with `beforeAll(async () => { await pgSql\`SELECT 1\`; await loadPrimedFixture('m010-30days'); })` (fixture load amortized) and `beforeEach(async () => { await seedProfileRows(); mockAnthropicParse.mockReset(); mockLoggerInfo.mockReset(); primeAllDimensionsValid(); })` (profile reset per-test for Pitfall P-36-02). Define TWO `it()` blocks: (1) **PTEST-02 Cycle 1 populated** — `now = new Date('2026-05-17T22:00:00Z')`; call orchestrator; assert `mockAnthropicParse.toHaveBeenCalledTimes(4)`; SELECT each profile_* row and assert `confidence > 0`, `substrateHash` matches `/^[0-9a-f]{64}$/`, `lastUpdated.getTime() > new Date('2026-05-01').getTime()`; assert `profile_history` has exactly 4 rows. (2) **PTEST-03 three-cycle** — Cycle 1 (`now=2026-05-17`): orchestrator → expect 4 cumulative calls; then **inspect `mockAnthropicParse.mock.calls[0]![0].system[0].text`** for `CURRENT PROFILE STATE` substring (D-20 / M010-10 prev-state injection). Cycle 2 (`now=2026-05-24`, identical substrate): orchestrator → expect STILL 4 cumulative calls (NOT 8); assert 4× `mockLoggerInfo` calls with key `'chris.profile.profile_skipped_no_change'`; assert `profile_history` still has 4 rows. Cycle 3 (`now=2026-05-31`, mutate substrate via `db.insert(pensieveEntries).values({ content: 'I moved from Saint Petersburg to Tbilisi this week.', epistemicTag: 'FACT', createdAt: new Date('2026-05-28T12:00:00Z'), userId: 1n, language: 'English', /* + other required fields per schema */ })`): orchestrator → assert `mockAnthropicParse.toHaveBeenCalledTimes(8)` (4 + 4 — per Phase 34 D-14 single shared substrate means any mutation invalidates ALL 4 dim hashes; this is the documented v1 trade-off, NOT 5); assert `profile_history` has 8 rows now; assert all 4 outcomes from this fire are `'profile_updated'`. Add `afterAll(async () => { await db.delete(profileHistory); await seedProfileRows(); })` to leave a clean state. File header JSDoc per TESTING.md:114-138.
  </action>
  <acceptance_criteria>
    - File exists with both PTEST-02 + PTEST-03 `it()` blocks — verify via `grep -cE "it\(['\"](PTEST-0[23]|.*Cycle [123]|.*populated)" src/memory/profiles/__tests__/integration-m010-30days.test.ts` ≥ 2
    - File asserts Cycle 3 cumulative call count is 8 (NOT 5) — verify via `grep -nE 'toHaveBeenCalledTimes\(8\)' src/memory/profiles/__tests__/integration-m010-30days.test.ts | wc -l` ≥ 1
    - File asserts Cycle 1 prompt contains `CURRENT PROFILE STATE` (D-20 / M010-10) — verify via `grep -nE 'CURRENT PROFILE STATE' src/memory/profiles/__tests__/integration-m010-30days.test.ts | wc -l` ≥ 1
    - File imports `seedProfileRows` from Task 2 — verify via `grep -nE "from.*seed-profile-rows" src/memory/profiles/__tests__/integration-m010-30days.test.ts | wc -l` ≥ 1
    - File has skip-when-absent pattern — verify via `grep -nE 'existsSync|FIXTURE_PRESENT' src/memory/profiles/__tests__/integration-m010-30days.test.ts | wc -l` ≥ 1
    - `bash scripts/test.sh src/memory/profiles/__tests__/integration-m010-30days.test.ts` exits 0 (when m010-30days fixture present, both tests pass; when absent, both tests skipped)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/memory/profiles/__tests__/integration-m010-30days.test.ts</automated>
  </verify>
  <done>PTEST-02 populated-case green (4 calls, all confidence>0, all substrate_hash 64-hex, 4 history rows); PTEST-03 three-cycle green (4/4/8 cumulative; 4× skip log on Cycle 2; M010-10 prev-state injection verified); seedProfileRows resets state between tests so cross-pollution from PTEST-04 cannot leak.</done>
</task>

<task type="auto">
  <name>Task 6: Integration test m010-5days — PTEST-04 sparse threshold enforcement</name>
  <files>src/memory/profiles/__tests__/integration-m010-5days.test.ts</files>
  <read_first>
    - src/memory/profiles/__tests__/generators.sparse.test.ts (FULL file — analog; scaffold lines 21-102 + sparse-case assertions 104-156 + before/after diff pattern 158-190)
    - src/memory/profiles/__tests__/integration-m010-30days.test.ts (created in Task 5 — same hoisted-mock block + vi.mock + imports shape; lift the structure but invert expected outcomes)
    - src/__tests__/fixtures/seed-profile-rows.ts (created in Task 2)
    - .planning/phases/36-tests/36-PATTERNS.md §"src/memory/profiles/__tests__/integration-m010-5days.test.ts" (lines 432-492 — full scaffold + 4-assertion shape: no Sonnet call, all outcomes profile_below_threshold, 4× threshold log, profile rows unchanged from seed)
    - .planning/phases/36-tests/36-CONTEXT.md D-21 (test file location — separate from m010-30days), D-22 (assertion shape verbatim)
    - .planning/phases/36-tests/36-RESEARCH.md §"Pitfall P-36-02" (loader doesn't seed profile_* — must call seedProfileRows in beforeEach)
    - src/memory/confidence.ts (MIN_ENTRIES_THRESHOLD = 10 — the sparse fixture must produce per-dim entry counts < 10 to trip this)
  </read_first>
  <action>
    Create `src/memory/profiles/__tests__/integration-m010-5days.test.ts` mirroring `generators.sparse.test.ts` per D-21. Lift the same vi.hoisted + vi.mock + imports block as Task 5's file (mockAnthropicParse + mockLoggerInfo etc.); the dimension-routing mock is NOT primed here because no Sonnet call should occur. Add skip-when-absent gate for `tests/fixtures/primed/m010-5days/MANIFEST.json`. `beforeAll`: `await pgSql\`SELECT 1\`; await loadPrimedFixture('m010-5days');`. `beforeEach`: `await db.delete(profileHistory); await seedProfileRows(); mockAnthropicParse.mockReset(); mockLoggerInfo.mockReset();` (do NOT call primeAllDimensionsValid — if Sonnet IS called, mock will throw because no implementation primed → test failure with clear signal). The single `it()` block: capture `allJBefore, allCBefore, allHBefore, allFBefore` via 4 SELECTs; call `updateAllOperationalProfiles({ now: new Date('2026-05-17T22:00:00Z') })`; assert (1) `expect(mockAnthropicParse).not.toHaveBeenCalled()` (D-14 verbatim); (2) `expect(outcomes.every(o => o.outcome === 'profile_below_threshold')).toBe(true)` (D-22); (3) `mockLoggerInfo.mock.calls.filter(c => c[1] === 'chris.profile.threshold.below_minimum')` has length 4 AND the dimensions extracted from `c[0].dimension` sort to `['capital', 'family', 'health', 'jurisdictional']` (per PATTERNS.md "Logger-spy log-key assertion" pattern); (4) `allJAfter == allJBefore` (and same for capital/health/family) — compare `substrateHash` AND `confidence` byte-equal before/after via the by-id Map pattern from PATTERNS.md lines 480-490. File header JSDoc per TESTING.md.
  </action>
  <acceptance_criteria>
    - File exists with PTEST-04 `it()` block — verify via `grep -nE "it\(['\"].*PTEST-04|.*sparse.*threshold|.*5-day" src/memory/profiles/__tests__/integration-m010-5days.test.ts | wc -l` ≥ 1
    - File asserts `mockAnthropicParse` NOT called — verify via `grep -nE 'not\.toHaveBeenCalled\(\)' src/memory/profiles/__tests__/integration-m010-5days.test.ts | wc -l` ≥ 1
    - File asserts 4× `'chris.profile.threshold.below_minimum'` log — verify via `grep -nE 'chris\.profile\.threshold\.below_minimum' src/memory/profiles/__tests__/integration-m010-5days.test.ts | wc -l` ≥ 1
    - File checks before/after profile-row equality — verify via `grep -nE 'allJBefore|byIdAfter|before/after' src/memory/profiles/__tests__/integration-m010-5days.test.ts | wc -l` ≥ 2
    - File imports `seedProfileRows` (P-36-02 mitigation) — verify via `grep -nE "from.*seed-profile-rows" src/memory/profiles/__tests__/integration-m010-5days.test.ts | wc -l` ≥ 1
    - `bash scripts/test.sh src/memory/profiles/__tests__/integration-m010-5days.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/memory/profiles/__tests__/integration-m010-5days.test.ts</automated>
  </verify>
  <done>Sparse fixture path verified end-to-end: orchestrator runs, ALL 4 dims trip threshold gate (zero Sonnet calls), 4× `below_minimum` log entries emitted (one per dim, alphabetically sorted), profile rows untouched (substrate_hash and confidence byte-equal to seed); cross-test isolation via seedProfileRows ensures PTEST-04 result is independent of PTEST-02/03 run order.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Run full Plan 36-01 test suite + commit boundary</name>
  <what-built>
    All 6 file modifications from Tasks 1-6: `synthesize-delta.ts` extension + unit test, `chat-ids.ts` allocation, `seed-profile-rows.ts` helper, `primed-sanity-m010.test.ts` HARN gate, `integration-m010-30days.test.ts` (PTEST-02 + PTEST-03), `integration-m010-5days.test.ts` (PTEST-04). Fixtures regenerated locally in Task 3 (gitignored — not committed).

    PTEST-01 verified via: (a) unit tests in Task 1, (b) HARN gate in Task 4.
    PTEST-02 + PTEST-03 verified via Task 5.
    PTEST-04 verified via Task 6.
  </what-built>
  <how-to-verify>
    Run the full plan-scope test suite in Docker (canonical command per MEMORY.md):
    ```bash
    bash scripts/test.sh \
      scripts/__tests__/synthesize-delta-profile-bias.test.ts \
      scripts/__tests__/synthesize-delta.test.ts \
      src/__tests__/fixtures/primed-sanity-m010.test.ts \
      src/memory/profiles/__tests__/integration-m010-30days.test.ts \
      src/memory/profiles/__tests__/integration-m010-5days.test.ts
    ```

    Expected: all 5 test files green (estimated runtime: ~3-5 min total in Docker; HARN ~30s, two integration files ~60s each with fixture load).

    **Then run the broader profile-test suite to confirm no regression in Phase 34 tests:**
    ```bash
    bash scripts/test.sh src/memory/profiles/__tests__/ src/__tests__/fixtures/
    ```

    Expected: Phase 34's `generators.two-cycle.test.ts`, `generators.sparse.test.ts`, `refine.test.ts`, `schemas.test.ts`, `shared.test.ts` all still green AND the new Phase 36 tests green.

    **Commit boundary (after green):** Encourage atomic commits per CONTEXT.md `<code_context>` "Conventional commits" list — one per task; final commit ties the plan together. Use `feat(36-01):` / `test(36-01):` prefixes.

    **Investigation triggers:**
    - If HARN m010-30days fails (≥12 per dim NOT met for some dim): per OQ-4 reconfirmation, either tune `PROFILE_BIAS_KEYWORDS` (Task 1), bump `ENTRIES_PER_DAY`, or extend `--target-days`. Re-run Task 3 regeneration. Document the tuning in a CONTEXT.md addendum.
    - If PTEST-03 Cycle 3 produces ≠ 8 calls: per Phase 34 D-14, ALL 4 dim hashes invalidate together on any pensieve mutation. If the test sees 5, the substrate hash is being computed per-dim somewhere — investigate `loadProfileSubstrate` for unexpected dim-filtering.
    - If PTEST-04 sees ANY Sonnet call (`mockAnthropicParse` called): the m010-5days fixture has too many entries for some dim (above 10 threshold). Re-check HARN m010-5days assertion (<10 per dim).
    - If cross-file pollution observed (test passes in isolation, fails when run as suite): `seedProfileRows()` is not being called in `beforeEach`, OR the ON CONFLICT DO UPDATE SET clauses miss a column.
  </how-to-verify>
  <resume-signal>Type "all 5 files green" with the test output summary, OR describe red tests/investigation needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → `scripts/synthesize-delta.ts` argv | Operator supplies `--profile-bias` strings; untrusted input crosses into the script |
| primed fixture jsonl → loadPrimedFixture → Postgres | Fixture content (Haiku-generated) is treated as test-substrate; assumed trusted (operator-generated locally; not from user input) |
| mockAnthropicParse → integration test assertions | Canned profile-shape responses; constants in test file; trusted |
| operator gitignored fixture artifacts → no commit | Pitfall P-36-01 — `.gitignore` line 23-25 prevents accidental commit of generated fixtures (which could contain PII-shaped content); HARD trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | S (Spoofing) | n/a | accept | Plan 36-01 is test infrastructure; no auth/identity surface — not applicable. |
| T-36-02 | T (Tampering) | `scripts/synthesize-delta.ts --profile-bias` argv | mitigate | Whitelist validation in parseCliArgs: only accept the 4 locked Dimension values (`jurisdictional`/`capital`/`health`/`family`); throw `UsageError` on any other value (Task 1 acceptance criterion). ASVS L1 V5.1 (input validation at entry boundary). |
| T-36-03 | R (Repudiation) | n/a | accept | Test-suite scope; no audit-log surface. |
| T-36-04 | I (Information disclosure) | Generated primed fixtures `tests/fixtures/primed/m010-{30days,5days}/` | mitigate | Fixtures gitignored (P-36-01 Option A). Even though m010 fixtures are Haiku-generated and contain no real PII, the gitignore prevents future maintainers from accidentally committing operator-regenerated content with leaked context. |
| T-36-05 | D (DoS) | Two-cycle test infinite loop | accept | The `for d in 0..synthDaysNeeded` loop in synthesize-delta.ts is bounded by `--target-days`; tests use small target-days (5, 30); no DoS surface. |
| T-36-06 | E (Elevation of privilege) | n/a | accept | No elevation surface in test code. |
| T-36-07 | V11 (Business logic) | PTEST-03 Cycle 3 cumulative call count assertion (the documented "8 not 5" trade-off) | mitigate | Inline test-file comment + assertion-message text references Phase 34 D-14 (single shared substrate by design) so a future drift to per-dim substrate filtering would surface as a planned change, not an undetected regression. PATTERNS.md "Note on cycle 3 call count discrepancy" lines 428 documents this; the test file MUST repeat the rationale inline. |
| T-36-08 | V11 (Business logic) | PTEST-04 "no Sonnet call" assertion (cost-discipline contract) | mitigate | `expect(mockAnthropicParse).not.toHaveBeenCalled()` is the cost-floor contract — if the orchestrator skips threshold gating, this fires. Hard fail (no skip-when-flaky retry). |
| T-36-09 | V5 (Input validation) | Pitfall P-36-04 keyword-grep false positives in HARN gate | accept | v1: accept double-counting (entries matching multiple dims count toward each). Documented in test file header. Deferred to v2.5.1 if production profile fidelity needs precise dim classification. Trade-off explicitly noted. |
</threat_model>

<verification>
## Plan-Level Verification

- All 6 new/modified files exist with the artifacts listed in `must_haves.artifacts`
- `bash scripts/test.sh scripts/__tests__/synthesize-delta-profile-bias.test.ts scripts/__tests__/synthesize-delta.test.ts src/__tests__/fixtures/primed-sanity-m010.test.ts src/memory/profiles/__tests__/integration-m010-30days.test.ts src/memory/profiles/__tests__/integration-m010-5days.test.ts` exits 0
- `bash scripts/test.sh src/memory/profiles/__tests__/ src/__tests__/fixtures/` exits 0 (no regression in Phase 34 tests)
- Plan-scope grep gates (filtered to skip comment-only matches):
  - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -cE "^export const PROFILE_BIAS_KEYWORDS|^export const PROFILE_BIAS_ROTATION"` ≥ 2
  - `grep -vE '^\s*//|^\s*\*' src/__tests__/fixtures/chat-ids.ts | grep -c "CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922)"` = 1
  - `grep -vE '^\s*//|^\s*\*' src/__tests__/fixtures/seed-profile-rows.ts | grep -c "ON CONFLICT.*DO UPDATE SET"` ≥ 4
- Pitfall P-36-01 mitigation present in 4 test files (each has `existsSync`-based skip-when-absent gate):
  - `grep -lE 'existsSync|FIXTURE_PRESENT' src/__tests__/fixtures/primed-sanity-m010.test.ts src/memory/profiles/__tests__/integration-m010-{30days,5days}.test.ts | wc -l` = 3 (the unit test for `--profile-bias` doesn't consume the fixture so it doesn't need this gate; Plan 36-02's live test does need it)
- Pitfall P-36-02 mitigation present in 3 fixture-consuming test files (each imports `seedProfileRows`):
  - `grep -lE "from.*seed-profile-rows" src/memory/profiles/__tests__/integration-m010-{30days,5days}.test.ts src/__tests__/fixtures/primed-sanity-m010.test.ts | wc -l` ≥ 2 (HARN sanity test may not need seed reset since it only reads pensieve_entries; integration tests require it)
</verification>

<success_criteria>
- ROADMAP Phase 36 success #1: `--profile-bias` flag exists; m010-30days HARN passes ≥12 per dim ✓ (Tasks 1, 3, 4)
- ROADMAP success #2: Real-DB integration test loads m010-30days, runs orchestrator, asserts confidence>0, last_updated advances, substrate_hash non-null, 4 history rows ✓ (Task 5 PTEST-02)
- ROADMAP success #3: Two-cycle (3-cycle actually) test: Cycle 1 = 4 calls; Cycle 2 identical → STILL 4 cumulative; Cycle 3 mutated → 8 cumulative; profile_history rows track correctly ✓ (Task 5 PTEST-03)
- ROADMAP success #4: Sparse fixture test: all 4 profiles return insufficient-data markers + confidence=0 + skip log ✓ (Task 6 PTEST-04)
- HARD CO-LOC #M10-6 honored: flag + fixtures + HARN + 4 integration tests all in this single plan
- Both Pitfalls (P-36-01, P-36-02) mitigated by design (skip-when-absent + seedProfileRows)
- Cross-test pollution impossible (seedProfileRows in every beforeEach)
- Phase 34 test suite still green (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/36-tests/36-01-SUMMARY.md` covering:
- What was built (5 new files + 2 modified)
- Verification results (which `bash scripts/test.sh ...` runs were green, with counts)
- Fixture-regeneration outcome from Task 3 (cost spent, per-dim entry counts on m010-30days for OQ-4 reconfirmation)
- Whether OQ-4 had to be retuned (default 4-way rotation OR adjusted keywords/days)
- Pitfall mitigations confirmed (P-36-01 skip-when-absent triggered in CI; P-36-02 cross-test isolation verified)
- Patterns established for Plan 36-02 to consume (seedProfileRows reuse, primed-fixture commit policy)
</output>
