---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/profile-prompt.ts
  - src/memory/__tests__/profile-prompt.test.ts
autonomous: true
requirements: [GEN-04, GEN-05]
requirements_addressed: [GEN-04, GEN-05]
must_haves:
  truths:
    - "assembleProfilePrompt(dimension, substrate, prevState, entryCount) is a pure function (zero DB, LLM, fs, env access) — mirrors M009 assembleWeeklyReviewPrompt (D-03)"
    - "Output for all 4 dimensions is { system: string, user: string }; system.startsWith('## Core Principles (Always Active)') — CONSTITUTIONAL_PREAMBLE first section (D-04, M010-06 anchor)"
    - "DO_NOT_INFER_DIRECTIVE constant exported + appears verbatim in every assembled system string for all 4 dimensions (D-05, M010-02 mitigation)"
    - "Volume-weight ceiling phrasing references 'data_consistency' (not 'confidence') and the substrings 'DO NOT emit a `confidence` field', 'fewer than 20 entries', 'MUST NOT exceed 0.5' (D-06, M010-01 mitigation)"
    - "When prevState is null → system does NOT contain '## CURRENT PROFILE STATE'; when prevState is non-null → system DOES contain '## CURRENT PROFILE STATE' rendering prior jsonb fields verbatim (D-07, M010-03 mitigation)"
    - "user prompt = 'Generate the operational profile for ${dimension}.' (string template, dimension name lowercased) (D-03)"
    - "Pure function: same inputs → same outputs across repeated calls (deterministic) — structural test covers this"
  artifacts:
    - path: "src/memory/profile-prompt.ts"
      provides: "assembleProfilePrompt + DO_NOT_INFER_DIRECTIVE + ProfilePromptDimension + AssembledProfilePrompt + section helpers (private to file)"
      min_lines: 120
      contains: "export function assembleProfilePrompt"
    - path: "src/memory/__tests__/profile-prompt.test.ts"
      provides: "Structural unit tests parametrized over all 4 dimensions — CONSTITUTIONAL_PREAMBLE first, DO_NOT_INFER, volume-weight ceiling, prevState conditional"
      min_lines: 80
      contains: "describe('assembleProfilePrompt"
  key_links:
    - from: "src/memory/profile-prompt.ts"
      to: "src/chris/personality.ts"
      via: "imports CONSTITUTIONAL_PREAMBLE re-used as first section of system string"
      pattern: "import.*CONSTITUTIONAL_PREAMBLE.*from.*chris/personality"
    - from: "src/memory/profile-prompt.ts"
      to: "src/memory/profiles/shared.ts"
      via: "type-only import of ProfileSubstrate (Plan 34-02 forward-declares; this plan imports the TYPE only — Wave 1 must compile against forward-declared type or local stub)"
      pattern: "import type.*ProfileSubstrate"
    - from: "src/memory/__tests__/profile-prompt.test.ts"
      to: "src/memory/profile-prompt.ts"
      via: "imports assembleProfilePrompt + DO_NOT_INFER_DIRECTIVE for structural assertions"
      pattern: "from '../profile-prompt"
---

<objective>
Ship the shared prompt builder `assembleProfilePrompt(dimension, substrate, prevState, entryCount)` + its structural test before any per-dimension generator exists. This plan is the HARD CO-LOC #M10-2 anchor: the gsd-plan-checker MUST refuse Plan 34-02 if this plan is incomplete. The shared builder prevents per-dimension prompt drift (M010-06 mitigation) by being THE ONLY way the four generators construct their Sonnet system text.

Purpose: GEN-04 (`assembleProfilePrompt` shared builder consumed by all four generators) + the prompt-side dependency surface for GEN-05 (the prompt phrasing for the hybrid confidence model — Sonnet emits `data_consistency`, host computes final `confidence`; this plan locks the exact Sonnet-facing text).

Output: 1 new source file (`src/memory/profile-prompt.ts`) + 1 new test file (`src/memory/__tests__/profile-prompt.test.ts`). Zero schema edits, zero migration edits, zero changes outside the prompt module.

Satisfies: GEN-04 (`assembleProfilePrompt` shared builder), GEN-05 (consumption surface — the volume-weight ceiling phrasing that lets the host's `computeProfileConfidence` operate against a non-inflated Sonnet output).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-inference-engine/34-CONTEXT.md
@.planning/phases/34-inference-engine/34-RESEARCH.md
@.planning/phases/34-inference-engine/34-PATTERNS.md
@.planning/phases/34-inference-engine/34-VALIDATION.md
@.planning/phases/33-profile-substrate/33-SUMMARY.md

# Codebase analogs (Pattern Catalog in 34-RESEARCH.md §Pattern 1 + 34-PATTERNS.md `src/memory/profile-prompt.ts` section)
@src/rituals/weekly-review-prompt.ts
@src/rituals/__tests__/weekly-review-prompt.test.ts
@src/chris/personality.ts
@src/memory/profiles/schemas.ts

<interfaces>
<!-- Contracts the executor consumes. All come from existing codebase OR are
     declared as part of this plan's deliverables. No codebase exploration needed. -->

From src/chris/personality.ts:30-40 (pre-existing):
```typescript
export const CONSTITUTIONAL_PREAMBLE: string;
// First line is verbatim '## Core Principles (Always Active)'
// Constant ends with trailing '\n\n' — callers use .trimEnd()
```

From src/memory/profiles/schemas.ts (created by Phase 33; lines 23-241 — VERIFIED):
```typescript
// Per-dimension v3 + v4 schema pairs. NOT imported by this plan's source file
// (the prompt builder doesn't render schemas; Plan 34-02 generators do).
// Listed here only so the executor knows the dimension keys are case-sensitive
// 'jurisdictional' | 'capital' | 'health' | 'family' (lowercase).
```

ProfileSubstrate TYPE FORWARD DECLARATION (this plan):
```typescript
// Plan 34-01 introduces ProfilePromptDimension + AssembledProfilePrompt; the
// ProfileSubstrate TYPE itself ships in Plan 34-02's src/memory/profiles/shared.ts.
// To avoid a Wave 1 forward-reference compile error, this plan imports the type
// only AFTER Plan 34-02 ships it, OR declares a minimal local structural type
// in profile-prompt.ts that Plan 34-02's exported type extends. Decision (D-03
// + executor discretion): declare the structural type INSIDE profile-prompt.ts
// (or in a co-located types.ts) so the prompt builder compiles standalone:
//
//   export type ProfileSubstrateView = {
//     pensieveEntries: { id: string; epistemicTag: string; content: string; createdAt: Date }[];
//     episodicSummaries: { summaryDate: string; summary: string }[];
//     decisions: { id: string; resolvedAt: Date; question: string; resolution: string }[];
//     entryCount: number;
//   };
//
// Plan 34-02's full ProfileSubstrate type is assignable to ProfileSubstrateView
// (structural compatibility — fields can be wider). No back-port needed.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Draft DO_NOT_INFER_DIRECTIVE + volume-weight ceiling phrasing + dimension-specific directives + write structural test (RED)</name>
  <read_first>
    - src/rituals/weekly-review-prompt.ts (full file — Pattern 1 in 34-RESEARCH.md §lines 196-238; the section-list-then-join + CONSTITUTIONAL_PREAMBLE.trimEnd() idiom)
    - src/rituals/__tests__/weekly-review-prompt.test.ts (lines 1-190 — the buildFixture helper + first-section anchor assertion + conditional-block test pattern that 34-PATTERNS.md §`src/memory/__tests__/profile-prompt.test.ts` (lines 375-424) instructs to mirror)
    - src/chris/personality.ts (lines 30-40 — CONSTITUTIONAL_PREAMBLE export; verify the first line is verbatim '## Core Principles (Always Active)' and the constant ends with '\n\n' so .trimEnd() is load-bearing)
    - .planning/phases/34-inference-engine/34-RESEARCH.md §OQ-2 Resolution (lines 850-895 — VERBATIM volume-weight ceiling phrasing that this task drafts into the source file)
    - .planning/phases/34-inference-engine/34-CONTEXT.md §D-05 + §D-06 + §D-07 (locked phrasing decisions)
  </read_first>
  <files>src/memory/__tests__/profile-prompt.test.ts (NEW FILE)</files>
  <behavior>
    - Test file is created BEFORE src/memory/profile-prompt.ts exists (RED phase — TDD per project convention; matches Phase 33 Plan 33-02 Task 1 pattern)
    - Parametrized over all 4 dimensions: 'jurisdictional' | 'capital' | 'health' | 'family'
    - Each `it()` asserts a specific substring presence/absence on the assembled output
    - Pure-function tests — no mocks, no DB, no async
    - Tests RED initially (profile-prompt.ts doesn't exist)
  </behavior>
  <action>
    Create `src/memory/__tests__/profile-prompt.test.ts`. The test file MUST:

    1. Define a `buildFixture(overrides?)` helper mirroring `src/rituals/__tests__/weekly-review-prompt.test.ts:33-90` shape, returning a complete `ProfileSubstrateView` (the structural type defined in Task 2 — at test-write time this type may not yet exist; the executor's TypeScript compile will go RED, which is the correct TDD signal).

    2. Define a `DIMENSIONS = ['jurisdictional', 'capital', 'health', 'family'] as const` array and a `describe.each(DIMENSIONS)` (or `it.each`) parametrization.

    3. For EACH of the 4 dimensions, assert these structural truths (per D-35):
       - `result.system.startsWith('## Core Principles (Always Active)')` — verifies CONSTITUTIONAL_PREAMBLE is the first section verbatim (D-04, M010-06)
       - `result.system.includes(DO_NOT_INFER_DIRECTIVE)` — verifies the constant appears verbatim (D-05)
       - `result.system.includes('data_consistency')` — volume-weight ceiling references the field name (D-06)
       - `result.system.includes('DO NOT emit a \`confidence\` field')` — host-computes rule explicit
       - `result.system.includes('fewer than 20 entries')` — volume-weight band phrasing
       - `result.system.includes('MUST NOT exceed 0.5')` — volume-weight ceiling value
       - `result.user === \`Generate the operational profile for ${dimension}.\`` — user prompt is exact template
       - Import CONSTITUTIONAL_PREAMBLE from '../../chris/personality.js' and double-assert `CONSTITUTIONAL_PREAMBLE.startsWith('## Core Principles (Always Active)')` (sentinel — protects against personality.ts drift)

    4. Conditional `## CURRENT PROFILE STATE` block (per D-07):
       - When `assembleProfilePrompt(dim, substrate, null, entryCount)` is called → `result.system` does NOT contain '## CURRENT PROFILE STATE'
       - When `assembleProfilePrompt(dim, substrate, { current_country: 'GE' }, entryCount)` (or analog jsonb shape) → `result.system` DOES contain '## CURRENT PROFILE STATE' AND contains the jsonb field value rendered into the block

    5. Refine-survives-zod-cast smoke assertion (RESEARCH.md residual risk lines 938-941 — soft signal in this file; harder assertion lives in Plan 34-02's refine.test.ts):
       - Assert that the assembled `system` for a `entryCount=15` fixture contains the exact substring `'When the substrate has fewer than 20 entries, your `data_consistency` value\n    MUST NOT exceed 0.5.'` (or equivalent stable substring from the locked OQ-2 phrasing in 34-RESEARCH.md lines 854-882). This catches Sonnet-side text-prompt drift even if the v4 .refine() is silently dropped.

    Use `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` to confirm the test RUNS (it should error on `Cannot find module '../profile-prompt'` — that is RED). Commit only after this RED state is confirmed.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/memory/__tests__/profile-prompt.test.ts` (~80+ lines)
    - `grep -c "describe\|it(" src/memory/__tests__/profile-prompt.test.ts` returns >= 6 (at minimum: CONSTITUTIONAL_PREAMBLE first, DO_NOT_INFER present, volume-weight refs `data_consistency`, prevState=null absence, prevState!=null presence, user prompt template)
    - `grep -c "jurisdictional\|capital\|health\|family" src/memory/__tests__/profile-prompt.test.ts | awk '$1>=4 {print "OK"}'` prints OK (all 4 dimensions named explicitly)
    - `grep -E "fewer than 20 entries|MUST NOT exceed 0\\.5|DO NOT emit a .confidence. field" src/memory/__tests__/profile-prompt.test.ts | wc -l` returns >= 3 (verbatim OQ-2 phrasing assertions present)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts 2>&1 | grep -E "Cannot find module|error TS2307"` matches (test is RED — profile-prompt.ts does not yet exist; the test correctly fails to import)
  </acceptance_criteria>
  <done>
    Test file committed with all structural assertions in place. Test runs but fails RED with module-not-found (correct TDD state). Commit message: `test(34-01): RED — structural tests for assembleProfilePrompt`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/memory/profile-prompt.ts (GREEN — assembleProfilePrompt + DO_NOT_INFER_DIRECTIVE + section helpers)</name>
  <read_first>
    - src/rituals/weekly-review-prompt.ts (lines 144-238 — the assembleWeeklyReviewPrompt section-list-then-join template that this file mirrors)
    - src/rituals/weekly-review-prompt.ts (lines 40-150 — the per-section build helpers: `buildRolePreamble`, `buildDateWindowBlock`, etc. — pattern for how each section is composed)
    - .planning/phases/34-inference-engine/34-RESEARCH.md §Pattern 1 (lines 196-298 — Phase 34 mirror code shown verbatim)
    - .planning/phases/34-inference-engine/34-RESEARCH.md §OQ-2 Resolution (lines 850-895 — DRAFT phrasing for volume-weight ceiling that ships verbatim into this file)
    - .planning/phases/34-inference-engine/34-PATTERNS.md §src/memory/profile-prompt.ts (lines 40-114 — keep-vs-adapt directive)
    - src/memory/__tests__/profile-prompt.test.ts (the test file from Task 1 — implementation must satisfy these assertions exactly)
  </read_first>
  <files>src/memory/profile-prompt.ts (NEW FILE)</files>
  <action>
    Create `src/memory/profile-prompt.ts`. The file MUST export:

    1. **TYPES**
       - `export type ProfilePromptDimension = 'jurisdictional' | 'capital' | 'health' | 'family';` (D-03, lowercase verbatim)
       - `export type AssembledProfilePrompt = { system: string; user: string };` (D-03 return type)
       - `export type ProfileSubstrateView` — structural type for the substrate slice this builder reads (see <interfaces> block; Plan 34-02's full ProfileSubstrate is assignable to it)

    2. **CONSTANTS**
       - `export const DO_NOT_INFER_DIRECTIVE: string` — verbatim block following D-05. Phrasing MUST include: "Do not infer facts from related-but-distinct entries", "Leave fields empty or marked 'insufficient data' when substrate does not contain the explicit fact", "Derivation from category similarity is NOT acceptable". Tone matches CONSTITUTIONAL_PREAMBLE — declarative, second-person, no hedging. Length ~200-400 chars (one substantive paragraph).
       - Internal const `DIMENSION_DIRECTIVES: Record<ProfilePromptDimension, string>` — one paragraph per dimension (Claude's Discretion per CONTEXT.md). Each tells Sonnet what to focus on (e.g., jurisdictional: country/residency/tax-status/visa-status; capital: liquid net worth/income streams/recurring obligations; health: physical/medical/sleep/exercise; family: relationships/dependents/family communication). Each directive MUST include the verbatim substring `"focus on"` (test-assertable anchor).

    3. **EXPORTED FUNCTION** — `export function assembleProfilePrompt(dimension: ProfilePromptDimension, substrate: ProfileSubstrateView, prevState: unknown | null, entryCount: number): AssembledProfilePrompt`

       Section ordering (D-04 → D-07; mirrors Pattern 1 verbatim):
       ```
       1. CONSTITUTIONAL_PREAMBLE.trimEnd()           — first section (D-04 anchor)
       2. buildRolePreamble(dimension)                — local helper; role framing for operational profile inference
       3. DO_NOT_INFER_DIRECTIVE                      — D-05
       4. buildVolumeWeightCeilingDirective(entryCount) — D-06 + OQ-2 verbatim phrasing
       5. buildPreviousStateBlock(prevState, dimension) — D-07 conditional (only when prevState !== null)
       6. DIMENSION_DIRECTIVES[dimension]             — Claude's Discretion
       7. buildSubstrateBlock(substrate)              — renders pensieveEntries/episodicSummaries/decisions as text blocks
       8. buildStructuredOutputDirective(dimension)   — names the v4 schema name + reminds Sonnet of zodOutputFormat shape
       ```

       Return: `{ system: sections.join('\n\n'), user: \`Generate the operational profile for ${dimension}.\` }` (D-03 user prompt verbatim).

    4. **PRIVATE HELPERS** (file-scoped, NOT exported):
       - `buildRolePreamble(dimension)` — one paragraph, names Greg as the subject, frames the inference as fact-extraction (not interpretation)
       - `buildVolumeWeightCeilingDirective(entryCount: number): string` — emits the locked OQ-2 phrasing from 34-RESEARCH.md lines 854-882 VERBATIM. Substrings that MUST be present: `'data_consistency'`, `'DO NOT emit a `confidence` field'`, `'fewer than 20 entries'`, `'MUST NOT exceed 0.5'`, `'20–49 entries'`, `'50+ entries'`
       - `buildPreviousStateBlock(prevState: unknown, dimension): string` — header line `'## CURRENT PROFILE STATE'`, then a JSON-stringified prevState (pretty-printed with 2-space indent), then a directive: "Update high-confidence fields ONLY when 3+ supporting substrate entries justify the change" (D-07 verbatim per CONTEXT.md)
       - `buildSubstrateBlock(substrate: ProfileSubstrateView): string` — header `'## SUBSTRATE'`, sub-headers `'### Pensieve entries (...)'`, `'### Episodic summaries (...)'`, `'### Resolved decisions (...)'`; each entry rendered on its own line with date + tag + truncated content (~200 chars)
       - `buildStructuredOutputDirective(dimension): string` — instructs Sonnet to emit the v4 schema shape; references the schema by name (`'JurisdictionalProfileSchemaV4'` etc.)

    5. **IMPORTS** (top of file):
       ```typescript
       import { CONSTITUTIONAL_PREAMBLE } from '../chris/personality.js';
       ```
       (no other imports beyond Node built-ins; the type-only ProfileSubstrateView is declared in this file per the <interfaces> block above)

    Run `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` after writing. All assertions from Task 1 must pass GREEN. If any assertion fails, FIX the source file — do NOT alter the test.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/memory/profile-prompt.ts` (~120+ lines)
    - `grep -c "export" src/memory/profile-prompt.ts` returns >= 4 (assembleProfilePrompt + DO_NOT_INFER_DIRECTIVE + ProfilePromptDimension + AssembledProfilePrompt + ProfileSubstrateView)
    - `grep -E "from '\\.\\./chris/personality" src/memory/profile-prompt.ts` matches (CONSTITUTIONAL_PREAMBLE import present)
    - `grep -E "CONSTITUTIONAL_PREAMBLE\\.trimEnd\\(\\)" src/memory/profile-prompt.ts` matches (first-section pattern from Pattern 1 honored)
    - `grep -E "Generate the operational profile for \\$\\{dimension\\}" src/memory/profile-prompt.ts` matches (user-prompt template verbatim per D-03)
    - `grep -c "fewer than 20 entries" src/memory/profile-prompt.ts | awk '$1>=1 {print "OK"}'` prints OK (volume-weight phrasing locked from OQ-2)
    - `grep -c "MUST NOT exceed 0\\.5" src/memory/profile-prompt.ts | awk '$1>=1 {print "OK"}'` prints OK
    - `grep -c "data_consistency" src/memory/profile-prompt.ts | awk '$1>=2 {print "OK"}'` prints OK (referenced in volume-weight + structured-output sections)
    - `grep -c "## CURRENT PROFILE STATE" src/memory/profile-prompt.ts | awk '$1>=1 {print "OK"}'` prints OK (prevState block header)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0 (all GREEN)
  </acceptance_criteria>
  <done>
    Test passes GREEN. All 4 dimensions produce structurally-conforming output. CONSTITUTIONAL_PREAMBLE is first section; DO_NOT_INFER_DIRECTIVE + volume-weight ceiling + prevState-conditional all asserted. Commit message: `feat(34-01): GREEN — assembleProfilePrompt shared builder + DO_NOT_INFER directive`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Plan-level Docker test gate — full suite green confirms no module-resolution / type regression</name>
  <read_first>
    - scripts/test.sh (lines 1-50 — verify env-load + Docker postgres bring-up flow per Phase 33 Plan 33-01 Task 5 SUMMARY)
    - .planning/phases/34-inference-engine/34-VALIDATION.md (Sampling Rate section — per-wave gate calls `bash scripts/test.sh src/memory/`)
  </read_first>
  <files>(no files written — verification gate only)</files>
  <action>
    Run `bash scripts/test.sh src/memory/` AND `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` to verify (a) the new file integrates with the existing src/memory test surface and (b) does not regress any Phase 33 tests (confidence.test.ts, profiles.test.ts, schemas.test.ts).

    Optionally also run `bash scripts/test.sh` (full Docker suite) per CLAUDE.md memory `feedback_always_run_docker_tests.md` ("Always run full Docker tests; never skip integration tests"). The 1412 passed / 53 skipped baseline from Phase 33 must hold (or improve if a flaky test stabilizes; report any new failures).

    On any failure, do NOT proceed to Plan 34-02 — the HARD CO-LOC #M10-2 contract is "shared builder ships BEFORE generators"; this gate is the ship check.
  </action>
  <verify>
    <automated>bash scripts/test.sh src/memory/</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh src/memory/` exits 0 (all src/memory/ tests pass; new profile-prompt.test.ts integrated cleanly)
    - `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0 (the new test file passes in isolation)
    - No Phase 33 test regression: confidence.test.ts + profiles.test.ts + schemas.test.ts still GREEN
    - `git status` shows only the 2 new files (`src/memory/profile-prompt.ts`, `src/memory/__tests__/profile-prompt.test.ts`) staged or committed — no other modifications
  </acceptance_criteria>
  <done>
    Both test invocations exit 0. Plan 34-01 is ready to commit and unlocks Plan 34-02 (HARD CO-LOC #M10-2 satisfied). Commit message: `chore(34-01): full src/memory test gate green — unlocks Plan 34-02`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → assembleProfilePrompt | Plan 34-02 generators call this with substrate + prevState; substrate is internal data (DB reads from Greg's Pensieve), prevState is internal data (prior profile row jsonb). Both originate inside the trust boundary (single-user app D009); no external untrusted input crosses this surface. |
| prompt builder output → Anthropic SDK | The assembled `system` string is sent to api.anthropic.com as part of `messages.parse({system: [...]})`. Content includes Greg's private Pensieve text. This is the same exfiltration surface accepted in M008 (episodic consolidate) + M009 (weekly review) — Phase 34 adds no NEW exfiltration; reuses the existing accepted boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Information Disclosure | assembleProfilePrompt → Sonnet API | accept | Single-user app (D009); private Pensieve text already sent to Anthropic via M008/M009 cron handlers using the identical SDK surface. No new exfiltration path introduced by this plan. |
| T-34-01-02 | Tampering | Volume-weight ceiling phrasing dropped (e.g., editor accidentally deletes OQ-2 paragraph) | mitigate | Structural test in Task 1 asserts verbatim substrings `'fewer than 20 entries'`, `'MUST NOT exceed 0.5'`, `'DO NOT emit a `confidence` field'`. Drift detected at CI before deploy (M010-01 confidence inflation mitigation). |
| T-34-01-03 | Tampering | DO_NOT_INFER_DIRECTIVE removed from one dimension's output (e.g., bug in dimension-specific branch) | mitigate | Test parametrizes over all 4 dimensions and asserts `system.includes(DO_NOT_INFER_DIRECTIVE)` per dimension. Branch-coverage gap detected at CI (M010-02 anti-hallucination mitigation). |
| T-34-01-04 | Repudiation | Prompt drift between dimensions (M010-06 — per-dimension prompt divergence over time) | mitigate | Shared builder = single source for all 4 dimensions (HARD CO-LOC #M10-2). Structural test asserts CONSTITUTIONAL_PREAMBLE first for ALL 4 dimensions. Plan-checker refuses Plan 34-02 if 34-01 incomplete. |
| T-34-01-05 | Information Disclosure | Previous-state block leaks prior PII into prompt (e.g., names, addresses from prior fire) | accept | Phase 33's profile rows contain only Greg's own structured operational facts; prevState is Greg's data sent back to Anthropic about Greg. No third-party PII surface. Same trust class as substrate itself. |
</threat_model>

<verification>
**Phase-level checks at end of Plan 34-01:**

1. `bash scripts/test.sh src/memory/__tests__/profile-prompt.test.ts` exits 0
2. `bash scripts/test.sh src/memory/` exits 0 (no regression of Phase 33 tests)
3. `grep -rE "from.*profile-prompt" src/` shows 0 importers OUTSIDE the test file (no Plan 34-02 generators yet — strictly Wave 1 boundary)
4. `git log --oneline | head -3` shows the test-RED commit BEFORE the implementation-GREEN commit (TDD discipline observed; Phase 33 Plan 33-02 Task 1 precedent)
</verification>

<success_criteria>
- `src/memory/profile-prompt.ts` exports `assembleProfilePrompt`, `DO_NOT_INFER_DIRECTIVE`, `ProfilePromptDimension`, `AssembledProfilePrompt`, `ProfileSubstrateView`
- All 5 structural test groups GREEN: CONSTITUTIONAL_PREAMBLE first, DO_NOT_INFER present, volume-weight ceiling phrasing exact, prevState conditional, user-prompt template
- Parametrization covers all 4 dimensions (jurisdictional/capital/health/family) — verified by `it.each` or `describe.each` block presence
- Pure function: zero imports of DB/LLM/fs/env modules in `src/memory/profile-prompt.ts`
- HARD CO-LOC #M10-2 satisfied: this plan completes BEFORE Plan 34-02 starts (plan-checker enforces)
</success_criteria>

<output>
After completion, create `.planning/phases/34-inference-engine/34-01-SUMMARY.md` recording:
- Exact bytes/lines of `src/memory/profile-prompt.ts` final shape
- Verbatim DO_NOT_INFER_DIRECTIVE text (paste the constant for future-Greg reference)
- Volume-weight ceiling phrasing as shipped (paste the final OQ-2 text; cross-link to 34-RESEARCH.md §OQ-2 in case future-Greg wants to compare drift)
- Per-dimension `DIMENSION_DIRECTIVES` text (4 paragraphs)
- Test count + GREEN confirmation
- Any deviations from the plan (executor's discretion observations)
</output>