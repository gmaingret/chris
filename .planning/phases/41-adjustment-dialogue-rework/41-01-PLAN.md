---
phase: 41-adjustment-dialogue-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rituals/display-names.ts
  - src/rituals/adjustment-dialogue.ts
  - src/rituals/skip-tracking.ts
autonomous: true
requirements:
  - ADJ-01
  - ADJ-02
  - ADJ-04
user_setup: []
tags:
  - rituals
  - adjustment-dialogue
  - p0-live-bug

must_haves:
  truths:
    - "Greg never sees the string 'isn't working' in an adjustment-dialogue Telegram message"
    - "Greg never sees 'daily daily_journal' or any other slug-with-cadence-prefix in adjustment-dialogue Telegram copy"
    - "Greg never sees raw config-field slugs (fire_at, fire_dow, skip_threshold) in the confirmation echo"
    - "After Greg replies yes to a config-patch confirmation, the next runRitualSweep tick does NOT re-fire shouldFireAdjustmentDialogue for that ritual"
    - "After Greg replies no to a config-patch confirmation, the next runRitualSweep tick does NOT re-fire shouldFireAdjustmentDialogue"
    - "After Greg replies 'drop it' or 'disable' (hard-disable refusal), skip_count is 0 in the rituals row (so if he manually re-enables, no immediate re-fire)"
    - "After Greg replies 'not now' (deferral refusal), skip_count is 0 in the rituals row (so when the 7-day mute expires, no immediate re-fire)"
    - "When the 30-day auto-paused mute expires (autoReEnableExpiredMutes path), skip_count is reset to 0 in the same UPDATE that flips enabled=true"
  artifacts:
    - path: "src/rituals/display-names.ts"
      provides: "RITUAL_DISPLAY_NAMES + CONFIG_FIELD_LABELS constant maps + displayName() + configFieldLabel() helpers"
      contains: "export const RITUAL_DISPLAY_NAMES"
    - path: "src/rituals/adjustment-dialogue.ts"
      provides: "Observational fire-side prompt + display-name substitution at lines 285/308/471/733 + skip_count reset at 4 completion sites + ritual_fire_events RESPONDED emissions paired with each reset"
      contains: "skipCount: 0"
    - path: "src/rituals/skip-tracking.ts"
      provides: "autoReEnableExpiredMutes resets skipCount=0 in the same UPDATE that flips enabled=true; emits paired RESPONDED ritual_fire_events row"
      contains: "skipCount: 0"
  key_links:
    - from: "src/rituals/adjustment-dialogue.ts"
      to: "src/rituals/display-names.ts"
      via: "import displayName, configFieldLabel"
      pattern: "from './display-names.js'"
    - from: "src/rituals/adjustment-dialogue.ts handleConfirmationReply (yes branch)"
      to: "rituals.skip_count + ritual_fire_events"
      via: "db.transaction wrapping UPDATE rituals SET skipCount=0 + INSERT ritual_fire_events RESPONDED"
      pattern: "skipCount: 0.*RITUAL_OUTCOME\\.RESPONDED|RITUAL_OUTCOME\\.RESPONDED.*skipCount: 0"
    - from: "src/rituals/adjustment-dialogue.ts handleConfirmationReply (no branch)"
      to: "rituals.skip_count + ritual_fire_events"
      via: "db.transaction wrapping UPDATE rituals SET skipCount=0 + INSERT ritual_fire_events RESPONDED"
      pattern: "user_no"
    - from: "src/rituals/adjustment-dialogue.ts routeRefusal (both branches)"
      to: "rituals.skip_count"
      via: "skipCount: 0 set in the same UPDATE that flips enabled OR config.adjustment_mute_until"
      pattern: "skipCount: 0"
    - from: "src/rituals/skip-tracking.ts autoReEnableExpiredMutes"
      to: "rituals.skip_count + ritual_fire_events"
      via: "skipCount: 0 added to existing SET clause; ritual_fire_events RESPONDED auto_re_enable emission"
      pattern: "skipCount: 0"
---

<objective>
Ship the live UX defect fix to Proxmox without waiting for FR/RU exemplar review. Three requirements, three files touched, four observable behaviors changed.

**Purpose:** Plan 41-01 is the P0 zero-lag fix for the bug actively re-firing on Greg's account at 2026-05-14 17:00 Paris. It closes (a) the wrongful "isn't working" assertion (ADJ-01), (b) the `daily daily_journal` slug-with-cadence-prefix leak (ADJ-02), and (c) the every-tick re-fire after threshold (ADJ-04 — the root cause of the cascade). Once 41-01 lands on Proxmox, Plan 41-02 layers FR/RU + security + test gate on top.

**Output:**
- `src/rituals/display-names.ts` (NEW) — ritual + config-field display-name maps (locale-aware shape, but Plan 41-01 only reads the `English` slot; Plan 41-02 wires FR/RU consumption)
- `src/rituals/adjustment-dialogue.ts` (MODIFIED) — observational copy + display-name substitution at the 4 sites + skip_count reset at 3 completion sites
- `src/rituals/skip-tracking.ts` (MODIFIED) — `autoReEnableExpiredMutes` resets skipCount=0 in the same UPDATE (BL-10 / ADJ-04 site #4)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md
@.planning/phases/41-adjustment-dialogue-rework/41-PATTERNS.md
@.planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md
@.planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md
@CLAUDE.md

# Source-of-truth files the executor must read before touching:
@src/rituals/adjustment-dialogue.ts
@src/rituals/skip-tracking.ts
@src/chris/refusal.ts
@src/chris/language.ts
@src/rituals/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Do NOT re-explore. -->

From src/chris/language.ts (already exported, consumed unchanged):
```typescript
export type Lang = 'English' | 'French' | 'Russian';
export function detectLanguage(text: string, previousLanguage: string | null): string;
export function getLastUserLanguage(chatId: string): string | null;
export async function getLastUserLanguageFromDb(chatId: bigint): Promise<string | null>;
export function setLastUserLanguage(chatId: string, language: string): void;
export function langOf(raw: string | null): Lang;
```

From src/rituals/types.ts (already imported in adjustment-dialogue.ts:45):
```typescript
export const RITUAL_OUTCOME = {
  RESPONDED: 'responded',
  IN_DIALOGUE: 'in_dialogue',
  // ... others
} as const;
export function parseRitualConfig(input: unknown): RitualConfig;  // throws ZodError on type mismatch
```

From src/chris/refusal.ts:180-193 (the exemplar):
```typescript
const ACKNOWLEDGMENTS: Record<string, string[]> = { English: [...], French: [...], Russian: [...] };
export function generateRefusalAcknowledgment(language: string): string;
```

NEW from src/rituals/display-names.ts (created by Task 1):
```typescript
export const RITUAL_DISPLAY_NAMES: Record<string, Record<Lang, string>>;
export const CONFIG_FIELD_LABELS: Record<string, Record<Lang, string>>;
export function displayName(slug: string, locale: Lang): string;
export function configFieldLabel(field: string, locale: Lang): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/rituals/display-names.ts with ritual + config-field display-name maps</name>
  <files>src/rituals/display-names.ts</files>
  <read_first>
    - src/chris/refusal.ts (read lines 178-193 — the exact ACKNOWLEDGMENTS shape Phase 41 mirrors)
    - src/chris/language.ts (read lines 94-119 — the Lang union + langOf narrowing helper)
    - .planning/phases/41-adjustment-dialogue-rework/41-PATTERNS.md §display-names.ts
  </read_first>
  <action>
    Create `src/rituals/display-names.ts` (new module). Implements decision D-41-03 from CONTEXT.md.

    Required exports — names and contracts are load-bearing, downstream Task 2 imports them by these exact names:

    1. `RITUAL_DISPLAY_NAMES: Record<string, Record<Lang, string>>` — keyed by ritual slug (the literal `rituals.name` column value from the DB), valued by `{ English, French, Russian }`. Three entries: `daily_journal`, `daily_wellbeing`, `weekly_review`. Use the exact EN/FR/RU strings locked in CONTEXT.md D-41-03 (evening journal / journal du soir / вечерний журнал; wellbeing check / check bien-être / проверка состояния; weekly review / bilan hebdo / еженедельный обзор). Note: "evening journal" not "daily journal" per CLAUDE.md `feedback_evening_journal_naming` — the slug-rename migration 0011 renamed the DB column but display must continue to call it "evening journal".
    2. `CONFIG_FIELD_LABELS: Record<string, Record<Lang, string>>` — keyed by config-field name. Exactly three entries matching the post-ADJ-05 enum (`fire_at`, `fire_dow`, `skip_threshold`). `mute_until` is NOT included because Plan 41-02 removes it from the Haiku whitelist — a label for a field the user can no longer reach is dead code. EN/FR/RU per CONTEXT.md D-41-03 (fire time / heure de déclenchement / время срабатывания; day of week / jour de la semaine / день недели; skip threshold / seuil de saut / порог пропусков).
    3. `displayName(slug: string, locale: Lang): string` — returns `RITUAL_DISPLAY_NAMES[slug]?.[locale] ?? slug`. Fallback-to-key matches `refusal.ts:191` pattern.
    4. `configFieldLabel(field: string, locale: Lang): string` — same shape with `CONFIG_FIELD_LABELS`.

    File header JSDoc must cite: Phase 41 ADJ-02, CONTEXT.md D-41-03, and the `ACKNOWLEDGMENTS` exemplar at refusal.ts:180-184. Note that Plan 41-01 only consumes the `English` slot (the EN-only ship); Plan 41-02 begins consuming the FR/RU slots.

    Import `Lang` from `../chris/language.js` (note the `.js` ESM suffix — matches all other imports in `src/rituals/`).

    No DB column, no migration, no schema_version bump. Pure TS module.
  </read_first>
  <action_continued>
    DO NOT add a `RITUAL_PROMPT_PRONOUNS` map or any other map beyond the two required by D-41-03 — D-41-03's "Claude's discretion" leaves that to Plan 41-02 research, not Plan 41-01.
  </action_continued>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "src/rituals/display-names\.ts" || echo "no_typecheck_errors_in_new_file"</automated>
    <automated>grep -c "^export" src/rituals/display-names.ts</automated>
    <automated>grep -q "RITUAL_DISPLAY_NAMES\['daily_journal'\]\|daily_journal:" src/rituals/display-names.ts && echo "ok"</automated>
    <automated>grep -q "evening journal" src/rituals/display-names.ts && echo "ok"</automated>
    <automated>grep -q "journal du soir" src/rituals/display-names.ts && echo "ok"</automated>
    <automated>grep -q "вечерний журнал" src/rituals/display-names.ts && echo "ok"</automated>
    <automated>grep -q "fire_at\|fire time" src/rituals/display-names.ts && grep -q "skip_threshold\|skip threshold" src/rituals/display-names.ts && echo "ok"</automated>
    <automated>! grep -q "mute_until" src/rituals/display-names.ts && echo "mute_until_correctly_absent"</automated>
  </verify>
  <acceptance_criteria>
    - `src/rituals/display-names.ts` exists
    - Exports exactly 4 symbols: `RITUAL_DISPLAY_NAMES`, `CONFIG_FIELD_LABELS`, `displayName`, `configFieldLabel`
    - `RITUAL_DISPLAY_NAMES.daily_journal.English === 'evening journal'`
    - `RITUAL_DISPLAY_NAMES.daily_journal.French === 'journal du soir'`
    - `RITUAL_DISPLAY_NAMES.daily_journal.Russian === 'вечерний журнал'`
    - `RITUAL_DISPLAY_NAMES.daily_wellbeing.English === 'wellbeing check'`
    - `RITUAL_DISPLAY_NAMES.weekly_review.English === 'weekly review'`
    - `CONFIG_FIELD_LABELS` contains exactly the keys `fire_at`, `fire_dow`, `skip_threshold` — `mute_until` is absent
    - `displayName('unknown_slug', 'English')` returns `'unknown_slug'` (fallback to key)
    - `configFieldLabel('unknown_field', 'English')` returns `'unknown_field'` (fallback to key)
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>
    File compiles. Imports cleanly into Task 2's `adjustment-dialogue.ts`. No tests yet — Plan 41-02 Task 4 adds the integration test that exercises the EN paths through this module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite adjustment-dialogue.ts user-facing copy with observational prompts + display-name substitution + skip_count reset at 3 completion sites</name>
  <files>src/rituals/adjustment-dialogue.ts</files>
  <read_first>
    - src/rituals/adjustment-dialogue.ts (FULL FILE — the whole 747-line file is the surface; do not rely on the line-citations alone, read it once end-to-end)
    - src/rituals/display-names.ts (from Task 1 — imports needed)
    - src/rituals/types.ts:181-195 (RITUAL_OUTCOME map — values used in fire-events inserts)
    - .planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md §BL-01..BL-08 + WR-01 (suggested fixes are load-bearing)
    - .planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md (decisions D-41-02, D-41-03, D-41-05)
  </read_first>
  <action>
    Five edit clusters in `src/rituals/adjustment-dialogue.ts`. All EN-only in Plan 41-01 (Plan 41-02 layers FR/RU on top of the same call sites; the display-name + config-field-label modules already expose the FR/RU slots).

    **Cluster A — Imports (top of file, line ~46):**
    Add `import { displayName, configFieldLabel } from './display-names.js';`. The locale wiring imports (`detectLanguage`, `getLastUserLanguage`, `getLastUserLanguageFromDb`, `setLastUserLanguage`, `langOf`, `Lang`) are NOT added in Plan 41-01 — Plan 41-02 owns ADJ-03 locale wiring. Plan 41-01 ships EN-only with the new copy strings hardcoded to the `'English'` slot via `displayName(ritual.name, 'English')` / `configFieldLabel(field, 'English')`. Document this with a one-line comment on the import: `// Plan 41-01 ships EN-only; Plan 41-02 wires Lang detection on top`.

    **Cluster B — `fireAdjustmentDialogue` (lines 280-329):**
    - Delete the `cadence` ternary at line 283 (BL-02 / WR-01 fix — display name encodes cadence, ternary swallows monthly/quarterly anyway).
    - Replace the `messageText` at lines 284-286 with the BL-01 observational copy variant locked in D-41-02. Exact EN string:
      `` `I noticed we've missed the ${displayName(ritual.name, 'English')} a few times. Want to adjust something, or keep it as is?` ``
      One line, no truncation. The previous two-line concat (`Reply with what to change, or 'no change' / 'drop it' if you'd prefer...`) is GONE — D-41-02 chose the BL-01 single-sentence variant because the dialogue purpose is already implicit, and the extra sentence diluted the agency-preserving framing.
    - In the `metadata` jsonb at line 305-309, replace `cadence` with `cadence: ritual.type` (direct passthrough, WR-01/WR-11 forward-compat for M013) and replace `ritualName: ritual.name` (slug) with `ritualName: displayName(ritual.name, 'English')` (display string). The slug is still recoverable via the `ritualId` foreign-key path; no diagnostic data lost.

    **Cluster C — `handleConfirmationReply` yes-branch (lines 529-535) — ADJ-04 site #1:**
    Wrap the existing `confirmConfigPatch` + sendMessage with a `db.transaction()` that does the skip_count reset + ritual_fire_events RESPONDED emission. Mirror the exact 2-write shape already in `ritualConfirmationSweep` (lines 688-701) — same outcome value, same metadata `source` discriminator. Use `source: 'user_yes'` in metadata. The sendMessage stays OUTSIDE the transaction (D-41-05 pattern: state-then-send; BL-11 outbox rework is v2.7 scope).

    Rewrite the user-facing sendMessage at line 531 to use the display-name map:
    `` `Applied: ${configFieldLabel(proposedChange.field, 'English')} = ${proposedChange.new_value}` ``
    (BL-02 WR-09 fix — was `Applied: ${proposedChange.field} = ${proposedChange.new_value}` which leaked `fire_at` slug.)

    **Cluster D — `handleConfirmationReply` no-branch (lines 536-552) — ADJ-04 site #2:**
    Wrap the existing `ritualConfigEvents` abort insert in `db.transaction()` together with: (a) UPDATE rituals SET skipCount=0, (b) INSERT ritual_fire_events RESPONDED with `metadata.source: 'user_no'`. sendMessage outside the transaction. The abort `ritualConfigEvents` insert (line 538-546) stays inside the transaction — it's part of the atomic completion event.

    Rewrite line 547 sendMessage to use `configFieldLabel(proposedChange.field, 'English')` in the same shape. Current copy `'OK, keeping current config'` is generic enough that it doesn't need a field-label injection, BUT change to:
    `` `OK, keeping ${configFieldLabel(proposedChange.field, 'English')} as is` ``
    so Greg sees what he just declined to change (BL-02 polish — was generic).

    **Cluster E — `routeRefusal` both branches (lines 125-185) — ADJ-04 site #3:**
    Hard-disable branch (lines 130-152): Wrap the existing UPDATE rituals SET enabled=false + ritualConfigEvents insert in `db.transaction()` together with: (a) UPDATE rituals SET skipCount=0 (merge into the SAME SET clause as enabled=false — one UPDATE statement, not two), (b) INSERT ritual_fire_events RESPONDED with `metadata.source: 'user_drop_it_or_disable'`. sendMessage at line 149-152 stays outside.

    Not-now branch (lines 153-184): Wrap the existing UPDATE rituals SET config=jsonb_set + ritualConfigEvents insert in `db.transaction()` together with: (a) merge skipCount=0 into the SAME SET clause as the jsonb_set (single UPDATE statement), (b) INSERT ritual_fire_events RESPONDED with `metadata.source: 'user_not_now'`. sendMessage at line 180-183 stays outside.

    Also rewrite line 471 sendMessage in `handleAdjustmentReply` evasive→auto_pause branch — current copy `'Pausing this ritual for 30 days...'` doesn't currently name the ritual (no slug leak), but for consistency with the observational voice, leave the text unchanged but ADD `displayName(ritual.name, 'English')` if needed. Re-read line 469-472 carefully — current text doesn't include the slug, so this site is actually fine; flagged in ADJ-02 only because the metadata path (line 308) was leaking it. Verify by grep and leave the user-facing send alone IF no slug leak exists; otherwise apply the substitution.

    Also rewrite line 733 sendMessage in `queueConfigPatchConfirmation` to use the display-name map:
    `` `Change ${configFieldLabel(proposedChange.field, 'English')} to ${proposedChange.new_value} — OK? (auto-applies in 60s if no reply)` ``
    (WR-09 fix — was leaking `fire_at` slug.)
  </action>
  <action_continued>
    **Do NOT change:** the `cadence` ternary in line 283 was removed; if any other site references `cadence` as a local, audit and either remove or replace with `ritual.type`. The metadata jsonb shape (`metadata.cadence`) MUST hold `ritual.type` directly (one of `'daily' | 'weekly' | 'monthly' | 'quarterly'`) per WR-01 forward-compat. Plan 41-02 owns the Haiku judge prompt (`ADJUSTMENT_JUDGE_PROMPT` at line 54-55), the FR/RU sendMessage variants, and the locale-detection wiring — DO NOT touch those in Plan 41-01 (scope creep guard; the FR/RU exemplars need user review before they ship, and 41-01 must ship without waiting).

    **Do NOT change ADJ-05 zod schemas at lines 193/203** — Plan 41-02 owns that. ADJ-06 candidate-config-parse in `confirmConfigPatch` — also Plan 41-02.
  </action_continued>
  <verify>
    <automated>! grep -q "isn't working" src/rituals/adjustment-dialogue.ts && echo "BL01_fixed"</automated>
    <automated>! grep -E "\\\$\\{cadence\\} \\\$\\{ritual\\.name\\}" src/rituals/adjustment-dialogue.ts && echo "cadence_doubling_gone"</automated>
    <automated>grep -q "I noticed we've missed" src/rituals/adjustment-dialogue.ts && echo "observational_copy_present"</automated>
    <automated>grep -q "from './display-names" src/rituals/adjustment-dialogue.ts && echo "import_added"</automated>
    <automated>grep -cE "skipCount: 0" src/rituals/adjustment-dialogue.ts</automated>
    <automated>grep -cE "db\.transaction" src/rituals/adjustment-dialogue.ts</automated>
    <automated>grep -q "source: 'user_yes'" src/rituals/adjustment-dialogue.ts && echo "yes_source_tagged"</automated>
    <automated>grep -q "source: 'user_no'" src/rituals/adjustment-dialogue.ts && echo "no_source_tagged"</automated>
    <automated>grep -q "source: 'user_drop_it_or_disable'" src/rituals/adjustment-dialogue.ts && echo "hard_disable_source_tagged"</automated>
    <automated>grep -q "source: 'user_not_now'" src/rituals/adjustment-dialogue.ts && echo "not_now_source_tagged"</automated>
    <automated>grep -q "configFieldLabel" src/rituals/adjustment-dialogue.ts && echo "field_label_used"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint -- src/rituals/adjustment-dialogue.ts src/rituals/display-names.ts</automated>
    <automated>bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The literal string `"isn't working"` is NOT present anywhere in `src/rituals/adjustment-dialogue.ts`
    - The literal substring `${cadence} ${ritual.name}` does NOT appear (template-literal removed)
    - Fire-side prompt at the rewritten `messageText` matches: `I noticed we've missed the ${displayName(...)} a few times. Want to adjust something, or keep it as is?`
    - File imports `displayName` and `configFieldLabel` from `'./display-names.js'`
    - `skipCount: 0` appears at exactly 4 distinct call sites inside adjustment-dialogue.ts: handleConfirmationReply-yes (Cluster C), handleConfirmationReply-no (Cluster D), routeRefusal-hard-disable (Cluster E first branch), routeRefusal-not-now (Cluster E second branch). The existing `ritualConfirmationSweep` reset at line 700 is preserved (5th, pre-existing). Total count: 5.
    - `db.transaction(` appears at exactly 4 call sites (the 4 Phase 41 sites — `ritualConfirmationSweep` is NOT wrapped because it already processes per-row idempotently)
    - Each of the 4 transactions emits a `ritual_fire_events` row with `outcome: RITUAL_OUTCOME.RESPONDED` and `metadata.source` set to one of `'user_yes' | 'user_no' | 'user_drop_it_or_disable' | 'user_not_now'`
    - The sendMessage at line ~531 (yes ack) renders `Applied: ${configFieldLabel(...)} = ${new_value}` — no raw `proposedChange.field` slug
    - The sendMessage at line ~547 (no ack) renders `OK, keeping ${configFieldLabel(...)} as is`
    - The sendMessage at line ~733 (confirmation echo) renders `Change ${configFieldLabel(...)} to ${new_value} — OK? (auto-applies in 60s if no reply)`
    - `npm run typecheck` exits 0
    - `npm run lint -- src/rituals/adjustment-dialogue.ts src/rituals/display-names.ts` exits 0
    - The existing `src/rituals/__tests__/adjustment-dialogue.integration.test.ts` continues to pass against the rewritten file under Docker PG harness (the test asserts mechanics — atomic-consume, classification routing, refusal pre-check — none of which changed). Any assertions on the literal copy must be updated as part of this task IF the existing tests broke; do so minimally and only for the changed-copy assertions.
  </acceptance_criteria>
  <done>
    Live UX defect closed (no more "isn't working"; no more `daily daily_journal`; no more raw `fire_at` slug in confirmation echo). Every-tick re-fire after threshold closed at the 3 in-file completion sites (Task 3 closes site #4 in skip-tracking.ts). Existing integration test passes against the rewrite.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add skipCount=0 reset + RESPONDED fire-event emission to autoReEnableExpiredMutes in skip-tracking.ts (ADJ-04 site #4 / BL-10)</name>
  <files>src/rituals/skip-tracking.ts</files>
  <read_first>
    - src/rituals/skip-tracking.ts (lines 236-314, the autoReEnableExpiredMutes function — read in full)
    - src/rituals/types.ts:181-195 (RITUAL_OUTCOME map)
    - .planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md §BL-10
    - .planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md §D-41-05 (4-sites contract)
  </read_first>
  <action>
    Two edits in `src/rituals/skip-tracking.ts:264-314` (`autoReEnableExpiredMutes`).

    **Edit 1 — extend the UPDATE rituals SET clause at line 289-295:**
    Add `skipCount: 0` to the existing SET object so the single UPDATE flips `enabled=true`, clears `mute_until`, AND resets `skip_count` atomically. Current shape:
    ```typescript
    .set({ enabled: true, config: sql`jsonb_set(${rituals.config}, '{mute_until}', 'null'::jsonb)` })
    ```
    Becomes:
    ```typescript
    .set({ enabled: true, skipCount: 0, config: sql`jsonb_set(${rituals.config}, '{mute_until}', 'null'::jsonb)` })
    ```
    One UPDATE statement, one transaction (Drizzle UPDATE is atomic by itself; no `db.transaction()` wrapping needed for the single statement). The existing `ritualConfigEvents` INSERT at lines 298-306 stays after, idempotent under retry (CONTEXT.md D-41-05 notes per-row independence — different ritual IDs cannot collide; if one fails, others still process).

    **Edit 2 — emit paired `ritual_fire_events` RESPONDED row after the UPDATE, before the existing ritualConfigEvents insert:**
    Insert a `ritualFireEvents` row with `outcome: RITUAL_OUTCOME.RESPONDED, metadata: { source: 'auto_re_enable', ritualId: row.id }`. This matches D-41-05's "replay consistency" requirement — `computeSkipCount` reads `ritual_fire_events` and needs a RESPONDED anchor at the reset boundary, otherwise the next `fired_no_response` event after re-enable would count from a baseline that includes the pre-pause skips. The new RESPONDED row makes the baseline `now()` instead, producing the correct `skipCount = 0` on replay.

    Order inside the for-loop body becomes:
    1. UPDATE rituals (enabled=true, skipCount=0, config) — line 289-295
    2. INSERT ritual_fire_events RESPONDED (NEW) — emit before the audit-event insert
    3. INSERT ritual_config_events (auto_re_enable kind) — existing, line 298-306
    4. logger.info — existing, line 308

    Import `RITUAL_OUTCOME` and `ritualFireEvents` from their existing modules — the file already imports `ritualFireEvents` at line 33 and `RITUAL_OUTCOME` at line 35; no new imports needed.

    **Do NOT wrap the loop body in a single transaction** — per the existing JSDoc at line 284-285 ("Each ritual is processed independently (not a transaction) — if one fails, others still process. Each update is idempotent."), this is a deliberate batch-tolerance pattern. Phase 41 preserves it. The single UPDATE statement and the per-row INSERTs are each individually atomic.
  </action>
  <verify>
    <automated>grep -n "skipCount: 0" src/rituals/skip-tracking.ts</automated>
    <automated>grep -A2 -B2 "enabled: true," src/rituals/skip-tracking.ts | grep -q "skipCount: 0" && echo "skipCount_in_same_SET"</automated>
    <automated>grep -q "source: 'auto_re_enable'" src/rituals/skip-tracking.ts && echo "auto_re_enable_source_tagged"</automated>
    <automated>grep -c "RITUAL_OUTCOME.RESPONDED" src/rituals/skip-tracking.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint -- src/rituals/skip-tracking.ts</automated>
    <automated>bash scripts/test.sh src/rituals/__tests__/self-protective-pause.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `skipCount: 0` appears exactly once in `src/rituals/skip-tracking.ts` (inside autoReEnableExpiredMutes' SET clause)
    - The `skipCount: 0` lives in the SAME `.set({...})` call as `enabled: true` and the `config: sql\`jsonb_set(...)\`` — a single UPDATE statement, not a separate one
    - A `db.insert(ritualFireEvents).values({...})` call exists inside the for-loop body in autoReEnableExpiredMutes, with `outcome: RITUAL_OUTCOME.RESPONDED` and `metadata.source: 'auto_re_enable'`
    - The fire-event INSERT is sequenced AFTER the UPDATE rituals statement and BEFORE the existing ritualConfigEvents INSERT (so RESPONDED appears in audit order before the auto_re_enable config-event)
    - `npm run typecheck` exits 0
    - `npm run lint -- src/rituals/skip-tracking.ts` exits 0
    - The existing `src/rituals/__tests__/self-protective-pause.test.ts` continues to pass (Phase 28 test, asserts the re-enable flips enabled=true and clears mute_until; the new skipCount=0 + RESPONDED emission is additive and shouldn't break it. If an assertion specifically checks for "no fire_events emitted" or similar, update minimally and reference D-41-05 in the test comment.)
  </acceptance_criteria>
  <done>
    All 4 ADJ-04 sites closed: yes-branch + no-branch in handleConfirmationReply, both refusal branches in routeRefusal (Task 2), and autoReEnableExpiredMutes (Task 3). The every-tick re-fire bug observed at 2026-05-14 17:00 Paris is closed at the source for every completion path. Plan 41-01 is ready to ship to Proxmox.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `runRitualSweep` → `fireAdjustmentDialogue` → `bot.api.sendMessage` | Outbound to Telegram; user-facing copy crosses here |
| `handleConfirmationReply` → DB UPDATE rituals.skip_count | State change crosses the read→write boundary on user reply |
| `routeRefusal` → DB UPDATE rituals.enabled + config.adjustment_mute_until | State change crosses the read→write boundary on refusal detection |
| `autoReEnableExpiredMutes` (cron) → DB UPDATE rituals.enabled + config + skip_count | Cron-driven state change; no user input boundary, but data-integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | `db.transaction()` wrapping handleConfirmationReply yes/no — partial-failure leaves skip_count out of sync | mitigate | Use `db.transaction()` to atomically wrap the UPDATE rituals + INSERT ritual_fire_events RESPONDED pair (Task 2 Clusters C, D, E). On rollback, skip_count + fire_events are consistent. sendMessage stays outside transaction (state-then-send pattern, D-41-05 — BL-11 outbox rework is v2.7 scope). |
| T-41-01-02 | Information Disclosure | Display-name map leaks via stack traces / logger.info if `displayName()` throws on null locale | accept | `displayName()` is pure lookup with `??` fallback; cannot throw. `Lang` union is closed (English/French/Russian only); `langOf` narrows null→English. No PII in the strings. |
| T-41-01-03 | Repudiation | autoReEnableExpiredMutes emits RESPONDED fire-event but no operator could prove which ritual was re-enabled if logger fails | accept | `ritual_config_events` already records the `auto_re_enable` audit row (lines 298-306, existing). The new `ritual_fire_events` RESPONDED row is additive — both audit trails persist independently. Defense-in-depth. |
| T-41-01-04 | Denial of Service | Observational copy change at line 285 could exceed Telegram's 4096-char message limit if `displayName` returned a pathological string | accept | `RITUAL_DISPLAY_NAMES` values are bounded constants ≤20 chars each. Fallback path returns the slug (≤32 chars per `actor varchar(32)` analog). Total message length stays well under 200 chars across all locales. |
| T-41-01-05 | Elevation of Privilege | (Out of Plan 41-01 scope — ADJ-05 mute_until whitelist tightening is in Plan 41-02) | transfer | Plan 41-02 Task 1 closes the `mute_until` privilege escalation. Plan 41-01 ships without that fix; the live bug is observational copy + slug leak + re-fire loop, none of which depend on the whitelist tightening. The privilege-escalation surface remained open for 14 days post-Phase-28 ship; another 24-48h delay until 41-02 is acceptable. |
| T-41-01-06 | Spoofing | (Not applicable — no auth boundary inside the changed code paths) | accept | All callers (PP#5 in engine.ts, runRitualSweep cron) are trusted internal consumers; no untrusted-input entry that wasn't already validated upstream. |

</threat_model>

<verification>
**Overall phase checks after both Tasks 2 + 3 complete:**

1. `npm run typecheck && npm run lint` exits 0 across the three modified/created files.
2. `bash scripts/test.sh src/rituals/__tests__/` runs ALL ritual integration tests against a real Docker Postgres. The pre-existing `adjustment-dialogue.integration.test.ts` and `self-protective-pause.test.ts` are the immediate canaries — both must pass.
3. End-to-end smoke (operator-driven, post-deploy on Proxmox): Force `skipCount` on a test ritual to its threshold, observe the fire-side message renders observational copy with `evening journal` (not `daily daily_journal`). Reply "yes" / "no" / "drop it" / "not now" through Telegram in four runs; query `SELECT skip_count, enabled, config->'adjustment_mute_until' FROM rituals` between runs and assert `skip_count=0` after each completion. Then bump system clock past `adjustment_mute_until` for the not-now case and reply "drop it" for the hard-disable case to verify the next sweep doesn't re-fire.
4. Query `SELECT outcome, metadata->>'source' FROM ritual_fire_events WHERE outcome='responded' AND metadata->>'source' IN ('user_yes', 'user_no', 'user_drop_it_or_disable', 'user_not_now', 'auto_re_enable') ORDER BY fired_at DESC LIMIT 10;` — after running the smoke, the table should contain one row per completion path with the expected source discriminator.
</verification>

<success_criteria>
- Greg's 2026-05-14 17:00 Paris UX defect cannot recur: the rendered copy is observational ("I noticed we've missed the evening journal a few times. Want to adjust something, or keep it as is?"), no slug leak, no "isn't working" assertion.
- Once Greg replies (yes/no/refusal-hard/refusal-not-now) OR once the 30-day auto-pause expires, `runRitualSweep` does NOT re-fire `shouldFireAdjustmentDialogue` for that ritual on the next tick — `rituals.skip_count = 0` is verifiable in the DB at every completion site.
- `ritual_fire_events` audit trail contains the new RESPONDED rows with the expected `metadata.source` discriminator at every completion event. `computeSkipCount` replay returns 0 immediately after the reset.
- Three files changed; one new file created. Zero schema migrations. Zero changes to Haiku prompts, Zod whitelist enums, locale-detection wiring, or integration tests beyond the minimal-update-to-changed-copy assertions in `adjustment-dialogue.integration.test.ts`.
- Plan 41-02 can layer FR/RU + security tighten + ADJ-06 candidate-parse + ADJ-07 test on top without colliding on any of Plan 41-01's touched lines (Plan 41-02 owns lines 54-55 ADJUSTMENT_JUDGE_PROMPT, 193 + 203 Zod enums, 568-616 confirmConfigPatch, and a new test file; Plan 41-01 owns the rest).
</success_criteria>

<output>
After completion, create `.planning/phases/41-adjustment-dialogue-rework/41-01-SUMMARY.md` documenting:
- Final EN copy strings (canonical reference for Plan 41-02 to mirror across FR/RU)
- The 4 ADJ-04 completion sites + their `metadata.source` discriminators
- Any deviations from D-41-02..D-41-05 (should be zero — auto-mode locked all decisions)
- Test-suite delta (which existing tests needed copy-assertion updates and why)
- A `## DEPLOYED` checkbox left unchecked — operator marks it after Proxmox deploy + smoke test
</output>
