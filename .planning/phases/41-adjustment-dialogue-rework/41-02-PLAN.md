---
phase: 41-adjustment-dialogue-rework
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - src/rituals/adjustment-dialogue.ts
  - src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts
autonomous: true
requirements:
  - ADJ-03
  - ADJ-05
  - ADJ-06
  - ADJ-07
user_setup: []
tags:
  - rituals
  - adjustment-dialogue
  - localization
  - security

must_haves:
  truths:
    - "The 8 user-facing sendMessage sites in adjustment-dialogue.ts render in Greg's detected locale (one of English/French/Russian) — no EN-only sites remain"
    - "The Haiku ADJUSTMENT_JUDGE_PROMPT instructs the classifier to handle EN/FR/RU inputs uniformly and gives one example per language for each classification class"
    - "A Haiku-parsed user reply with proposed_change.field='mute_until' fails Zod parse at the boundary — both v3 and v4 schemas reject 'mute_until' as an invalid enum value"
    - "A Haiku-parsed user reply with type-mismatched new_value (e.g., {field: 'fire_at', new_value: 42} or {field: 'fire_dow', new_value: 'Monday'}) is rejected in confirmConfigPatch via RitualConfigSchema candidate-parse — no jsonb_set runs, no config_invalid on next sweep"
    - "Rejected config-patches log chris.adjustment.config_patch.invalid_type with {field, new_value, error}, insert a ritual_config_events row with patch.kind='rejected', and send Greg a localized error message"
    - "src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts exercises the 5 completion paths (yes / no / refusal-hard / refusal-not-now / auto-re-enable-after-mute-expiry) and asserts runRitualSweep does NOT re-fire shouldFireAdjustmentDialogue on the next tick after each path"
    - "The new integration test passes against real Docker Postgres via bash scripts/test.sh (per MEMORY.md 'Always run full Docker tests')"
  artifacts:
    - path: "src/rituals/adjustment-dialogue.ts"
      provides: "FR/RU localization across 8 sendMessage sites + locale-agnostic Haiku judge prompt + zod whitelist tighten (mute_until removed) + per-field candidate-parse validation in confirmConfigPatch"
      contains: "z.enum(['fire_at', 'fire_dow', 'skip_threshold'])"
    - path: "src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts"
      provides: "5-case integration test for no-refire after completion (ADJ-07)"
      contains: "describe('Phase 41 ADJ-07"
  key_links:
    - from: "src/rituals/adjustment-dialogue.ts user-facing sendMessage sites"
      to: "src/rituals/display-names.ts displayName + configFieldLabel"
      via: "import + per-call substitution using locale narrowed from detectLanguage/getLastUserLanguageFromDb"
      pattern: "displayName\\(.*locale\\)|configFieldLabel\\(.*locale\\)"
    - from: "src/rituals/adjustment-dialogue.ts AdjustmentClassificationSchema + AdjustmentClassificationSchemaV4"
      to: "Zod enum literals"
      via: "removing 'mute_until' from the .field enum on BOTH v3 (line 193) and v4 (line 203)"
      pattern: "z\\.enum\\(\\['fire_at', 'fire_dow', 'skip_threshold'\\]\\)"
    - from: "src/rituals/adjustment-dialogue.ts confirmConfigPatch"
      to: "src/rituals/types.ts parseRitualConfig"
      via: "candidate-config in-memory build + parseRitualConfig(candidate) gate before jsonb_set"
      pattern: "parseRitualConfig\\(candidate\\)|parseRitualConfig\\(\\{"
    - from: "src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts"
      to: "runRitualSweep + shouldFireAdjustmentDialogue + ritual_fire_events"
      via: "post-completion sweep call + assertion that outcome !== 'in_dialogue' and skipCount === 0"
      pattern: "runRitualSweep\\(.*\\)|shouldFireAdjustmentDialogue\\("
---

<objective>
Close the remaining four ADJ requirements on top of Plan 41-01's live-fix: FR/RU localization (ADJ-03), Haiku-whitelist privilege-escalation closure (ADJ-05), per-field config-patch validation (ADJ-06), and the regression-class integration test (ADJ-07).

**Purpose:** Plan 41-01 stops Greg's currently-firing UX defect and re-fire loop in EN. Plan 41-02 (a) renders the same surface in Greg's primary locale (FR), (b) closes the Haiku-whitelist `mute_until` privilege escalation that lets a parsed user reply globally suppress the ritual channel, (c) prevents Haiku-malformed config patches from bricking rituals via the next-sweep `parseRitualConfig` throw, and (d) ships the integration test that ratchets the no-refire invariant into CI.

**Output:**
- `src/rituals/adjustment-dialogue.ts` (MODIFIED) — locale wiring imports + 8 sendMessage sites localized via per-area `Record<Lang, string>` maps + ADJUSTMENT_JUDGE_PROMPT rewritten locale-agnostic + Zod schemas (v3 + v4) `mute_until` removed + confirmConfigPatch candidate-parse gate added
- `src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts` (NEW) — 5-case Docker-PG integration test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md
@.planning/phases/41-adjustment-dialogue-rework/41-PATTERNS.md
@.planning/phases/41-adjustment-dialogue-rework/41-01-PLAN.md
@.planning/phases/41-adjustment-dialogue-rework/41-01-SUMMARY.md
@.planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md
@CLAUDE.md

# Source-of-truth files the executor MUST read before touching:
@src/rituals/adjustment-dialogue.ts
@src/rituals/display-names.ts
@src/chris/refusal.ts
@src/chris/language.ts
@src/rituals/types.ts
@src/rituals/scheduler.ts
@src/rituals/__tests__/adjustment-dialogue.integration.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Do NOT re-explore. -->

From src/chris/language.ts:
```typescript
export type Lang = 'English' | 'French' | 'Russian';
export function detectLanguage(text: string, previousLanguage: string | null): string;
export function getLastUserLanguage(chatId: string): string | null;
export async function getLastUserLanguageFromDb(chatId: bigint): Promise<string | null>;
export function setLastUserLanguage(chatId: string, language: string): void;
export function langOf(raw: string | null): Lang;
```

From src/rituals/display-names.ts (created by Plan 41-01):
```typescript
export const RITUAL_DISPLAY_NAMES: Record<string, Record<Lang, string>>;     // keys: daily_journal, daily_wellbeing, weekly_review
export const CONFIG_FIELD_LABELS: Record<string, Record<Lang, string>>;      // keys: fire_at, fire_dow, skip_threshold
export function displayName(slug: string, locale: Lang): string;
export function configFieldLabel(field: string, locale: Lang): string;
```

From src/rituals/types.ts:
```typescript
export const RitualConfigSchema = z.object({...}).strict();   // per-field types: fire_at z.string regex HH:mm, fire_dow z.int min 1 max 7 optional, skip_threshold z.int min 1 max 10
export function parseRitualConfig(input: unknown): RitualConfig;  // throws ZodError on type mismatch
```

From src/rituals/scheduler.ts:
```typescript
export async function runRitualSweep(now?: Date): Promise<RitualFireResult[]>;
```

From src/rituals/skip-tracking.ts:
```typescript
export async function shouldFireAdjustmentDialogue(ritual: typeof rituals.$inferSelect): Promise<boolean>;
export async function autoReEnableExpiredMutes(now?: Date): Promise<number>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten Haiku-controllable field whitelist + add per-field candidate-parse validation in confirmConfigPatch (ADJ-05 + ADJ-06)</name>
  <files>src/rituals/adjustment-dialogue.ts</files>
  <read_first>
    - src/rituals/adjustment-dialogue.ts (the WHOLE file; lines 189-210 zod schemas, lines 558-616 confirmConfigPatch are the surface)
    - src/rituals/types.ts:54-97 (RitualConfigSchema + parseRitualConfig — the existing schema is the ADJ-06 candidate-parse gate)
    - .planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md §BL-07, §BL-08 (suggested fixes are load-bearing)
    - .planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md §D-41-06 + §D-41-07
  </read_first>
  <action>
    Two edit clusters in `src/rituals/adjustment-dialogue.ts`.

    **Cluster A — Zod whitelist tighten (ADJ-05 / BL-07):**
    At line 193 (`AdjustmentClassificationSchema` v3) and line 203 (`AdjustmentClassificationSchemaV4` v4), change:
    ```typescript
    field: z.enum(['fire_at', 'fire_dow', 'skip_threshold', 'mute_until'])
    field: zV4.enum(['fire_at', 'fire_dow', 'skip_threshold', 'mute_until'])
    ```
    To:
    ```typescript
    field: z.enum(['fire_at', 'fire_dow', 'skip_threshold'])
    field: zV4.enum(['fire_at', 'fire_dow', 'skip_threshold'])
    ```
    Per D-41-06: do NOT replace with `adjustment_mute_until`. The `routeRefusal` not-now branch already writes `adjustment_mute_until` directly without Haiku input — adding a parallel Haiku path defeats the refusal-first ordering. Existing config rows with `mute_until` in jsonb are unaffected (only Haiku is blocked from setting it — enforced at the Zod parse boundary in `classifyAdjustmentReply` line 246, the v3 re-validate after the v4 SDK parse).

    Update the JSDoc comment block above line 213 (around lines 222-225, the "Security:" paragraph) to reflect the post-tighten state: enum is now `['fire_at', 'fire_dow', 'skip_threshold']`, not `[..., 'mute_until']`.

    **Cluster B — Per-field candidate-parse gate (ADJ-06 / BL-08):**
    In `confirmConfigPatch` (lines 568-616), insert a new STEP between STEP 1 (read current config, lines 573-582) and STEP 2 (jsonb_set UPDATE, lines 584-591).

    The new step builds a candidate config in memory and gates the write on `parseRitualConfig(candidate)`. Per D-41-07 logic:
    1. `const candidate = { ...cfg, [proposedChange.field]: proposedChange.new_value };`
    2. `try { parseRitualConfig(candidate); } catch (err) { ... reject path ... return; }`

    On the reject path:
    - Log `chris.adjustment.config_patch.invalid_type` at WARN level with `{ ritualId, field: proposedChange.field, attempted_new_value: proposedChange.new_value, err: String(err), actor }`. Use existing logger.warn shape.
    - Insert a `ritualConfigEvents` row with `actor` (the function's existing parameter) and `patch: { kind: 'rejected', field: proposedChange.field, attempted_new_value: proposedChange.new_value, error: String(err), source: actor === 'auto_apply_on_timeout' ? 'sweep' : 'reply' }`. The `kind: 'rejected'` discriminator extends the existing audit-event envelope (RESEARCH Landmine 1 of Phase 28); the new kind is additive and doesn't break any consumers.
    - Send Greg a localized error message via `bot.api.sendMessage(Number(config.telegramAuthorizedUserId), REJECT_ERROR_MSG[locale](configFieldLabel(proposedChange.field, locale)))`. The locale resolution for this site uses `getLastUserLanguageFromDb` because `confirmConfigPatch` is called from both reply-side handlers (locale already in session) AND from `ritualConfirmationSweep` (cron context, no session) — the DB-backed lookup is the safe path for both. Use the `Lang`-narrowed locale (`langOf(await getLastUserLanguageFromDb(BigInt(config.telegramAuthorizedUserId)))`).
    - `return;` — no jsonb_set, no successful audit insert. The function returns void per its existing signature.

    Define the `REJECT_ERROR_MSG` map near the other per-area locale maps Task 2 will add (or co-locate above `confirmConfigPatch`):
    ```typescript
    const REJECT_ERROR_MSG: Record<Lang, (fieldLbl: string) => string> = {
      English: (f) => `That value doesn't look like the right type for ${f} — keeping current config.`,
      French:  (f) => `Cette valeur ne semble pas avoir le bon type pour ${f} — je garde la config actuelle.`,
      Russian: (f) => `Это значение не похоже на правильный тип для ${f} — оставляю текущую конфигурацию.`,
    };
    ```

    **Do NOT remove the existing `sql.raw` jsonb_set call** at line 589. WR-10 (parameterized jsonb path) is explicitly deferred to v2.7 per CONTEXT.md `<deferred>`. The enum-tightening in Cluster A is sufficient defense-in-depth for Phase 41.

    **Do NOT add a per-field coercion layer** (e.g., normalizing `"21:30"` ISO time strings, or coercing `"3"` → `3` for skip_threshold). D-41-07 explicitly leaves coercion out of scope: `RitualConfigSchema` already accepts `"21:30"` as a valid `fire_at` (z.string), and `skip_threshold` requires `z.number` so `"3"` correctly fails — that's the intended behavior, not a bug to fix.
  </action>
  <verify>
    <automated>grep -E "z\\.enum\\(\\['fire_at', 'fire_dow', 'skip_threshold'\\]\\)" src/rituals/adjustment-dialogue.ts && echo "v3_enum_tightened"</automated>
    <automated>grep -E "zV4\\.enum\\(\\['fire_at', 'fire_dow', 'skip_threshold'\\]\\)" src/rituals/adjustment-dialogue.ts && echo "v4_enum_tightened"</automated>
    <automated>! grep -E "z(V4)?\\.enum\\(\\[.*'mute_until'" src/rituals/adjustment-dialogue.ts && echo "mute_until_removed_from_both"</automated>
    <automated>grep -q "parseRitualConfig(candidate)" src/rituals/adjustment-dialogue.ts && echo "candidate_parse_present"</automated>
    <automated>grep -q "kind: 'rejected'" src/rituals/adjustment-dialogue.ts && echo "rejected_kind_emitted"</automated>
    <automated>grep -q "chris.adjustment.config_patch.invalid_type" src/rituals/adjustment-dialogue.ts && echo "rejection_log_present"</automated>
    <automated>grep -q "REJECT_ERROR_MSG" src/rituals/adjustment-dialogue.ts && echo "localized_rejection_msg_present"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint -- src/rituals/adjustment-dialogue.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both AdjustmentClassificationSchema (v3, line ~193) and AdjustmentClassificationSchemaV4 (v4, line ~203) use the enum `['fire_at', 'fire_dow', 'skip_threshold']` exactly — `'mute_until'` is NOT present in either schema's field enum
    - `confirmConfigPatch` (lines ~568-616) contains an in-memory `candidate` config build (spread + override) and a `parseRitualConfig(candidate)` call gated behind try/catch
    - On the catch branch: (a) `logger.warn` with `chris.adjustment.config_patch.invalid_type`, (b) `db.insert(ritualConfigEvents)` with `patch.kind === 'rejected'`, (c) `bot.api.sendMessage` with a localized REJECT_ERROR_MSG, (d) `return` before any jsonb_set call
    - On the try-success branch: the existing jsonb_set UPDATE + `kind: 'apply'` audit insert run unchanged (lines 586-604 preserved)
    - `npm run typecheck` exits 0
    - `npm run lint -- src/rituals/adjustment-dialogue.ts` exits 0
  </acceptance_criteria>
  <done>
    ADJ-05 closed (privilege escalation surface). ADJ-06 closed (no more next-sweep brick from Haiku-malformed patches). Task 2 layers locale on top of these now-tightened paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Localize 8 sendMessage sites + Haiku judge prompt for FR/RU across adjustment-dialogue.ts (ADJ-03)</name>
  <files>src/rituals/adjustment-dialogue.ts</files>
  <read_first>
    - src/rituals/adjustment-dialogue.ts (current state — including the changes from Plan 41-01 Task 2 and Task 1 above; the executor reads the file FRESH)
    - src/chris/language.ts (the helpers being imported)
    - src/chris/engine.ts:352-370 (the locale wiring exemplar in the existing codebase)
    - src/rituals/display-names.ts (FR/RU slots already populated by Plan 41-01 Task 1)
    - .planning/milestones/v2.4-phases/28-skip-tracking-adjustment-dialogue/28-REVIEW.md §BL-03 (the 8 sites are catalogued there with exact line citations)
    - .planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md §D-41-02 (FR exemplar), §D-41-04 (locale resolution strategy)
  </read_first>
  <action>
    Three edit clusters in `src/rituals/adjustment-dialogue.ts`.

    **Cluster A — Locale-detection wiring imports + helpers:**
    Add imports near the existing language-adjacent imports (top of file):
    ```typescript
    import { detectLanguage, getLastUserLanguage, getLastUserLanguageFromDb, setLastUserLanguage, langOf, type Lang } from '../chris/language.js';
    ```
    Plan 41-01 deliberately did NOT add these; Plan 41-02 owns them.

    Wire locale resolution at the entry of each user-facing handler:

    - In `fireAdjustmentDialogue` (line ~280), BEFORE composing `messageText`: resolve `const locale: Lang = langOf(await getLastUserLanguageFromDb(chatId))` where `chatId` is the existing `BigInt(config.telegramAuthorizedUserId)` at line 288. The DB-backed lookup is the right path for cron context (no user text yet). Reuse the locale across the message build, the metadata jsonb, and the fire-event insert.

    - In `handleAdjustmentReply` (line ~342), AFTER the atomic-consume but BEFORE the refusal pre-check (line 371), resolve:
      ```typescript
      const chatIdStrAdj = String(chatId);
      const prevLang = getLastUserLanguage(chatIdStrAdj);
      const detectedLang = detectLanguage(text, prevLang);
      if (detectedLang) setLastUserLanguage(chatIdStrAdj, detectedLang);
      const locale: Lang = langOf(detectedLang);
      ```
      Pass `locale` down to `routeRefusal` (extend its signature) and into the sendMessage at line 469-472 (auto_pause branch).

    - In `handleConfirmationReply` (line ~489), same resolution shape as handleAdjustmentReply. Pass `locale` to `confirmConfigPatch` (extend its signature) and use locally for the yes/no sendMessage at line 531/547.

    - In `queueConfigPatchConfirmation` (line ~722), accept `locale: Lang` as a new parameter from the caller (`handleAdjustmentReply` change-requested branch passes it through).

    **Cluster B — Per-area locale string maps (the 8 sites + Haiku prompt):**
    Define `Record<Lang, string>` or `Record<Lang, (...) => string>` maps inline near the top of each function for locality-of-reference, mirroring the `ACKNOWLEDGMENTS` shape from `refusal.ts:180-184`. Each map covers EXACTLY one rendering site.

    Eight user-facing sendMessage call sites map to eight strings:
    1. **L151 hard_disable ack** — `HARD_DISABLE_ACK[locale]`:
       - English: `'OK, disabling this ritual. You can re-enable it manually anytime.'`
       - French:  `'D'accord, je désactive ce rituel. Tu peux le réactiver manuellement à tout moment.'`
       - Russian: `'Хорошо, отключаю этот ритуал. Ты можешь включить его вручную в любое время.'`
    2. **L182 not_now ack** — `NOT_NOW_ACK[locale]`:
       - English: `"OK, I'll skip the adjustment dialogue for 7 days. Skip-tracking continues."`
       - French:  `"D'accord, je passe le dialogue d'ajustement pendant 7 jours. Le suivi des sauts continue."`
       - Russian: `"Хорошо, пропускаю диалог настройки на 7 дней. Учёт пропусков продолжается."`
    3. **L285-286 fire-side observational prompt** — `OBSERVATIONAL_PROMPT[locale](dispName)`:
       - English: `(name) => \`I noticed we've missed the ${name} a few times. Want to adjust something, or keep it as is?\``  (already shipped by Plan 41-01 — Plan 41-02 wraps in the map and changes the call site to look up by locale; the EN string is unchanged from D-41-02)
       - French:  `(name) => \`J'ai remarqué qu'on a sauté le rituel ${name} plusieurs fois. Tu veux ajuster quelque chose, ou on garde comme ça ?\``
       - Russian: `(name) => \`Я заметил, что мы пропустили ${name} несколько раз. Хочешь что-то изменить, или оставим как есть?\``
    4. **L471 auto_pause message** — `AUTO_PAUSE_MSG[locale](dispName, dateISO)`:
       - English: `(n, d) => \`Pausing the ${n} for 30 days — feels like the timing isn't right. It will auto-re-enable on ${d}.\``
       - French:  `(n, d) => \`Je mets le ${n} en pause pendant 30 jours — j'ai l'impression que le timing ne va pas. Réactivation auto le ${d}.\``
       - Russian: `(n, d) => \`Ставлю ${n} на паузу на 30 дней — кажется, момент не подходящий. Автоматическое включение ${d}.\``
       (Plan 41-01 didn't substitute the displayName here because the EN copy didn't reference the ritual; Plan 41-02 ADDS the displayName substitution because the FR/RU variants need it for naturalness. This is an additive change, not a regression of 41-01.)
    5. **L531 yes-applied ack** — `APPLIED_ACK[locale](fieldLbl, newValue)`:
       - English: `(f, v) => \`Applied: ${f} = ${v}\``
       - French:  `(f, v) => \`Appliqué : ${f} = ${v}\``
       - Russian: `(f, v) => \`Применено: ${f} = ${v}\``
    6. **L547 no-keeping ack** — `KEPT_ACK[locale](fieldLbl)`:
       - English: `(f) => \`OK, keeping ${f} as is\``
       - French:  `(f) => \`D'accord, je garde ${f} tel quel\``
       - Russian: `(f) => \`Хорошо, оставляю ${f} как есть\``
    7. **L733 confirmation echo** — `CONFIRM_ECHO[locale](fieldLbl, newValue)`:
       - English: `(f, v) => \`Change ${f} to ${v} — OK? (auto-applies in 60s if no reply)\``
       - French:  `(f, v) => \`Changer ${f} en ${v} — OK ? (s'applique auto dans 60 s sans réponse)\``
       - Russian: `(f, v) => \`Изменить ${f} на ${v} — OK? (применится автоматически через 60 сек без ответа)\``
    8. **REJECT_ERROR_MSG** — already defined by Task 1 above; lives in the same family.

    For each of the 7 sendMessage call sites: replace the existing hardcoded EN string (which Plan 41-01 left in EN per its scope) with `MAP[locale](displayName(ritual.name, locale))` or `MAP[locale](configFieldLabel(field, locale), newValue)` as appropriate. Use the locale variable resolved in Cluster A. Each substitution uses the appropriate display-name or field-label helper from `display-names.ts` so the SLUG never appears in the final string.

    **Cluster C — Haiku judge prompt rewrite (ADJUSTMENT_JUDGE_PROMPT line 54-55 / BL-03 second half):**
    Replace the current EN-only prompt with a language-agnostic instruction that tells Haiku the reply may be in EN/FR/RU and gives ONE example per language for each classification class. The classification semantics MUST NOT change (still 3 classes: `change_requested`, `no_change`, `evasive`); only the prompt's language coverage broadens.

    New prompt skeleton (one string constant, ~600 chars):
    ```
    You are classifying a user's reply to an adjustment-dialogue message about a recurring ritual. The user may reply in English, French, or Russian — handle all three uniformly. The ritual has been skipped too many times and the user was asked what should change. Classify the reply into exactly one category:
    - 'change_requested' (user wants a specific change to the ritual config). Examples: EN "move it to 9pm", FR "à 21h plutôt", RU "перенеси на 21:00".
    - 'no_change' (user says no change needed, or wants to keep going). Examples: EN "no change, all good", FR "rien à changer", RU "ничего не менять".
    - 'evasive' (vague, dismissive, or unclear). Examples: EN "idk lol", FR "bof", RU "не знаю".
    If change_requested, extract the proposed change field (one of: fire_at, fire_dow, skip_threshold) and the new value.
    Output JSON: { classification: 'change_requested'|'no_change'|'evasive', proposed_change: { field, new_value } | null, confidence: number 0-1 }.
    ```
    Note: the field list `(one of: fire_at, fire_dow, skip_threshold)` matches the post-Task-1 enum (no `mute_until`) — keeping the prompt in sync with the schema is load-bearing for Haiku output quality.

    **Do NOT change the structured-output Zod parse contract** at lines 232-247 — the v3/v4 dual schemas already validate Haiku's output. The prompt change is locale-coverage only.
  </action>
  <verify>
    <automated>grep -q "from '../chris/language" src/rituals/adjustment-dialogue.ts && echo "lang_imports_added"</automated>
    <automated>grep -q "langOf(" src/rituals/adjustment-dialogue.ts && echo "lang_narrowing_used"</automated>
    <automated>grep -q "getLastUserLanguageFromDb" src/rituals/adjustment-dialogue.ts && echo "db_locale_lookup_used"</automated>
    <automated>! grep -qE "^\\s*'OK, disabling this ritual\\." src/rituals/adjustment-dialogue.ts && echo "hardcoded_EN_string_removed"</automated>
    <automated>grep -c "Record<Lang," src/rituals/adjustment-dialogue.ts</automated>
    <automated>grep -q "OBSERVATIONAL_PROMPT" src/rituals/adjustment-dialogue.ts && echo "fire_prompt_localized"</automated>
    <automated>grep -q "HARD_DISABLE_ACK\\|NOT_NOW_ACK\\|APPLIED_ACK\\|KEPT_ACK\\|CONFIRM_ECHO\\|AUTO_PAUSE_MSG" src/rituals/adjustment-dialogue.ts</automated>
    <automated>grep -q "may reply in English, French, or Russian" src/rituals/adjustment-dialogue.ts && echo "haiku_prompt_localized"</automated>
    <automated>! grep -E "may reply in English\\. only|English-only" src/rituals/adjustment-dialogue.ts</automated>
    <automated>! grep -E "'fire_at', 'fire_dow', 'skip_threshold', 'mute_until'" src/rituals/adjustment-dialogue.ts && echo "haiku_prompt_field_list_synced"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint -- src/rituals/adjustment-dialogue.ts</automated>
    <automated>bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File imports `detectLanguage`, `getLastUserLanguage`, `getLastUserLanguageFromDb`, `setLastUserLanguage`, `langOf`, and `type Lang` from `'../chris/language.js'`
    - `fireAdjustmentDialogue` resolves `locale: Lang` via `langOf(await getLastUserLanguageFromDb(chatId))` BEFORE composing `messageText`
    - `handleAdjustmentReply`, `handleConfirmationReply` each resolve locale via the `detectLanguage` + `setLastUserLanguage` reply-side path AFTER the atomic-consume and BEFORE any user-facing sendMessage
    - 7 `Record<Lang, ...>` locale maps exist in the file (HARD_DISABLE_ACK, NOT_NOW_ACK, OBSERVATIONAL_PROMPT, AUTO_PAUSE_MSG, APPLIED_ACK, KEPT_ACK, CONFIRM_ECHO). REJECT_ERROR_MSG from Task 1 brings the total to 8.
    - Each map has exactly 3 entries: `English`, `French`, `Russian` — no other keys
    - Every user-facing `sendMessage(...)` call in adjustment-dialogue.ts looks up its string via `MAP[locale](...)` — no hardcoded EN string literals remain at sendMessage sites
    - `ADJUSTMENT_JUDGE_PROMPT` mentions all three languages (English, French, Russian) and gives at least one example per language for each of the three classifications
    - `ADJUSTMENT_JUDGE_PROMPT` field list matches `'fire_at, fire_dow, skip_threshold'` (no `mute_until` — synced with Task 1 enum tightening)
    - `npm run typecheck` exits 0
    - `npm run lint -- src/rituals/adjustment-dialogue.ts` exits 0
    - The pre-existing `adjustment-dialogue.integration.test.ts` continues to pass (it sets `mockSendMessage` blindly without asserting on copy contents — should be insensitive to localization. If any assertion does check copy literals, update minimally to match the new map structure.)
  </acceptance_criteria>
  <done>
    ADJ-03 closed. All 8 sendMessage sites + Haiku judge prompt render per Greg's detected locale. Plan 41-01's EN-only string literals are now wrapped in maps with FR/RU peers; the EN slot is unchanged, so Plan 41-01's behavior on EN is preserved exactly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts integration test exercising 5 completion paths (ADJ-07)</name>
  <files>src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts</files>
  <read_first>
    - src/rituals/__tests__/adjustment-dialogue.integration.test.ts (the exact analog test — copy the harness shape, mocks, fixture pattern)
    - src/rituals/__tests__/self-protective-pause.test.ts (the test that exercises autoReEnableExpiredMutes — relevant for the 5th case)
    - src/rituals/adjustment-dialogue.ts (post-Task-1 + Task-2 state, especially the 4 completion sites)
    - src/rituals/skip-tracking.ts (autoReEnableExpiredMutes — post-Plan-41-01 Task 3 state)
    - src/rituals/scheduler.ts:85-300 (runRitualSweep — the function the test calls to assert no-refire)
    - scripts/test.sh (Docker Postgres harness — the test runs through this)
    - .planning/phases/41-adjustment-dialogue-rework/41-CONTEXT.md §D-41-08
  </read_first>
  <action>
    Create `src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts`. Five test cases inside a single `describe('Phase 41 ADJ-07 — no re-fire after completion', ...)` block.

    **Module-level setup** — copy the hoisted-mock + vi.mock pattern from adjustment-dialogue.integration.test.ts:18-40 EXACTLY (load-bearing for Pitfall 6 — Sonnet/messages.create must NEVER be called from this path; the cumulative afterAll assertion enforces this). Mock only `bot.api.sendMessage` and `anthropic.messages.parse`. Real Drizzle, real Postgres.

    **Fixture helper** — copy `createTestRitual` from the analog test but rename the fixture to `'adj-no-refire-integration-test-ritual'` so it doesn't collide with the existing test's data (both tests may run in the same Docker session). Seed `skipCount: 3` and `skip_threshold: 3` so `shouldFireAdjustmentDialogue` returns true on the first sweep.

    **Cleanup** — copy the cleanup shape from the analog test; delete by the new fixture name only.

    **Test cases (each is one `it(...)` block):**

    **Case 1 — yes-reply path:** Seed ritual at threshold → call `fireAdjustmentDialogue(ritual)` → manually insert an `adjustment_confirmation` pending row matching what the change-requested path would produce (or drive through `handleAdjustmentReply` with a mocked Haiku `change_requested` response) → call `handleConfirmationReply(pending, chatId, 'yes')` → assert: (a) the rituals row has `skipCount === 0`, (b) a `ritual_fire_events` row exists with `outcome='responded'` AND `metadata->>'source'='user_yes'`, (c) `shouldFireAdjustmentDialogue(refreshed_ritual)` returns `false`, (d) `await runRitualSweep(new Date(Date.now() + 1000))` does NOT produce any result with `outcome === 'in_dialogue'` for this ritual (filter by `ritualId`).

    **Case 2 — no-reply path:** Same setup → call `handleConfirmationReply(pending, chatId, 'no')` → same 4 assertions but expect `metadata->>'source'='user_no'`.

    **Case 3 — refusal hard_disable path:** Seed ritual at threshold → call `fireAdjustmentDialogue(ritual)` to insert the `adjustment_dialogue` pending row → call `handleAdjustmentReply(pending, chatId, 'drop it')` (text matches the EN_PATTERNS "drop it" regex in refusal.ts, fires the hard_disable branch in `routeRefusal`) → assert: (a) `enabled === false`, (b) `skipCount === 0`, (c) fire-event with `metadata->>'source'='user_drop_it_or_disable'`, (d) `runRitualSweep` does NOT produce `in_dialogue` (it'll skip the disabled ritual anyway via the existing predicate — that's correct).

    **Case 4 — refusal not_now path:** Seed ritual at threshold → fire adjustment dialogue → call `handleAdjustmentReply(pending, chatId, 'not now please')` (matches `ADJUSTMENT_NOT_NOW_PATTERN`, fires the not_now branch) → assert: (a) `enabled === true` (not_now doesn't disable, just defers), (b) `config.adjustment_mute_until` is set to a future timestamp ~7 days out, (c) `skipCount === 0`, (d) fire-event with `metadata->>'source'='user_not_now'`, (e) `shouldFireAdjustmentDialogue(refreshed)` returns `false` (both because skipCount=0 AND adjustment_mute_until is in the future — either alone is sufficient; the test verifies BOTH gates close).

    **Case 5 — auto-re-enable after mute-expiry path:** Seed a ritual with `enabled: false`, `skipCount: 3`, and `config.mute_until` set to 5 minutes ago (already expired) → call `autoReEnableExpiredMutes(new Date())` → assert: (a) `enabled === true`, (b) `skipCount === 0`, (c) `config.mute_until === null`, (d) a `ritual_fire_events` row exists with `metadata->>'source'='auto_re_enable'`, (e) `runRitualSweep` against the re-enabled ritual does NOT produce `in_dialogue` (skipCount=0 means the predicate doesn't hit even though next_run_at may be due).

    **Mock orchestration:** For Cases 1 + 2 only, you need a pre-staged `adjustment_confirmation` pending row with `metadata.proposed_change` set. The cleanest path is to drive through `handleAdjustmentReply` with a mocked `mockAnthropicParse.mockResolvedValueOnce({ parsed_output: { classification: 'change_requested', proposed_change: { field: 'fire_at', new_value: '21:00' }, confidence: 0.95 }, ... })` — this exercises the full reply→confirm→consume flow. Cases 3 + 4 don't need Haiku at all (refusal pre-check short-circuits before classifyAdjustmentReply). Case 5 doesn't need any mocks beyond sendMessage (autoReEnableExpiredMutes never calls Haiku).

    **Cumulative invariant (Pitfall 6):**
    ```typescript
    afterAll(async () => {
      expect(mockAnthropicCreate).not.toHaveBeenCalled();
      await cleanup();
      await sql.end({ timeout: 5 }).catch(() => {});
    });
    ```
    Verifies Sonnet/messages.create is never invoked through any of these paths. Identical to the existing test's invariant.

    **Header comment block** — include the run-harness line:
    ```
    * Run via canonical Docker harness:
    *   bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts
    ```
    And reference Phase 41 ADJ-07 + CONTEXT.md D-41-08.

    **Do NOT gate the test under `INTEGRATION=1` env var.** The existing `adjustment-dialogue.integration.test.ts` (Phase 28) is unconditional and runs in the default Docker harness. Phase 41 matches that convention per D-41-08 "Plan 41-02 inspects the convention and matches" — convention is unconditional.
  </action>
  <verify>
    <automated>test -f src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "file_exists"</automated>
    <automated>grep -c "^  it(" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts</automated>
    <automated>grep -q "ADJ-07" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "requirement_cited"</automated>
    <automated>grep -q "expect(mockAnthropicCreate).not.toHaveBeenCalled" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "pitfall6_invariant_present"</automated>
    <automated>grep -q "runRitualSweep" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "sweep_called"</automated>
    <automated>grep -q "shouldFireAdjustmentDialogue" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "predicate_asserted"</automated>
    <automated>grep -q "autoReEnableExpiredMutes" src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts && echo "auto_re_enable_path_covered"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint -- src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts</automated>
    <automated>bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts` exists
    - File contains exactly 5 `it(` test blocks corresponding to the 5 completion paths: yes, no, refusal-hard-disable, refusal-not-now, auto-re-enable-after-mute-expiry
    - Each `it` block (a) seeds a fixture ritual at threshold (or with pre-expired mute for Case 5), (b) drives the completion path, (c) asserts `skipCount === 0` after the reset, (d) asserts a paired `ritual_fire_events` row with the expected `metadata.source` discriminator exists, (e) asserts `runRitualSweep` (or `shouldFireAdjustmentDialogue`) does NOT re-fire `in_dialogue` for the same ritual on the next call
    - `afterAll` includes the cumulative `mockAnthropicCreate.not.toHaveBeenCalled()` Pitfall 6 invariant
    - Test runs end-to-end via `bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts` against real Docker Postgres and exits 0
    - `npm run typecheck` and `npm run lint` exit 0 on the new file
    - The fixture name `'adj-no-refire-integration-test-ritual'` does NOT collide with any other test fixture in the directory
  </acceptance_criteria>
  <done>
    ADJ-07 closed. The regression class around ADJ-04 — "did anyone reset skip_count on every completion path?" — is now CI-ratchet ed; future refactors that forget the reset on any of the 5 sites will fail this test under Docker harness. Combined with Plan 41-01 Tasks 2 + 3, all 7 ADJ requirements ship.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Haiku → AdjustmentClassificationSchema parse | Untrusted LLM output crosses into the v3+v4 zod gate; ADJ-05 closes the `mute_until` privilege escalation, ADJ-06 closes the per-field type-mismatch surface |
| Haiku → confirmConfigPatch jsonb_set | Even after the enum gate, type-mismatched values cross via `new_value: string | number | null`; candidate-parse gate (ADJ-06) closes this |
| Greg's reply (Telegram text) → detectLanguage | Untrusted text crosses into locale narrowing; the franc library is restricted to {eng, fra, rus} and unknown languages fall back to previous OR English |
| Docker test harness → real Postgres | Test fixtures cross the DB boundary; cleanup-by-fixture-name is the isolation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Elevation of Privilege | Haiku-parsed `{field: 'mute_until', new_value: <future>}` bypasses refusal-first ordering and globally suppresses the ritual channel | mitigate | Task 1 Cluster A removes `mute_until` from BOTH v3 (line 193) and v4 (line 203) zod enums. The v4 SDK boundary refuses the parse; even if v4 drifts from v3, the v3 re-validate at line 246 catches it. `adjustment_mute_until` is NOT added as a replacement (D-41-06) — the not-now refusal path already covers that via `routeRefusal` without Haiku. |
| T-41-02-02 | Tampering | Haiku returns `{field: 'fire_at', new_value: 42}` → jsonb_set writes the number → next sweep `parseRitualConfig` throws ZodError → outcome='config_invalid' → ritual silently bricked | mitigate | Task 1 Cluster B builds a candidate config in memory and gates the jsonb_set on `parseRitualConfig(candidate)`. ZodError on candidate routes to the rejected-audit path, NOT the write path. Existing `RitualConfigSchema` per-field types (fire_at z.string regex, fire_dow z.int 1-7, skip_threshold z.int 1-10) catch all type-mismatch cases. |
| T-41-02-03 | Information Disclosure | Detected-locale logging could expose Greg's language preference in logs visible to operators | accept | Locale is already logged via `chris.engine.pp5.adjustment_dialogue` and similar messages in the existing codebase; Phase 41 adds no new disclosure surface beyond the existing chris.adjustment.* log lines, all of which are operator-internal and never user-facing. |
| T-41-02-04 | Denial of Service | Locale map lookups inside hot paths (every sendMessage) could throw on unexpected Lang value | accept | All maps are `Record<Lang, X>` with `Lang` a closed 3-member union; `langOf` narrows null/unknown → 'English' before the lookup. No throw path. The fallback returns the input slug, not undefined. |
| T-41-02-05 | Spoofing | The integration test seeds rituals as a test user — could it leak into prod-like state? | accept | Test runs only under `scripts/test.sh` which starts a fresh Docker postgres + tears it down via the trap. The fixture name `adj-no-refire-integration-test-ritual` is unique to this test; the cleanup deletes only rows matching that name. Cumulative `mockAnthropicCreate.not.toHaveBeenCalled` invariant catches any accidental real-Sonnet invocation. |
| T-41-02-06 | Repudiation | A rejected config patch (ADJ-06 reject path) writes to ritual_config_events but the user might not connect the localized error message to the audit row | accept | The `chris.adjustment.config_patch.invalid_type` log line captures `{ritualId, field, attempted_new_value, error, actor}` — sufficient operator audit trail. User-facing message is intentionally generic ("That value doesn't look like the right type") to avoid leaking Zod error internals. |

</threat_model>

<verification>
**Overall phase checks after Tasks 1, 2, 3 complete:**

1. `npm run typecheck && npm run lint` exits 0 across the two modified files.
2. `bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue.integration.test.ts` (existing test, Phase 28) — must still pass with all the Plan 41-01 + 41-02 changes layered.
3. `bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts` (new test, Phase 41 Task 3) — passes 5/5 cases.
4. `bash scripts/test.sh src/rituals/__tests__/self-protective-pause.test.ts` (existing Phase 28 test) — must still pass.
5. Unit-test-the-schema sanity: invoke `AdjustmentClassificationSchema.safeParse({classification: 'change_requested', proposed_change: {field: 'mute_until', new_value: 'x'}, confidence: 0.9})` — expect `success: false`. Same for `field: 'enabled'`. Both v3 and v4 schemas reject. (Optional — covered indirectly by the integration test's mocked-Haiku-output path.)
6. End-to-end smoke on Proxmox (operator-driven): with Greg's locale set to French in DB conversations history, force a fresh threshold-hit on a test ritual; observe the fire-side prompt renders in French ("J'ai remarqué qu'on a sauté..."). Reply "à 21h plutôt" — Haiku classifies as change_requested → confirmation echo renders in French → reply "oui" → applied. Query `ritual_fire_events` and `ritual_config_events` — RESPONDED + apply audit rows both present, all `metadata.source` discriminators populated.
</verification>

<success_criteria>
- The 8 user-facing sendMessage sites in `src/rituals/adjustment-dialogue.ts` render in Greg's detected locale per `getLastUserLanguageFromDb` (cron) or `detectLanguage` (reply-side) — verified by inspecting all sendMessage call sites and confirming each accesses a `MAP[locale](...)` lookup.
- `ADJUSTMENT_JUDGE_PROMPT` instructs Haiku to handle EN/FR/RU uniformly; field list synced with the post-ADJ-05 tightened enum.
- Both v3 + v4 zod schemas reject `'mute_until'` in `proposed_change.field` — verifiable by reading lines 193 + 203 and confirming the enum literal contains only 3 values.
- `confirmConfigPatch` performs an in-memory candidate-parse via `parseRitualConfig` and rejects type-mismatched values BEFORE the jsonb_set write — verified by reading the function and confirming the try/catch wraps the parse, the catch path emits `kind: 'rejected'` audit + localized error message + early return.
- `src/rituals/__tests__/adjustment-dialogue-no-refire.test.ts` exists, contains 5 cases covering yes / no / refusal-hard-disable / refusal-not-now / auto-re-enable-after-mute-expiry, asserts `skipCount === 0` + paired RESPONDED fire-event + no-refire on next `runRitualSweep`, and runs to completion via `bash scripts/test.sh` against Docker PG.
- Combined with Plan 41-01 (ADJ-01, ADJ-02, ADJ-04), all 7 ADJ requirements ship. ROADMAP Phase 41 success criteria 1-6 are all satisfied.
- Plan 41-01's EN behavior is preserved byte-for-byte at the EN locale slot (the FR/RU strings sit alongside, not in place of).
</success_criteria>

<output>
After completion, create `.planning/phases/41-adjustment-dialogue-rework/41-02-SUMMARY.md` documenting:
- The final FR + RU exemplars for each of the 8 sites + Haiku prompt (canonical reference for Phase 46 to consume / promote into the shared i18n layer)
- Confirmation that ADJUSTMENT_JUDGE_PROMPT field list matches the post-ADJ-05 enum (no `mute_until`)
- The ADJ-06 rejection path's audit-event envelope shape (`{kind: 'rejected', field, attempted_new_value, error, source}`)
- The 5-case integration test's fixture names + assertion shapes (so future tests can borrow the pattern)
- Any deviations from D-41-04..D-41-08 (should be zero — auto-mode locked all decisions)
- A `## DEPLOYED` checkbox left unchecked — operator marks it after Proxmox deploy + FR smoke test
</output>
