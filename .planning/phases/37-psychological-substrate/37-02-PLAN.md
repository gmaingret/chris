---
phase: 37
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - src/memory/confidence.ts
  - src/memory/profiles/psychological-shared.ts
  - src/memory/profiles.ts
  - src/memory/profiles/__tests__/psychological-schemas.test.ts
  - src/memory/profiles/__tests__/psychological-shared.test.ts
  - src/memory/profiles/__tests__/psychological-profiles.test.ts
  - src/memory/profiles/__tests__/psych-boundary-audit.test.ts
autonomous: true
requirements: [PSCH-07, PSCH-08, PSCH-09, PSCH-10]
requirements_addressed: [PSCH-07, PSCH-08, PSCH-09, PSCH-10]
must_haves:
  truths:
    - "loadPsychologicalSubstrate('hexaco', now) returns only pensieve_entries with source='telegram' AND (epistemic_tag IS NULL OR epistemic_tag != 'RITUAL_RESPONSE') from the previous calendar month in Europe/Paris timezone — Gmail/Immich/Drive rows provably absent; RITUAL_RESPONSE rows provably absent (Pitfall 3 mitigation: speech-source contamination prevented at SQL WHERE clause, not JS post-filter)"
    - "loadPsychologicalSubstrate returns discriminated union: { belowThreshold: true, wordCount, neededWords } when wordCount < 5000; { belowThreshold: false, corpus, episodicSummaries, wordCount, prevHistorySnapshot } when wordCount >= 5000 — downstream Phase 38 generators MUST narrow on belowThreshold before accessing corpus (TypeScript enforces)"
    - "Word counting uses inline text.trim().split(/\\s+/).filter(s => s.length > 0).length per entry, summed — NOT messages.countTokens (Pitfall 2 mitigation: 5000-word floor not biased against Cyrillic substrates by 1.5–2.5× Russian token inflation)"
    - "Calendar-month boundary computed via Luxon DateTime.fromJSDate(now, { zone: 'Europe/Paris' }).startOf('month').minus({ months: 1 }) — DST-safe; matches Phase 38's '1st of month 09:00 Paris' cron semantics (the previous calendar month)"
    - "MIN_SPEECH_WORDS=5000 and RELATIONAL_WORD_COUNT_THRESHOLD=2000 constants exported from src/memory/confidence.ts; isAboveWordThreshold(wordCount) uses >= (NOT <) per M009 lt→lte lesson — wordCount === 5000 is 'at threshold' → above"
    - "Word-count gate and entry-count gate are INDEPENDENT: isAboveWordThreshold(wordCount) is the only gate consulted by psychological substrate loader; isAboveThreshold(entryCount) from M010 is NOT imported into psychological-shared.ts (per CONTEXT.md D-29 + specifics §5 — composing the two thresholds rejects a valid 5,200-word + 8-entry substrate)"
    - "getPsychologicalProfiles() returns { hexaco, schwartz, attachment } typed structured object; on DB error returns per-profile null + logger.warn (never throws — mirrors M010 D005 fire-and-forget consistency)"
    - "3-layer Zod v3 parse defense in readOnePsychologicalProfile: (1) schema_version not in PSYCHOLOGICAL_PROFILE_SCHEMAS dispatcher → null + warn 'chris.psychological.profile.read.schema_mismatch'; (2) safeParse failure → null + warn 'chris.psychological.profile.read.parse_failed'; (3) any throw → null + warn 'chris.psychological.profile.read.unknown_error' — distinct log namespace from M010's chris.profile.read.* events"
    - "stripPsychologicalMetadataColumns strips id, name, schemaVersion, substrateHash, overallConfidence, wordCount, wordCountAtLastRun, lastUpdated, createdAt, relationalWordCount, activated — Pitfall 7 mitigation (M010's stripMetadataColumns is operational-specific; M011 has 4 extra metadata columns)"
    - "psych-boundary-audit.test.ts enforces D047 in both directions: FAILS if /\\b(jurisdictional|capital|health|family)\\b/ appears in psychological-schemas.ts or psychological-shared.ts; FAILS if /\\b(hexaco|schwartz|attachment)\\b/ appears in operational profile files (profile-prompt.ts, profile-updater.ts, profiles/shared.ts, profiles/schemas.ts, profiles/jurisdictional.ts, profiles/capital.ts, profiles/health.ts, profiles/family.ts) — Pitfall 4 from PITFALLS.md (boundary violations) mitigated at CI time"
    - "Pitfall 6 (audit-test false-positive on comments) accepted: regex is /\\b(hexaco|schwartz|attachment)\\b/ and these tokens unlikely to appear in operational docstrings; remediation = rename offending docstring word, not relax regex"
  artifacts:
    - path: "src/memory/confidence.ts"
      provides: "MIN_SPEECH_WORDS=5000 + RELATIONAL_WORD_COUNT_THRESHOLD=2000 + isAboveWordThreshold(wordCount) appended at end of file (D-29); existing M010 helpers MIN_ENTRIES_THRESHOLD/SATURATION/computeProfileConfidence/isAboveThreshold UNMODIFIED"
      contains: "export const MIN_SPEECH_WORDS = 5000"
      min_lines: 70
    - path: "src/memory/profiles/psychological-shared.ts"
      provides: "loadPsychologicalSubstrate<T>(profileType, now) with discriminated-union return + private countWords helper + Luxon calendar-month boundary + source filter + 5000-word gate + prevHistorySnapshot lookup"
      contains: "export async function loadPsychologicalSubstrate"
      min_lines: 90
    - path: "src/memory/profiles.ts"
      provides: "PsychologicalProfileType type re-export + PsychologicalProfiles interface + PSYCHOLOGICAL_PROFILE_SCHEMAS dispatcher + stripPsychologicalMetadataColumns helper + readOnePsychologicalProfile<T> + getPsychologicalProfiles() — all APPENDED after getOperationalProfiles (line ~199); operational reader UNMODIFIED"
      contains: "export async function getPsychologicalProfiles"
    - path: "src/memory/profiles/__tests__/psychological-schemas.test.ts"
      provides: "Unit tests for Zod v3 + v4 dual schemas — happy path (cold-start NULLs + populated row), boundary tests (score 1.0/5.0 OK, 0.5 reject, 5.5 reject), confidence 0.0/1.0 OK, .strict() rejects unknown keys, v3/v4 lockstep parity"
      min_lines: 90
    - path: "src/memory/profiles/__tests__/psychological-shared.test.ts"
      provides: "Integration tests (real Docker DB) for loadPsychologicalSubstrate: source filter (telegram only, gmail/immich/drive excluded), RITUAL_RESPONSE exclusion, calendar-month boundary, below-threshold branch at 4800 words, above-threshold branch at 5200 words, prevHistorySnapshot retrieval"
      min_lines: 120
    - path: "src/memory/profiles/__tests__/psychological-profiles.test.ts"
      provides: "Unit + integration tests for getPsychologicalProfiles never-throw + 3-layer Zod parse defense (DB connection error → null + warn; schema_version=999 → null + warn schema_mismatch; corrupted jsonb → null + warn parse_failed; populated row → typed parse success)"
      min_lines: 80
    - path: "src/memory/profiles/__tests__/psych-boundary-audit.test.ts"
      provides: "Two-directional regex sweep: operational vocab forbidden in 2 psychological files; psychological vocab forbidden in 8 operational files; self-allowlisted by absence from its own input arrays"
      min_lines: 60
  key_links:
    - from: "src/memory/profiles/psychological-shared.ts"
      to: "src/db/schema.ts"
      via: "imports pensieveEntries, episodicSummaries, profileHistory from db/schema.js (the 3 tables queried)"
      pattern: "from '../../db/schema"
    - from: "src/memory/profiles/psychological-shared.ts"
      to: "src/memory/confidence.ts"
      via: "imports MIN_SPEECH_WORDS for the wordCount < MIN_SPEECH_WORDS gate (D-19)"
      pattern: "from '../confidence"
    - from: "src/memory/profiles.ts"
      to: "src/memory/profiles/psychological-schemas.ts"
      via: "imports HexacoProfileSchemaV3, SchwartzProfileSchemaV3, AttachmentProfileSchemaV3 + types for PSYCHOLOGICAL_PROFILE_SCHEMAS dispatcher (D-24)"
      pattern: "from './profiles/psychological-schemas"
    - from: "src/memory/profiles.ts"
      to: "src/memory/profiles/psychological-shared.ts"
      via: "imports + re-exports PsychologicalProfileType for stable consumer import path (Phase 39+)"
      pattern: "from './profiles/psychological-shared"
    - from: "src/memory/profiles/__tests__/psych-boundary-audit.test.ts"
      to: "src/memory/profiles/psychological-shared.ts"
      via: "fs.readFile + regex sweep — must NOT match /\\b(jurisdictional|capital|health|family)\\b/"
      pattern: "psychological-shared\\.ts"
---

<objective>
Ship the M011 psychological-profile read substrate + boundary defenses on top of Plan 37-01's persistence layer. After this plan: `getPsychologicalProfiles()` is exportable from `src/memory/profiles.ts` with a never-throw contract + 3-layer Zod v3 parse defense; `loadPsychologicalSubstrate(profileType, now)` is exportable from `src/memory/profiles/psychological-shared.ts` with a typed discriminated-union return shape that gates Sonnet calls BEFORE the 5,000-word floor; `psych-boundary-audit.test.ts` enforces D047 in CI; and four test files cover the new code paths.

Purpose: PSCH-07 (substrate loader with source filter + Luxon calendar-month boundary), PSCH-08 (5,000-word floor + word counting + word_count_at_last_run persistence), PSCH-09 (typed never-throw reader), PSCH-10 (boundary audit). The confidence helpers (`MIN_SPEECH_WORDS`, `RELATIONAL_WORD_COUNT_THRESHOLD`, `isAboveWordThreshold`) are Phase 37 deliverables per D-29 — they ship in this plan because they're substrate, not inference, and Phase 38 generators will depend on them.

Output: 1 modified source file (`confidence.ts` append-only), 1 new substrate-loader file (`psychological-shared.ts`), 1 modified reader file (`profiles.ts` append-only after `getOperationalProfiles`), 4 new test files. Zero schema edits, zero migration edits. The pitfall pyramid covered by this plan: Pitfall 2 (sparse-data overconfidence via word-count gate), Pitfall 3 (speech-source contamination via SQL WHERE source='telegram'), Pitfall 4 (psychological-vs-operational boundary violations via regex sweep audit), Pitfall 7 (3-layer Zod parse defense for stripMetadata + schema-version mismatch).

Satisfies: PSCH-07, PSCH-08, PSCH-09, PSCH-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-psychological-substrate/37-CONTEXT.md
@.planning/phases/37-psychological-substrate/37-RESEARCH.md
@.planning/phases/37-psychological-substrate/37-PATTERNS.md
@.planning/phases/37-psychological-substrate/37-VALIDATION.md
@.planning/phases/37-psychological-substrate/37-01-SUMMARY.md
@.planning/research/PITFALLS.md

# Codebase analogs (read before touching the corresponding file)
@src/memory/confidence.ts
@src/memory/profiles.ts
@src/memory/profiles/shared.ts
@src/memory/profiles/schemas.ts
@src/memory/profiles/__tests__/schemas.test.ts
@src/memory/profiles/__tests__/shared.test.ts
@src/pensieve/retrieve.ts
@src/chris/__tests__/boundary-audit.test.ts

# Phase 33 plan precedent (exact structural mirror for the substrate/reader split)
@.planning/milestones/v2.5-phases/33-profile-substrate/33-02-PLAN.md

<interfaces>
<!-- Interfaces shipped by Plan 37-01 — these are AVAILABLE at the start of this
     plan. The executor does NOT need to explore the codebase to find their
     definitions; they are documented here verbatim. -->

From src/memory/profiles/psychological-schemas.ts (shipped by Plan 37-01 Task 1):
```typescript
export const HexacoProfileSchemaV3: z.ZodType<HexacoProfileData>;
export const HexacoProfileSchemaV4: zV4.ZodType<unknown>;
export const SchwartzProfileSchemaV3: z.ZodType<SchwartzProfileData>;
export const SchwartzProfileSchemaV4: zV4.ZodType<unknown>;
export const AttachmentProfileSchemaV3: z.ZodType<AttachmentProfileData>;
export const AttachmentProfileSchemaV4: zV4.ZodType<unknown>;
export type HexacoProfileData;
export type SchwartzProfileData;
export type AttachmentProfileData;
```

From src/db/schema.ts (shipped by Plan 37-01 Task 2):
```typescript
export const profileHexaco: PgTable<...>;
export const profileSchwartz: PgTable<...>;
export const profileAttachment: PgTable<...>;
// Plus 4 existing M010 profile tables and profileHistory (unchanged)
```

From src/memory/profiles.ts (existing M010 — INSERT POINT FOR NEW EXPORTS IS AFTER getOperationalProfiles at ~line 199):
```typescript
// Existing — DO NOT MODIFY:
export type Dimension = 'jurisdictional' | 'capital' | 'health' | 'family';  // line 59
export interface ProfileRow<T> {                                              // lines 41-46
  data: T;
  confidence: number;
  lastUpdated: Date;
  schemaVersion: number;
}
const PROFILE_SCHEMAS: Record<Dimension, Record<number, z.ZodTypeAny>>;       // line 83-87
async function readOneProfile<T>(dim, table): Promise<ProfileRow<T> | null>;  // line 100-152
function stripMetadataColumns(row): Record<string, unknown>;                  // line 160-177 (M010 helper)
export async function getOperationalProfiles(): Promise<OperationalProfiles>; // line 191-199
```

From src/memory/profiles/shared.ts (M010 operational substrate loader — analog reference, NOT imported):
```typescript
export async function loadProfileSubstrate(now: Date = new Date()): Promise<ProfileSubstrate>;
// Differences from M011 to mirror:
//   - 60-day rolling window (M010) → calendar-month Luxon boundary (M011)
//   - No source filter (M010) → eq(source, 'telegram') (M011)
//   - inArray(epistemic_tag, PROFILE_SUBSTRATE_TAGS) (M010) → or(isNull, ne(RITUAL_RESPONSE)) (M011)
//   - Counts pensieveEntries.length (M010) → counts words across entry.content (M011)
//   - Single shape return (M010) → discriminated union (M011)
//   - Also queries decisions (M010) → NOT queried (M011 substrate is corpus-only)
```

From src/db/schema.ts:31-46 (epistemic_tag enum values — VERIFIED):
```typescript
// Enum includes 'RITUAL_RESPONSE' — Drizzle ne() accepts the literal.
// Column at line 120 is NULLABLE: epistemicTag: epistemicTagEnum('epistemic_tag') — no .notNull()
```

From src/pensieve/retrieve.ts (analog for never-throw pattern + getEpisodicSummariesRange):
```typescript
export async function getEpisodicSummariesRange(start: Date, end: Date): Promise<EpisodicSummary[]>;
// Same helper M010's loadProfileSubstrate uses; just-in-time call signature.
```

From src/memory/confidence.ts (existing — APPEND-ONLY):
```typescript
export const MIN_ENTRIES_THRESHOLD = 10;  // M010, untouched
export const SATURATION = 50;              // M010, untouched
export function computeProfileConfidence(entries: number, dataConsistency: number): number;
export function isAboveThreshold(entryCount: number): boolean;  // M010, untouched (uses >= MIN_ENTRIES_THRESHOLD)
// ↑ Phase 37 appends MIN_SPEECH_WORDS, RELATIONAL_WORD_COUNT_THRESHOLD, isAboveWordThreshold AFTER these.
```

From src/chris/__tests__/boundary-audit.test.ts (analog for the boundary-audit test — M008 precedent VERIFIED at 127 lines):
- Imports: readFile from 'node:fs/promises', fileURLToPath from 'node:url', dirname/resolve from 'node:path', describe/it/expect from 'vitest'
- PROJECT_ROOT = resolve(__dirname, '..', '..', '..') — depth 3 for M008 at src/chris/__tests__/
- Per-line regex scan via for-loop split on '\n'
- Test file self-allowlisted by absence from its own hardcoded input arrays
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend src/memory/confidence.ts — append MIN_SPEECH_WORDS, RELATIONAL_WORD_COUNT_THRESHOLD, isAboveWordThreshold</name>
  <read_first>
    - src/memory/confidence.ts (entire file — ~66 lines; understand existing M010 helpers MIN_ENTRIES_THRESHOLD, SATURATION, computeProfileConfidence, isAboveThreshold)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-29, D-30 — append-only discipline + WORD_SATURATION explicitly deferred)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/confidence.ts` section — divergences + verbatim append block)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Pitfall 3 line 471-477 — word-count gate must NOT compose with entry-count gate)
  </read_first>
  <files>src/memory/confidence.ts (MODIFIED — append-only at end of file)</files>
  <behavior>
    - Three new exports appended AT END of file: `MIN_SPEECH_WORDS` (constant 5000), `RELATIONAL_WORD_COUNT_THRESHOLD` (constant 2000), `isAboveWordThreshold(wordCount: number): boolean`.
    - Existing M010 helpers (MIN_ENTRIES_THRESHOLD, SATURATION, computeProfileConfidence, isAboveThreshold) UNMODIFIED.
    - `isAboveWordThreshold` uses `>=` semantics per CONTEXT.md specifics ("M009 lt→lte lesson; wordCount === MIN_SPEECH_WORDS is at-threshold → above").
    - JSDoc on each export cites the relevant CONTEXT.md decision and the related PSCH-08 / D028 rationale.
    - No `WORD_SATURATION` constant — explicitly deferred to Phase 38 per D-30.
    - The two threshold systems (entry-count for operational, word-count for psychological) are independent and do NOT compose into a single helper.
  </behavior>
  <action>
    Append three exports at the end of src/memory/confidence.ts (after the existing `isAboveThreshold` function, preserving all existing content + line numbers).

    Export 1: `MIN_SPEECH_WORDS` numeric constant value 5000 with JSDoc citing PSCH-08 floor — psychological profile substrate requires this many words of first-party telegram speech in the previous calendar month before a generator is allowed to fire; locked in 37-CONTEXT.md D-29.

    Export 2: `RELATIONAL_WORD_COUNT_THRESHOLD` numeric constant value 2000 with JSDoc citing D028 attachment activation — `profile_attachment` flips `activated = true` when relational_word_count >= this over a 60-day window; population sweep is post-M011 (deferred per CONTEXT.md).

    Export 3: function `isAboveWordThreshold(wordCount: number): boolean` returning `wordCount >= MIN_SPEECH_WORDS`. JSDoc cites M009 lt→lte lesson — wordCount === MIN_SPEECH_WORDS is "at threshold" → above (NOT below). This is the parallel to `isAboveThreshold(entryCount)` for psychological substrates.

    Do NOT modify any existing M010 export, function body, or type. Do NOT import anything new. Do NOT export a `WORD_SATURATION` constant (deferred to Phase 38 per D-30; Phase 37 only ships the floor).

    Verify file still has the original M010 helpers via grep after the edit.
  </action>
  <acceptance_criteria>
    - `export const MIN_SPEECH_WORDS = 5000` line present exactly once.
    - `export const RELATIONAL_WORD_COUNT_THRESHOLD = 2000` line present exactly once.
    - `export function isAboveWordThreshold(wordCount: number): boolean` defined; body returns `wordCount >= MIN_SPEECH_WORDS` (verify: function body contains `wordCount >= MIN_SPEECH_WORDS` literal).
    - Existing M010 exports UNMODIFIED: `MIN_ENTRIES_THRESHOLD = 10`, `SATURATION = 50`, `computeProfileConfidence`, `isAboveThreshold` all still present (verify each via grep -q).
    - No `WORD_SATURATION` constant introduced (deferred to Phase 38 per D-30): `grep -v "^//" src/memory/confidence.ts | grep -v "^\*" | grep -c "WORD_SATURATION"` returns `0`.
    - `npx tsc --noEmit` produces zero `error TS.*confidence\.ts` lines.
    - JSDoc on each new export references the relevant D-29 / D028 / M009-lt→lte rationale.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^export const MIN_SPEECH_WORDS = 5000" src/memory/confidence.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export const RELATIONAL_WORD_COUNT_THRESHOLD = 2000" src/memory/confidence.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export function isAboveWordThreshold" src/memory/confidence.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export const MIN_ENTRIES_THRESHOLD = 10" src/memory/confidence.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export function isAboveThreshold" src/memory/confidence.ts | grep -q "^1$"</automated>
    <automated>grep -v "^//" src/memory/confidence.ts | grep -v "^\\*" | grep -c "WORD_SATURATION" | grep -q "^0$"</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "confidence\\.ts.*error" && echo FAIL && exit 1 || echo "✓ tsc clean for confidence.ts"</automated>
  </verify>
  <done>
    src/memory/confidence.ts has three new exports appended at end. Existing M010 helpers untouched. tsc --noEmit exits 0. No WORD_SATURATION constant present (deferred per D-30).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/memory/profiles/psychological-shared.ts — loadPsychologicalSubstrate + discriminated union</name>
  <read_first>
    - src/memory/profiles/shared.ts (lines 1-100 — imports + ProfileSubstrate shape; lines 206-254 — loadProfileSubstrate body as the operational analog)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 4 lines 705-811 — verbatim psychological-shared.ts skeleton; Pitfall 3 lines 463-469 — speech-source contamination via SQL WHERE)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles/psychological-shared.ts` section — divergences #1-12 enumerated)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-15..D-20 — file location, signature, source filter, word counting, gate semantics, prevHistorySnapshot)
    - .planning/research/PITFALLS.md (Pitfalls 2 + 3 — sparse-data overconfidence + speech-source contamination)
    - src/db/schema.ts (lines 31-46, 115-131 — epistemicTagEnum values + pensieveEntries column nullability)
  </read_first>
  <files>src/memory/profiles/psychological-shared.ts (NEW FILE)</files>
  <behavior>
    - Exports `PsychologicalProfileType` type union 'hexaco' | 'schwartz' | 'attachment'.
    - Exports `PsychologicalSubstrate<T>` discriminated union with two branches keyed on `belowThreshold: true | false`.
    - Exports async function `loadPsychologicalSubstrate<T = unknown>(profileType: PsychologicalProfileType, now: Date = new Date()): Promise<PsychologicalSubstrate<T>>`.
    - Private helper `countWords(text)` — inline whitespace-split count; NOT exported (per CONTEXT.md specifics §4).
    - Private constant `PROFILE_TYPE_TO_TABLE_NAME: Record<PsychologicalProfileType, string>` mapping each profile type to its discriminator value for the profile_history query ('hexaco' → 'profile_hexaco', etc.).
    - Query builds: pensieveEntries SELECT with `eq(source, 'telegram')`, `or(isNull(epistemicTag), ne(epistemicTag, 'RITUAL_RESPONSE'))`, `isNull(deletedAt)`, `gte(createdAt, windowStart)`, `lte(createdAt, windowEnd)`. Calendar-month boundary from Luxon DateTime.fromJSDate(now, { zone: 'Europe/Paris' }).startOf('month').minus({ months: 1 }) → start; .endOf('month') → end.
    - Word count computed inline by summing countWords(entry.content) across corpus. Gate: `if (wordCount < MIN_SPEECH_WORDS) return { belowThreshold: true, wordCount, neededWords: MIN_SPEECH_WORDS - wordCount };`. Uses `<` (strict less-than) so wordCount === 5000 falls into the above-threshold branch (matches isAboveWordThreshold semantics).
    - Above-threshold branch: parallel Promise.all loads episodicSummaries (via getEpisodicSummariesRange) + prevSnapshotRow (db.select from profileHistory where profileTableName equals the mapped value, ordered by recordedAt DESC limit 1). Returns the full above-threshold shape with `prevHistorySnapshot: (prevSnapshotRow[0]?.snapshot as T | undefined) ?? null`.
    - NO calls to computeSubstrateHash (Phase 38 owns that). NO query against decisions table (M010-only).
    - NO import of `isAboveThreshold` from confidence.ts (Pitfall 2 mitigation — entry-count and word-count gates are independent).
  </behavior>
  <action>
    Create `src/memory/profiles/psychological-shared.ts`. Header docstring cites Phase 37 Plan 37-02 Task 2 (PSCH-07 + PSCH-08), 37-CONTEXT.md D-15..D-20, and explicitly lists the M010 divergences (source filter, calendar-month boundary, word counting, discriminated union return).

    Imports: from 'drizzle-orm' → `{ and, desc, eq, gte, isNull, lte, ne, or }`. From 'luxon' → `{ DateTime }`. From '../../db/connection.js' → `{ db }`. From '../../db/schema.js' → `{ pensieveEntries, episodicSummaries, profileHistory }`. From '../../pensieve/retrieve.js' → `{ getEpisodicSummariesRange }`. From '../confidence.js' → `{ MIN_SPEECH_WORDS }`.

    Export `type PsychologicalProfileType = 'hexaco' | 'schwartz' | 'attachment';`.

    Export discriminated-union type `PsychologicalSubstrate<T>` per CONTEXT.md D-16 verbatim: two branches keyed on `belowThreshold: true | false`. Below-threshold branch has `wordCount: number` and `neededWords: number`. Above-threshold branch has `belowThreshold: false`, `corpus: (typeof pensieveEntries.$inferSelect)[]`, `episodicSummaries: (typeof episodicSummaries.$inferSelect)[]`, `wordCount: number`, `prevHistorySnapshot: T | null`.

    Define private constant `const PROFILE_TYPE_TO_TABLE_NAME: Record<PsychologicalProfileType, string> = { hexaco: 'profile_hexaco', schwartz: 'profile_schwartz', attachment: 'profile_attachment' } as const;`.

    Define private function `countWords(text: string): number { return text.trim().split(/\s+/).filter((s) => s.length > 0).length; }`. JSDoc explicitly cites D-18: NOT messages.countTokens (Pitfall 2 — Russian token inflation 1.5–2.5× biases the floor against Cyrillic substrates).

    Export `async function loadPsychologicalSubstrate<T = unknown>(profileType: PsychologicalProfileType, now: Date = new Date()): Promise<PsychologicalSubstrate<T>>`.

    Body: Compute calendar-month boundary. `const nowParis = DateTime.fromJSDate(now, { zone: 'Europe/Paris' });`, `const monthStart = nowParis.startOf('month').minus({ months: 1 });`, `const monthEnd = monthStart.endOf('month');`, `const windowStart = monthStart.toJSDate();`, `const windowEnd = monthEnd.toJSDate();`.

    Query corpus via `db.select().from(pensieveEntries).where(and(eq(pensieveEntries.source, 'telegram'), or(isNull(pensieveEntries.epistemicTag), ne(pensieveEntries.epistemicTag, 'RITUAL_RESPONSE')), isNull(pensieveEntries.deletedAt), gte(pensieveEntries.createdAt, windowStart), lte(pensieveEntries.createdAt, windowEnd)));`.

    Compute `wordCount = corpus.reduce((sum, entry) => sum + countWords(entry.content), 0);`.

    Gate: `if (wordCount < MIN_SPEECH_WORDS) { return { belowThreshold: true, wordCount, neededWords: MIN_SPEECH_WORDS - wordCount }; }`. Use `<` not `<=` (5000 → above-threshold branch).

    Above-threshold: `const [summaries, prevSnapshotRow] = await Promise.all([ getEpisodicSummariesRange(windowStart, windowEnd), db.select().from(profileHistory).where(eq(profileHistory.profileTableName, PROFILE_TYPE_TO_TABLE_NAME[profileType])).orderBy(desc(profileHistory.recordedAt)).limit(1) ]);`. Return `{ belowThreshold: false, corpus, episodicSummaries: summaries, wordCount, prevHistorySnapshot: (prevSnapshotRow[0]?.snapshot as T | undefined) ?? null };`.

    Do NOT import or call computeSubstrateHash (Phase 38 owns hash logic). Do NOT import isAboveThreshold from confidence.ts (Pitfall 2 mitigation — word-count and entry-count gates are independent; composing rejects valid 5,200-word substrates). Do NOT query the decisions table.
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/psychological-shared.ts` exists.
    - Exports `type PsychologicalProfileType = 'hexaco' | 'schwartz' | 'attachment'`.
    - Exports `PsychologicalSubstrate<T>` discriminated union (verify: contains both `belowThreshold: true` and `belowThreshold: false` branches).
    - Exports `async function loadPsychologicalSubstrate<T = unknown>(profileType: PsychologicalProfileType, now: Date = new Date()): Promise<PsychologicalSubstrate<T>>`.
    - Source filter present: `eq(pensieveEntries.source, 'telegram')`.
    - RITUAL_RESPONSE exclusion present: pattern `ne(pensieveEntries.epistemicTag, 'RITUAL_RESPONSE')` AND `isNull(pensieveEntries.epistemicTag)` both appear inside an `or(...)`.
    - Imports `MIN_SPEECH_WORDS` from `../confidence.js`; uses it in the `wordCount < MIN_SPEECH_WORDS` gate.
    - Calendar-month boundary via Luxon `DateTime.fromJSDate(now, { zone: 'Europe/Paris' }).startOf('month').minus({ months: 1 })`.
    - Private `countWords` helper present using `text.trim().split(/\s+/).filter` pattern; NOT exported.
    - Above-threshold branch loads episodicSummaries via `getEpisodicSummariesRange` and prevHistorySnapshot via db.select on profileHistory (ordered by recordedAt DESC limit 1).
    - DOES NOT import `isAboveThreshold` from confidence.ts (Pitfall 2 mitigation): `grep -v "^//" src/memory/profiles/psychological-shared.ts | grep -v "^ \*" | grep -c "isAboveThreshold"` returns `0`.
    - DOES NOT import or call `computeSubstrateHash` (Phase 38 owns hash logic).
    - DOES NOT query the decisions table (M011 substrate is corpus-only).
    - `npx tsc --noEmit` produces zero `error TS.*psychological-shared\.ts` lines.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/memory/profiles/psychological-shared.ts</automated>
    <automated>grep -c "^export async function loadPsychologicalSubstrate" src/memory/profiles/psychological-shared.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export type PsychologicalProfileType" src/memory/profiles/psychological-shared.ts | grep -q "^1$"</automated>
    <automated>grep -c "eq(pensieveEntries.source, 'telegram')" src/memory/profiles/psychological-shared.ts | grep -q "^1$"</automated>
    <automated>grep -c "RITUAL_RESPONSE" src/memory/profiles/psychological-shared.ts | grep -q "^1$"</automated>
    <automated>grep -c "MIN_SPEECH_WORDS" src/memory/profiles/psychological-shared.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -v "^//" src/memory/profiles/psychological-shared.ts | grep -v "^\\*" | grep -c "isAboveThreshold\\|countTokens\\|computeSubstrateHash" | grep -q "^0$"</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "psychological-shared\\.ts.*error" && echo FAIL && exit 1 || echo "✓ tsc clean for psychological-shared.ts"</automated>
  </verify>
  <done>
    File exists at src/memory/profiles/psychological-shared.ts. Exports PsychologicalProfileType, PsychologicalSubstrate<T>, loadPsychologicalSubstrate<T>. Source filter uses eq(source, 'telegram') + or(isNull, ne(RITUAL_RESPONSE)). Word count via inline countWords helper (private). 5000-word gate fires before any episodicSummaries / profileHistory query. tsc --noEmit exits 0. No imports of isAboveThreshold, countTokens, or computeSubstrateHash.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend src/memory/profiles.ts — append PsychologicalProfileType re-export, PsychologicalProfiles interface, PSYCHOLOGICAL_PROFILE_SCHEMAS dispatcher, stripPsychologicalMetadataColumns, readOnePsychologicalProfile, getPsychologicalProfiles</name>
  <read_first>
    - src/memory/profiles.ts (entire file — verify the line of getOperationalProfiles definition (~line 191-199 per RESEARCH.md); understand existing imports at lines 18-36, ProfileRow at lines 41-46, Dimension at line 59, PROFILE_INJECTION_MAP at 70-74, PROFILE_SCHEMAS at 83-87, readOneProfile at 100-152, stripMetadataColumns at 160-177, OperationalProfiles at ~180, getOperationalProfiles at 191-199)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 5 lines 814-923 — verbatim psychological reader skeleton; section "Specific Research Findings §7" lines 1185-1198 — log event names + 3-layer parse defense)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles.ts` section — divergences #1-8 enumerated; Pitfall 7 metadata-column stripping)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-21..D-24 — reader API contract, never-throw, 3-layer parse defense, schema dispatcher)
    - src/memory/profiles/psychological-schemas.ts (created by Plan 37-01 — provides schemas and types this task imports)
    - src/memory/profiles/psychological-shared.ts (created in Task 2 — provides PsychologicalProfileType this task re-exports)
  </read_first>
  <files>src/memory/profiles.ts (MODIFIED — append after getOperationalProfiles)</files>
  <behavior>
    - All new exports added AFTER getOperationalProfiles (~line 199). Operational reader + types + helpers UNMODIFIED.
    - New imports appended to existing import block at top of file: profileHexaco/profileSchwartz/profileAttachment from '../db/schema.js' (added to existing schema import block); HexacoProfileSchemaV3/SchwartzProfileSchemaV3/AttachmentProfileSchemaV3 + type imports from './profiles/psychological-schemas.js'; PsychologicalProfileType from './profiles/psychological-shared.js'.
    - Re-export: `export type { PsychologicalProfileType } from './profiles/psychological-shared.js';` so Phase 39+ consumers have a stable import path from profiles.ts.
    - New interface `PsychologicalProfiles` with three fields: hexaco/schwartz/attachment, each typed `ProfileRow<HexacoProfileData> | null` (etc.). Mirrors `OperationalProfiles` shape.
    - New module-private const `PSYCHOLOGICAL_PROFILE_SCHEMAS: Record<PsychologicalProfileType, Record<number, z.ZodTypeAny>>` with three entries, each mapping schema version 1 to the corresponding V3 schema.
    - New module-private function `stripPsychologicalMetadataColumns(row): Record<string, unknown>` strips 11 metadata columns: id, name, schemaVersion, substrateHash, overallConfidence, wordCount, wordCountAtLastRun, lastUpdated, createdAt, relationalWordCount, activated. Converts Drizzle camelCase keys to snake_case for the surviving jsonb fields (Pitfall 7 mitigation).
    - New async function `readOnePsychologicalProfile<T>(profileType: PsychologicalProfileType, table: PgTable): Promise<ProfileRow<T> | null>` — 3-layer parse defense with distinct log event names per D-23.
    - New export `getPsychologicalProfiles(): Promise<PsychologicalProfiles>` — parallel reader of 3 profiles via Promise.all.
    - Log event names follow chris.psychological.profile.read.{schema_mismatch | parse_failed | unknown_error} namespace (distinct from M010's chris.profile.read.* events).
    - `lastUpdated: row.lastUpdated ?? new Date(0)` coalesces null-last-updated (cold start) to epoch — preserves the ProfileRow<T>.lastUpdated: Date contract while encoding "never run" state.
  </behavior>
  <action>
    Append to existing schema import block (after the four operational profile tables): `profileHexaco`, `profileSchwartz`, `profileAttachment` imports from '../db/schema.js'.

    Add a new import block: `import { HexacoProfileSchemaV3, SchwartzProfileSchemaV3, AttachmentProfileSchemaV3, type HexacoProfileData, type SchwartzProfileData, type AttachmentProfileData } from './profiles/psychological-schemas.js';`.

    Add re-export: `export type { PsychologicalProfileType } from './profiles/psychological-shared.js';`. Phase 39 consumers will import PsychologicalProfileType from `'src/memory/profiles.js'` (stable path).

    Append after getOperationalProfiles (~line 199), in this order:

    Interface `PsychologicalProfiles`: three fields `hexaco: ProfileRow<HexacoProfileData> | null`, `schwartz: ProfileRow<SchwartzProfileData> | null`, `attachment: ProfileRow<AttachmentProfileData> | null`. Export.

    Module-private const `PSYCHOLOGICAL_PROFILE_SCHEMAS: Record<PsychologicalProfileType, Record<number, z.ZodTypeAny>> = { hexaco: { 1: HexacoProfileSchemaV3 }, schwartz: { 1: SchwartzProfileSchemaV3 }, attachment: { 1: AttachmentProfileSchemaV3 } };`. NO export — internal dispatcher only.

    Module-private function `stripPsychologicalMetadataColumns(row: Record<string, any>): Record<string, unknown>`. Destructure with names: `const { id, name, schemaVersion, substrateHash, overallConfidence, wordCount, wordCountAtLastRun, lastUpdated, createdAt, relationalWordCount, activated, ...rest } = row;`. Discard the 11 metadata vars via `void`. Convert remaining keys from camelCase to snake_case via `k.replace(/[A-Z]/g, m => '_' + m.toLowerCase())`. Return the snake-case object. JSDoc explicitly cites Pitfall 7: relationalWordCount + activated are attachment-only but the helper is shared — harmlessly absent on hexaco/schwartz rows.

    Module-private async function `readOnePsychologicalProfile<T>(profileType: PsychologicalProfileType, table: any): Promise<ProfileRow<T> | null>` with try/catch outer wrapping:
    - SELECT row by `eq(table.name, 'primary')` limit 1; if rows.length === 0 return null
    - Layer 1: lookup parser via `PSYCHOLOGICAL_PROFILE_SCHEMAS[profileType][row.schemaVersion as number]`. If !parser → `logger.warn({ profileType, schemaVersion: row.schemaVersion }, 'chris.psychological.profile.read.schema_mismatch'); return null;`
    - Layer 2: `const dataToValidate = stripPsychologicalMetadataColumns(row); const parsed = parser.safeParse(dataToValidate); if (!parsed.success) { logger.warn({ profileType, error: parsed.error.message }, 'chris.psychological.profile.read.parse_failed'); return null; }`
    - Return `{ data: parsed.data as T, confidence: row.overallConfidence, lastUpdated: row.lastUpdated ?? new Date(0), schemaVersion: row.schemaVersion }`
    - Layer 3: outer catch → `logger.warn({ profileType, error: error instanceof Error ? error.message : String(error) }, 'chris.psychological.profile.read.unknown_error'); return null;`

    Export `async function getPsychologicalProfiles(): Promise<PsychologicalProfiles> { const [hexaco, schwartz, attachment] = await Promise.all([ readOnePsychologicalProfile<HexacoProfileData>('hexaco', profileHexaco), readOnePsychologicalProfile<SchwartzProfileData>('schwartz', profileSchwartz), readOnePsychologicalProfile<AttachmentProfileData>('attachment', profileAttachment) ]); return { hexaco, schwartz, attachment }; }`.

    Do NOT modify the existing M010 readOneProfile, stripMetadataColumns, PROFILE_SCHEMAS, PROFILE_INJECTION_MAP, getOperationalProfiles, or formatProfilesForPrompt. Operational reader path stays the canonical type for the 8+ existing call sites per D-21.

    Verify line ordering: new psychological exports appear AFTER getOperationalProfiles, NOT before. Phase 39 will add the parallel `formatPsychologicalProfilesForPrompt`; this plan does not.
  </action>
  <acceptance_criteria>
    - `export type { PsychologicalProfileType } from './profiles/psychological-shared.js';` re-export present.
    - New imports for `profileHexaco/profileSchwartz/profileAttachment` from `'../db/schema.js'`.
    - New imports for `HexacoProfileSchemaV3/SchwartzProfileSchemaV3/AttachmentProfileSchemaV3` + types from `'./profiles/psychological-schemas.js'`.
    - `export interface PsychologicalProfiles` defined with 3 fields (hexaco/schwartz/attachment).
    - Module-private const `PSYCHOLOGICAL_PROFILE_SCHEMAS` defined (not exported).
    - Module-private function `stripPsychologicalMetadataColumns` defined; strips ALL 11 metadata column names (id, name, schemaVersion, substrateHash, overallConfidence, wordCount, wordCountAtLastRun, lastUpdated, createdAt, relationalWordCount, activated).
    - Module-private async function `readOnePsychologicalProfile<T>` defined with try/catch wrapping; 3 layers with distinct log event names.
    - `export async function getPsychologicalProfiles()` defined using `Promise.all` of 3 readOnePsychologicalProfile calls; returns `PsychologicalProfiles`.
    - All 3 log event names present in profiles.ts: `chris.psychological.profile.read.schema_mismatch`, `chris.psychological.profile.read.parse_failed`, `chris.psychological.profile.read.unknown_error`.
    - `row.lastUpdated ?? new Date(0)` coalescence present (cold-start null → epoch).
    - Existing M010 functions UNMODIFIED: `readOneProfile`, `stripMetadataColumns`, `getOperationalProfiles`, `formatProfilesForPrompt` (verify each still grep-matchable).
    - New psych exports appear AFTER `getOperationalProfiles` (verify line order via grep -n).
    - `npx tsc --noEmit` produces zero `error TS.*profiles\.ts` lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^export async function getPsychologicalProfiles" src/memory/profiles.ts | grep -q "^1$"</automated>
    <automated>grep -c "^export interface PsychologicalProfiles" src/memory/profiles.ts | grep -q "^1$"</automated>
    <automated>grep -c "chris\\.psychological\\.profile\\.read\\.\\(schema_mismatch\\|parse_failed\\|unknown_error\\)" src/memory/profiles.ts | xargs -I {} test {} -ge 3</automated>
    <automated>grep -c "PSYCHOLOGICAL_PROFILE_SCHEMAS" src/memory/profiles.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "stripPsychologicalMetadataColumns" src/memory/profiles.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "from './profiles/psychological-shared" src/memory/profiles.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "from './profiles/psychological-schemas" src/memory/profiles.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "export async function getOperationalProfiles" src/memory/profiles.ts | grep -q "^1$"</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "profiles\\.ts.*error" && echo FAIL && exit 1 || echo "✓ tsc clean for profiles.ts"</automated>
  </verify>
  <done>
    src/memory/profiles.ts has new exports (PsychologicalProfiles interface, getPsychologicalProfiles function) appended after getOperationalProfiles. PSYCHOLOGICAL_PROFILE_SCHEMAS dispatcher + stripPsychologicalMetadataColumns + readOnePsychologicalProfile module-private. All three log event names present. PsychologicalProfileType re-exported. tsc --noEmit exits 0. Operational reader path unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create psych-boundary-audit.test.ts — two-directional regex sweep (PSCH-10)</name>
  <read_first>
    - src/chris/__tests__/boundary-audit.test.ts (entire 127-line file — exact M008 analog to mirror)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 6 lines 925-1013 — verbatim psych-boundary-audit skeleton; Pitfall 6 lines 494-498 — false-positive on comments / docstrings accepted; section "Specific Research Findings §8" lines 1200-1210 — explicit array pattern, no glob, depth=4)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles/__tests__/psych-boundary-audit.test.ts` section — divergences #1-9)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-31..D-34 — file location, two-directional sweep, self-allowlist)
  </read_first>
  <files>src/memory/profiles/__tests__/psych-boundary-audit.test.ts (NEW FILE)</files>
  <behavior>
    - Imports describe/it/expect from 'vitest', readFile from 'node:fs/promises', fileURLToPath from 'node:url', dirname/resolve from 'node:path'.
    - PROJECT_ROOT walks UP 4 levels from `src/memory/profiles/__tests__/` to project root (deeper than M008's 3 levels).
    - Helper `findHits(contents, pattern)` returns Array<{line, text}> per-line regex matches; failure messages cite D047.
    - Two regex patterns: `/\b(jurisdictional|capital|health|family)\b/` for operational vocab; `/\b(hexaco|schwartz|attachment)\b/` for psychological vocab.
    - Two file arrays: PSYCHOLOGICAL_FILES = 2 entries (psychological-schemas.ts, psychological-shared.ts); OPERATIONAL_FILES = 8 entries (profile-prompt.ts, profile-updater.ts, profiles/{shared,schemas,jurisdictional,capital,health,family}.ts).
    - profiles.ts EXPLICITLY OMITTED from both arrays — it intentionally contains both vocabularies (per D-21).
    - The test file ITSELF (psych-boundary-audit.test.ts) NOT in either array — self-allowlisted by absence (D-34).
    - Two describe blocks (one per direction), each generating per-file `it` cases via for-loop.
    - Failure assertion: `expect(hits, 'D047 violation: ...').toEqual([])` so the diagnostic message lists every offending line:text.
  </behavior>
  <action>
    Create `src/memory/profiles/__tests__/psych-boundary-audit.test.ts`. Header docstring cites Phase 37 Plan 37-02 Task 4 (PSCH-10), 37-CONTEXT.md D-31..D-34, D047, and Pitfall 6 (false-positive remediation = rename docstring word, not relax regex).

    Imports: `import { describe, it, expect } from 'vitest';`, `import { readFile } from 'node:fs/promises';`, `import { fileURLToPath } from 'node:url';`, `import { dirname, resolve } from 'node:path';`.

    Setup: `const __filename = fileURLToPath(import.meta.url);`, `const __dirname = dirname(__filename);`, `const PROJECT_ROOT = resolve(__dirname, '..', '..', '..', '..');` (depth 4 — verified against test file location).

    Helper `async function readSource(relPath: string): Promise<string> { return readFile(resolve(PROJECT_ROOT, relPath), 'utf8'); }`.

    Helper `function findHits(contents: string, pattern: RegExp): Array<{ line: number; text: string }> { const hits = []; const lines = contents.split('\n'); for (let i = 0; i < lines.length; i++) { const line = lines[i] ?? ''; if (pattern.test(line)) hits.push({ line: i + 1, text: line.trim() }); } return hits; }`.

    Helper `function formatHits(hits): string { return hits.map(h => '  line ' + h.line + ': ' + h.text).join('\n'); }`.

    Constants: `const OPERATIONAL_VOCAB = /\b(jurisdictional|capital|health|family)\b/;`, `const PSYCHOLOGICAL_VOCAB = /\b(hexaco|schwartz|attachment)\b/;`.

    `const PSYCHOLOGICAL_FILES = ['src/memory/profiles/psychological-schemas.ts', 'src/memory/profiles/psychological-shared.ts'];`. Self-allowlist: do NOT include 'src/memory/profiles/__tests__/psych-boundary-audit.test.ts' in this array.

    `const OPERATIONAL_FILES = ['src/memory/profile-prompt.ts', 'src/memory/profile-updater.ts', 'src/memory/profiles/shared.ts', 'src/memory/profiles/schemas.ts', 'src/memory/profiles/jurisdictional.ts', 'src/memory/profiles/capital.ts', 'src/memory/profiles/health.ts', 'src/memory/profiles/family.ts'];`. profiles.ts intentionally omitted (D-21 — contains both vocabs by design).

    `describe('PSCH-10: D047 boundary — operational vocab forbidden in psychological files', () => { for (const file of PSYCHOLOGICAL_FILES) { it(\`\${file} has zero operational-vocabulary references\`, async () => { const src = await readSource(file); const hits = findHits(src, OPERATIONAL_VOCAB); expect(hits, \`D047 violation: \${file} references operational vocabulary at:\\n\` + formatHits(hits) + '\\n\\nPsychological profile files must not reference operational profile dimensions. Cross-reading is permitted via profiles.ts; cross-writing in shared substrate is forbidden.').toEqual([]); }); } });`.

    `describe('PSCH-10: D047 boundary — psychological vocab forbidden in operational files', () => { for (const file of OPERATIONAL_FILES) { it(\`\${file} has zero psychological-vocabulary references\`, async () => { const src = await readSource(file); const hits = findHits(src, PSYCHOLOGICAL_VOCAB); expect(hits, \`D047 violation: \${file} references psychological vocabulary at:\\n\` + formatHits(hits) + '\\n\\nOperational profile files must not reference psychological profile dimensions. Cross-writing in shared substrate is forbidden.').toEqual([]); }); } });`.

    Do NOT add any additional regex patterns or file arrays. Do NOT include profiles.ts in either array. Do NOT include this test file in either array. Pitfall 6 (false positive on comments) is accepted: if a false-positive surfaces in the future, remediation is to rename the offending docstring word in the source file, NOT to relax the regex.
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exists.
    - Imports `readFile` from `'node:fs/promises'`, `fileURLToPath` from `'node:url'`, `dirname/resolve` from `'node:path'`, `describe/it/expect` from `'vitest'`.
    - `PROJECT_ROOT = resolve(__dirname, '..', '..', '..', '..')` — depth 4.
    - Regex constants: `OPERATIONAL_VOCAB = /\b(jurisdictional|capital|health|family)\b/` AND `PSYCHOLOGICAL_VOCAB = /\b(hexaco|schwartz|attachment)\b/`.
    - `PSYCHOLOGICAL_FILES` array has exactly 2 entries (psychological-schemas.ts, psychological-shared.ts).
    - `OPERATIONAL_FILES` array has exactly 8 entries (profile-prompt.ts, profile-updater.ts, profiles/shared.ts, profiles/schemas.ts, profiles/{jurisdictional,capital,health,family}.ts).
    - `src/memory/profiles.ts` is NOT in either array (D-21 — contains both vocabs by design).
    - `psych-boundary-audit.test.ts` itself is NOT in either array (self-allowlist via absence per D-34).
    - Two describe blocks, each generating per-file `it` cases via for-loop.
    - `npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exits 0 with 10 passing tests (2 + 8).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/memory/profiles/__tests__/psych-boundary-audit.test.ts</automated>
    <automated>grep -c "OPERATIONAL_VOCAB" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "PSYCHOLOGICAL_VOCAB" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "PSYCHOLOGICAL_FILES" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "OPERATIONAL_FILES" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "src/memory/profiles\\.ts" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | grep -q "^0$"</automated>
    <automated>grep -c "psych-boundary-audit\\.test\\.ts" src/memory/profiles/__tests__/psych-boundary-audit.test.ts | grep -q "^0$"</automated>
    <automated>npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts 2>&amp;1 | tee /tmp/audit.log; grep -q "10 passed\\|10 tests passed\\|10 pass" /tmp/audit.log || grep -E "Test Files\\s+1 passed" /tmp/audit.log</automated>
  </verify>
  <done>
    File exists at src/memory/profiles/__tests__/psych-boundary-audit.test.ts. Two describe blocks, 2+8 = 10 it cases. profiles.ts and self-test-file both omitted from arrays (intentional self-allowlist). npx vitest run passes — 0 violations across 10 file checks. Pitfall 6 documented in header docstring.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create psychological-schemas.test.ts — Zod v3+v4 dual schema round-trip + boundary tests (PSCH-06)</name>
  <read_first>
    - src/memory/profiles/__tests__/schemas.test.ts (existing M010 analog if present — same structure; otherwise infer from schemas.ts:1-60 pattern)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles/__tests__/psychological-schemas.test.ts` section — divergences #1-6)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 3 lines 624-704 — schema shapes to test against)
    - src/memory/profiles/psychological-schemas.ts (created by Plan 37-01 Task 1 — the schemas being tested)
  </read_first>
  <files>src/memory/profiles/__tests__/psychological-schemas.test.ts (NEW FILE)</files>
  <behavior>
    - Pure unit test — no Docker, no DB, no async setup.
    - Tests cold-start parse (all dims NULL) for HEXACO + Schwartz + Attachment under v3 schemas.
    - Tests populated-row parse (all dims have score/confidence/last_updated) for HEXACO + Schwartz + Attachment under v3 schemas.
    - Boundary tests: score 1.0 OK, score 5.0 OK, score 0.5 reject (below min), score 5.5 reject (above max), confidence 0.0 OK, confidence 1.0 OK, confidence -0.1 reject (below min), confidence 1.1 reject (above max), last_updated valid ISO 8601 OK, last_updated 'not-iso' reject.
    - .strict() tests: unknown top-level key (e.g., extra: 'wat') in HexacoProfileSchemaV3 → reject; unknown per-dim key in nested dimension object → reject.
    - v3/v4 lockstep parity: same valid populated fixture parses under both HexacoProfileSchemaV3 and HexacoProfileSchemaV4 — both .success=true.
    - NO DB calls — runs in microseconds.
  </behavior>
  <action>
    Create `src/memory/profiles/__tests__/psychological-schemas.test.ts`. Header docstring cites Phase 37 Plan 37-02 Task 5 (PSCH-06 coverage).

    Imports: `import { describe, it, expect } from 'vitest';` + named imports for 6 schemas (HexacoProfileSchemaV3/V4, SchwartzProfileSchemaV3/V4, AttachmentProfileSchemaV3/V4) and 3 types (HexacoProfileData, SchwartzProfileData, AttachmentProfileData) from '../psychological-schemas.js'.

    Define cold-start fixtures: `const validHexacoColdStart = { honesty_humility: null, emotionality: null, extraversion: null, agreeableness: null, conscientiousness: null, openness: null };` — all 6 dims null. Similarly `validSchwartzColdStart` with 10 nulls, `validAttachmentColdStart` with 3 nulls.

    Define populated fixtures: `const validHexacoPopulated` with each dim set to `{ score: 3.5, confidence: 0.7, last_updated: '2026-06-01T00:00:00.000Z' }`. Same shape for Schwartz (10 dims) and Attachment (3 dims).

    describe('HexacoProfileSchemaV3 — happy path'): three `it` cases — cold-start parse success (`.safeParse(validHexacoColdStart).success === true`), populated parse success, populated parse output matches input.

    describe('HexacoProfileSchemaV3 — boundary tests'): score 1.0 OK, score 5.0 OK, score 0.5 reject, score 5.5 reject, confidence 0.0 OK, confidence 1.0 OK, confidence -0.1 reject, confidence 1.1 reject, last_updated valid ISO OK, last_updated 'not-iso' reject. Each `it` constructs a fixture mutating exactly one dim's one field and asserts .safeParse().success matches expectation.

    describe('HexacoProfileSchemaV3 — .strict() rejects unknown keys'): two `it` cases — unknown top-level key (e.g., `{ ...validHexacoPopulated, extra: 'wat' }`) → reject; unknown per-dim key (`{ ...validHexacoPopulated, openness: { score: 3, confidence: 0.5, last_updated: '...', extra: 'wat' } }`) → reject.

    describe('HexacoProfileSchemaV4 — lockstep with V3'): cold-start parses successfully under V4; populated parses successfully under V4.

    Repeat the four describe blocks for SchwartzProfileSchemaV3/V4 (10 dims) and AttachmentProfileSchemaV3/V4 (3 dims), parameterizing the fixture variants. To keep the file under ~250 lines, factor a `makeDimensionFixture(score, confidence, lastUpdated)` helper at the top and use it inside the fixture definitions.

    Run-in-isolation invocation comment: `npx vitest run src/memory/profiles/__tests__/psychological-schemas.test.ts`.
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/__tests__/psychological-schemas.test.ts` exists.
    - Imports 6 schemas + 3 types from `'../psychological-schemas.js'`.
    - Cold-start fixture for HEXACO has all 6 dims `null`; for Schwartz all 10 nulls; for Attachment all 3 nulls — each parses successfully under v3.
    - Populated fixture has at least one `{ score, confidence, last_updated }` per dim type — parses successfully under v3.
    - Boundary tests cover: score 1.0 OK, score 5.0 OK, score 0.5 reject, score 5.5 reject, confidence 0.0 OK, confidence 1.0 OK, confidence -0.1 reject, confidence 1.1 reject (verify via grep for the boundary values).
    - `.strict()` rejection tests: unknown top-level key rejected AND unknown per-dim key rejected.
    - v3/v4 lockstep parity: same valid fixture parses success under both schemas.
    - NO DB calls in this file (verify: no `db.select`, no `db.insert`, no docker/postgres imports).
    - `npx vitest run src/memory/profiles/__tests__/psychological-schemas.test.ts` exits 0 with all test cases green.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/memory/profiles/__tests__/psychological-schemas.test.ts</automated>
    <automated>grep -c "HexacoProfileSchemaV3" src/memory/profiles/__tests__/psychological-schemas.test.ts | xargs -I {} test {} -ge 3</automated>
    <automated>grep -c "SchwartzProfileSchemaV3" src/memory/profiles/__tests__/psychological-schemas.test.ts | xargs -I {} test {} -ge 3</automated>
    <automated>grep -c "AttachmentProfileSchemaV3" src/memory/profiles/__tests__/psychological-schemas.test.ts | xargs -I {} test {} -ge 3</automated>
    <automated>grep -c "\\.success" src/memory/profiles/__tests__/psychological-schemas.test.ts | xargs -I {} test {} -ge 10</automated>
    <automated>npx vitest run src/memory/profiles/__tests__/psychological-schemas.test.ts 2>&amp;1 | tee /tmp/schemas-test.log; grep -E "Test Files\\s+1 passed" /tmp/schemas-test.log || grep -E "passed.*failed.*0" /tmp/schemas-test.log</automated>
  </verify>
  <done>
    File exists with cold-start, populated, boundary, .strict(), and v4-lockstep test cases for all three profile types. npx vitest run passes 100%. No DB calls (microsecond-fast).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create psychological-shared.test.ts — real-DB integration tests for loadPsychologicalSubstrate (PSCH-07, PSCH-08)</name>
  <read_first>
    - src/memory/profiles/__tests__/shared.test.ts (lines 1-120 — M010 analog: Docker harness reference, TRUNCATE-CASCADE cleanup, fixture-insert pattern)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles/__tests__/psychological-shared.test.ts` section — divergences #1-7)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 4 + "Open Questions §5" lines 1370-1374 — real DB recommended; Pitfall 2/3 mitigation tests)
    - src/memory/profiles/psychological-shared.ts (created in Task 2 — the function being tested)
    - src/db/schema.ts (pensieveEntries, episodicSummaries, profileHistory definitions for fixture inserts)
  </read_first>
  <files>src/memory/profiles/__tests__/psychological-shared.test.ts (NEW FILE)</files>
  <behavior>
    - Real Docker Postgres integration test (NOT mocked db.select chain — per RESEARCH.md OQ-5 recommendation).
    - TRUNCATE-CASCADE cleanup of pensieve_entries, episodic_summaries, profile_history before each describe block (mirror shared.test.ts:42-51 pattern).
    - Pinned `now` fixture (e.g., `new Date('2026-06-15T12:00:00Z')`) — the previous calendar month boundary in Europe/Paris is May 2026 (DST-safe; Paris is CEST UTC+2 in May, but Luxon handles).
    - Source-filter test: insert 3 pensieve_entries (source='telegram' + non-RITUAL, source='gmail', source='telegram' + epistemic_tag='RITUAL_RESPONSE') all in May 2026; assert loadPsychologicalSubstrate('hexaco', pinnedNow) returns corpus.length === 1 (only the telegram + non-RITUAL row).
    - Below-threshold test: insert telegram rows summing to 4800 words in May 2026; assert return is `{ belowThreshold: true, wordCount: 4800, neededWords: 200 }`.
    - Above-threshold test: insert telegram rows summing to 5200 words in May 2026; assert return is `{ belowThreshold: false, corpus: [...], wordCount: 5200, episodicSummaries: [...], prevHistorySnapshot: null }`.
    - Word-count accuracy test: insert telegram row with known content `'word '.repeat(5001).trim()` (exactly 5001 words after trim+split); assert wordCount === 5001 and belowThreshold === false.
    - Boundary-day test: insert row at start-of-May vs end-of-May Paris → both included; insert row at end-of-April Paris → excluded; insert row at start-of-June Paris → excluded.
    - prevHistorySnapshot test: insert profile_history row with profileTableName='profile_hexaco' + a known snapshot jsonb, assert loadPsychologicalSubstrate('hexaco', pinnedNow) returns that snapshot in the above-threshold branch.
    - Pitfall 2 mitigation explicit: assert that the word-count gate uses words NOT tokens (insert a Russian-text row at 4500 words — that's ~10000 tokens; assert gate returns belowThreshold:true since 4500 < 5000).
    - Pitfall 3 mitigation explicit: assert that loadPsychologicalSubstrate never returns a Gmail row (insert gmail row at 6000 words; without the source filter, this would push above threshold).
  </behavior>
  <action>
    Create `src/memory/profiles/__tests__/psychological-shared.test.ts`. Header docstring cites Phase 37 Plan 37-02 Task 6 (PSCH-07 + PSCH-08), real-DB integration discipline, and Pitfall 2 + Pitfall 3 mitigation tests.

    Imports: vitest (describe, it, expect, beforeEach, afterEach), drizzle-orm sql for TRUNCATE, db from '../../../db/connection.js', pensieveEntries + episodicSummaries + profileHistory from '../../../db/schema.js', loadPsychologicalSubstrate from '../psychological-shared.js'.

    Define `cleanupTables` async helper: TRUNCATE TABLE pensieve_entries CASCADE; TRUNCATE TABLE episodic_summaries CASCADE; TRUNCATE TABLE profile_history CASCADE (CRITICAL addition vs M010 — M011 substrate loader reads profile_history).

    Use beforeEach to call cleanupTables.

    describe('loadPsychologicalSubstrate — source filter correctness (PSCH-07; Pitfall 3 mitigation)'): pinnedNow = new Date('2026-06-15T12:00:00Z'); insert 3 fixture rows all dated 2026-05-15 (within previous calendar month in Paris): (a) source='telegram', epistemic_tag=null, content='word '.repeat(6000); (b) source='gmail', epistemic_tag=null, content='word '.repeat(6000); (c) source='telegram', epistemic_tag='RITUAL_RESPONSE', content='word '.repeat(6000). Invoke loadPsychologicalSubstrate('hexaco', pinnedNow). Assert above-threshold branch. Assert corpus.length === 1. Assert corpus[0].source === 'telegram'. Assert corpus[0].epistemicTag !== 'RITUAL_RESPONSE'.

    describe('loadPsychologicalSubstrate — below-threshold branch (PSCH-08)'): insert telegram rows summing to 4800 words in May 2026. Invoke. Assert return.belowThreshold === true. Assert return.wordCount === 4800. Assert return.neededWords === 200.

    describe('loadPsychologicalSubstrate — above-threshold branch (PSCH-08)'): insert telegram rows summing to 5200 words. Assert belowThreshold === false; wordCount === 5200; corpus.length > 0; prevHistorySnapshot === null (no profile_history row inserted in this case).

    describe('loadPsychologicalSubstrate — word-count accuracy (PSCH-08; Pitfall 2 mitigation)'): insert single row with content = 'word '.repeat(5001).trim(). Assert wordCount === 5001. Then insert single row with Russian content (~4500 words; e.g., 'слово '.repeat(4500).trim()). Assert wordCount === 4500. Assert belowThreshold === true (NOT biased by Russian token inflation).

    describe('loadPsychologicalSubstrate — calendar-month boundary (DST-safe)'): insert telegram row at exactly May 1 00:00 Paris (April 30 22:00 UTC) with 5200 words → included; insert row at May 31 23:59 Paris (May 31 21:59 UTC) → included; insert row at April 30 23:59 Paris → excluded; insert row at June 1 00:01 Paris → excluded. Pin pinnedNow at June 15. Assert corpus.length === 2 (only the two May rows).

    describe('loadPsychologicalSubstrate — prevHistorySnapshot lookup'): insert telegram rows summing to 5200 words. Then INSERT INTO profile_history (profile_table_name, profile_id, snapshot) VALUES ('profile_hexaco', '<uuid>', '{"foo": "bar"}'::jsonb). Invoke loadPsychologicalSubstrate('hexaco', pinnedNow). Assert prevHistorySnapshot === { foo: 'bar' }.

    Helper to insert a pensieve_entry: provide source, epistemic_tag, content, created_at (in UTC). Use db.insert(pensieveEntries).values({...}).

    Run-in-isolation comment: `bash scripts/test.sh src/memory/profiles/__tests__/psychological-shared.test.ts` (full Docker harness REQUIRED).
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/__tests__/psychological-shared.test.ts` exists.
    - Real Docker Postgres integration test (imports `db` from `connection.js`, NOT vi.mock on db).
    - `cleanupTables` helper TRUNCATEs `pensieve_entries`, `episodic_summaries`, AND `profile_history` (CRITICAL: profile_history must be in the truncate list; M011 reads it).
    - At least 5 describe blocks: source filter correctness, below-threshold, above-threshold, word-count accuracy (EN + RU), calendar-month boundary, prevHistorySnapshot.
    - Source-filter test inserts at least 3 fixture rows (one gmail, one telegram+RITUAL_RESPONSE, one telegram+null) and asserts corpus.length === 1.
    - Below-threshold test asserts return shape `{ belowThreshold: true, wordCount: 4800, neededWords: 200 }`.
    - Above-threshold test asserts `belowThreshold: false`, wordCount ≥ 5000, prevHistorySnapshot defined.
    - Russian fixture test inserts `'слово '.repeat(4500)` and asserts wordCount === 4500 AND belowThreshold === true (Pitfall 2 mitigation).
    - Calendar-month boundary test asserts April-end + June-start rows EXCLUDED; May rows INCLUDED.
    - prevHistorySnapshot test inserts profile_history row with profileTableName='profile_hexaco' and asserts the snapshot returns.
    - `bash scripts/test.sh` runs this test against real Docker postgres and all describe blocks pass.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/memory/profiles/__tests__/psychological-shared.test.ts</automated>
    <automated>grep -c "describe(" src/memory/profiles/__tests__/psychological-shared.test.ts | xargs -I {} test {} -ge 5</automated>
    <automated>grep -c "TRUNCATE TABLE profile_history" src/memory/profiles/__tests__/psychological-shared.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "source: 'gmail'\\|source: 'telegram'" src/memory/profiles/__tests__/psychological-shared.test.ts | xargs -I {} test {} -ge 2</automated>
    <automated>grep -c "RITUAL_RESPONSE" src/memory/profiles/__tests__/psychological-shared.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "belowThreshold" src/memory/profiles/__tests__/psychological-shared.test.ts | xargs -I {} test {} -ge 4</automated>
    <automated>bash scripts/test.sh 2>&amp;1 | tee /tmp/full-test.log; grep -q "psychological-shared\\.test\\.ts" /tmp/full-test.log; grep -E "Test Files.*passed|psychological-shared.*passed" /tmp/full-test.log</automated>
  </verify>
  <done>
    File exists with 5+ describe blocks covering source filter, below-threshold, above-threshold, word-count accuracy (EN + RU), calendar-month boundary, prevHistorySnapshot. bash scripts/test.sh executes the test against real Docker postgres with all assertions green. Pitfall 2 + Pitfall 3 mitigations explicitly tested.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Create psychological-profiles.test.ts — getPsychologicalProfiles never-throw + 3-layer parse defense (PSCH-09)</name>
  <read_first>
    - src/memory/profiles.ts (lines 100-152 — readOneProfile + 3-layer parse defense; lines 191-199 — getOperationalProfiles parallel reader)
    - src/memory/__tests__/relational.test.ts (lines 1-50 if present — vi.hoisted DB mock chain pattern) or any vitest test file showing `vi.hoisted` + DB-error mocking
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (section "Specific Research Findings §7" lines 1185-1198 — distinct log event namespace)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles.ts` divergence #4 — distinct log event names)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-22, D-23 — never-throw + 3-layer defense semantics)
  </read_first>
  <files>src/memory/profiles/__tests__/psychological-profiles.test.ts (NEW FILE)</files>
  <behavior>
    - Mix of unit tests (mocked db connection failure → null + warn) and real-DB integration tests (happy path against the migration 0013 seed rows).
    - 3-layer parse defense coverage: Layer 1 (schema_version=999 in stored row → null + warn 'chris.psychological.profile.read.schema_mismatch'); Layer 2 (corrupted jsonb e.g., `'{"score": "not-a-number"}'` → null + warn 'chris.psychological.profile.read.parse_failed'); Layer 3 (db.select throws → null + warn 'chris.psychological.profile.read.unknown_error').
    - Happy path: after migration 0013 applies, getPsychologicalProfiles() returns 3 non-null profiles (each with all dims null since seed rows have 'null'::jsonb defaults; this MUST round-trip through the v3 .nullable() factory successfully).
    - Per-profile isolation: if hexaco row has schema_version=999 but schwartz + attachment have schema_version=1, getPsychologicalProfiles() returns { hexaco: null, schwartz: ProfileRow<...>, attachment: ProfileRow<...> } — partial failures don't cascade.
    - DB error simulation: stub `db.select` to throw mid-call; getPsychologicalProfiles() returns { hexaco: null, schwartz: null, attachment: null } (all three null; never throws); logger.warn called 3 times with 'chris.psychological.profile.read.unknown_error' event name.
  </behavior>
  <action>
    Create `src/memory/profiles/__tests__/psychological-profiles.test.ts`. Header docstring cites Phase 37 Plan 37-02 Task 7 (PSCH-09 coverage), never-throw contract, 3-layer Zod parse defense.

    Imports: vitest (describe, it, expect, vi, beforeEach, afterEach), db + drizzle from '../../../db/connection.js', profileHexaco/profileSchwartz/profileAttachment from '../../../db/schema.js', getPsychologicalProfiles from '../../profiles.js', logger from '../../../utils/logger.js' (or wherever logger lives in this codebase).

    Use vi.spyOn(logger, 'warn') to capture warning calls. After each test: vi.restoreAllMocks().

    describe('getPsychologicalProfiles — happy path (real DB seed rows from migration 0013)'): assume migration 0013 has been applied via test.sh; query directly. Invoke getPsychologicalProfiles(). Assert result.hexaco !== null. Assert result.hexaco.data.honesty_humility === null (cold-start). Assert result.hexaco.confidence === 0 (overall_confidence seed value). Assert result.hexaco.schemaVersion === 1. Repeat for schwartz + attachment.

    describe('getPsychologicalProfiles — Layer 1 schema_version mismatch'): UPDATE profile_hexaco SET schema_version = 999 WHERE name = 'primary'. Invoke getPsychologicalProfiles(). Assert result.hexaco === null. Assert result.schwartz !== null AND result.attachment !== null (per-profile isolation). Assert logger.warn called with event 'chris.psychological.profile.read.schema_mismatch' and { profileType: 'hexaco', schemaVersion: 999 }. Cleanup: UPDATE profile_hexaco SET schema_version = 1.

    describe('getPsychologicalProfiles — Layer 2 safeParse failure on corrupted jsonb'): UPDATE profile_hexaco SET honesty_humility = '{"score": "not-a-number", "confidence": 0.5, "last_updated": "2026-06-01T00:00:00.000Z"}'::jsonb. Invoke. Assert result.hexaco === null. Assert logger.warn called with event 'chris.psychological.profile.read.parse_failed'. Cleanup: UPDATE profile_hexaco SET honesty_humility = 'null'::jsonb.

    describe('getPsychologicalProfiles — Layer 3 unknown throw (mocked DB failure)'): use vi.spyOn to make `db.select` throw `new Error('connection refused')`. Invoke getPsychologicalProfiles(). Assert result.hexaco === null AND result.schwartz === null AND result.attachment === null. Assert logger.warn called 3 times (one per profile read attempt) with event 'chris.psychological.profile.read.unknown_error' and message including 'connection refused'.

    describe('getPsychologicalProfiles — never throws'): wrap each describe-block invocation in `try { ... } catch (e) { expect.fail('getPsychologicalProfiles threw: ' + e); }`. Confirms the never-throw contract holds across all three layers.

    Run-in-isolation comment: `bash scripts/test.sh src/memory/profiles/__tests__/psychological-profiles.test.ts`.
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/__tests__/psychological-profiles.test.ts` exists.
    - Imports `vi` from `'vitest'` for mocking; uses `vi.spyOn(logger, 'warn')` to capture warning calls.
    - At least 4 describe blocks: happy path (real DB cold-start), Layer 1 (schema_version mismatch), Layer 2 (corrupted jsonb), Layer 3 (DB connection throw), never-throws contract.
    - Happy-path test asserts result.hexaco.data.honesty_humility === null (cold-start) AND result.hexaco.confidence === 0 AND result.hexaco.schemaVersion === 1; same for schwartz + attachment.
    - Layer 1 test UPDATEs schema_version=999 on profile_hexaco, asserts result.hexaco === null AND result.schwartz + result.attachment still parse (per-profile isolation), AND logger.warn called with event 'chris.psychological.profile.read.schema_mismatch'.
    - Layer 2 test UPDATEs honesty_humility to corrupted jsonb (e.g., score as string), asserts result.hexaco === null AND logger.warn called with event 'chris.psychological.profile.read.parse_failed'.
    - Layer 3 test stubs `db.select` to throw, asserts all 3 results null AND logger.warn called 3 times with event 'chris.psychological.profile.read.unknown_error'.
    - Each describe block wrapped in try/catch with `expect.fail('threw: ...')` to enforce never-throw contract.
    - `bash scripts/test.sh` runs this test against real Docker postgres with all assertions green.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/memory/profiles/__tests__/psychological-profiles.test.ts</automated>
    <automated>grep -c "chris\\.psychological\\.profile\\.read\\.schema_mismatch" src/memory/profiles/__tests__/psychological-profiles.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "chris\\.psychological\\.profile\\.read\\.parse_failed" src/memory/profiles/__tests__/psychological-profiles.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "chris\\.psychological\\.profile\\.read\\.unknown_error" src/memory/profiles/__tests__/psychological-profiles.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>grep -c "vi\\.spyOn\\|vi\\.fn\\|vi\\.mock" src/memory/profiles/__tests__/psychological-profiles.test.ts | xargs -I {} test {} -ge 1</automated>
    <automated>bash scripts/test.sh 2>&amp;1 | tee /tmp/full-test.log; grep -q "psychological-profiles\\.test\\.ts" /tmp/full-test.log; grep -E "Test Files.*passed|psychological-profiles.*passed" /tmp/full-test.log</automated>
  </verify>
  <done>
    File exists with happy-path (real DB, cold-start parse success), 3-layer parse defense (mismatch + parse_failed + unknown_error), per-profile isolation, never-throw test cases. bash scripts/test.sh executes against real Docker postgres with all assertions green. logger.warn spies confirm exact event-name discipline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db.select → row jsonb | Untyped jsonb data crosses into the application; Zod v3 .safeParse at read boundary is the only validation. 3-layer parse defense catches corruption + schema drift. |
| pensieveEntries.source / epistemic_tag → substrate corpus | Filter via SQL WHERE (not JS post-filter) — Drizzle parameterized eq/ne — prevents speech-source contamination (Pitfall 3). |
| psychological-shared.ts → confidence.ts | Only MIN_SPEECH_WORDS imported; isAboveThreshold (entry-count) NOT imported to prevent gate composition error (Pitfall 2). |
| psychological-*.ts ↔ operational profiles/*.ts | Both directions enforced by psych-boundary-audit.test.ts regex sweep (D047). |
| logger.warn → application observability | Distinct event namespace chris.psychological.profile.read.* (vs M010's chris.profile.read.*) enables future log filtering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering | jsonb dim columns at read boundary | mitigate | 3-layer Zod v3 parse defense: schema_version mismatch → null + schema_mismatch warn; safeParse failure → null + parse_failed warn; outer catch → null + unknown_error warn. Each layer returns null per-profile so Promise.all aggregates 3 nullable results — partial failures don't cascade. |
| T-37-10 | Information Disclosure | substrate corpus speech sources | mitigate | SQL WHERE source='telegram' AND epistemic_tag != 'RITUAL_RESPONSE' (drizzle parameterized eq/ne) — prevents Gmail/Immich/Drive leakage at the query layer. Verified by Task 6 integration test inserting all 3 sources and asserting corpus.length === 1 (telegram only). |
| T-37-11 | Tampering | psychological/operational vocabulary cross-pollution | mitigate | psych-boundary-audit.test.ts regex sweep both directions; fast (<1s) vitest unit test runs in every `bash scripts/test.sh` invocation. Pitfall 4 from PITFALLS.md. |
| T-37-12 | Denial of Service | wordCount on giant pensieve content | accept | Telegram message size limits cap individual entry content; calendar-month window caps corpus size to ~30 days × max_msgs_per_day; inline countWords is O(n) on a small bounded n. |
| T-37-13 | Tampering | Pitfall 2 (sparse-data overconfidence — gate composition error) | mitigate | psychological-shared.ts explicitly does NOT import isAboveThreshold from confidence.ts. wordCount and entryCount gates are independent. Task 2 verify automation grep-asserts the absence of `isAboveThreshold` import. |
| T-37-14 | Information Disclosure | Logger warn payloads | mitigate | logger.warn payloads include profileType + error.message ONLY; never include the row data, schema_version values are integers (no PII), error.message is from Zod or DB driver (no Greg-content). No PII in log namespace. |
| T-37-15 | Tampering | Pitfall 7 (stripPsychologicalMetadataColumns misses new columns) | mitigate | The helper explicitly destructures all 11 metadata column names (D-21 verbatim list); attachment-only columns (relationalWordCount, activated) are harmlessly absent on hexaco/schwartz rows. Tested by Task 7 happy-path test (cold-start parse success across all 3 profile types). |
| T-37-16 | Denial of Service | Pitfall 6 (boundary-audit false positive on comments) | accept | Locked vocab list minimizes false-positive risk; if one surfaces in the future, remediation = rename offending docstring word, NOT relax regex. Documented in test file header. |
</threat_model>

<verification>
After all 7 tasks complete:

1. **TypeScript clean:** `npx tsc --noEmit` exits 0 with no errors in confidence.ts, psychological-shared.ts, profiles.ts, or any new test file.
2. **Boundary audit green:** `npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exits 0 with 10 passing tests (2 psychological-file checks + 8 operational-file checks).
3. **Zod schemas tested:** `npx vitest run src/memory/profiles/__tests__/psychological-schemas.test.ts` exits 0 with all 30+ cases passing (microseconds).
4. **Substrate loader integration green:** `bash scripts/test.sh` runs psychological-shared.test.ts against real Docker postgres; all 5+ describe blocks pass (source filter, below-threshold, above-threshold, word-count accuracy EN/RU, calendar-month boundary, prevHistorySnapshot).
5. **Reader never-throws:** `bash scripts/test.sh` runs psychological-profiles.test.ts; happy-path against migration 0013 seed rows + 3-layer parse defense (mismatch + parse_failed + unknown_error) + per-profile isolation + DB-error-throws-and-returns-3-nulls — all green.
6. **Pitfall sweep:** Pitfalls 2 (sparse-data overconfidence), 3 (speech-source contamination), 4 (D047 vocab leak), 6 (boundary-audit false positive accepted), 7 (stripMetadata coverage) all mitigated by artifacts above.
7. **Phase 37 complete:** All 10 PSCH requirements (PSCH-01..10) satisfied across Plans 37-01 + 37-02.
</verification>

<success_criteria>
Plan 37-02 is complete when:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `bash scripts/test.sh` exits 0 on fresh Docker postgres with all 4 new test files green (psych-boundary-audit, psychological-schemas, psychological-shared, psychological-profiles)
- [ ] PSCH-07 satisfied: loadPsychologicalSubstrate filters source='telegram' AND excludes RITUAL_RESPONSE; episodic summaries scoped to previous calendar month in Europe/Paris (Luxon DST-safe)
- [ ] PSCH-08 satisfied: 5000-word floor enforced BEFORE Sonnet call (discriminated union belowThreshold branch); inline whitespace word counting (NOT countTokens — verified by Russian fixture)
- [ ] PSCH-09 satisfied: getPsychologicalProfiles() returns typed structured object with never-throw contract; 3-layer Zod v3 parse defense with distinct log event namespace
- [ ] PSCH-10 satisfied: psych-boundary-audit.test.ts enforces D047 both directions; self-allowlisted by absence from its own arrays
- [ ] confidence.ts append-only edit; existing M010 helpers untouched
- [ ] profiles.ts append-only edit; existing M010 reader (getOperationalProfiles) untouched
- [ ] All 4 Pitfall mitigations verified by tests: Pitfall 2 (gate composition error), Pitfall 3 (speech-source contamination), Pitfall 4 (D047 boundary), Pitfall 7 (stripMetadata coverage)
</success_criteria>

<output>
After completion, create `.planning/phases/37-psychological-substrate/37-02-SUMMARY.md` documenting:
- 7 files modified/created (confidence.ts, psychological-shared.ts, profiles.ts, 4 test files)
- Test pass output snippet (npx vitest run for each new test file)
- bash scripts/test.sh log snippet showing migration 0013 smoke gate + all 4 new test files green
- Distinct log event names confirmed via grep
- Boundary-audit coverage matrix (10 file checks, 2+8 split)
- Phase 37 close-out: all 10 PSCH requirements satisfied; handoff to Phase 38 (psychological-shared.ts + getPsychologicalProfiles + confidence helpers now importable)
- Status of all 4 Pitfall mitigations (2/3/4/6/7 — explicitly verified by which test cases)
</output>
