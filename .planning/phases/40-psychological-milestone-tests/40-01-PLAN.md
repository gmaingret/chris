---
phase: 40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/synthesize-delta.ts
  - scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts
  - src/__tests__/fixtures/chat-ids.ts
  - src/__tests__/fixtures/seed-psych-profile-rows.ts
  - src/__tests__/fixtures/primed-sanity-m011.test.ts
  - src/memory/profiles/__tests__/integration-m011-1000words.test.ts
  - src/memory/profiles/__tests__/integration-m011-30days.test.ts
  - tests/fixtures/primed/m011-30days/
  - tests/fixtures/primed/m011-1000words/
autonomous: false
requirements: [PMT-01, PMT-02, PMT-03, PMT-04, PMT-05]
tags: [m011, psychological-profiles, primed-fixture, integration-test, harn-sanity, unconditional-fire, inverse-m010]

must_haves:
  truths:
    - "`scripts/synthesize-delta.ts` accepts boolean `--psych-profile-bias` flag; when set, every day's Haiku style-transfer prompt receives the designed-signature keyword hint built from `PSYCH_PROFILE_BIAS_KEYWORDS` (D-03/D-05)"
    - "`PSYCH_PROFILE_BIAS_KEYWORDS: readonly string[]` constant exists in `scripts/synthesize-delta.ts` containing keyword nudges across 6 trait categories per D-05 (Openness, Conscientiousness, Honesty-Humility, Self-Direction, Benevolence, Universalism)"
    - "`OPENNESS_SIGNAL_PHRASES: readonly string[]` constant exists in `scripts/synthesize-delta.ts` containing the D-07 canonical 6-phrase signature retention list — load-bearing for the HARN signal-phrase gate (Pitfall §7 — Haiku style-transfer averaging toward Greg's habitual register erases the designed signature)"
    - "When `--psych-profile-bias` is omitted, the Haiku prompt is byte-identical to the pre-change shape (m009-21days + m010 regeneration are byte-stable)"
    - "When BOTH `--profile-bias` (M010) AND `--psych-profile-bias` (M011) are set on the same invocation, the psych hint wins (planner-locked precedence per PATTERNS.md §scripts/synthesize-delta.ts — `psychHint ?? m010Hint`); legacy invocations unaffected"
    - "Operator can regenerate `m011-30days` (D-09 — target-days 30 → ~6,000 telegram words above 5,000 floor with margin) and `m011-1000words` (D-12 — target-days 5 → ~1,000 words below floor) locally via documented `npx tsx scripts/regenerate-primed.ts ...` commands"
    - "HARN sanity gate `primed-sanity-m011.test.ts` (D-06 verbatim — load-bearing for Pitfall §7 / Pitfall 10 PITFALLS.md): for m011-30days asserts (a) total telegram-source pensieve wordCount > 5000, (b) ≥1 OPENNESS_SIGNAL_PHRASES phrase present in synthesized content; for m011-1000words asserts wordCount < 5000 AND wordCount >= 1 (anti-zero); both blocks use the FIXTURE_PRESENT skip-when-absent pattern from M010 precedent"
    - "Shared helper `seedPsychProfileRows()` extracted from Phase 38's inline `cleanupAll()` (RESEARCH source map line 339) idempotently resets `profile_hexaco`, `profile_schwartz`, `profile_attachment` rows to cold-start state AND scopes `profile_history` wipe to `profile_table_name IN ('profile_hexaco', 'profile_schwartz', 'profile_attachment')` so M010 history rows are preserved (cross-milestone decoupling)"
    - "PMT-03 sparse-threshold integration test (`integration-m011-1000words.test.ts`): loading `m011-1000words` + invoking both generators against the discriminated-union substrate asserts (a) `mockAnthropicParse.toHaveBeenCalledTimes(0)`, (b) both outcomes `'skipped_below_threshold'`, (c) 2× `chris.psychological.<profileType>.skipped_below_threshold` log entries, (d) all 3 profile rows preserved at cold-start (`overall_confidence=0`, `word_count < 5000`), (e) zero `profile_history` rows; Pitfall §2 (sparse-data overconfidence) is structurally prevented at the substrate-loader level"
    - "PMT-03 `word_count_at_last_run` assertion reconciled per RESEARCH Open Q4 Option A: assert `wordCountAtLastRun === 0` (matches actual Phase 37/38 short-circuit code path — `runPsychologicalProfileGenerator` returns BEFORE the upsert; D-17 spec was written before Phase 38 implementation reality was inspected; v2.6.1 may add a 'write word_count_at_last_run before short-circuit' code path)"
    - "PMT-04 populated integration test (`integration-m011-30days.test.ts` — same file as PMT-05 per D-22): loading `m011-30days` + Cycle 1 orchestrator call asserts (a) HEXACO row populated with `overall_confidence > 0` + all 6 dim scores non-null + matching designed signature within ±0.8 tolerance per dim (D-21 — Openness ≥ 3.7, Conscientiousness ≥ 3.2, Honesty-Humility ≥ 3.4, emotionality unchecked per D-04), (b) Schwartz row populated with `overall_confidence > 0` + all 10 value scores non-null + within ±0.8 per dim (Self-Direction ≥ 3.7, Benevolence ≥ 3.7, Universalism ≥ 3.5, Conformity ≤ 2.8, Power ≤ 2.8), (c) `profile_history` has exactly 2 new rows (1 per profile_type), (d) `substrate_hash` matches `/^[0-9a-f]{64}$/` on both rows"
    - "PMT-05 three-cycle UNCONDITIONAL-FIRE integration test (same file as PMT-04): Cycle 1 (NOW_C1 = 2026-05-01) asserts `mockAnthropicParse.toHaveBeenCalledTimes(2)`; Cycle 2 (NOW_C2 = 2026-06-01, IDENTICAL substrate, no new pensieve inserts) asserts CUMULATIVE `toHaveBeenCalledTimes(4)` (**NOT 2** — INVERSE of M010 PTEST-03 idempotency); Cycle 3 (NOW_C3 = 2026-07-01, INSERTed 5 new telegram pensieve entries ~250 words each in the previous-month window) asserts cumulative `toHaveBeenCalledTimes(6)`; `profile_history` scales 2→4→6; per-cycle assertion that `substrateHash` matches `/^[0-9a-f]{64}$/` (recorded, not blank — proves hash machinery wired even when not used for skip); all outcomes are `'updated'` across all cycles (the `'skipped_no_change'` enum value MUST NOT exist per Phase 38 `PsychologicalProfileGenerationOutcome`)"
    - "PMT-05 test docblock contains the VERBATIM D-24 INVERSE-OF-M010 comment block (5 lines: 'This test is the INVERSE of M010's PTEST-03 idempotency test. / M010 PTEST-03 asserts hash-skip behavior. M011 PMT-05 asserts UNCONDITIONAL / FIRE (cumulative 4 calls after Cycle 2 with identical substrate). If a future / refactor introduces hash-skip \"for consistency with M010\", this test fails. / Do NOT \"fix\" the test — the divergence is intentional per PGEN-06.')"
    - "PMT-05 defense-in-depth with Phase 38's contract-level three-cycle test (D-25): Phase 38 used INLINE mocked substrate; Phase 40 uses PRIMED FIXTURE — orchestrator + substrate-loader + Drizzle paths exercised end-to-end; both files assert `toHaveBeenCalledTimes(4)` after Cycle 2"
    - "`CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt(99923)` appended to `src/__tests__/fixtures/chat-ids.ts` after the existing `CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922)` (sequential allocation per TESTING.md:105 convention); referenced by Plan 40-02 PMT-06"

  artifacts:
    - path: "scripts/synthesize-delta.ts"
      provides: "`--psych-profile-bias` boolean flag + `PSYCH_PROFILE_BIAS_KEYWORDS` constant + `OPENNESS_SIGNAL_PHRASES` constant + `psychDimensionHintFor(dayIndex, enabled)` helper + parseArgs registration + per-day Haiku prompt-hint append + help text"
      contains: "PSYCH_PROFILE_BIAS_KEYWORDS"
    - path: "scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts"
      provides: "Unit tests for --psych-profile-bias flag acceptance, helper output, hint injection, legacy parity"
    - path: "src/__tests__/fixtures/chat-ids.ts"
      provides: "CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt(99923) constant — consumed by Plan 40-02"
      contains: "CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION"
    - path: "src/__tests__/fixtures/seed-psych-profile-rows.ts"
      provides: "seedPsychProfileRows({ dbOverride? }) — idempotent reset of profile_hexaco + profile_schwartz + profile_attachment cold-start rows + scoped profile_history wipe"
      exports: ["seedPsychProfileRows"]
    - path: "src/__tests__/fixtures/primed-sanity-m011.test.ts"
      provides: "HARN sanity gate — m011-30days (wordCount > 5000 + ≥1 OPENNESS_SIGNAL_PHRASES) + m011-1000words (wordCount < 5000 + wordCount >= 1); skip-when-absent"
    - path: "src/memory/profiles/__tests__/integration-m011-1000words.test.ts"
      provides: "PMT-03 sparse-threshold real-DB integration test — zero Sonnet, both skipped_below_threshold outcomes, all 3 rows preserved"
    - path: "src/memory/profiles/__tests__/integration-m011-30days.test.ts"
      provides: "PMT-04 populated + PMT-05 three-cycle UNCONDITIONAL-FIRE fixture-driven integration tests (same file per D-22)"
    - path: "tests/fixtures/primed/m011-30days/"
      provides: "Operator-regenerated primed fixture — ~6,000 telegram words across 30 days with designed signature + ≥1 OPENNESS_SIGNAL_PHRASES retained"
    - path: "tests/fixtures/primed/m011-1000words/"
      provides: "Operator-regenerated sparse fixture — ~1,000 telegram words across 5 days (below 5,000-word floor)"

  key_links:
    - from: "scripts/synthesize-delta.ts"
      to: "Haiku style-transfer per-day prompt (call site lines 678-691 — see PATTERNS.md §scripts/synthesize-delta.ts)"
      via: "psychDimensionHintFor(d, psychEnabled) returned hint string OR'd via `psychHint ?? m010Hint` precedence"
      pattern: "psychDimensionHintFor\\("
    - from: "src/__tests__/fixtures/primed-sanity-m011.test.ts"
      to: "scripts/synthesize-delta.ts (PSYCH_PROFILE_BIAS_KEYWORDS, OPENNESS_SIGNAL_PHRASES)"
      via: "single-source-of-truth import — NOT duplicated"
      pattern: "from.*synthesize-delta"
    - from: "src/memory/profiles/__tests__/integration-m011-30days.test.ts"
      to: "src/memory/psychological-profile-updater.ts (updateAllPsychologicalProfiles)"
      via: "direct orchestrator invocation in 3-cycle loop after seedPsychProfileRows() reset"
      pattern: "updateAllPsychologicalProfiles\\("
    - from: "src/memory/profiles/__tests__/integration-m011-1000words.test.ts"
      to: "src/__tests__/fixtures/seed-psych-profile-rows.ts (seedPsychProfileRows)"
      via: "beforeEach reset to migration-0013 cold-start state"
      pattern: "seedPsychProfileRows\\(\\)"
---

<objective>
Ship the M011 fixture-driven integration test surface: extend `scripts/synthesize-delta.ts` with the `--psych-profile-bias` boolean flag + designed-signature keyword constants, document fixture regeneration commands for `m011-30days` + `m011-1000words`, extract the `seedPsychProfileRows()` helper from Phase 38's inline `cleanupAll()`, allocate `CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION` for Plan 40-02 consumption, and write 4 test files covering PMT-01 (unit + HARN), PMT-02 (operator-action checkpoint regeneration + HARN presence verification), PMT-03 (sparse threshold), PMT-04 (populated), PMT-05 (three-cycle UNCONDITIONAL FIRE — INVERSE of M010 PTEST-03).

Purpose: mirror Phase 36's 2-plan structure (D-01) — Plan 40-01 ships flag + 2 fixtures + HARN + 3 integration tests in one atomic plan. Splitting them creates synth-pipeline/test mismatch (a bias-keyword change in one plan + the test that depends on the resulting fixture content in another plan). The HARN signal-phrase gate is load-bearing for Pitfall §7 — without it, Haiku style-transfer averaging toward Greg's habitual register can erase the designed signature, causing PMT-04 to fail not because the engine is broken but because the fixture lacks signal.

Output:
- 1 modified CLI script (`scripts/synthesize-delta.ts`)
- 1 new unit test file (`scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts`)
- 1 modified chat-ids registry (`src/__tests__/fixtures/chat-ids.ts`)
- 1 new fixture helper (`src/__tests__/fixtures/seed-psych-profile-rows.ts`)
- 1 new HARN sanity test (`src/__tests__/fixtures/primed-sanity-m011.test.ts`)
- 2 new integration test files (`integration-m011-1000words.test.ts`, `integration-m011-30days.test.ts`)
- 1 operator checkpoint: regenerate `m011-30days` + `m011-1000words` fixtures locally (gitignored — manual command; ~$0.10-0.15 Haiku spend on first run, free thereafter via VCR cache)
- 1 operator checkpoint: run full plan-scope test suite + verify HARN gate green + no Phase 38 regression
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-psychological-milestone-tests/40-CONTEXT.md
@.planning/phases/40-psychological-milestone-tests/40-RESEARCH.md
@.planning/phases/40-psychological-milestone-tests/40-PATTERNS.md
@.planning/phases/40-psychological-milestone-tests/40-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@.planning/phases/37-psychological-substrate/37-02-SUMMARY.md
@.planning/phases/38-psychological-inference-engine/38-02-SUMMARY.md
@.planning/phases/38-psychological-inference-engine/38-03-SUMMARY.md
@.planning/phases/39-psychological-surfaces/39-02-SUMMARY.md
@.planning/milestones/v2.5-phases/36-tests/36-01-PLAN.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase + Phase 37/38/39 outputs. -->

From `src/__tests__/fixtures/chat-ids.ts` (current state — append AFTER line 34):
```typescript
export const CHAT_ID_VAGUE_VALIDATOR_LIVE = BigInt(99920);
export const CHAT_ID_M009_SYNTHETIC_FIXTURE = BigInt(99921);
export const CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922);
// NEW (Plan 40-01 Task 2): export const CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt(99923);
```

From `src/db/schema.ts` (psychological profile tables — Phase 37 migration 0013):
- `profileHexaco` — `name`, `schemaVersion`, `substrateHash`, `overallConfidence`, `wordCount`, `wordCountAtLastRun`, `honestyHumility/emotionality/extraversion/agreeableness/conscientiousness/openness` (jsonb), `lastUpdated`
- `profileSchwartz` — same shape + 10 value jsonb columns (`selfDirection`, `stimulation`, `hedonism`, `achievement`, `power`, `security`, `conformity`, `tradition`, `benevolence`, `universalism`)
- `profileAttachment` — schema-only in M011 with `relationalWordCount`, `activated`; `anxious/avoidant/secure` jsonb columns (NOT exercised by generators per Phase 38 D-23 — deferred to v2.6.1 weekly sweep)
- `profileHistory` — polymorphic snapshot table with `profile_table_name` discriminator column; the `seedPsychProfileRows` helper MUST scope the wipe via `WHERE profile_table_name IN ('profile_hexaco', 'profile_schwartz', 'profile_attachment')` so M010 history rows are preserved

From `src/memory/profiles/psychological-shared.ts` (Phase 37 PSCH-07/08):
```typescript
export type PsychologicalSubstrate =
  | { kind: 'belowThreshold'; wordCount: number; neededWords: number }
  | { kind: 'sufficient'; corpus: PensieveEntry[]; episodicSummaries: EpisodicSummary[]; wordCount: number; prevHistorySnapshot: ProfileHistoryRow | null };

export async function loadPsychologicalSubstrate(
  profileType: 'hexaco' | 'schwartz' | 'attachment',
  now: Date,
): Promise<PsychologicalSubstrate>;
// Filters `pensieve_entries.source='telegram'` AND `epistemic_tag != 'RITUAL_RESPONSE'`

export type PsychologicalProfileGenerationOutcome = 'updated' | 'skipped_below_threshold' | 'error';
// NOTE: 'skipped_no_change' MUST NOT exist (PGEN-06 unconditional fire contract)
```

From `src/memory/profiles/hexaco.ts` + `schwartz.ts` (Phase 38 PGEN-02/03):
```typescript
export async function generateHexacoProfile(opts: { substrate: PsychologicalSubstrate }):
  Promise<{ outcome: PsychologicalProfileGenerationOutcome; ... }>;
export async function generateSchwartzProfile(opts: { substrate: PsychologicalSubstrate }):
  Promise<{ outcome: PsychologicalProfileGenerationOutcome; ... }>;
```

From `src/memory/psychological-profile-updater.ts` (Phase 38 PGEN-04 + PGEN-06):
```typescript
export async function updateAllPsychologicalProfiles(opts?: { now?: Date }):
  Promise<{ hexaco: PromiseSettledResult<...>; schwartz: PromiseSettledResult<...> }>;
// UNCONDITIONAL FIRE — substrate_hash recorded but does NOT short-circuit (contrast M010 GEN-07 hash-skip)
// Inline comment documents the divergence from M010 with rationale per PGEN-06
```

From `src/memory/profiles.ts` (Phase 37 PSCH-09 + Phase 39 PSURF-01/02):
```typescript
export async function getPsychologicalProfiles():
  Promise<{ hexaco: HexacoRow | null; schwartz: SchwartzRow | null; attachment: AttachmentRow | null }>;
export const PSYCHOLOGICAL_PROFILE_INJECTION_MAP: Readonly<Record<Mode, readonly PsychologicalProfileType[]>>;
// REFLECT + PSYCHOLOGY map to ['hexaco', 'schwartz']; COACH explicitly absent
export function formatPsychologicalProfilesForPrompt(map, profiles): string;
// Includes PSYCHOLOGICAL_HARD_RULE_EXTENSION footer when populated
```

From `src/__tests__/fixtures/load-primed.ts` (M008 D041 — unchanged for M011):
```typescript
export async function loadPrimedFixture(name: string, opts?: { dbOverride?: postgres.Sql }): Promise<void>;
// FK-safe loader for the 10 fixture tables — does NOT touch profile_hexaco/schwartz/attachment
// Use seedPsychProfileRows() for that (Plan 40-01 Task 2)
```

From `scripts/synthesize-delta.ts` (existing M010 `--profile-bias` infrastructure — Phase 36 Plan 36-01):
- Lines 47: `import { parseArgs } from 'node:util';`
- Lines 76+: `ENTRIES_PER_DAY`, `PROFILE_BIAS_ROTATION`, `PROFILE_BIAS_KEYWORDS` (the M011 constants are SIBLINGS, not replacements)
- Lines 185-193: `dimensionHintFor(dayIndex, biases)` — M011 adds `psychDimensionHintFor(dayIndex, enabled)` parallel helper
- Lines 197-208: `Args` interface — M011 adds `psychProfileBias?: boolean` field
- Lines 272-287: parseArgs block — M011 adds `'psych-profile-bias': { type: 'boolean', default: false }`
- Lines 684-691: per-day Haiku call site — M011 adds `const psychHint = psychDimensionHintFor(d, psychEnabled); const hint = psychHint ?? m010Hint;`

From `src/__tests__/fixtures/seed-profile-rows.ts` (Phase 36 sibling — shape model for seed-psych-profile-rows.ts):
```typescript
export async function seedProfileRows(opts?: { dbOverride?: postgres.Sql }): Promise<void>;
// Idempotent ON CONFLICT DO UPDATE SET for 4 M010 profile tables; clears profile_history
// M011 SIBLING (NOT a parameterization) — different tables, different cold-start values
```

From `src/memory/__tests__/psychological-profile-updater.integration.test.ts` (Phase 38 contract test — lines 183-226 `cleanupAll()`):
- This body is the seed-psych-profile-rows.ts source — extract verbatim, parameterize the postgres.Sql override, scope profile_history wipe

From `src/memory/profiles/__tests__/integration-m010-30days.test.ts` (Phase 36 — structural mirror for integration-m011-30days.test.ts):
- vi.hoisted block (mockAnthropicParse + mockLoggerInfo/Warn/Error) at top
- vi.mock('../../../llm/client.js', ...) and vi.mock('../../../utils/logger.js', ...)
- FIXTURE_PRESENT skip-when-absent pattern
- beforeAll loads fixture ONCE (amortized) — beforeEach calls seed helper + resets mocks + re-primes router
- Two it() blocks sharing fixture: PTEST-02 populated + PTEST-03 three-cycle (M011 inverts the Cycle 2 assertion semantics — same `toHaveBeenCalledTimes(4)` number, opposite meaning)

From `src/__tests__/fixtures/primed-sanity-m010.test.ts` (verbatim scaffold for primed-sanity-m011.test.ts per D-33):
- Two describe blocks (one per fixture) — each with independent FIXTURE_PRESENT gate
- Single-source-of-truth keyword constant import from synthesize-delta.ts
- console.log skip message naming the regeneration command verbatim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/synthesize-delta.ts with --psych-profile-bias flag + signature constants + unit tests (PMT-01)</name>
  <files>scripts/synthesize-delta.ts, scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts</files>
  <read_first>
    - scripts/synthesize-delta.ts (full file ~834 lines — ranged reads: lines 1-220 for imports + parseArgs + ENTRIES_PER_DAY block; lines 260-350 for parseArgs registration + Args interface; lines 660-770 for per-day Haiku call site)
    - scripts/__tests__/synthesize-delta-profile-bias.test.ts (existing M010 sibling — full file; the new psych test mirrors its scaffold)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md §"--psych-profile-bias flag mechanism" (D-03..D-08 — flag is BOOLEAN not value; signature is SINGLE not 4-way rotation; HARN gate runs in primed-sanity test file)
    - .planning/phases/40-psychological-milestone-tests/40-PATTERNS.md §"scripts/synthesize-delta.ts (MODIFIED)" (lines 25-160 — full diff guide: flat tuple vs Record, helper signature, precedence rule `psychHint ?? m010Hint`, help text)
    - .planning/phases/40-psychological-milestone-tests/40-RESEARCH.md §"Open Questions #1" (lines 700-704 — single-source-of-truth recommendation; co-locate both constants in synthesize-delta.ts and import from sanity test)
    - .planning/research/PITFALLS.md §"Pitfall 10: Synthetic Fixture Design Failures" (lines 344-372 — signal erasure rationale; OPENNESS_SIGNAL_PHRASES is the load-bearing gate against this pitfall)
    - .planning/milestones/v2.5-phases/36-tests/36-01-PLAN.md Task 1 (M010 precedent — same flag-extension scaffold; the M011 task is the SIBLING addition, NOT a replacement)
  </read_first>
  <action>
    Modify `scripts/synthesize-delta.ts` as a sibling extension to the existing `--profile-bias` infrastructure (do NOT replace; M009/M010 regen invocations must remain byte-stable per Pitfall §7 mitigation). Five additive changes:

    (1) **Add two new exported constants** near `ENTRIES_PER_DAY` (around line 76, after `PROFILE_BIAS_KEYWORDS`). `PSYCH_PROFILE_BIAS_KEYWORDS: readonly string[]` is a FLAT tuple (NOT a `Record<Dimension, ...>`) containing keyword nudges across 6 trait categories per D-05 verbatim. `OPENNESS_SIGNAL_PHRASES: readonly string[]` contains the D-07 canonical 6-phrase signature retention list verbatim: `'worth exploring'`, `"I'd be curious"`, `'different angle'`, `'I wonder if'`, `'have you considered'`, `'another perspective'`. Both `as const`. Export both.

    (2) **Add helper function** `psychDimensionHintFor(dayIndex: number, enabled: boolean): string | undefined` matching PATTERNS.md §scripts/synthesize-delta.ts lines 87-95: `if (!enabled) return undefined; void dayIndex; return PSYCH_PROFILE_BIAS_KEYWORDS.join(', ');` (API symmetry with M010's `dimensionHintFor` even though dayIndex is unused — there is ONE signature for the milestone, not a 4-way rotation per D-03).

    (3) **Register parseArgs option** in the existing block (around line 272-287): `'psych-profile-bias': { type: 'boolean', default: false }`. Keep `'profile-bias': { type: 'string', multiple: true }` unchanged. Both flags may co-exist on a single invocation.

    (4) **Extend `Args` interface + parseCliArgs return** (around lines 197-208 and 334-342): add `psychProfileBias?: boolean;` field; pass through from parsed args.

    (5) **Plumb to per-day Haiku call site** (around lines 678-691): add `const psychEnabled = opts.psychProfileBias ?? false;` outside the loop. Inside the per-day loop, compute both hints and apply the planner-locked precedence `psychHint ?? m010Hint`:
        ```typescript
        const psychHint = psychDimensionHintFor(d, psychEnabled);
        const m010Hint = dimensionHintFor(d, profileBias);
        const hint = psychHint ?? m010Hint;
        const systemPrompt = buildHaikuSystemPrompt(fewShot, dayDateStr, ENTRIES_PER_DAY, hint);
        ```
        Document the precedence rule in a one-line comment: "When --psych-profile-bias is set, it's the dominant signature for the milestone (PMT-01 D-03)." Update the help text in `printUsage()` to list `--psych-profile-bias` with a one-line description per PATTERNS.md lines 152-159.

    Write `scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts` as a sibling to the existing `synthesize-delta-profile-bias.test.ts`. Lift the mock scaffold (vi.mock for `cachedMessagesParse`, `autoRefreshIfStale`, `anthropic`, `logger`, `postgres`) from the existing M010 file. Cover 6 cases: (a) `--psych-profile-bias` accepted as boolean (presence = true, absence = false); (b) `psychDimensionHintFor(0, true)` returns the joined `PSYCH_PROFILE_BIAS_KEYWORDS` tuple; (c) `psychDimensionHintFor(0, false)` returns undefined; (d) `psychDimensionHintFor(N, true)` returns the SAME hint for any N (no rotation — D-03 single signature); (e) when both `--profile-bias jurisdictional` AND `--psych-profile-bias` are set, the per-day Haiku prompt receives the psych hint (precedence asserted); (f) when neither flag is set, the Haiku prompt is byte-identical to the pre-change shape (legacy parity — verify via snapshot or by inspecting the systemPrompt argument passed to `cachedMessagesParse`).
  </action>
  <acceptance_criteria>
    - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -c '^export const PSYCH_PROFILE_BIAS_KEYWORDS'` ≥ 1
    - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -c '^export const OPENNESS_SIGNAL_PHRASES'` ≥ 1
    - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -c '^export function psychDimensionHintFor'` ≥ 1
    - `grep -c "'psych-profile-bias': { type: 'boolean'" scripts/synthesize-delta.ts` ≥ 1
    - `grep -c "psychHint ?? m010Hint" scripts/synthesize-delta.ts` ≥ 1 (precedence rule applied at call site)
    - `OPENNESS_SIGNAL_PHRASES` contains exactly the 6 D-07 phrases — verify `node -e "import('./scripts/synthesize-delta.ts').then(m=>{const x=m.OPENNESS_SIGNAL_PHRASES; const want=['worth exploring',\"I'd be curious\",'different angle','I wonder if','have you considered','another perspective']; console.log(want.every(p=>x.includes(p))&&x.length===6?'ok':'FAIL: '+JSON.stringify(x));})"` returns 'ok' (or equivalent vitest assertion in the unit test)
    - `bash scripts/test.sh scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts` exits 0 with all 6 cases green
    - `bash scripts/test.sh scripts/__tests__/synthesize-delta-profile-bias.test.ts scripts/__tests__/synthesize-delta.test.ts` exits 0 (M010 + base test suite still green — legacy parity per D-08 / Pitfall §7 mitigation)
    - `npx tsc --noEmit 2>&1 | grep -E 'synthesize-delta' | wc -l` = 0 (no TS errors introduced)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts scripts/__tests__/synthesize-delta-profile-bias.test.ts scripts/__tests__/synthesize-delta.test.ts</automated>
  </verify>
  <done>The `--psych-profile-bias` boolean flag accepted; PSYCH_PROFILE_BIAS_KEYWORDS + OPENNESS_SIGNAL_PHRASES exported; psychDimensionHintFor helper returns the joined hint when enabled, undefined when disabled; per-day Haiku call site applies psych precedence over m010; M010 regen byte-stable; 6 unit test cases green.</done>
</task>

<task type="auto">
  <name>Task 2: Allocate CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION + create seedPsychProfileRows helper</name>
  <files>src/__tests__/fixtures/chat-ids.ts, src/__tests__/fixtures/seed-psych-profile-rows.ts</files>
  <read_first>
    - src/__tests__/fixtures/chat-ids.ts (full file — last value is `CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922)` on line 34; next sequential is 99923)
    - src/__tests__/fixtures/seed-profile-rows.ts (M010 sibling — model for the public API + optional dbOverride + logger.info pattern; do NOT parameterize, the M011 helper is a sibling per RESEARCH §"profile_attachment column list" note)
    - src/memory/__tests__/psychological-profile-updater.integration.test.ts lines 183-226 (Phase 38 inline `cleanupAll()` — verbatim source body to extract; the wipe + reset logic is correct but is currently duplicated inline; this task DRYs it into a shared helper)
    - src/db/migrations/0013_psychological_substrate.sql (Phase 37 migration — verbatim seed-row INSERT statements for all 3 psychological tables; the cold-start values are: name='primary', schema_version=1, substrate_hash='', overall_confidence=0, word_count=0, word_count_at_last_run=0, all dimension columns NULL/'null'::jsonb; profile_attachment additionally: relational_word_count=0, activated=false)
    - src/db/schema.ts (profileHexaco, profileSchwartz, profileAttachment, profileHistory Drizzle exports — confirm column names + profileTableName discriminator on profileHistory)
    - src/db/connection.ts (db + sql exports — the sql binding from `import { sql as pgSql } from '../../db/connection.js'` is what dbOverride accepts)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md §"Test infrastructure mirror" (D-33 — sanity test mirrors primed-sanity-m010 shape; helper extracted from Phase 38 cleanupAll())
    - .planning/phases/40-psychological-milestone-tests/40-PATTERNS.md §"src/__tests__/fixtures/seed-psych-profile-rows.ts" (lines 336-452 — full helper body with the scoped profile_history wipe and the per-table reset shape)
    - .planning/codebase/TESTING.md (chat-id sequential allocation convention — appender adds the next ID, never reuses)
  </read_first>
  <action>
    **Sub-task A — chat-id allocation.** Append after `CHAT_ID_LIVE_ANTI_HALLUCINATION = BigInt(99922)` (current line 34) in `src/__tests__/fixtures/chat-ids.ts`:
    ```typescript
    /** Phase 40 Plan 02 PMT-06 live 3-of-3 anti-trait-authority test (src/memory/profiles/__tests__/live-psych-anti-hallucination.test.ts). */
    export const CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt(99923);
    ```
    (TESTING.md sequential convention; planning context CONTEXT.md `<canonical_refs>` locks 99923 as the next ID — sibling to 99922 from Phase 36 Plan 36-01).

    **Sub-task B — seed-psych-profile-rows.ts helper.** Create the new file mirroring `seed-profile-rows.ts` SHAPE (NOT parameterization — the M010/M011 helpers are siblings; different tables, different cold-start values per Phase 38 cleanupAll() vs Phase 36 seedProfileRows distinct seed values). Export `seedPsychProfileRows(opts?: { dbOverride?: postgres.Sql }): Promise<void>`. Body extracted from Phase 38's inline `cleanupAll()` at `src/memory/__tests__/psychological-profile-updater.integration.test.ts:183-226` with three critical adaptations:

    1. **SCOPED profile_history wipe** — use `db.delete(profileHistory).where(inArray(profileHistory.profileTableName, ['profile_hexaco', 'profile_schwartz', 'profile_attachment']))` so M010 history rows (operational profile snapshots) are preserved. This is the cross-milestone decoupling per PATTERNS.md §seed-psych-profile-rows.ts header.

    2. **Reset profile_hexaco, profile_schwartz, profile_attachment rows** via `db.update(...).set({ ... }).where(eq(<table>.name, 'primary'))`. For each table, set all mutable columns back to migration-0013 cold-start state (substrateHash='', overallConfidence=0, wordCount=0, wordCountAtLastRun=0, all dimension jsonb columns to `sql\`'null'::jsonb\``, lastUpdated=null). For profile_attachment additionally reset relationalWordCount=0, activated=false.

    3. **Verify column list against migration 0013.** PATTERNS.md flags that Phase 38's cleanupAll() only reset profile_hexaco + profile_schwartz because attachment wasn't shipped at Phase 38 time; the helper MUST include attachment per Phase 37 PSCH-04 which DID ship the attachment table schema. Read `src/db/schema.ts` `profileAttachment` definition to confirm column list; reset ALL mutable columns.

    Use `client: postgres.Sql = opts.dbOverride ?? pgSql;` as in M010 sibling. Log `logger.info({ tables: 3, historyWiped: true }, 'fixture.seed_psych_profile_rows.done')` on completion. The function MUST be idempotent — calling it twice in a row produces the same end state and writes 0 net new history rows.

    The header docblock should explicitly cite (a) "Sibling of M010's `seed-profile-rows.ts`, NOT a parameterization", (b) the scoped profile_history wipe rationale (cross-milestone decoupling), (c) the source extraction from Phase 38 cleanupAll() lines 183-226.
  </action>
  <acceptance_criteria>
    - `grep -cE '^export const CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt\(99923\)' src/__tests__/fixtures/chat-ids.ts` = 1
    - `src/__tests__/fixtures/seed-psych-profile-rows.ts` exists — `test -f src/__tests__/fixtures/seed-psych-profile-rows.ts && echo ok` returns "ok"
    - `grep -cE '^export async function seedPsychProfileRows' src/__tests__/fixtures/seed-psych-profile-rows.ts` = 1
    - File contains the SCOPED profile_history wipe — verify `grep -cE "inArray.*profile_(hexaco|schwartz|attachment)|profileTableName.*profile_hexaco" src/__tests__/fixtures/seed-psych-profile-rows.ts` ≥ 1
    - File resets all 3 psychological profile tables — verify `grep -cE "update\(profileHexaco\)|update\(profileSchwartz\)|update\(profileAttachment\)" src/__tests__/fixtures/seed-psych-profile-rows.ts` ≥ 3
    - File does NOT use unscoped wipe (would erase M010 rows) — `grep -cE "delete\(profileHistory\)\s*(;|\.then|$)" src/__tests__/fixtures/seed-psych-profile-rows.ts` = 0 (must always be `.where(...)` scoped)
    - TypeScript compiles — `npx tsc --noEmit 2>&1 | grep -E 'seed-psych-profile-rows|chat-ids' | wc -l` = 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "seed-psych-profile-rows|chat-ids" | head -5; echo "exit:$?"</automated>
  </verify>
  <done>Chat-id 99923 allocated for Plan 40-02 consumption; seedPsychProfileRows helper compiles, exports correct signature, scopes profile_history wipe to M011 tables only (M010 cross-milestone isolation), resets all 3 tables to migration-0013 cold-start; idempotent.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator regenerates m011-30days + m011-1000words primed fixtures locally (PMT-02)</name>
  <files>tests/fixtures/primed/m011-30days/, tests/fixtures/primed/m011-1000words/ (operator-regenerated; gitignored — not committed)</files>
  <read_first>
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md D-08 (VCR cache invalidation; operator regen via --force --seed 42), D-09 (m011-30days target — 30 days, ~6,000 words), D-10 (≥6,000 telegram words across 30 days target), D-12 (m011-1000words target — 5 days, ~1,000 words), D-13 (sparse-fixture HARN expectation wordCount < 5000)
    - .planning/research/PITFALLS.md §"Pitfall 10: Synthetic Fixture Design Failures" lines 344-372 (signal erasure recovery rationale — what to do if HARN gate fails)
    - scripts/regenerate-primed.ts (existing M008 D041 regen pipeline; accepts arbitrary --milestone name; --psych-profile-bias pass-through from Task 1)
    - .gitignore lines 23-25 (tests/fixtures/primed/*/ exclusion — operator artifacts NOT committed)
  </read_first>
  <action>
    (Manual operator action — orchestrator displays the verification instructions below, then waits for the resume-signal before continuing. The new --psych-profile-bias flag from Task 1 changes the Haiku prompt hash, invalidating any existing VCR cache. Operator runs the two regeneration commands listed in how-to-verify; first run hits live Anthropic API at ~/bin/bash.10-0.15 combined; subsequent runs use refreshed VCR cache and cost zero.)
  </action>
  <what-built>
    The `--psych-profile-bias` flag now exists in `scripts/synthesize-delta.ts` (Task 1). The two new primed fixtures (`tests/fixtures/primed/m011-30days/` and `tests/fixtures/primed/m011-1000words/`) are required by Tasks 4-6 (HARN gate + 2 integration tests) and by Plan 40-02 PMT-06.

    Per Pitfall P-36-01 Option A (precedent from Phase 36), both directories are gitignored (`.gitignore` lines 23-25 cover `tests/fixtures/primed/*/` exclusion) and MUST be regenerated locally on every fresh checkout. The VCR cache (`tests/fixtures/.vcr/`) is also gitignored. First run hits the live Anthropic API (~$0.10-0.15 for ~35 Haiku calls combined — D-08); subsequent runs use VCR cache (free).

    **Critical mitigation against Pitfall §7 (synthetic signal erasure):** The bias keywords change the Haiku prompt hash → existing VCR cache misses → fresh Anthropic call on the first regenerate run. If the HARN sanity test (Task 4) fails because Haiku averaged away the designed signature, the operator inspects Haiku output, tunes `PSYCH_PROFILE_BIAS_KEYWORDS` (Task 1), and re-runs with `--force --reseed-vcr`.
  </what-built>
  <how-to-verify>
    Run these two commands in order from the repo root, ensuring `ANTHROPIC_API_KEY` is in `.env` (already configured per prod):

    **Generate m011-30days (~30 Haiku calls, ~$0.10 first run; D-09):**
    ```bash
    cd /home/claude/chris && npx tsx scripts/regenerate-primed.ts --milestone m011 --target-days 30 --psych-profile-bias --force --seed 42
    ```

    **Generate m011-1000words (~5 Haiku calls, ~$0.02 first run; D-12):**
    ```bash
    cd /home/claude/chris && npx tsx scripts/regenerate-primed.ts --milestone m011-1000words --target-days 5 --psych-profile-bias --force --seed 42
    ```

    **Verify both succeeded:**
    ```bash
    ls tests/fixtures/primed/m011-30days/MANIFEST.json tests/fixtures/primed/m011-1000words/MANIFEST.json && wc -l tests/fixtures/primed/m011-30days/pensieve_entries.jsonl tests/fixtures/primed/m011-1000words/pensieve_entries.jsonl
    ```

    Expected: both MANIFEST.json files exist; m011-30days has ~90 entries (30 days × 3 entries/day, ~200 words/day × 30 = ~6,000 words — D-10); m011-1000words has ~15 entries (5 × 3, ~1,000 words — D-12).

    **Investigation triggers (run Task 4 HARN gate after this completes; if it fails):**
    - **m011-30days HARN fails on wordCount > 5000:** entries are too short. Increase `ENTRIES_PER_DAY` in Task 1's synthesize-delta.ts modification, or extend `--target-days 32`, and rerun. Re-confirm via wc -l.
    - **m011-30days HARN fails on signal-phrase presence (Pitfall §7 — load-bearing failure mode):** Haiku style-transfer erased the designed signature. Tune `PSYCH_PROFILE_BIAS_KEYWORDS` in Task 1 to add more explicit Openness phrases (e.g., literal copies of OPENNESS_SIGNAL_PHRASES candidates), then rerun with `--force --reseed-vcr`. If after 2 tuning iterations the signal still doesn't retain, defer to v2.6.1 to consider hand-crafted entries per Pitfall 10 PITFALLS.md:359-365.
    - **m011-1000words HARN fails on wordCount < 5000:** synth determinism drifted up. Reduce `--target-days 4` or trim `ENTRIES_PER_DAY` for the 1000words milestone branch.
  </how-to-verify>
  <verify>
    <automated>ls tests/fixtures/primed/m011-30days/MANIFEST.json tests/fixtures/primed/m011-1000words/MANIFEST.json 2>&amp;1 | wc -l</automated>
  </verify>
  <done>Operator confirms via resume-signal: both fixtures regenerated; MANIFEST.json present at both paths; m011-30days has wc -l ~90 (~6,000 words target per D-10); m011-1000words has wc -l ~15 (~1,000 words target per D-12); actual Anthropic spend recorded (~$0.10-0.15 expected per D-08); any signal-phrase tuning iterations documented.</done>
  <resume-signal>Type "fixtures regenerated" and paste the `wc -l` output, OR describe issues encountered (auth errors, HARN-threshold concerns, VCR cache misses, signal-phrase tuning iterations)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: HARN sanity gate — primed-sanity-m011.test.ts (PMT-01 HARN half + PMT-02 presence verification)</name>
  <files>src/__tests__/fixtures/primed-sanity-m011.test.ts</files>
  <read_first>
    - src/__tests__/fixtures/primed-sanity-m010.test.ts (FULL file — D-33 mandates verbatim scaffold mirror; specifically the FIXTURE_PRESENT / skipIfAbsent pattern and the per-describe-block fixture-name structure)
    - src/__tests__/fixtures/primed-sanity.test.ts (older M008 sibling — confirm imports + beforeAll loadPrimedFixture shape)
    - .planning/phases/40-psychological-milestone-tests/40-PATTERNS.md §"src/__tests__/fixtures/primed-sanity-m011.test.ts" (lines 209-333 — full scaffold to lift verbatim; two describe blocks per D-33 — one for m011-30days, one for m011-1000words; lint trip-wire pattern for PSYCH_PROFILE_BIAS_KEYWORDS at lines 327-332)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md D-06 (HARN gate verbatim — both wordCount > 5000 AND signal-phrase present invariants), D-07 (OPENNESS_SIGNAL_PHRASES list), D-13 (m011-1000words wordCount < 5000), D-33 (single audit surface — both fixtures asserted in the same file)
    - .planning/research/PITFALLS.md §"Pitfall 10: Synthetic Fixture Design Failures" (lines 344-372 — load-bearing rationale for the signal-phrase invariant)
    - scripts/synthesize-delta.ts (created in Task 1 — import PSYCH_PROFILE_BIAS_KEYWORDS + OPENNESS_SIGNAL_PHRASES — single source of truth per RESEARCH Open Q1)
    - src/db/schema.ts (pensieveEntries — for the source='telegram' word-count SQL)
    - tests/fixtures/primed/m011-30days/MANIFEST.json + tests/fixtures/primed/m011-1000words/MANIFEST.json (created in Task 3)
  </read_first>
  <action>
    Create `src/__tests__/fixtures/primed-sanity-m011.test.ts` mirroring `primed-sanity-m010.test.ts` verbatim per D-33. The file MUST contain two independent describe blocks — one for each fixture — each with its own FIXTURE_PRESENT gate. Detailed structure:

    (1) **Header docblock (15-25 lines)** per PATTERNS.md lines 213-240: cites Phase 40 Plan 01; cites Pitfall §7 / PITFALLS.md Pitfall 10 as the load-bearing rationale for the signal-phrase invariant; documents the two describe blocks; includes the regeneration command verbatim.

    (2) **Imports**:
    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest';
    import { existsSync } from 'node:fs';
    import { sql as drizzleSql } from 'drizzle-orm';
    import { db } from '../../db/connection.js';
    import { loadPrimedFixture } from './load-primed.js';
    import {
      PSYCH_PROFILE_BIAS_KEYWORDS,
      OPENNESS_SIGNAL_PHRASES,
    } from '../../../scripts/synthesize-delta.js';
    ```
    Single-source-of-truth import per RESEARCH Open Q1 — constants live in synthesize-delta.ts.

    (3) **Lint trip-wire** (PATTERNS.md lines 327-332): inline check `if (PSYCH_PROFILE_BIAS_KEYWORDS.length < 10) { console.warn('[primed-sanity-m011] PSYCH_PROFILE_BIAS_KEYWORDS shrank to ${len}; signal density may degrade'); }` so the imported constants surface a soft trip-wire if the upstream export shape shrinks.

    (4) **m011-30days describe block** per D-06:
    - `const M30_NAME = 'm011-30days'; const M30_PATH = \`tests/fixtures/primed/${M30_NAME}/MANIFEST.json\`; const M30_PRESENT = existsSync(M30_PATH);`
    - `if (!M30_PRESENT) console.log` with the operator-instruction regen command verbatim per D-09.
    - `const skipIfM30Absent = M30_PRESENT ? describe : describe.skip;`
    - `skipIfM30Absent('primed-sanity-m011: m011-30days fixture (PMT-01 HARN; D-06)', () => { beforeAll(async () => { await loadPrimedFixture(M30_NAME); }); ... });`
    - First `it()`: assert total telegram-source pensieve wordCount > 5000 via:
        ```sql
        SELECT COALESCE(SUM(array_length(regexp_split_to_array(content, '\\s+'), 1)), 0)::int AS word_count
        FROM pensieve_entries WHERE source = 'telegram' AND deleted_at IS NULL
        ```
      Phase 37 substrate filter — non-telegram entries don't count.
    - Second `it()`: assert ≥1 OPENNESS_SIGNAL_PHRASES phrase present in telegram-source pensieve content (concatenate all rows' `content`, lowercase, check `OPENNESS_SIGNAL_PHRASES.filter(p => allContent.includes(p.toLowerCase())).length >= 1`). The error message MUST cite Pitfall §7 / Pitfall 10 PITFALLS.md and recommend `--force --reseed-vcr` regen.

    (5) **m011-1000words describe block** per D-13:
    - Same FIXTURE_PRESENT / skipIfAbsent pattern with `M1K_NAME = 'm011-1000words'` and the matching regen command.
    - First `it()`: assert wordCount < 5000 (same SQL, opposite expected).
    - Second `it()`: assert wordCount >= 1 (anti-zero trip-wire per CONTEXT.md D-13).

    Run via Docker harness: `bash scripts/test.sh src/__tests__/fixtures/primed-sanity-m011.test.ts`. When fixtures absent, both describe blocks skip cleanly with stdout regen instructions.
  </action>
  <acceptance_criteria>
    - File exists with TWO describe blocks: `grep -cE "skipIfM30Absent\(|skipIfM1KAbsent\(|describe\(.*m011-(30days|1000words)" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 2
    - File imports BOTH constants from synthesize-delta.ts: `grep -cE "from .*synthesize-delta" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1 AND `grep -cE "PSYCH_PROFILE_BIAS_KEYWORDS.*OPENNESS_SIGNAL_PHRASES|OPENNESS_SIGNAL_PHRASES.*PSYCH_PROFILE_BIAS_KEYWORDS" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - File contains skip-when-absent pattern for BOTH fixtures: `grep -cE "existsSync.*m011-30days|M30_PRESENT" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1 AND `grep -cE "existsSync.*m011-1000words|M1K_PRESENT" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - File asserts wordCount > 5000 for m011-30days: `grep -cE "toBeGreaterThan\(5000\)" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - File asserts wordCount < 5000 for m011-1000words: `grep -cE "toBeLessThan\(5000\)" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - File checks OPENNESS_SIGNAL_PHRASES presence: `grep -cE "OPENNESS_SIGNAL_PHRASES\.filter|OPENNESS_SIGNAL_PHRASES.*includes" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - File cites Pitfall §7 / Pitfall 10 in signal-phrase test or error message: `grep -ciE "Pitfall.*7|Pitfall.*10|signal.*eras|haiku.*aver" src/__tests__/fixtures/primed-sanity-m011.test.ts` ≥ 1
    - `bash scripts/test.sh src/__tests__/fixtures/primed-sanity-m011.test.ts` exits 0 (when fixtures present: all 4 assertions pass; when absent: blocks skip cleanly with stdout regen instructions)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/__tests__/fixtures/primed-sanity-m011.test.ts</automated>
  </verify>
  <done>HARN sanity gate enforces wordCount > 5000 AND ≥1 OPENNESS_SIGNAL_PHRASES retained on m011-30days (Pitfall §7 load-bearing gate); wordCount < 5000 AND >= 1 on m011-1000words (D-13 trip-wire); both describe blocks skip cleanly when their fixture absent; single-source-of-truth import of bias constants enforced.</done>
</task>

<task type="auto">
  <name>Task 5: Integration test integration-m011-1000words.test.ts — PMT-03 sparse threshold</name>
  <files>src/memory/profiles/__tests__/integration-m011-1000words.test.ts</files>
  <read_first>
    - src/memory/profiles/__tests__/integration-m010-5days.test.ts (FULL file ~236 lines — D-15 mandates this as the direct shape mirror; lift the vi.hoisted + vi.mock + FIXTURE_PRESENT gate + beforeAll/beforeEach/afterAll scaffold verbatim, adapt assertions to psychological-substrate semantics)
    - src/memory/profiles/__tests__/integration-m010-30days.test.ts (FULL file — shared hoisted-mock scaffold reference)
    - src/memory/profiles/psychological-shared.ts (Phase 37 — `loadPsychologicalSubstrate` discriminated union; `belowThreshold` branch; `runPsychologicalProfileGenerator` short-circuit at the `belowThreshold` branch — verify that the short-circuit RETURNS BEFORE the upsert per RESEARCH §Open Q4 — this is the key reconciliation point)
    - src/memory/psychological-profile-updater.ts (Phase 38 — `updateAllPsychologicalProfiles` orchestrator; both generators fired via Promise.allSettled)
    - src/memory/profiles/hexaco.ts + src/memory/profiles/schwartz.ts (Phase 38 — `generateHexacoProfile` + `generateSchwartzProfile` signatures; both emit `'skipped_below_threshold'` outcome on below-floor substrate)
    - src/__tests__/fixtures/seed-psych-profile-rows.ts (created in Task 2 — beforeEach reset helper)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md D-14 (m011-1000words expected outcome verbatim — orchestrator short-circuits, no Sonnet calls, `'skipped_below_threshold'`, profile rows preserved), D-15 (test file location), D-16 (mock SDK + real Postgres), D-17 (assertion shape — RECONCILE PER OPEN Q4 OPTION A — see action), D-23 attachment generator NOT invoked
    - .planning/phases/40-psychological-milestone-tests/40-PATTERNS.md §"integration-m011-1000words.test.ts" (lines 456-621 — full body to lift; the OQ-4 reconciliation note is at lines 611-616)
    - .planning/phases/40-psychological-milestone-tests/40-RESEARCH.md §"Open Questions #4" (lines 715-718 — load-bearing reconciliation: Phase 37/38 short-circuit returns BEFORE the upsert, so word_count_at_last_run is NOT updated on the below-threshold path; PMT-03 D-17 spec was authored before this code path was inspected; assert `=== 0` per Option A)
    - .planning/research/PITFALLS.md §"Pitfall 2: Sparse-Data Overconfidence Above the 5,000-Word Floor" (lines 53-81 — load-bearing for the zero-Sonnet-call assertion which structurally prevents this pitfall at the substrate-loader level)
  </read_first>
  <action>
    Create `src/memory/profiles/__tests__/integration-m011-1000words.test.ts` mirroring `integration-m010-5days.test.ts` per D-15. The file MUST contain:

    (1) **Header docblock (15-25 lines) per PATTERNS.md lines 460-487:** names PMT-03 sparse-threshold integration test; cites D-14 + D-17 + RESEARCH Open Q4 reconciliation; documents the 5-part assertion shape; documents the OQ-4 reconciliation explicitly: "Per RESEARCH §Open Questions #4 — Phase 37/38 below-threshold short-circuit returns BEFORE the row upsert (`runPsychologicalProfileGenerator` at `psychological-shared.ts` returns early). Therefore `word_count_at_last_run` is NOT updated on the below-threshold path. PMT-03 D-17's 'word_count_at_last_run updated to the current wordCount' spec was authored before the Phase 38 implementation was inspected. Reconciled per Option A: assert `wordCountAtLastRun === 0` (matches the actual code path). A v2.6.1 follow-up may add a 'write word_count_at_last_run BEFORE short-circuit' code path; if/when that ships, this assertion bumps to `=== currentWordCount`."

    (2) **vi.hoisted + vi.mock scaffold** (PATTERNS.md lines 491-528 — verbatim from integration-m010-5days.test.ts):
    ```typescript
    const { mockAnthropicParse, mockLoggerInfo, mockLoggerWarn, mockLoggerError } = vi.hoisted(() => ({
      mockAnthropicParse: vi.fn(),
      mockLoggerInfo: vi.fn(),
      mockLoggerWarn: vi.fn(),
      mockLoggerError: vi.fn(),
    }));
    vi.mock('../../../llm/client.js', async (importOriginal) => {
      const orig = await importOriginal<typeof import('../../../llm/client.js')>();
      return { ...orig, anthropic: { messages: { parse: mockAnthropicParse, create: vi.fn() } } };
    });
    vi.mock('../../../utils/logger.js', () => ({
      logger: { info: mockLoggerInfo, warn: mockLoggerWarn, error: mockLoggerError, debug: vi.fn() },
    }));
    ```
    Mock the wrapper at `../../../llm/client.js`, NOT `@anthropic-ai/sdk` directly (PATTERNS.md anti-pattern at line 1033).

    (3) **Imports AFTER vi.mock** (PATTERNS.md lines 520-528) including `db`, `sql as pgSql`, `profileHexaco`, `profileSchwartz`, `profileAttachment`, `profileHistory`, `loadPsychologicalSubstrate`, `generateHexacoProfile`, `generateSchwartzProfile`, `loadPrimedFixture`, `seedPsychProfileRows`.

    (4) **FIXTURE_PRESENT gate:** `const FIXTURE_NAME = 'm011-1000words'; const FIXTURE_PATH = ...; const FIXTURE_PRESENT = existsSync(FIXTURE_PATH); const skipIfAbsent = FIXTURE_PRESENT ? describe : describe.skip;` with operator-instruction stdout log when absent.

    (5) **Test body** (PATTERNS.md lines 541-618):
    - `const NOW = new Date('2026-05-01T09:00:00.000Z');` (anchor within fixture date range)
    - `skipIfAbsent('integration-m011-1000words: PMT-03 sparse threshold enforcement', () => { ... })`
    - `beforeAll`: `await pgSql\`SELECT 1 as ok\`; await loadPrimedFixture(FIXTURE_NAME);` — amortized fixture load
    - `beforeEach`: `await seedPsychProfileRows(); mockAnthropicParse.mockReset(); mockLogger*.mockReset();` — NO mock priming (any Sonnet call = contract violation)
    - `afterAll`: `await db.delete(profileHistory); await seedPsychProfileRows();` — leave clean state
    - Single `it('PMT-03: 1000 words → both generators trip threshold, zero Sonnet, profiles preserved', async () => { ... })`:
      - Snapshot rows before: `const hexacoBefore = await db.select().from(profileHexaco); ...` for all 3 tables
      - Load substrate: `const hexacoSubstrate = await loadPsychologicalSubstrate('hexaco', NOW); const schwartzSubstrate = await loadPsychologicalSubstrate('schwartz', NOW);` — assert `expect(hexacoSubstrate.kind).toBe('belowThreshold'); expect(schwartzSubstrate.kind).toBe('belowThreshold');`
      - Fire 2 generators: `const outcomes = await Promise.all([generateHexacoProfile({ substrate: hexacoSubstrate }), generateSchwartzProfile({ substrate: schwartzSubstrate })]);`
      - **Assertion 1** (D-14 / D-17 / Pitfall §2 structural prevention): `expect(mockAnthropicParse).not.toHaveBeenCalled();`
      - **Assertion 2**: `expect(outcomes.every(o => o.outcome === 'skipped_below_threshold')).toBe(true);`
      - **Assertion 3** (D-17 — 2 log entries, not 3 — attachment NOT invoked per D-23): `const skipLogCalls = mockLoggerInfo.mock.calls.filter(c => typeof c[1] === 'string' && c[1].startsWith('chris.psychological.') && c[1].endsWith('.skipped_below_threshold')); expect(skipLogCalls).toHaveLength(2);`
      - **Assertion 4** (rows preserved at cold-start): `const hexacoAfter = await db.select().from(profileHexaco); const schwartzAfter = await db.select().from(profileSchwartz); expect(hexacoAfter[0]!.overallConfidence).toBe(0); expect(schwartzAfter[0]!.overallConfidence).toBe(0); expect(hexacoAfter[0]!.wordCount).toBeLessThan(5000); expect(schwartzAfter[0]!.wordCount).toBeLessThan(5000);`
      - **Assertion 5** (zero history rows): `const history = await db.select().from(profileHistory); expect(history).toHaveLength(0);`
      - **Assertion 6** (attachment unchanged — D-23): `const attachmentAfter = await db.select().from(profileAttachment); expect(attachmentAfter[0]!.overallConfidence).toBe(0);`
      - **Assertion 7 (OQ-4 RECONCILIATION — Option A)**: `expect(hexacoAfter[0]!.wordCountAtLastRun, 'OQ-4: Phase 37/38 short-circuit returns BEFORE upsert; word_count_at_last_run NOT updated on below-threshold path').toBe(0); expect(schwartzAfter[0]!.wordCountAtLastRun).toBe(0);` with an inline comment naming the OQ-4 reconciliation and the v2.6.1 follow-up potential.

    Run via Docker harness: `bash scripts/test.sh src/memory/profiles/__tests__/integration-m011-1000words.test.ts`.
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/memory/profiles/__tests__/integration-m011-1000words.test.ts && echo ok` = "ok"
    - File contains the PMT-03 it() block: `grep -cE "it\(['\"]PMT-03|sparse threshold|1000 words" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 1
    - File asserts mockAnthropicParse NOT called: `grep -cE 'mockAnthropicParse.*not\.toHaveBeenCalled|not\.toHaveBeenCalled.*mockAnthropicParse' src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 1
    - File asserts both outcomes are `'skipped_below_threshold'`: `grep -cE "skipped_below_threshold" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 2
    - File asserts 2 skipped_below_threshold log entries (not 3 — attachment not invoked): `grep -cE "toHaveLength\(2\)|skipLogCalls" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 1
    - File imports seedPsychProfileRows (P-36-02 mitigation): `grep -cE "from.*seed-psych-profile-rows" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 1
    - File contains the OQ-4 reconciliation assertion + comment: `grep -cE "wordCountAtLastRun.*toBe\(0\)|OQ-4|Open Q.?4" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 2 (one in assertion + one in docblock/comment)
    - File has FIXTURE_PRESENT gate: `grep -cE "existsSync|FIXTURE_PRESENT" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 1
    - `bash scripts/test.sh src/memory/profiles/__tests__/integration-m011-1000words.test.ts` exits 0 (passes when m011-1000words fixture present; skips cleanly when absent)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/memory/profiles/__tests__/integration-m011-1000words.test.ts</automated>
  </verify>
  <done>PMT-03 sparse-threshold green: substrate loader returns belowThreshold for both profile types; both generators short-circuit; zero Sonnet calls; 2 skip-log entries (HEXACO + Schwartz only — attachment not invoked per D-23); profile rows preserved at cold-start; OQ-4 reconciled per Option A — assertion documents the v2.6.1 follow-up; Pitfall §2 (sparse-data overconfidence) structurally prevented.</done>
</task>

<task type="auto">
  <name>Task 6: Integration test integration-m011-30days.test.ts — PMT-04 populated + PMT-05 three-cycle UNCONDITIONAL FIRE</name>
  <files>src/memory/profiles/__tests__/integration-m011-30days.test.ts</files>
  <read_first>
    - src/memory/profiles/__tests__/integration-m010-30days.test.ts (FULL file ~438 lines — D-18 structural mirror; lift the vi.hoisted + vi.mock + FIXTURE_PRESENT + beforeAll/beforeEach/afterAll + 3-cycle scaffold; INVERT Cycle 2 idempotency assertion semantics per D-22/D-24)
    - src/memory/__tests__/psychological-profile-updater.integration.test.ts (FULL file ~451 lines — Phase 38 contract-level 3-cycle test; lines 271-391 are the cycle semantics source; lines 100-160 are the designed-signature canned response factories; lines 149-160 are the profile-focus routing mock — both lift verbatim with the D-04 designed-signature values tuned in)
    - src/memory/psychological-profile-updater.ts (Phase 38 — `updateAllPsychologicalProfiles` orchestrator; UNCONDITIONAL FIRE comment block — the contract this test enforces)
    - src/memory/profiles.ts (Phase 37 PSCH-09 — `getPsychologicalProfiles` reader; used to verify post-cycle row state)
    - src/__tests__/fixtures/seed-psych-profile-rows.ts (created in Task 2 — beforeEach reset)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md D-18 (test file location), D-19 (PMT-04 assertion shape verbatim), D-20 (mocked Sonnet returns designed-signature; this is plumbing test, not empirical Sonnet validation), D-21 (±0.8 tolerance per dimension empirically-justified by FEATURES r ≈ .31–.41), D-22 (same file as PMT-04), D-23 (3-cycle structure verbatim — Cycle 1=2, Cycle 2=4, Cycle 3=6 cumulative), D-24 (INVERSE-OF-M010 docblock VERBATIM), D-25 (defense-in-depth with Phase 38 contract test)
    - .planning/phases/40-psychological-milestone-tests/40-PATTERNS.md §"integration-m011-30days.test.ts" (lines 624-816 — full body; D-04 designed-signature canned responses at lines 689-729; profile-focus routing mock at lines 734-745; Cycle 3 INSERT pattern at lines 803-815)
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md D-04 (designed personality signature LOCKED — HEXACO HIGH: Openness, Conscientiousness, Honesty-Humility; Schwartz HIGH: Self-Direction, Benevolence, Universalism; Schwartz LOW: Conformity, Power)
    - .planning/research/FEATURES.md (speech-inference r ≈ .31–.41 — basis for ±0.8 tolerance D-21)
    - .planning/research/PITFALLS.md §"Pitfall 1: Sycophancy Injection via Profile Authority Framing" (lines 11-52 — load-bearing for the unconditional-fire contract: a regression to hash-skip would mean monthly data points are missing → consistency time series broken → Hard Rule's evidence base weakens; this test is the defense)
  </read_first>
  <action>
    Create `src/memory/profiles/__tests__/integration-m011-30days.test.ts` mirroring `integration-m010-30days.test.ts` STRUCTURE but INVERTING the cycle-2 idempotency assertion semantics per D-22/D-24. The file MUST contain:

    (1) **Header docblock (25-40 lines)** per PATTERNS.md lines 630-660 with the VERBATIM D-24 inverse-of-M010 comment block (5 lines minimum):
    ```
    // CRITICAL: This test is the INVERSE of M010's PTEST-03 idempotency test.
    // M010 PTEST-03 asserts hash-skip behavior. M011 PMT-05 asserts UNCONDITIONAL
    // FIRE (cumulative 4 calls after Cycle 2 with identical substrate). If a future
    // refactor introduces hash-skip "for consistency with M010", this test fails.
    // Do NOT "fix" the test — the divergence is intentional per PGEN-06.
    ```
    Plus the defense-in-depth note per D-25 — Phase 38's contract test used inline substrate; this file uses primed fixture → orchestrator + loader + Drizzle paths exercised end-to-end.

    (2) **vi.hoisted + vi.mock scaffold** verbatim from integration-m010-30days.test.ts (same `../../../llm/client.js` + `../../../utils/logger.js` paths since both files live at `src/memory/profiles/__tests__/`).

    (3) **Imports AFTER vi.mock** (PATTERNS.md lines 669-685) including `sql`, `eq`, `or` from drizzle-orm; `DateTime` from luxon; `db`, `sql as pgSql`; the 4 Drizzle exports (pensieveEntries, profileHexaco, profileSchwartz, profileAttachment, profileHistory); `loadPsychologicalSubstrate`; `generateHexacoProfile`, `generateSchwartzProfile`; `updateAllPsychologicalProfiles`; `loadPrimedFixture`; `seedPsychProfileRows`.

    (4) **Designed-signature canned responses** per D-04 + D-21 (PATTERNS.md lines 689-729) — two factories `validHexacoResponse()` and `validSchwartzResponse()` returning the D-04 locked signature:
    - HEXACO: openness=4.5/0.7, conscientiousness=4.0/0.65, honesty_humility=4.2/0.6, emotionality=3.0/0.5 (unchecked per D-04), extraversion=3.5/0.5, agreeableness=3.5/0.5; `overall_confidence=0.62`
    - Schwartz: self_direction=4.5/0.55, benevolence=4.5/0.6, universalism=4.3/0.55 (HIGH); conformity=2.0/0.45, power=2.0/0.5 (LOW); other 5 mid values; `overall_confidence=0.7`
    Each dim emits `{ score, confidence, last_updated: '2026-04-15T09:00:00.000Z' }`.

    (5) **Profile-focus routing mock** `primeAllProfileTypesValid()` per PATTERNS.md lines 734-745 — routes on system text containing `'## Profile Focus — HEXACO Big-Six Personality'` vs `'## Profile Focus — Schwartz Universal Values'`. Throws clearly on unrouted prompts.

    (6) **FIXTURE_PRESENT gate** for m011-30days.

    (7) **Time anchors** per PATTERNS.md lines 757-759 — calendar-month boundaries:
    ```typescript
    const NOW_C1 = DateTime.fromISO('2026-05-01T09:00:00', { zone: 'Europe/Paris' }).toJSDate();
    const NOW_C2 = DateTime.fromISO('2026-06-01T09:00:00', { zone: 'Europe/Paris' }).toJSDate();
    const NOW_C3 = DateTime.fromISO('2026-07-01T09:00:00', { zone: 'Europe/Paris' }).toJSDate();
    ```

    (8) **Outer skipIfAbsent describe block** wrapping TWO `it()` blocks (PMT-04 + PMT-05 share the fixture + beforeAll/beforeEach/afterAll per D-22):

    **beforeAll**: amortized fixture load — `await pgSql\`SELECT 1\`; await loadPrimedFixture('m011-30days');`
    **beforeEach**: `await seedPsychProfileRows(); mockAnthropicParse.mockReset(); mockLogger*.mockReset(); primeAllProfileTypesValid();` — re-prime router per test
    **afterAll**: `await db.delete(profileHistory); await seedPsychProfileRows();`

    **it #1 — PMT-04 populated** (D-19 verbatim):
    - Call `updateAllPsychologicalProfiles({ now: NOW_C1 })` ONCE
    - Read both rows: `const hexaco = await db.select().from(profileHexaco); const schwartz = await db.select().from(profileSchwartz);`
    - Assert `mockAnthropicParse.toHaveBeenCalledTimes(2)` (1 HEXACO + 1 Schwartz — attachment not invoked per D-23)
    - Assert HEXACO row: `expect(hexaco[0]!.overallConfidence).toBeGreaterThan(0); expect(hexaco[0]!.substrateHash).toMatch(/^[0-9a-f]{64}$/);` AND **per-dim ±0.8 tolerance per D-21**: for each of the 6 HEXACO dims, extract `score` from the jsonb column and assert against the designed signature: `expect(Math.abs(parsedOpenness.score - 4.5)).toBeLessThanOrEqual(0.8); expect(Math.abs(parsedConscientiousness.score - 4.0)).toBeLessThanOrEqual(0.8); expect(Math.abs(parsedHonestyHumility.score - 4.2)).toBeLessThanOrEqual(0.8);` (skip emotionality per D-04 — only check the 3 HIGH-anchored dims). For Schwartz check all 5 anchored dims (self_direction, benevolence, universalism HIGH; conformity, power LOW): `expect(Math.abs(parsedSelfDirection.score - 4.5)).toBeLessThanOrEqual(0.8);` etc.
    - Assert `profile_history` has 2 rows: `const history = await db.select().from(profileHistory).where(inArray(profileHistory.profileTableName, ['profile_hexaco', 'profile_schwartz'])); expect(history).toHaveLength(2);`
    - Per Pitfall §2 mitigation note inline: "±0.8 tolerance is a SANITY bound on the mock's designed values landing in the row — NOT an empirical Sonnet evaluation. Empirical signature detection is PMT-06's job (live test)."

    **it #2 — PMT-05 three-cycle UNCONDITIONAL FIRE** (D-23 verbatim, D-24 docblock present):
    - **Cycle 1** (`now: NOW_C1`): `await updateAllPsychologicalProfiles({ now: NOW_C1 });` → assert `mockAnthropicParse.toHaveBeenCalledTimes(2)`; assert `profile_history` row count = 2; assert per-cycle substrate_hash matches `/^[0-9a-f]{64}$/` on both rows.
    - **Cycle 2** (`now: NOW_C2`, IDENTICAL substrate — NO new pensieve inserts): `await updateAllPsychologicalProfiles({ now: NOW_C2 });` → **CRITICAL INVERSE ASSERTION**: `expect(mockAnthropicParse, 'PMT-05 D-23 INVERSE: identical substrate must NOT short-circuit; UNCONDITIONAL FIRE per PGEN-06').toHaveBeenCalledTimes(4);` (cumulative — **NOT 2**); assert `profile_history` row count = 4; assert all outcomes are `'updated'` (NOT `'skipped_no_change'` — that enum value MUST NOT exist per Phase 38 PsychologicalProfileGenerationOutcome).
    - **Cycle 3** (`now: NOW_C3`, INSERT 5 new telegram pensieve entries in the previous-month window per PATTERNS.md lines 803-815):
        ```typescript
        const c3Mid = DateTime.fromObject({ year: 2026, month: 6, day: 18, hour: 12 }, { zone: 'Europe/Paris' }).toJSDate();
        for (let i = 0; i < 5; i++) {
          await db.insert(pensieveEntries).values({
            content: `Cycle 3 distinct entry ${i} ${'word '.repeat(50)}`,
            epistemicTag: null,
            source: 'telegram',  // CRITICAL — Phase 37 substrate filter
            createdAt: c3Mid,
            // ... other required schema fields (userId, language, etc. — read schema.ts to confirm)
          });
        }
        ```
        Then `await updateAllPsychologicalProfiles({ now: NOW_C3 });` → assert `mockAnthropicParse.toHaveBeenCalledTimes(6)` cumulative; `profile_history` row count = 6.
    - **Pitfall 3 mitigation note inline**: do NOT assert substrate_hash equality across cycles — pensieve UUIDs change on re-insert (different ULIDs → different hashes even with semantically identical content). Only assert hash IS recorded (matches the 64-hex regex).
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/memory/profiles/__tests__/integration-m011-30days.test.ts && echo ok` = "ok"
    - File contains BOTH PMT-04 + PMT-05 `it()` blocks: `grep -cE "it\(['\"](PMT-04|PMT-05|populated|three-cycle)" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 2
    - File contains the D-24 INVERSE-OF-M010 comment block verbatim (5 lines): `grep -cE "INVERSE.*M010|UNCONDITIONAL.*FIRE|cumulative 4 calls after Cycle 2|divergence is intentional per PGEN-06" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 3
    - File asserts Cycle 2 cumulative `toHaveBeenCalledTimes(4)` (NOT 2 — INVERSE of M010): `grep -cE 'toHaveBeenCalledTimes\(4\)' src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 1
    - File asserts Cycle 3 cumulative `toHaveBeenCalledTimes(6)`: `grep -cE 'toHaveBeenCalledTimes\(6\)' src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 1
    - File asserts ±0.8 tolerance per dim for D-04 anchored dims: `grep -cE "toBeLessThanOrEqual\(0\.8\)|Math\.abs\(.*\.score" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 5 (at least 5 of the 8 anchored dims — 3 HEXACO + 5 Schwartz)
    - File contains routing mock dispatching on profile-focus prompt: `grep -cE "Profile Focus — HEXACO|Profile Focus — Schwartz" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 2
    - File contains Cycle 3 INSERT with source='telegram': `grep -cE "db\.insert\(pensieveEntries\)|source: 'telegram'" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 2
    - File imports seedPsychProfileRows: `grep -cE "from.*seed-psych-profile-rows" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 1
    - File asserts substrate_hash 64-hex pattern: `grep -cE "\\^\\[0-9a-f\\]\\{64\\}\\$|toMatch.*64" src/memory/profiles/__tests__/integration-m011-30days.test.ts` ≥ 1
    - `bash scripts/test.sh src/memory/profiles/__tests__/integration-m011-30days.test.ts` exits 0 (passes when m011-30days present; skips cleanly when absent)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/memory/profiles/__tests__/integration-m011-30days.test.ts</automated>
  </verify>
  <done>PMT-04 green: HEXACO + Schwartz rows populated with designed signature within ±0.8 tolerance per anchored dim, 2 profile_history rows, 64-hex substrate_hash. PMT-05 green: Cycle 1=2, Cycle 2=4 cumulative (INVERSE of M010 — UNCONDITIONAL FIRE per PGEN-06), Cycle 3=6 cumulative; D-24 inverse-of-M010 docblock present verbatim; defense-in-depth with Phase 38's contract test confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Run full Plan 40-01 test suite + verify no Phase 38 regression + commit boundary</name>
  <files>(none — manual operator gate; runs the full plan-scope + broader test suite to confirm no Phase 37/38/39 regression before milestone close)</files>
  <read_first>
    - .planning/phases/40-psychological-milestone-tests/40-CONTEXT.md §"Implementation Decisions" (full file — confirm all decision IDs honored)
    - .planning/phases/40-psychological-milestone-tests/40-RESEARCH.md §"Open Questions #4" (OQ-4 PMT-03 word_count_at_last_run reconciliation — record which Option held in plan SUMMARY)
    - src/memory/__tests__/psychological-profile-updater.integration.test.ts (Phase 38 contract-level 3-cycle test — must remain green after Plan 40-01 ships the seedPsychProfileRows helper)
    - CLAUDE.md (Docker postgres + pnpm + scripts/test.sh canonical test workflow per MEMORY.md "Always run full Docker tests")
  </read_first>
  <action>
    (Manual operator action — orchestrator displays the verification instructions below, then waits for the resume-signal before continuing. After all 6 task-scoped automated tests are green, the operator runs the broader regression sweep and confirms Phase 37/38/39 tests still pass. Commit boundary: one commit per task with conventional-commit prefixes feat(40-01)/test(40-01); plan SUMMARY documents OQ-4 outcome + any PSYCH_PROFILE_BIAS_KEYWORDS tuning iterations.)
  </action>
  <what-built>
    All 7 file changes from Tasks 1-6: `scripts/synthesize-delta.ts` extension + unit test, `chat-ids.ts` allocation, `seed-psych-profile-rows.ts` helper, `primed-sanity-m011.test.ts` HARN gate, `integration-m011-1000words.test.ts` (PMT-03), `integration-m011-30days.test.ts` (PMT-04 + PMT-05). Two fixtures regenerated in Task 3 (gitignored — not committed).

    PMT-01 verified via: (a) unit tests in Task 1, (b) HARN gate in Task 4.
    PMT-02 verified via: Task 3 operator regen + Task 4 HARN presence-and-content gate.
    PMT-03 verified via Task 5.
    PMT-04 + PMT-05 verified via Task 6.
  </what-built>
  <how-to-verify>
    Run the full plan-scope test suite in Docker (canonical command per MEMORY.md):
    ```bash
    bash scripts/test.sh \
      scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts \
      scripts/__tests__/synthesize-delta-profile-bias.test.ts \
      scripts/__tests__/synthesize-delta.test.ts \
      src/__tests__/fixtures/primed-sanity-m011.test.ts \
      src/memory/profiles/__tests__/integration-m011-1000words.test.ts \
      src/memory/profiles/__tests__/integration-m011-30days.test.ts
    ```

    Expected: all 6 test files green (estimated runtime: ~3-5 min total in Docker; HARN ~30s, two integration files ~60s each with fixture load).

    **Then run the broader psychological-profile-test suite + Phase 38 contract test to confirm no regression:**
    ```bash
    bash scripts/test.sh src/memory/profiles/__tests__/ src/memory/__tests__/psychological-profile-updater.integration.test.ts src/__tests__/fixtures/
    ```

    Expected: Phase 37 (psych-boundary-audit, psychological-shared, psychological-schemas), Phase 38 (hexaco.test.ts, schwartz.test.ts, psychological-profile-updater.integration.test.ts — the contract-level 3-cycle test), Phase 39 (psychological-profiles.test.ts) all still green AND the new Phase 40 tests green. **The Phase 38 contract test asserting `toHaveBeenCalledTimes(4)` on Cycle 2 of inline substrate stays green** (defense-in-depth per D-25 — Plan 40-01 added the fixture-driven sibling; the contract-level test is unchanged).

    **Then run the full Docker harness for the final integration check:**
    ```bash
    bash scripts/test.sh
    ```
    (No args — runs full suite excluding the 5 known fork-IPC excludes per scripts/test.sh; never runs live PMT-06 test from Plan 40-02 because RUN_LIVE_TESTS is unset.)

    Expected: full suite green; no regression in any prior milestone test.

    **Investigation triggers:**
    - **HARN signal-phrase gate fails LOUD (Pitfall §7 / Pitfall 10 mitigation triggered):** Haiku averaged away the designed signature on first regen. Tune `PSYCH_PROFILE_BIAS_KEYWORDS` in Task 1 — add literal copies of OPENNESS_SIGNAL_PHRASES candidates as keyword nudges (e.g., `"intellectual curiosity, worth exploring, novel ideas, ..."`); then re-run Task 3 with `--force --reseed-vcr`; then re-run Task 4. If after 2 tuning iterations the signal still doesn't retain, document in plan SUMMARY and defer signature-retention to v2.6.1 (hand-crafted entries per PITFALLS.md:359-365 Recommendation).
    - **PMT-05 Cycle 2 produces toHaveBeenCalledTimes(2) instead of 4:** orchestrator regressed to hash-skip — Phase 38 PGEN-06 INVARIANT BROKEN. Audit `src/memory/psychological-profile-updater.ts` for any hash-skip condition; the UNCONDITIONAL FIRE comment should be present and the generators should fire unconditionally. This is the regression class D-24 is structurally designed to detect.
    - **PMT-04 ±0.8 tolerance fails:** the mock's designed-signature values are NOT landing in the row. Audit the generator's upsert path — does it preserve all 6/10 dim scores? Pitfall §2 mitigation: every row in `profile_history` should also have all dims. If the upsert is dropping dims, fix at the source (Phase 38 hexaco.ts/schwartz.ts).
    - **PMT-03 OQ-4 assertion fails (`wordCountAtLastRun` !== 0):** Phase 38 short-circuit path WAS updated to write `word_count_at_last_run` before return — RESEARCH Open Q4 spec was the actual behavior, NOT a v2.6.1 follow-up. Update the assertion to `=== currentWordCount` AND document in plan SUMMARY that Option B held (not Option A).
    - **Phase 38 contract test regression** (lines 271-391 of psychological-profile-updater.integration.test.ts): Plan 40-01 should NOT modify that file. If it goes red, the new seed-psych-profile-rows helper has different reset semantics than the inline cleanupAll() — reconcile.

    **Commit boundary (after green):** Per CONTEXT.md `<canonical_refs>` "Conventional commits" — one commit per task. Final commit ties the plan together. Use `feat(40-01):` / `test(40-01):` prefixes.
  </how-to-verify>
  <verify>
    <automated>echo "Manual operator gate — see how-to-verify for the bash scripts/test.sh full-suite invocation; operator records pass/skip counts and confirms Phase 38 contract-level 3-cycle test still green"</automated>
  </verify>
  <done>Operator confirms via resume-signal: all 6 plan-scope test files green; broader psychological-profile-test suite + Phase 38 contract test green (no regression); OQ-4 reconciliation outcome recorded (Option A held — assert wordCountAtLastRun === 0 — OR Option B held — assert === currentWordCount); HARN signal-phrase tuning iterations (if any) documented in plan SUMMARY; commit boundary applied per CONTEXT.md conventional-commit convention.</done>
  <resume-signal>Type "all 6 files green + no Phase 38 regression" with the test output summary (file counts + pass/skip counts) + actual Task 3 spend (~$0.10-0.15 expected), OR describe red tests / signal-phrase tuning iterations / OQ-4 reconciliation outcome.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → `scripts/synthesize-delta.ts` argv | Operator supplies `--psych-profile-bias` boolean flag; parseArgs strict mode rejects unknown values automatically (D-03 — no whitelist needed unlike M010's string flag) |
| primed fixture jsonl → loadPrimedFixture → Postgres | Fixture content (Haiku-generated) treated as test substrate; assumed trusted (operator-generated locally; not from user input) |
| mockAnthropicParse → integration test assertions | Canned designed-signature responses; constants live in test file; trusted |
| operator gitignored fixture artifacts → no commit | Pitfall P-36-01 — `.gitignore` excludes `tests/fixtures/primed/*/`; HARD trust boundary against accidental commit of operator-regenerated content (m011 fixtures may inherit Greg's prod snapshot patterns via fetch-prod-data.ts) |
| Phase 38 contract test → seed-psych-profile-rows.ts (this plan) | Plan 40-01 extracts cleanupAll body into a shared helper; the Phase 38 contract test still uses its inline cleanupAll (NOT modified by this plan); behavior must remain identical or the Phase 38 contract test goes red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | S (Spoofing) | n/a | accept | Plan 40-01 is test infrastructure; no auth/identity surface. |
| T-40-01-02 | T (Tampering) | `scripts/synthesize-delta.ts --psych-profile-bias` boolean flag | mitigate | parseArgs `type: 'boolean', strict: true` rejects unknown forms automatically (PMT-01 D-03 — boolean flag, no value-set; tighter than M010's string-array flag which required explicit whitelist). ASVS L1 V5.1 input validation at CLI entry boundary verified via Task 1 unit test case (a). |
| T-40-01-03 | T (Tampering) | seed-psych-profile-rows.ts profile_history wipe | mitigate | SCOPED wipe via `inArray(profileHistory.profileTableName, ['profile_hexaco', 'profile_schwartz', 'profile_attachment'])` prevents accidental erasure of M010 operational profile_history rows when M011 tests run against a shared database. Cross-milestone decoupling enforced at the helper boundary (acceptance criterion #6 on Task 2). |
| T-40-01-04 | R (Repudiation) | n/a | accept | Test-suite scope; no audit-log surface. |
| T-40-01-05 | I (Information disclosure) | Generated primed fixtures `tests/fixtures/primed/m011-{30days,1000words}/` | mitigate | Fixtures gitignored (P-36-01 Option A inherited from M010). m011-30days inherits Greg's prod-snapshot Haiku-style entries which may contain PII-shaped content; gitignore prevents accidental commit. |
| T-40-01-06 | D (DoS) | per-day Haiku call loop | accept | `--target-days` bounded; m011-30days = 30 day loop = 30 Haiku calls = ~$0.10. No DoS surface. |
| T-40-01-07 | E (Elevation of privilege) | n/a | accept | No elevation surface in test code. |
| T-40-01-08 | V11 (Business logic) | PMT-05 Cycle 2 cumulative `toHaveBeenCalledTimes(4)` assertion (the INVERSE-of-M010 UNCONDITIONAL FIRE contract) | mitigate | D-24 verbatim docblock inline + assertion message naming PGEN-06 ensures any future drift to hash-skip surfaces as a planned change. Phase 38's contract test asserts the same number via inline substrate; this plan adds fixture-driven defense-in-depth. The `toHaveBeenCalledTimes(4)` literal acts as a TYPO-PROOF gate against the M010 idempotency pattern accidentally being copy-pasted here. |
| T-40-01-09 | V11 (Business logic) | PMT-03 `mockAnthropicParse.not.toHaveBeenCalled()` assertion (cost-floor contract — Pitfall §2 structural prevention) | mitigate | If the orchestrator regressed to skipping the below-threshold gate, this assertion fires loudly. Hard fail with no flaky-retry pattern. |
| T-40-01-10 | V11 (Business logic) | PMT-01 HARN signal-phrase invariant (Pitfall §7 load-bearing — without this, PMT-04 ±0.8 tolerance gate is vacuous) | mitigate | HARN failure stops the test pyramid before integration tests run. Operator forced to tune keywords + re-regen. Documented escape valve: if 2 tuning iterations fail, defer to v2.6.1 hand-crafted entries per PITFALLS.md Pitfall 10 Recommendation. |
| T-40-01-11 | V5 (Input validation) | Cycle 3 INSERT into pensieve_entries with arbitrary content | accept | Test scope — content is controlled by the test (literal string + word ' '.repeat(50)). No external input vector. Required schema fields (userId, language, etc.) verified via schema.ts read in Task 6 read_first. |
</threat_model>

<verification>
## Plan-Level Verification

- All 7 new/modified files exist with the artifacts listed in `must_haves.artifacts`
- `bash scripts/test.sh scripts/__tests__/synthesize-delta-psych-profile-bias.test.ts scripts/__tests__/synthesize-delta-profile-bias.test.ts scripts/__tests__/synthesize-delta.test.ts src/__tests__/fixtures/primed-sanity-m011.test.ts src/memory/profiles/__tests__/integration-m011-1000words.test.ts src/memory/profiles/__tests__/integration-m011-30days.test.ts` exits 0
- `bash scripts/test.sh src/memory/profiles/__tests__/ src/memory/__tests__/psychological-profile-updater.integration.test.ts src/__tests__/fixtures/` exits 0 (no regression in Phase 37/38/39 tests)
- Plan-scope grep gates (filtered to skip comment-only matches per `grep -vE '^\s*//|^\s*\*'`):
  - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -cE "^export const (PSYCH_PROFILE_BIAS_KEYWORDS|OPENNESS_SIGNAL_PHRASES)"` ≥ 2
  - `grep -vE '^\s*//|^\s*\*' scripts/synthesize-delta.ts | grep -c "^export function psychDimensionHintFor"` = 1
  - `grep -vE '^\s*//|^\s*\*' src/__tests__/fixtures/chat-ids.ts | grep -c "CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = BigInt(99923)"` = 1
  - `grep -vE '^\s*//|^\s*\*' src/__tests__/fixtures/seed-psych-profile-rows.ts | grep -cE "update\(profileHexaco\)|update\(profileSchwartz\)|update\(profileAttachment\)"` ≥ 3
  - `grep -vE '^\s*//|^\s*\*' src/memory/profiles/__tests__/integration-m011-30days.test.ts | grep -c "toHaveBeenCalledTimes(4)"` ≥ 1 (PMT-05 Cycle 2 INVERSE assertion)
  - `grep -vE '^\s*//|^\s*\*' src/memory/profiles/__tests__/integration-m011-30days.test.ts | grep -c "toHaveBeenCalledTimes(6)"` ≥ 1 (PMT-05 Cycle 3)
- Pitfall §7 mitigation present in HARN test: `grep -E 'OPENNESS_SIGNAL_PHRASES|Pitfall.*7|signal.*eras' src/__tests__/fixtures/primed-sanity-m011.test.ts | head -5 | wc -l` ≥ 2
- Pitfall §2 mitigation present in PMT-03 test: `grep -E 'not\.toHaveBeenCalled|skipped_below_threshold' src/memory/profiles/__tests__/integration-m011-1000words.test.ts | wc -l` ≥ 3
- PMT-05 D-24 inverse-of-M010 docblock VERBATIM (5 lines): grep for each of the 5 lines independently — none may be paraphrased
- OQ-4 reconciliation documented in PMT-03 file: `grep -ciE "OQ-?4|Open Q.?4|word_count_at_last_run.*toBe\(0\)" src/memory/profiles/__tests__/integration-m011-1000words.test.ts` ≥ 2
</verification>

<success_criteria>
- ROADMAP Phase 40 success #1: `scripts/synthesize-delta.ts --psych-profile-bias` produces fixture with wordCount > 5000 AND ≥1 OPENNESS_SIGNAL_PHRASES present; HARN sanity gate verifies both invariants ✓ (Tasks 1, 3, 4)
- ROADMAP success #2: Sparse-threshold test loads m011-1000words → zero Sonnet calls, all 3 profile rows confidence=0, word_count < 5000, skipped_below_threshold outcome ✓ (Task 5 PMT-03; OQ-4 reconciled Option A)
- ROADMAP success #3: Populated test loads m011-30days → HEXACO + Schwartz populated with overall_confidence > 0, designed signature within ±0.8 per anchored dim, profile_history rows for both ✓ (Task 6 PMT-04)
- ROADMAP success #4: Three-cycle unconditional-fire test: Cycle 1 = 2 Sonnet calls; Cycle 2 IDENTICAL = cumulative 4 (NOT 2); Cycle 3 with INSERTed entries = cumulative 6; D-24 INVERSE-OF-M010 docblock present ✓ (Task 6 PMT-05)
- D-01 plan structure: 2 plans matching REQUIREMENTS PMT-01..05 → Plan 40-01; PMT-06 → Plan 40-02 ✓
- D-02 strict ordering: 40-01 ships before 40-02 (PMT-06 imports m011-30days fixture) ✓
- D-22 same file rationale: PMT-04 + PMT-05 share `integration-m011-30days.test.ts` to amortize fixture load ✓
- D-24 verbatim docblock present (5 lines from CONTEXT.md) ✓
- D-25 defense-in-depth with Phase 38 contract test confirmed (both files assert `toHaveBeenCalledTimes(4)` on Cycle 2 — Phase 38 via inline substrate; Plan 40-01 via primed fixture) ✓
- OQ-4 reconciled per Option A — PMT-03 asserts `wordCountAtLastRun === 0` matching the actual Phase 37/38 short-circuit code path; v2.6.1 follow-up documented in plan SUMMARY ✓
- Pitfall §1 (D027 sycophancy regression class): Plan 40-01 builds the fixture + signature that Plan 40-02's PMT-06 adversarial regex sweep verifies against
- Pitfall §2 (sparse-data overconfidence): structurally prevented by PMT-03's `mockAnthropicParse.not.toHaveBeenCalled()` (the substrate-loader gate fires upstream of Sonnet)
- Pitfall §7 (signal erasure): load-bearing HARN signal-phrase gate fails LOUD if Haiku averages away the signature
- Phase 37/38/39 test suite still green (no regression)
- Plan 40-02 has fixture + chat ID + helper ready to consume
</success_criteria>

<output>
After completion, create `.planning/phases/40-psychological-milestone-tests/40-01-SUMMARY.md` covering:
- What was built (7 files: 1 modified script + 1 unit test + 1 modified chat-ids + 1 helper + 1 HARN + 2 integration tests; 2 gitignored fixtures)
- Verification results (which `bash scripts/test.sh ...` runs were green, with file counts)
- Fixture-regeneration outcome from Task 3: actual cost spent, per-fixture wc -l counts, total telegram-source wordCount, OPENNESS_SIGNAL_PHRASES count retained
- HARN signal-phrase tuning iterations (if any) — document the final PSYCH_PROFILE_BIAS_KEYWORDS state if it was tuned from D-05 defaults
- OQ-4 reconciliation outcome: Option A held (wordCountAtLastRun === 0) OR Option B held (=== currentWordCount); if Option B, document the Phase 37/38 code path that does the write
- Pitfall mitigations confirmed (§7 HARN green; §2 PMT-03 zero-call gate; D-24 INVERSE-OF-M010 docblock present)
- Patterns established for Plan 40-02: seedPsychProfileRows reuse, fixture commit policy (gitignored), CHAT_ID_LIVE_PSYCH_ANTI_HALLUCINATION = 99923 allocated
- No-regression confirmation: Phase 38 contract-level 3-cycle test (psychological-profile-updater.integration.test.ts) still green
</output>
</content>
</invoke>