---
phase: 35
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/memory/profiles.ts
  - src/memory/__tests__/profiles.test.ts
  - src/chris/personality.ts
  - src/chris/__tests__/personality.test.ts
  - src/chris/modes/reflect.ts
  - src/chris/modes/coach.ts
  - src/chris/modes/psychology.ts
  - src/chris/__tests__/reflect.test.ts
  - src/chris/__tests__/coach.test.ts
  - src/chris/__tests__/psychology.test.ts
  - src/chris/__tests__/journal.test.ts
  - src/chris/__tests__/interrogate.test.ts
  - src/chris/__tests__/produce.test.ts
  - src/chris/__tests__/photos.test.ts
  - src/decisions/__tests__/resolution.test.ts
autonomous: true
requirements:
  - SURF-02
tags:
  - phase-35
  - surfaces
  - profile-injection
  - profile-injection-map
  - mode-handler-wiring

must_haves:
  truths:
    - "PROFILE_INJECTION_MAP exported from src/memory/profiles.ts with REFLECT=['jurisdictional','capital','health','family'], COACH=['capital','family'], PSYCHOLOGY=['health','jurisdictional']"
    - "formatProfilesForPrompt(profiles, mode) is a pure function in src/memory/profiles.ts returning string"
    - "formatProfilesForPrompt returns empty string when mode not in injection map, all in-scope dimensions null, or all in-scope dimensions zero-confidence (D-12)"
    - "formatProfilesForPrompt skips health dimension when confidence < 0.5 (D-09 health gate)"
    - "formatProfilesForPrompt appends staleness note 'Note: profile data from {YYYY-MM-DD} — may not reflect current state.' when last_updated > 21 days ago (D-10)"
    - "formatProfilesForPrompt truncates per-dimension renderings at 2000 chars with '...' marker (D-11)"
    - "formatProfilesForPrompt output starts with '## Operational Profile (grounded context — not interpretation)' header (D-13) when at least one dimension renders"
    - "buildSystemPrompt in personality.ts consumes extras.operationalProfiles and prepends the block ABOVE pensieveContext in REFLECT/COACH/PSYCHOLOGY cases (D-07); JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY silently drop it"
    - "REFLECT/COACH/PSYCHOLOGY handlers call getOperationalProfiles() then formatProfilesForPrompt(profiles, MODE) then pass result via extras.operationalProfiles to buildSystemPrompt (D-14 call order)"
    - "JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY handlers do NOT call getOperationalProfiles — negative invariant asserted in 5 mode-handler tests + resolution.test.ts (D-28)"
  artifacts:
    - path: "src/memory/profiles.ts"
      provides: "PROFILE_INJECTION_MAP + formatProfilesForPrompt(profiles, mode) — pure function"
      contains: "PROFILE_INJECTION_MAP"
    - path: "src/memory/__tests__/profiles.test.ts"
      provides: "PROFILE_INJECTION_MAP shape test + formatProfilesForPrompt unit tests (empty-string, staleness, char cap, health gate)"
      contains: "PROFILE_INJECTION_MAP"
    - path: "src/chris/personality.ts"
      provides: "extras.operationalProfiles consumed; block prepended above pensieveContext in REFLECT/COACH/PSYCHOLOGY (per D-07)"
      contains: "operationalProfiles"
    - path: "src/chris/modes/reflect.ts"
      provides: "REFLECT mode handler invokes getOperationalProfiles + formatProfilesForPrompt + passes via extras"
      contains: "formatProfilesForPrompt"
    - path: "src/chris/modes/coach.ts"
      provides: "COACH mode handler — same wiring"
      contains: "formatProfilesForPrompt"
    - path: "src/chris/modes/psychology.ts"
      provides: "PSYCHOLOGY mode handler — same wiring"
      contains: "formatProfilesForPrompt"
  key_links:
    - from: "src/chris/modes/reflect.ts"
      to: "src/memory/profiles.ts"
      via: "getOperationalProfiles + formatProfilesForPrompt imports"
      pattern: "formatProfilesForPrompt\\(profiles,\\s*'REFLECT'\\)"
    - from: "src/chris/modes/coach.ts"
      to: "src/memory/profiles.ts"
      via: "same imports"
      pattern: "formatProfilesForPrompt\\(profiles,\\s*'COACH'\\)"
    - from: "src/chris/modes/psychology.ts"
      to: "src/memory/profiles.ts"
      via: "same imports"
      pattern: "formatProfilesForPrompt\\(profiles,\\s*'PSYCHOLOGY'\\)"
    - from: "src/chris/personality.ts REFLECT/COACH/PSYCHOLOGY switch cases"
      to: "extras.operationalProfiles"
      via: "prepend block to contextValue before .replace('{pensieveContext}', ...)"
      pattern: "operationalProfiles"
---

<objective>
Ship the named-constant + pure-function plumbing that turns the `extras.operationalProfiles` slot opened in Plan 35-01 into a wired profile-injection pipeline for REFLECT/COACH/PSYCHOLOGY mode handlers. After this plan: (1) `PROFILE_INJECTION_MAP` constant in `src/memory/profiles.ts` locks per-mode subsets per D-08 + M010-08 mitigation; (2) `formatProfilesForPrompt(profiles, mode)` is a pure function honoring health-confidence gate (D-09), 21-day staleness (D-10), 2000-char per-dimension cap (D-11), and empty-string return (D-12); (3) REFLECT/COACH/PSYCHOLOGY handlers wire `getOperationalProfiles() → formatProfilesForPrompt() → buildSystemPrompt(..., { operationalProfiles })` in D-14 order; (4) personality.ts prepends the formatted block ABOVE `{pensieveContext}` per D-07; (5) 5 mode-handler negative-invariant tests (JOURNAL/INTERROGATE/PRODUCE/PHOTOS) + 1 resolution.test.ts negative test confirm out-of-scope modes do NOT call getOperationalProfiles (D-28). Closes SURF-02.

Purpose: SURF-02. Per M010-08, "modifying each handler independently without a shared mapping means each developer makes independent injection scope decisions" — the named constant + shared pure function is the M010-08 mitigation surface. Per M010-07, health profile carries the highest topic-drift risk → confidence ≥ 0.5 gate. Per UX Pitfall in PITFALLS.md line 415, 21-day staleness qualifier prevents Chris from referencing a 6-week-old profile as current.

Output: 1 new pure function + 1 new named constant + 3 mode-handler wiring edits + 1 personality.ts injection point + 8 new mode-handler tests (3 positive + 5 negative) + 1 negative resolution test + 4-6 new unit tests for PROFILE_INJECTION_MAP and formatProfilesForPrompt. Approximately 5 tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-surfaces/35-CONTEXT.md
@.planning/phases/35-surfaces/35-RESEARCH.md
@.planning/phases/35-surfaces/35-PATTERNS.md
@.planning/phases/35-surfaces/35-VALIDATION.md
@.planning/phases/35-surfaces/35-01-PLAN.md
@.planning/phases/33-profile-substrate/33-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- From Plan 35-01 (already shipped at this wave): -->

export interface ChrisContextExtras {
  language?: string;
  declinedTopics?: DeclinedTopic[];
  operationalProfiles?: string;   // pre-rendered string; Plan 35-02 consumes this slot
}

export function buildSystemPrompt(
  mode: ChrisMode,
  pensieveContext?: string,
  relationalContext?: string,
  extras?: ChrisContextExtras,
): string;

<!-- From Phase 33 (src/memory/profiles.ts:41-53): -->

export interface ProfileRow<T> {
  data: T;
  confidence: number;
  lastUpdated: Date;
  schemaVersion: number;
}

export interface OperationalProfiles {
  jurisdictional: ProfileRow<JurisdictionalProfileData> | null;
  capital: ProfileRow<CapitalProfileData> | null;
  health: ProfileRow<HealthProfileData> | null;
  family: ProfileRow<FamilyProfileData> | null;
}

export async function getOperationalProfiles(): Promise<OperationalProfiles>;

<!-- New exports added by Plan 35-02 (this plan): -->

export type Dimension = 'jurisdictional' | 'capital' | 'health' | 'family';

export const PROFILE_INJECTION_MAP: Readonly<Record<'REFLECT' | 'COACH' | 'PSYCHOLOGY', readonly Dimension[]>> = {
  REFLECT: ['jurisdictional', 'capital', 'health', 'family'],
  COACH: ['capital', 'family'],
  PSYCHOLOGY: ['health', 'jurisdictional'],
} as const;

export function formatProfilesForPrompt(
  profiles: OperationalProfiles,
  mode: string,
): string;

<!-- Per-dimension type shapes from src/memory/profiles/schemas.ts (read at impl time): -->
<!-- JurisdictionalProfileData / CapitalProfileData / HealthProfileData / FamilyProfileData -->
<!-- — snake_case fields per FEATURES.md §2.1-2.4. formatProfilesForPrompt's renderDimensionForPrompt helper switch-cases over these. -->
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROFILE_INJECTION_MAP constant + Dimension type export to src/memory/profiles.ts</name>
  <files>src/memory/profiles.ts</files>
  <read_first>
    - src/memory/profiles.ts (lines 1-80 — current exports + Dimension internal type at line 61)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-08 — verbatim map values; M010-08 rationale)
    - .planning/phases/35-surfaces/35-PATTERNS.md (§"src/memory/profiles.ts" — LEGAL_TRANSITIONS analog at decisions/lifecycle.ts:11-25 for Record/as const pattern, lines 509-545)
    - .planning/research/PITFALLS.md (M010-08 — lines 234-273; PROFILE_INJECTION_MAP locked text at line 252-258)
  </read_first>
  <action>
    Edit `src/memory/profiles.ts` ONLY. Two additions, ordered:

    (a) **Export the existing internal `Dimension` type** (currently declared as `type Dimension = 'jurisdictional' | 'capital' | 'health' | 'family'` at line 61). Change `type Dimension` to `export type Dimension`. Plan 35-03's `/profile` handler will import this type; co-locating the type with the reader keeps the dependency arrow clean.

    (b) **Add `PROFILE_INJECTION_MAP` named constant** after the `OperationalProfiles` interface (around line 53). Verbatim values per D-08 (DO NOT reorder dimensions, DO NOT alter the casing):
    ```
    /**
     * Per-mode subset of profile dimensions to inject into the system prompt.
     * Locked per Phase 35 D-08 + PITFALLS.md M010-08 mitigation (lines 250-258):
     *   - REFLECT synthesizes across all 4 dimensions by design
     *   - COACH gets decisions + constraints only — health → topic-drift risk (M010-08(b))
     *   - PSYCHOLOGY needs clinical + situational grounding only
     * JOURNAL / INTERROGATE / PRODUCE / PHOTOS / ACCOUNTABILITY absent by design
     * (D-28 negative invariant — no injection for these modes).
     */
    export const PROFILE_INJECTION_MAP: Readonly<Record<'REFLECT' | 'COACH' | 'PSYCHOLOGY', readonly Dimension[]>> = {
      REFLECT: ['jurisdictional', 'capital', 'health', 'family'],
      COACH: ['capital', 'family'],
      PSYCHOLOGY: ['health', 'jurisdictional'],
    } as const;
    ```

    Mirror the `Readonly<Record<...>>` + `as const` shape of `src/decisions/lifecycle.ts:LEGAL_TRANSITIONS` (the canonical project pattern per PATTERNS.md line 518-523). Do NOT inline literal values in any mode handler — the named constant is the single source of truth per M010-08.

    No new tests in this task; Task 2 unit-tests this constant alongside `formatProfilesForPrompt`.
  </action>
  <acceptance_criteria>
    - `grep -c "export const PROFILE_INJECTION_MAP" src/memory/profiles.ts` returns 1
    - `grep -c "export type Dimension" src/memory/profiles.ts` returns 1
    - `grep -cE "REFLECT:.*\\['jurisdictional', 'capital', 'health', 'family'\\]" src/memory/profiles.ts` returns 1
    - `grep -cE "COACH:.*\\['capital', 'family'\\]" src/memory/profiles.ts` returns 1
    - `grep -cE "PSYCHOLOGY:.*\\['health', 'jurisdictional'\\]" src/memory/profiles.ts` returns 1
    - `npx tsc --noEmit src/memory/profiles.ts 2>&1 | grep -cE "error TS" | awk '$1==0 {print "OK"}'` prints `OK`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export const PROFILE_INJECTION_MAP" src/memory/profiles.ts | awk '$1==1 {print "OK"}'</automated>
  </verify>
  <done>
    PROFILE_INJECTION_MAP exported with verbatim D-08 values; Dimension type exported. Plan 35-03 can import both. Mode handlers (Task 4) will consult PROFILE_INJECTION_MAP via formatProfilesForPrompt (Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement formatProfilesForPrompt + unit tests (gates + truncation + staleness + empty-string contract)</name>
  <files>src/memory/profiles.ts, src/memory/__tests__/profiles.test.ts</files>
  <behavior>
    - mode not in PROFILE_INJECTION_MAP (JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY/unknown) → returns "" (D-12.a)
    - All in-scope dimensions return profile=null (DB error / missing row) → returns "" (D-12.b)
    - All in-scope dimensions have confidence === 0 → returns "" (D-12.c)
    - Health dimension with confidence < 0.5 → skipped from rendering (D-09); if it was the only candidate, returns "" (D-12.d)
    - At least one in-scope dimension renders → output starts with "## Operational Profile (grounded context — not interpretation)" verbatim header (D-13)
    - lastUpdated > 21 days ago → block appends "Note: profile data from YYYY-MM-DD — may not reflect current state." (D-10)
    - Per-dimension rendered block exceeds 2000 chars → truncated to 1999 chars + "..." marker (D-11 — `block.slice(0, 1997) + '...'` or equivalent; total ≤ 2000)
    - REFLECT mode renders all 4 dimensions in declaration order: jurisdictional, capital, health, family
    - COACH mode renders only capital + family (in that order)
    - PSYCHOLOGY mode renders only health + jurisdictional (in that order)
    - Health gate: with COACH and PSYCHOLOGY both including health: COACH does NOT include health (it's not in COACH's PROFILE_INJECTION_MAP); PSYCHOLOGY includes health only if confidence >= 0.5
  </behavior>
  <read_first>
    - src/memory/profiles.ts (lines 1-80 — full module head + new PROFILE_INJECTION_MAP from Task 1)
    - src/memory/profiles/schemas.ts (4 ProfileData type shapes — fields the renderer accesses by name)
    - src/rituals/weekly-review-prompt.ts (lines 144-188 — `assembleWeeklyReviewPrompt` push-sections-join pattern; per PATTERNS.md line 546-581)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/35-surfaces/35-PATTERNS.md (§"Mirror for `formatProfilesForPrompt`", lines 584-628)
    - .planning/phases/35-surfaces/35-RESEARCH.md (Pattern 2 — full implementation skeleton at lines 281-339)
    - .planning/phases/33-profile-substrate/33-CONTEXT.md (D-12, D-13 — reader returns null on DB/schema error; never throws)
  </read_first>
  <action>
    TDD order: WRITE TESTS FIRST. Each behavior bullet above maps to ≥ 1 test case. Use `vi.setSystemTime(...)` to control "now" for staleness tests so the assertion is deterministic.

    Step 1: Create `src/memory/__tests__/profiles.test.ts` if it does not exist; otherwise extend the existing file. Add a new `describe('formatProfilesForPrompt', ...)` block. Use deterministic fixed timestamps: `const NOW = new Date('2026-05-13T12:00:00Z')` and `const STALE_DATE = new Date('2026-04-01T12:00:00Z')` (42 days before NOW). Define helper `makeRow<T>(data: T, confidence: number, lastUpdated: Date): ProfileRow<T>` that returns `{ data, confidence, lastUpdated, schemaVersion: 1 }`.

    Build a `MOCK_PROFILES_FRESH` fixture with all 4 dimensions populated at confidence 0.7, lastUpdated NOW; a `MOCK_PROFILES_ALL_NULL` fixture; a `MOCK_PROFILES_HEALTH_LOW_CONF` fixture where health.confidence = 0.3 (below 0.5 gate). Sample data values can be small (e.g., `{ current_country: 'France' }` for jurisdictional, `{ fi_target: 1_500_000 }` for capital) — implementation must access the snake_case fields from `profiles/schemas.ts`.

    Write the following test cases inside the describe block. Run each with `vi.setSystemTime(NOW)` in beforeEach:

    1. `it('mode not in PROFILE_INJECTION_MAP returns empty string', ...)` — assert `formatProfilesForPrompt(MOCK_PROFILES_FRESH, 'JOURNAL')`, `'INTERROGATE'`, `'PRODUCE'`, `'PHOTOS'`, `'ACCOUNTABILITY'`, and `'UNKNOWN_MODE'` ALL return `''`.

    2. `it('all in-scope dimensions null → empty string', ...)` — `formatProfilesForPrompt(MOCK_PROFILES_ALL_NULL, 'REFLECT')` returns `''`.

    3. `it('all in-scope dimensions zero-confidence → empty string', ...)` — build a fixture where all 4 have confidence === 0; assert REFLECT returns `''`.

    4. `it('health below 0.5 confidence is skipped → PSYCHOLOGY with only health low-conf + jurisdictional null returns empty', ...)` — fixture where health.confidence = 0.3 and jurisdictional = null; assert PSYCHOLOGY returns `''`.

    5. `it('health below 0.5 is skipped but jurisdictional renders → PSYCHOLOGY non-empty starting with header', ...)` — fixture where health.confidence = 0.3 (skipped) and jurisdictional.confidence = 0.7 (renders); assert output starts with `'## Operational Profile (grounded context — not interpretation)'` AND does NOT contain anything from health.

    6. `it('staleness qualifier appears when lastUpdated > 21 days ago', ...)` — fixture where jurisdictional.lastUpdated = STALE_DATE (42d before NOW); assert output contains `'Note: profile data from 2026-04-01 — may not reflect current state.'`.

    7. `it('staleness qualifier absent when lastUpdated ≤ 21 days ago', ...)` — jurisdictional.lastUpdated = 14d before NOW; assert output does NOT contain `'Note: profile data from'`.

    8. `it('per-dimension block truncated at 2000 chars with ... marker', ...)` — fixture where jurisdictional.data has a large string field (e.g., 5000 chars); REFLECT output's jurisdictional block ≤ 2000 chars AND ends with `'...'`.

    9. `it('REFLECT renders all 4 dimensions in declaration order', ...)` — index assertion: `output.indexOf('jurisdictional') < output.indexOf('capital') < output.indexOf('health') < output.indexOf('family')` (or whatever per-dimension marker words `renderDimensionForPrompt` emits).

    10. `it('COACH renders only capital + family — no health, no jurisdictional content', ...)` — fixture with all 4 populated; assert COACH output contains capital + family markers and does NOT contain any string from health or jurisdictional `data` fields.

    11. `it('PROFILE_INJECTION_MAP exact shape', ...)` — assert `PROFILE_INJECTION_MAP.REFLECT.length === 4` AND `PROFILE_INJECTION_MAP.COACH` equals `['capital', 'family']` AND `PROFILE_INJECTION_MAP.PSYCHOLOGY` equals `['health', 'jurisdictional']`. Also assert `'JOURNAL' in PROFILE_INJECTION_MAP === false` and same for INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY.

    Run `npx vitest run src/memory/__tests__/profiles.test.ts` — all new tests MUST FAIL (RED — `formatProfilesForPrompt` does not exist yet).

    Step 2: Implement `formatProfilesForPrompt` in `src/memory/profiles.ts`. Use the skeleton from PATTERNS.md lines 584-628 and RESEARCH.md lines 305-339. Pseudocode:

    ```
    export function formatProfilesForPrompt(profiles: OperationalProfiles, mode: string): string {
      const scope = PROFILE_INJECTION_MAP[mode as 'REFLECT' | 'COACH' | 'PSYCHOLOGY'];
      if (!scope) return '';

      const sections: string[] = [];
      const now = Date.now();
      const STALENESS_MS = 21 * 86_400_000;
      const CAP = 2000;

      for (const dim of scope) {
        const row = profiles[dim];
        if (!row) continue;
        if (row.confidence === 0) continue;
        if (dim === 'health' && row.confidence < 0.5) continue;

        let block = renderDimensionForPrompt(dim, row);
        if (block.length > CAP) block = block.slice(0, CAP - 3) + '...';
        if (now - row.lastUpdated.getTime() > STALENESS_MS) {
          const dateStr = row.lastUpdated.toISOString().slice(0, 10);
          block += `\nNote: profile data from ${dateStr} — may not reflect current state.`;
        }
        sections.push(block);
      }

      if (sections.length === 0) return '';
      return '## Operational Profile (grounded context — not interpretation)\n\n' + sections.join('\n\n');
    }
    ```

    Define `renderDimensionForPrompt(dim, row)` as a private switch-case helper. Per-dimension renderer accesses snake_case fields from `row.data` (e.g., jurisdictional: `current_country`, `physical_location`, `residency_status`, `tax_residency`, `next_planned_move`, `planned_move_date`, `passport_citizenships`; capital: `fi_target`, `fi_phase`, `active_legal_entities`; health: per FEATURES.md §2.3; family: per FEATURES.md §2.4 — read schemas.ts for exact field names). Output should include a leading dimension-name label so the assertions in tests #9, #10 can pattern-match (e.g., `### Jurisdictional`).

    DO NOT export `renderDimensionForPrompt` — it is internal-only. The export surface is `PROFILE_INJECTION_MAP` (Task 1) + `formatProfilesForPrompt` (this task).

    Step 3: Run `npx vitest run src/memory/__tests__/profiles.test.ts` — all 11 tests MUST PASS (GREEN). If any fail, fix the implementation (NOT the tests) until they pass.

    Step 4: Refactor if needed — extract repeated formatting helpers. Keep the per-dimension switch-case clean.
  </behavior>
  <action>
    See `<behavior>` block above. TDD: tests first (RED), then implementation (GREEN), then optional refactor.

    Important: the truncation marker test (#8) requires the renderer to emit content of provably > 2000 chars for the test fixture. If the per-dimension switch-case renders fixed-format fields where the test data can't naturally exceed 2000 chars, supply a long string in one of the data fields (e.g., a 3000-char `data_consistency` notes field, or a long entry in `active_legal_entities`) so the truncation branch is genuinely exercised.

    Per CONVENTIONS.md ESM rule: `import type { OperationalProfiles, Dimension } from './profiles.js'` in the test file (not `from './profiles'`).
  </action>
  <acceptance_criteria>
    - `grep -c "export function formatProfilesForPrompt" src/memory/profiles.ts` returns 1
    - `npx vitest run src/memory/__tests__/profiles.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*passed.*failed|Tests.*passed" | head -3` shows 0 failed; ≥ 11 new tests in the file
    - `grep -cE "it\\(|test\\(" src/memory/__tests__/profiles.test.ts | awk '$1>=11 {print "OK"}'` prints `OK`
    - `grep -cE "## Operational Profile \\(grounded context — not interpretation\\)" src/memory/profiles.ts` returns 1 (verbatim header per D-13)
    - `grep -cE "21 \\* 86_?400_?000|STALENESS_MS|STALENESS_DAYS" src/memory/profiles.ts` returns ≥ 1
    - `grep -cE "2000|CAP|PER_DIMENSION_CHAR_CAP" src/memory/profiles.ts` returns ≥ 1
    - `grep -cE "row\\.confidence < 0\\.5|HEALTH_CONFIDENCE_FLOOR" src/memory/profiles.ts` returns ≥ 1
    - `npx tsc --noEmit 2>&1 | grep -cE "error TS" | awk '$1==0 {print "OK"}'` prints `OK`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/memory/__tests__/profiles.test.ts --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <done>
    formatProfilesForPrompt implemented per D-08..D-13 contract; 11+ unit tests green; all 6 gates (out-of-scope mode, all-null, all-zero-conf, health<0.5, staleness, char-cap) covered. The pure-function pipeline plug-in for Plan 35-03's `/profile` reuse and the mode-handler wiring in Task 4 below.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire personality.ts to inject extras.operationalProfiles ABOVE pensieveContext in REFLECT/COACH/PSYCHOLOGY</name>
  <files>src/chris/personality.ts, src/chris/__tests__/personality.test.ts</files>
  <read_first>
    - src/chris/personality.ts (full file, especially lines 94-149 — switch-case after Plan 35-01 refactor)
    - src/chris/__tests__/personality.test.ts (existing test patterns for return-string assertions)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-07 — placement above {pensieveContext}; D-13 header — already part of formatProfilesForPrompt output, so personality.ts just prepends `extras.operationalProfiles` as-is)
    - .planning/phases/35-surfaces/35-PATTERNS.md (§"Operational Profile injection point" lines 379-400)
  </read_first>
  <action>
    Per D-07: when `extras.operationalProfiles` is a non-empty string AND the mode is REFLECT/COACH/PSYCHOLOGY, the prompt template substitution prepends the block to `pensieveContext` BEFORE the `.replace('{pensieveContext}', ...)` call. JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY receive but IGNORE `operationalProfiles` (silent drop — mirrors current `relationalContext`-on-JOURNAL behavior).

    Step 1: Remove the `void operationalProfiles;` no-op statement added in Plan 35-01 Task 1 (slot-reserved marker is now consumed).

    Step 2: In the switch-case body for REFLECT/COACH/PSYCHOLOGY cases (currently lines 111-125 after Plan 35-01 edits), prepend the operational-profile block to `contextValue` BEFORE the `.replace('{pensieveContext}', ...)` call. Pattern:

    ```
    case 'REFLECT': {
      const pensieveWithProfile = operationalProfiles
        ? `${operationalProfiles}\n\n${contextValue}`
        : contextValue;
      modeBody = REFLECT_SYSTEM_PROMPT
        .replace('{pensieveContext}', pensieveWithProfile)
        .replace('{relationalContext}', relationalContext || 'No observations accumulated yet.');
      break;
    }
    ```

    Apply the same shape to COACH and PSYCHOLOGY cases. The `operationalProfiles` string from `formatProfilesForPrompt` ALREADY contains the `## Operational Profile (grounded context — not interpretation)` header (per Task 2 implementation + D-13), so personality.ts just prepends it as-is — do NOT add the header here.

    DO NOT add the prepend to JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY cases. They receive the field via destructured `operationalProfiles` local but ignore it (D-07 silent drop; D-28 negative invariant verified in Task 4 + 5).

    Step 3: Extend `src/chris/__tests__/personality.test.ts` with 3 new positive tests + 5 new negative tests asserting the block placement:

    Positive (3):
    - `it('REFLECT prepends operationalProfiles ABOVE pensieveContext when extras.operationalProfiles is non-empty', ...)` — call `buildSystemPrompt('REFLECT', 'PENSIEVE_FACTS', 'REL', { operationalProfiles: '## Operational Profile (grounded context — not interpretation)\n\nfake-jurisdictional-block' })`; assert returned string contains `'fake-jurisdictional-block'` AND its position is BEFORE `'PENSIEVE_FACTS'` in the output (`output.indexOf('fake-jurisdictional-block') < output.indexOf('PENSIEVE_FACTS')`).
    - Same for COACH.
    - Same for PSYCHOLOGY.

    Negative (5):
    - `it('JOURNAL silently drops extras.operationalProfiles', ...)` — call with `{ operationalProfiles: 'should-not-appear' }`; assert returned string does NOT contain `'should-not-appear'`.
    - Same for INTERROGATE, PRODUCE, PHOTOS, ACCOUNTABILITY.

    Also add: `it('REFLECT with empty operationalProfiles string behaves identically to undefined', ...)` — calling with `{ operationalProfiles: '' }` returns the same string as calling without the field. This is the "all-null path" smoke test — `formatProfilesForPrompt` returns `''` in that case and personality.ts must not prepend an empty header.

    Run `npx vitest run src/chris/__tests__/personality.test.ts` — all tests (existing + new) GREEN.
  </action>
  <acceptance_criteria>
    - `grep -cE "operationalProfiles\\s*\\?\\s*\\`|operationalProfiles[^=]\\s*[\\?]" src/chris/personality.ts | awk '$1>=3 {print "OK"}'` prints `OK` (3 conditional prepends — one per REFLECT/COACH/PSYCHOLOGY case)
    - `grep -c "void operationalProfiles" src/chris/personality.ts` returns 0 (Plan 35-01 slot-reserved no-op removed)
    - `grep -cE "it\\(.*operationalProfiles" src/chris/__tests__/personality.test.ts | awk '$1>=8 {print "OK"}'` prints `OK` (3 positive + 5 negative + 1 empty-string smoke = 9 new tests minimum)
    - `npx vitest run src/chris/__tests__/personality.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*0 failed|^Tests" | head -3` shows zero failed
    - Behavior assertion (positive case): the new REFLECT test passes — `output.indexOf(operationalProfilesString) < output.indexOf(pensieveContextString)`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/chris/__tests__/personality.test.ts --reporter=basic 2>&1 | tail -10</automated>
  </verify>
  <done>
    personality.ts consumes extras.operationalProfiles by prepending it to contextValue ABOVE the {pensieveContext} substitution for REFLECT/COACH/PSYCHOLOGY; out-of-scope modes silently drop the field. 9 new tests in personality.test.ts cover the positive (3) and negative (5) cases + 1 empty-string smoke. Slot-reserved `void operationalProfiles` no-op from Plan 35-01 removed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire REFLECT/COACH/PSYCHOLOGY handlers to call getOperationalProfiles + formatProfilesForPrompt + pass via extras</name>
  <files>src/chris/modes/reflect.ts, src/chris/modes/coach.ts, src/chris/modes/psychology.ts, src/chris/__tests__/reflect.test.ts, src/chris/__tests__/coach.test.ts, src/chris/__tests__/psychology.test.ts</files>
  <read_first>
    - src/chris/modes/reflect.ts (lines 60-90 — buildMessageHistory + buildSystemPrompt call surrounded by relationalContext block; Plan 35-01 left it in `{ language, declinedTopics }` shape)
    - src/chris/modes/coach.ts (same line range)
    - src/chris/modes/psychology.ts (same)
    - src/chris/__tests__/reflect.test.ts (lines 1-110 — vi.mock setup; lines 200-250 — current assertion shapes adjusted in Plan 35-01)
    - src/chris/__tests__/coach.test.ts (same)
    - src/chris/__tests__/psychology.test.ts (same)
    - src/memory/profiles.ts (after Task 1+2 — PROFILE_INJECTION_MAP + formatProfilesForPrompt now exist)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-14 — call order; D-15 — test coverage; D-30 — positive injection tests)
    - .planning/phases/35-surfaces/35-PATTERNS.md (§"src/chris/modes/reflect.ts" lines 414-458 — mirror block)
  </read_first>
  <action>
    Same wiring for all 3 mode handlers. Insert AFTER the relationalContext block (lines 69-72 in reflect.ts; analogous lines in coach.ts and psychology.ts) and BEFORE the `buildSystemPrompt(...)` call.

    Step 1: For each of reflect.ts / coach.ts / psychology.ts, add at the top:
    ```
    import { getOperationalProfiles, formatProfilesForPrompt } from '../../memory/profiles.js';
    ```

    Step 2: Insert immediately before the `buildMessageHistory` call:
    ```
    // Phase 35 — inject operational profiles for in-scope mode (D-14, D-08)
    const profiles = await getOperationalProfiles();
    const operationalProfiles = formatProfilesForPrompt(profiles, 'REFLECT');
    ```
    Change `'REFLECT'` to `'COACH'` in coach.ts, `'PSYCHOLOGY'` in psychology.ts.

    Step 3: Update the `buildSystemPrompt(...)` call to pass operationalProfiles through extras. From Plan 35-01 it currently reads:
    ```
    const systemPrompt = buildSystemPrompt('REFLECT', pensieveContext, relationalContext, { language, declinedTopics });
    ```
    Becomes:
    ```
    const systemPrompt = buildSystemPrompt('REFLECT', pensieveContext, relationalContext, {
      language,
      declinedTopics,
      operationalProfiles,
    });
    ```

    Per D-14: NO caching layer in Phase 35. Each REFLECT/COACH/PSYCHOLOGY invocation reads profiles fresh. Performance Trap caching is deferred to v2.5.1.

    Step 4: For each mode handler test file (reflect/coach/psychology.test.ts), add positive-injection assertions:

    (a) Extend the existing `vi.mock` block with a mock for `'../../memory/profiles.js'`:
    ```
    const mockGetOperationalProfiles = vi.fn();
    const mockFormatProfilesForPrompt = vi.fn();
    vi.mock('../../memory/profiles.js', () => ({
      getOperationalProfiles: mockGetOperationalProfiles,
      formatProfilesForPrompt: mockFormatProfilesForPrompt,
    }));
    ```

    (b) In `beforeEach` add:
    ```
    mockGetOperationalProfiles.mockResolvedValue({ jurisdictional: null, capital: null, health: null, family: null });
    mockFormatProfilesForPrompt.mockReturnValue('## Operational Profile (grounded context — not interpretation)\n\nfake-rendered-profile');
    ```

    (c) Add positive-injection test:
    ```
    it('calls getOperationalProfiles + formatProfilesForPrompt + passes operationalProfiles via extras (Phase 35 SURF-02)', async () => {
      await handleReflect(CHAT_ID, TEST_QUERY);  // or handleCoach / handlePsychology
      expect(mockGetOperationalProfiles).toHaveBeenCalledTimes(1);
      expect(mockFormatProfilesForPrompt).toHaveBeenCalledWith(
        expect.any(Object),
        'REFLECT',  // or 'COACH' / 'PSYCHOLOGY'
      );
      expect(mockBuildSystemPrompt).toHaveBeenCalledWith(
        'REFLECT',
        expect.any(String),
        expect.any(String),
        expect.objectContaining({
          operationalProfiles: expect.stringContaining('Operational Profile'),
        }),
      );
    });
    ```

    (d) Update the existing assertion shape that Plan 35-01 set to `{ language: undefined, declinedTopics: undefined }` — now the assertion must also tolerate `operationalProfiles: <string>`. Use `expect.objectContaining({ language: undefined, declinedTopics: undefined })` (without specifying operationalProfiles) so the existing test does not over-specify.

    Run `npx vitest run src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts` — all green.
  </action>
  <acceptance_criteria>
    - `grep -c "getOperationalProfiles\\|formatProfilesForPrompt" src/chris/modes/reflect.ts` returns ≥ 2 (one import line + one call) — same for coach.ts and psychology.ts
    - `grep -cE "formatProfilesForPrompt\\(profiles,\\s*'REFLECT'\\)" src/chris/modes/reflect.ts` returns 1; same shape with `'COACH'` in coach.ts and `'PSYCHOLOGY'` in psychology.ts
    - `grep -cE "operationalProfiles," src/chris/modes/{reflect,coach,psychology}.ts | grep -cE ":1$" | awk '$1==3 {print "OK"}'` prints `OK` (each of 3 mode handlers passes operationalProfiles via extras)
    - `npx vitest run src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts --reporter=basic 2>&1 | grep "0 failed" | wc -l | awk '$1>=1 {print "OK"}'` prints `OK`
    - Each of reflect/coach/psychology test file has at least 1 new test referencing `mockGetOperationalProfiles.*toHaveBeenCalled`: `grep -lE "mockGetOperationalProfiles.*toHaveBeenCalled" src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts | wc -l | awk '$1==3 {print "OK"}'` prints `OK`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <done>
    REFLECT/COACH/PSYCHOLOGY handlers invoke getOperationalProfiles → formatProfilesForPrompt → buildSystemPrompt(...) per D-14 order; 3 new positive-injection tests in the corresponding test files verify the call chain. Plan 35-01's "expects existing extras shape" assertions remain compatible via `expect.objectContaining(...)`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Negative-injection invariant tests for JOURNAL/INTERROGATE/PRODUCE/PHOTOS + ACCOUNTABILITY (D-28)</name>
  <files>src/chris/__tests__/journal.test.ts, src/chris/__tests__/interrogate.test.ts, src/chris/__tests__/produce.test.ts, src/chris/__tests__/photos.test.ts, src/decisions/__tests__/resolution.test.ts</files>
  <read_first>
    - src/chris/__tests__/journal.test.ts (existing vi.mock setup)
    - src/chris/__tests__/interrogate.test.ts (existing vi.mock setup)
    - src/chris/__tests__/produce.test.ts (existing vi.mock setup)
    - src/chris/__tests__/photos.test.ts (existing vi.mock setup)
    - src/decisions/__tests__/resolution.test.ts (existing vi.mock setup; if file does not exist, Wave 0 VALIDATION.md row 2 requires creating)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-28 — negative invariant; D-30 — 5 negative tests + resolution.test.ts)
    - .planning/phases/35-surfaces/35-VALIDATION.md (Per-Task Verification Map row 35-02-* "negative invariant unit")
  </read_first>
  <action>
    Per D-28: JOURNAL, INTERROGATE, PRODUCE, PHOTOS, ACCOUNTABILITY do NOT call `getOperationalProfiles()`. This is the negative invariant that prevents accidental wiring drift in a future phase. Tests assert the mock is NEVER called.

    Pre-check: verify `src/decisions/__tests__/resolution.test.ts` exists. Run `ls src/decisions/__tests__/resolution.test.ts`. If absent: create it. Use `src/chris/__tests__/journal.test.ts` shape as scaffold (vi.mock setup for the resolution.ts deps, then a minimal handleResolution invocation). If creating, scope minimally — only enough fixture to exercise one handleResolution call so the negative-injection assertion is meaningful. Note: Plan 35-02's resolution.test.ts is OK to be skeletal — full decision-resolution coverage belongs elsewhere (already exists in deferred-items or live-accountability suites).

    Step 1: For each of journal.test.ts / interrogate.test.ts / produce.test.ts / photos.test.ts / resolution.test.ts:

    (a) Add the profiles-module mock at the top:
    ```
    const mockGetOperationalProfiles = vi.fn();
    vi.mock('../../memory/profiles.js', () => ({
      getOperationalProfiles: mockGetOperationalProfiles,
      // formatProfilesForPrompt NOT imported by these handlers — but mock it anyway
      // so any future accidental import is caught loudly.
      formatProfilesForPrompt: vi.fn(),
      PROFILE_INJECTION_MAP: { /* matches production shape */ },
    }));
    ```

    For resolution.test.ts the path is `'../../memory/profiles.js'` from `src/decisions/__tests__/` — adjust import path accordingly.

    (b) In `beforeEach` add `mockGetOperationalProfiles.mockResolvedValue({ jurisdictional: null, capital: null, health: null, family: null });` so even if it WERE accidentally called, it would return safely (defense for test independence).

    (c) Add a negative-invariant test:
    ```
    it('does NOT call getOperationalProfiles (D-28 negative invariant — out-of-scope mode)', async () => {
      await handleJournal(CHAT_ID, TEST_QUERY);  // or handleInterrogate / handleProduce / handlePhotos / handleResolution
      expect(mockGetOperationalProfiles).not.toHaveBeenCalled();
    });
    ```

    For photos.test.ts: handlePhotos signature accepts different params (vision input) — adapt the invocation accordingly. The assertion stays `not.toHaveBeenCalled()`.

    For resolution.test.ts: handleResolution accepts a decision row + ctx. Use whatever minimal fixture the existing file already sets up; if the file is being created fresh, build the smallest fixture that lets handleResolution run to completion without external network. If a complete fixture is infeasible (e.g., needs real DB), mark this test `it.skip` with a TODO referencing D-28 — the skip itself documents the negative invariant intent, and the photos/journal/interrogate/produce coverage suffices for the wire-drift detector.

    Run `npx vitest run src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts src/decisions/__tests__/resolution.test.ts` — all green.
  </action>
  <acceptance_criteria>
    - `grep -lE "mockGetOperationalProfiles.*not\\.toHaveBeenCalled" src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts | wc -l | awk '$1==4 {print "OK"}'` prints `OK`
    - `grep -cE "vi\\.mock\\('\\.\\.\\/\\.\\.\\/memory\\/profiles" src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts | awk -F: '{print $2}' | grep -vc "^0$" | awk '$1==4 {print "OK"}'` prints `OK`
    - `ls src/decisions/__tests__/resolution.test.ts` exits 0 (file exists — either pre-existed or created in this task)
    - `grep -cE "not\\.toHaveBeenCalled|D-28|negative invariant" src/decisions/__tests__/resolution.test.ts` returns ≥ 1 (test exists OR documented skip with rationale)
    - `npx vitest run src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts src/decisions/__tests__/resolution.test.ts --reporter=basic 2>&1 | grep -E "0 failed" | wc -l | awk '$1>=1 {print "OK"}'` prints `OK`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts src/decisions/__tests__/resolution.test.ts --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <done>
    5 negative-invariant tests in place (JOURNAL/INTERROGATE/PRODUCE/PHOTOS + ACCOUNTABILITY via resolution.test.ts) asserting `mockGetOperationalProfiles.not.toHaveBeenCalled()`. The wire-drift detector is in place — if a future phase accidentally adds `getOperationalProfiles()` to one of these handlers, the corresponding test fails immediately. Full Docker test gate (`bash scripts/test.sh`) passes with no regression vs Plan 35-01 baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 33 reader (`getOperationalProfiles`) → formatProfilesForPrompt | Profile JSONB content comes from Sonnet-inferred upserts (Phase 34 generators); treated as semi-trusted output. The format function does NOT escape or sanitize beyond the 2000-char cap. |
| formatProfilesForPrompt → mode-handler extras.operationalProfiles | Pre-rendered string flows into the system prompt. If poisoned, Sonnet sees the poisoned context as "grounded context — not interpretation." Mitigation: Phase 34 generators run against tagged Pensieve entries Greg himself authored — adversarial-content vector is single-user-only. |
| Mode handler → buildSystemPrompt | Already established Phase 35-01 boundary; no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-V11-01 | E (Elevation via topic-drift) | COACH mode injection scope | mitigate | D-08 + PROFILE_INJECTION_MAP locks COACH to `['capital','family']` only — explicit exclusion of `health`. The named-constant pattern (Task 1) prevents per-handler drift. Unit test #11 in Task 2 asserts the map values. If a future contributor adds health to COACH's list, the test fails and the M010-08(b) topic-drift risk is caught at CI time. |
| T-35-02-V11-02 | E (Elevation via stale-fact injection) | formatProfilesForPrompt staleness gate | mitigate | D-10 + Task 2 test #6 enforce the 21-day staleness qualifier. If a 6-week-old jurisdictional profile is injected, Chris sees the explicit `Note: profile data from ... — may not reflect current state.` qualifier — Sonnet conditions its response accordingly. Without the qualifier, Sonnet could treat stale "considering a move to Portugal" as current (M010-08(a)). |
| T-35-02-V11-03 | E (Elevation via speculative health injection) | health < 0.5 gate | mitigate | D-09 + Task 2 tests #4, #5 enforce that health profile renders only when `confidence >= 0.5`. Speculative low-confidence health hypotheses (Sonnet output from sparse 10-entry minimum threshold) never reach the system prompt as "grounded context." |
| T-35-02-V8-01 | I (Information disclosure via unbounded prompt growth) | per-dimension 2000-char cap | mitigate | D-11 + Task 2 test #8 enforce the 2000-char cap per dimension with `...` truncation marker. Worst-case 4 dimensions × 2000 chars = 8000 chars = ~2000 tokens injected per REFLECT call. Performance Trap mitigation. |
| T-35-02-V7-01 | R (Repudiation via silent wiring drift) | D-28 negative invariant | mitigate | Task 5 adds 5 negative-injection tests asserting JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY do NOT call getOperationalProfiles. Future accidental wiring is caught at test-run time, not in production. The PROFILE_INJECTION_MAP constant only includes REFLECT/COACH/PSYCHOLOGY — out-of-scope modes have NO entry, so even if a handler accidentally called `formatProfilesForPrompt(profiles, 'JOURNAL')`, the return value is `''` (D-12.a fallback). Defense-in-depth: explicit test + structural fallback. |
| T-35-02-V7-02 | D (DoS via cache absence) | per-call DB read | accept | D-14 — no caching layer in Phase 35; each REFLECT/COACH/PSYCHOLOGY message triggers 4 DB reads via `getOperationalProfiles()`. Single-user scale (~10 messages/day max). Phase 33's reader uses `Promise.all` parallel reads — overhead ~4ms per message. Caching deferred to v2.5.1 (Deferred Ideas in CONTEXT.md). |

**Security gate disposition:** All high-severity threats mitigated. Two low-severity accepts (caching deferred; ground-up prompt content trust is single-user-only). Proceed.
</threat_model>

<verification>
1. PROFILE_INJECTION_MAP exported from src/memory/profiles.ts with the exact 3 keys + verbatim D-08 dimension arrays.
2. Dimension type re-exported from src/memory/profiles.ts for Plan 35-03 import.
3. formatProfilesForPrompt is a pure function (no I/O, no logger calls, no DB calls) — verified by reading the implementation.
4. 11+ unit tests in src/memory/__tests__/profiles.test.ts cover all 6 gates (out-of-scope mode, all-null, all-zero-conf, health<0.5, staleness, char-cap) + REFLECT-renders-4 + COACH-renders-2 + PSYCHOLOGY-renders-2 + MAP-shape.
5. personality.ts switch-case for REFLECT/COACH/PSYCHOLOGY prepends operationalProfiles to contextValue before .replace('{pensieveContext}', ...) — verified by 3 positive + 5 negative + 1 empty-string smoke test in personality.test.ts.
6. REFLECT/COACH/PSYCHOLOGY mode handlers each have 1 positive test asserting getOperationalProfiles call + formatProfilesForPrompt call + operationalProfiles in buildSystemPrompt extras.
7. JOURNAL/INTERROGATE/PRODUCE/PHOTOS test files each have 1 negative test asserting mockGetOperationalProfiles.not.toHaveBeenCalled().
8. resolution.test.ts exists and has the ACCOUNTABILITY negative-injection test (or documented skip).
9. Full Docker suite (`bash scripts/test.sh`) green at-or-above Plan 35-01 baseline.
10. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
SURF-02 closed: PROFILE_INJECTION_MAP + formatProfilesForPrompt + personality.ts injection point + REFLECT/COACH/PSYCHOLOGY wiring + 3 positive tests + 5 negative tests + 1 ACCOUNTABILITY negative test all ship in this plan. M010-08 mitigation (per-mode subset via named constant) honored. M010-08(a) staleness gate + M010-08(b) health-confidence gate + M010-08(c) per-dimension truncation gate all implemented + unit-tested. Plan 35-03 can now import PROFILE_INJECTION_MAP + Dimension type + formatProfilesForPrompt from src/memory/profiles.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/35-surfaces/35-02-SUMMARY.md`. Surface:
- The new export surface from src/memory/profiles.ts (PROFILE_INJECTION_MAP, formatProfilesForPrompt, Dimension type)
- The 3 positive + 5 negative mode-handler test count
- formatProfilesForPrompt's empty-string return contract (the 4 trigger conditions D-12.a..d)
- The 2000-char truncation behavior + 21-day staleness behavior + health<0.5 gate (all tested)
- Update REQUIREMENTS.md traceability: SURF-02 → Plan 35-02 → status "Complete"
- Note for Plan 35-03 executor: `formatProfileForDisplay` (per-dimension user-facing renderer in profile.ts) is a DIFFERENT function from `formatProfilesForPrompt` (this plan's prompt-side renderer). DO NOT confuse them — display targets Telegram users with localized strings; prompt targets Sonnet with structured second-person facts.
</output>
