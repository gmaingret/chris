---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proactive/mute.ts
  - src/proactive/triggers/opus-analysis.ts
  - .planning/phases/11-identity-grounding/11-01-SUMMARY.md
  - .planning/phases/11-identity-grounding/11-02-SUMMARY.md
  - .planning/phases/11-identity-grounding/11-03-SUMMARY.md
autonomous: true
requirements: []
tags: [identity, tech-debt, frontmatter-hygiene]

must_haves:
  truths:
    - "src/proactive/mute.ts generateMuteAcknowledgment system prompt addresses Greg — 'Chris, Greg\\'s close friend' and 'Greg has asked' both present; literal token 'John' does not appear anywhere in the file"
    - "src/proactive/triggers/opus-analysis.ts OPUS_SYSTEM_PROMPT says 'friendship between Chris and Greg'; literal token 'John' does not appear anywhere in the file"
    - "All three Phase 11 SUMMARY files (11-01, 11-02, 11-03) have a 'requirements-completed:' frontmatter line with values exactly matching the corresponding PLAN.md 'requirements:' field"
    - "Full Docker-backed test suite (./scripts/test.sh) exits 0 — 848/848 (or current suite count) pass"
    - "TEST-03 remains 3-of-3 stable (JOURNAL grounding live-integration block does not regress)"
    - "Project-wide 'John' residuals are confined to the documented by-design surfaces only: CONTRADICTION_DETECTION_PROMPT, RELATIONAL_MEMORY_PROMPT (both in src/llm/prompts.ts), src/memory/relational.ts exchange labels, and JSDoc comments in src/memory/sync/*.ts"
  artifacts:
    - path: "src/proactive/mute.ts"
      provides: "generateMuteAcknowledgment Sonnet system prompt with Greg"
      contains: "Greg's close friend"
    - path: "src/proactive/triggers/opus-analysis.ts"
      provides: "OPUS_SYSTEM_PROMPT with Greg"
      contains: "friendship between Chris and Greg"
    - path: ".planning/phases/11-identity-grounding/11-01-SUMMARY.md"
      provides: "Frontmatter backfilled with requirements-completed"
      contains: "requirements-completed:"
    - path: ".planning/phases/11-identity-grounding/11-02-SUMMARY.md"
      provides: "Frontmatter backfilled with requirements-completed"
      contains: "requirements-completed:"
    - path: ".planning/phases/11-identity-grounding/11-03-SUMMARY.md"
      provides: "Frontmatter backfilled with requirements-completed"
      contains: "requirements-completed:"
  key_links:
    - from: "src/proactive/mute.ts generateMuteAcknowledgment"
      to: "user-visible Chris mute acknowledgment output"
      via: "Sonnet system prompt string"
      pattern: "Chris, Greg's close friend"
    - from: ".planning/phases/11-identity-grounding/11-0{1,2,3}-SUMMARY.md"
      to: "v2.0-MILESTONE-AUDIT 3-source cross-check"
      via: "requirements-completed: frontmatter line"
      pattern: "^requirements-completed:"
---

<objective>
Close the three tech-debt items surfaced by v2.0-MILESTONE-AUDIT.md:

1. L1 user-visible: rename "John" → "Greg" in src/proactive/mute.ts:174 (generateMuteAcknowledgment Sonnet prompt). This slipped through Phase 11's rename sweep whose scope was explicitly the 6 user-facing mode templates + src/proactive/prompts.ts.
2. L2 internal: rename "John" → "Greg" in src/proactive/triggers/opus-analysis.ts:36 (OPUS_SYSTEM_PROMPT — JSON-generation, never user-visible; cosmetic consistency).
3. Frontmatter hygiene: backfill `requirements-completed:` on all 3 Phase 11 SUMMARY files so the v2.0 audit's 3-source cross-check (VERIFICATION + SUMMARY-frontmatter + REQUIREMENTS.md) upgrades from 2-source to 3-source for RETR-01, RETR-02, RETR-04, TEST-03.

Purpose: Mechanical cleanup before /gsd-complete-milestone v2.0. No new features, no refactors, no behavioral change. The Phase 11 TEST-03 gate must remain GREEN.

Output: 5 edited files (2 source, 3 SUMMARY); full Docker suite green; scoped `\bJohn\b` residuals match the audit's documented by-design list exactly.

Scope boundaries (from 12-CONTEXT.md Deferred Ideas — DO NOT TOUCH):
- CONTRADICTION_DETECTION_PROMPT and RELATIONAL_MEMORY_PROMPT in src/llm/prompts.ts — by design (Pitfall 3, classifier-only).
- src/memory/sync/*.ts JSDoc "John" references — cosmetic, accepted tech debt.
- src/memory/relational.ts:58 "John:" exchange label — by design (classifier training-stable label).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/12-identity-rename-residuals-frontmatter-hygiene/12-CONTEXT.md
@.planning/phases/11-identity-grounding/11-VERIFICATION.md
@.planning/phases/11-identity-grounding/11-RESEARCH.md
@.planning/phases/11-identity-grounding/11-01-PLAN.md
@.planning/phases/11-identity-grounding/11-02-PLAN.md
@.planning/phases/11-identity-grounding/11-03-PLAN.md

@src/proactive/mute.ts
@src/proactive/triggers/opus-analysis.ts
@.planning/phases/06-memory-audit/06-01-SUMMARY.md

<interfaces>
<!-- Authoritative values for Task 3, extracted from 11-0{1,2,3}-PLAN.md `requirements:` fields -->

11-01-PLAN.md:
  requirements:
    - RETR-01
    - RETR-02
  → backfill target: requirements-completed: [RETR-01, RETR-02]

11-02-PLAN.md:
  requirements:
    - RETR-01
    - RETR-02
    - RETR-04
  → backfill target: requirements-completed: [RETR-01, RETR-02, RETR-04]

11-03-PLAN.md:
  requirements:
    - TEST-03
    - RETR-04
  → backfill target: requirements-completed: [TEST-03, RETR-04]

Frontmatter format reference (from .planning/phases/06-memory-audit/06-01-SUMMARY.md line 39):
  requirements-completed: [RETR-03]
  (single-line YAML array, placed inside the existing `---`-delimited frontmatter block, near the bottom alongside other metadata fields)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "John" → "Greg" in src/proactive/mute.ts generateMuteAcknowledgment system prompt</name>
  <files>src/proactive/mute.ts</files>
  <read_first>
    - src/proactive/mute.ts (full file — target template literal is at line 174 inside generateMuteAcknowledgment's `system[].text`)
    - .planning/phases/12-identity-rename-residuals-frontmatter-hygiene/12-CONTEXT.md (LOCKED concrete strings)
    - .planning/phases/11-identity-grounding/11-RESEARCH.md Pitfall 3 (what stays "John" by design — confirms this surface is correctly in scope)
  </read_first>
  <action>
    In `src/proactive/mute.ts`, the target is the Sonnet system prompt string literal assigned to `system[0].text` inside `generateMuteAcknowledgment` (currently line 174). Perform these TWO exact substitutions, preserving all other characters (quoting, punctuation, ${dateStr} interpolation, surrounding sentences):

    1. `You are Chris, John's close friend.` → `You are Chris, Greg's close friend.`
    2. `John has asked you to be quiet for a while.` → `Greg has asked you to be quiet for a while.`

    After the edit, the line 174 template literal reads (with `${dateStr}` preserved verbatim):
    `You are Chris, Greg's close friend. Greg has asked you to be quiet for a while. Acknowledge this naturally and warmly in 1-2 sentences. You'll be quiet until ${dateStr}. Don't be robotic or formal — just be a friend who respects boundaries. Don't repeat the exact date back mechanically.`

    DO NOT touch:
    - The fallback string at line 186 (`Got it — I'll give you some space until ${dateStr}.`) — no "John" in it.
    - The JSDoc comment at line 153-156 (`Generate a natural acknowledgment from Chris about being quiet until a date. Uses Sonnet for Chris's natural voice.`) — no "John" in it, but leave as-is regardless.
    - Any variable name, import, type, or function signature in the file.
    - The user-input echo at line 178 (`content: \`Be quiet until ${dateStr}.\``) — no "John" in it.

    Sanity check before committing: the file should have NO remaining `\bJohn\b` matches. Per the audit, this file currently has exactly the 2 occurrences listed above; removing both should zero out the count.
  </action>
  <verify>
    <automated>npx vitest run src/proactive/__tests__/mute.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "You are Chris, Greg's close friend" src/proactive/mute.ts` returns 1.
    - `grep -c "Greg has asked you to be quiet" src/proactive/mute.ts` returns 1.
    - `grep -c '\bJohn\b' src/proactive/mute.ts` returns 0.
    - `grep -c "\${dateStr}" src/proactive/mute.ts` is unchanged from pre-edit count (template interpolation preserved).
    - Existing unit tests for mute.ts still pass (or if no dedicated test file exists, the targeted vitest run exits 0 with "no test files found" treated as non-failing — in which case rely on Task 4's full suite).
    - TypeScript compile clean (no errors from tsc via the test command pipeline).
  </acceptance_criteria>
  <done>
    Two substitutions landed verbatim, zero `\bJohn\b` left in src/proactive/mute.ts, no other file touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename "John" → "Greg" in src/proactive/triggers/opus-analysis.ts OPUS_SYSTEM_PROMPT</name>
  <files>src/proactive/triggers/opus-analysis.ts</files>
  <read_first>
    - src/proactive/triggers/opus-analysis.ts (full file — OPUS_SYSTEM_PROMPT starts at line 36)
    - .planning/phases/12-identity-rename-residuals-frontmatter-hygiene/12-CONTEXT.md (LOCKED concrete strings)
    - .planning/v2.0-MILESTONE-AUDIT.md Tech Debt item 2 (confirms this is L2 internal JSON-generation prompt, never user-visible)
  </read_first>
  <action>
    In `src/proactive/triggers/opus-analysis.ts`, the target is the template literal assigned to `OPUS_SYSTEM_PROMPT` (line 36). Perform this EXACT substitution:

    1. `friendship between Chris and John` → `friendship between Chris and Greg`

    After the edit, line 36 reads:
    `const OPUS_SYSTEM_PROMPT = \`You are an analytical assistant reviewing relational context about a friendship between Chris and Greg.`

    Inspection of the current file shows this is the ONLY `\bJohn\b` occurrence inside OPUS_SYSTEM_PROMPT (no other "John" tokens inside the template literal). If the executor finds additional `\bJohn\b` matches anywhere in the file during the read, rename them to "Greg" as well — but based on the current file contents, only one substitution is required.

    DO NOT touch:
    - The JSDoc comment at lines 1-10.
    - The `AnalysisSection` interface or `OpusAnalysisResult` type (lines 17-27).
    - The `SAFE_DEFAULT` const (lines 31-34).
    - The runOpusAnalysis function body, logger tags, or error-handling paths (lines 66-131).
    - The JSON response shape spec embedded in the prompt (lines 48-62) — no "John" there.

    Sanity check: after the edit, `grep -c '\bJohn\b' src/proactive/triggers/opus-analysis.ts` returns 0.
  </action>
  <verify>
    <automated>npx vitest run src/proactive/triggers/__tests__/opus-analysis.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "friendship between Chris and Greg" src/proactive/triggers/opus-analysis.ts` returns 1.
    - `grep -c "friendship between Chris and John" src/proactive/triggers/opus-analysis.ts` returns 0.
    - `grep -c '\bJohn\b' src/proactive/triggers/opus-analysis.ts` returns 0.
    - TypeScript compile clean.
    - If an opus-analysis unit test file exists, it passes; otherwise rely on Task 4's full suite.
  </acceptance_criteria>
  <done>
    Single substitution landed, zero `\bJohn\b` left in the file, no other file touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill requirements-completed: frontmatter on all 3 Phase 11 SUMMARY files</name>
  <files>.planning/phases/11-identity-grounding/11-01-SUMMARY.md, .planning/phases/11-identity-grounding/11-02-SUMMARY.md, .planning/phases/11-identity-grounding/11-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/11-identity-grounding/11-01-SUMMARY.md (full file — existing frontmatter block)
    - .planning/phases/11-identity-grounding/11-02-SUMMARY.md (full file — existing frontmatter block)
    - .planning/phases/11-identity-grounding/11-03-SUMMARY.md (full file — existing frontmatter block)
    - .planning/phases/11-identity-grounding/11-01-PLAN.md (authoritative source for requirements: field — already loaded in context: [RETR-01, RETR-02])
    - .planning/phases/11-identity-grounding/11-02-PLAN.md (authoritative source: [RETR-01, RETR-02, RETR-04])
    - .planning/phases/11-identity-grounding/11-03-PLAN.md (authoritative source: [TEST-03, RETR-04])
    - .planning/phases/06-memory-audit/06-01-SUMMARY.md (frontmatter format reference — line 39 shows the single-line YAML array style to match)
  </read_first>
  <action>
    For each of the three Phase 11 SUMMARY files, add a single `requirements-completed:` line inside the existing `---`-delimited YAML frontmatter block at the top of the file. Match the single-line YAML array format used in Phase 06/07/09 backfilled SUMMARYs (see 06-01-SUMMARY.md line 39 for the reference style).

    Placement: inside the existing frontmatter block, near the bottom alongside `metrics:` / other scalar fields. Do not create a new `---` delimiter. Do not reorder existing fields. Do not modify any other line in these files (body prose stays verbatim).

    **File 1: `.planning/phases/11-identity-grounding/11-01-SUMMARY.md`**
    Add line: `requirements-completed: [RETR-01, RETR-02]`
    (Values come verbatim from 11-01-PLAN.md `requirements:` field.)

    **File 2: `.planning/phases/11-identity-grounding/11-02-SUMMARY.md`**
    Add line: `requirements-completed: [RETR-01, RETR-02, RETR-04]`
    (Values come verbatim from 11-02-PLAN.md `requirements:` field.)

    **File 3: `.planning/phases/11-identity-grounding/11-03-SUMMARY.md`**
    Add line: `requirements-completed: [TEST-03, RETR-04]`
    (Values come verbatim from 11-03-PLAN.md `requirements:` field — order matches PLAN.md order.)

    DO NOT:
    - Use block YAML style (`- RETR-01` on its own line) — use the single-line array style `[RETR-01, RETR-02]` for consistency with Phase 06/07/09 backfills.
    - Add a new `---` delimiter, a new heading, or a "## Requirements" body section.
    - Modify any existing frontmatter key's value.
    - Edit any line outside the frontmatter block.

    After all three edits, the YAML frontmatter on each file must parse cleanly (verified by the vitest + grep checks below — no YAML parser invocation needed).
  </action>
  <verify>
    <automated>for f in .planning/phases/11-identity-grounding/11-01-SUMMARY.md .planning/phases/11-identity-grounding/11-02-SUMMARY.md .planning/phases/11-identity-grounding/11-03-SUMMARY.md; do echo "=== $f ==="; grep -c '^requirements-completed:' "$f"; grep '^requirements-completed:' "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed:' .planning/phases/11-identity-grounding/11-01-SUMMARY.md` returns 1.
    - `grep -c '^requirements-completed:' .planning/phases/11-identity-grounding/11-02-SUMMARY.md` returns 1.
    - `grep -c '^requirements-completed:' .planning/phases/11-identity-grounding/11-03-SUMMARY.md` returns 1.
    - `grep -F 'requirements-completed: [RETR-01, RETR-02]' .planning/phases/11-identity-grounding/11-01-SUMMARY.md` matches exactly one line (no RETR-04).
    - `grep -F 'requirements-completed: [RETR-01, RETR-02, RETR-04]' .planning/phases/11-identity-grounding/11-02-SUMMARY.md` matches exactly one line.
    - `grep -F 'requirements-completed: [TEST-03, RETR-04]' .planning/phases/11-identity-grounding/11-03-SUMMARY.md` matches exactly one line.
    - The `requirements-completed:` line appears BEFORE the closing `---` delimiter in each file (`awk '/^---$/{c++} c==2{exit} /^requirements-completed:/{print NR}'` prints a line number in each file).
    - No body prose was modified: `git diff --stat` on each SUMMARY file shows only insertions (no deletions) of a single line.
  </acceptance_criteria>
  <done>
    Three files each have one new `requirements-completed:` line with values exactly matching the corresponding PLAN.md `requirements:` field; no other content changed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run full Docker test suite and verify scoped grep residuals match audit's documented by-design list</name>
  <files>(no file edits — verification only)</files>
  <read_first>
    - .planning/phases/12-identity-rename-residuals-frontmatter-hygiene/12-CONTEXT.md (Verification strategy section — defines the scoped grep expectations)
    - .planning/v2.0-MILESTONE-AUDIT.md Tech Debt Summary (authoritative by-design residual list)
    - .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md (TEST-03 gate baseline for regression check)
    - scripts/test.sh (confirms Docker postgres lifecycle)
  </read_first>
  <action>
    Tasks 1-3 must be landed before this task runs. This task is gated on their completion (implicit via same-plan execution order — no explicit `depends_on` between tasks in a single plan, but the executor runs them in order).

    Pre-flight:
    1. `grep -c '\bJohn\b' src/proactive/mute.ts` → 0 (Task 1 done).
    2. `grep -c '\bJohn\b' src/proactive/triggers/opus-analysis.ts` → 0 (Task 2 done).
    3. `grep -c '^requirements-completed:' .planning/phases/11-identity-grounding/11-01-SUMMARY.md` → 1 (and same for 11-02, 11-03; Task 3 done).

    Step A — Run the full Docker-backed test suite per project memory (`always_run_docker_tests` — never skip integration tests, always start real postgres):

    ```
    ./scripts/test.sh
    ```

    Expected: exit 0 with "848 passed" (or the current full-suite count). If the count has drifted from 848 because upstream work added tests, capture the new number; the gate is "exit 0 with zero failures", NOT a hardcoded 848.

    Step B — Confirm TEST-03 3-of-3 stability (non-regression of Phase 11 gate). The full-suite run in Step A already exercises live-integration.test.ts; confirm the vitest summary shows the JOURNAL grounding block passing 3/3. If the full suite passes but TEST-03 is flaky (e.g., 2/3 in this run), record it but do NOT fail the task — Phase 11's own gate already stands at GREEN and this phase does not widen scope.

    Step C — Scoped residual grep. Confirm project-wide `\bJohn\b` hits in src/ are ONLY in the documented by-design surfaces:

    ```
    grep -rn '\bJohn\b' src/
    ```

    Expected hits (and ONLY these — per 12-CONTEXT.md Deferred Ideas and v2.0-MILESTONE-AUDIT.md Tech Debt item 3):
    - `src/llm/prompts.ts` — inside CONTRADICTION_DETECTION_PROMPT and RELATIONAL_MEMORY_PROMPT template literals only (classifier prompts, by-design).
    - `src/memory/relational.ts` — line 58 area, "John:" exchange label (classifier training-stable, by-design).
    - `src/memory/sync/*.ts` — JSDoc comments only (cosmetic, accepted tech debt).

    UNEXPECTED hits (any of the below) → FAIL this task and report back for remediation:
    - Any hit in `src/proactive/mute.ts` (Task 1 regression).
    - Any hit in `src/proactive/triggers/opus-analysis.ts` (Task 2 regression).
    - Any hit in `src/chris/personality.ts`, `src/proactive/prompts.ts`, or the 6 user-facing mode templates in `src/llm/prompts.ts` (Phase 11 regression).
    - Any hit inside code (not JSDoc) in `src/memory/sync/*.ts` — JSDoc-only is acceptable, code tokens are not.

    Step D — Record outcomes. Append a short verification note (plain-text, no new file required — can go in the commit body or the 12-SUMMARY.md this plan produces via the `<output>` section). Note:
    - Suite pass count (e.g., "848/848 passed").
    - TEST-03 per-case results (nationality/location/business — each PASS or FAIL).
    - The exact list of files containing remaining `\bJohn\b` hits, with "by-design" rationale next to each.
  </action>
  <verify>
    <automated>./scripts/test.sh 2>&1 | tail -30 && echo "=== SCOPED JOHN RESIDUALS ===" && grep -rn '\bJohn\b' src/ | grep -vE '(CONTRADICTION_DETECTION_PROMPT|RELATIONAL_MEMORY_PROMPT|src/memory/relational\.ts|src/memory/sync/.*\.ts)' | tee /tmp/unexpected-john.txt; test ! -s /tmp/unexpected-john.txt</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/test.sh` exits 0.
    - Vitest summary shows 0 test failures across the entire suite (unit + integration + live-integration tiers).
    - TEST-03 (JOURNAL grounding) — each of the 3 cases (nationality, location, business) passes; 3-of-3 for this run.
    - TEST-07 (INTERROGATE citation / structured fact accuracy) passes — no regression from the `{ includeDate: false }` wiring preserved by Phase 11.
    - `grep -c '\bJohn\b' src/proactive/mute.ts` returns 0.
    - `grep -c '\bJohn\b' src/proactive/triggers/opus-analysis.ts` returns 0.
    - `grep -c '\bJohn\b' src/chris/personality.ts` returns 0 (Phase 11 baseline preserved).
    - `grep -c '\bJohn\b' src/proactive/prompts.ts` returns 0 (Phase 11 baseline preserved).
    - The "unexpected hits" filter in the `<verify>` command produces an empty file (`/tmp/unexpected-john.txt` has zero bytes).
    - All remaining `\bJohn\b` hits in `src/` reside exclusively in: CONTRADICTION_DETECTION_PROMPT (src/llm/prompts.ts), RELATIONAL_MEMORY_PROMPT (src/llm/prompts.ts), src/memory/relational.ts (exchange label), or JSDoc comments in src/memory/sync/*.ts.
  </acceptance_criteria>
  <done>
    Full Docker suite passes; TEST-03 stable; scoped `\bJohn\b` residuals match the audit's documented by-design list with zero deviations. Phase 12 tech-debt items are closed and ready for /gsd-complete-milestone v2.0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Tasks 1-2 replace one hard-coded persona string with another hard-coded persona string. Task 3 edits YAML frontmatter in documentation files. Task 4 is verification only. No new I/O surface, auth path, data egress, or user-controlled interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | src/proactive/mute.ts Sonnet system prompt | accept | The prompt is sent only to Anthropic API (existing trust relationship). No new PII added — the rename replaces one first-name token with another. No change to data classes. |
| T-12-02 | Tampering | Phase 11 SUMMARY frontmatter | accept | SUMMARY files are documentation artifacts under version control; the `requirements-completed:` field is informational (3-source audit cross-check). Values are authoritatively sourced from the corresponding PLAN.md `requirements:` fields. No trust-sensitive logic reads this field at runtime. |

No other STRIDE categories activate — confirmed by 11-RESEARCH.md Security Domain (V2/V3/V4/V5/V6 all "no") and the fact that this phase makes no algorithmic or surface change beyond string substitution + docs metadata.
</threat_model>

<verification>
Consolidated phase-level checks (after all 4 tasks land):

1. `grep -rn '\bJohn\b' src/proactive/mute.ts src/proactive/triggers/opus-analysis.ts` returns zero hits.
2. `grep -rn '\bJohn\b' src/ | grep -vE '(CONTRADICTION_DETECTION_PROMPT|RELATIONAL_MEMORY_PROMPT|src/memory/relational\.ts|src/memory/sync/.*\.ts)'` returns zero hits.
3. `grep -l 'requirements-completed' .planning/phases/11-identity-grounding/*-SUMMARY.md | wc -l` returns 3.
4. `./scripts/test.sh` exits 0 with full suite pass (848/848 or current count) and TEST-03 3-of-3.
5. Phase 11 baseline preserved: `grep -c '\bJohn\b' src/chris/personality.ts src/proactive/prompts.ts` returns 0 each; `grep -F 'Facts about you (Greg)' src/chris/personality.ts` and `grep -F 'includeDate: false' src/chris/modes/journal.ts` both still match.
</verification>

<success_criteria>
- Zero `\bJohn\b` tokens in src/proactive/mute.ts and src/proactive/triggers/opus-analysis.ts.
- Three Phase 11 SUMMARY files each have a correct `requirements-completed:` line matching their PLAN.md `requirements:` field verbatim.
- Full Docker-backed test suite passes (exit 0, zero failures), per project memory `always_run_docker_tests`.
- TEST-03 JOURNAL grounding stays 3-of-3 stable in this run (non-regression of Phase 11 gate).
- All remaining `\bJohn\b` residuals in src/ are confined to the audit's documented by-design surfaces (CONTRADICTION_DETECTION_PROMPT, RELATIONAL_MEMORY_PROMPT, src/memory/relational.ts exchange label, src/memory/sync/*.ts JSDoc).
- v2.0-MILESTONE-AUDIT 3-source cross-check for RETR-01, RETR-02, RETR-04, TEST-03 upgrades from 2-source to 3-source (VERIFICATION + SUMMARY frontmatter + REQUIREMENTS.md all aligned).
</success_criteria>

<output>
After completion, create `.planning/phases/12-identity-rename-residuals-frontmatter-hygiene/12-01-SUMMARY.md` documenting:
- Exact strings substituted in src/proactive/mute.ts and src/proactive/triggers/opus-analysis.ts (before/after).
- Confirmation that 3 Phase 11 SUMMARY files received `requirements-completed:` with the correct values.
- Full-suite pass count and TEST-03 per-case results from Task 4.
- The final scoped residual grep output confirming only by-design hits remain.
- A `requirements-completed:` frontmatter line of its own (value: `[]` — this phase has no new REQ-IDs; it closes tech debt).
</output>
