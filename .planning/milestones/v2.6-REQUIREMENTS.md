# v2.6 M011 Psychological Profiles — Requirements (Archived)

**Status:** SHIPPED 2026-05-14 — 28/28 requirements complete
**Tag:** v2.6
**Audit:** [v2.6-MILESTONE-AUDIT.md](./v2.6-MILESTONE-AUDIT.md) — PASSED

## v2.6 M011 Requirements

### PSCH — Psychological substrate (Phase 37)

- [x] **PSCH-01**: Migration 0013 creates three tables (`profile_hexaco`, `profile_schwartz`, `profile_attachment`) atomically with the Never-Retrofit Checklist on each. HARD CO-LOC #M11-1 — migration SQL + Drizzle schema + meta snapshot + `scripts/test.sh` psql line + `psychological-schemas.ts` types ship atomically. **Outcome:** Validated — applied cleanly on fresh Docker postgres; 18 Never-Retrofit cols + 2 attachment-only cols verified by smoke gate.
- [x] **PSCH-02**: `profile_hexaco` defines six jsonb dimension columns each typed `{score, confidence, last_updated}` plus `overall_confidence`. **Outcome:** Validated — Drizzle compile-time `.$type<HexacoDimension>` inference accepted by `pnpm tsc --noEmit`.
- [x] **PSCH-03**: `profile_schwartz` defines ten jsonb value columns (same shape as PSCH-02) + `overall_confidence`. **Outcome:** Validated.
- [x] **PSCH-04**: `profile_attachment` defines three jsonb columns (schema-only; population gated on D028 activation trigger). **Outcome:** Validated — schema shipped in migration 0013 with `relational_word_count` + `activated` columns from day 1.
- [x] **PSCH-05**: Cold-start seed rows inserted in migration 0013 for all 3 tables with all dimensions NULL, `overall_confidence=0`, `word_count=0`. **Outcome:** Validated — `getPsychologicalProfiles()` returns valid rows on fresh deploy.
- [x] **PSCH-06**: Zod v3 + Zod v4 dual schemas for all three profile shapes in `src/memory/profiles/psychological-schemas.ts`. **Outcome:** Validated — 35 unit tests covering v3/v4 lockstep, cold-start NULL parsing, boundary cases.
- [x] **PSCH-07**: `loadPsychologicalSubstrate(profileType, now)` filters `pensieve_entries.source='telegram'` AND excludes `epistemic_tag='RITUAL_RESPONSE'`; episodic summaries scoped to previous calendar month via Luxon. **Outcome:** Validated — SQL-level filter at Drizzle layer (NOT JS post-filter); 11 real-DB integration tests.
- [x] **PSCH-08**: 5,000-word floor enforced BEFORE any Sonnet call via inline whitespace word counting. **Outcome:** Validated — discriminated-union return forces `belowThreshold` branch handling at type level.
- [x] **PSCH-09**: `getPsychologicalProfiles()` never-throw contract — returns `null` per profile on DB error; 3-layer Zod v3 parse defense. **Outcome:** Validated — 9 integration + mocked-DB tests.
- [x] **PSCH-10**: `psych-boundary-audit.test.ts` two-directional D047 boundary enforcement. **Outcome:** Validated — 10 test cases, self-allowlisted by absence; still green after Phase 38/39/40 ships.

### PGEN — Psychological inference engine (Phase 38)

- [x] **PGEN-01**: `assemblePsychologicalProfilePrompt` shared builder in `src/memory/psychological-profile-prompt.ts` with `PSYCHOLOGICAL_HARD_RULE_EXTENSION` constant. HARD CO-LOC #M11-2 — builder ships BEFORE either dimension generator. **Outcome:** Validated — 28 structural tests asserting all 3 constants present + per-profileType directives.
- [x] **PGEN-02**: HEXACO generator at `src/memory/profiles/hexaco.ts` — single Sonnet 4.6 call for all 6 dimensions. **Outcome:** Validated — 8 unit tests against mocked SDK.
- [x] **PGEN-03**: Schwartz generator at `src/memory/profiles/schwartz.ts` — single Sonnet 4.6 call for all 10 values. **Outcome:** Validated — 8 unit tests.
- [x] **PGEN-04**: `updateAllPsychologicalProfiles()` orchestrator invokes HEXACO + Schwartz via `Promise.allSettled`. Attachment generator NOT included. **Outcome:** Validated — per-generator error isolation verified by mocked-failure injection test.
- [x] **PGEN-05**: Monthly cron `'0 9 1 * *'` Europe/Paris registered; `cron.validate` fail-fast at config load; `/health` reports `psychological_profile_cron_registered: true`. **Outcome:** Validated — 12-month Luxon-based collision-check test confirms no same-hour overlap with M010 Sunday 22:00 cron.
- [x] **PGEN-06**: **UNCONDITIONAL monthly fire** — substrate_hash recorded for audit but does NOT short-circuit Sonnet call. **Outcome:** Validated — three-cycle integration test asserts cumulative 4 calls after Cycle 2 (NOT 2 — inverse of M010 GEN-07 idempotency); D-18 rationale comment locked in `psychological-profile-updater.ts`.
- [x] **PGEN-07**: `prevHistorySnapshot` passed into Sonnet prompt; model self-reports `data_consistency`; host does NOT compute stddev. **Outcome:** Validated — host-side inter-period math deferred to v2.6.1 / CONS-01.

### PSURF — Psychological surfaces (Phase 39)

- [x] **PSURF-01**: `PSYCHOLOGICAL_PROFILE_INJECTION_MAP` distinct constant — REFLECT/PSYCHOLOGY → `['hexaco', 'schwartz']`, COACH explicitly absent at type level. **Outcome:** Validated — map key union excludes COACH structurally.
- [x] **PSURF-02**: `formatPsychologicalProfilesForPrompt` returns empty string for null/below-threshold/zero-confidence; populated returns block with imported `PSYCHOLOGICAL_HARD_RULE_EXTENSION` footer. **Outcome:** Validated — 13 formatter unit tests + 5 map-shape tests; Hard Rule footer imported (single source of truth) not redeclared.
- [x] **PSURF-03**: `ChrisContextExtras` extended with `psychologicalProfiles?: string`; REFLECT + PSYCHOLOGY handlers wire reader → formatter → buildSystemPrompt. **Outcome:** Validated — 9 substitution-order tests; JOURNAL/INTERROGATE/PRODUCE/PHOTOS/ACCOUNTABILITY silently drop the field.
- [x] **PSURF-04**: `/profile` Telegram command extended with HEXACO + Schwartz + Attachment sections; insufficient-data branch displays "need N more words"; Attachment displays "not yet active (D028)". **Outcome:** Validated — `MSG.m011Placeholder` removed; 3-reply for-of loop ships HEXACO/Schwartz/Attachment via `formatPsychologicalProfileForDisplay`.
- [x] **PSURF-05**: `formatPsychologicalProfileForDisplay` pure function + golden-output inline-snapshot test (HARD CO-LOC #M11-3). **Outcome:** Validated — 15 snapshots (4 scenarios × FR/RU variants); COACH negative-invariant regex sweep test green; no operational tokens leak into psych files; no psych tokens leak into operational files.

### PMT — Psychological milestone tests (Phase 40)

- [x] **PMT-01**: `scripts/synthesize-delta.ts` extended with `--psych-profile-bias` boolean flag + HARN sanity gate. **Outcome:** Validated — flag-pass-through wired in `regenerate-primed.ts` composer (post-ship fix `e553d61`); HARN gate asserts wordCount > 5000 AND ≥1 OPENNESS_SIGNAL_PHRASES present; commit `93b0432` strengthened the Haiku prompt with exemplar phrases (5/6 phrases retained after `--reseed-vcr`).
- [x] **PMT-02**: `tests/fixtures/primed/m011-30days/` (regenerated with `--target-days 60` → 16,325 telegram words + 5/6 signal phrases) + `tests/fixtures/primed/m011-1000words/` sparse fixture. **Outcome:** Validated 2026-05-14 (gitignored per Phase 40 design).
- [x] **PMT-03**: Sparse-threshold real-DB integration test (m011-1000words). **Outcome:** Validated — test structurally complete (FIXTURE_PRESENT skip gate per D045); fires zero Sonnet calls; outcome `'skipped_below_threshold'` per generator.
- [x] **PMT-04**: Populated-fixture real-DB integration test (m011-30days) with ±0.8 tolerance per dimension. **Outcome:** Validated — test structurally complete; ±0.8 tolerance bound empirically justified by speech-inference r ≈ .31–.41 per FEATURES.
- [x] **PMT-05**: Unconditional-monthly-fire three-cycle integration test. **Outcome:** Validated — Cycle 1=2, Cycle 2=4 cumulative (NOT 2, INVERSE of M010 PTEST-03), Cycle 3=6 cumulative; D-24 docblock verbatim locks the inverse-of-idempotency contract.
- [x] **PMT-06**: Live 3-of-3 atomic milestone-gate test against real Sonnet 4.6. **Outcome:** Validated 2026-05-14T13:09Z — 3/3 green; ~$0.25 spend; 57.5s wall-clock; zero hallucinated facts; zero trait-authority constructions matching 5 FORBIDDEN_TRAIT_AUTHORITY_PATTERNS × 16 HEXACO+Schwartz traits.

## Traceability

All 28 requirements: ✓ satisfied. Cross-referenced (a) phase VERIFICATION.md status, (b) SUMMARY.md frontmatter `requirements-completed`, (c) live PMT-06 milestone-gate sign-off.

| Requirement | Phase | Plan | Status |
|-------------|-------|------|--------|
| PSCH-01..06 | 37 | 37-01 | ✓ satisfied |
| PSCH-07..10 | 37 | 37-02 | ✓ satisfied |
| PGEN-01 | 38 | 38-01 | ✓ satisfied |
| PGEN-02, 03, 06, 07 | 38 | 38-02 | ✓ satisfied |
| PGEN-04, 05 | 38 | 38-03 | ✓ satisfied |
| PSURF-01, 02, 03, 05 (COACH) | 39 | 39-01 | ✓ satisfied |
| PSURF-04, 05 (display + golden) | 39 | 39-02 | ✓ satisfied |
| PMT-01..05 | 40 | 40-01 | ✓ satisfied |
| PMT-06 | 40 | 40-02 | ✓ satisfied (live gate 2026-05-14T13:09Z) |

## Future requirements (deferred to v2.6.1)

These items are explicitly out of M011 scope per CONTEXT.md decisions:

- **CONS-01** — Host-side inter-period consistency stddev modulation of `computeProfileConfidence`. Needs ≥3 monthly fires.
- **CONS-02** — Trait change-detection alerts (HEXACO/Schwartz shifts ≥ 0.5 month-over-month).
- **ATT-POP-01** — Attachment dimension population via weekly-sweep activation trigger (D028; 2,000 words relational speech over 60 days).
- **NARR-01** — Sonnet-generated narrative psychological profile summary (interpretation-of-inference = hallucination amplifier; M014).
- **CIRC-01** — Schwartz circumplex-ordering display in `/profile` output.
- **CROSS-VAL-01** — HEXACO × Schwartz cross-validation.
- **SAT-CAL-01** — `wordSaturation` constant tuning post-empirical (after 4-8 months of real M011 operation).

## Post-ship tech debt

Surfaced during PMT-06 operator run (2026-05-14):

1. Loader word-count gap (m011-30days raw → PG count loss; worked around with 60-day fixture; diagnostic owed)
2. m011-1000words contradictions FK violation (worked around inline; permanent fix: pre-filter in synthesize-delta)
3. M010 operational fixture schema_mismatch warns (unrelated to M011 assertions; refresh M010 fixtures in v2.6.1)
