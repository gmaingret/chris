---
phase: 31-rename-voice-note-to-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/0011_rename_daily_voice_note_to_journal.sql
  - src/db/migrations/meta/0011_snapshot.json
  - src/db/migrations/meta/_journal.json
  - src/rituals/voice-note.ts
  - src/rituals/__tests__/voice-note.test.ts
  - src/rituals/__tests__/voice-note-handler.test.ts
  - src/rituals/__tests__/voice-note-suppression.test.ts
  - src/rituals/scheduler.ts
  - src/rituals/wellbeing.ts
  - src/rituals/weekly-review.ts
  - src/rituals/adjustment-dialogue.ts
  - src/rituals/skip-tracking.ts
  - src/rituals/types.ts
  - src/rituals/__tests__/scheduler.test.ts
  - src/rituals/__tests__/wellbeing.test.ts
  - src/rituals/__tests__/skip-tracking.test.ts
  - src/rituals/__tests__/skip-tracking.integration.test.ts
  - src/rituals/__tests__/should-fire-adjustment.test.ts
  - src/rituals/__tests__/prompt-rotation-property.test.ts
  - src/chris/engine.ts
  - src/chris/__tests__/engine.test.ts
  - src/chris/__tests__/engine-mute.test.ts
  - src/chris/__tests__/engine-refusal.test.ts
  - src/chris/__tests__/engine-pp5.test.ts
  - src/chris/__tests__/engine-pp5-adjustment.test.ts
  - src/proactive/sweep.ts
  - scripts/test.sh
  - scripts/regen-snapshots.sh
  - scripts/fire-ritual.ts
  - PLAN.md
  - PRD_Project_Chris.md
  - M009_Ritual_Infrastructure.md
autonomous: true
requirements: []
tags: [rename, terminology-cleanup, voice-note-to-journal, atomic, migration-0011]
must_haves:
  truths:
    - "After this plan, `rg 'voice[-_ ]?note|VoiceNote|voiceNote|VOICE_NOTE' --hidden -g '!node_modules' -g '!dist' -g '!.git' -g '!.planning' -g '!src/db/migrations/0007*' -g '!src/db/migrations/meta/_journal.json'` returns ZERO matches in active source code (excluding the historical 0007 migration file + drizzle journal which retain their original names per D-31-04)."
    - "All 5 file renames done via `git mv` (preserves history): src/rituals/voice-note.ts → journal.ts; src/rituals/__tests__/voice-note*.test.ts → journal*.test.ts (3 test files)."
    - "Migration 0011_rename_daily_voice_note_to_journal.sql exists + drizzle meta updated (_journal.json entry + 0011_snapshot.json)."
    - "PP#5 detector in src/chris/engine.ts accepts BOTH 'ritual_voice_note' (legacy) AND 'ritual_journal' (new) for source_subtype matching (D-31-03 backward compat)."
    - "`bash scripts/test.sh` reports the same test count post-rename (no test deletion or addition); all tests pass."
    - "`npx tsc --noEmit` clean (no type errors from the rename)."
  artifacts:
    - path: "src/db/migrations/0011_rename_daily_voice_note_to_journal.sql"
      provides: "atomic UPDATE rituals SET name='daily_journal' WHERE name='daily_voice_note'"
      contains: "daily_journal, daily_voice_note"
    - path: "src/rituals/journal.ts"
      provides: "fireJournal, recordJournalResponse, shouldSuppressJournalFire, JOURNAL_PROMPT_BAG (or whatever the constant is), all formerly named after voice_note"
      contains: "fireJournal, daily_journal"
  key_links:
    - from: "src/db/migrations/meta/_journal.json"
      to: "src/db/migrations/0011_rename_daily_voice_note_to_journal.sql"
      via: "drizzle journal entry"
      pattern: "0011_rename_daily_voice_note_to_journal"
    - from: "src/rituals/scheduler.ts dispatchRitualHandler"
      to: "src/rituals/journal.ts fireJournal"
      via: "switch case routing"
      pattern: "case 'daily_journal':"
    - from: "src/chris/engine.ts PP#5 detector"
      to: "(legacy + new source_subtype values)"
      via: "set membership check"
      pattern: "ritual_voice_note.*ritual_journal|ritual_journal.*ritual_voice_note"
---

<objective>
Atomic rename of the historically-misnamed `voice_note` ritual to `journal` across the entire active codebase. Phase 26's text-based ritual was named after a feature (audio voice messages) that was never implemented; this rename matches the name to reality.

5 file moves, ~10 function/symbol renames, ~5 string-constant renames, ~30 source files touched (~130 references), 1 new migration (0011) + drizzle meta update, 3 top-level docs updated.

Honor D-31-03 backward-compat: PP#5 detector accepts BOTH `'ritual_voice_note'` and `'ritual_journal'` because Pensieve entries written between Phase 26 ship (2026-04-28) and this rename (2026-05-04) carry the old subtype.

Honor D-31-04 migration shape: `UPDATE rituals SET name='daily_journal' WHERE name='daily_voice_note'`. UUID-preserving. Idempotent (the WHERE clause makes re-run a no-op).

Honor scope boundaries from CONTEXT.md:
- Do NOT rename `src/db/migrations/0007_daily_voice_note_seed.sql` (drizzle journal references it; renaming breaks migration replay)
- Do NOT rename `.planning/phases/26-daily-voice-note-ritual/` directory or its contents (historical archive)
- Do NOT modify drizzle journal entries for migrations 0001..0010
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-rename-voice-note-to-journal/31-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0011 + drizzle meta entries</name>
  <files>
    src/db/migrations/0011_rename_daily_voice_note_to_journal.sql
    src/db/migrations/meta/0011_snapshot.json
    src/db/migrations/meta/_journal.json
  </files>
  <read_first>
    - src/db/migrations/0010_adjustment_dialogue.sql (closest analog — pure DDL/data migration; no schema changes)
    - src/db/migrations/meta/_journal.json (drizzle journal — must add a new entry following the existing pattern; preserve ordering)
    - src/db/migrations/meta/0010_snapshot.json (closest snapshot template)
  </read_first>
  <action>
    Create `src/db/migrations/0011_rename_daily_voice_note_to_journal.sql`:

    ```sql
    -- Migration 0011: Rename daily_voice_note ritual to daily_journal (Phase 31)
    -- The Phase 26 ritual was historically named `daily_voice_note` but the
    -- feature is purely text-based (Chris sends a prompt, Greg replies in text;
    -- audio voice messages get a polite decline per VOICE-05). Naming was
    -- misleading; this rename matches the name to reality.
    --
    -- Idempotent: WHERE clause makes re-run a no-op.
    -- UUID-preserving: only `name` field changes; all FK references in
    -- ritual_fire_events / ritual_responses / ritual_pending_responses /
    -- ritual_config_events point to the UUID and remain valid.

    UPDATE rituals SET name = 'daily_journal'
    WHERE name = 'daily_voice_note';
    ```

    Add corresponding `0011_snapshot.json` (copy 0010_snapshot.json, update version + tag — schema is unchanged so the snapshot content is identical to 0010's; only the metadata changes).

    Append to `src/db/migrations/meta/_journal.json` a new entry following the existing pattern (idx, version, when, tag). Use `tag: "0011_rename_daily_voice_note_to_journal"` and the next sequential idx after the existing 0010 entry.
  </action>
  <verify>
    <automated>node -e "const j = require('./src/db/migrations/meta/_journal.json'); const last = j.entries[j.entries.length - 1]; console.log(last.tag); process.exit(last.tag === '0011_rename_daily_voice_note_to_journal' ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/migrations/0011_rename_daily_voice_note_to_journal.sql` exists
    - `src/db/migrations/meta/0011_snapshot.json` exists
    - `_journal.json` has a new last entry with tag `0011_rename_daily_voice_note_to_journal`
    - The SQL file contains both `daily_journal` and `daily_voice_note` (the rename pair)
  </acceptance_criteria>
  <done>Migration substrate ready. The actual psql apply happens in Plan 31-02.</done>
</task>

<task type="auto">
  <name>Task 2: git mv the 5 source/test files</name>
  <files>
    src/rituals/voice-note.ts → src/rituals/journal.ts
    src/rituals/__tests__/voice-note.test.ts → src/rituals/__tests__/journal.test.ts
    src/rituals/__tests__/voice-note-handler.test.ts → src/rituals/__tests__/journal-handler.test.ts
    src/rituals/__tests__/voice-note-suppression.test.ts → src/rituals/__tests__/journal-suppression.test.ts
  </files>
  <read_first>None — this is a pure file move.</read_first>
  <action>
    Run:
    ```bash
    git mv src/rituals/voice-note.ts src/rituals/journal.ts
    git mv src/rituals/__tests__/voice-note.test.ts src/rituals/__tests__/journal.test.ts
    git mv src/rituals/__tests__/voice-note-handler.test.ts src/rituals/__tests__/journal-handler.test.ts
    git mv src/rituals/__tests__/voice-note-suppression.test.ts src/rituals/__tests__/journal-suppression.test.ts
    ```

    `git mv` preserves history (git tracks the rename via similarity index). Do NOT use `mv` + `git add` — that breaks history continuity for `git blame`/`git log --follow`.

    Note: only 4 file renames listed (voice-note.ts + 3 test files). The frontmatter `files_modified` shows 5 because the 5th is the directory listing itself; only 4 actual `git mv` invocations.
  </action>
  <verify>
    <automated>git status --porcelain | grep -E '^R[ MA]' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain | grep '^R'` shows 4 renamed entries
    - The new files exist at their new paths
    - The old files no longer exist
    - `git log --follow src/rituals/journal.ts` shows the file's pre-rename history (Phase 26 commits visible)
  </acceptance_criteria>
  <done>Files moved. Symbol renames inside the files happen in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Symbol + string-constant rename across all source</name>
  <files>(all source files in files_modified, excluding the historical migration 0007 + drizzle meta of 0007 + .planning/ archives)</files>
  <read_first>
    - src/rituals/journal.ts (the just-renamed file — find all internal symbols to rename)
    - src/chris/engine.ts (PP#5 detector — needs D-31-03 backward-compat dual-accept)
    - src/rituals/types.ts (ritual type union — may reference 'voice_note' as a kind discriminant)
    - 31-CONTEXT.md decisions D-31-01 through D-31-05
  </read_first>
  <action>
    Apply these renames systematically using `sd`/`sed` (preferred) or by reading each file + Edit:

    **Function names:**
    - `fireVoiceNote` → `fireJournal`
    - `recordRitualVoiceResponse` → `recordJournalResponse`
    - `shouldSuppressVoiceNoteFire` → `shouldSuppressJournalFire`
    - `voiceNoteRitual` → `journalRitual` (any variable)
    - Any other camelCase identifier containing `VoiceNote` → `Journal`

    **String literals (user-facing data + log keys + DB row names):**
    - `'daily_voice_note'` → `'daily_journal'` (this is the rituals.name DB row value; in code it appears in dispatcher switch cases, log fields, and seed lookups)
    - `'ritual_voice_note'` → `'ritual_journal'` (Pensieve metadata.source_subtype value)
    - Log key segments containing `voice_note` → `journal` (e.g., `'rituals.voice_note.fired'` → `'rituals.journal.fired'`)
    - `'voice_note'` → `'journal'` for any other discriminant string (e.g., metadata.kind values in ritual_pending_responses)

    **Comments + docstrings:**
    - Any natural-language reference to "voice note" / "voice_note" / "VoiceNote" → "journal" / "journal" / "Journal" — preserve casing context (sentence-start vs. inline)
    - The phrase "daily voice note ritual" in comments → "daily journal ritual" or "evening journal prompt" depending on context
    - Comments referring to "VOICE-05" requirement (polite decline of audio messages) STAY — VOICE-05 is the requirement ID and refers to the audio-decline behavior which is unaffected

    **CRITICAL — D-31-03 PP#5 backward-compat in src/chris/engine.ts:**
    Find the PP#5 detector that checks `entry.metadata?.source_subtype === 'ritual_voice_note'` (or similar). Change to accept BOTH legacy and new:
    ```typescript
    // BEFORE:
    const isRitualResponse = entry.metadata?.source_subtype === 'ritual_voice_note';

    // AFTER (D-31-03 backward-compat for historical entries):
    const RITUAL_JOURNAL_SUBTYPES = ['ritual_voice_note', 'ritual_journal'] as const;
    const isRitualResponse = RITUAL_JOURNAL_SUBTYPES.includes(
      entry.metadata?.source_subtype as typeof RITUAL_JOURNAL_SUBTYPES[number],
    );
    ```
    Add an inline comment explaining the legacy-accept: `// Phase 31 D-31-03: accept both legacy ('ritual_voice_note') and new ('ritual_journal') subtypes; historical Pensieve entries written before 2026-05-04 use the legacy value.`

    **CRITICAL — scripts/test.sh seed-row gate (D-31-05):**
    Find the smoke check that asserts `daily_voice_note` row exists post-migration. Update to assert `daily_journal` exists post-migration 0011. Remove the `daily_voice_note` check (post-0011 it doesn't exist anymore).

    **DO NOT TOUCH:**
    - `src/db/migrations/0007_daily_voice_note_seed.sql` content — historical record; the row it INSERTed has been renamed by 0011
    - `src/db/migrations/meta/_journal.json` entries for 0001..0010 — never modify drizzle journal history
    - `.planning/phases/26-daily-voice-note-ritual/` directory and its contents — historical archive
    - Any other `.planning/` archive (per CONTEXT.md scope)

    **Recommended approach:**
    1. Run `rg -l 'voice[-_ ]?note|VoiceNote|voiceNote|VOICE_NOTE' --hidden -g '!node_modules' -g '!dist' -g '!.tmp' -g '!.git' -g '!.planning' -g '!src/db/migrations/0007*' -g '!src/db/migrations/meta/_journal.json'` to find all sites
    2. For each file, Read → Edit (or `sd 'voice_note' 'journal' <file>` if `sd` is available; falls back to `sed -i 's/voice_note/journal/g; s/VoiceNote/Journal/g; s/voiceNote/journal/g; s/VOICE_NOTE/JOURNAL/g'`)
    3. After bulk-replace, manually inspect `src/chris/engine.ts` to add the D-31-03 backward-compat block (sed will replace `'ritual_voice_note'` → `'ritual_journal'` losing the legacy)
    4. Re-run the rg from step 1; should return ZERO matches in active source
  </action>
  <verify>
    <automated>rg 'voice[-_ ]?note|VoiceNote|voiceNote|VOICE_NOTE' --hidden -g '!node_modules' -g '!dist' -g '!.tmp' -g '!.git' -g '!.planning' -g '!src/db/migrations/0007*' -g '!src/db/migrations/meta/_journal.json' 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'voice[-_ ]?note|VoiceNote|voiceNote|VOICE_NOTE' ...` (with the exclusions above) returns ZERO matches OR returns ONLY the D-31-03 backward-compat reference in src/chris/engine.ts (the legacy 'ritual_voice_note' string in the dual-accept set)
    - `npx tsc --noEmit` reports zero errors
    - `grep -c 'fireJournal' src/rituals/journal.ts` returns ≥ 1 (the function definition exists)
    - `grep -c 'fireVoiceNote' src/rituals/journal.ts` returns 0 (no leftovers)
    - `grep 'ritual_voice_note' src/chris/engine.ts` returns the D-31-03 backward-compat line (intentional exception)
  </acceptance_criteria>
  <done>All in-code symbols + strings renamed. Backward compat for legacy Pensieve entries preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Update top-level docs (PLAN.md, PRD_Project_Chris.md, M009_Ritual_Infrastructure.md)</name>
  <files>PLAN.md, PRD_Project_Chris.md, M009_Ritual_Infrastructure.md</files>
  <read_first>The 3 files (each has 4-5 voice-note references — rename in natural-language prose context)</read_first>
  <action>
    For each of the 3 top-level docs:
    1. Read the file to understand the context of each "voice note" reference
    2. Rename "voice note" / "voice_note" / "VoiceNote" / "Voice Note" → "journal" / "journal" / "Journal" / "Journal" preserving casing context
    3. Phrases like "the daily voice note ritual" → "the daily journal ritual" or "the evening journal prompt"
    4. References to "VOICE-05" requirement ID stay (it's the requirement label for the audio-decline behavior, which is unaffected)

    These docs are user-facing terminology (PRD, planning doc) so be MORE careful here than in code comments — read the surrounding sentence and pick the natural renaming. If a phrase reads awkwardly post-rename, paraphrase rather than mechanically substituting.
  </action>
  <verify>
    <automated>rg 'voice[-_ ]?note|VoiceNote|voiceNote' PLAN.md PRD_Project_Chris.md M009_Ritual_Infrastructure.md 2>&amp;1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'voice[-_ ]?note|VoiceNote|voiceNote' PLAN.md PRD_Project_Chris.md M009_Ritual_Infrastructure.md` returns ZERO matches
    - The 3 docs still read naturally (no obvious search-and-replace clunkers)
  </acceptance_criteria>
  <done>Top-level docs aligned with new terminology.</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite + typecheck</name>
  <files>(none — verification task)</files>
  <read_first>None — read the test output</read_first>
  <action>
    Run the canonical test gate:
    ```bash
    bash scripts/test.sh 2>&1 | tail -50
    ```

    Expected: all M009 ritual tests pass under their new file names + new symbol names. The seed-row gate in scripts/test.sh now checks `daily_journal` (post-migration 0011) instead of `daily_voice_note`.

    Also typecheck:
    ```bash
    npx tsc --noEmit 2>&1 | tail -20
    ```

    If any test fails or typecheck errors: STOP. Read the failure, fix the missed rename site, re-run. Do NOT mark the task done if anything fails.
  </action>
  <verify>
    <automated>bash scripts/test.sh 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` reports the same test-pass count as pre-rename baseline (zero new failures)
    - `npx tsc --noEmit` reports zero errors
    - Spot-check: `journal.test.ts`, `journal-handler.test.ts`, `journal-suppression.test.ts` are all in the test output (proves the file moves were picked up by vitest)
  </acceptance_criteria>
  <done>Code rename verified. Plan 31-02 (live deploy) is unblocked.</done>
</task>

</tasks>

<verification>
1. `rg 'voice[-_ ]?note|VoiceNote|voiceNote|VOICE_NOTE' --hidden -g '!node_modules' -g '!dist' -g '!.tmp' -g '!.git' -g '!.planning' -g '!src/db/migrations/0007*' -g '!src/db/migrations/meta/_journal.json'` returns ZERO matches OR ONLY the D-31-03 backward-compat block in src/chris/engine.ts
2. `git log --follow src/rituals/journal.ts` shows the pre-rename Phase 26 history (proves git mv preserved history)
3. `bash scripts/test.sh` reports same test-pass count as baseline
4. `npx tsc --noEmit` clean
5. Migration 0011 file + drizzle meta entries present and well-formed
6. PP#5 detector backward-compat verified inline (D-31-03)
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| In-flight Pensieve entries → PP#5 detector | Historical entries from Phase 26 ship (2026-04-28) carry `metadata.source_subtype = 'ritual_voice_note'`; new entries post-rename carry `'ritual_journal'`. Detector must accept both or those legacy entries get misclassified. |
| Migration 0011 idempotency | If 0011 runs twice, the second run is a no-op (WHERE clause). If 0011 runs against a pre-Phase-26 DB (no `daily_voice_note` row exists), the UPDATE matches zero rows — safe. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering (silent loss of historical Pensieve detection) | src/chris/engine.ts PP#5 source_subtype check | mitigate | D-31-03 dual-accept set membership; legacy 'ritual_voice_note' permanently accepted alongside new 'ritual_journal'. Tested via the existing engine-pp5 test file (extend with one assertion that both subtypes detect). |
| T-31-02 | Information disclosure (leftover voice-note string in code suggests feature exists) | any source file | mitigate | Final rg check in Task 3 must return ZERO active-code matches (excluding D-31-03 backward-compat block). |
| T-31-03 | Denial of service (migration 0011 fails on prod, leaves DB in inconsistent state) | live deploy | mitigate (deferred to Plan 31-02) | Plan 31-02 applies migration 0011 BEFORE redeploying chris container. If migration fails, container is not redeployed; rituals continue under old name until ops investigates. |
</threat_model>

<success_criteria>
1. All 5 file renames done via `git mv` (history preserved)
2. Migration 0011 + drizzle meta complete
3. ZERO active-code references to voice_note/VoiceNote/voiceNote/VOICE_NOTE outside the D-31-03 backward-compat block
4. PP#5 detector accepts both legacy + new source_subtype values (D-31-03)
5. `bash scripts/test.sh` passes (same test count as baseline)
6. `npx tsc --noEmit` clean
7. Top-level docs (PLAN.md, PRD_Project_Chris.md, M009_Ritual_Infrastructure.md) aligned with new terminology
8. scripts/test.sh seed-row gate updated (D-31-05)
</success_criteria>

<output>
Create `.planning/phases/31-rename-voice-note-to-journal/31-01-SUMMARY.md` per standard template. Document:
- All 5 file renames (with old → new paths)
- Total rename count (sites touched, files modified)
- The D-31-03 backward-compat block in src/chris/engine.ts (quote the actual code)
- Test pass count post-rename (proves zero regressions)
- Any rename sites the executor flagged as "ambiguous" and how they were resolved
</output>
