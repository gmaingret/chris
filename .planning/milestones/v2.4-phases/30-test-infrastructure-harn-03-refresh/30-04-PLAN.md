---
phase: 30-test-infrastructure-harn-03-refresh
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/rituals/__tests__/live-weekly-review.test.ts
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [TEST-31]
tags: [live-llm-test, anti-flattery, skip-if-gate, cost-discipline, hard-co-loc-6]
must_haves:
  truths:
    - "Default `bash scripts/test.sh` SKIPS `src/rituals/__tests__/live-weekly-review.test.ts` (zero API spend on every test run, even with `ANTHROPIC_API_KEY=test-key` set per scripts/test.sh:79). Verified by running `bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` without `RUN_LIVE_TESTS` set and observing the test counts as skipped."
    - "Manual invocation `RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` runs the test against real Sonnet (NOT executed by Phase 30 itself; documented in TESTING.md as post-deploy verification path)."
    - "ZERO changes to marker derivation. The 40-marker import-and-spread block at lines 35-43 (Phase 29-04 canonical) is BYTE-IDENTICAL post-edit. Plan 30-04 modifies only the skipIf line (line 46) + 2 comment markers (lines 4 + 45)."
    - "TESTING.md `### Live Tests (manual gate required)` sub-section (added by Plan 30-01 Task 5 as a skeleton) is extended with the M009 TEST-31 row + manual invocation pattern + explicit cost reference (~$0.05/run)."
  artifacts:
    - path: "src/rituals/__tests__/live-weekly-review.test.ts"
      provides: "skipIf gate tightened from `!ANTHROPIC_API_KEY` to `!RUN_LIVE_TESTS || !ANTHROPIC_API_KEY`"
      contains: "RUN_LIVE_TESTS"
    - path: ".planning/codebase/TESTING.md"
      provides: "M009 TEST-31 row added to the Live Tests table; manual invocation snippet for live-weekly-review.test.ts"
      contains: "live-weekly-review"
  key_links:
    - from: "src/rituals/__tests__/live-weekly-review.test.ts:46 (describe.skipIf predicate)"
      to: "process.env.RUN_LIVE_TESTS"
      via: "logical-OR with !ANTHROPIC_API_KEY"
      pattern: "RUN_LIVE_TESTS.*ANTHROPIC_API_KEY"
    - from: ".planning/codebase/TESTING.md Live Tests table"
      to: "src/rituals/__tests__/live-weekly-review.test.ts"
      via: "row entry in markdown table"
      pattern: "live-weekly-review"
---

<objective>
Flip the skipIf gate on Phase 29-04's `src/rituals/__tests__/live-weekly-review.test.ts` from `!ANTHROPIC_API_KEY` to `!RUN_LIVE_TESTS || !ANTHROPIC_API_KEY` per D-30-03 cost discipline. **Smallest plan in Phase 30 by LoC** (~10 LoC + 1 doc paragraph). HARD CO-LOCATION #6 enforced: TEST-31 lives in its own plan, distinct from Plan 30-02 (TEST-23..30) and Plan 30-03 (TEST-32).

Phase 29-04 already shipped:
- The test file (`live-weekly-review.test.ts`)
- The 40-marker derivation via 3-import spread (lines 35-43; canonical per Phase 29-04 SUMMARY)
- The 3-of-3 atomic loop with `fallbacks === 0` strict assertion (lines 49-97)
- The adversarial week fixture (`fixtures/adversarial-week.ts`)
- A skipIf gate (`!process.env.ANTHROPIC_API_KEY`) — but this gate is foot-gunned because `scripts/test.sh:79` defaults `ANTHROPIC_API_KEY=test-key` (truthy → skip would NOT trigger).

Plan 30-04 adds the `RUN_LIVE_TESTS` upper gate so the default `npm test` skips this file even when `ANTHROPIC_API_KEY=test-key` is set. Manual invocation requires both env vars.

Purpose: Cost discipline. Avoid the foot-gun where someone adds `ANTHROPIC_API_KEY` to GitHub Actions secrets (e.g., for migrations) and silently incurs $0.05/run × N CI invocations.

Output: 1 changed line in `live-weekly-review.test.ts` (the skipIf predicate); 2 comment-marker rewordings (lines 4 + 45); extension to the TESTING.md Live Tests section established by Plan 30-01 Task 5.

**Honors locked decisions:**
- D-30-01 (Plan 30-04 owns the gate flip; Phase 29 owned the implementation)
- D-30-03 (tighten skipIf to BOTH `RUN_LIVE_TESTS` AND `ANTHROPIC_API_KEY`; mirrors M008 TEST-22 pattern)
- D-30-04 (Wave 2; functionally independent of Wave 1 substrate)
- HARD CO-LOC #6 (TEST-31 own plan, NOT bundled with synthetic-fixture or weekly-review impl)

**Honors RESEARCH corrections:**
- Pitfall 10 — `ANTHROPIC_API_KEY=test-key` is truthy and bypasses single-gate skipIf; D-30-03 dual-gate directly mitigates this
- Pitfall 12 — there are 40 markers, NOT 17. Phase 29-04 already exported the canonical 3-import spread at lines 35-43. Plan 30-04 makes ZERO changes to marker derivation — only the skipIf line + comment markers
- 30-PATTERNS.md "src/rituals/__tests__/live-weekly-review.test.ts" section (lines 388-413) — exact one-line edit
- File path is `src/rituals/__tests__/live-weekly-review.test.ts` (NOT `live-anti-flattery.test.ts` as CONTEXT.md drafted) — confirmed at top of file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-infrastructure-harn-03-refresh/30-CONTEXT.md
@.planning/phases/30-test-infrastructure-harn-03-refresh/30-RESEARCH.md
@.planning/phases/30-test-infrastructure-harn-03-refresh/30-PATTERNS.md
@.planning/phases/30-test-infrastructure-harn-03-refresh/30-01-SUMMARY.md

@.planning/phases/29-weekly-review/29-04-SUMMARY.md
@.planning/codebase/TESTING.md

@src/rituals/__tests__/live-weekly-review.test.ts
@src/rituals/weekly-review.ts
@src/rituals/__tests__/fixtures/adversarial-week.ts
@scripts/test.sh

<interfaces>
<!-- Key types and contracts the executor needs. Extracted verbatim from codebase. -->

From src/rituals/__tests__/live-weekly-review.test.ts:1-50 (verified file shape):
```typescript
/**
 * src/rituals/__tests__/live-weekly-review.test.ts — Phase 29 Plan 04 SCAFFOLD
 *
 * PHASE-30: enable in TEST-31 (HARD CO-LOC #6).                              ← line 4 — REWORD
 * ...
 */
import { describe, expect, it } from 'vitest';
import { generateWeeklyObservation } from '../weekly-review.js';
import { ADVERSARIAL_WEEK_INPUT } from './fixtures/adversarial-week.js';

// D-10 refined import block (verbatim — NO redeclaration of any source-set entry):
import { VALIDATION_MARKERS } from '../../chris/__tests__/live-integration.test.js';
import { REFLEXIVE_OPENER_FIRST_WORDS } from '../../chris/praise-quarantine.js';
import { FLATTERY_MARKERS } from '../../episodic/__tests__/live-anti-flattery.test.js';

const FORBIDDEN_FLATTERY_MARKERS: readonly string[] = [
  ...VALIDATION_MARKERS,
  ...Array.from(REFLEXIVE_OPENER_FIRST_WORDS),
  ...FLATTERY_MARKERS,
];                                                                              ← lines 35-43 — UNTOUCHED

// PHASE-30: enable in TEST-31 (flip skipIf, add to scripts/test.sh excluded list).  ← line 45 — REWORD
describe.skipIf(!process.env.ANTHROPIC_API_KEY)(                                 ← line 46 — MODIFY
  'live-weekly-review (PHASE-30: enable in TEST-31; HARD CO-LOC #6)',           ← line 47 — REWORD describe label
  () => {
```

From scripts/test.sh:79 (verified `ANTHROPIC_API_KEY=test-key` default — the foot-gun D-30-03 mitigates):
The script sets `ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-test-key}"` so a single-gate `!ANTHROPIC_API_KEY` skipIf would NOT skip. The new `!RUN_LIVE_TESTS || !ANTHROPIC_API_KEY` predicate skips when EITHER condition holds — `RUN_LIVE_TESTS` is unset by default, so the test skips even with `ANTHROPIC_API_KEY=test-key`.

From .planning/codebase/TESTING.md (Plan 30-01 Task 5 added the skeleton):
The `### Live Tests (manual gate required — RUN_LIVE_TESTS + ANTHROPIC_API_KEY)` sub-section was added by Plan 30-01 with a 1-row table for M008 TEST-22 and a comment "Plan 30-04 will append the M009 TEST-31 row...". Plan 30-04 extends this table with the M009 row + adds a manual-invocation snippet for live-weekly-review.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tighten skipIf gate + reword 2 comment markers in live-weekly-review.test.ts (D-30-03)</name>
  <files>src/rituals/__tests__/live-weekly-review.test.ts</files>
  <read_first>
    - src/rituals/__tests__/live-weekly-review.test.ts:1-50 (verify line numbers — comment at line 4, comment at line 45, describe.skipIf at line 46)
    - 30-PATTERNS.md "src/rituals/__tests__/live-weekly-review.test.ts" section (lines 388-413) — exact one-line edit + comment-marker rewordings
    - 30-RESEARCH.md "Pitfall 10" (lines 478-487) — why dual-gate is needed
    - 30-RESEARCH.md "Pitfall 12" (lines 496-505) — DO NOT change marker derivation; the 40-marker spread at lines 35-43 is canonical
  </read_first>
  <action>
    Apply EXACTLY 3 surgical edits to `src/rituals/__tests__/live-weekly-review.test.ts`. NO other changes — the marker derivation block (lines 35-43), the imports, the test body (lines 49-97), and the timeout (line 99) all stay byte-identical.

    **Edit 1 — line 46 (the skipIf predicate; THE substantive change per D-30-03):**

    ```typescript
    // BEFORE (line 46):
    describe.skipIf(!process.env.ANTHROPIC_API_KEY)(

    // AFTER (line 46):
    describe.skipIf(!process.env.RUN_LIVE_TESTS || !process.env.ANTHROPIC_API_KEY)(
    ```

    The dual-gate (`RUN_LIVE_TESTS || ANTHROPIC_API_KEY`) skips when EITHER env var is missing/falsy. `RUN_LIVE_TESTS` is unset by default — so even when `scripts/test.sh:79` sets `ANTHROPIC_API_KEY=test-key`, the test skips cleanly.

    **Edit 2 — line 4 (file header comment; reword the marker per 30-PATTERNS.md line 408-411):**

    ```typescript
    // BEFORE (line 4):
     * PHASE-30: enable in TEST-31 (HARD CO-LOC #6).

    // AFTER (line 4):
     * PHASE-30: live execution gate flipped per TEST-31 (HARD CO-LOC #6).
    ```

    **Edit 3 — line 45 (inline marker comment above the skipIf; reword per 30-PATTERNS.md line 401):**

    ```typescript
    // BEFORE (line 45):
    // PHASE-30: enable in TEST-31 (flip skipIf, add to scripts/test.sh excluded list).

    // AFTER (line 45):
    // PHASE-30: live execution gate flipped per TEST-31 (HARD CO-LOC #6).
    ```

    **Edit 4 (optional clean-up; per 30-PATTERNS.md line 402-405) — line 47 describe label:**

    ```typescript
    // BEFORE (line 47):
      'live-weekly-review (PHASE-30: enable in TEST-31; HARD CO-LOC #6)',

    // AFTER (line 47):
      'live-weekly-review (HARD CO-LOC #6 — TEST-31)',
    ```

    This rewording removes the obsolete "PHASE-30: enable in TEST-31" planning marker (Phase 30 IS active and this gate flip IS the enable). Cosmetic but keeps the file from carrying stale planning markers.

    **CRITICAL — RESEARCH Pitfall 12 — DO NOT TOUCH:**
    - Lines 30-43 (imports + 40-marker spread): byte-identical post-edit. Per Phase 29-04 SUMMARY line 73 + 46, the 8 + 15 + 17 = 40 markers are canonical. Any redeclaration would violate D-10 refinement.
    - Lines 49-97 (the it() block body): byte-identical. The 3-of-3 atomic loop, the `fallbacks === 0` W-3 LOCK assertion, the per-iteration soft expects, the final hard assertion — ALL Phase 29-04 work; ALL stays.
    - Line 99 (the 90s timeout): byte-identical.

    **Verification trick — diff size budget:**
    Total diff for this task should be ~5-8 changed lines (1 substantive + 2 comment rewordings + 1 optional describe-label cleanup + 0 deletions). If `git diff src/rituals/__tests__/live-weekly-review.test.ts | grep -c '^+'` reports more than 10 added lines, you accidentally touched something else — revert and re-do.

    Per D-30-03 (cost discipline). Per HARD CO-LOC #6 (TEST-31 own plan). Per 30-PATTERNS.md "src/rituals/__tests__/live-weekly-review.test.ts" section.
  </action>
  <verify>
    <automated>grep -E "RUN_LIVE_TESTS.*ANTHROPIC_API_KEY|ANTHROPIC_API_KEY.*RUN_LIVE_TESTS" src/rituals/__tests__/live-weekly-review.test.ts &amp;&amp; ! grep -q "PHASE-30: enable in TEST-31" src/rituals/__tests__/live-weekly-review.test.ts &amp;&amp; bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The skipIf line at the (formerly) line 46 contains BOTH `RUN_LIVE_TESTS` AND `ANTHROPIC_API_KEY` env-var references in a logical-OR (`||`) predicate.
    - `grep "PHASE-30: enable in TEST-31" src/rituals/__tests__/live-weekly-review.test.ts` returns NOTHING (both stale markers reworded).
    - The 40-marker import-and-spread block at lines 35-43 is byte-identical (verify via `grep -c "VALIDATION_MARKERS\\|REFLEXIVE_OPENER_FIRST_WORDS\\|FLATTERY_MARKERS"` returns 6 — 3 imports + 3 spread/Array.from invocations).
    - Default `bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` (without `RUN_LIVE_TESTS`) reports the test as SKIPPED (output should mention "skipped" or zero-tests-run for that describe block).
    - `npx tsc --noEmit` clean for the file.
    - `git diff src/rituals/__tests__/live-weekly-review.test.ts | grep -c '^+[^+]'` returns ≤ 8 added lines (substantive + comment rewordings only).
  </acceptance_criteria>
  <done>
    skipIf gate dual-gated. Default test runs skip the live anti-flattery suite (cost discipline). Marker derivation untouched. Comments updated to reflect Phase 30 owning the gate flip.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend TESTING.md Live Tests table with M009 TEST-31 row + manual invocation snippet</name>
  <files>.planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/codebase/TESTING.md (entire file — locate the `### Live Tests (manual gate required — RUN_LIVE_TESTS + ANTHROPIC_API_KEY)` sub-section added by Plan 30-01 Task 5)
    - 30-PATTERNS.md ".planning/codebase/TESTING.md" section (lines 567-587) — exact M009 row format + manual snippet
    - 30-RESEARCH.md "Pitfall 10" (lines 478-487) — operational mitigation language
  </read_first>
  <action>
    Modify the `### Live Tests (manual gate required)` sub-section in `.planning/codebase/TESTING.md`. Plan 30-01 Task 5 added the section as a skeleton with 1 M008 row + a comment "Plan 30-04 will append the M009 TEST-31 row...". Plan 30-04 fulfills that pending TODO.

    **Edit 1 — extend the Live Tests table with the M009 row:**

    ```markdown
    BEFORE (table established by Plan 30-01 Task 5):

    | File | Phase | Cost / run |
    |------|-------|------------|
    | `src/episodic/__tests__/live-anti-flattery.test.ts` | M008 TEST-22 / D038 | ~$0.45 |

    *Plan 30-04 will append the M009 TEST-31 row (`src/rituals/__tests__/live-weekly-review.test.ts`, ~$0.05/run) after flipping its skipIf gate.*

    AFTER (Plan 30-04 fulfills):

    | File | Phase | Cost / run |
    |------|-------|------------|
    | `src/episodic/__tests__/live-anti-flattery.test.ts` | M008 TEST-22 / D038 | ~$0.45 |
    | `src/rituals/__tests__/live-weekly-review.test.ts` | M009 TEST-31 / Phase 30-04 | ~$0.05 |
    ```

    Delete the placeholder italics line `*Plan 30-04 will append the M009 TEST-31 row...*` (it is now fulfilled).

    **Edit 2 — extend the manual invocation snippet:**

    Plan 30-01 Task 5 added a single bash snippet for `live-anti-flattery.test.ts`. Add an M009-specific snippet below it (or change the existing example to point at the M009 file, since M009 is the more recent reference). Recommended: keep BOTH snippets:

    ```markdown
    **Manual invocation pattern:**

    ```bash
    # M008 TEST-22 (anti-flattery for episodic summarization):
    RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... \
      npx vitest run src/episodic/__tests__/live-anti-flattery.test.ts

    # M009 TEST-31 (anti-flattery for weekly-review observation):
    RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... \
      npx vitest run src/rituals/__tests__/live-weekly-review.test.ts
    ```

    Or via `bash scripts/test.sh` if Docker postgres is also needed for any DB-backed assertions:

    ```bash
    RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... \
      bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts
    ```

    The default `npm test` / `bash scripts/test.sh` runs WITHOUT `RUN_LIVE_TESTS=1` set, so these tests skip cleanly even when `ANTHROPIC_API_KEY` is the truthy `test-key` placeholder set by `scripts/test.sh:79`.
    ```

    **Edit 3 — append a brief note about TEST-31 deferral to post-deploy verification:**

    Add a short paragraph after the snippets:

    ```markdown
    **Verification status (Phase 30):** TEST-31 is NOT executed by Phase 30 itself (cost discipline). The test is staged + gated and ready for manual invocation. Post-deploy verification path: after M009 ships, run the M009 manual invocation pattern above against real Sonnet to confirm the CONSTITUTIONAL_PREAMBLE injection holds end-to-end against adversarial input (3-of-3 atomic against `ADVERSARIAL_WEEK_INPUT`; expected: zero of the 40 forbidden markers across all 3 iterations + zero fallbacks per the W-3 LOCK assertion in the test).
    ```

    Per D-30-03 (TEST-31 cost discipline). Per 30-PATTERNS.md ".planning/codebase/TESTING.md" section. Per HARD CO-LOC #6 documentation.
  </action>
  <verify>
    <automated>grep -c "live-weekly-review" .planning/codebase/TESTING.md &amp;&amp; grep -c "RUN_LIVE_TESTS=1" .planning/codebase/TESTING.md &amp;&amp; ! grep -q "Plan 30-04 will append" .planning/codebase/TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - The Live Tests table has 2 rows: M008 (`live-anti-flattery.test.ts`) AND M009 (`live-weekly-review.test.ts`).
    - The placeholder italics line `*Plan 30-04 will append the M009 TEST-31 row...*` is REMOVED (no longer pending).
    - The manual invocation snippet shows both M008 and M009 commands with the dual `RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=...` env-var prefix.
    - A "Verification status (Phase 30)" paragraph documents that TEST-31 is staged-but-not-executed, with the post-deploy invocation pattern.
    - `grep -c "live-weekly-review" .planning/codebase/TESTING.md` returns ≥ 2 (table row + manual snippet).
  </acceptance_criteria>
  <done>
    TESTING.md Live Tests section fully populated. M008 TEST-22 and M009 TEST-31 patterns side-by-side. Manual invocation discoverable for future operators. HARD CO-LOC #6 documented.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -E "RUN_LIVE_TESTS.*ANTHROPIC_API_KEY|ANTHROPIC_API_KEY.*RUN_LIVE_TESTS" src/rituals/__tests__/live-weekly-review.test.ts` returns the dual-gate predicate line.
2. `bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` reports the test as SKIPPED (zero API spend; default behavior).
3. `git diff src/rituals/__tests__/live-weekly-review.test.ts` shows ≤ 8 added lines (substantive + 2-3 comment rewordings).
4. The 40-marker import-and-spread block at lines 35-43 is byte-identical (verify via grep counts).
5. `.planning/codebase/TESTING.md` Live Tests table has 2 rows; the placeholder italics line is removed.
6. `npx tsc --noEmit` clean for the modified test file.
7. HARD CO-LOC #6 satisfied: TEST-31 lives in its own plan (this one), distinct from synthetic-fixture (Plan 30-02) and cron-registration (Plan 30-03).

**NOT VERIFIED IN THIS PLAN:** The actual live execution of TEST-31 against real Sonnet. That is intentionally deferred to post-deploy verification per D-30-03 cost discipline. The plan SHIPS the gate flip; the operator runs the test manually with `RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-...` after M009 deploys.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → real Anthropic API (Sonnet model) | Manual-invocation only; double-gated by RUN_LIVE_TESTS + ANTHROPIC_API_KEY |
| operator → ANTHROPIC_API_KEY env var | Real key only set in operator's local shell; never committed |
| CI → ANTHROPIC_API_KEY secret (potential foot-gun) | If a future CI workflow sets ANTHROPIC_API_KEY for migrations or seed scripts, the dual-gate prevents accidental TEST-31 firing (RUN_LIVE_TESTS is the upper gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure | API key leak in test logs | mitigate | Test code never prints `process.env.ANTHROPIC_API_KEY`. Test asserts on observation/question text only — never echoes the key. The dual-gate also prevents accidental CI runs that might leak the key into log artifacts. |
| T-30-COST-01 | Resource exhaustion (silent CI billing) | live-weekly-review.test.ts firing in CI when ANTHROPIC_API_KEY is set as a secret for unrelated workflow (migration/seed) | mitigate | D-30-03 dual-gate: `RUN_LIVE_TESTS` is the upper gate; CI workflows never set this without explicit operator decision. Default `npm test` skips even when `ANTHROPIC_API_KEY=test-key` is the truthy placeholder (per scripts/test.sh:79 default). |
| T-30-MARKER-DRIFT | Tampering | Future change to one of the 3 marker source sets (VALIDATION_MARKERS, REFLEXIVE_OPENER_FIRST_WORDS, FLATTERY_MARKERS) | accept | Marker derivation lives in 3 source files Plan 30-04 does NOT touch. If a future plan adds/removes markers, the canonical 40-count grows/shrinks; the test's `[]` assertion stays valid (just more/fewer markers checked). The TEST-31 contract is "zero markers regardless of count" — robust to source-set drift. |
</threat_model>

<success_criteria>
1. `live-weekly-review.test.ts` skipIf line at line 46 contains BOTH `RUN_LIVE_TESTS` AND `ANTHROPIC_API_KEY` env-var references.
2. The 40-marker import-and-spread block at lines 35-43 is byte-identical to the Phase 29-04 canonical (RESEARCH Pitfall 12 honored).
3. Default `bash scripts/test.sh` runs SKIP this test (zero API spend).
4. Manual invocation pattern documented in `.planning/codebase/TESTING.md`.
5. The Live Tests table in TESTING.md has 2 rows: M008 + M009.
6. Total diff size ≤ 8 added lines in the test file (per acceptance criteria).
7. HARD CO-LOC #6 satisfied: TEST-31 in its own plan, distinct from synthetic-fixture (Plan 30-02) and cron-registration (Plan 30-03).
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-infrastructure-harn-03-refresh/30-04-SUMMARY.md` per the standard template, with `requirements-completed: [TEST-31]` in frontmatter.

In the SUMMARY's "Decisions Made" section, document:
- Whether Edit 4 (line 47 describe-label cleanup) was applied (optional cosmetic; document the choice).
- The actual git diff line count (verify ≤ 8 added lines; if more, the executor accidentally touched something else and should revert).
- Confirmation that lines 35-43 (the 40-marker spread) are byte-identical to the Phase 29-04 canonical.
- Whether the default `bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` run reported the test as skipped (proves the dual-gate works).
- The post-deploy verification path: a TODO for the next milestone-completion phase OR for the next post-deploy UAT cycle to manually run `RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... bash scripts/test.sh src/rituals/__tests__/live-weekly-review.test.ts` once Phase 30 ships.
</output>
