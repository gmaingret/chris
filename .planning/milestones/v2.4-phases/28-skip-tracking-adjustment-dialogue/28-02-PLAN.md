---
phase: 28-skip-tracking-adjustment-dialogue
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/rituals/skip-tracking.ts
  - src/rituals/scheduler.ts
  - src/rituals/__tests__/skip-tracking.test.ts
  - src/rituals/__tests__/should-fire-adjustment.test.ts
autonomous: true
requirements: [SKIP-03]
tags: [rituals, skip-tracking, adjustment-dialogue, predicate, cadence-aware, m009]

must_haves:
  truths:
    - "computeSkipCount(ritualId) returns 0 for a fresh ritual with no events; matches the count of fired_no_response events since the most recent reset event (responded / wellbeing_completed) per D-28-03"
    - "shouldFireAdjustmentDialogue(ritual) returns true when ritual.skip_count >= effective_threshold (daily=3, weekly=2); honors per-ritual override via rituals.config.skip_threshold; honors adjustment_mute_until 7-day deferral"
    - "Existing seed migrations 0007/0008/0009 already have correct skip_threshold values per cadence — RESEARCH Landmine 4 — verified by SQL audit task; NO migration 0010 ships from Plan 28-02"
    - "shouldFireAdjustmentDialogue is invoked in runRitualSweep AFTER the atomic-fire claim (RIT-10) and BEFORE dispatchRitualHandler — predicate gates whether to fire the standard handler OR the adjustment dialogue handler (Plan 28-03 wires the actual handler dispatch; Plan 28-02 just exports the predicate and emits an in_dialogue outcome on hit)"
  artifacts:
    - path: "src/rituals/skip-tracking.ts"
      provides: "computeSkipCount + shouldFireAdjustmentDialogue + cadence-default-threshold helpers (no DB schema changes)"
      min_lines: 100
    - path: "src/rituals/scheduler.ts"
      provides: "predicate dispatch — in runRitualSweep, after atomic-fire claim, call shouldFireAdjustmentDialogue; on true, log + return in_dialogue outcome (Plan 28-03 replaces with real handler)"
      contains: "shouldFireAdjustmentDialogue"
    - path: "src/rituals/__tests__/skip-tracking.test.ts"
      provides: "unit tests for computeSkipCount + shouldFireAdjustmentDialogue + cadence-default lookup"
      min_lines: 80
    - path: "src/rituals/__tests__/should-fire-adjustment.test.ts"
      provides: "real-DB integration test wiring the predicate into runRitualSweep + verifying skip_threshold by cadence"
      min_lines: 100
  key_links:
    - from: "src/rituals/skip-tracking.ts computeSkipCount"
      to: "src/db/schema.ts ritualFireEvents"
      via: "drizzle .select() over ritualFireEvents WHERE outcome IN reset-events ORDER BY firedAt DESC LIMIT 1; then COUNT(*) where outcome = fired_no_response AND firedAt >= baseline"
      pattern: "ritualFireEvents"
    - from: "src/rituals/scheduler.ts runRitualSweep"
      to: "src/rituals/skip-tracking.ts shouldFireAdjustmentDialogue"
      via: "import + call after atomic-fire claim, before dispatchRitualHandler"
      pattern: "shouldFireAdjustmentDialogue\\("
---

<objective>
Phase 28 Wave 2 synthesis. Build the cadence-aware skip-threshold predicate ON TOP OF Plan 28-01's substrate. Per RESEARCH Landmine 4: the seed migrations 0007/0008/0009 already have correct skip_threshold values (3/3/2) — Plan 28-02 includes a verify-only SQL audit task, NOT a migration write. Per RESEARCH Open Question 5: computeSkipCount handles the "no events ever" baseline by returning 0. The predicate is wired into runRitualSweep but actually firing the adjustment dialogue is Plan 28-03 scope — Plan 28-02 just emits 'in_dialogue' outcome on hit and returns (the predicate is the gate; the handler is the next plan).

Purpose: SKIP-03 (cadence-aware skip thresholds: daily=3, weekly=2; per-cadence default at row creation; per-ritual override via rituals.config.skip_threshold).

Output: NEW src/rituals/skip-tracking.ts module exporting computeSkipCount + shouldFireAdjustmentDialogue + cadenceDefaultThreshold helpers; scheduler.ts wired to call the predicate; unit + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-VALIDATION.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-01-SUMMARY.md
@src/rituals/types.ts
@src/rituals/scheduler.ts
@src/db/schema.ts
@src/db/migrations/0007_daily_voice_note_seed.sql
@src/db/migrations/0008_wellbeing_seed.sql
@src/db/migrations/0009_weekly_review_seed.sql

<interfaces>
From src/rituals/types.ts (post Plan 28-01):
  - RitualFireOutcome union with 12 variants including 'fired_no_response', 'responded', 'wellbeing_completed', 'window_missed'
  - RITUAL_OUTCOME const map with 12 keys
  - RitualConfigSchema with adjustment_mute_until 9th optional field
  - RitualConfig type with skip_threshold (number int 1-10) + adjustment_mute_until (string datetime nullable optional)

From src/db/schema.ts ritualFireEvents (unchanged):
  Columns id, ritualId, firedAt, outcome (text), metadata (jsonb), createdAt
  Plan 28-01 populates this table from voice-note + wellbeing + weekly-review handlers AND ritualResponseWindowSweep

From src/rituals/scheduler.ts post Plan 28-01 (lines 82-93):
  runRitualSweep entry sequence:
    1. logger.info('rituals.sweep.start')
    2. await ritualResponseWindowSweep(now)  ← ADDED by Plan 28-01
    3. STEP 0: hasReachedRitualDailyCap channel-cap check
    4. STEP 1: SELECT due rituals LIMIT 1
    ... (existing STEP 2-5)
    5. STEP 5: tryFireRitualAtomic
    6. STEP 6: dispatchRitualHandler
  Plan 28-02 INSERTS shouldFireAdjustmentDialogue check between STEP 5 and STEP 6 — when true, return in_dialogue outcome instead of dispatching the standard handler.

From migrations (RESEARCH Landmine 4 — already correct, NO write needed):
  - 0007_daily_voice_note_seed.sql:33 — config jsonb has "skip_threshold":3 ✓ (daily default = 3)
  - 0008_wellbeing_seed.sql:29 — config jsonb has "skip_threshold": 3 ✓ (daily default = 3)
  - 0009_weekly_review_seed.sql:51 — config jsonb has "skip_threshold": 2 ✓ (weekly default = 2)

From CONTEXT.md D-28-03 + D-28-04:
  - "computeSkipCount queries ritual_fire_events from the most recent reset event (or ritual creation) and counts fired_no_response outcomes since"
  - "Daily=3, Weekly=2. Set at row creation per cadence type. Per-ritual override via rituals.config.skip_threshold (already in RitualConfigSchema, already validated 1-10)"
  - "An intermediate 'responded' resets the count to 0; subsequent 'fired_no_response' events restart counting"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify seed-skip_threshold values via SQL audit (NO migration writes)</name>
  <files>src/rituals/__tests__/skip-tracking.test.ts</files>
  <read_first>
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 4 (lines 527-533) — explicit: 0007/0008/0009 are ALREADY correct; this task is a verification task NOT a migration task
    - src/db/migrations/0007_daily_voice_note_seed.sql:33
    - src/db/migrations/0008_wellbeing_seed.sql:29
    - src/db/migrations/0009_weekly_review_seed.sql:51
  </read_first>
  <behavior>
    - Test 1: After scripts/test.sh runs all 9 migrations, the daily_voice_note row has config->>'skip_threshold' = '3' (daily default per D-28-04).
    - Test 2: daily_wellbeing has config->>'skip_threshold' = '3'.
    - Test 3: weekly_review has config->>'skip_threshold' = '2' (weekly default).
    - Test 4 (negative): If anyone changes one of the seed configs to a wrong value (e.g. skip_threshold=5 on weekly_review), this test FAILS — verifying the audit is meaningful (not just tautology).
  </behavior>
  <action>
    Create or extend src/rituals/__tests__/skip-tracking.test.ts (this same file will hold the unit tests in Task 3 — start it here).

    Add a top-level describe block: 'Phase 28 SKIP-03 — Seed skip_threshold audit (RESEARCH Landmine 4)'.

    For each of the three seeded rituals (daily_voice_note, daily_wellbeing, weekly_review), write an `it()` test that:
       1. Queries db.select({ skipThreshold: sql<number>\`(config->>'skip_threshold')::int\` }).from(rituals).where(eq(rituals.name, $name)).limit(1)
       2. Asserts the returned skipThreshold matches the cadence default (3 for daily_*; 2 for weekly_*)

    The test relies on scripts/test.sh having already applied migrations 0007, 0008, 0009 — those migrations seed the rows. No setup needed beyond the standard test harness.

    Document inline at the top of this describe block: "Per RESEARCH Landmine 4 — verified 2026-04-29 — ALL seed migrations have correct skip_threshold today. This audit is a regression detector: if someone bumps skip_threshold via a future seed without coordinating with Phase 28's predicate logic, this test catches the drift before runtime mis-firing."
  </action>
  <verify>
    <automated>bash scripts/test.sh src/rituals/__tests__/skip-tracking.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/test.sh src/rituals/__tests__/skip-tracking.test.ts reports the 3 audit tests passing
    - File src/rituals/__tests__/skip-tracking.test.ts exists with a "Seed skip_threshold audit" describe block
    - The describe block contains exactly 3 it() tests, one per seeded ritual
    - No migration 0010 file exists in src/db/migrations/ at this point (Plan 28-03 will create it for a different reason — metadata column on ritual_pending_responses)
  </acceptance_criteria>
  <done>
    Audit task confirms RESEARCH Landmine 4 — seed migrations 0007/0008/0009 have correct skip_threshold values today. Plan 28-02 ships ZERO migration writes. Future seed-config drift is caught by this regression test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/rituals/skip-tracking.ts with computeSkipCount + shouldFireAdjustmentDialogue + cadenceDefaultThreshold</name>
  <files>src/rituals/skip-tracking.ts</files>
  <read_first>
    - src/rituals/types.ts post Plan 28-01 (RITUAL_OUTCOME const map keys, RitualConfig type with adjustment_mute_until)
    - src/db/schema.ts:367-389 (rituals table — has skipCount integer column)
    - src/db/schema.ts:457-464 (ritualFireEvents shape)
    - src/decisions/lifecycle.ts:107-133 (M007 D004 replay-projection precedent — analog for D-28-03 spirit)
    - src/proactive/state.ts:169-176 (predicate-with-denormalized-counter shape)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/skip-tracking.ts" section (lines 153-249) — implementation pre-specified
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Open Question 5 (lines 588-591) — baseline-zero-events case
  </read_first>
  <behavior>
    - Test 1 (computeSkipCount, no events): returns 0 for a freshly-seeded ritual with zero ritualFireEvents rows.
    - Test 2 (computeSkipCount, only fired events): seed 5 'fired' events; computeSkipCount returns 0 (only 'fired_no_response' counts).
    - Test 3 (computeSkipCount, fired_no_response only): seed 3 'fired_no_response' events; returns 3.
    - Test 4 (computeSkipCount, reset by responded): seed [fired_no_response, fired_no_response, responded, fired_no_response]; returns 1 (count from after the 'responded' baseline).
    - Test 5 (computeSkipCount, reset by wellbeing_completed): seed [fired_no_response, fired_no_response, wellbeing_completed, fired_no_response, fired_no_response]; returns 2.
    - Test 6 (shouldFireAdjustmentDialogue, daily under threshold): ritual.type='daily', skipCount=2, threshold=3 → returns false.
    - Test 7 (shouldFireAdjustmentDialogue, daily at threshold): skipCount=3, threshold=3 → returns TRUE.
    - Test 8 (shouldFireAdjustmentDialogue, weekly at threshold): ritual.type='weekly', skipCount=2, threshold=2 → TRUE.
    - Test 9 (shouldFireAdjustmentDialogue, per-ritual override): config.skip_threshold=5 (override), skipCount=4 → false; skipCount=5 → TRUE.
    - Test 10 (shouldFireAdjustmentDialogue, adjustment_mute_until in future): adjustment_mute_until = now+1d, skipCount >= threshold → returns FALSE (D-28-08 7-day deferral honored).
    - Test 11 (shouldFireAdjustmentDialogue, adjustment_mute_until in past): adjustment_mute_until = now-1d, skipCount >= threshold → returns TRUE (mute expired, predicate fires).
    - Test 12 (cadenceDefaultThreshold): returns 3 for 'daily' cadence; 2 for 'weekly'; 2 for 'monthly' (forward-compat per D-28-04); 1 for 'quarterly'.
  </behavior>
  <action>
    Create src/rituals/skip-tracking.ts. File header JSDoc cites: "Phase 28 Plan 02 (SKIP-03) — cadence-aware skip-threshold predicate + replay projection. ON TOP OF Plan 28-01 substrate (ritual_fire_events writes from all 3 ritual handlers + ritualResponseWindowSweep). Per CONTEXT.md D-28-03: skip_count is denormalized projection, rebuildable by replay; this module owns the rebuild logic. Per RESEARCH Landmine 4: NO migration writes — seed migrations 0007/0008/0009 already have correct skip_threshold values."

    Imports:
       - drizzle-orm: { and, desc, eq, gte, sql }
       - ../db/connection.js: { db }
       - ../db/schema.js: { rituals, ritualFireEvents }
       - ./types.js: { RITUAL_OUTCOME, type RitualConfig }

    Export 1: cadenceDefaultThreshold(cadence): number
       Pure function. Switch on cadence ('daily'|'weekly'|'monthly'|'quarterly') returning 3/2/2/1 respectively (per D-28-04 + RESEARCH OQ#5 forward-compat). Cite "// D-28-04: monthly=2 / quarterly=1 are forward-compat for M013 (no monthly/quarterly rituals exist in M009)".

    Export 2: async computeSkipCount(ritualId: string): Promise<number>
       Body matches PATTERNS.md lines 187-219 verbatim with one fix: the IN clause must include ALL reset-class outcomes from RITUAL_OUTCOME — RESPONDED, WELLBEING_COMPLETED, and (forward-compat for Plan 28-03) ADJUSTMENT_COMPLETED if Plan 28-03 introduces that outcome. For Plan 28-02, ship with the two known reset outcomes (RESPONDED + WELLBEING_COMPLETED). Plan 28-03's adjustment-completion outcome is added in 28-03's plan body.

       Step 1: SELECT firedAt FROM ritualFireEvents WHERE ritualId = $1 AND outcome IN ('responded', 'wellbeing_completed') ORDER BY firedAt DESC LIMIT 1. Use sql template literal with `IN (${RITUAL_OUTCOME.RESPONDED}, ${RITUAL_OUTCOME.WELLBEING_COMPLETED})` for safety (drizzle parameterizes string params).
       Step 2: baseline = lastReset?.firedAt ?? new Date(0) (epoch fallback per RESEARCH OQ#5).
       Step 3: SELECT COUNT(*)::int FROM ritualFireEvents WHERE ritualId = $1 AND outcome = 'fired_no_response' AND firedAt >= baseline. Return the count.

    Export 3: async shouldFireAdjustmentDialogue(ritual): Promise<boolean>
       Signature: `(ritual: typeof rituals.$inferSelect): Promise<boolean>`. Body:
       Step 1: Parse cfg from ritual.config via parseRitualConfig (import from ./types.js — but careful: we already import RITUAL_OUTCOME and RitualConfig type; ALSO add parseRitualConfig to the named imports). Wrap in try/catch and return false on parse error (defensive — caller scheduler.ts will already have caught config_invalid earlier in the sweep, but defense-in-depth).
       Step 2: Per D-28-08 — if cfg.adjustment_mute_until is set AND the parsed date is in the future relative to now, return false (7-day deferral honored).
       Step 3: const threshold = cfg.skip_threshold (always present per RitualConfigSchema; cadence-default audit in Task 1 confirms this matches cadenceDefaultThreshold(ritual.type) for the 3 seeded rituals).
       Step 4: return ritual.skipCount >= threshold.

       JSDoc: "Per CONTEXT.md D-28-04 + Plan 28-01 D-28-03 — predicate consults the denormalized rituals.skip_count column (incremented by Plan 28-01's ritualResponseWindowSweep). The denormalization tradeoff is documented in D-28-03; computeSkipCount is the rebuild fallback for audit / disaster recovery."

    Add unit tests (Tests 1-12 above) to src/rituals/__tests__/skip-tracking.test.ts (extending the file from Task 1). Tests 1-5 require real DB (use the seedRitualWithFireEvents fixture from Plan 28-01 src/rituals/__tests__/fixtures/skip-tracking.ts). Tests 6-12 are pure-function (no DB) — pass in mock ritual objects directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep skip-tracking | head -10 && bash scripts/test.sh src/rituals/__tests__/skip-tracking.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to src/rituals/skip-tracking.ts or skip-tracking.test.ts
    - bash scripts/test.sh src/rituals/__tests__/skip-tracking.test.ts reports all 15 tests passing (3 audit from Task 1 + 12 from Task 2)
    - File src/rituals/skip-tracking.ts exports exactly 3 named functions: cadenceDefaultThreshold, computeSkipCount, shouldFireAdjustmentDialogue (verifiable by `grep -E "^export (async )?function" src/rituals/skip-tracking.ts | wc -l` returning 3)
    - File contains zero string-literal outcome references — only RITUAL_OUTCOME.* (verifiable by `grep -E "'(responded|fired_no_response|wellbeing_completed)'" src/rituals/skip-tracking.ts | wc -l` returning 0)
    - File JSDoc cites RESEARCH Landmine 4 (the no-migration-needed verdict): grep -F "Landmine 4" src/rituals/skip-tracking.ts returns at least 1 match
  </acceptance_criteria>
  <done>
    Three pure helpers shipped. computeSkipCount handles the no-events baseline correctly per OQ#5. shouldFireAdjustmentDialogue honors per-ritual overrides AND adjustment_mute_until 7-day deferral. All references go through RITUAL_OUTCOME const map.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire shouldFireAdjustmentDialogue into runRitualSweep — emit in_dialogue outcome on hit</name>
  <files>src/rituals/scheduler.ts, src/rituals/__tests__/should-fire-adjustment.test.ts</files>
  <read_first>
    - src/rituals/scheduler.ts:158-219 (STEP 5 atomic-fire claim + STEP 6 dispatch — the new predicate inserts BETWEEN them)
    - src/rituals/skip-tracking.ts (just-created in Task 2)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/scheduler.ts" section (lines 253-274)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md "Integration Points" (lines 205-214) — runRitualSweep order is documented; Plan 28-02 inserts shouldFireAdjustmentDialogue check between atomic-fire and dispatch
  </read_first>
  <behavior>
    - Test 1: When shouldFireAdjustmentDialogue returns true (ritual.skipCount >= threshold), runRitualSweep returns a result with outcome='in_dialogue' for that ritual; dispatchRitualHandler is NOT called (proven by spy / mock); rituals.skip_count is NOT touched on this branch (the threshold-met state persists; Plan 28-03's actual dialogue handler will reset on user reply).
    - Test 2: When shouldFireAdjustmentDialogue returns false, runRitualSweep proceeds to dispatchRitualHandler as before — the predicate check is transparent on the false path.
    - Test 3: Predicate check happens AFTER tryFireRitualAtomic claim succeeds — verifiable by inspecting log order: 'rituals.fire.atomic' precedes 'rituals.adjustment_dialogue.predicate_hit' (when applicable).
    - Test 4: With adjustment_mute_until in future + skipCount over threshold, shouldFireAdjustmentDialogue returns false; runRitualSweep proceeds with standard dispatch.
  </behavior>
  <action>
    Edit src/rituals/scheduler.ts:

    (a) Add import at top of file (after existing imports near line 44):
        import { shouldFireAdjustmentDialogue } from './skip-tracking.js';

    (b) Inside runRitualSweep, AFTER the if (!fireResult.fired) race-loss branch closes at line 176 and BEFORE the try { const outcome = await dispatchRitualHandler(...) } block at line 183, INSERT the predicate check:

        // Phase 28 Plan 02 SKIP-03 — adjustment-dialogue predicate dispatch.
        // After atomic-fire claim succeeds (next_run_at advanced — RIT-10), check
        // whether skip_count has reached the cadence-aware threshold. If yes,
        // skip standard handler dispatch and emit in_dialogue outcome — Plan
        // 28-03 will replace this with the actual fireAdjustmentDialogue handler.
        // Plan 28-02 ships the predicate gate; Plan 28-03 wires the handler.
        if (await shouldFireAdjustmentDialogue(ritual)) {
          logger.info(
            { ritualId: ritual.id, skipCount: ritual.skipCount, type: ritual.type },
            'rituals.adjustment_dialogue.predicate_hit',
          );
          await incrementRitualDailyCount(config.proactiveTimezone);
          results.push({
            ritualId: ritual.id,
            type: ritual.type,
            fired: false,
            outcome: 'in_dialogue',
          });
          return results;
        }

    The 'in_dialogue' outcome string is already in the union (Plan 25 shipped it in anticipation of Phase 28 — see types.ts line 81 JSDoc "'in_dialogue' — Phase 28: user is mid-conversation, defer to next tick"). Plan 28-02 is the FIRST consumer of the outcome.

    Note: skipCount is NOT touched on this branch — the threshold-met state persists. Plan 28-03's actual dialogue handler will reset skip_count on Greg's reply (or after the 60s confirmation window applies the patch). This is intentional: the predicate is the gate, not the action.

    Note: incrementRitualDailyCount IS called on this branch — the channel-cap accounting tracks "ritual fire attempts that consumed the channel slot", per scheduler.ts:204-208 existing comment. The adjustment dialogue consumes the slot.

    Create src/rituals/__tests__/should-fire-adjustment.test.ts:
       - Real-DB integration test using the seedRitualWithFireEvents fixture from Plan 28-01
       - vi.hoisted mocks for bot.api.sendMessage (mocked since dispatchRitualHandler may invoke handlers that send Telegram messages)
       - vi.hoisted mocks for the 3 fire handlers (fireVoiceNote, fireWellbeing, fireWeeklyReview) so we can spy on them and assert NOT called when predicate hits
       - 4 it() tests covering the 4 behaviors above
       - Each test seeds a fresh ritual via the fixture, sets skipCount appropriately, then calls runRitualSweep(now) and asserts on the returned results array + spy call counts
       - Cumulative afterAll mockAnthropicCreate.not.toHaveBeenCalled() invariant
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scheduler|should-fire" | head -10 && bash scripts/test.sh src/rituals/__tests__/should-fire-adjustment.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to scheduler.ts or should-fire-adjustment.test.ts
    - bash scripts/test.sh src/rituals/__tests__/should-fire-adjustment.test.ts reports all 4 tests passing
    - grep -E "shouldFireAdjustmentDialogue" src/rituals/scheduler.ts reports >= 2 matches (1 import + 1 invocation)
    - grep -E "rituals.adjustment_dialogue.predicate_hit" src/rituals/scheduler.ts reports exactly 1 match (the new logger line)
    - grep -E "outcome: 'in_dialogue'" src/rituals/scheduler.ts reports exactly 1 match (the new branch return)
    - bash scripts/test.sh src/rituals/__tests__/scheduler.test.ts (existing test file from Phase 25/26/29) still passes — Plan 28-02 changes do not break the scheduler regression suite
  </acceptance_criteria>
  <done>
    runRitualSweep checks shouldFireAdjustmentDialogue after atomic-fire claim. On threshold-met: emits in_dialogue, increments daily counter, returns. On below-threshold: passes through to existing dispatch. Plan 28-03 will replace the in_dialogue stub with the actual adjustment-dialogue handler dispatch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computeSkipCount → ritualFireEvents read | read-only projection; no mutation; replay-deterministic |
| shouldFireAdjustmentDialogue → ritual.config parse | strict-mode Zod parse from Plan 25 D-25-02; rejects unknown fields. adjustment_mute_until added in Plan 28-01 Task 1 — opt-in, defaults to undefined |
| scheduler.ts predicate → in_dialogue outcome | new branch in runRitualSweep; consumes channel-cap slot; does NOT mutate skip_count; defers actual dialogue handler to Plan 28-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-S2 | Spoofing | rituals.config.skip_threshold operator-supplied override | mitigate | RitualConfigSchema z.number().int().min(1).max(10) — bounded; strict mode rejects unknown fields. Operator cannot inject skip_threshold = 0 (which would auto-fire dialogue on every miss). |
| T-28-T2 | Tampering | denormalized rituals.skip_count drift from event log | mitigate | computeSkipCount is the audit/recovery path. Plan 28-02 unit Test 4 + 5 prove the projection rule against varied event sequences. Plan 28-01's per-handler skip_count writes happen in same code path as event insert (sequential, accepted tradeoff per D-28-03). |
| T-28-T3 | Tampering | adjustment_mute_until injection (operator could set far-future date to permanently mute the dialogue) | accept | Operator-controlled config field; same trust level as enabled flag. Greg has direct DB access; no Telegram-surface mutation. |
| T-28-D2 | DoS | computeSkipCount unbounded scan of ritualFireEvents | mitigate | Query has WHERE ritualId = $1 (indexed via ritual_responses_ritual_id_fired_at_idx in Phase 25; ritualFireEvents has no analogous index but rows-per-ritual is bounded by daily fire frequency × 90-day audit window — practical max ~120 rows). For absolute safety, future Plan could add idx on (ritual_id, fired_at DESC); deferred. |

</threat_model>

<verification>
Phase 28 Plan 02 acceptance:

1. **TS clean**: npx tsc --noEmit reports zero errors attributable to changed files.
2. **All 3 tasks' targeted vitest runs green**: skip-tracking.test.ts (15 tests: 3 audit + 12 unit), should-fire-adjustment.test.ts (4 tests).
3. **Full Docker gate**: bash scripts/test.sh src/rituals/ reports rituals suite green; substrate gates (0006-0009) all pass; existing scheduler.test.ts from Phase 25/26/29 still passes.
4. **Predicate honors all 3 inputs**: cadenceDefaultThreshold + cfg.skip_threshold override + adjustment_mute_until — proven by Tests 6-11.
5. **NO migration 0010 created in this plan**: ls src/db/migrations/0010_*.sql returns empty (Plan 28-03 owns the metadata column migration).
6. **No regression on Plan 28-01 acceptance**: bash scripts/test.sh src/rituals/__tests__/skip-tracking.integration.test.ts still green.
</verification>

<success_criteria>
ROADMAP success criterion 1 (Phase 28) full coverage of cadence-threshold logic:
- "After 3 consecutive fired_no_response outcomes on a daily ritual (or 2 on weekly), the next sweep tick fires the adjustment dialogue message INSTEAD of the standard prompt" — Plan 28-02 ships the PREDICATE that gates the dispatch. The actual fireAdjustmentDialogue handler is Plan 28-03; Plan 28-02 emits 'in_dialogue' outcome as the gate signal.
- "system_suppressed and window_missed outcomes do NOT count toward the threshold" — proven by Plan 28-01's outcome-filter rules + Plan 28-02 computeSkipCount Test 2 (only fired_no_response counts).
- "rituals.skip_count is rebuildable from ritual_fire_events by replay" — proven by Plan 28-02 computeSkipCount Tests 1-5.
</success_criteria>

<output>
After completion, create .planning/phases/28-skip-tracking-adjustment-dialogue/28-02-SUMMARY.md. SUMMARY frontmatter MUST include `requirements-completed: [SKIP-03]` per PROC-02 upstream verdict.
</output>
