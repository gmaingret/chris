---
phase: 28-skip-tracking-adjustment-dialogue
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - src/db/migrations/0010_adjustment_dialogue.sql
  - src/db/migrations/meta/0010_snapshot.json
  - src/db/migrations/meta/_journal.json
  - src/db/schema.ts
  - src/rituals/adjustment-dialogue.ts
  - src/rituals/scheduler.ts
  - src/chris/engine.ts
  - src/cron-registration.ts
  - src/index.ts
  - scripts/test.sh
  - src/rituals/__tests__/adjustment-dialogue.integration.test.ts
  - src/rituals/__tests__/confirmation-window.test.ts
  - src/chris/__tests__/engine-pp5-adjustment.test.ts
autonomous: true
requirements: [SKIP-04, SKIP-05]
tags: [rituals, skip-tracking, adjustment-dialogue, llm-haiku, confirmation-window, migration-0010, m009]

must_haves:
  truths:
    - "Migration 0010 adds metadata jsonb column (default '{}'::jsonb, NULLABLE) + partial index on ritual_pending_responses for adjustment_confirmation rows"
    - "src/db/schema.ts mirror of metadata column lands in the same plan as the migration (HARD CO-LOC #7 lineage parity)"
    - "scripts/test.sh applies migration 0010 + asserts metadata column exists BEFORE vitest runs (per Phase 25-26-27-29 seed-row gate pattern)"
    - "fireAdjustmentDialogue + handleAdjustmentReply + confirmConfigPatch + ritualConfirmationSweep export from src/rituals/adjustment-dialogue.ts"
    - "Haiku 3-class classification via messages.parse + zodOutputFormat with v3+v4 dual schema; retry-cap-2; templated EN-only fallback; confidence < 0.7 defaults to 'evasive'"
    - "60s confirmation window via DB-row + new 1-minute cron (NOT setTimeout); narrow ritualConfirmationSweep helper (NOT runRitualSweep) per RESEARCH Landmine 5"
    - "PP#5 in src/chris/engine.ts dispatches by metadata.kind — NULL/absent kind falls through to existing voice-note path (RESEARCH Landmine 6); 'adjustment_dialogue' and 'adjustment_confirmation' kinds short-circuit with return ''"
    - "scheduler.ts replaces Plan 28-02's stub 'in_dialogue' return with real fireAdjustmentDialogue dispatch"
    - "Cumulative mockAnthropicCreate.not.toHaveBeenCalled() assertion in adjustment-dialogue.integration.test.ts proves Pitfall 6 invariant preserved across the new PP#5 branches"
  artifacts:
    - path: "src/db/migrations/0010_adjustment_dialogue.sql"
      provides: "ALTER TABLE ritual_pending_responses ADD COLUMN metadata jsonb DEFAULT '{}'::jsonb + partial index"
      contains: "ALTER TABLE \"ritual_pending_responses\""
    - path: "src/db/migrations/meta/0010_snapshot.json"
      provides: "drizzle-kit snapshot mirror of post-migration schema (hand-cloned from 0009 with re-chained id/prevId per Phase 27 SUMMARY pattern)"
      min_lines: 1500
    - path: "src/db/schema.ts"
      provides: "metadata jsonb column on ritualPendingResponses + partial index on (expires_at) WHERE consumed_at IS NULL AND metadata->>'kind' = 'adjustment_confirmation'"
      contains: "metadata: jsonb('metadata')"
    - path: "src/rituals/adjustment-dialogue.ts"
      provides: "fireAdjustmentDialogue + handleAdjustmentReply + handleConfirmationReply + confirmConfigPatch + ritualConfirmationSweep"
      min_lines: 250
    - path: "src/chris/engine.ts"
      provides: "PP#5 dispatch by metadata.kind preserving voice-note default branch (Pitfall 6 invariant)"
      contains: "handleAdjustmentReply"
    - path: "src/cron-registration.ts"
      provides: "1-minute cron tick wired to deps.ritualConfirmationSweep (separate from ritualSweepCron); CronRegistrationStatus extended with ritualConfirmation field"
      contains: "ritualConfirmationSweep"
    - path: "scripts/test.sh"
      provides: "migration 0010 apply + metadata column assertion gate (mirrors Phase 26-27-29 seed-row gate pattern)"
      contains: "0010_adjustment_dialogue"
  key_links:
    - from: "src/chris/engine.ts PP#5"
      to: "src/rituals/adjustment-dialogue.ts handleAdjustmentReply / handleConfirmationReply"
      via: "metadata.kind switch — voice-note default path preserved when kind IS NULL"
      pattern: "handleAdjustmentReply|handleConfirmationReply"
    - from: "src/rituals/scheduler.ts runRitualSweep"
      to: "src/rituals/adjustment-dialogue.ts fireAdjustmentDialogue"
      via: "REPLACES Plan 28-02 in_dialogue stub with real fireAdjustmentDialogue dispatch"
      pattern: "fireAdjustmentDialogue\\("
    - from: "src/cron-registration.ts every-minute cron"
      to: "src/rituals/adjustment-dialogue.ts ritualConfirmationSweep"
      via: "cron.schedule('* * * * *', ritualConfirmationSweep) — narrow helper, NOT runRitualSweep"
      pattern: "\\* \\* \\* \\* \\*"

user_setup: []
---

<objective>
Phase 28 Wave 3 high-LLM-surface plan. Front-loads migration 0010 (adds metadata jsonb to ritual_pending_responses — RESEARCH Landmine 2 says column does NOT exist today). Builds the adjustment dialogue handler with Haiku 3-class classification, 60s confirmation window via DB-row + new 1-minute cron sweep, and PP#5 dispatch by metadata.kind. The PP#5 extension is the highest-risk change in Phase 28 — Pitfall 6 (voice-note path regression) is mitigated by a kind===NULL/undefined fallback to the existing voice-note path AND a cumulative mockAnthropicCreate.not.toHaveBeenCalled() integration test.

Purpose: SKIP-04 (adjustment dialogue + Haiku 3-class) + SKIP-05 (60s confirmation window — auto-apply on yes/no-reply, abort on no).

Output: Migration 0010 + drizzle meta snapshot + scripts/test.sh gate + schema.ts mirror; src/rituals/adjustment-dialogue.ts module; PP#5 dispatch extension; 1-minute cron + index.ts call-site update; 3 test files (mocked-Haiku integration, real-DB confirmation-window, engine-pp5-adjustment regression).

**This plan replaces Plan 28-02's `outcome: 'in_dialogue'` stub in scheduler.ts with the real fireAdjustmentDialogue handler dispatch — that change MUST land in this plan, not deferred to Plan 28-04.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-CONTEXT.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-VALIDATION.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-01-SUMMARY.md
@.planning/phases/28-skip-tracking-adjustment-dialogue/28-02-SUMMARY.md
@.planning/phases/26-daily-voice-note-ritual/26-02-SUMMARY.md
@src/rituals/types.ts
@src/rituals/voice-note.ts
@src/rituals/skip-tracking.ts
@src/rituals/scheduler.ts
@src/rituals/weekly-review.ts
@src/chris/engine.ts
@src/chris/refusal.ts
@src/cron-registration.ts
@src/index.ts
@src/db/schema.ts
@src/db/migrations/0007_daily_voice_note_seed.sql
@src/db/migrations/meta/_journal.json
@scripts/test.sh

<interfaces>
From migration 0009 → 0010 lineage (RESEARCH Landmine 2 + Phase 27 SUMMARY pattern):
  - Pure-DDL migration adding nullable column with DEFAULT '{}'::jsonb on ritual_pending_responses
  - drizzle-kit may report "No schema changes" when run after 0009 if the schema.ts mirror lands in the SAME commit AS the SQL — pattern: hand-clone meta/0009_snapshot.json to meta/0010_snapshot.json with re-chained id/prevId, then add a new entry to _journal.json
  - scripts/test.sh adds a new MIGRATION_10_SQL constant + apply line + post-apply assertion gate

From RESEARCH Landmine 1 (ritual_config_events schema):
  Actual schema: { id, ritualId, actor varchar(32), patch jsonb, createdAt }
  CONTEXT.md D-28-09 description (change_kind / old_value / new_value / source / source_metadata) is INCORRECT.
  Resolution: discriminated envelope inside `patch` jsonb. actor varchar(32) gets values like 'system' | 'user' | 'auto_apply_on_timeout'. patch.kind ∈ {'apply'|'abort'|'auto_disable'|'auto_re_enable'|'patch_aborted'}. Plan 28-03 applies confirmations in this shape; Plan 28-04 extends with refusal-path writes.

From RESEARCH Landmine 5 (cron isolation):
  runRitualSweep is currently invoked from 21:00 cron + 10:00 sweep + manual scripts.
  Plan 28-03 must add a NARROW NEW helper ritualConfirmationSweep — NOT extend runRitualSweep itself with a 1-minute tick.
  cron.schedule('* * * * *', () => deps.ritualConfirmationSweep()) — separate call site.

From RESEARCH Landmine 6 (PP#5 metadata NULL fallback):
  After Plan 28-03 migration 0010, voice-note pending rows have metadata = '{}' (default). PP#5 dispatch reads `(pending.metadata as { kind?: string } | null)?.kind`. When kind is undefined, fallthrough to existing recordRitualVoiceResponse — preserves Phase 26 voice-note path.

From src/rituals/weekly-review.ts:263-285 (Haiku messages.parse + zodOutputFormat pattern — verbatim copy):
  v3+v4 dual schema; v4 at SDK boundary; v3 for runtime re-validate. eslint-disable-next-line @typescript-eslint/no-explicit-any on the format cast.

From src/chris/refusal.ts:131-156 (detectRefusal — read-only reuse for Plan 28-04 refusal pre-check; Plan 28-03 does NOT add refusal pre-check, that lives in 28-04):
  Function signature: detectRefusal(text: string): RefusalResult.

From src/chris/engine.ts:167-208 (PP#5 voice-note path):
  Existing branch dispatches to recordRitualVoiceResponse on findActivePendingResponse hit. Plan 28-03 wraps this in a metadata.kind switch, preserves the default branch.

From src/index.ts:88-93 (registerCrons call site):
  Plan 28-03 adds ritualConfirmationSweep to the deps object passed.

From scripts/test.sh:14-92 (migration apply + substrate gate pattern):
  MIGRATION_N_SQL constants (lines 8-17). After migration apply (line 67), substrate gates assert presence of seeded rows / partial indexes / column existence. Plan 28-03 adds MIGRATION_10_SQL apply + metadata-column assertion mirroring the prompt_text-NOT-NULL gate at line 119-127.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0010 + meta snapshot + journal entry + schema.ts mirror</name>
  <files>src/db/migrations/0010_adjustment_dialogue.sql, src/db/migrations/meta/0010_snapshot.json, src/db/migrations/meta/_journal.json, src/db/schema.ts</files>
  <read_first>
    - src/db/migrations/0007_daily_voice_note_seed.sql (DEFAULT-then-DROP-DEFAULT idiom; partial-index pattern)
    - src/db/migrations/0008_wellbeing_seed.sql (idempotency guards in pure-DML migration)
    - src/db/migrations/0009_weekly_review_seed.sql (no-op DDL for snapshot byte-stability — but Plan 28-03 IS DDL, not pure DML, so different rules)
    - src/db/migrations/meta/0009_snapshot.json (clone source for 0010_snapshot.json)
    - src/db/migrations/meta/_journal.json (entries — add idx 10 for 0010)
    - src/db/schema.ts:485-505 (ritualPendingResponses — extend with metadata column + adjustment_confirmation partial index)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/db/migrations/0010_adjustment_dialogue.sql" section (lines 278-322) — exact migration contents pre-specified
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 2 (lines 516-520) — confirms column does NOT exist today
  </read_first>
  <action>
    (a) Create src/db/migrations/0010_adjustment_dialogue.sql with this exact content (per PATTERNS.md lines 296-318):

        -- 0010_adjustment_dialogue.sql — Phase 28 (M009 v2.4) — adjustment dialogue substrate.
        -- Adds metadata jsonb to ritual_pending_responses so PP#5 can dispatch by
        -- metadata.kind ∈ {'adjustment_dialogue', 'adjustment_confirmation'}.
        -- Per RESEARCH Landmine 2: column does NOT exist today (verified by
        -- grep -n metadata src/db/schema.ts against ritualPendingResponses block
        -- at lines 485-505).
        --
        -- DEFAULT '{}'::jsonb avoids backfill ambiguity for existing voice-note
        -- rows (zero on prod today; zero in dev fixtures). Mirrors Phase 26
        -- prompt_text DEFAULT-then-DROP-DEFAULT pattern (0007:19-23) BUT we keep
        -- the DEFAULT in place because metadata is NULLABLE — voice-note rows
        -- legitimately have no metadata; PP#5 fallback (RESEARCH Landmine 6)
        -- treats NULL/missing kind as the voice-note default path.
        --
        -- IF NOT EXISTS guard makes this idempotent under raw-psql apply path
        -- (scripts/test.sh) and operator re-runs.

        ALTER TABLE "ritual_pending_responses"
          ADD COLUMN IF NOT EXISTS "metadata" jsonb DEFAULT '{}'::jsonb;
        --> statement-breakpoint

        -- Partial index for the 1-minute confirmation sweep (D-28-06 + RESEARCH Landmine 5).
        -- Scoped to rows where consumed_at IS NULL AND metadata->>'kind' = 'adjustment_confirmation'.
        -- ritualConfirmationSweep helper does an indexed range scan on expires_at;
        -- sub-millisecond when zero rows pending.
        CREATE INDEX IF NOT EXISTS "ritual_pending_responses_adjustment_confirmation_idx"
          ON "ritual_pending_responses" USING btree ("expires_at")
          WHERE "consumed_at" IS NULL AND "metadata"->>'kind' = 'adjustment_confirmation';

    (b) Create src/db/migrations/meta/0010_snapshot.json by cloning 0009_snapshot.json. Then make these specific edits:
       - Generate a new UUID for the top-level "id" field (use `uuidgen` via shell or hand-write a v4 UUID like `1234abcd-...`)
       - Set "prevId" to the existing 0009 snapshot's "id" value (read from 0009_snapshot.json's first line)
       - Locate the "ritual_pending_responses" table block in the cloned snapshot. Inside its "columns" object, ADD a new "metadata" entry mirroring the schema.ts addition: `{ "name": "metadata", "type": "jsonb", "primaryKey": false, "notNull": false, "default": "'{}'::jsonb" }`
       - Inside the same table block's "indexes" object, ADD an entry for "ritual_pending_responses_adjustment_confirmation_idx" with: `{ "name": "ritual_pending_responses_adjustment_confirmation_idx", "columns": [{"expression": "expires_at", "isExpression": false, "asc": true, "nulls": "last"}], "isUnique": false, "concurrently": false, "method": "btree", "where": "\"consumed_at\" IS NULL AND \"metadata\"->>'kind' = 'adjustment_confirmation'" }`

       Reference: read existing partial index entries (e.g., `ritual_pending_responses_chat_id_active_idx`) in 0009_snapshot.json to match the exact structural shape — the JSON keys (especially `where`, `method`) must match drizzle-kit's serialization format.

    (c) Append a new entry to src/db/migrations/meta/_journal.json's "entries" array:
        ```json
        { "idx": 10, "version": "7", "when": <current epoch ms>, "tag": "0010_adjustment_dialogue", "breakpoints": true }
        ```
       Use a current-time timestamp via `node -e "console.log(Date.now())"` and substitute into the new entry. Maintain the existing JSON formatting (preserved indentation matching prior entries).

    (d) Edit src/db/schema.ts ritualPendingResponses block (lines 485-505):
       - Add `metadata: jsonb('metadata').default(sql\`'{}'::jsonb\`),` AFTER the `promptText` line (line 494) and BEFORE `createdAt` (line 495)
       - In the index list (lines 497-504), AFTER the existing chat_id_active_idx entry (line 501-503), ADD a second index entry:
         ```typescript
         index('ritual_pending_responses_adjustment_confirmation_idx')
           .on(table.expiresAt)
           .where(sql`${table.consumedAt} IS NULL AND ${table.metadata}->>'kind' = 'adjustment_confirmation'`),
         ```

    (e) Run `npx drizzle-kit generate` to verify the schema.ts <-> migration <-> snapshot all align. If drizzle-kit reports "No schema changes, nothing to migrate", the meta snapshot was already correct (manually-cloned). If drizzle-kit emits a NEW SQL file, DELETE it (the hand-authored 0010_adjustment_dialogue.sql is canonical) but DO NOT delete the auto-generated meta snapshot if drizzle-kit produced one — instead, diff it against the hand-cloned 0010_snapshot.json and reconcile any differences (drizzle-kit's auto-snapshot is the gold standard for byte-exact format).

    Note on idempotency: per Phase 26 D-26-01-B and Phase 27 SUMMARY pattern — if drizzle-kit auto-generates a new SQL file (e.g., 0011_*.sql or similar), DELETE that auto-generated SQL because the hand-authored 0010_adjustment_dialogue.sql is canonical (it has the comment block + idempotency guards). Keep ONLY the meta snapshot from drizzle-kit if it's byte-stable; otherwise keep the hand-cloned snapshot.
  </action>
  <verify>
    <automated>ls src/db/migrations/0010_adjustment_dialogue.sql src/db/migrations/meta/0010_snapshot.json && grep -c '"idx": 10' src/db/migrations/meta/_journal.json && grep -c "metadata: jsonb" src/db/schema.ts && grep -c "ritual_pending_responses_adjustment_confirmation_idx" src/db/schema.ts && grep -c "ritual_pending_responses_adjustment_confirmation_idx" src/db/migrations/meta/0010_snapshot.json</automated>
  </verify>
  <acceptance_criteria>
    - File src/db/migrations/0010_adjustment_dialogue.sql exists and contains "ADD COLUMN IF NOT EXISTS \"metadata\" jsonb"
    - File src/db/migrations/meta/0010_snapshot.json exists and is well-formed JSON (verifiable by `node -e "JSON.parse(require('fs').readFileSync('src/db/migrations/meta/0010_snapshot.json'))"` not throwing)
    - _journal.json contains an entries[] entry with `"idx": 10` AND `"tag": "0010_adjustment_dialogue"`
    - src/db/schema.ts ritualPendingResponses block now has a metadata column entry: grep -c "metadata: jsonb" src/db/schema.ts >= 1
    - schema.ts also has the new partial index: grep -c "ritual_pending_responses_adjustment_confirmation_idx" src/db/schema.ts >= 1
    - Snapshot has the index too: grep -c "ritual_pending_responses_adjustment_confirmation_idx" src/db/migrations/meta/0010_snapshot.json >= 1
    - npx tsc --noEmit reports zero errors attributable to schema.ts changes
  </acceptance_criteria>
  <done>
    Migration SQL + drizzle meta + journal + schema.ts mirror all aligned. Per Phase 25 HARD CO-LOC #7 spirit, all 3 lineage artifacts (SQL, snapshot, journal) ship in the same task; schema.ts mirror is also same-task per drizzle-kit alignment requirement.
  </done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2 [BLOCKING]: Apply migration 0010 to fresh Docker postgres + extend scripts/test.sh with apply line + metadata-column assertion gate</name>
  <files>scripts/test.sh</files>
  <read_first>
    - scripts/test.sh in full (182 LoC; understand the per-migration apply pattern lines 14-67 and the per-Phase substrate-gate pattern lines 75-155)
    - src/db/migrations/0010_adjustment_dialogue.sql (just created in Task 1)
  </read_first>
  <action>
    (a) Edit scripts/test.sh:

       - Add `MIGRATION_10_SQL="src/db/migrations/0010_adjustment_dialogue.sql"` to the constants block at line 17 (after the existing MIGRATION_9_SQL line)
       - Add a psql apply line for migration 0010 after the existing migration 9 apply (line 66-67):
         ```bash
         docker compose -f "$COMPOSE_FILE" exec -T postgres \
           psql -U chris -d chris -v ON_ERROR_STOP=1 -q < "$MIGRATION_10_SQL"
         ```
       - Add a Phase 28 substrate gate AFTER the Phase 29 weekly_review seed gate (after line 155, before the anchor-bias regression guard at line 157). Mirror the prompt_text NOT-NULL gate shape from lines 119-127:
         ```bash
         # Phase 28 (M009 v2.4) — adjustment dialogue substrate gate.
         # Migration 0010 adds metadata jsonb column to ritual_pending_responses
         # (RESEARCH Landmine 2 — column did not exist before Phase 28). Plan 28-03
         # depends on this column being present for PP#5 dispatch by metadata.kind.
         # Failure exits BEFORE vitest so a missing column blocks the test suite.
         METADATA_COL_CHECK=$(docker compose -f "$COMPOSE_FILE" exec -T postgres \
           psql -U chris -d chris -tAc \
           "SELECT column_name FROM information_schema.columns
            WHERE table_name='ritual_pending_responses' AND column_name='metadata' LIMIT 1;")

         if [[ "$METADATA_COL_CHECK" != "metadata" ]]; then
           echo "❌ FAIL: ritual_pending_responses.metadata column missing after migration 0010 (got: '$METADATA_COL_CHECK')"
           exit 1
         fi

         METADATA_IDX_CHECK=$(docker compose -f "$COMPOSE_FILE" exec -T postgres \
           psql -U chris -d chris -tAc \
           "SELECT indexname FROM pg_indexes WHERE indexname = 'ritual_pending_responses_adjustment_confirmation_idx';")

         if [[ "$METADATA_IDX_CHECK" != "ritual_pending_responses_adjustment_confirmation_idx" ]]; then
           echo "❌ FAIL: adjustment_confirmation partial index missing after migration 0010 (got: '$METADATA_IDX_CHECK')"
           exit 1
         fi

         echo "✓ Phase 28 migration 0010 substrate verified (metadata column + adjustment_confirmation partial index)"
         ```

    (b) Run `bash scripts/test.sh` against a freshly-cleaned Docker postgres (the script does `docker compose down --timeout 5` in cleanup; the trap also runs on script exit, so a fresh run starts clean). The migration apply step should succeed; the new substrate gate must pass; the existing 4 prior gates (0006 6|1|3, 0007 voice-note, 0008 wellbeing, 0009 weekly_review) must still pass.

       Note: This is the [BLOCKING] schema-push step per the planner contract — the project uses Drizzle and applies migrations via raw psql in scripts/test.sh (NOT via drizzle-kit migrate at test time). The "schema push" equivalent is `bash scripts/test.sh` itself successfully running migration 0010 + asserting the metadata column exists. NO separate `npx drizzle-kit push` invocation is required because the project's canonical apply path is the per-migration psql script, validated by the seed-row gates. Per the planner contract `scripts/test.sh` is the canonical entry point; this task verifies the gate fires green on a clean DB.
  </action>
  <verify>
    <automated>bash scripts/test.sh --reporter=basic 2>&1 | grep -E "Phase 28 migration 0010|adjustment_confirmation partial index|✓|❌" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - bash scripts/test.sh runs to completion (or at least past the migration apply + substrate gate sections) without exiting non-zero on any pre-vitest gate
    - Output contains "✓ Phase 28 migration 0010 substrate verified (metadata column + adjustment_confirmation partial index)"
    - All prior substrate gates also pass (Phase 25 6|1|3, Phase 26 voice-note seed + index + prompt_text, Phase 27 wellbeing seed, Phase 29 weekly_review seed, anchor-bias guard)
    - grep -c "MIGRATION_10_SQL" scripts/test.sh reports >= 2 (1 declaration + 1 apply line)
    - grep -c "0010_adjustment_dialogue" scripts/test.sh reports >= 1
    - grep -c "metadata column missing after migration 0010" scripts/test.sh reports exactly 1 line (the new gate fail message)
  </acceptance_criteria>
  <done>
    Migration 0010 applies cleanly to fresh Docker postgres. scripts/test.sh substrate gate verifies metadata column + adjustment_confirmation partial index post-migration. The blocking schema-push step is satisfied — Plan 28-03 cannot pass verification without this gate green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/rituals/adjustment-dialogue.ts with fireAdjustmentDialogue + handleAdjustmentReply + handleConfirmationReply + confirmConfigPatch + ritualConfirmationSweep</name>
  <files>src/rituals/adjustment-dialogue.ts, src/rituals/__tests__/adjustment-dialogue.integration.test.ts, src/rituals/__tests__/confirmation-window.test.ts</files>
  <read_first>
    - src/rituals/voice-note.ts:179-230, 289-365 (atomic-consume + fire-side patterns to copy)
    - src/rituals/weekly-review.ts:131-186, 263-285 (v3+v4 dual schema + Haiku messages.parse pattern)
    - src/rituals/skip-tracking.ts (just-shipped Plan 28-02 — adjustment dialogue calls the same predicate via shouldFireAdjustmentDialogue and queries ritual_fire_events similarly)
    - src/llm/client.ts (verify HAIKU_MODEL export and anthropic client shape)
    - src/db/schema.ts (post-Task-1 metadata column on ritualPendingResponses)
    - src/db/schema.ts:457-477 (ritualFireEvents + ritualConfigEvents shapes — RESEARCH Landmine 1: ritualConfigEvents has columns id/ritualId/actor varchar(32)/patch jsonb/createdAt — NOT change_kind/old_value/etc per CONTEXT.md D-28-09)
    - src/chris/__tests__/engine-pp5.test.ts (cumulative not-called pattern — Pitfall 6 mitigation analog for the new test file)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/rituals/adjustment-dialogue.ts" section (lines 366-589) — extensive pattern map per sub-section A through F
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md "§Code Examples" (lines 318-423) and "§Common Pitfalls" (lines 262-318)
  </read_first>
  <behavior>
    Adjustment-dialogue.integration.test.ts:
    - Test 1 (fire-side): fireAdjustmentDialogue(ritual) sends Telegram message containing the ritual name + cadence-aware text "This [daily/weekly] [name] ritual isn't working — what should change?" and inserts ritual_pending_responses row with metadata.kind = 'adjustment_dialogue'.
    - Test 2 (Haiku change_requested branch): Mock Haiku to return { classification: 'change_requested', proposed_change: { field: 'fire_at', new_value: '19:30' }, confidence: 0.95 }. Inject text reply via handleAdjustmentReply. Assert: confirmation Telegram echo sent ("change voice note fire to 19:30 — OK?"); a NEW ritual_pending_responses row with metadata.kind = 'adjustment_confirmation' AND metadata.proposed_change exists; expires_at = firedAt + 60s.
    - Test 3 (Haiku no_change branch): Mock returns { classification: 'no_change', proposed_change: null, confidence: 0.9 }. handleAdjustmentReply runs. Assert: skip_count reset to 0 (acknowledgment); ritual_fire_events row with outcome='responded' (the dialogue itself counts as a response); no confirmation pending row created.
    - Test 4 (Haiku evasive branch): Mock returns { classification: 'evasive', proposed_change: null, confidence: 0.8 }. Assert: ritual_responses row written with metadata.kind='adjustment_dialogue_response' AND metadata.classification='evasive' (Plan 28-04's hasReachedEvasiveTrigger reads this).
    - Test 5 (low confidence default-evasive): Mock returns { classification: 'change_requested', proposed_change: null, confidence: 0.4 }. Assert: behaves as evasive (per CONTEXT.md "default to evasive on confidence < 0.7"); no confirmation pending row; ritual_responses row tagged classification='evasive'.
    - Test 6 (retry-cap-2): Mock first 2 Haiku calls throw; 3rd call would succeed but retry-cap-2 means after 2 failures we use templated EN fallback. Assert: classification falls back to 'no_change' with templated phrasing (mirroring Phase 29 retry-cap-2 + templated fallback pattern at weekly-review.ts:131-186); chris.adjustment.fallback_fired log emitted.
    - Cumulative afterAll: mockAnthropicCreate.not.toHaveBeenCalled() — only Haiku messages.parse is allowed (PP#5 short-circuits engine; ZERO Sonnet calls).

    Confirmation-window.test.ts:
    - Test 7 (yes branch): Seed adjustment_confirmation pending row + proposed_change. Inject "yes" via handleConfirmationReply. Assert: rituals.config.fire_at updated; ritual_config_events row with actor='user' + patch.kind='apply' + patch.field='fire_at' + patch.old_value + patch.new_value.
    - Test 8 (no branch): Inject "no". Assert: rituals.config UNCHANGED; ritual_config_events row with actor='user' + patch.kind='abort'.
    - Test 9 (timeout branch): Seed adjustment_confirmation row with expires_at = now() - 1s. Call ritualConfirmationSweep(now). Assert: rituals.config.fire_at updated; ritual_config_events row with actor='auto_apply_on_timeout' + patch.kind='apply'.
    - Test 10 (race vs PP#5): Seed adjustment_confirmation row, expires_at = now(). Concurrently call ritualConfirmationSweep AND handleConfirmationReply for the same row (race scenario). Assert: exactly ONE patch applied (atomic-consume picks one winner); the other returns silently.
  </behavior>
  <action>
    Create src/rituals/adjustment-dialogue.ts. File header JSDoc cites: "Phase 28 Plan 03 (SKIP-04 + SKIP-05) — adjustment dialogue handler with Haiku 3-class classification + 60s confirmation window. ON TOP OF Plan 28-01 substrate (ritual_fire_events writes) + Plan 28-02 predicate (shouldFireAdjustmentDialogue). Per RESEARCH Landmine 1: ritual_config_events writes use the discriminated envelope inside `patch` jsonb (NOT the change_kind/old_value/new_value top-level columns CONTEXT.md D-28-09 incorrectly described). Per RESEARCH Landmine 2: this module REQUIRES migration 0010 (metadata jsonb on ritual_pending_responses). Per RESEARCH Landmine 5: ritualConfirmationSweep is a NARROW helper, NOT runRitualSweep — registered separately in cron-registration.ts."

    Imports:
       - drizzle-orm: { and, eq, gte, isNull, lte, sql }
       - zod: { z }
       - zod/v4 as zV4
       - @anthropic-ai/sdk/helpers/zod: { zodOutputFormat }
       - ../config.js: { config }
       - ../db/connection.js: { db }
       - ../db/schema.js: { rituals, ritualPendingResponses, ritualResponses, ritualFireEvents, ritualConfigEvents }
       - ../utils/logger.js: { logger }
       - ../utils/errors.js: { StorageError }
       - ../bot/bot.js: { bot }
       - ../llm/client.js: { anthropic, HAIKU_MODEL }
       - ./types.js: { RITUAL_OUTCOME, parseRitualConfig, type RitualConfig }
       - ./skip-tracking.js: { shouldFireAdjustmentDialogue }

    Constants:
       const CONFIRMATION_WINDOW_SECONDS = 60;  // D-28-06 locked spec
       const HAIKU_MAX_RETRIES = 2;  // D-28-05 retry cap (mirrors Phase 29)
       const CONFIDENCE_DEFAULT_EVASIVE_THRESHOLD = 0.7;  // CONTEXT.md "default-evasive on low confidence"
       const ADJUSTMENT_JUDGE_PROMPT = "You are classifying a user's reply to an adjustment-dialogue message... Output JSON: { classification: 'change_requested'|'no_change'|'evasive', proposed_change: { field, new_value } | null, confidence: number 0-1 }." (full text per PATTERNS.md §D and CONTEXT.md D-28-05; planner picks exact phrasing within constraints — keep under 600 chars)

    Schemas (v3+v4 dual per PATTERNS.md §D lines 493-516):
       AdjustmentClassificationSchema (v3 — runtime re-validate)
       AdjustmentClassificationSchemaV4 (v4 — SDK boundary, no refine)
       Both have shape { classification: enum 3 values, proposed_change: object|null, confidence: number 0-1 }

    Export 1: async fireAdjustmentDialogue(ritual): Promise<RitualFireOutcome>
       Body:
       1. Compose message text per CONTEXT.md D-28-05 — "This [daily/weekly] [name] ritual isn't working — what should change? Reply with what to change, or 'no change' / 'drop it' if you'd prefer to keep skipping or stop entirely." Use ritual.type to pick 'daily' or 'weekly' wording; ritual.name for the slot.
       2. await bot.api.sendMessage(config.telegramAuthorizedUserId, messageText) — send BEFORE inserting pending row (mirror voice-note.ts:340 sequencing — if Telegram fails, no stale binding).
       3. INSERT INTO ritual_pending_responses with metadata = { kind: 'adjustment_dialogue', cadence: ritual.type, ritualName: ritual.name }, expires_at = now + 18h (mirror RESPONSE_WINDOW_HOURS = 18 from voice-note.ts:68; the dialogue has the same window as a voice-note response — Greg might not reply for hours).
       4. INSERT ritual_fire_events row with outcome = RITUAL_OUTCOME.IN_DIALOGUE, metadata = { adjustmentDialogueId: pendingResponseId }.
       5. logger.info('chris.adjustment.fired', { ritualId: ritual.id, skipCount: ritual.skipCount }).
       6. Return 'in_dialogue' (the union variant from Plan 25; semantically: "ritual fired ITS adjustment dialogue, not its standard prompt").

    Export 2: async handleAdjustmentReply(pending, chatId, text): Promise<string>
       Plan 28-03 ships the Haiku classification path; refusal pre-check is added by Plan 28-04 BEFORE the Haiku call.

       Body:
       1. Atomic-consume the pending row via UPDATE ritualPendingResponses SET consumedAt = now WHERE id = pending.id AND consumedAt IS NULL RETURNING (mirror voice-note.ts:184-204). On race-loss, return '' silently.
       2. Classify via classifyAdjustmentReply(text) — internal helper running Haiku messages.parse with retry-cap-2 + templated fallback. On retry-cap exhaustion, returns { classification: 'no_change', proposed_change: null, confidence: 1.0, isFallback: true } (templated fallback semantics per PATTERNS.md §D).
       3. Apply confidence-default-evasive: if !isFallback && confidence < 0.7, override classification to 'evasive'.
       4. Branch on classification:
          - 'change_requested': call queueConfigPatchConfirmation(pending.ritualId, parsed.proposed_change). Send Telegram echo "Change [field] to [value] — OK? (auto-applies in 60s if no reply)". INSERT ritual_pending_responses row metadata.kind='adjustment_confirmation' + metadata.proposed_change + expires_at = now + 60s. Log chris.adjustment.classified change_requested.
          - 'no_change': db.update(rituals).set({ skipCount: 0 }).where(eq(rituals.id, pending.ritualId)). INSERT ritual_fire_events outcome=RESPONDED metadata={dialogueId, classification: 'no_change'}. INSERT ritual_responses row metadata.kind='adjustment_dialogue_response' + metadata.classification='no_change'. Log chris.adjustment.classified no_change.
          - 'evasive': INSERT ritual_responses row metadata.kind='adjustment_dialogue_response' + metadata.classification='evasive' + metadata.greg_text=text (Plan 28-04's hasReachedEvasiveTrigger queries this). Do NOT reset skip_count (the user did not engage). Log chris.adjustment.classified evasive.
       5. Return '' (IN-02 silent-skip — engine PP#5 short-circuits regardless of internal branch).

    Export 3: async handleConfirmationReply(pending, chatId, text): Promise<string>
       Body:
       1. Atomic-consume the confirmation pending row.
       2. Parse text for "yes" / "no" — accept "yes", "y", "ok", "confirm", "apply" (case-insensitive) as yes; "no", "n", "abort", "cancel" as no; anything else default to no (conservative).
       3. Read pending.metadata.proposed_change (typed via the metadata jsonb shape).
       4. Branch:
          - yes: call confirmConfigPatch(pending.ritualId, proposed_change, actor: 'user'). Send Telegram echo "Applied: [field] = [value]". Log chris.adjustment.applied.
          - no: INSERT ritualConfigEvents { ritualId, actor: 'user', patch: { kind: 'abort', field: proposed_change.field, source: 'user_explicit_no' } }. Send Telegram echo "OK, keeping current config". Log chris.adjustment.aborted.
       5. Return ''.

    Export 4: async confirmConfigPatch(ritualId, proposed_change, actor): Promise<void>
       Body (per RESEARCH Landmine 1 — discriminated envelope inside patch jsonb):
       1. Read current rituals.config jsonb to capture old_value for the field being patched.
       2. Apply via UPDATE rituals SET config = jsonb_set(config, '{[field]}', '[new_value]'::jsonb, true) WHERE id = ritualId. Use the postgres-js String() workaround per wellbeing.ts:148-150 JSDoc.
       3. INSERT ritualConfigEvents.values({ ritualId, actor: actor (varchar 32 — 'user' | 'auto_apply_on_timeout' | 'system'), patch: { kind: 'apply', field: proposed_change.field, old_value, new_value: proposed_change.new_value, source: actor === 'auto_apply_on_timeout' ? 'sweep' : 'reply' } }).
       4. Log chris.adjustment.config_patched with { ritualId, field, oldValue, newValue, actor }.

    Export 5: async ritualConfirmationSweep(now: Date = new Date()): Promise<number>
       Per RESEARCH Landmine 5: NARROW helper. Body:
       1. SELECT id, ritualId, metadata FROM ritualPendingResponses WHERE consumedAt IS NULL AND expiresAt <= now AND metadata->>'kind' = 'adjustment_confirmation' LIMIT 10. If empty, return 0 (sub-millisecond hot path).
       2. For each expired row: atomic-consume (mirror voice-note.ts:184-204). On race-loss, continue.
       3. Read row.metadata.proposed_change (jsonb cast; typed via inline type assertion).
       4. Call confirmConfigPatch(row.ritualId, proposed_change, actor: 'auto_apply_on_timeout').
       5. INSERT ritualFireEvents row with outcome=RESPONDED metadata={confirmationId: row.id, source: 'auto_apply_on_timeout'} (the user implicitly responded by not replying — D-28-06 contract).
       6. Reset rituals.skip_count = 0 (per D-28-03 — adjustment-completion is a reset event).
       7. Increment processed counter; loop continues.
       8. Log chris.adjustment.confirmation_sweep.done with { processedCount, scanned: expired.length } and return processedCount.

    Internal helper: async classifyAdjustmentReply(text): Promise<{...} & { isFallback: boolean }>
       Retry-cap-2 loop wrapping anthropic.messages.parse (mirror weekly-review.ts:419-480 retry pattern). On Zod parse failure or null parsed_output, retry. On 2 failures, return templated fallback { classification: 'no_change', proposed_change: null, confidence: 1.0, isFallback: true } and log chris.adjustment.fallback_fired.

    Create src/rituals/__tests__/adjustment-dialogue.integration.test.ts (real-DB harness mirroring engine-pp5.test.ts shape):
       - vi.hoisted mocks for mockAnthropicCreate + mockAnthropicParse + mockSendMessage
       - vi.mock '../../llm/client.js' + vi.mock '../../bot/bot.js' (per engine-pp5.test.ts:23-95 pattern)
       - Top-level describe: 'Phase 28 Plan 03 SKIP-04 — adjustment dialogue + Haiku classification'
       - afterAll cumulative: mockAnthropicCreate.not.toHaveBeenCalled() (Pitfall 6 carry-over) — Sonnet/messages.create must NEVER be called from this path; only Haiku messages.parse is allowed
       - Tests 1-6 from the behavior list

    Create src/rituals/__tests__/confirmation-window.test.ts (real-DB):
       - Same mock shape as above
       - Top-level describe: 'Phase 28 Plan 03 SKIP-05 — 60s confirmation window'
       - Tests 7-10 from the behavior list
       - For Test 9 (timeout): use vi.setSystemTime(firedAt + 61_000) to advance the clock past expiry
       - For Test 10 (race): not a true concurrency test — sequence: call ritualConfirmationSweep first (should consume), then call handleConfirmationReply (should silently no-op via atomic-consume race-lost branch); reverse order is also tested
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "adjustment-dialogue|confirmation-window" | head -10 && bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue.integration.test.ts 2>&1 | tail -20 && bash scripts/test.sh src/rituals/__tests__/confirmation-window.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to adjustment-dialogue.ts or the two new test files
    - bash scripts/test.sh src/rituals/__tests__/adjustment-dialogue.integration.test.ts reports all 6 tests passing; afterAll cumulative not-called assertion passes
    - bash scripts/test.sh src/rituals/__tests__/confirmation-window.test.ts reports all 4 tests passing
    - File src/rituals/adjustment-dialogue.ts exports exactly 5 named functions: fireAdjustmentDialogue, handleAdjustmentReply, handleConfirmationReply, confirmConfigPatch, ritualConfirmationSweep — verifiable by `grep -E "^export async function" src/rituals/adjustment-dialogue.ts | wc -l` returning 5
    - grep -c "RITUAL_OUTCOME\." src/rituals/adjustment-dialogue.ts >= 3 (IN_DIALOGUE, RESPONDED, etc — no string literal outcomes)
    - grep -c "ritualConfigEvents" src/rituals/adjustment-dialogue.ts >= 2 (at least 2 inserts: applyConfirmedPatch + abort path)
    - grep -E "actor: '(user|auto_apply_on_timeout|system)'" src/rituals/adjustment-dialogue.ts | wc -l >= 2 (per RESEARCH Landmine 1 envelope-in-patch shape)
    - grep -F "patch.kind" src/rituals/adjustment-dialogue.ts || grep -E "kind: '(apply|abort)'" src/rituals/adjustment-dialogue.ts >= 1 (the discriminated envelope per Landmine 1)
  </acceptance_criteria>
  <done>
    adjustment-dialogue.ts exports 5 functions implementing the full SKIP-04 + SKIP-05 contract. Haiku 3-class with v3+v4 dual schema + retry-cap-2 + templated fallback + confidence-default-evasive. ritualConfigEvents writes use the envelope-in-patch shape per RESEARCH Landmine 1. All ritual_fire_events emits use RITUAL_OUTCOME const map. Cumulative not-called assertion passes (Pitfall 6 invariant preserved through these new branches).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend PP#5 in src/chris/engine.ts with metadata.kind dispatch (preserves voice-note default per Pitfall 6)</name>
  <files>src/chris/engine.ts, src/chris/__tests__/engine-pp5-adjustment.test.ts</files>
  <read_first>
    - src/chris/engine.ts:160-220 (PP#5 region — the entire if (pending) {...} block)
    - src/rituals/adjustment-dialogue.ts (just-shipped Task 3 — handleAdjustmentReply + handleConfirmationReply signatures)
    - src/chris/__tests__/engine-pp5.test.ts in full (the cumulative not-called pattern + describe shape — your new test file should mirror)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 6 (lines 544-547) — NULL/undefined kind MUST fall through to voice-note path
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/chris/engine.ts" section (lines 592-650)
  </read_first>
  <behavior>
    - Test 1 (default branch — voice-note path preserved): Insert pending row with metadata = {} (kind absent) — this is the default after migration 0010 because voice-note.ts:345 doesn't set metadata. processMessage(text) calls recordRitualVoiceResponse — Pensieve entry written with epistemic_tag = 'RITUAL_RESPONSE'. Cumulative mockAnthropicCreate.not.toHaveBeenCalled() passes (Pitfall 6 invariant).
    - Test 2 (default branch — metadata IS NULL): Insert pending row with metadata = NULL (defensive — covers any future reset or column-default-NULL scenario). Same outcome as Test 1.
    - Test 3 (adjustment_dialogue branch): Insert pending row with metadata = { kind: 'adjustment_dialogue', cadence: 'daily', ritualName: 'daily_voice_note' }. Mock handleAdjustmentReply (since Plan 28-03 already ships that). processMessage(text) calls handleAdjustmentReply (verified via spy) and returns '' (silent skip). Sonnet/Anthropic create still NOT called.
    - Test 4 (adjustment_confirmation branch): Insert pending row with metadata = { kind: 'adjustment_confirmation', proposed_change: {...} }. processMessage("yes") calls handleConfirmationReply. Returns ''. Anthropic NOT called.
    - Cumulative afterAll: mockAnthropicCreate.not.toHaveBeenCalled() across all 4 HIT-path tests — proves Pitfall 6 invariant preserved across the new branches.
  </behavior>
  <action>
    Edit src/chris/engine.ts:

    (a) Add imports near line 76 (existing rituals import block):
        import { handleAdjustmentReply, handleConfirmationReply } from '../rituals/adjustment-dialogue.js';

    (b) REPLACE the existing `if (pending) { try { const result = await recordRitualVoiceResponse(...); ... } }` block at lines 176-208 with the metadata.kind switch shape per PATTERNS.md lines 615-642. Preserve the existing race-loss / other-error catch branches verbatim — only add the kind dispatch above the existing recordRitualVoiceResponse call.

       New shape:
       ```typescript
       if (pending) {
         // Phase 28 Plan 03 SKIP-04 + SKIP-05 — metadata.kind dispatch (RESEARCH Landmine 6).
         // Voice-note pending rows had no metadata pre-Phase-28 (column did not exist).
         // After migration 0010, rows default to '{}'::jsonb so metadata->>'kind'
         // returns undefined, falling through to the voice-note path. NULL metadata
         // (defensive) also falls through.
         const kind = (pending.metadata as { kind?: string } | null)?.kind;
         try {
           if (kind === 'adjustment_dialogue') {
             await handleAdjustmentReply(pending, chatId, text);
             logger.info({ pendingId: pending.id, kind }, 'chris.engine.pp5.adjustment_dialogue');
             return ''; // IN-02 silent-skip — Pitfall 6 invariant preserved
           }
           if (kind === 'adjustment_confirmation') {
             await handleConfirmationReply(pending, chatId, text);
             logger.info({ pendingId: pending.id, kind }, 'chris.engine.pp5.adjustment_confirmation');
             return '';
           }
           // Default branch (kind === undefined / null / 'voice_note') — existing path.
           const result = await recordRitualVoiceResponse(pending, chatId, text);
           logger.info(
             { pendingId: pending.id, ritualId: pending.ritualId, pensieveEntryId: result.pensieveEntryId },
             'chris.engine.pp5.hit',
           );
           return ''; // IN-02 silent-skip
         } catch (depositErr) {
           // Existing race-loss / other-error handling — preserve verbatim from lines 188-207.
           if (depositErr instanceof Error && depositErr.message === 'ritual.pp5.race_lost') {
             logger.info({ pendingId: pending.id }, 'chris.engine.pp5.race_lost');
             return '';
           }
           if (depositErr instanceof Error && depositErr.message === 'ritual.adjustment.race_lost') {
             logger.info({ pendingId: pending.id }, 'chris.engine.pp5.adjustment_race_lost');
             return '';
           }
           logger.warn(
             { err: depositErr instanceof Error ? depositErr.message : String(depositErr), pendingId: pending.id },
             'chris.engine.pp5.deposit_error',
           );
         }
       }
       ```

    (c) Create src/chris/__tests__/engine-pp5-adjustment.test.ts. Mirror src/chris/__tests__/engine-pp5.test.ts:
       - vi.hoisted mocks for mockAnthropicCreate, mockAnthropicParse, mockHandleAdjustmentReply, mockHandleConfirmationReply
       - vi.mock '../../llm/client.js' (same shape as engine-pp5.test.ts)
       - vi.mock '../../rituals/adjustment-dialogue.js' to inject the spy mocks for handleAdjustmentReply + handleConfirmationReply
       - vi.mock the fire-and-forget pipeline modules (embeddings, tagger, relational) per engine-pp5.test.ts:42-58
       - Top-level describe: 'Phase 28 Plan 03 — PP#5 metadata.kind dispatch (Pitfall 6 carry-over)'
       - afterAll: cumulative mockAnthropicCreate.not.toHaveBeenCalled(); cleanup db rows; sql.end() at last describe per TESTING.md convention
       - Tests 1-4 from the behavior list
       - For Test 1 + 2 (default branch): import the REAL recordRitualVoiceResponse (don't mock the rituals/voice-note module) so we exercise the actual deposit path. Mock storePensieveEntry to return { id: 'mock-pensieve-id' }.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "engine\.ts|engine-pp5-adjustment" | head -10 && bash scripts/test.sh src/chris/__tests__/engine-pp5-adjustment.test.ts 2>&1 | tail -20 && bash scripts/test.sh src/chris/__tests__/engine-pp5.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to engine.ts or engine-pp5-adjustment.test.ts
    - bash scripts/test.sh src/chris/__tests__/engine-pp5-adjustment.test.ts reports all 4 tests passing; cumulative not-called assertion passes
    - bash scripts/test.sh src/chris/__tests__/engine-pp5.test.ts (existing Phase 26 test) STILL passes — Plan 28-03 changes do NOT break the voice-note PP#5 path
    - grep -E "metadata\?\.kind|metadata->>'kind'|kind === 'adjustment_dialogue'" src/chris/engine.ts | wc -l >= 1 (the new dispatch logic)
    - grep -c "handleAdjustmentReply\|handleConfirmationReply" src/chris/engine.ts >= 2 (1 import + 2 invocations)
    - The existing 'chris.engine.pp5.hit' log line still exists in engine.ts (default voice-note branch preserved): grep -F "chris.engine.pp5.hit" src/chris/engine.ts >= 1
  </acceptance_criteria>
  <done>
    PP#5 dispatches by metadata.kind. Default branch (voice-note) preserved per RESEARCH Landmine 6 — null/missing kind falls through. Cumulative not-called assertion proves Sonnet is never reached through any of the 4 PP#5 HIT-path branches. The Phase 26 engine-pp5.test.ts still passes (no regression).
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire scheduler.ts to call fireAdjustmentDialogue (replaces Plan 28-02 in_dialogue stub) + register 1-minute confirmation sweep cron</name>
  <files>src/rituals/scheduler.ts, src/cron-registration.ts, src/index.ts</files>
  <read_first>
    - src/rituals/scheduler.ts post Plan 28-02 (the Plan 28-02 if (await shouldFireAdjustmentDialogue(ritual)) {...} branch — Plan 28-03 replaces the stub return with a real handler dispatch)
    - src/rituals/adjustment-dialogue.ts (just-shipped — fireAdjustmentDialogue + ritualConfirmationSweep signatures)
    - src/cron-registration.ts (full file — pattern to extend with new cron + new deps interface field + new status field)
    - src/index.ts:75-115 (registerCrons call site)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-PATTERNS.md "src/cron-registration.ts" section (lines 654-727)
    - .planning/phases/28-skip-tracking-adjustment-dialogue/28-RESEARCH.md Landmine 5 (lines 535-542) — NARROW helper, NOT runRitualSweep extension
  </read_first>
  <action>
    (a) Edit src/rituals/scheduler.ts:

       - Add import: `import { fireAdjustmentDialogue } from './adjustment-dialogue.js';`
       - REPLACE the Plan 28-02 stub `if (await shouldFireAdjustmentDialogue(ritual)) { ... results.push({...outcome: 'in_dialogue'}); return results; }` branch. The new body:

         ```typescript
         if (await shouldFireAdjustmentDialogue(ritual)) {
           logger.info(
             { ritualId: ritual.id, skipCount: ritual.skipCount, type: ritual.type },
             'rituals.adjustment_dialogue.predicate_hit',
           );
           try {
             const outcome = await fireAdjustmentDialogue(ritual);
             await incrementRitualDailyCount(config.proactiveTimezone);
             results.push({
               ritualId: ritual.id,
               type: ritual.type,
               fired: outcome === 'in_dialogue',
               outcome,
             });
           } catch (err) {
             logger.error({ err, ritualId: ritual.id }, 'rituals.adjustment_dialogue.error');
             await incrementRitualDailyCount(config.proactiveTimezone);
             results.push({
               ritualId: ritual.id,
               type: ritual.type,
               fired: false,
               outcome: 'in_dialogue',
               error: err,
             });
           }
           return results;
         }
         ```

       Net change: the stub `outcome: 'in_dialogue'` push is replaced with a real handler call that returns 'in_dialogue' on success (per Plan 28-03 Task 3 fireAdjustmentDialogue contract).

    (b) Edit src/cron-registration.ts (per PATTERNS.md lines 660-727):

       - Add to RegisterCronsDeps interface (after runRitualSweep field):
         `/** Phase 28 D-28-06 — 1-minute confirmation sweep handler. */
          ritualConfirmationSweep: () => Promise<void>;`

       - Add to CronRegistrationStatus interface (after ritual: 'registered' | 'failed'):
         `ritualConfirmation: 'registered' | 'failed';`

       - Initialize the new status field in the status const inside registerCrons body:
         `ritualConfirmation: 'failed',`

       - AFTER the existing 21:00 ritual sweep registration block (line 100-115), ADD:
         ```typescript
         // Phase 28 D-28-06 — 1-minute confirmation sweep (every minute).
         // NARROW helper (NOT runRitualSweep) per RESEARCH Landmine 5: ONLY scans for
         // expired adjustment_confirmation pending rows. Sub-millisecond when no work.
         // CRON-01 try/catch belt-and-suspenders.
         cron.schedule(
           '* * * * *',
           async () => {
             try {
               await deps.ritualConfirmationSweep();
             } catch (err) {
               logger.error({ err }, 'rituals.confirmation_sweep.error');
             }
           },
           { timezone: deps.config.proactiveTimezone },
         );
         status.ritualConfirmation = 'registered';
         logger.info(
           { cron: '* * * * *', timezone: deps.config.proactiveTimezone },
           'rituals.confirmation_sweep.scheduled',
         );
         ```

    (c) Edit src/index.ts:88-93 (the registerCrons call site):

       - Add import at the top of the file (alongside existing rituals/* imports): `import { ritualConfirmationSweep } from './rituals/adjustment-dialogue.js';`
       - Extend the registerCrons({...}) deps object to include `ritualConfirmationSweep` in the passed object:
         ```typescript
         cronStatus = registerCrons({
           config,
           runSweep,
           runRitualSweep,
           runConsolidateYesterday,
           ritualConfirmationSweep, // Phase 28 D-28-06
         });
         ```

    (d) Update existing src/cron-registration.test.ts (if it exists) and src/index.ts route /health (if it exposes ritualConfirmation status) to reflect the new status key. If /health route does not currently expose all status fields explicitly, no /health change is needed. Read src/server/health.ts (or equivalent) first to determine whether `cronStatus.ritualConfirmation` should be added to its return shape — if Phase 25 RIT-12 only checked `cronStatus.ritual === 'registered'`, this new key is internal-only and /health is unchanged. Verify and document either way.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scheduler\.ts|cron-registration|index\.ts" | head -15 && grep -c "ritualConfirmationSweep" src/cron-registration.ts && grep -c "fireAdjustmentDialogue" src/rituals/scheduler.ts && grep -E "outcome: 'in_dialogue'" src/rituals/scheduler.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit reports zero errors attributable to scheduler.ts, cron-registration.ts, index.ts
    - grep -c "ritualConfirmationSweep" src/cron-registration.ts reports >= 3 (interface field + status init + invocation)
    - grep -c "fireAdjustmentDialogue" src/rituals/scheduler.ts reports >= 2 (1 import + 1 invocation)
    - grep -E "'\* \* \* \* \*'" src/cron-registration.ts | wc -l reports >= 1 (the every-minute cron string)
    - grep -F "ritualConfirmation: 'registered'" src/cron-registration.ts >= 1
    - The Plan 28-02 stub (push outcome: 'in_dialogue' WITHOUT calling fireAdjustmentDialogue) is GONE from scheduler.ts — verifiable by the new code containing both `await fireAdjustmentDialogue` and `incrementRitualDailyCount` inside the predicate-hit branch
    - bash scripts/test.sh src/rituals/__tests__/should-fire-adjustment.test.ts (Plan 28-02's test) — must be UPDATED in this task if it asserted on the stub behavior, OR if it just asserts on outcome string equality, no change needed. Read the test file first to determine.
  </acceptance_criteria>
  <done>
    scheduler.ts now dispatches fireAdjustmentDialogue on threshold hit (replaces Plan 28-02 stub). cron-registration.ts wires the 1-minute confirmation sweep — narrow helper per RESEARCH Landmine 5. index.ts call-site updated. Status field 'ritualConfirmation' exposed for future /health surface. /health and Phase 25-26-27-29 cron tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Greg → Telegram → bot.on('message:text') → engine PP#5 → handleAdjustmentReply | adversarial Greg-text could attempt to inject prompt-poisoning into the Haiku classifier; classifier is structured-output (3 enum values + bounded-confidence number) so prose injection cannot mutate the parse shape |
| Haiku 3-class classifier output → confirmConfigPatch field whitelist | proposed_change.field is z.enum(['fire_at', 'fire_dow', 'skip_threshold', 'mute_until']) — Haiku CANNOT propose patches to other fields (e.g., `enabled`, `name`, raw `config` blob mutation paths) |
| ritual_pending_responses metadata jsonb | written server-side only; metadata.kind is read-only by PP#5; no Telegram-surface mutation of metadata |
| ritual_config_events.actor varchar(32) | bounded by varchar(32) length; valid actors written by Plan 28-03 are 'user' (≤4 chars) and 'auto_apply_on_timeout' (21 chars) — both fit |
| 1-minute confirmation sweep cron handler | wrapped in try/catch (CRON-01 belt-and-suspenders); a thrown handler does NOT crash node-cron timer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-28-01** | Spoofing | ritual_fire_events insertions (skip-count poisoning) | mitigate | ritual_fire_events writes are server-side only; no user-facing API for direct insertion. Greg's only inputs are Telegram messages routed through PP#5; PP#5 cannot directly insert ritual_fire_events. |
| **T-28-02** | Tampering | Haiku-classified change_requested with malicious patch (config patch injection) | mitigate | Strict Zod schema on `proposed_change.field` — z.enum(['fire_at', 'fire_dow', 'skip_threshold', 'mute_until']). Only these 4 fields are mutable via the dialogue path. AdjustmentClassificationSchemaV4 is the SDK-boundary gate; AdjustmentClassificationSchema (v3) re-validates after parse — any drift fails the v3 parse and triggers retry. |
| **T-28-03** | Repudiation | Confirmation race — Greg replies "no" at second 59, sweep applies at second 60 | mitigate | Atomic-consume UPDATE...RETURNING with consumed_at IS NULL guard on the confirmation pending row. PP#5 path and ritualConfirmationSweep path BOTH use the same atomic-consume — whichever wins consumes the row; loser silently no-ops via the race-lost branch in handleConfirmationReply / ritualConfirmationSweep. |
| **T-28-S5** | Spoofing | metadata.kind injection by future code path that writes to ritual_pending_responses without setting kind | accept | Default '{}'::jsonb for the column means kind is undefined for any non-Phase-28 writer; PP#5 explicitly falls through to voice-note path on undefined kind (RESEARCH Landmine 6). New kinds added by future phases must be explicitly added to the engine.ts switch — no implicit unknown-kind dispatch. |
| **T-28-D3** | DoS | 1-minute cron tick with backlog of expired confirmations | mitigate | Per-call LIMIT 10 in ritualConfirmationSweep (Task 3); backlog drains over subsequent ticks; partial index `ritual_pending_responses_adjustment_confirmation_idx` keeps the SELECT sub-millisecond when zero rows pending. |
| **T-28-I3** | Information disclosure | Haiku judge prompt + reply text logged to chris.adjustment.classified | accept | Greg's reply text is already in Telegram + Pensieve; logging the classification adds operational telemetry but no new PII surface. The Haiku judge prompt is a constant string (no Greg-data interpolation) so prompt-injection-into-logs is not a concern. |
| **T-28-E3** | Elevation of privilege | adjustment dialogue could escalate to disabling rituals (set enabled=false via config patch) | mitigate | proposed_change.field whitelist EXCLUDES `enabled`. Disabling a ritual requires the M006 refusal path (Plan 28-04 — explicit "drop it" intent). The Haiku-classified change_requested path can ONLY mutate fire_at / fire_dow / skip_threshold / mute_until. |

(Note: T-28-04 — Refusal bypass — is mitigated by Plan 28-04, not Plan 28-03. The pre-check in handleAdjustmentReply is added there.)

</threat_model>

<verification>
Phase 28 Plan 03 acceptance:

1. **TS clean**: npx tsc --noEmit zero errors attributable to changed files.
2. **All test files green**: adjustment-dialogue.integration.test.ts (6 tests), confirmation-window.test.ts (4 tests), engine-pp5-adjustment.test.ts (4 tests). Cumulative not-called assertion passes in each (Pitfall 6 invariant preserved across all new branches).
3. **Phase 26 engine-pp5.test.ts still passes** — voice-note PP#5 path is the default branch, untouched on the kind===undefined path.
4. **Plan 28-02's should-fire-adjustment.test.ts still passes** (or is updated in Task 5 if it asserted on stub-only behavior).
5. **Migration 0010 applies cleanly**: bash scripts/test.sh substrate gate at the new "Phase 28 migration 0010 substrate verified" line passes; existing 4 prior gates (0006/0007/0008/0009) all still pass.
6. **Cron registration**: status.ritualConfirmation == 'registered' after registerCrons; cron-registration.test.ts (existing Phase 25 test) still passes.
7. **No setTimeout regressions**: grep -F "setTimeout" src/rituals/adjustment-dialogue.ts returns ZERO matches (D-28-06 — DB-row + cron-sweep mechanism, NOT setTimeout).
</verification>

<success_criteria>
ROADMAP success criterion 2 full coverage:
- "Haiku parses Greg's free-text reply into exactly one of change_requested / no_change / evasive" — proven by Tests 2-5 in adjustment-dialogue.integration.test.ts
- "on change_requested, Chris echoes the proposed patch ('change voice note fire to 19:30 — OK?')" — proven by Test 2
- "on 'yes' or no response within 60 seconds, the patch lands in rituals.config and is recorded in ritual_config_events" — Tests 7 + 9 in confirmation-window.test.ts
- "on explicit 'no', the patch is aborted and logged" — Test 8

Plan 28-03 also delivers the substrate for ROADMAP success criterion 1's adjustment-dialogue dispatch (the threshold gate in Plan 28-02 now actually fires the dialogue).
</success_criteria>

<output>
After completion, create .planning/phases/28-skip-tracking-adjustment-dialogue/28-03-SUMMARY.md. SUMMARY frontmatter MUST include `requirements-completed: [SKIP-04, SKIP-05]` per PROC-02 upstream verdict.
</output>
