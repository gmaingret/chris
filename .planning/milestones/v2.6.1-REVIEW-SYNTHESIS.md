---
created: 2026-05-14
purpose: Synthesize 14 phase code-reviews into v2.6.1 scope decision
total_findings: 142
blockers: 45
warnings: 97
phases_reviewed: [24, 25, 26, 27, 28, 29, 30, 31, 34, 36, 37, 38, 39, 40]
---

# v2.6.1 Code Review Synthesis

14 parallel `gsd-code-reviewer` agents reviewed all phases lacking REVIEW.md (v2.3 → v2.6). Output: 142 findings.

## Per-Phase Totals

| Phase | Title | BL | WR |
|---|---|---:|---:|
| 24 | primed-fixture-pipeline | 6 | 11 |
| 25 | ritual-scheduling-foundation | 2 | 7 |
| 26 | daily-voice-note-ritual | 4 | 6 |
| 27 | daily-wellbeing-snapshot | 3 | 6 |
| 28 | skip-tracking-adjustment-dialogue | **12** | **12** |
| 29 | weekly-review | 3 | 6 |
| 30 | test-infrastructure-harn-03 | 3 | 7 |
| 31 | rename-voice-note-to-journal | 0 | 4 |
| 34 | inference-engine (M010) | 3 | 7 |
| 36 | tests (M010 milestone gate) | 4 | 6 |
| 37 | psychological-substrate | 0 | 5 |
| 38 | psychological-inference-engine | 0 | 6 |
| 39 | psychological-surfaces | 0 | 7 |
| 40 | psychological-milestone-tests | 5 | 7 |
| **TOTAL** | | **45** | **97** |

## Cross-Phase Themes (P0 — production correctness)

### T1. Adjustment-Dialogue Cluster (live UX defect + cascading bugs)

Phase 28 yielded 12 BLOCKERs around the surface that fired the live bug at 17:00 Paris today.

- **Live bug** (BL-01/02/03): "isn't working" wrongful assertion + "daily daily_journal" slug doubling + 8 EN-only send sites in FR/RU user context. `src/rituals/adjustment-dialogue.ts:285` + lines 308, 471, 733, 151, 182, 286, 531, 547, 733.
- **skip_count never resets** on yes/no/refusal completion (BL-04/05/06/10): adjustment dialogue re-fires every sweep tick after threshold; defeats SKIP-06 self-protection; makes 30-day pause meaningless. Files: `adjustment-dialogue.ts:529-535`, `:536-552`, `:125-185`, `skip-tracking.ts:286-313`.
- **Haiku-whitelist privilege escalation** (BL-07): `mute_until` is in Haiku-controllable field enum → a user reply parsed by Haiku into `{field: 'mute_until', new_value: <future date>}` silently disables the entire ritual channel. CONTEXT mitigation protected `enabled`, missed `mute_until`.
- **Type-validation gap on Haiku config patches** (BL-08): `confirmConfigPatch` writes Haiku's `new_value` into config jsonb without per-field validation. `{field: 'fire_at', new_value: 42}` permanently bricks the ritual.

### T2. Atomicity / Race Conditions (production correctness)

- **P25 BL-01** — `tryFireRitualAtomic` clock-resolution race re-armed by every-minute cron at `src/rituals/idempotency.ts:111-119`. M009 second-fire bug class returning via JS-clock ms collisions. Fix: SET via `sql\`now()\``.
- **P25 BL-02** — non-transactional paired-insert in `ritualResponseWindowSweep` at `scheduler.ts:374-425`. Mid-row throw swallowed; row already consumed; silent audit-log + skip-counter loss.
- **P27 BL-01** — rapid-tap race triggers N completion side-effects (duplicate `wellbeing_completed` events, repeated `editMessageText`) at `src/rituals/wellbeing.ts:227-258`.
- **P27 BL-02** — `handleSkip` clobbers concurrent partial taps via full-jsonb overwrite at `wellbeing.ts:328-346`. Silent data loss (data-fidelity-mandate violation).
- **P27 BL-03** — `findOpenWellbeingRow` can match stale prior-day NULL rows under DST edge at `wellbeing.ts:422-450`.
- **P29 BL-01** — write-order race in weekly-review fire: Telegram send AFTER `respondedAt` + Pensieve persist, BEFORE `ritualFireEvents`. If send throws → response row lies, Pensieve has orphan, no fire_event, `next_run_at` advanced. `weekly-review.ts:627-691`.

### T3. CI Milestone Gates Are Silent (zero regression detection)

- **P36 BL-01** — All 5 M010 milestone-gate test files use `existsSync(MANIFEST) ? describe : describe.skip` against gitignored fixtures. **CI sees no fixtures → reports skip, not fail → M010 milestone gate provides zero regression detection.** Files: `integration-m010-30days.test.ts:125-137`, `integration-m010-5days.test.ts:105-120`, `primed-sanity-m010.test.ts:171,239`, `live-anti-hallucination.test.ts:136`.
- **P40 BL-01** — Same pattern in M011 PMT-03 + output-dir path bug at `synthesize-delta.ts:937` (`m011-1000words-5days` vs `m011-1000words`). Silent skip forever.
- **P30** — D045 silent-skip in BOTH primed-sanity AND synthetic-fixture suites; no CI hard-fail. M009 milestone gate can vanish.

### T4. Prompt Injection Surface

- **P34 BL-01** — Operational inference: Pensieve `content`, episodic `summary`, decision `decisionText`/`resolution` interpolated into Sonnet system prompt with no escaping. A Pensieve entry containing `\n\n## CURRENT PROFILE STATE\n{...forged...}` can hijack structured-output contract; persists via `onConflictDoUpdate`. `src/memory/profile-prompt.ts:312-345`.
- **P38 WR-01** — Same class on psychological inference: `psychological-profile-prompt.ts:393-403`. Lower practical risk (trust boundary = Greg's own messages), but defense-in-depth gap.

### T5. Contract Violations (operational + psychological inference)

- **P34 BL-02** — `stripMetadataColumns` doesn't strip `dataConsistency` → every non-first fire shows Sonnet its prior data_consistency value, anchoring on stale data. Contradicts "host computes, you don't emit" contract. `src/memory/profiles/shared.ts:321-337`.
- **P34 BL-03** — `extract<X>PrevState` misclaims seed rows produce "insufficient data" markers; Phase 33 seeds use `'{}'::jsonb` → first-fire-after-deploy shows Sonnet empty fields + anti-drift directive (worst case for M010-03 drift). 4 files: jurisdictional.ts:59-66 + 3 siblings.
- **P38 WR-02** — Sonnet's `data_consistency` field is logged then discarded; no column exists. CONS-01 (v2.6.1 deferred item) has no historical signal to consume. Blocks future work.

### T6. FR/RU Localization (already in scope, expanded with precise lines)

- **P28 BL-03** — 8 EN-only sites in adjustment-dialogue.ts + EN-only Haiku judge prompt
- **P29 BL-03** — `WEEKLY_REVIEW_HEADER` hardcoded EN, prepended on every fire (including 2026-05-10 first fire — Greg got EN header above FR body). `weekly-review.ts:68, 621`.
- **P29 WR-03** — Broken FR regex `qu['e]?est-ce que` matches gibberish, misses curly apostrophes
- **P26 WR-03** — Journal PROMPTS English-only, sent without language detection
- **P39 WR-01..04,06,07** — 21 EN-only literals in `src/bot/handlers/profile.ts` catalogued by line:
  - Qualifier strings: 705, 706, 707
  - HEXACO dim labels: 715-720 (6 entries)
  - Schwartz dim labels: 728-737 (10 entries)
  - Score/confidence tokens: 791, 806
  - Plus `qualifierFor` duplicated in two files (drift risk for D027 mitigation)

### T7. Fixture Pipeline (3 known v2.6.1 items + Phase 24 collateral)

- **LOAD-01 root cause (P40):** HARN counts global telegram pensieve at `primed-sanity-m011.test.ts:89-100`; substrate filters by calendar-month window at `psychological-shared.ts:259-273`. Not a "3x loss" — empirical: 16,325 raw → 9,834 (Apr) / 4,115 (May) / 2,376 (Jun) — population is a window slice, not a loader bug.
- **FK-01 root cause (P40):** `synthesize-delta.ts:683-690` (fallback UUIDs) + `:934` (unconditional fusion). Pre-filter `fusedContradictions` against `Set(fusedPensieve.map(p=>p.id))` at line 934.
- **PMT-01 bias prompt edge (P40):** `phrasesClause` silently dropped unless `dimensionHint` also truthy at `synthesize-delta.ts:584-594`. Plus strict-inequality boundary at `primed-sanity-m011.test.ts:141,188` (`wordCount === 5000` vs substrate `< 5000` gate).
- **P24 BL-01** — Migration list hardcoded `0000..0005`, repo ships `0000..0013` → operator regen half-migrates DB; `wellbeing_snapshots.jsonl` silently dropped.
- **P24 BL-02** — SSH tunnel to prod Postgres has no `StrictHostKeyChecking` → MITM can capture `PROD_PG_PASSWORD`.
- **P24 BL-03** — `pensieve_embeddings.embedding vector(1024) NOT NULL` will fail `jsonb_populate_recordset` coerce; hidden because fixture has 0-byte embeddings JSONL. First non-empty regen hard-fails.

### T8. Schema Hygiene (root cause for M010 schema_mismatch)

- **P34 schema-drift root cause:** Phase 33 seed defaults `'{}'::jsonb` missing required nullable fields like `energy_30d_mean` → read-time `.strict()` rejects → `family.parent_care_responsibilities` + `health.wellbeing_trend` schema_mismatch warns. Fix: follow-on migration.
- **P37 WR-01** — No DB CHECK constraints on jsonb score/confidence ranges (HEXACO 1-5, Schwartz 0-7, confidence 0-1); only `overall_confidence` has CHECK. Defense-in-depth gap behind Zod.

### T9. Test Quality (calendar bombs, tautological assertions)

- **P36 BL-03** — Calendar bomb: `live-anti-hallucination.test.ts:164` uses real `new Date()`; fixture range ends 2026-05-19; after ~2026-07-18 the 60-day window is empty; assertion fails with misleading "Phase 35 wiring regression" message. ~9 weeks of shelf life.
- **P36 BL-04** — Prev-state injection asserted with `.some()` → passes if only 1 of 4 dimensions correctly injects. First-fire-celebration-blindness regression class incompletely defended.
- **P30 BL-03** — TEST-30 date-grounding assertion is tautological: mocks Sonnet to inject `${WEEKLY_REVIEW_SUNDAY_ISO}` literally, then asserts persisted observation contains that string. Tests templating, not grounding. REQ uncovered.
- **P30 BL-02** — Docstring claims cleanup "preserves the fixture's organic wellbeing_snapshots" but next line is unscoped `db.delete(wellbeingSnapshots)`. Lying docstring + silent fixture corruption.

### T10. Operational Hygiene (lower priority)

- P25 WR-02/05/06: cron-validate accepts 6-field expressions; scheduler reentrancy with no advisory lock; poison-pill `config_invalid` consumes per-tick slot forever
- P25 WR-04: TOCTOU on `incrementRitualDailyCount` (comment says safe; actual every-minute cron violates assumption)
- P31 WR-01/02/03: dead-code backward-compat constant; test inconsistency with D-31-03; `db.delete(rituals)` with no WHERE in test
- P37 WR-02: psychological-shared.ts ballooned 669 lines (Phase 38 merged in)
- P38 WR-06: duplicate error logging risk → operator double-alert

## v2.6.1 Scoping Recommendation

The original 7-item scope (ADJ-COPY-01 + 6 from invocation) is now visibly inadequate. A **conservative cleanup pass** should pull in P0 themes T1–T4 + T7 (which subsumes the original LOAD-01/FK-01/FIX-01) + T6 (which subsumes the original L10N-01). T5/T8/T9/T10 can be split into v2.6.1 stretch goals or deferred to v2.7.

| Cluster | P0 items | Recommended for v2.6.1 |
|---|---|---|
| T1 Adjustment-dialogue | 7 (BL-01..08,10) | **Yes — replaces ADJ-COPY-01** |
| T2 Atomicity/races | 6 | **Yes — production correctness** |
| T3 Silent CI milestone gates | 3 (P30,36,40 BL-01) | **Yes — gates are fake right now** |
| T4 Prompt injection | 2 (P34 BL-01, P38 WR-01) | **Yes — P34 is exploitable** |
| T5 Contract violations | 3 (P34 BL-02,03, P38 WR-02) | Recommend yes — affects M010+M011 inference |
| T6 FR/RU localization | ~30 sites | **Yes — replaces L10N-01 with comprehensive scope** |
| T7 Fixture pipeline | 6 (3 known + 3 Phase 24) | **Yes — replaces LOAD-01/FK-01/FIX-01** |
| T8 Schema hygiene | 2 (P34 root cause, P37 WR-01) | Recommend yes — feeds T5 |
| T9 Test quality | 4 (P36 BL-03,04; P30 BL-02,03) | Stretch — non-blocking but real |
| T10 Operational hygiene | ~10 WR | Defer to v2.7 backlog |

Plus the user's already-confirmed in-scope items:
- CIRC-01 — Schwartz circumplex display
- CROSS-VAL-01 — HEXACO × Schwartz cross-validation

If T1–T7 all go in, v2.6.1 becomes ~10 thematic requirements (each cluster = 1 REQ, line-by-line items become tasks within phases under that REQ). This is still a focused cleanup pass, not a feature milestone.
