# Milestone v2.2: M008 Episodic Consolidation

**Status:** SHIPPED 2026-04-19
**Phases:** 20, 21, 22, 22.1 (INSERTED), 23
**Total Plans:** 17 plans across 5 phases
**Requirements:** 35/35 satisfied

## Overview

Added a second memory tier above the raw Pensieve store — end-of-day episodic summaries that compress each day's entries into a structured narrative with importance scoring. Foundation for M009 weekly review (which consumes daily summaries as substrate) and M010+ profile inference (which needs consolidated episodes, not raw entries). Pensieve remains authoritative — episodic tier is a projection, not a replacement.

## Phases

### Phase 20: Schema + Tech Debt

**Goal**: The `episodic_summaries` table exists in Docker with correct indexes, the migration lineage is clean, and downstream phases have type-safe imports to build against.
**Depends on**: Phase 19 (v2.1 complete)
**Requirements**: TD-01, EPI-01, EPI-02, EPI-03, EPI-04
**Plans**: 3 plans

- [x] 20-01-PLAN.md — TECH-DEBT-19-01 clean-slate drizzle snapshot regen (TD-01) — completed 2026-04-18
- [x] 20-02-PLAN.md — `episodic_summaries` schema + migration 0005 + Zod types + `episodicCron` config (EPI-01..04) — completed 2026-04-18
- [x] 20-03-PLAN.md — Zod + schema integration tests + Docker gate (TEST coverage) — completed 2026-04-18 (Docker gate 853 passing / 61 failing / 914 total; +10 new tests; zero regressions)

**Details:**

Success Criteria (all TRUE):
1. `drizzle-kit generate` runs against a freshly-migrated Docker Postgres and reports "No schema changes, nothing to migrate" — no spurious diff, confirming 0001/0003 snapshots are regenerated and the lineage is clean.
2. Migration 0005 applies cleanly in the Docker test suite: `episodic_summaries` table exists with `UNIQUE(summary_date)`, `GIN(topics)`, and `btree(importance)` — all three indexes present in the initial migration, not retrofitted.
3. `import { EpisodicSummary } from 'src/episodic/types.ts'` compiles without error; `EpisodicSummary.parse({})` throws a Zod validation error (confirming Zod schema is live).
4. `config.episodicCron` is accessible at runtime with default value `"0 23 * * *"` — type-checked, no TypeScript errors.
5. The 152-test Docker Postgres gate remains green after the schema change.

### Phase 21: Consolidation Engine

**Goal**: Chris can generate a structurally correct, anti-sycophantic, verbatim-grounded episodic summary for any given calendar date, with importance scores calibrated by rubric and floor hooks, and surface Telegram notification on failure.
**Depends on**: Phase 20
**Requirements**: CONS-01..CONS-12 (12)
**Plans**: 4 plans

- [x] 21-01-PLAN.md — SDK bump to @anthropic-ai/sdk ^0.90.0 + export CONSTITUTIONAL_PREAMBLE from personality.ts (CONS-04 seed) — completed 2026-04-18 (Docker gate 857/61/918, +4)
- [x] 21-02-PLAN.md — Prompt assembler in src/episodic/prompts.ts (CONS-04, 05, 09, 10, 11) — completed 2026-04-18 (Docker gate 877/61/938, +20)
- [x] 21-03-PLAN.md — Read-only source queries in src/episodic/sources.ts (CONS-08, CONS-09 data layer) — completed 2026-04-18 (Docker gate 889/61/950, +12)
- [x] 21-04-PLAN.md — runConsolidate() end-to-end in src/episodic/consolidate.ts + notifyConsolidationError + 12 integration tests (CONS-01, 02, 03, 06, 07, 12) — completed 2026-04-18 (Docker gate 901/61/962, +12)

**Details:**

Success Criteria (all TRUE):
1. `runConsolidate(date)` on a day with zero Pensieve entries produces no Sonnet API call and no database row — confirmed by log output and DB query.
2. `runConsolidate(date)` twice for the same date with entries present inserts exactly one row; the second call returns `{ skipped: 'existing' }` without any Sonnet API call.
3. A unit test verifies the assembled consolidation system prompt contains the M006 constitutional preamble's anti-sycophancy clauses — the preamble is explicitly present, not assumed from engine context.
4. A fixture day containing a structural M007 decision produces a summary with `importance >= 6`; a fixture day containing an M002 contradiction produces a summary with `importance >= 7`, and the summary text or key_quotes preserves both contradicting positions verbatim.
5. A fixture day with fewer than 3 entries uses the sparse-entry variant prompt.

### Phase 22: Cron + Retrieval Routing

**Goal**: The daily consolidation fires automatically at 23:00 in Greg's timezone without interfering with the proactive sweep; queries to Chris route to raw entries or episodic summaries based on recency and query intent; INTERROGATE mode injects date-anchored summaries; summary text is provably absent from Known Facts and `pensieve_embeddings`.
**Depends on**: Phase 21
**Requirements**: CRON-01, CRON-02, RETR-01..RETR-06 (8)
**Plans**: 5 plans

- [x] 22-01-PLAN.md — `getEpisodicSummary` + `getEpisodicSummariesRange` exports in `src/pensieve/retrieve.ts` (RETR-01) — completed 2026-04-18 (Docker gate 911/61/972, +10)
- [x] 22-02-PLAN.md — Two-dimensional retrieval routing in `retrieveContext` (RETR-02 + RETR-03) — completed 2026-04-19 (5 named RoutingReason literals; VERBATIM_KEYWORDS EN/FR/RU; high-importance descent at importance≥8; Docker gate 934/15/949 excluded-suite)
- [x] 22-03-PLAN.md — INTERROGATE date-anchored summary injection (RETR-04) — completed 2026-04-19 (three-tier regex fast-path EN/FR/RU + Haiku fallback gated on 49-keyword heuristic; Docker gate 958/15/973, +24)
- [x] 22-04-PLAN.md — Known Facts + pensieve_embeddings boundary audit (RETR-05 + RETR-06) — completed 2026-04-19
- [x] 22-05-PLAN.md — Independent cron registration in `src/index.ts` with DST safety (CRON-01 + CRON-02) — completed 2026-04-19

**Details:**

Success Criteria (all TRUE):
1. The cron is registered as an independent `cron.schedule()` call in `src/index.ts` — grepping the file shows it is not nested inside `runSweep` or any other cron handler.
2. A query about an event from 3 days ago returns raw Pensieve entries; a query about an event from 10 days ago returns the episodic summary for that period; a query containing verbatim-fidelity keywords returns raw entries regardless of age.
3. A query against a day with a summary of `importance >= 8` returns both the summary and the underlying raw source entries.
4. An INTERROGATE-mode query mentioning a date or period older than 7 days injects matching episodic summaries into the context block.
5. `grep` of `src/chris/personality.ts` (Known Facts) and `src/pensieve/embeddings.ts` (INSERT path) contains no reference to `episodic_summaries`. Enforced by `src/chris/__tests__/boundary-audit.test.ts`.

### Phase 22.1: Wire `retrieveContext` into chat-mode handlers (INSERTED — gap closure)

**Goal**: Recency-based retrieval routing (RETR-02) and high-importance raw descent (RETR-03) are LIVE for chat modes — a 30-day-old user query in JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE escalates to the episodic-summary tier per the contract already shipped in Phase 22 Plan 02.
**Depends on**: Phase 22
**Requirements**: RETR-02 (wiring), RETR-03 (wiring) — implementation already shipped in Phase 22; this phase closes the gap that mode handlers bypass `retrieveContext` and call `hybridSearch` directly.
**Gap Closure**: Closes the v2.2-MILESTONE-AUDIT.md tech-debt item `retrieveContext` orphan-orchestrator finding.
**Plans**: 1 plan

- [x] 22.1-01-PLAN.md — Wire JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE handlers to `retrieveContext` via `hybridOptions` passthrough + `summaryToSearchResult` adapter + 15 regression tests — completed 2026-04-19 (Docker gate 996/15/1011, +15)

**Details:**

Success Criteria (all TRUE):
1. Each of JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE handlers calls `retrieveContext({ query, queryDate, rawLimit, hybridOptions: MODE_SEARCH_OPTIONS })` instead of calling `hybridSearch` directly.
2. A regression test per mode asserts old-dated query (queryAge > 7 days, no verbatim keyword) returns a summary-tier result and recent query (queryAge ≤ 7 days) returns raw entries — the routing decision actually fires.
3. INTERROGATE bypass and `/summary` (which calls `getEpisodicSummary` directly) are both unaffected — their existing tests still pass byte-identically.
4. The Docker test suite remains green vs the post-23-04 baseline (981 passed / 15 environmental).

### Phase 23: Test Suite + Backfill + `/summary`

**Goal**: The consolidation pipeline is validated end-to-end by a deterministic 14-day synthetic fixture; Greg can run a backfill for all days since M007 went live; and Greg can inspect any day's episodic summary via Telegram command.
**Depends on**: Phase 22
**Requirements**: TEST-15..TEST-22 (8), OPS-01, CMD-01 (10)
**Plans**: 4 plans

- [x] 23-01-PLAN.md — `src/episodic/__tests__/synthetic-fixture.test.ts` (TEST-15..TEST-21) — completed 2026-04-19 (Docker gate 973/15/988, +9)
- [x] 23-02-PLAN.md — `scripts/backfill-episodic.ts` operator script (OPS-01) — completed 2026-04-19 (Docker gate 976/15/991, +3)
- [x] 23-03-PLAN.md — `/summary [YYYY-MM-DD]` Telegram command (CMD-01) — completed 2026-04-19 (Docker gate 981/15/996, +5)
- [x] 23-04-PLAN.md — Live anti-flattery integration test TEST-22 — completed 2026-04-19 (targeted live Sonnet 1/1 in 25.37s; 3-of-3 atomic; zero flattery markers; excluded-suite 981/15/996 with TEST-22 file in exclusion list)

**Details:**

Success Criteria (all TRUE):
1. The 14-day synthetic fixture runs to completion in Docker with zero calendar time: all eight test cases pass (importance correlation r > 0.7, recency routing, DST boundary, idempotency retry, decision-day floor, contradiction dual-position, live anti-flattery gated on API key, sparse-entry no-hallucination).
2. `scripts/backfill-episodic.ts --from <date> --to <date>` inserts one row per calendar date that had Pensieve entries, skips dates with no entries, and running it a second time produces zero new inserts (idempotent).
3. Sending `/summary` in Telegram returns yesterday's summary; `/summary 2026-04-15` returns that specific date's summary; `/summary` for a date with no entries returns a clear "no summary for that date" message — not an error.
4. The Docker Postgres test gate count is higher than 152 (new fixture tests added to the passing suite). Cleared by 829 (981 vs 152).

---

## Milestone Summary

**Decimal Phases:**

- **Phase 22.1: Wire retrieveContext into chat-mode handlers (INSERTED)** — inserted 2026-04-19 after the v2.2 integration check identified `retrieveContext` as an orphaned export (zero production callers — chat modes called `hybridSearch` directly). 1-plan phase closed the wiring gap and flipped the milestone audit `status: tech_debt → passed` for RETR-02/03.

**Key Decisions (v2.2 milestone-level):**

- **D009 (v2.2) — episodic_summaries 8-column shape locked in Phase 20 CONTEXT:** `id` (uuid pk) + `summary_date` (date, NOT NULL, UNIQUE) + `summary` (text, NOT NULL) + `importance` (integer 1-10 CHECK) + `topics` (text[], default '{}') + `emotional_arc` (text, NOT NULL) + `key_quotes` (text[], default '{}') + `source_entry_ids` (uuid[], default '{}') + `created_at` (timestamptz, default now()). All three indexes (`UNIQUE(summary_date)`, `GIN(topics)`, `btree(importance)`) ship in migration 0005, not retrofitted.
- **Pensieve remains authoritative — episodic tier is a projection, not a replacement.** RETR-05/06 boundary preservation: summary text NEVER enters the Known Facts block (`src/chris/personality.ts`) and NEVER embeds into `pensieve_embeddings`. Enforced structurally by `src/chris/__tests__/boundary-audit.test.ts` — fails if `\bepisodic_summaries\b|\bepisodicSummaries\b` ever appears in personality.ts / ground-truth.ts / embeddings.ts.
- **retrieveContext as two-dimensional routing (recency + query intent).** `RECENCY_BOUNDARY_DAYS = 7` inclusive; `HIGH_IMPORTANCE_THRESHOLD = 8` inclusive. Five named `RoutingReason` literals (`verbatim-keyword`, `recent`, `no-summary-fallback`, `high-importance-descent`, `summary-only`) for diagnostic visibility. Pure keyword matching in the verbatim-fidelity fast-path — NO Haiku call (M008 deferral; M009+ may add Haiku fallback when miss rate is measurable).
- **INTERROGATE has its own ad-hoc date routing, separate from general retrieveContext — intentional.** `interrogate.ts:61-91` documents the rationale: different UX contract (citation-anchored Q&A) and different header form (`## Recent Episode Context (interpretation, not fact)` D031 boundary marker before Known Facts + verbatim Pensieve quotes). The two bypass the orchestrator by design.
- **Live anti-flattery test as empirical proof M006 preamble works end-to-end in consolidation.** TEST-22 surveys 17 forbidden flattery markers from existing M006 conventions (live-integration.test.ts VALIDATION_MARKERS + praise-quarantine.ts REFLEXIVE_OPENER_FIRST_WORDS + CONSTITUTIONAL_PREAMBLE Three Forbidden Behaviors). 3-of-3 atomic against real Sonnet on an adversarial fixture designed to bait flattery. Passed with zero markers across 3 iterations — empirical proof the preamble threads from engine into cron-invoked consolidation.
- **No Haiku fallback in verbatim-keyword fast-path (M008 deferral).** 15-keyword EN/FR/RU `VERBATIM_KEYWORDS` list is the sole gate; afterAll cumulative assertion `expect(mockAnthropicCreate).not.toHaveBeenCalled()` enforces. M009+ may add a Haiku fallback when real-world miss rate is measurable.
- **Decimal phase 22.1 for mid-milestone gap closure (vs. re-opening Phase 22).** Pattern borrowed from v2.1 Phase 15.1/16.1 precedent. Explicit (INSERTED) marker in the progress table; plan numbering `22.1-01` (not 22-06). Clean audit trail.

**Issues Resolved:**

- Drizzle-kit snapshot lineage (TECH-DEBT-19-01 from v2.1) resolved in Phase 20 Plan 01 via clean-slate iterative replay (`scripts/regen-snapshots.sh`). Phase 20 WR-02 hardened the script's EXIT trap to guard `0005_snapshot.json` via `REGEN_PRODUCED_0005` flag — re-run no longer destroys the committed snapshot.
- `retrieveContext` orphan-orchestrator finding from initial integration check closed in Phase 22.1 Plan 01.
- 14/14 Warning code-review findings resolved in post-audit fix cycle (P21 WR-01 tz plumbing; P22 WR-02 noon-UTC anchor; P22 WR-03 `\b` leading anchor for FR/RU month-day; P22 WR-04 calendar-validity reject for Feb 30/April 31/Feb 29 non-leap; P23 WR-01 Luxon ISO validation for `/summary`; P20 WR-02 EXIT-trap guard). Integration re-check flipped the audit `status: tech_debt → passed`.

**Issues Deferred:**

- **`getEpisodicSummariesRange`** — exported and tested but has zero production callers in v2.2. Acceptable as forward substrate for M009 weekly review (which will fan out across a 7-day window).
- **Phase 21 WR-02 retry-on-all-errors policy** — 1 Warning deferred with documented design rationale. `runConsolidate` retries once on any Sonnet/Zod error rather than classifying by error type. Rationale: the retry window is a recovery mechanism for transient failures; over-selective retry (only on rate-limit, for example) complicates the happy path without measurable benefit. M009+ may revisit if error patterns emerge.
- **12 human-UAT items carried from v2.1** (live Telegram feel, ACCOUNTABILITY tone, `/decisions` dashboard format, FR/RU localization) — still pending human runs.

**Technical Debt Incurred:**

- **Env-level vitest fork-IPC hang under HuggingFace EACCES.** Pre-existing M006/M007 issue surfacing again in full `bash scripts/test.sh` runs for Plans 22-02/03/05 and 23-01..04. Underlying cause: root-owned `node_modules/@huggingface/transformers` cache directory + `live-integration.test.ts` 401-retry loop triggering unhandled rejections under vitest 4 fork mode. Documented operational mitigation: 5-file excluded-suite (`live-integration.test.ts`, `live-accountability.test.ts`, `vague-validator-live.test.ts`, `contradiction-false-positive.test.ts`, `live-anti-flattery.test.ts`) reaches exit 0 in ~28s. Worth a future fix-up phase.
- **SUMMARY frontmatter hygiene still partial** — some plan-level SUMMARY.md files in Phases 20-23 may omit `one_liner:` / `requirements-completed:` frontmatter fields. Non-blocking; REQUIREMENTS.md remains the source of truth for requirement closure. Hygiene tech-debt persists from v2.1.

---

## Integration Check Summary

Integration re-check 2026-04-19 (post-code-review-fix):

| Metric | Result |
|---|---|
| Total requirements checked | 35 / 35 |
| WIRED | 35 (was 32 pre-22.1) |
| PARTIALLY WIRED | 0 (was 2) |
| UNWIRED | 0 (was 1) |
| E2E flows verified | 6 / 6 |
| Flows COMPLETE | 6 (was 5) |
| Flows DEGRADED | 0 (was 1) |
| Flows BROKEN | 0 |
| Boundary violations (RETR-05/06) | 0 |
| Orphaned exports | 0 (`retrieveContext` closed by Phase 22.1) |

All 35 milestone requirements are now wired end-to-end through committed code. The single material gap from the prior check (RETR-02/03 orphan-orchestrator) is closed — `retrieveContext` is now imported and called by all five chat-mode handlers (JOURNAL, REFLECT, COACH, PSYCHOLOGY, PRODUCE). The new `summaryToSearchResult` adapter bridges episodic rows into the same `SearchResult[]` pipeline mode handlers already consume. The six post-fix items (P21 WR-01 tz plumbing, P22 WR-02 noon-UTC, P22 WR-04 calendar validity, P23 WR-01 Luxon `/summary` validation, P20 WR-02 EXIT-trap guard, P22.1 wiring) are all present in the committed source.

E2E Flows:

| # | E2E Flow | Status |
|---|----------|--------|
| 1 | Daily cron consolidation (`cron → runConsolidateYesterday → runConsolidate → sources + prompt + Sonnet + INSERT → notify`) | COMPLETE |
| 2 | General retrieval routing (chat modes → `retrieveContext` → recency-based raw vs summary) | COMPLETE — closed in Phase 22.1 Plan 01; all 5 chat modes wired |
| 3 | INTERROGATE date-anchored injection (`extractQueryDate` → `getEpisodicSummary` → context injection) | COMPLETE |
| 4 | Operator backfill (`scripts/backfill-episodic.ts --from --to → runConsolidate per day`) | COMPLETE |
| 5 | `/summary [YYYY-MM-DD]` Telegram command | COMPLETE |
| 6 | Boundary preservation (Known Facts + pensieve_embeddings never reference episodic_summaries) | COMPLETE — enforced by `boundary-audit.test.ts` |

See `milestones/v2.2-INTEGRATION-CHECK.md` for the full re-check report and `milestones/v2.2-MILESTONE-AUDIT.md` for audit disposition.

---

_For current project status, see .planning/ROADMAP.md_
