---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/profiles/psychological-schemas.ts
  - src/db/schema.ts
  - src/db/migrations/0013_psychological_profiles.sql
  - src/db/migrations/meta/0013_snapshot.json
  - src/db/migrations/meta/_journal.json
  - scripts/test.sh
  - scripts/regen-snapshots.sh
autonomous: true
requirements: [PSCH-01, PSCH-02, PSCH-03, PSCH-04, PSCH-05, PSCH-06]
requirements_addressed: [PSCH-01, PSCH-02, PSCH-03, PSCH-04, PSCH-05, PSCH-06]
must_haves:
  truths:
    - "Migration 0013 applies cleanly on a fresh Docker Postgres and creates 3 new tables: profile_hexaco (6 jsonb dim columns), profile_schwartz (10 jsonb value columns), profile_attachment (3 jsonb dim columns + relational_word_count + activated)"
    - "All 3 psychological profile tables have the Never-Retrofit Checklist columns from day one (D-06): schema_version int NOT NULL DEFAULT 1, substrate_hash text NOT NULL DEFAULT '', name text NOT NULL UNIQUE DEFAULT 'primary', overall_confidence real NOT NULL DEFAULT 0 CHECK (0..1), word_count int NOT NULL DEFAULT 0, word_count_at_last_run int NOT NULL DEFAULT 0, last_updated timestamptz (nullable), created_at timestamptz NOT NULL DEFAULT now()"
    - "profile_attachment additionally has relational_word_count int NOT NULL DEFAULT 0 and activated boolean NOT NULL DEFAULT false from day one (D-07) — Pitfall 1 (Never-Retrofit incompleteness) mitigation"
    - "After migration applies, each of the 3 profile tables has exactly 1 sentinel row name='primary' with overall_confidence=0, word_count=0, word_count_at_last_run=0, substrate_hash='', schema_version=1, last_updated IS NULL, and all dimension columns default to JSON 'null' literal (D-08, D-12)"
    - "profile_attachment seed row also has relational_word_count=0 and activated=false (D-14)"
    - "_journal.json appends entry { idx: 13, version: '7', tag: '0013_psychological_profiles', when: > 1778482284254, breakpoints: true } — validate-journal-monotonic.ts passes (Pitfall 5 mitigation)"
    - "TypeScript compiler accepts profileHexaco/profileSchwartz/profileAttachment exports in src/db/schema.ts with .$type<HexacoProfileData[dim]>() inference resolving via psychological-schemas.ts imports; tsc --noEmit exits 0"
    - "Zod v3+v4 dual schemas in psychological-schemas.ts export HexacoProfileSchemaV3/V4, SchwartzProfileSchemaV3/V4, AttachmentProfileSchemaV3/V4 + inferred TypeScript types HexacoProfileData/SchwartzProfileData/AttachmentProfileData (PSCH-06)"
    - "scripts/test.sh applies migration 0013 after 0012 (sequential order) AND runs a new migration-0013 smoke gate asserting 3 tables + 3 seed rows + Never-Retrofit column count = 18 (3 tables × 6 cols) + attachment-specific count = 2"
    - "scripts/regen-snapshots.sh bumps cleanup-flag sentinels from 0013 → 0014 (lines 120, 143, 150, 153, 382-385, 397-399) so the real 0013_snapshot.json is preserved; bash scripts/regen-snapshots.sh prints 'No schema changes' confirming schema.ts ↔ 0013_snapshot.json lock-step"
    - "Pitfall 4 (Drizzle .$type<T>() jsonb nullability) avoided: jsonb dim columns typed as HexacoProfileData['honesty_humility'] etc. where the Zod factory uses .nullable() — TS infers HexacoDimension | null at the column level"
    - "HARD CO-LOC #M11-1 satisfied: all 7 artifacts in this one plan; splitting reproduces M010 PITFALL M010-11 lineage break (D-03)"
  artifacts:
    - path: "src/memory/profiles/psychological-schemas.ts"
      provides: "Zod v3 + v4 dual schemas for HEXACO (6 dims), Schwartz (10 values), Attachment (3 dims) + shared per-dim factories (hexacoSchwartzDimensionSchemaV3 + attachmentDimensionSchemaV3 nominally separate per D-27) + exported types HexacoProfileData/SchwartzProfileData/AttachmentProfileData"
      min_lines: 90
      contains: "export const HexacoProfileSchemaV3"
    - path: "src/db/schema.ts"
      provides: "3 new pgTable exports (profileHexaco/profileSchwartz/profileAttachment) inserted after profileFamily (line 613) and before profileHistory (line 646), plus appended type imports from ../memory/profiles/psychological-schemas.js"
      contains: "export const profileHexaco"
    - path: "src/db/migrations/0013_psychological_profiles.sql"
      provides: "Hand-authored DDL — 3× CREATE TABLE IF NOT EXISTS + 3× sentinel-row INSERT ... ON CONFLICT (name) DO NOTHING with statement-breakpoint markers"
      min_lines: 110
      contains: "CREATE TABLE IF NOT EXISTS \"profile_hexaco\""
    - path: "src/db/migrations/meta/0013_snapshot.json"
      provides: "Drizzle-kit regenerated meta snapshot for migration 0013; chains from 0012_snapshot.json via prevId"
    - path: "src/db/migrations/meta/_journal.json"
      provides: "Journal entry idx=13 tag=0013_psychological_profiles when > 1778482284254 breakpoints=true"
      contains: "0013_psychological_profiles"
    - path: "scripts/test.sh"
      provides: "MIGRATION_13_SQL declaration + psql apply line after the 0012 apply + new migration-0013 smoke gate (3 tables + seed rows + 18 + 2 column-count assertions)"
      contains: "MIGRATION_13_SQL"
    - path: "scripts/regen-snapshots.sh"
      provides: "MIGRATION_13 variable + apply_sql chain extension + Phase 37 comment block + 0013→0014 sentinel bumps at 5 sites"
      contains: "MIGRATION_13"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/memory/profiles/psychological-schemas.ts"
      via: "type-only imports of HexacoProfileData/SchwartzProfileData/AttachmentProfileData for jsonb .$type<T>() annotations on the 3 new pgTable exports"
      pattern: "import type.*from '../memory/profiles/psychological-schemas"
    - from: "scripts/test.sh"
      to: "src/db/migrations/0013_psychological_profiles.sql"
      via: "psql ON_ERROR_STOP=1 apply (line after MIGRATION_12_SQL apply at line 83)"
      pattern: "psql.*MIGRATION_13_SQL"
    - from: "src/db/migrations/meta/_journal.json"
      to: "src/db/migrations/0013_psychological_profiles.sql"
      via: "tag field exactly matches SQL filename basename (drizzle migrator lookup)"
      pattern: "0013_psychological_profiles"
    - from: "src/db/migrations/meta/0013_snapshot.json"
      to: "src/db/schema.ts"
      via: "drizzle-kit-generated lock-step snapshot; bash scripts/regen-snapshots.sh prints 'No schema changes' confirming match"
      pattern: "profile_hexaco"
---

<objective>
Ship the M011 psychological-profile persistence substrate as ONE atomic migration plan (HARD CO-LOC #M11-1 per CONTEXT.md D-03). Migration 0013 creates the 3 new psychological profile tables (HEXACO/Schwartz/Attachment) with the full Never-Retrofit Checklist on each, inserts cold-start seed rows (all dims NULL, overall_confidence=0), regenerates the drizzle meta snapshot, extends `scripts/test.sh` and `scripts/regen-snapshots.sh` in lock-step, and ships the Zod v3+v4 dual schemas that the Drizzle `.$type<T>()` annotations import.

Purpose: Splitting any of the 7 artifacts reproduces the M010 PITFALL M010-11 lineage break. The Never-Retrofit Checklist (7 columns per D-06 + 2 attachment-only per D-07) MUST all land in this plan — adding `relational_word_count`, `activated`, `word_count_at_last_run`, `substrate_hash`, or `schema_version` in a future ALTER migration is forbidden (Pitfall 1). `psychological-schemas.ts` ships HERE because `src/db/schema.ts`'s `.$type<HexacoProfileData['honesty_humility']>()` annotations need the type exports at typecheck time (same coupling as M010 schema.ts ↔ schemas.ts at lines 21-27).

Output: 1 new SQL migration (~110 lines), 3 new pgTable definitions in schema.ts, 1 new Zod schemas file (~90 lines), 1 new drizzle meta snapshot, 1 appended journal entry, ~30 lines of new bash in `scripts/test.sh`, ~10 lines of 0013→0014 sentinel bumps in `scripts/regen-snapshots.sh`. Zero new files outside `src/db/` and `src/memory/profiles/psychological-schemas.ts`.

Satisfies: PSCH-01 (3-table migration + Never-Retrofit), PSCH-02 (HEXACO 6 dims), PSCH-03 (Schwartz 10 values), PSCH-04 (Attachment 3 dims schema-only), PSCH-05 (cold-start seed rows), PSCH-06 (Zod v3+v4 dual schemas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-psychological-substrate/37-CONTEXT.md
@.planning/phases/37-psychological-substrate/37-RESEARCH.md
@.planning/phases/37-psychological-substrate/37-PATTERNS.md
@.planning/phases/37-psychological-substrate/37-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

# Codebase analogs (read before touching the corresponding file)
@src/db/migrations/0012_operational_profiles.sql
@src/db/schema.ts
@src/db/migrations/meta/_journal.json
@src/memory/profiles/schemas.ts
@scripts/test.sh
@scripts/regen-snapshots.sh
@scripts/validate-journal-monotonic.ts

# Phase 33 plan precedent (exact structural mirror for #M11-1)
@.planning/milestones/v2.5-phases/33-profile-substrate/33-01-PLAN.md

<interfaces>
<!-- These interfaces emerge from the codebase (verified live 2026-05-13). The
     executor consumes them directly; no codebase exploration needed beyond the
     file being edited. -->

From src/db/schema.ts:21-27 (existing M010 type imports — APPEND PSYCHOLOGICAL TYPES TO THIS BLOCK):
```typescript
import type {
  JurisdictionalProfileData,
  CapitalProfileData,
  HealthProfileData,
  FamilyProfileData,
  ProfileSnapshot,
} from '../memory/profiles/schemas.js';
// NEW APPEND:
import type {
  HexacoProfileData,
  SchwartzProfileData,
  AttachmentProfileData,
} from '../memory/profiles/psychological-schemas.js';
```

From src/db/schema.ts:1-18 (existing pgTable/column-type imports — ALREADY INCLUDES boolean, jsonb, uuid, text, integer, real, timestamp, check, sql; no new pgTable-side imports needed):
```typescript
import { pgTable, uuid, text, jsonb, timestamp, integer, real, boolean, check, sql } from 'drizzle-orm/pg-core';
```

From src/db/schema.ts:613-646 (insertion gap between profileFamily and profileHistory — VERIFIED at exactly those line numbers in this session):
```typescript
export const profileFamily = pgTable(  // starts line 613
  'profile_family',
  { /* ... */ },
);

// ← INSERT profileHexaco, profileSchwartz, profileAttachment HERE

export const profileHistory = pgTable(  // starts line 646
  'profile_history',
  /* ... */
);
```

From src/db/migrations/meta/_journal.json (last entry — VERIFIED):
```json
{
  "idx": 12,
  "version": "7",
  "when": 1778482284254,
  "tag": "0012_operational_profiles",
  "breakpoints": true
}
```

From src/memory/profiles/schemas.ts:1-19 (operational Zod analog — header + import block):
```typescript
import { z } from 'zod';
import * as zV4 from 'zod/v4';
```

From scripts/test.sh:19-20 + :81-83 (M010 migration apply lines — VERIFIED):
```bash
MIGRATION_11_SQL="src/db/migrations/0011_rename_daily_voice_note_to_journal.sql"
MIGRATION_12_SQL="src/db/migrations/0012_operational_profiles.sql"
# ... line 81: psql ... < $MIGRATION_11_SQL
# ... line 83: psql ... < $MIGRATION_12_SQL
```

From scripts/regen-snapshots.sh (M010 cleanup-flag pattern — VERIFIED lines 67, 120, 122, 143, 150, 152-153, 375, 382-385, 397-399):
- L67: `MIGRATION_12="${MIGRATIONS_DIR}/0012_operational_profiles.sql"`
- L120: comment "post-0012 acceptance-gate artifact (0013_snapshot.json...)"
- L143: `find ... -name "0013_acceptance_check*.sql" -delete`
- L153: `find "${META_DIR}" -name "0013_snapshot.json" -delete`
- L375: `apply_sql "${MIGRATION_12}"`
- L382-385: comment "0013_snapshot.json..." + `REGEN_PRODUCED_ACCEPTANCE=1`
- L397-399: post-apply cleanup of `0013_acceptance_check*.sql` + `0013_snapshot.json`
</interfaces>
</context>

<deployment>
**Production discipline (CLAUDE.md):** Phase 37 deploys via container restart; drizzle migrator auto-applies migration 0013 on first startup post-merge. Operator must explicitly confirm prod push — this plan does NOT autonomously touch production Postgres at 192.168.1.50. CI green + `bash scripts/regen-snapshots.sh` 'No schema changes' is the gate before operator confirmation. NEVER skip Docker tests (CLAUDE.md memory: "Always run full Docker tests").
</deployment>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create psychological-schemas.ts with Zod v3+v4 dual schemas for HEXACO, Schwartz, Attachment</name>
  <read_first>
    - src/memory/profiles/schemas.ts (entire file — operational Zod v3+v4 dual-schema analog; identical structure with operational dim fields)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 3 lines 624-704 — verbatim psychological-schemas.ts skeleton)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/memory/profiles/psychological-schemas.ts` section — divergences enumerated)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-09, D-10, D-25, D-26, D-27 — per-dim shape + dual-schema discipline + nominal factory separation)
  </read_first>
  <files>src/memory/profiles/psychological-schemas.ts (NEW FILE)</files>
  <behavior>
    - File defines 3 v3 Zod schemas (HexacoProfileSchemaV3, SchwartzProfileSchemaV3, AttachmentProfileSchemaV3), 3 v4 mirrors, 3 inferred TypeScript types (HexacoProfileData, SchwartzProfileData, AttachmentProfileData) all exported.
    - HEXACO has 6 dimension fields. Schwartz has 10 value fields. Attachment has 3 dimension fields.
    - All v3 schemas use .strict() at the top level (rejects unknown jsonb keys at read boundary; defends against future psychological-shape drift). v4 mirrors OMIT .strict() per M009 D-29-02 (SDK doesn't parse strict-mode JSON Schema).
    - Two nominally separate per-dim factories per D-27: hexacoSchwartzDimensionSchemaV3 (shared between HEXACO and Schwartz) + attachmentDimensionSchemaV3 (identical shape, separately named for nominal typing + future divergence).
    - Each factory shape: { score: z.number().min(1).max(5), confidence: z.number().min(0).max(1), last_updated: z.string().datetime() }.strict().nullable() — the .nullable() at factory level enables cold-start seed rows with all dims set to JSON null.
    - v4 factory equivalent: zV4.object({ score: zV4.number().min(1).max(5), confidence: zV4.number().min(0).max(1), last_updated: zV4.string() }).nullable() — last_updated drops .datetime() per RESEARCH.md Example 3 line 644.
    - NO data_consistency field at the schema level (M011 stores per-dim confidence; row-level overall_confidence is metadata stripped by reader).
    - NO evidence_count field (D-11 — deferred to v2.6.1).
    - attachment schema-level fields: anxious, avoidant, secure (3 dims). NO relational_word_count or activated in the Zod schema — they live as row-level metadata columns stripped by stripPsychologicalMetadataColumns in Plan 37-02.
  </behavior>
  <action>
    Create `src/memory/profiles/psychological-schemas.ts`. File header docstring must cite Phase 37 Plan 37-01 Task 1 (PSCH-06 substrate) + reference 37-CONTEXT.md D-25..D-28 + state the v3/v4 lockstep discipline.

    Imports: `import { z } from 'zod';` and `import * as zV4 from 'zod/v4';` — same as schemas.ts:18-19.

    Define two private factory pairs at top of file. Factory `hexacoSchwartzDimensionSchemaV3`: z.object with three fields `score` (z.number().min(1).max(5)), `confidence` (z.number().min(0).max(1)), `last_updated` (z.string().datetime()) — chained with .strict() then .nullable(). Factory `hexacoSchwartzDimensionSchemaV4`: zV4.object with same three fields but `last_updated: zV4.string()` (no .datetime()) — chained with .nullable() only (no .strict()). Mirror these as `attachmentDimensionSchemaV3` and `attachmentDimensionSchemaV4` with identical bodies but distinct names per D-27.

    Define `HexacoProfileSchemaV3`: z.object with six fields (honesty_humility, emotionality, extraversion, agreeableness, conscientiousness, openness) each set to hexacoSchwartzDimensionSchemaV3, chained with .strict() at the top level. Export `type HexacoProfileData = z.infer<typeof HexacoProfileSchemaV3>`.

    Define `HexacoProfileSchemaV4`: zV4.object with same six fields each set to hexacoSchwartzDimensionSchemaV4. NO .strict().

    Define `SchwartzProfileSchemaV3`: z.object with ten value fields (self_direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism) each set to hexacoSchwartzDimensionSchemaV3, .strict(). Export `type SchwartzProfileData`.

    Define `SchwartzProfileSchemaV4`: zV4 mirror, no .strict().

    Define `AttachmentProfileSchemaV3`: z.object with three fields (anxious, avoidant, secure) each set to attachmentDimensionSchemaV3, .strict(). Export `type AttachmentProfileData`.

    Define `AttachmentProfileSchemaV4`: zV4 mirror, no .strict().

    Export all six schemas (3 v3 + 3 v4) and all three types via named exports. Field-name discipline: snake_case in Zod schemas (matches jsonb column names in migration SQL); camelCase only at the Drizzle TypeScript level (schema.ts handles the conversion via column-name-as-arg).
  </action>
  <acceptance_criteria>
    - File `src/memory/profiles/psychological-schemas.ts` exists.
    - Exactly 6 `export const ...ProfileSchemaV3` / `...ProfileSchemaV4` lines present (3 v3 + 3 v4): `grep -c "^export const \(Hexaco\|Schwartz\|Attachment\)ProfileSchemaV[34]" src/memory/profiles/psychological-schemas.ts` returns `6`.
    - Exactly 3 `export type ...ProfileData` lines present: `grep -c "^export type \(Hexaco\|Schwartz\|Attachment\)ProfileData" src/memory/profiles/psychological-schemas.ts` returns `3`.
    - File imports `z` from `'zod'` AND `* as zV4` from `'zod/v4'` (both imports present).
    - All 3 v3 top-level schemas use `.strict()` (verify: at least 3 top-level `.strict()` chained at the schema closing brace).
    - v4 mirrors omit `.strict()` per M009 D-29-02.
    - Per-dim factory `hexacoSchwartzDimensionSchemaV3` is `.nullable()` at the factory level (cold-start seed rows with all dims null parse successfully).
    - Per-dim factory `attachmentDimensionSchemaV3` is separately named per D-27 (nominal typing for future divergence).
    - `npx tsc --noEmit` produces zero `error TS` lines for `psychological-schemas.ts`.
    - No `data_consistency` or `evidence_count` fields in any schema (D-11 deferred).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tee /tmp/tsc.log; grep -E "psychological-schemas\\.ts" /tmp/tsc.log; if grep -E "psychological-schemas\\.ts.*error" /tmp/tsc.log; then echo FAIL; exit 1; fi; echo "✓ tsc clean for psychological-schemas.ts"</automated>
    <automated>grep -c "^export const \\(HexacoProfileSchemaV3\\|HexacoProfileSchemaV4\\|SchwartzProfileSchemaV3\\|SchwartzProfileSchemaV4\\|AttachmentProfileSchemaV3\\|AttachmentProfileSchemaV4\\)" src/memory/profiles/psychological-schemas.ts; test "$(grep -c "^export const \\(HexacoProfileSchemaV3\\|HexacoProfileSchemaV4\\|SchwartzProfileSchemaV3\\|SchwartzProfileSchemaV4\\|AttachmentProfileSchemaV3\\|AttachmentProfileSchemaV4\\)" src/memory/profiles/psychological-schemas.ts)" = "6"</automated>
    <automated>grep -E "^export type (HexacoProfileData|SchwartzProfileData|AttachmentProfileData)" src/memory/profiles/psychological-schemas.ts | wc -l | grep -q "^3$"</automated>
  </verify>
  <done>
    File exists at src/memory/profiles/psychological-schemas.ts. Exports 6 Zod schemas (3 v3 + 3 v4) + 3 inferred TypeScript types. tsc --noEmit exits 0 for this file. Two private factories defined (hexacoSchwartz* and attachment*) per D-27 nominal separation. v3 schemas use .strict() at top level; v4 mirrors omit .strict(). Per-dim shape factory body is .nullable() so cold-start seed rows with all dims null parse successfully.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 3 pgTable exports + type imports to src/db/schema.ts (profileHexaco, profileSchwartz, profileAttachment)</name>
  <read_first>
    - src/db/schema.ts (lines 1-30 — existing import block including type imports from M010 schemas.ts; lines 536-660 — full M010 profile table block including profileJurisdictional (line 536), profileCapital, profileHealth, profileFamily (line 613), profileHistory (line 646))
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 2 lines 586-622 — verbatim profileHexaco pgTable skeleton; section "Specific Research Findings §2" lines 1089-1102 — insertion-point line numbers and .$type<T>() nullability)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/db/schema.ts` section — divergences enumerated #1-9)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-06, D-07, D-08 — Never-Retrofit Checklist columns)
    - src/memory/profiles/psychological-schemas.ts (just created in Task 1 — provides the TS types for .$type<T>())
  </read_first>
  <files>src/db/schema.ts (MODIFIED — 3 new pgTable exports + appended type import block)</files>
  <behavior>
    - Three new pgTable exports inserted in the 10-line gap between profileFamily (ends ~line 636) and profileHistory (starts line 646). Order: profileHexaco, profileSchwartz, profileAttachment.
    - Type import block appended to existing import (lines 21-27) — adds HexacoProfileData, SchwartzProfileData, AttachmentProfileData from ../memory/profiles/psychological-schemas.js.
    - No existing M010 profile tables modified. profileHistory (line 646) reused unchanged per D-04.
    - Each new pgTable mirrors profileJurisdictional shape (line 536) with M011 column substitutions (D-06, D-07, D-08).
    - Drizzle column-type imports (pgTable, uuid, text, jsonb, timestamp, integer, real, boolean, check, sql) ALREADY present in schema.ts:1-18 — no new imports needed.
    - Pitfall 4 mitigation: each jsonb dim column uses `.$type<HexacoProfileData['honesty_humility']>()` where the Zod factory is .nullable() — TypeScript infers HexacoDimension | null at the column level. Column-level .notNull() is used to match M010 pattern (the JSON 'null' literal is a value, not SQL NULL).
  </behavior>
  <action>
    Append to existing type-import block at src/db/schema.ts:21-27. After the M010 type import block (`import type { JurisdictionalProfileData, ... } from '../memory/profiles/schemas.js';`), add a new import: `import type { HexacoProfileData, SchwartzProfileData, AttachmentProfileData } from '../memory/profiles/psychological-schemas.js';`.

    Insert three new pgTable exports between line 636 (end of profileFamily) and line 646 (start of profileHistory). Order: profileHexaco, profileSchwartz, profileAttachment.

    `profileHexaco` definition: `export const profileHexaco = pgTable('profile_hexaco', { id: uuid('id').primaryKey().default(sql\`gen_random_uuid()\`).notNull(), name: text('name').notNull().default('primary').unique(), schemaVersion: integer('schema_version').notNull().default(1), substrateHash: text('substrate_hash').notNull().default(''), overallConfidence: real('overall_confidence').notNull().default(0), wordCount: integer('word_count').notNull().default(0), wordCountAtLastRun: integer('word_count_at_last_run').notNull().default(0), honestyHumility: jsonb('honesty_humility').$type<HexacoProfileData['honesty_humility']>().notNull().default(sql\`'null'::jsonb\`), emotionality: jsonb('emotionality').$type<HexacoProfileData['emotionality']>().notNull().default(sql\`'null'::jsonb\`), extraversion: jsonb('extraversion').$type<HexacoProfileData['extraversion']>().notNull().default(sql\`'null'::jsonb\`), agreeableness: jsonb('agreeableness').$type<HexacoProfileData['agreeableness']>().notNull().default(sql\`'null'::jsonb\`), conscientiousness: jsonb('conscientiousness').$type<HexacoProfileData['conscientiousness']>().notNull().default(sql\`'null'::jsonb\`), openness: jsonb('openness').$type<HexacoProfileData['openness']>().notNull().default(sql\`'null'::jsonb\`), lastUpdated: timestamp('last_updated', { withTimezone: true }), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow() }, (table) => [ check('profile_hexaco_overall_confidence_bounds', sql\`${table.overallConfidence} >= 0 AND ${table.overallConfidence} <= 1\`) ]);`. CRITICAL: `lastUpdated` has NO .notNull() and NO .defaultNow() — nullable, null = "never run" per D-06.

    `profileSchwartz` definition: identical shape to profileHexaco but with 10 jsonb value columns named selfDirection ('self_direction'), stimulation ('stimulation'), hedonism ('hedonism'), achievement ('achievement'), power ('power'), security ('security'), conformity ('conformity'), tradition ('tradition'), benevolence ('benevolence'), universalism ('universalism'). Each typed `.$type<SchwartzProfileData['<field>']>()`. CHECK constraint name: `profile_schwartz_overall_confidence_bounds`.

    `profileAttachment` definition: identical metadata-column shape to profileHexaco but with 3 jsonb dim columns (anxious typed `AttachmentProfileData['anxious']`, avoidant `AttachmentProfileData['avoidant']`, secure `AttachmentProfileData['secure']`) AND two additional row-level columns: `relationalWordCount: integer('relational_word_count').notNull().default(0)` and `activated: boolean('activated').notNull().default(false)` per D-07. CHECK constraint name: `profile_attachment_overall_confidence_bounds`. Position the two extra columns AFTER wordCountAtLastRun and BEFORE the jsonb dim columns to mirror the migration SQL column order.

    DO NOT modify profileHistory (line 646) — it remains unchanged per D-04. The discriminator column profileTableName is already freeform text NOT NULL and accommodates the new string values 'profile_hexaco', 'profile_schwartz', 'profile_attachment' without schema change.
  </action>
  <acceptance_criteria>
    - 3 new `export const profile*` lines: `profileHexaco`, `profileSchwartz`, `profileAttachment` (verify: `grep -c "^export const profile\(Hexaco\|Schwartz\|Attachment\) = pgTable" src/db/schema.ts` returns `3`).
    - Type-only import block appended: `import type { HexacoProfileData, SchwartzProfileData, AttachmentProfileData } from '../memory/profiles/psychological-schemas.js';`
    - Each of the 3 psych pgTables has `schemaVersion: integer('schema_version').notNull().default(1)` line.
    - Each of the 3 psych pgTables has `substrateHash: text('substrate_hash').notNull().default('')` line.
    - Each of the 3 psych pgTables has `name: text('name').notNull().default('primary').unique()` line.
    - Each of the 3 psych pgTables has `overallConfidence: real('overall_confidence').notNull().default(0)` (rename from M010 `confidence`).
    - Each of the 3 psych pgTables has `wordCount` AND `wordCountAtLastRun` integer columns (D-06).
    - profileAttachment additionally has `relationalWordCount: integer('relational_word_count')...` AND `activated: boolean('activated')...` (D-07).
    - Each of the 3 psych pgTables has `check('profile_*_overall_confidence_bounds', ...)` constraint.
    - `lastUpdated` is nullable (no `.notNull()` and no `.defaultNow()`) per D-06 — null = "never run".
    - profileHistory (line 646 in original) UNMODIFIED; verify: `grep -A 8 "^export const profileHistory" src/db/schema.ts` matches the pre-edit content.
    - `npx tsc --noEmit` produces zero `error TS.*schema.ts` lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^export const \\(profileHexaco\\|profileSchwartz\\|profileAttachment\\) = pgTable" src/db/schema.ts | grep -q "^3$"</automated>
    <automated>grep -c "from '../memory/profiles/psychological-schemas" src/db/schema.ts | grep -q "^1$"</automated>
    <automated>grep -E "relational_word_count|activated" src/db/schema.ts | grep -v "^#" | wc -l | xargs -I {} test {} -ge 2</automated>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "schema\\.ts.*error" && echo FAIL && exit 1 || echo "✓ tsc clean for schema.ts"</automated>
  </verify>
  <done>
    src/db/schema.ts has 3 new pgTable exports between profileFamily and profileHistory. Type imports from psychological-schemas.js appended. profileHistory (line 646 originally; may have shifted) unchanged. tsc --noEmit exits 0. profileAttachment includes relational_word_count + activated row-level columns. lastUpdated on all 3 tables is nullable (no .notNull(), no .defaultNow()).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hand-author migration 0013 SQL — 3× CREATE TABLE + 3× sentinel-row INSERT</name>
  <read_first>
    - src/db/migrations/0012_operational_profiles.sql (entire file ~200 lines — verbatim template; mirrors structure 1:1)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (Example 1 lines 509-584 — verbatim 0013 SQL skeleton with 'null'::jsonb defaults + NOT NULL discipline + sentinel-row INSERT pattern; section "Specific Research Findings §1" lines 1050-1087 — column DDL ordering convention)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/db/migrations/0013_psychological_profiles.sql` section — divergences #1-11 from 0012 enumerated)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-06, D-07, D-08, D-12, D-13, D-14 — Never-Retrofit + seed rows)
    - .planning/research/PITFALLS.md (Pitfalls 4 + 7 — Never-Retrofit incompleteness, speech-source contamination)
  </read_first>
  <files>src/db/migrations/0013_psychological_profiles.sql (NEW FILE)</files>
  <behavior>
    - Hand-authored Postgres-flavored SQL with CREATE TABLE IF NOT EXISTS + --> statement-breakpoint markers (mirrors 0012 idempotency guards for raw-psql replay safety in scripts/test.sh).
    - Header docstring cites Phase 37 (M011 v2.6), HARD CO-LOC #M11-1, all 7 atomic artifacts, and the Never-Retrofit Checklist column set (Pitfall 1 + D042 mitigation).
    - Three CREATE TABLE statements: profile_hexaco (6 jsonb cols), profile_schwartz (10 jsonb cols), profile_attachment (3 jsonb cols + relational_word_count int + activated boolean). Each table has the full Never-Retrofit Checklist.
    - Three sentinel-row INSERT statements with `ON CONFLICT ("name") DO NOTHING` (D-12, D-13, D-14). Three separate INSERT statements (not one composite block) per CONTEXT.md "Claude's Discretion" recommendation — easier to grep for in a migration audit.
    - Each CREATE TABLE ends with --> statement-breakpoint marker; each INSERT ends with --> statement-breakpoint marker. Final marker after the last INSERT.
  </behavior>
  <action>
    Create `src/db/migrations/0013_psychological_profiles.sql`. Header comment block mirrors 0012:1-23 with the following substitutions: "Phase 37 (M011 v2.6) — Psychological profile substrate.", "HARD CO-LOCATION #M11-1: this SQL + src/db/schema.ts table defs + migrations/meta/0013_snapshot.json + _journal.json entry + scripts/test.sh psql apply line + scripts/regen-snapshots.sh cleanup-flag bump + src/memory/profiles/psychological-schemas.ts Zod type exports ALL ship in Plan 37-01 atomically.". List the Never-Retrofit columns per D-06 + D-07 inline in the comment block. Do NOT cite ground-truth.ts (psychological profiles are inferred-only).

    First CREATE TABLE block: `CREATE TABLE IF NOT EXISTS "profile_hexaco"` with columns in this exact order (matching schema.ts via Drizzle convention): `id uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL`, `name text DEFAULT 'primary' NOT NULL`, `schema_version integer DEFAULT 1 NOT NULL`, `substrate_hash text DEFAULT '' NOT NULL`, `overall_confidence real DEFAULT 0 NOT NULL`, `word_count integer DEFAULT 0 NOT NULL`, `word_count_at_last_run integer DEFAULT 0 NOT NULL`, then 6 jsonb dim columns each `jsonb DEFAULT 'null'::jsonb NOT NULL` (honesty_humility, emotionality, extraversion, agreeableness, conscientiousness, openness), then `last_updated timestamp with time zone` (NO DEFAULT, NULLABLE per D-06), then `created_at timestamp with time zone DEFAULT now() NOT NULL`. End with two CONSTRAINTs: `CONSTRAINT "profile_hexaco_name_unique" UNIQUE("name")` and `CONSTRAINT "profile_hexaco_overall_confidence_bounds" CHECK ("profile_hexaco"."overall_confidence" >= 0 AND "profile_hexaco"."overall_confidence" <= 1)`. Close with `);` then `--> statement-breakpoint` on next line.

    Second CREATE TABLE block: identical shape to profile_hexaco but for `profile_schwartz` with 10 jsonb value columns in this exact alphabetical-by-academic-construct order: self_direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism. CHECK constraint name: `profile_schwartz_overall_confidence_bounds`. UNIQUE constraint name: `profile_schwartz_name_unique`. End with --> statement-breakpoint.

    Third CREATE TABLE block: `profile_attachment` with 3 jsonb dim columns (anxious, avoidant, secure) PLUS two additional row-level columns after word_count_at_last_run and BEFORE the jsonb columns: `relational_word_count integer DEFAULT 0 NOT NULL` and `activated boolean DEFAULT false NOT NULL` (per D-07). CHECK constraint name: `profile_attachment_overall_confidence_bounds`. UNIQUE constraint name: `profile_attachment_name_unique`. End with --> statement-breakpoint.

    First INSERT: `INSERT INTO "profile_hexaco" ("name", "schema_version", "substrate_hash", "overall_confidence", "word_count", "word_count_at_last_run") VALUES ('primary', 1, '', 0, 0, 0) ON CONFLICT ("name") DO NOTHING;`. End with --> statement-breakpoint. Dim columns are omitted — they default to 'null'::jsonb per the CREATE TABLE default. last_updated omitted — it's nullable with no default, so the row will have NULL (correct cold-start state).

    Second INSERT: identical to first but `INSERT INTO "profile_schwartz"`. Same column set, same values.

    Third INSERT: `INSERT INTO "profile_attachment" ("name", "schema_version", "substrate_hash", "overall_confidence", "word_count", "word_count_at_last_run", "relational_word_count", "activated") VALUES ('primary', 1, '', 0, 0, 0, 0, false) ON CONFLICT ("name") DO NOTHING;`. End with --> statement-breakpoint.

    DO NOT include any data_consistency column (M011 stores per-dim confidence; D-11 deferred field omitted). DO NOT include any seed-row data populated from ground-truth.ts (psychological profiles are inferred-only — distinct from M010 where capital had confidence=0.2 from ground truth). All seed rows have overall_confidence=0 (cold start; D-12).
  </action>
  <acceptance_criteria>
    - File `src/db/migrations/0013_psychological_profiles.sql` exists.
    - Exactly 3 `CREATE TABLE IF NOT EXISTS "profile_*"` statements (`profile_hexaco`, `profile_schwartz`, `profile_attachment`).
    - Exactly 3 sentinel-row INSERT statements with `ON CONFLICT ("name") DO NOTHING`.
    - Each CREATE TABLE has `schema_version integer DEFAULT 1 NOT NULL`, `substrate_hash text DEFAULT '' NOT NULL`, `name text DEFAULT 'primary' NOT NULL`, `overall_confidence real DEFAULT 0 NOT NULL`, `word_count integer DEFAULT 0 NOT NULL`, `word_count_at_last_run integer DEFAULT 0 NOT NULL`.
    - Each CREATE TABLE has `last_updated timestamp with time zone` (no DEFAULT, nullable) per D-06.
    - Each CREATE TABLE has `created_at timestamp with time zone DEFAULT now() NOT NULL`.
    - profile_hexaco has 6 jsonb columns (honesty_humility, emotionality, extraversion, agreeableness, conscientiousness, openness), each `jsonb DEFAULT 'null'::jsonb NOT NULL`.
    - profile_schwartz has 10 jsonb columns (self_direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism), each `jsonb DEFAULT 'null'::jsonb NOT NULL`.
    - profile_attachment has 3 jsonb columns (anxious, avoidant, secure), PLUS `relational_word_count integer DEFAULT 0 NOT NULL` AND `activated boolean DEFAULT false NOT NULL` (D-07).
    - Each CREATE TABLE has `CONSTRAINT "profile_*_name_unique" UNIQUE("name")` and `CONSTRAINT "profile_*_overall_confidence_bounds" CHECK (... >= 0 AND ... <= 1)`.
    - All INSERTs include the Never-Retrofit columns with cold-start values; profile_attachment INSERT additionally has `relational_word_count=0` and `activated=false`.
    - Statement-breakpoint markers separate every statement (`--> statement-breakpoint`).
    - NO `data_consistency` column (M011 stores per-dim confidence; D-11 deferred).
    - NO references to ground-truth.ts in seed-row data (psychological profiles are inferred-only).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/db/migrations/0013_psychological_profiles.sql</automated>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS \"profile_\\(hexaco\\|schwartz\\|attachment\\)\"" src/db/migrations/0013_psychological_profiles.sql | grep -q "^3$"</automated>
    <automated>grep -c "INSERT INTO \"profile_\\(hexaco\\|schwartz\\|attachment\\)\"" src/db/migrations/0013_psychological_profiles.sql | grep -q "^3$"</automated>
    <automated>grep -c "ON CONFLICT (\"name\") DO NOTHING" src/db/migrations/0013_psychological_profiles.sql | grep -q "^3$"</automated>
    <automated>grep -c "relational_word_count\\|activated" src/db/migrations/0013_psychological_profiles.sql | xargs -I {} test {} -ge 2</automated>
    <automated>grep -v '^--' src/db/migrations/0013_psychological_profiles.sql | grep -c "schema_version integer DEFAULT 1 NOT NULL" | grep -q "^3$"</automated>
  </verify>
  <done>
    File exists at src/db/migrations/0013_psychological_profiles.sql. Contains exactly 3 CREATE TABLE statements (profile_hexaco/schwartz/attachment), 3 INSERT statements (1 per table) with ON CONFLICT (name) DO NOTHING, and statement-breakpoint markers between every statement. Never-Retrofit Checklist columns present on all 3 tables. profile_attachment has relational_word_count + activated columns. last_updated nullable (no DEFAULT). No data_consistency, no evidence_count.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Extend scripts/regen-snapshots.sh — MIGRATION_13 var + apply_sql chain + 0013→0014 sentinel bumps (5 sites)</name>
  <read_first>
    - scripts/regen-snapshots.sh (entire relevant blocks at lines 55-75, 117-155, 372-400; verify exact line numbers in current state, may have shifted ±2)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (section "Specific Research Findings §5" lines 1148-1164 — exact 5 site edits enumerated)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`scripts/regen-snapshots.sh` section — divergences #1-8 enumerated)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-36 — cleanup-flag bump 0013→0014)
  </read_first>
  <files>scripts/regen-snapshots.sh (MODIFIED at 5+ sites)</files>
  <behavior>
    - Add `MIGRATION_13="${MIGRATIONS_DIR}/0013_psychological_profiles.sql"` variable declaration after MIGRATION_12 (around line 67).
    - Append `apply_sql "${MIGRATION_13}"` after the existing `apply_sql "${MIGRATION_12}"` (around line 375).
    - Bump comment block at lines 117-122 to reference Phase 37 / 0013 → 0014 transition (preserve Phase 33 comment lineage; append a Phase 37 paragraph).
    - Bump cleanup `find -delete` references: line 143 keep `0013_acceptance_check*.sql` AND ADD `0014_acceptance_check*.sql`; line 150 update comment; line 153 change `find -name "0013_snapshot.json"` → `find -name "0014_snapshot.json"` (the committed 0013_snapshot.json is now real and must be preserved; only post-0013 acceptance artifacts get wiped).
    - Bump post-apply comment at lines 382-385 to reference 0013→0014 transition.
    - Bump post-apply cleanup at lines 397-399 to use 0014 sentinels.
    - Do NOT remove any existing 0012 references (incremental cleanup pattern).
  </behavior>
  <action>
    Insert variable declaration at scripts/regen-snapshots.sh:67 (after the existing `MIGRATION_12=` line): `MIGRATION_13="${MIGRATIONS_DIR}/0013_psychological_profiles.sql"`.

    Append Phase 37 comment paragraph after the existing Phase 33 comment block (around line 121, before `REGEN_PRODUCED_ACCEPTANCE=0` at line 122). Content: "Phase 37 extends this discipline to 0013 — committed 0013_snapshot.json must be preserved (it ships in Plan 37-01); only the post-0013 acceptance-gate artifact (0014_snapshot.json named by drizzle's sequence-counter) is wiped when this run produced it."

    At line 143 (cleanup function pre-script): append a new line after the existing `0013_acceptance_check*.sql` deletion: `find "${MIGRATIONS_DIR}" -maxdepth 1 -name "0014_acceptance_check*.sql" -delete 2>/dev/null || true`. Preserve the 0013 line (incremental pattern).

    At line 150 comment + line 152-153 cleanup guard: update comment to reference `0014_snapshot.json`. Change line 153 `find "${META_DIR}" -name "0013_snapshot.json" -delete` to `find "${META_DIR}" -name "0014_snapshot.json" -delete`. This is the critical edit — the real 0013_snapshot.json must NOT be deleted; only future 0014 acceptance artifacts.

    After `apply_sql "${MIGRATION_12}"` at line 375, append a new line: `apply_sql "${MIGRATION_13}"`.

    Update comment at lines 382-384 to read: "Mark that THIS run is responsible for any post-0013 snapshot ... 0014_snapshot.json so it never blows away a legitimate committed snapshot on a script re-run after a future plan lands a real 0014 migration. (The number after 0013 is 0014.)". Keep `REGEN_PRODUCED_ACCEPTANCE=1` at line 385 unchanged.

    Update success-path cleanup at lines 397-399 — change "number after 0012 is 0013" comment to "number after 0013 is 0014"; change `find ... -name "0013_acceptance_check*.sql" -delete` to use `0014_acceptance_check*.sql`; change `find "${META_DIR}" -name "0013_snapshot.json" -delete` to use `0014_snapshot.json`.

    CRITICAL: do NOT delete the existing 0012 references anywhere. The cleanup-flag pattern is incremental; stale-artifact safety belts from prior phases stay in place (per PATTERNS.md divergence #8).
  </action>
  <acceptance_criteria>
    - `MIGRATION_13="${MIGRATIONS_DIR}/0013_psychological_profiles.sql"` declared (around line 67-68).
    - `apply_sql "${MIGRATION_13}"` line appended after the existing `apply_sql "${MIGRATION_12}"` (around line 376).
    - Phase 37 comment paragraph present somewhere in the file referencing "Phase 37 extends this discipline to 0013".
    - At least 2 references to `0014_snapshot.json` (the new sentinel post-bump): `grep -c "0014_snapshot\.json" scripts/regen-snapshots.sh` ≥ 2.
    - At least 2 references to `0014_acceptance_check`: `grep -c "0014_acceptance_check" scripts/regen-snapshots.sh` ≥ 2.
    - Existing `0012` references PRESERVED (incremental cleanup pattern): `grep -c "MIGRATION_12\|0012" scripts/regen-snapshots.sh` ≥ 5.
    - `bash -n scripts/regen-snapshots.sh` exits 0 (syntax check).
    - `bash scripts/regen-snapshots.sh` runs to completion and prints "No schema changes" at the acceptance gate (proves schema.ts ↔ 0013_snapshot.json lock-step). This may be run as part of Task 5 verification.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^MIGRATION_13=" scripts/regen-snapshots.sh | grep -q "^1$"</automated>
    <automated>grep -c "apply_sql \"\\${MIGRATION_13}\"" scripts/regen-snapshots.sh | grep -q "^1$"</automated>
    <automated>grep -v "^#" scripts/regen-snapshots.sh | grep -c "0014_snapshot\\.json" | xargs -I {} test {} -ge 2</automated>
    <automated>grep -v "^#" scripts/regen-snapshots.sh | grep -c "0014_acceptance_check" | xargs -I {} test {} -ge 2</automated>
    <automated>bash -n scripts/regen-snapshots.sh</automated>
  </verify>
  <done>
    scripts/regen-snapshots.sh has MIGRATION_13 variable declaration, apply_sql chain extension, 0013→0014 sentinel bumps at 5 sites, Phase 37 comment paragraph appended. bash -n syntax check passes. All 0012 references preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Regenerate drizzle meta — 0013_snapshot.json + _journal.json idx-13 entry</name>
  <read_first>
    - scripts/regen-snapshots.sh (entire file — understand the acceptance-gate flow)
    - src/db/migrations/meta/_journal.json (verify current state: 13 entries, last idx=12 when=1778482284254)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (section "Specific Research Findings §3" lines 1103-1127 — _journal.json format + monotonic-when requirement)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`src/db/migrations/meta/_journal.json` section — entry format)
  </read_first>
  <files>src/db/migrations/meta/0013_snapshot.json (NEW — drizzle-kit emitted), src/db/migrations/meta/_journal.json (MODIFIED — append idx 13 entry)</files>
  <behavior>
    - Run `bash scripts/regen-snapshots.sh` (with the Task-4 edits in place) — drizzle-kit applies migrations 0000-0013 against fresh Docker postgres and emits 0013_snapshot.json from schema.ts.
    - drizzle-kit also auto-appends the idx-13 entry to _journal.json with `when` set to current timestamp (strictly greater than 1778482284254 = 2026-05-13 04:11:24 UTC).
    - Acceptance gate at the end of regen-snapshots.sh runs `drizzle-kit generate` against the post-0013 state and asserts "No schema changes" — proves schema.ts ↔ 0013_snapshot.json are lock-step (Pitfall 5 mitigation).
    - Pitfall 5 (stale _journal.json `when` timestamp) avoided automatically by drizzle-kit using Date.now().
  </behavior>
  <action>
    Ensure Docker Compose stack is running (`docker compose -f docker-compose.local.yml up -d postgres` if not already) — regen-snapshots.sh uses port 5434 by default.

    Run `bash scripts/regen-snapshots.sh`. Expected behavior: script applies all 14 migrations (0000-0013) in sequence; drizzle-kit emits a new `src/db/migrations/meta/0013_snapshot.json` matching the schema.ts post-Task-2 state; drizzle-kit appends a new entry `{ idx: 13, version: "7", when: <Date.now() ms>, tag: "0013_psychological_profiles", breakpoints: true }` to `_journal.json`; the acceptance gate at script end prints "No schema changes" confirming lock-step.

    If the script reports "schema changes detected" at the acceptance gate, the schema.ts edits do not match the migration SQL — diff `0013_psychological_profiles.sql` against the auto-generated acceptance SQL and fix the mismatch in schema.ts (NOT in the migration — migration is hand-authored). Common causes per Pitfall 4: missing .notNull() on a jsonb column, missing default value, dim column type mismatch.

    Verify the new _journal.json entry: `cat src/db/migrations/meta/_journal.json | python3 -c "import sys, json; d=json.load(sys.stdin); print(d['entries'][-1])"` — must show `idx=13`, `tag='0013_psychological_profiles'`, `when` strictly greater than `1778482284254`.

    Run `npx tsx scripts/validate-journal-monotonic.ts` to confirm Phase 32 #3 monotonic-when guardrail accepts the new entry.

    Do NOT hand-author 0013_snapshot.json — its byte layout is drizzle-kit-specific and hand-edits break lineage validation. If regen-snapshots.sh fails, the planner-checker must diagnose schema.ts; do NOT manually patch 0013_snapshot.json.
  </action>
  <acceptance_criteria>
    - File `src/db/migrations/meta/0013_snapshot.json` exists (drizzle-kit emitted).
    - `_journal.json` has 14 entries (was 13 before this task); last entry has `idx: 13`, `tag: "0013_psychological_profiles"`, `version: "7"`, `breakpoints: true`, and `when > 1778482284254` (the prior 0012 timestamp).
    - `npx tsx scripts/validate-journal-monotonic.ts` exits 0 (Phase 32 #3 guardrail accepts the new entry).
    - `bash scripts/regen-snapshots.sh` prints "No schema changes" at the acceptance gate (schema.ts post-Task-2 matches 0013_snapshot.json byte-for-byte).
    - `0013_snapshot.json` contains the strings `profile_hexaco`, `profile_schwartz`, `profile_attachment` (drizzle-kit serialized table definitions).
    - 0013_snapshot.json `prevId` field chains correctly from 0012_snapshot.json's id (drizzle lineage validation).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/db/migrations/meta/0013_snapshot.json</automated>
    <automated>python3 -c "import json; d=json.load(open('src/db/migrations/meta/_journal.json')); last=d['entries'][-1]; assert last['idx']==13, f'idx is {last[\"idx\"]}'; assert last['tag']=='0013_psychological_profiles', f'tag is {last[\"tag\"]}'; assert last['when']>1778482284254, f'when {last[\"when\"]} not monotonic'; print('✓ journal entry valid')"</automated>
    <automated>npx tsx scripts/validate-journal-monotonic.ts</automated>
    <automated>python3 -c "import json; s=json.load(open('src/db/migrations/meta/0013_snapshot.json')); tables=s.get('tables',{}); names=[t for t in tables]; assert 'profile_hexaco' in str(names) or any('profile_hexaco' in n for n in names), f'profile_hexaco not in snapshot: {names}'; print('✓ snapshot contains profile_hexaco')"</automated>
  </verify>
  <done>
    src/db/migrations/meta/0013_snapshot.json exists (drizzle-kit-emitted). _journal.json appended idx-13 entry with tag='0013_psychological_profiles' and monotonic `when`. validate-journal-monotonic.ts exits 0. bash scripts/regen-snapshots.sh acceptance gate prints "No schema changes" (lock-step confirmed).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Extend scripts/test.sh — MIGRATION_13_SQL apply line + migration-0013 smoke gate (3 tables + 3 seed rows + Never-Retrofit column counts)</name>
  <read_first>
    - scripts/test.sh (lines 1-100 — variable declarations + migration apply chain; lines 218-265 — M010 smoke gate as the analog)
    - .planning/phases/37-psychological-substrate/37-RESEARCH.md (section "Specific Research Findings §4" lines 1129-1147 — exact insertion points; "Open Questions §3-4" lines 1360-1369 — smoke-gate strategy + column count)
    - .planning/phases/37-psychological-substrate/37-PATTERNS.md (`scripts/test.sh` section — full smoke gate template for Phase 37)
    - .planning/phases/37-psychological-substrate/37-CONTEXT.md (D-35 — append after 0012 line; D-06+D-07 — Never-Retrofit columns to assert)
  </read_first>
  <files>scripts/test.sh (MODIFIED — variable + apply line + new smoke gate block)</files>
  <behavior>
    - MIGRATION_13_SQL variable added at line 21 (after MIGRATION_12_SQL declaration at line 20).
    - psql apply block appended immediately after the existing 0012 apply at line 83 (sequential order mandatory per D-35).
    - NEW migration-0013 smoke gate block appended AFTER the existing 0012 smoke gate (after line ~265) — does NOT replace or merge with 0012 gate per RESEARCH.md OQ-3 recommendation.
    - Smoke gate asserts: (a) 3 new tables exist, (b) each has exactly 1 sentinel row with name='primary', (c) all 3 have overall_confidence=0, word_count=0, word_count_at_last_run=0, substrate_hash='', schema_version=1, (d) profile_attachment additionally has relational_word_count=0 and activated=false, (e) profile_history row count unchanged (no Phase 37 history writes), (f) Never-Retrofit column count = 18 (3 tables × 6 cols: schema_version, substrate_hash, name, overall_confidence, word_count, word_count_at_last_run), (g) profile_attachment-specific column count = 2 (relational_word_count, activated).
  </behavior>
  <action>
    Insert `MIGRATION_13_SQL="src/db/migrations/0013_psychological_profiles.sql"` at scripts/test.sh:21 (immediately after the existing line 20 `MIGRATION_12_SQL=...`).

    Insert psql apply block at scripts/test.sh:84 (immediately after the existing line 83 `psql ... < $MIGRATION_12_SQL` line). The new block mirrors the 0012 pattern: `docker compose -f "$COMPOSE_FILE" exec -T postgres psql -U chris -d chris -v ON_ERROR_STOP=1 -q < "$MIGRATION_13_SQL"`.

    Append a new smoke gate block after the existing 0012 smoke gate (after the current line ~265). Begin with header comment: `# Phase 37 (M011 v2.6) — psychological profiles substrate smoke gate. HARD CO-LOCATION #M11-1: this gate ships in the SAME plan as the migration SQL + drizzle meta snapshot + schema.ts table defs (Plan 37-01).`

    Smoke gate body: `echo "🔍 Verifying migration 0013 substrate..."`, then a psql `-tAq -c` query selecting (in this order with `|` as delimiter): `(SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('profile_hexaco', 'profile_schwartz', 'profile_attachment'))` AS table_count, `(SELECT COUNT(*) FROM profile_hexaco WHERE name = 'primary')` AS hex_seed, same for schwartz + attachment, `(SELECT overall_confidence::text FROM profile_hexaco WHERE name = 'primary')` AS hex_conf, `(SELECT word_count::text FROM profile_hexaco WHERE name = 'primary')` AS hex_wc, `(SELECT relational_word_count::text FROM profile_attachment WHERE name = 'primary')` AS att_rwc, `(SELECT activated::text FROM profile_attachment WHERE name = 'primary')` AS att_act. Tee output to /tmp/m011_smoke.txt. Then `grep -q "^3|1|1|1|0|0|0|f$" /tmp/m011_smoke.txt || { echo "❌ Migration 0013 substrate incomplete or seed values wrong"; cat /tmp/m011_smoke.txt; exit 1; }`. Final `echo "✓ Migration 0013 substrate verified (3 tables + 3 seed rows + cold-start values)"`.

    Append a Never-Retrofit column-count smoke assertion immediately after the substrate gate: `echo "🔍 Verifying migration 0013 non-retrofittable columns..."`, then psql `SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = 'public' AND table_name IN ('profile_hexaco', 'profile_schwartz', 'profile_attachment') AND column_name IN ('schema_version', 'substrate_hash', 'name', 'overall_confidence', 'word_count', 'word_count_at_last_run');` — expect `^18$` (3 tables × 6 cols). Tee to /tmp/m011_cols.txt; grep-fail on mismatch with diagnostic message including the actual count.

    Append a profile_attachment-specific column-count assertion: psql `SELECT COUNT(*) FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'profile_attachment' AND column_name IN ('relational_word_count', 'activated');` — expect `^2$`. Grep-fail on mismatch.

    Do NOT modify the existing 0012 smoke gate at lines 218-265 — keep regression isolation per RESEARCH.md OQ-3 recommendation.
  </action>
  <acceptance_criteria>
    - `MIGRATION_13_SQL="src/db/migrations/0013_psychological_profiles.sql"` declared at line 21 (immediately after the existing line 20 MIGRATION_12_SQL).
    - Exactly 1 new psql apply line for MIGRATION_13_SQL appended after the existing line 83 (0012 apply).
    - New smoke gate block appended AFTER the existing 0012 smoke gate (after current line ~265); does NOT modify the 0012 gate.
    - Smoke gate asserts: `^3|1|1|1|0|0|0|f$` for table_count + 3 seed counts + hex_conf + hex_wc + att_rwc + att_act.
    - Never-Retrofit column-count assertion: `^18$` for the 6 retrofit-protected cols × 3 tables = 18 (schema_version, substrate_hash, name, overall_confidence, word_count, word_count_at_last_run).
    - profile_attachment-specific column-count assertion: `^2$` for relational_word_count + activated.
    - `bash -n scripts/test.sh` exits 0 (syntax check).
    - `bash scripts/test.sh` on fresh Docker postgres exits 0 with both "✓ Migration 0013 substrate verified" AND "✓ Migration 0013 non-retrofittable columns" echoes in the log.
    - The existing 0012 smoke gate at lines 218-265 is UNCHANGED (regression isolation per RESEARCH.md OQ-3).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^MIGRATION_13_SQL=" scripts/test.sh | grep -q "^1$"</automated>
    <automated>grep -c "MIGRATION_13_SQL" scripts/test.sh | xargs -I {} test {} -ge 2</automated>
    <automated>bash -n scripts/test.sh</automated>
    <automated>bash scripts/test.sh 2>&amp;1 | tee /tmp/test-sh.log; grep -q "✓ Migration 0013 substrate verified" /tmp/test-sh.log</automated>
    <automated>grep -q "✓ Migration 0013 non-retrofittable columns" /tmp/test-sh.log || { echo "Never-Retrofit col-count gate missing"; exit 1; }</automated>
  </verify>
  <done>
    scripts/test.sh has MIGRATION_13_SQL variable, psql apply line after MIGRATION_12_SQL apply, new migration-0013 smoke gate appended after the M010 0012 gate. bash scripts/test.sh on fresh Docker postgres exits 0 with both Phase 37 gate echo messages. The 18-column Never-Retrofit assertion passes; the 2-column attachment-specific assertion passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration-0013 → live Postgres | Hand-authored SQL applied with `ON_ERROR_STOP=1`. Inputs are literal values only; no parameter substitution surface. Trust boundary is the migration file's CI-reviewed contents. |
| schema.ts → psychological-schemas.ts | Type-only import boundary; runtime data never crosses this edge (Drizzle `.$type<T>()` is compile-time only). |
| drizzle-kit → _journal.json | drizzle-kit emits idx-13 entry with `when: Date.now()`. Trust boundary is `validate-journal-monotonic.ts` (Phase 32 #3 guardrail) — rejects any non-monotonic `when` BEFORE migrations apply. |
| scripts/regen-snapshots.sh → meta/*.json | Script regenerates meta artifacts; trust boundary is the acceptance-gate "No schema changes" output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | Migration 0013 SQL | mitigate | All literal values; no string concatenation. `ON_ERROR_STOP=1` halts on any unexpected statement. CREATE TABLE IF NOT EXISTS guards prevent destruction on re-apply (M010 0012 precedent verified). |
| T-37-02 | Tampering | `regen-snapshots.sh` cleanup flag | mitigate | 0013→0014 sentinel bump preserves the committed 0013_snapshot.json; without the bump, a future `regen-snapshots.sh` run would `find -delete` the real 0013_snapshot.json. RESEARCH.md A3 flags this as the highest-risk informational gap; mitigation is the 5-site bump in Task 4 + verification via bash -n + the acceptance-gate "No schema changes" output. |
| T-37-03 | Repudiation | `_journal.json` monotonic `when` | mitigate | `validate-journal-monotonic.ts` (Phase 32 #3) runs BEFORE migrations apply in scripts/test.sh; rejects any stale `when` value. Task 5 verifies the new entry passes the guardrail. |
| T-37-04 | Information Disclosure | psychological-schemas.ts Zod schemas | accept | Schemas describe data shape, not values. No PII or secrets in this file. Public-API surface intentional (Phase 38 generators + Phase 37 reader will import these schemas). |
| T-37-05 | Denial of Service | Migration 0013 raw-psql apply path | accept | Single user (Greg); single migration; idempotent CREATE TABLE IF NOT EXISTS. Postgres 16 limits enforce per-row constraint. No exhaustion attack vector. |
| T-37-06 | Elevation of Privilege | Sentinel-row UNIQUE constraint | mitigate | `UNIQUE("name")` + `ON CONFLICT ("name") DO NOTHING` guards against duplicate-sentinel-row insertion. Application convention (1 row per profile table) enforced at DB level. |
| T-37-07 | Tampering | Pitfall 4 (Drizzle nullable jsonb confusion) | mitigate | Each jsonb dim column typed `.$type<HexacoProfileData['<field>']>()` where the Zod factory is `.nullable()` — TypeScript correctly infers `HexacoDimension | null`. Cold-start `.score` access on null dimension is a compile-error (forces consumer to narrow). |
| T-37-08 | Tampering | Pitfall 1 (Never-Retrofit incompleteness) | mitigate | All 7 non-retrofittable columns ship in 0013 per D-06; +2 attachment-only per D-07. test.sh column-count smoke assertion (Task 6) verifies 18 + 2 columns present. ALTER TABLE in any future 0014+ migration adding these columns would be a regression caught by the smoke gate failing in the future. |
</threat_model>

<verification>
After all 6 tasks complete:

1. **Migration applies cleanly:** `bash scripts/test.sh` (fresh Docker postgres) exits 0 with "✓ Migration 0013 substrate verified" + "✓ Migration 0013 non-retrofittable columns" echoes present in the log.
2. **TypeScript clean:** `npx tsc --noEmit` exits 0 — no errors in schema.ts or psychological-schemas.ts.
3. **Drizzle lineage clean:** `bash scripts/regen-snapshots.sh` exits 0 and prints "No schema changes" at the acceptance gate.
4. **Journal monotonic:** `npx tsx scripts/validate-journal-monotonic.ts` exits 0.
5. **Phase 33 pattern parity:** The 7 artifacts of HARD CO-LOC #M11-1 are all present in this plan's git diff:
   - `src/memory/profiles/psychological-schemas.ts` (NEW)
   - `src/db/schema.ts` (3 new pgTable exports + appended type imports)
   - `src/db/migrations/0013_psychological_profiles.sql` (NEW)
   - `src/db/migrations/meta/0013_snapshot.json` (NEW, drizzle-kit emitted)
   - `src/db/migrations/meta/_journal.json` (1 new entry)
   - `scripts/test.sh` (variable + apply line + smoke gate)
   - `scripts/regen-snapshots.sh` (variable + apply line + 5-site sentinel bump)
6. **Pitfall sweep:** Pitfalls 1 (Never-Retrofit incompleteness), 4 (Drizzle nullable jsonb), 5 (stale _journal `when`) all mitigated by the artifacts above. Pitfalls 2, 3, 6, 7 are Plan 37-02's responsibility (substrate loader + reader + boundary audit).
</verification>

<success_criteria>
Plan 37-01 is complete when:
- [ ] `bash scripts/test.sh` exits 0 on fresh Docker postgres with both Phase 37 smoke-gate echoes present
- [ ] `npx tsc --noEmit` exits 0
- [ ] `bash scripts/regen-snapshots.sh` exits 0 with "No schema changes" at the acceptance gate
- [ ] `npx tsx scripts/validate-journal-monotonic.ts` exits 0
- [ ] PSCH-01 satisfied: 3 tables created with full Never-Retrofit Checklist (18 base cols + 2 attachment-only)
- [ ] PSCH-02 satisfied: profile_hexaco has 6 jsonb dim cols typed via .$type<HexacoProfileData[...]>()
- [ ] PSCH-03 satisfied: profile_schwartz has 10 jsonb value cols
- [ ] PSCH-04 satisfied: profile_attachment has 3 jsonb dim cols + relational_word_count + activated (schema-only; population deferred to D028)
- [ ] PSCH-05 satisfied: 3 cold-start seed rows with overall_confidence=0, word_count=0, all dims NULL via 'null'::jsonb default
- [ ] PSCH-06 satisfied: 3 v3 + 3 v4 Zod schemas exported from psychological-schemas.ts
- [ ] HARD CO-LOC #M11-1 satisfied: all 7 artifacts in the same plan's git commit
</success_criteria>

<output>
After completion, create `.planning/phases/37-psychological-substrate/37-01-SUMMARY.md` documenting:
- 7 artifacts shipped atomically
- Smoke gate output (snippet of "✓ Migration 0013 substrate verified" + column-count assertions)
- regen-snapshots.sh acceptance-gate output (No schema changes)
- _journal.json idx-13 entry `when` value used
- Any deviations from CONTEXT.md decisions (with rationale)
- Status of all 4 Pitfalls 1/4/5 mitigations
- Handoff note to Plan 37-02: psychological-schemas.ts is now importable; PsychologicalProfileType type is NOT yet exported (lives in Plan 37-02's psychological-shared.ts)
</output>
