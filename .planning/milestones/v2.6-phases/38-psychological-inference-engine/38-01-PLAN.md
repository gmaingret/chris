---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/psychological-profile-prompt.ts
  - src/memory/__tests__/psychological-profile-prompt.test.ts
autonomous: true
requirements: [PGEN-01]
requirements_addressed: [PGEN-01]
tags: [psychological-profile, prompt-builder, m011, hexaco, schwartz, hard-rule-d027]
must_haves:
  truths:
    - "assemblePsychologicalProfilePrompt(profileType, substrate, prevState, wordCount) is a pure function — zero DB, LLM, fs, env access (D-03)"
    - "Output for both profileTypes is { system: string, user: string }; system.startsWith('## Core Principles (Always Active)') — CONSTITUTIONAL_PREAMBLE first section verbatim (D-05)"
    - "DO_NOT_INFER_DIRECTIVE imported from ./profile-prompt.js (M010) by named import — NOT redeclared — one source of truth (D-06)"
    - "PSYCHOLOGICAL_HARD_RULE_EXTENSION exported constant; exact verbatim 8-line phrasing from D-07 (Pitfall 1 — D027 trait-authority sycophancy mitigation point #1; Phase 39 PSURF-02 will reuse this same constant by import)"
    - "Per-profileType directive present in assembled system: '## Profile Focus — HEXACO' for hexaco; '## Profile Focus — Schwartz' for schwartz; both include 'r ≈ .31–.41' empirical-limits framing (D-10)"
    - "Word-count framing instructs Sonnet to emit `data_consistency` (0-1) AND `overall_confidence` (0-1); explicitly tells Sonnet 'do NOT emit a `confidence` field directly' (D-08)"
    - "When prevState is null → system does NOT contain '## CURRENT PROFILE STATE'; when prevState is non-null → system DOES contain '## CURRENT PROFILE STATE' rendering the snapshot as JSON.stringify(prevState, null, 2) (D-09)"
    - "user prompt = 'Generate the HEXACO psychological profile for Greg.' for hexaco / 'Generate the SCHWARTZ psychological profile for Greg.' for schwartz (D-03)"
    - "PsychologicalProfilePromptType = 'hexaco' | 'schwartz' — attachment deferred per D-23 (NARROWER than Phase 37's PsychologicalProfileType union)"
    - "Pure function: same inputs → same outputs across repeated calls (deterministic) — structural test covers this"
    - "psych-boundary-audit.test.ts (PSCH-10) stays green — no operational tokens (jurisdictional|capital|health|family) appear anywhere in this file or its test (Pitfall 3)"
  artifacts:
    - path: "src/memory/psychological-profile-prompt.ts"
      provides: "assemblePsychologicalProfilePrompt + PSYCHOLOGICAL_HARD_RULE_EXTENSION + PsychologicalProfilePromptType + AssembledPsychologicalProfilePrompt + PsychologicalProfileSubstrateView + section helpers (private)"
      min_lines: 180
      contains: "export function assemblePsychologicalProfilePrompt"
    - path: "src/memory/__tests__/psychological-profile-prompt.test.ts"
      provides: "Structural unit tests parametrized over both profile types — CONSTITUTIONAL_PREAMBLE first, DO_NOT_INFER, Hard Rule extension, per-profileType directive, prevState conditional, word-count framing"
      min_lines: 120
      contains: "describe('assemblePsychologicalProfilePrompt"
  key_links:
    - from: "src/memory/psychological-profile-prompt.ts"
      to: "src/chris/personality.ts"
      via: "imports CONSTITUTIONAL_PREAMBLE re-used as first section of system string (D-05)"
      pattern: "import.*CONSTITUTIONAL_PREAMBLE.*from.*chris/personality"
    - from: "src/memory/psychological-profile-prompt.ts"
      to: "src/memory/profile-prompt.ts"
      via: "named import DO_NOT_INFER_DIRECTIVE — one source of truth across M010 + M011 (D-06)"
      pattern: "import \\{ DO_NOT_INFER_DIRECTIVE \\} from"
    - from: "src/memory/__tests__/psychological-profile-prompt.test.ts"
      to: "src/memory/psychological-profile-prompt.ts"
      via: "imports assemblePsychologicalProfilePrompt + PSYCHOLOGICAL_HARD_RULE_EXTENSION for structural assertions"
      pattern: "from '../psychological-profile-prompt"
---

<objective>
Ship the shared prompt builder `assemblePsychologicalProfilePrompt(profileType, substrate, prevState, wordCount)` + its structural test before any psychological generator exists. This plan is the HARD CO-LOC #M11-2 anchor: Plan 38-02 cannot ship until this plan is complete, because both HEXACO and Schwartz generators import this builder. The shared builder prevents per-profileType prompt drift (M010-06 lesson applied to M011) by being THE ONLY way the two generators construct their Sonnet system text.

Purpose: PGEN-01 — `assemblePsychologicalProfilePrompt` shared builder forked from M010 `assembleProfilePrompt` (cannot extend `ProfilePromptDimension` union without breaking exhaustive switches at `src/memory/profile-prompt.ts:51,124,363`). Prompt body locks the Hard Rule D027 extension inline (Pitfall 1 — trait-authority sycophancy mitigation point #1; Phase 39 PSURF-02 will reuse the same constant at the injection-block layer for mitigation point #2).

Output: 1 new source file (`src/memory/psychological-profile-prompt.ts` — file location at sibling level to M010's `src/memory/profile-prompt.ts`, NOT under `profiles/`, per D-04) + 1 new test file (`src/memory/__tests__/psychological-profile-prompt.test.ts`). Zero schema edits, zero migration edits, zero changes outside the prompt module.

Satisfies: PGEN-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-psychological-inference-engine/38-CONTEXT.md
@.planning/phases/38-psychological-inference-engine/38-RESEARCH.md
@.planning/phases/38-psychological-inference-engine/38-PATTERNS.md
@.planning/phases/38-psychological-inference-engine/38-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/37-psychological-substrate/37-01-SUMMARY.md
@.planning/phases/37-psychological-substrate/37-02-SUMMARY.md

# Direct analog from M010 Phase 34 (mirror with locked divergences D-03..D-10)
@.planning/milestones/v2.5-phases/34-inference-engine/34-01-PLAN.md
@.planning/milestones/v2.5-phases/34-inference-engine/34-01-SUMMARY.md

# Codebase analogs (Pattern Catalog in 38-PATTERNS.md §1 + 38-RESEARCH.md §"Plan 38-01")
@src/memory/profile-prompt.ts
@src/memory/__tests__/profile-prompt.test.ts
@src/chris/personality.ts
@src/memory/profiles/psychological-shared.ts
@src/memory/profiles/__tests__/psych-boundary-audit.test.ts

<interfaces>
<!-- Contracts the executor consumes. All come from existing codebase. -->

From src/chris/personality.ts:
```typescript
export const CONSTITUTIONAL_PREAMBLE: string;  // M006 constitutional preamble; first section of every prompt
```

From src/memory/profile-prompt.ts:
```typescript
// Lines 107-110 — IMPORTED VERBATIM per D-06. NOT redeclared in psych file.
export const DO_NOT_INFER_DIRECTIVE: string;
```

From src/memory/profiles/psychological-shared.ts (Phase 37):
```typescript
// Line 64 — the Phase 37 union (3 types); Phase 38 narrows to 2 per D-23
export type PsychologicalProfileType = 'hexaco' | 'schwartz' | 'attachment';

// Lines 81-93 — the substrate discriminated union returned by loadPsychologicalSubstrate.
// Plan 38-01's PsychologicalProfileSubstrateView is the READONLY STRUCTURAL VIEW
// of the above-threshold branch (belowThreshold: false). The prompt builder receives
// the structural view, not the full discriminated union (generator narrows first).
export type PsychologicalSubstrate<T> =
  | { belowThreshold: true; wordCount: number; neededWords: number }
  | { belowThreshold: false; corpus: PensieveRow[]; episodicSummaries: EpisodicRow[]; wordCount: number; prevHistorySnapshot: T | null };
```

NEW contracts THIS PLAN exports (Plan 38-02 consumers):
```typescript
// src/memory/psychological-profile-prompt.ts
export type PsychologicalProfilePromptType = 'hexaco' | 'schwartz';
export type AssembledPsychologicalProfilePrompt = { system: string; user: string };
export type PsychologicalProfileSubstrateView = {
  corpus: ReadonlyArray<{ id: string; epistemicTag: string | null; content: string; createdAt: Date }>;
  episodicSummaries: ReadonlyArray<{ summaryDate: string; summary: string }>;
  wordCount: number;
};
export const PSYCHOLOGICAL_HARD_RULE_EXTENSION: string;
export function assemblePsychologicalProfilePrompt(
  profileType: PsychologicalProfilePromptType,
  substrate: PsychologicalProfileSubstrateView,
  prevState: unknown | null,
  wordCount: number,
): AssembledPsychologicalProfilePrompt;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author psychological-profile-prompt.ts with 9-section assembler</name>
  <files>src/memory/psychological-profile-prompt.ts</files>
  <read_first>
    - src/memory/profile-prompt.ts (full 377 lines — fork target; section ordering at lines 175-223 is the literal mirror with substitutions)
    - src/chris/personality.ts (CONSTITUTIONAL_PREAMBLE export point — first section of system; D-05)
    - src/memory/profiles/psychological-shared.ts:1-110 (PsychologicalProfileType union + PsychologicalSubstrate discriminated-union shape — type-only references)
    - .planning/phases/38-psychological-inference-engine/38-CONTEXT.md (D-03..D-10, D-23 — locked decisions)
    - .planning/phases/38-psychological-inference-engine/38-PATTERNS.md §1 ("src/memory/psychological-profile-prompt.ts" — full pattern map with 8 divergences enumerated)
    - .planning/phases/38-psychological-inference-engine/38-RESEARCH.md §"Plan 38-01: Shared Prompt Builder — File Skeleton" (verbatim file skeleton lines 422-533 of 38-RESEARCH.md)
    - .planning/research/PITFALLS.md (Pitfall 1 D027 sycophancy — Hard Rule extension is mitigation point #1)
    - src/memory/profiles/__tests__/psych-boundary-audit.test.ts (Pitfall 3 — new file MUST pass this audit: zero operational tokens `jurisdictional|capital|health|family` anywhere including comments)
    - .planning/milestones/v2.5-phases/34-inference-engine/34-01-SUMMARY.md (direct analog summary)
  </read_first>
  <action>
    Create `src/memory/psychological-profile-prompt.ts` at the sibling level of `src/memory/profile-prompt.ts` (NOT under `profiles/`) per D-04. File structure:

    **Imports (top of file):**
    - `import { CONSTITUTIONAL_PREAMBLE } from '../chris/personality.js';` (D-05)
    - `import { DO_NOT_INFER_DIRECTIVE } from './profile-prompt.js';` — NAMED import only; do NOT use `import *` (Pitfall 3 mitigation per D-06)
    - Type-only imports as needed from `./profiles/psychological-shared.js`

    **Public types (exports):**
    - `export type PsychologicalProfilePromptType = 'hexaco' | 'schwartz';` (D-03 + D-23 — narrower than Phase 37 union; attachment deferred to v2.6.1 / ATT-POP-01)
    - `export type AssembledPsychologicalProfilePrompt = { system: string; user: string };`
    - `export type PsychologicalProfileSubstrateView` — readonly structural shape with three fields: `corpus: ReadonlyArray<{id: string; epistemicTag: string | null; content: string; createdAt: Date}>`, `episodicSummaries: ReadonlyArray<{summaryDate: string; summary: string}>`, `wordCount: number`. Note `epistemicTag` is nullable (PSCH-07 corpus filter excludes RITUAL_RESPONSE but other tags including null pass through).

    **Public constant `PSYCHOLOGICAL_HARD_RULE_EXTENSION`** (D-07 — phrasing locked VERBATIM):
    Define as exported const string by joining with `'\n'` the following 9 lines IN ORDER:
    1. `## Psychological Profile Framing (D027 extension — REQUIRED)`
    2. `` (empty string)
    3. `These trait scores describe statistical tendencies inferred from speech patterns,`
    4. `NOT facts about who Greg is. You MUST NOT:`
    5. `- Use these scores to tell Greg he is "the kind of person who..."`
    6. `- Appeal to his trait scores as evidence that his current reasoning is correct`
    7. `- Construct advice that validates his existing position by citing his personality`
    8. `The Hard Rule (D027) applies here with additional force: psychological traits are`
    9. `not evidence. Evaluate every claim on its merits regardless of what the profile says.`

    JSDoc above the constant: note "Pitfall 1 mitigation point #1 — D027 trait-authority sycophancy. Phase 39 PSURF-02 will re-export and re-inject this constant at the surface-level (mitigation point #2). Both points are required; absence of either fails the Hard Rule contract."

    **Private constant `PROFILE_TYPE_DIRECTIVES: Record<PsychologicalProfilePromptType, string>`** (D-10):
    - `hexaco` directive (join with `'\n'`): heading `## Profile Focus — HEXACO Big-Six Personality`, body emphasizes "infer Greg's HEXACO Big-Six scores across all 6 dimensions (honesty-humility, emotionality, extraversion, agreeableness, conscientiousness, openness)", explicit "one theoretical framework; coherent inference requires considering all 6 together, not independently", and verbatim empirical-limits framing: `speech-based personality inference accuracy bound is r ≈ .31–.41; confidence should reflect this ceiling, not project precision the substrate cannot support.`
    - `schwartz` directive: heading `## Profile Focus — Schwartz Universal Values`, body emphasizes "infer Greg's Schwartz value priorities across all 10 universal values (self-direction, stimulation, hedonism, achievement, power, security, conformity, tradition, benevolence, universalism)", explicit "10 values are arranged in a circular motivational continuum; coherent inference acknowledges opposing-value tradeoffs (e.g. Self-Direction ↔ Conformity)", and verbatim empirical-limits framing including `r ≈ .31–.41`.

    **Public function `assemblePsychologicalProfilePrompt(profileType, substrate, prevState, wordCount): AssembledPsychologicalProfilePrompt`** — pure function, zero side effects. Body builds `sections: string[]` in the following EXACT ORDER (mirror M010 `assembleProfilePrompt` at `src/memory/profile-prompt.ts:175-223`), then returns `{ system: sections.join('\n\n'), user: <user string> }`:

    1. `sections.push(CONSTITUTIONAL_PREAMBLE.trimEnd());` (D-05)
    2. `sections.push(buildRolePreamble(profileType));` (psychological-trait-inference framing — separate from M010's operational fact-extraction framing)
    3. `sections.push(DO_NOT_INFER_DIRECTIVE);` (D-06 — imported)
    4. `sections.push(PSYCHOLOGICAL_HARD_RULE_EXTENSION);` (D-07 — INLINE Hard Rule extension; Pitfall 1 mitigation #1)
    5. `sections.push(buildWordCountFraming(wordCount));` (D-08 — replaces M010 volume-weight ceiling; tells Sonnet to emit `data_consistency` AND `overall_confidence` and to NOT emit a separate `confidence` field at top level)
    6. `if (prevState !== null) sections.push(buildPreviousStateBlock(prevState));` (D-09 — conditional)
    7. `sections.push(PROFILE_TYPE_DIRECTIVES[profileType]);` (D-10)
    8. `sections.push(buildSubstrateBlock(substrate));` (corpus + episodic + wordCount only — NO decisions array per Phase 37's substrate shape)
    9. `sections.push(buildStructuredOutputDirective(profileType));` (last; names `HexacoProfileSchemaV4Sdk` / `SchwartzProfileSchemaV4Sdk` per RESEARCH Finding 1; lists required top-level fields including `data_consistency` and `overall_confidence`)

    Return `{ system: sections.join('\n\n'), user: \`Generate the ${profileType.toUpperCase()} psychological profile for Greg.\` }`.

    **Private section builders** (mirror M010 `profile-prompt.ts:227-377` shape; all return strings):

    - `buildRolePreamble(profileType: PsychologicalProfilePromptType): string` — psychological-trait-inference framing. Lead line: `## Your Role`. Body explains: "You are inferring stable trait-level dispositions from a corpus of Greg's first-person speech. Trait inference is pattern aggregation across many statements — never quotation extraction from any single one." For hexaco: mention HEXACO 6-dim framework; for schwartz: mention Schwartz 10-value circumplex. (Both still appear in the dedicated per-profileType block at section 7; section 2's role preamble is the higher-level framing.)

    - `buildWordCountFraming(wordCount: number): string` (D-08 — REPLACES M010's `buildVolumeWeightCeilingDirective`). Heading `## Volume & Confidence Reporting`. Body: word count is communicated explicitly (e.g., "The substrate contains {wordCount} words of Greg's first-person Telegram speech from the previous calendar month."). Then verbatim phrasing: "Report `data_consistency` in the 0.0–1.0 range as a combined volume + cross-month consistency signal. Report `overall_confidence` in the 0.0–1.0 range as your top-level confidence in the profile inference. The host code reads `data_consistency` and `overall_confidence` directly; do NOT emit a `confidence` field at the top level (per-dimension `confidence` values within each dimension object are still required)."

    - `buildPreviousStateBlock(prevState: unknown): string` (D-09 — adjusted phrasing for slow-moving-trait domain). Heading `## CURRENT PROFILE STATE`. Body: "The current profile snapshot (from the most recent successful fire) is shown below as JSON. Use this as the baseline.", then `JSON.stringify(prevState, null, 2)`, then "Update discipline: trait scores are slow-moving by definition. Update a high-confidence dimension score ONLY when substantial cross-month behavioral evidence justifies the change. When the substrate does not provide new evidence on a dimension, carry the prior score forward with adjusted `last_updated`."

    - `buildSubstrateBlock(substrate: PsychologicalProfileSubstrateView): string` (adapted from M010 `profile-prompt.ts:305-349` — DROP decisions section). Heading `## Substrate`. Render: a count + summary of `corpus` (Pensieve rows: `${createdAt.toISOString().slice(0, 10)} [${epistemicTag ?? 'untagged'}] ${truncated content}`) and a count + summary of `episodicSummaries`. Last line: `Total word count: ${substrate.wordCount}`.

    - `buildStructuredOutputDirective(profileType: PsychologicalProfilePromptType): string`. Heading `## Structured Output Contract`. Body names the SDK schema (`HexacoProfileSchemaV4Sdk` / `SchwartzProfileSchemaV4Sdk` — Plan 38-02 will define these by extending Phase 37's v4 schemas with top-level `data_consistency` + `overall_confidence` per RESEARCH Finding 1). Explicitly lists required top-level fields: per-dimension objects + `data_consistency` (number 0-1) + `overall_confidence` (number 0-1).

    **Anti-shallow-execution requirements:**
    - Do NOT introduce ANY operational token (`jurisdictional`, `capital`, `health`, `family`) anywhere in this file or its accompanying test, including comments. The `psych-boundary-audit.test.ts` audit fails the build otherwise (Pitfall 3).
    - Do NOT use `import *` from `./profile-prompt.js`. ONLY named imports — `DO_NOT_INFER_DIRECTIVE` is the only symbol imported from M010's prompt module.
    - Do NOT redeclare `DO_NOT_INFER_DIRECTIVE` (D-06 violation — one source of truth across M010 + M011).
    - Do NOT silently widen `PsychologicalProfilePromptType` to include `'attachment'` (D-23 deferred to v2.6.1).
    - File header JSDoc must reference D-03..D-10, D-23, and HARD CO-LOC #M11-2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no new TypeScript errors involving `src/memory/psychological-profile-prompt.ts`
    - `grep -c "^export function assemblePsychologicalProfilePrompt" src/memory/psychological-profile-prompt.ts` returns `1`
    - `grep -c "^export const PSYCHOLOGICAL_HARD_RULE_EXTENSION" src/memory/psychological-profile-prompt.ts` returns `1`
    - `grep -c "^export type PsychologicalProfilePromptType" src/memory/psychological-profile-prompt.ts` returns `1`
    - `grep -v '^#\|^//\|^ \*' src/memory/psychological-profile-prompt.ts | grep -c "DO_NOT_INFER_DIRECTIVE" >= 2` (imported once + used once in assembler body); `grep -c "import \{ DO_NOT_INFER_DIRECTIVE \} from" src/memory/psychological-profile-prompt.ts` returns `1`
    - `grep -c "CONSTITUTIONAL_PREAMBLE" src/memory/psychological-profile-prompt.ts` >= `2` (import + use)
    - `grep -v '^#\|^//\|^ \*' src/memory/psychological-profile-prompt.ts | grep -cE "\b(jurisdictional|capital|health|family)\b"` returns `0` (Pitfall 3 — boundary audit anchor)
    - `grep -c "## Profile Focus — HEXACO" src/memory/psychological-profile-prompt.ts` >= `1`
    - `grep -c "## Profile Focus — Schwartz" src/memory/psychological-profile-prompt.ts` >= `1`
    - `grep -cE "r ≈ \.31.\.41" src/memory/psychological-profile-prompt.ts` >= `1` (empirical-limits framing present in both directive blocks)
    - `grep -c "data_consistency" src/memory/psychological-profile-prompt.ts` >= `1` (host-emit contract per D-08 — Sonnet told to emit `data_consistency`)
    - `grep -c "overall_confidence" src/memory/psychological-profile-prompt.ts` >= `1` (Sonnet told to emit `overall_confidence` per RESEARCH Finding 1)
    - `npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exits 0 (the new file MUST pass D047 boundary audit — no operational tokens)
  </acceptance_criteria>
  <done>
    src/memory/psychological-profile-prompt.ts compiles; exports the public surface listed in `<interfaces>`; contains all 9 sections of the assembler body; defers attachment per D-23; passes `psych-boundary-audit.test.ts` with zero operational-token contamination.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author psychological-profile-prompt.test.ts structural test</name>
  <files>src/memory/__tests__/psychological-profile-prompt.test.ts</files>
  <read_first>
    - src/memory/__tests__/profile-prompt.test.ts (full file — direct analog test for M010 builder; structural-assertion style)
    - src/memory/psychological-profile-prompt.ts (the module under test — Task 1 output)
    - .planning/phases/38-psychological-inference-engine/38-CONTEXT.md (D-05..D-10 — assertion items)
    - .planning/phases/38-psychological-inference-engine/38-RESEARCH.md §"Plan 38-01" structural test assertions block (lines 535-547 of 38-RESEARCH.md)
    - src/memory/profile-prompt.ts:107-110 (DO_NOT_INFER_DIRECTIVE — assertion target)
    - src/memory/profiles/__tests__/psych-boundary-audit.test.ts (Pitfall 3 — test file MUST pass boundary audit)
  </read_first>
  <action>
    Create `src/memory/__tests__/psychological-profile-prompt.test.ts`. Mirror the structural-test style at `src/memory/__tests__/profile-prompt.test.ts` (vitest `describe`/`it`; no DB, no mocks, no Sonnet — pure-function assertions on the assembled `system` and `user` strings).

    Test fixture: define a small helper `makeSubstrate(wordCount: number = 6000): PsychologicalProfileSubstrateView` returning a shape with 3-5 minimal corpus rows (synthetic ids, `epistemicTag: null` for some + non-null for others, content < 200 chars each), 2 episodic summaries, and the given wordCount. Also define `makePrevState()` returning a small jsonb-able object with a couple of dimension keys (e.g., `{ openness: { score: 4.0, confidence: 0.6, last_updated: '2026-04-01T09:00:00Z' } }`).

    **Test cases (organize under `describe('assemblePsychologicalProfilePrompt', ...)`):**

    Parametrized over BOTH `profileType: 'hexaco' | 'schwartz'` via `describe.each([['hexaco'], ['schwartz']] as const)`:

    1. **CONSTITUTIONAL_PREAMBLE is first section (D-05).** Assert `result.system.startsWith('## Core Principles (Always Active)')` — the M006 preamble heading. Use `import { CONSTITUTIONAL_PREAMBLE } from '../../chris/personality.js'` and assert `result.system.startsWith(CONSTITUTIONAL_PREAMBLE.trimEnd())`.

    2. **DO_NOT_INFER_DIRECTIVE present (D-06).** `import { DO_NOT_INFER_DIRECTIVE } from '../profile-prompt.js';` and `expect(result.system.includes(DO_NOT_INFER_DIRECTIVE)).toBe(true);`.

    3. **PSYCHOLOGICAL_HARD_RULE_EXTENSION present (D-07).** `expect(result.system.includes(PSYCHOLOGICAL_HARD_RULE_EXTENSION)).toBe(true);` and `expect(result.system).toContain('## Psychological Profile Framing (D027 extension — REQUIRED)');`.

    4. **Word-count framing tells Sonnet about data_consistency + overall_confidence (D-08).** `expect(result.system).toContain('data_consistency');` and `expect(result.system).toContain('overall_confidence');` and `expect(result.system).toContain('do NOT emit a `confidence` field');` (or matching phrase from Task 1's builder).

    5. **Word count value appears in the substrate framing.** `const substrate = makeSubstrate(6247);` then `const result = assemble(..., 6247);` and `expect(result.system).toContain('6247');` (or `expect(result.system).toMatch(/6,?247/)`).

    6. **Per-profileType directive present (D-10).** For `hexaco`: `expect(result.system).toContain('## Profile Focus — HEXACO');`. For `schwartz`: `expect(result.system).toContain('## Profile Focus — Schwartz');`. Both: `expect(result.system).toMatch(/r ≈ \.31.\.41/);` (empirical-limits framing).

    7. **prevState null → no CURRENT PROFILE STATE section (D-09).** Pass `prevState: null` to assembler; assert `expect(result.system).not.toContain('## CURRENT PROFILE STATE');`.

    8. **prevState non-null → CURRENT PROFILE STATE section renders snapshot as JSON (D-09).** Pass `prevState: makePrevState()`; assert `expect(result.system).toContain('## CURRENT PROFILE STATE');` and `expect(result.system).toContain(JSON.stringify(makePrevState(), null, 2));`.

    9. **user string contains profile type uppercase + 'psychological profile for Greg' (D-03).** For `hexaco`: `expect(result.user).toBe('Generate the HEXACO psychological profile for Greg.');`. For `schwartz`: same with SCHWARTZ.

    10. **Pure-function determinism.** Call assembler twice with identical args; assert `result1.system === result2.system && result1.user === result2.user;`.

    11. **No operational tokens leak through (negative invariant — Pitfall 3).** `expect(result.system).not.toMatch(/\b(jurisdictional|capital|health|family)\b/);` — runtime-level check that the prompt body itself is clean (in addition to the static boundary audit).

    12. **Section order: CONSTITUTIONAL_PREAMBLE first, then role preamble, then DO_NOT_INFER, then Hard Rule extension** — assert `system.indexOf('## Hallucination Floor') < system.indexOf('## Psychological Profile Framing')` and `system.indexOf('## Core Principles') < system.indexOf('## Hallucination Floor')`.

    **One-off tests (outside `describe.each`, for surface checks):**
    - **PsychologicalProfilePromptType narrower than Phase 37 union (D-23).** Compile-time/type-only check: the TypeScript signature of `assemblePsychologicalProfilePrompt` accepts `'hexaco' | 'schwartz'` but rejects `'attachment'`. Implement as a `@ts-expect-error` comment line followed by an invalid call: `// @ts-expect-error attachment deferred per D-23` then `assemblePsychologicalProfilePrompt('attachment', makeSubstrate(), null, 100);` — this asserts the union is narrowed.
    - **PSYCHOLOGICAL_HARD_RULE_EXTENSION constant contains the exact 8-line phrasing from D-07.** Assert the constant `includes` each of these substrings (verbatim): `'These trait scores describe statistical tendencies inferred from speech patterns'`, `'NOT facts about who Greg is'`, `'tell Greg he is "the kind of person who..."'`, `'his trait scores as evidence that his current reasoning is correct'`, `'validates his existing position by citing his personality'`, `'The Hard Rule (D027) applies here with additional force'`, `'Evaluate every claim on its merits regardless of what the profile says.'`.

    Total: ~13-15 `it()` blocks. Aim for ~150-200 lines.

    **Anti-shallow-execution requirements:**
    - Test file MUST pass `psych-boundary-audit.test.ts` — no operational tokens anywhere, including in test data fixtures.
    - Tests assert literal substring presence, not paraphrased content (D-07 phrasing is locked verbatim).
    - Tests are PURE — no DB, no `vi.mock`, no Sonnet. Pure-function assertions only.
  </action>
  <verify>
    <automated>npx vitest run src/memory/__tests__/psychological-profile-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/memory/__tests__/psychological-profile-prompt.test.ts` exits 0
    - Test output shows ≥13 passing `it()` blocks across both profile types
    - `grep -c "describe.each" src/memory/__tests__/psychological-profile-prompt.test.ts` >= `1` (parametrized over profile types)
    - `grep -c "@ts-expect-error" src/memory/__tests__/psychological-profile-prompt.test.ts` >= `1` (attachment-narrowing assertion per D-23)
    - `grep -v '^#\|^//\|^ \*' src/memory/__tests__/psychological-profile-prompt.test.ts | grep -cE "\b(jurisdictional|capital|health|family)\b"` returns `0` (Pitfall 3)
    - `npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exits 0 (new test file MUST pass D047 boundary audit)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Structural test passes for both `hexaco` and `schwartz` profile types; asserts CONSTITUTIONAL_PREAMBLE first, DO_NOT_INFER imported (not redeclared), PSYCHOLOGICAL_HARD_RULE_EXTENSION verbatim phrasing, per-profileType directive present, word-count framing tells Sonnet to emit data_consistency + overall_confidence, prevState conditional, user string formatted; boundary audit stays green.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/memory/__tests__/psychological-profile-prompt.test.ts` exits 0
- `npx vitest run src/memory/profiles/__tests__/psych-boundary-audit.test.ts` exits 0 — D047 boundary audit (PSCH-10) regression check
- `bash scripts/test.sh` full Docker suite green (per CLAUDE.md memory "Always run full Docker tests")
</verification>

<success_criteria>
- `src/memory/psychological-profile-prompt.ts` exists with all public exports listed in `<interfaces>`
- `assemblePsychologicalProfilePrompt('hexaco', ...)` and `('schwartz', ...)` each return `{ system, user }` where the system string contains, in order: `CONSTITUTIONAL_PREAMBLE`, `DO_NOT_INFER_DIRECTIVE` (imported from M010), `PSYCHOLOGICAL_HARD_RULE_EXTENSION` (verbatim 8-line D-07 phrasing), word-count framing instructing Sonnet to emit `data_consistency` + `overall_confidence`, per-profileType directive with `r ≈ .31–.41` ceiling language, and the substrate block (without decisions section)
- `PSYCHOLOGICAL_HARD_RULE_EXTENSION` is exported as a const string (Phase 39 PSURF-02 will re-export it for surface-level injection)
- `psych-boundary-audit.test.ts` (PSCH-10) stays green — zero operational-token contamination in new files
- Plan 38-02 can compile against the contracts declared by this plan (HARD CO-LOC #M11-2 satisfied — builder ships before generators)
</success_criteria>

<output>
After completion, create `.planning/phases/38-psychological-inference-engine/38-01-SUMMARY.md`
</output>
