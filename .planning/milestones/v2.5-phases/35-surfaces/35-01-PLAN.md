---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chris/personality.ts
  - src/chris/modes/journal.ts
  - src/chris/modes/interrogate.ts
  - src/chris/modes/reflect.ts
  - src/chris/modes/coach.ts
  - src/chris/modes/psychology.ts
  - src/chris/modes/produce.ts
  - src/chris/modes/photos.ts
  - src/decisions/resolution.ts
  - src/chris/__tests__/personality.test.ts
  - src/chris/__tests__/engine.test.ts
  - src/chris/__tests__/reflect.test.ts
  - src/chris/__tests__/coach.test.ts
  - src/chris/__tests__/psychology.test.ts
  - src/chris/__tests__/journal.test.ts
  - src/chris/__tests__/interrogate.test.ts
  - src/chris/__tests__/produce.test.ts
  - src/chris/__tests__/photos.test.ts
  - src/chris/__tests__/photos-memory.test.ts
autonomous: true
requirements:
  - SURF-01
tags:
  - phase-35
  - surfaces
  - refactor
  - buildSystemPrompt
  - hard-co-loc-m10-4

must_haves:
  truths:
    - "buildSystemPrompt signature accepts (mode, pensieveContext?, relationalContext?, extras?: ChrisContextExtras) across all production call sites"
    - "ChrisContextExtras is exported from src/chris/personality.ts with shape { language?, declinedTopics?, operationalProfiles? } — all optional"
    - "ACCOUNTABILITY overload semantics preserved verbatim — the 84-92 line comment block in personality.ts stays unchanged"
    - "resolution.ts:252-257 ACCOUNTABILITY call site passes { language: rawLang } as the extras 4th arg (no declinedTopics in that scope)"
    - "All 8 production call sites compile and pass type-check with the new signature in a SINGLE commit (no partial refactor red-build window)"
    - "All 10 test files that reference buildSystemPrompt continue to pass — 2 direct (personality.test.ts, engine.test.ts) + 8 mocked-import (reflect/coach/psychology/journal/interrogate/produce/photos/photos-memory) with updated assertion shapes"
  artifacts:
    - path: "src/chris/personality.ts"
      provides: "buildSystemPrompt with new signature + exported ChrisContextExtras interface"
      contains: "export interface ChrisContextExtras"
    - path: "src/chris/modes/reflect.ts"
      provides: "REFLECT mode handler with migrated call shape (operationalProfiles slot empty until Plan 35-02)"
      contains: "buildSystemPrompt('REFLECT'"
    - path: "src/chris/modes/coach.ts"
      provides: "COACH mode handler with migrated call shape"
      contains: "buildSystemPrompt('COACH'"
    - path: "src/chris/modes/psychology.ts"
      provides: "PSYCHOLOGY mode handler with migrated call shape"
      contains: "buildSystemPrompt('PSYCHOLOGY'"
    - path: "src/decisions/resolution.ts"
      provides: "ACCOUNTABILITY call site migrated; overload semantics preserved"
      contains: "{ language: rawLang }"
  key_links:
    - from: "src/chris/modes/{reflect,coach,psychology,journal,interrogate,produce,photos}.ts"
      to: "src/chris/personality.ts"
      via: "buildSystemPrompt import with extras object"
      pattern: "buildSystemPrompt\\([^,]+,[^,]+,[^,]+,\\s*\\{"
    - from: "src/decisions/resolution.ts"
      to: "src/chris/personality.ts"
      via: "buildSystemPrompt('ACCOUNTABILITY', ..., { language: rawLang })"
      pattern: "buildSystemPrompt\\(\\s*'ACCOUNTABILITY'"
    - from: "src/chris/__tests__/{reflect,coach,psychology,journal,interrogate,produce,photos,photos-memory}.test.ts"
      to: "mocked buildSystemPrompt"
      via: "toHaveBeenCalledWith assertions on the new positional shape (4 args, not 5)"
      pattern: "toHaveBeenCalledWith\\([^)]*expect\\.objectContaining"
---

<objective>
Refactor `buildSystemPrompt` in `src/chris/personality.ts` to accept `(mode, pensieveContext?, relationalContext?, extras?: ChrisContextExtras)` and migrate ALL call sites + ALL test assertion shapes atomically in a single plan. This is HARD CO-LOCATION #M10-4 — partial refactor leaves the TypeScript build red and breaks every mode handler test. The atomic refactor is the prerequisite for Plan 35-02's `operationalProfiles` injection wiring.

Purpose: SURF-01 + HARD CO-LOC #M10-4. Open the `extras` slot on `buildSystemPrompt` so Plan 35-02 can pass a pre-rendered operational-profile block as `extras.operationalProfiles` without changing the signature again. The refactor is mechanical (positional `(...,language, declinedTopics)` → `(...,{ language, declinedTopics })`) but broad — 8 production sites + 10 test files. Per D-29: NO new tests are added; the existing 47 + 230 + N test sites are the regression net.

Output: `src/chris/personality.ts` with the new signature + `ChrisContextExtras` export; 8 production call sites migrated; 10 test files with updated assertion shapes; full Docker test suite green (no regression to the 1504-passing baseline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-surfaces/35-CONTEXT.md
@.planning/phases/35-surfaces/35-RESEARCH.md
@.planning/phases/35-surfaces/35-PATTERNS.md
@.planning/phases/35-surfaces/35-VALIDATION.md
@.planning/phases/34-inference-engine/34-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current signature being refactored (src/chris/personality.ts:94-100): -->

export function buildSystemPrompt(
  mode: ChrisMode,
  pensieveContext?: string,
  relationalContext?: string,
  language?: string,
  declinedTopics?: DeclinedTopic[],
): string;

export type ChrisMode = 'JOURNAL' | 'INTERROGATE' | 'REFLECT' | 'COACH' | 'PSYCHOLOGY' | 'PRODUCE' | 'PHOTOS' | 'ACCOUNTABILITY';

export interface DeclinedTopic {
  topic: string;
  originalSentence: string;
}

<!-- Target signature (Plan 35-01 task 1): -->

export interface ChrisContextExtras {
  language?: string;
  declinedTopics?: DeclinedTopic[];
  operationalProfiles?: string;   // pre-rendered string from Plan 35-02; ignored in Plan 35-01 (slot reserved)
}

export function buildSystemPrompt(
  mode: ChrisMode,
  pensieveContext?: string,
  relationalContext?: string,
  extras?: ChrisContextExtras,
): string;
</interfaces>

<call_site_inventory>
<!-- D-06 inventory verified 2026-05-13 against current main. Re-verify in Task 1 -->
<!-- via `grep -rn "buildSystemPrompt(" src/` before any code change. -->

Production sites (8):
  1. src/chris/personality.ts:94          — declaration (refactored in Task 1)
  2. src/chris/modes/journal.ts:81        — buildSystemPrompt('JOURNAL', pensieveContext, undefined, language, declinedTopics)
  3. src/chris/modes/interrogate.ts:115   — buildSystemPrompt('INTERROGATE', pensieveContext, undefined, language, declinedTopics)
  4. src/chris/modes/reflect.ts:76        — buildSystemPrompt('REFLECT', pensieveContext, relationalContext, language, declinedTopics)
  5. src/chris/modes/coach.ts:76          — buildSystemPrompt('COACH', pensieveContext, relationalContext, language, declinedTopics)
  6. src/chris/modes/psychology.ts:77     — buildSystemPrompt('PSYCHOLOGY', pensieveContext, relationalContext, language, declinedTopics)
  7. src/chris/modes/produce.ts:72        — buildSystemPrompt('PRODUCE', pensieveContext, undefined, language, declinedTopics)
  8. src/chris/modes/photos.ts:182        — buildSystemPrompt('JOURNAL', undefined, undefined, language, declinedTopics)  [Photos uses JOURNAL persona]
  9. src/decisions/resolution.ts:252-257  — buildSystemPrompt('ACCOUNTABILITY', decisionContext, temporalContext, rawLang)  [NO declinedTopics in scope]

Test files (10):
  - DIRECT (2): src/chris/__tests__/personality.test.ts (31 occurrences) + src/chris/__tests__/engine.test.ts (16 occurrences) — call the real function; assertion is on return string
  - MOCKED (8): src/chris/__tests__/{reflect,coach,psychology,journal,interrogate,produce,photos,photos-memory}.test.ts — each has `vi.mock('../personality.js', () => ({ buildSystemPrompt: mockBuildSystemPrompt }))` AND each has `toHaveBeenCalledWith('MODE', expect.any(String), expect.any(String), undefined, undefined)` style assertions THAT WILL FAIL POST-REFACTOR because the 4th positional arg becomes an object (or undefined), not a string

Critical drift from CONTEXT.md D-06: D-06 named only personality.test.ts + engine.test.ts as "test files affected". RESEARCH.md surfaced 8 ADDITIONAL test files where assertion shapes will silently break. Plan 35-01 MUST update all 10.
</call_site_inventory>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor buildSystemPrompt signature + export ChrisContextExtras + re-verify call-site inventory</name>
  <files>src/chris/personality.ts</files>
  <read_first>
    - src/chris/personality.ts (lines 1-177 — current signature + body + ACCOUNTABILITY overload comment)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-03, D-04, D-05, D-06, D-07)
    - .planning/phases/35-surfaces/35-PATTERNS.md (§"src/chris/personality.ts (modified — signature refactor)" excerpt lines 330-410)
  </read_first>
  <action>
    Step 0 (pre-work gate): Run `grep -rn "buildSystemPrompt(" src/ | grep -v node_modules` and confirm 8 production sites + 10 test files match the inventory in `<call_site_inventory>` above. If grep returns ANY site not listed, STOP and surface the deviation in the commit body before proceeding.

    Step 1: Edit `src/chris/personality.ts` ONLY (callers migrate in later tasks). Make these changes atomically:

    (a) Add `export interface ChrisContextExtras` immediately after the existing `DeclinedTopic` interface (around line 23). Shape per D-04: `{ language?: string; declinedTopics?: DeclinedTopic[]; operationalProfiles?: string }`. All three fields optional. Add a TSDoc comment explaining `operationalProfiles` is a pre-rendered string from `formatProfilesForPrompt()` (Plan 35-02) — NOT the structured `OperationalProfiles` object. Reference D-04 and HARD CO-LOC #M10-4 in the TSDoc.

    (b) Change the function signature at line 94 from `(mode, pensieveContext?, relationalContext?, language?, declinedTopics?)` to `(mode, pensieveContext?, relationalContext?, extras?: ChrisContextExtras)`. Inside the body, destructure on entry: `const { language, declinedTopics, operationalProfiles } = extras ?? {};`. The rest of the body uses these locals exactly as before (lines 101-176 reference them unchanged).

    (c) DO NOT TOUCH the 84-92 line ACCOUNTABILITY overload comment block (D-05 — verbatim preservation). DO NOT add operational-profile injection logic in this task — that ships in Plan 35-02. The destructured `operationalProfiles` local is intentionally unused here (TypeScript will warn — silence with `void operationalProfiles;` immediately after destructure, with a comment "Plan 35-02 consumes this — slot reserved by SURF-01 atomic refactor"). This deliberate no-op proves the signature surface exists without consuming it.

    (d) Update the TSDoc block above the function (lines 67-93) so the parameter list reads: `mode`, `pensieveContext`, `relationalContext`, `extras`. Keep the per-mode usage notes (JOURNAL/INTERROGATE silently drop relationalContext; REFLECT/COACH/PSYCHOLOGY use both contexts; ACCOUNTABILITY overload).

    DO NOT migrate any caller in this task. The build MUST be red after Task 1 (callers still pass 5 positional args). Tasks 2-4 close the red window.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface ChrisContextExtras" src/chris/personality.ts` returns 1
    - `grep -c "extras?: ChrisContextExtras" src/chris/personality.ts` returns 1
    - `grep -c "const { language, declinedTopics, operationalProfiles } = extras" src/chris/personality.ts` returns 1
    - `grep -c "IN-04: ACCOUNTABILITY mode overloads the parameter semantics" src/chris/personality.ts` returns 1 (84-92 comment preserved)
    - `grep -c "void operationalProfiles" src/chris/personality.ts` returns 1 (Plan 35-01 slot-reserved no-op)
    - `npx tsc --noEmit 2>&1 | grep -c "src/chris/modes/" | awk '$1>=5 {print "RED-BUILD-EXPECTED"}'` prints `RED-BUILD-EXPECTED` (build is red because callers still pass 5 args — closed in Tasks 2-4)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export interface ChrisContextExtras" src/chris/personality.ts | awk '$1==1 {print "OK"}' && grep -c "extras?: ChrisContextExtras" src/chris/personality.ts | awk '$1==1 {print "OK"}'</automated>
  </verify>
  <done>
    ChrisContextExtras interface exported; buildSystemPrompt signature has 4th `extras?` arg; ACCOUNTABILITY overload comment block intact; body destructures `extras` and references the three named locals; `operationalProfiles` slot reserved but unconsumed (no-op `void` statement); existing tests will fail at this point — that's expected; Tasks 2-4 close the red window.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 8 production call sites to extras-object shape</name>
  <files>src/chris/modes/journal.ts, src/chris/modes/interrogate.ts, src/chris/modes/reflect.ts, src/chris/modes/coach.ts, src/chris/modes/psychology.ts, src/chris/modes/produce.ts, src/chris/modes/photos.ts, src/decisions/resolution.ts</files>
  <read_first>
    - src/chris/modes/reflect.ts (lines 70-76 — current shape; relational + buildSystemPrompt block)
    - src/chris/modes/journal.ts (lines 78-83)
    - src/chris/modes/interrogate.ts (lines 113-115)
    - src/chris/modes/coach.ts (lines 73-76)
    - src/chris/modes/psychology.ts (lines 74-77)
    - src/chris/modes/produce.ts (lines 70-72)
    - src/chris/modes/photos.ts (lines 180-182)
    - src/decisions/resolution.ts (lines 245-260 — ACCOUNTABILITY call site; rawLang scope)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-06 inventory + D-05 ACCOUNTABILITY rule)
    - .planning/phases/35-surfaces/35-PATTERNS.md (Pattern 1: Mechanical Signature Migration, lines 230-280)
  </read_first>
  <action>
    Migrate all 8 production call sites mechanically — positional `(...,language, declinedTopics)` → `(...,{ language, declinedTopics })`. No new logic, no operationalProfiles, no behavior change — pure call-shape migration. Plan 35-02 adds operationalProfiles to reflect/coach/psychology in a follow-up wave.

    Per D-06 + PATTERNS.md Pattern 1, each migration is:

    1. `src/chris/modes/journal.ts:81` — `buildSystemPrompt('JOURNAL', pensieveContext, undefined, language, declinedTopics)` → `buildSystemPrompt('JOURNAL', pensieveContext, undefined, { language, declinedTopics })`

    2. `src/chris/modes/interrogate.ts:115` — same shape, INTERROGATE.

    3. `src/chris/modes/reflect.ts:76` — `buildSystemPrompt('REFLECT', pensieveContext, relationalContext, language, declinedTopics)` → `buildSystemPrompt('REFLECT', pensieveContext, relationalContext, { language, declinedTopics })`. DO NOT add operationalProfiles here — that's Plan 35-02 scope.

    4. `src/chris/modes/coach.ts:76` — same as reflect, COACH.

    5. `src/chris/modes/psychology.ts:77` — same as reflect, PSYCHOLOGY.

    6. `src/chris/modes/produce.ts:72` — `buildSystemPrompt('PRODUCE', pensieveContext, undefined, language, declinedTopics)` → `buildSystemPrompt('PRODUCE', pensieveContext, undefined, { language, declinedTopics })`.

    7. `src/chris/modes/photos.ts:182` — `buildSystemPrompt('JOURNAL', undefined, undefined, language, declinedTopics)` → `buildSystemPrompt('JOURNAL', undefined, undefined, { language, declinedTopics })`. Photos uses JOURNAL persona (unchanged).

    8. `src/decisions/resolution.ts:252-257` — per D-05, ACCOUNTABILITY overload preserved verbatim. Migrate positional `rawLang` → `{ language: rawLang }`:
       ```
       buildSystemPrompt(
         'ACCOUNTABILITY',
         decisionContext,
         temporalContext,
         { language: rawLang },
       )
       ```
       This call site does NOT have `declinedTopics` in scope — verify by reading lines 230-260 of resolution.ts. Do NOT invent a declinedTopics value. Only `language` flows through extras here (intentional per D-06 line "no declinedTopics — verify intentional in plan").

    After this task, all 8 production call sites compile against the new signature. `npx tsc --noEmit` should be clean on `src/chris/` and `src/decisions/` (but test files are still red — Task 3 closes that).
  </action>
  <acceptance_criteria>
    - `grep -rn "buildSystemPrompt(" src/chris/modes/ src/decisions/resolution.ts | grep -vE "\\{ language|\\{ language: rawLang"` returns 0 lines (no positional-form callers remain in production code)
    - `grep -c "{ language: rawLang }" src/decisions/resolution.ts` returns 1
    - `grep -rn "buildSystemPrompt.*language, declinedTopics)" src/chris/modes/` returns 0 lines
    - `npx tsc --noEmit src/chris/modes/*.ts src/chris/personality.ts src/decisions/resolution.ts 2>&1 | grep -cE "error TS" | awk '$1==0 {print "OK"}'` prints `OK` (note: full project tsc may still be red due to test files in Task 3)
    - The 84-92 ACCOUNTABILITY comment in personality.ts remains byte-identical to Task 1 state: `git diff src/chris/personality.ts -- | grep -E "^[-+].*IN-04: ACCOUNTABILITY" | wc -l` returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "buildSystemPrompt(" src/chris/modes/ src/decisions/resolution.ts | grep -vE "language|operationalProfiles" | grep -v "^$" | wc -l | awk '$1==0 {print "OK"}'</automated>
  </verify>
  <done>
    All 8 production call sites migrated to extras-object shape; ACCOUNTABILITY overload semantics preserved (only language flows through extras at resolution.ts:252-257; decisionContext+temporalContext continue to ride pensieveContext+relationalContext slots); production code type-checks against new signature. Test files remain red — Task 3 closes that.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate 2 direct-call test files (personality.test.ts + engine.test.ts) — 47 sites total</name>
  <files>src/chris/__tests__/personality.test.ts, src/chris/__tests__/engine.test.ts</files>
  <read_first>
    - src/chris/__tests__/personality.test.ts (full file, ~230 lines — 31 buildSystemPrompt calls)
    - src/chris/__tests__/engine.test.ts (full file — 16 buildSystemPrompt calls; mostly in `describe('buildSystemPrompt interpolates for COACH', ...)` style blocks)
    - .planning/phases/35-surfaces/35-PATTERNS.md (Pattern 1 example for test-file migration, lines 267-279)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-29 — no NEW tests added; existing test bodies validate new shape automatically)
  </read_first>
  <action>
    Mechanically migrate every direct call to `buildSystemPrompt(mode, ...)` in these 2 test files. The transformation rule:

    - `buildSystemPrompt('MODE', pensieve, relational, language, declinedTopics)` → `buildSystemPrompt('MODE', pensieve, relational, { language, declinedTopics })`
    - `buildSystemPrompt('MODE', undefined, undefined, undefined, [{...}])` → `buildSystemPrompt('MODE', undefined, undefined, { declinedTopics: [{...}] })`
    - `buildSystemPrompt('MODE', undefined, undefined, 'French', undefined)` → `buildSystemPrompt('MODE', undefined, undefined, { language: 'French' })`
    - `buildSystemPrompt('MODE')` (no extras) → unchanged (extras is optional)
    - `buildSystemPrompt('MODE', ctx)` (1 context arg) → unchanged
    - `buildSystemPrompt('MODE', ctx1, ctx2)` (2 context args) → unchanged

    Per D-29 — NO new tests added. The existing test bodies validate the new shape automatically because they assert on the return-string content (Known Facts block injection, mode-specific template interpolation, language directive presence, declined-topics formatting) — none of those output assertions change.

    Special-case patterns to watch for in `personality.test.ts`:
    - Tests that pass ONLY language (no declinedTopics): wrap as `{ language: ... }`
    - Tests that pass ONLY declinedTopics (no language): wrap as `{ declinedTopics: [...] }`
    - Tests that pass NEITHER: pass `{}` or omit the 4th arg entirely (TypeScript happy with both since `extras?` is optional)

    In `engine.test.ts`, the 16 sites are mostly in the `describe('buildSystemPrompt interpolates for COACH'/'PSYCHOLOGY'/'PRODUCE', ...)` blocks where the test calls `realBuildSystemPrompt(mode, pensieveContext)` with only 2 args — those don't change. The other sites may pass language/declinedTopics — migrate those to extras-object form.

    After this task, `npx vitest run src/chris/__tests__/personality.test.ts src/chris/__tests__/engine.test.ts` MUST be green (this is the Plan 35-01 regression gate per VALIDATION.md row 1).
  </action>
  <acceptance_criteria>
    - `grep -cE "buildSystemPrompt\\([^)]*,[^)]*,[^)]*,[^,)]*,[^)]*\\)" src/chris/__tests__/personality.test.ts src/chris/__tests__/engine.test.ts | awk -F: '{sum+=$2} END {print sum}' | awk '$1==0 {print "OK"}'` prints `OK` (zero 5-arg positional callers remain in these 2 test files)
    - `npx vitest run src/chris/__tests__/personality.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*passed|failed" | grep -q "0 failed" && echo OK` prints `OK`
    - `npx vitest run src/chris/__tests__/engine.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*passed|failed" | grep -q "0 failed" && echo OK` prints `OK`
    - No new `it(...)` or `test(...)` blocks added: `git diff --stat src/chris/__tests__/personality.test.ts src/chris/__tests__/engine.test.ts` shows roughly equal `+/-` line counts (mechanical migration, no test additions)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/chris/__tests__/personality.test.ts src/chris/__tests__/engine.test.ts --reporter=basic 2>&1 | tail -10</automated>
  </verify>
  <done>
    personality.test.ts and engine.test.ts use the extras-object form at every call site; both files green; no new tests added (per D-29). The Plan 35-01 regression gate (VALIDATION.md row 1) closes here.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate 8 mocked-import test files — fix toHaveBeenCalledWith assertion shapes</name>
  <files>src/chris/__tests__/reflect.test.ts, src/chris/__tests__/coach.test.ts, src/chris/__tests__/psychology.test.ts, src/chris/__tests__/journal.test.ts, src/chris/__tests__/interrogate.test.ts, src/chris/__tests__/produce.test.ts, src/chris/__tests__/photos.test.ts, src/chris/__tests__/photos-memory.test.ts</files>
  <read_first>
    - src/chris/__tests__/reflect.test.ts (lines 230-250 — the canonical `expect(mockBuildSystemPrompt).toHaveBeenCalledWith('REFLECT', expect.any(String), expect.any(String), undefined, undefined)` assertion)
    - src/chris/__tests__/coach.test.ts (lines 240-255 — same shape)
    - src/chris/__tests__/psychology.test.ts (lines 237-250 — same shape)
    - src/chris/__tests__/journal.test.ts (entire file — fewer call-shape assertions, more output-content assertions)
    - src/chris/__tests__/interrogate.test.ts (lines 460-500 — has `mockBuildSystemPrompt.mockImplementation(realPersonality.buildSystemPrompt)` which uses the REAL function — assertion shape there is on the return value, not call args)
    - src/chris/__tests__/produce.test.ts
    - src/chris/__tests__/photos.test.ts
    - src/chris/__tests__/photos-memory.test.ts (line 109: `buildSystemPrompt: vi.fn(() => 'You are Chris...')` — just a stub, no toHaveBeenCalledWith assertion)
    - .planning/phases/35-surfaces/35-RESEARCH.md (§Summary line 11-13 — drift surfaced: 8 test files, NOT 2)
    - .planning/phases/35-surfaces/35-CONTEXT.md (D-29 — existing test bodies validate new shape)
  </read_first>
  <action>
    These 8 test files mock `buildSystemPrompt` via `vi.mock('../personality.js', ...)` and use `expect(mockBuildSystemPrompt).toHaveBeenCalledWith(...)` assertions. The current assertion shape is positional 5-arg:
    ```
    expect(mockBuildSystemPrompt).toHaveBeenCalledWith(
      'REFLECT',
      expect.any(String),     // pensieveContext
      expect.any(String),     // relationalContext
      undefined,              // language
      undefined,              // declinedTopics
    );
    ```

    Post-refactor, the production handler calls `buildSystemPrompt('REFLECT', pensieveContext, relationalContext, { language, declinedTopics })` — so the mock receives 4 args, the 4th being an object. The `undefined, undefined` will fail because there's no 5th arg, and even if you collapse to 4 args the 4th arg is now `{ language: undefined, declinedTopics: undefined }`, NOT `undefined` itself.

    Migration rule: replace the last two `undefined, undefined` (or specific values) with a SINGLE 4th argument that matches the extras shape. Use `expect.objectContaining({...})` when the test wants to be permissive:

    Before:
    ```
    expect(mockBuildSystemPrompt).toHaveBeenCalledWith('REFLECT', expect.any(String), expect.any(String), undefined, undefined);
    ```

    After (Plan 35-01 — operationalProfiles NOT YET injected by handler):
    ```
    expect(mockBuildSystemPrompt).toHaveBeenCalledWith(
      'REFLECT',
      expect.any(String),
      expect.any(String),
      expect.objectContaining({ language: undefined, declinedTopics: undefined }),
    );
    ```

    OR — preferred shorter form per existing project idiom:
    ```
    expect(mockBuildSystemPrompt).toHaveBeenCalledWith(
      'REFLECT',
      expect.any(String),
      expect.any(String),
      { language: undefined, declinedTopics: undefined },
    );
    ```

    Use whichever style the surrounding test code prefers (look at the immediately adjacent assertions in the same file).

    File-by-file specifics:

    1. **reflect.test.ts (line 236)** + **coach.test.ts (~245)** + **psychology.test.ts (~242)**: positive-injection tests will be ADDED in Plan 35-02 for these 3 files. For Plan 35-01 ONLY, the assertion should accept `{ language: undefined, declinedTopics: undefined }` (no operationalProfiles yet). Plan 35-02 will UPDATE these same assertions to include `operationalProfiles: expect.stringContaining('Operational Profile')`.

    2. **journal.test.ts**: review every assertion that names `mockBuildSystemPrompt`. Out-of-scope mode (no operationalProfiles in Plan 35-02 either). Assertion shape stays at `{ language: ..., declinedTopics: ... }`.

    3. **interrogate.test.ts**: has TWO patterns — `mockBuildSystemPrompt.toHaveBeenCalledWith(...)` AND `mockBuildSystemPrompt.mockImplementation(realPersonality.buildSystemPrompt)` (the latter wires through to the real function for the "episodic block before Known Facts" test at line 476). The `mockImplementation` line keeps working since the real function has the same return contract. Only the `toHaveBeenCalledWith` sites need migration.

    4. **produce.test.ts, photos.test.ts**: same shape as reflect/coach. `photos-memory.test.ts:109` is just a stub (`vi.fn(() => 'You are Chris...')`) — no `toHaveBeenCalledWith` assertion to update; verify by reading the file.

    DO NOT add new tests in this task. DO NOT add operationalProfiles assertions yet. Plan 35-02 is the place for both. This task closes the Plan 35-01 red-build window for these 8 test files.

    After this task: full Docker test gate (`bash scripts/test.sh src/chris/`) must be GREEN with same passing count as pre-Plan-35-01 baseline (no regression).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*failed" | grep -cE "0 failed|^$" | awk '$1>=1 {print "OK"}'` prints `OK`
    - `npx vitest run src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/produce.test.ts src/chris/__tests__/photos.test.ts src/chris/__tests__/photos-memory.test.ts --reporter=basic 2>&1 | grep -E "Test Files.*failed" | grep -cE "0 failed|^$" | awk '$1>=1 {print "OK"}'` prints `OK`
    - `grep -rnE "toHaveBeenCalledWith\\([^)]*undefined,\\s*undefined\\s*\\)" src/chris/__tests__/{reflect,coach,psychology,journal,interrogate,produce,photos}.test.ts | wc -l | awk '$1==0 {print "OK"}'` prints `OK` (no stale `undefined, undefined` 5-arg-trailer patterns remain)
    - `npx tsc --noEmit 2>&1 | grep -cE "error TS" | awk '$1==0 {print "OK"}'` prints `OK` (full project type-check clean)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/chris/__tests__/ --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <done>
    All 8 mocked-import test files updated to assert the new 4-arg extras-object shape (no stale 5-arg `undefined, undefined` trailers); full project type-checks clean; entire `src/chris/__tests__/` suite green. Plan 35-01 atomic refactor closed. Plan 35-02 wires operationalProfiles into REFLECT/COACH/PSYCHOLOGY in a subsequent wave.
  </done>
</task>

<task type="auto">
  <name>Task 5: Full Docker test gate + final regression check</name>
  <files></files>
  <read_first>
    - .planning/phases/35-surfaces/35-VALIDATION.md (§Per-Task Verification Map row 35-01-*)
    - .planning/phases/34-inference-engine/34-03-SUMMARY.md (§Test Count + GREEN Confirmation — 1504 passing baseline)
  </read_first>
  <action>
    Run the full Docker test gate to confirm no regression. The Plan 34-03 baseline was 1504 passed / 12 skipped / 29 pre-existing live-API failures.

    Step 1: `bash scripts/test.sh src/chris/` — confirm full chris/ test suite passes (this exercises personality.test.ts + engine.test.ts + 8 mode-handler tests + others).

    Step 2: `bash scripts/test.sh src/decisions/` — confirm resolution.ts call-site change doesn't break decisions tests.

    Step 3: `bash scripts/test.sh` — full Docker suite. Expected count: 1504 passed / 12 skipped / 29 failed (same as Plan 34-03 baseline; 29 failures are pre-existing live-API tests requiring real ANTHROPIC_API_KEY in sandbox — documented in .planning/phases/34-inference-engine/deferred-items.md).

    If any NEW failure surfaces (count > 29 or any failure outside the pre-existing live-API set), STOP and document in summary. Do not commit the failing state.

    Step 4: If green, this task writes no files — it's a verification-only gate. Commit message: `test(35-01): full Docker suite green post-buildSystemPrompt-refactor (SURF-01)`. No code changes in this commit (the test gate confirms no regression after the previous 4 task commits).
  </action>
  <acceptance_criteria>
    - `bash scripts/test.sh src/chris/ 2>&1 | tail -5 | grep -E "passed|failed"` shows zero new failures vs pre-Plan-35-01 baseline
    - `bash scripts/test.sh 2>&1 | grep -E "Test Files.*passed.*failed" | grep -E "^\\s*Test Files\\s+\\d+ failed" | grep -cE "^\\s*Test Files\\s+(0|29)" | awk '$1>=1 {print "OK"}'` prints `OK` (passing count is at-or-above baseline; failed count == 29 pre-existing or 0)
    - `git log --oneline -5 | grep -cE "35-01" | awk '$1>=4 {print "OK"}'` prints `OK` (at least 4 task commits from Plan 35-01 visible — Task 1 refactor, Task 2 production sites, Task 3 direct test files, Task 4 mocked test files)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test.sh src/chris/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    Full Docker test gate passes at-or-above the Plan 34-03 baseline (1504 passed / 12 skipped). No new failures introduced. Plan 35-01 SURF-01 complete; the buildSystemPrompt signature now exposes the `extras.operationalProfiles` slot that Plan 35-02 will wire into REFLECT/COACH/PSYCHOLOGY mode handlers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → buildSystemPrompt | Mode handlers pass in user-derived strings (pensieveContext from search results, relationalContext from observations, declinedTopics from user session) — all already-sanitized prior to this layer. The refactor preserves the existing boundary contract (no new untrusted-input surface). |
| test mocks → production module | Test files use `vi.mock('../personality.js', ...)` — this is in-process test boundary, no security implication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-V8-01 | I (Information Disclosure) | personality.ts — ChrisContextExtras destructuring | mitigate | Destructure `extras ?? {}` (not `extras!`) so a caller passing `undefined` does not throw; the three locals default to `undefined` and the downstream `if (language)` / `if (declinedTopics && ...)` guards already handle nullish — no new info-leak surface. Verified by Task 1's destructure-with-default idiom. |
| T-35-01-V8-02 | T (Tampering) | resolution.ts ACCOUNTABILITY overload | mitigate | D-05 + Task 2 step 8 preserve the overload semantics verbatim — `pensieveContext` still feeds `{decisionContext}`, `relationalContext` still feeds `{pensieveContext}` template slot. A code reviewer who misunderstands the overload could "fix" it and break ACCOUNTABILITY mode. Mitigation: the 84-92 comment block stays verbatim AND `personality.ts:133-148` ACCOUNTABILITY case logic stays byte-identical. Acceptance criterion checks the comment is unchanged. |
| T-35-01-V11-01 | E (Elevation of Privilege via missed migration) | 10 test files | mitigate | If even ONE of the 10 test files retains a 5-arg positional assertion, the mocked handler test will silently pass against the new 4-arg call shape (or fail noisily). Tasks 3+4 explicitly migrate all 10 files; Task 5 runs the full Docker gate to confirm zero regression. Acceptance criterion `grep -rnE "toHaveBeenCalledWith\\([^)]*undefined,\\s*undefined\\s*\\)"` returns 0 lines. |
| T-35-01-V7-01 | R (Repudiation — silent build-red between tasks) | inter-task atomicity | accept | Tasks 1-4 introduce a transient red-build window where production callers don't match the new signature (Task 1) and tests don't match the new assertion shape (Tasks 2-4). This is by design — HARD CO-LOC #M10-4 requires the refactor land in ONE plan, not multiple plans. The 4 task commits land sequentially and Task 5's full-gate verification closes the window. Acceptable because: (a) no deploy happens between tasks (orchestrator owns final merge); (b) git history makes the rollback path obvious. |

**Security gate disposition:** Phase 35-01 is a mechanical refactor with zero new attack surface. Low severity. No high-severity threats — proceed.
</threat_model>

<verification>
1. ChrisContextExtras interface exported from src/chris/personality.ts with shape `{ language?, declinedTopics?, operationalProfiles? }` (all optional).
2. buildSystemPrompt signature is `(mode, pensieveContext?, relationalContext?, extras?: ChrisContextExtras)` — 4 args, not 5.
3. ACCOUNTABILITY overload preserved verbatim — the 84-92 line comment block is byte-identical to pre-refactor state.
4. All 8 production call sites use extras-object form; zero positional 5-arg callers remain in production code (`grep -rn "buildSystemPrompt(" src/ | grep -vE "language|operationalProfiles" | grep -v "personality.ts"` returns 0 lines that match the 5-arg form).
5. All 10 test files (2 direct + 8 mocked) compile and pass — `npx vitest run src/chris/__tests__/ src/decisions/__tests__/` is green.
6. Full Docker suite (`bash scripts/test.sh`) passes at-or-above Plan 34-03 baseline (1504 passed / 12 skipped; 29 pre-existing live-API failures unchanged).
7. `npx tsc --noEmit` is clean (0 type errors) across the full project.
8. 4-5 task commits visible in git log with `35-01` conventional-commit scope.
</verification>

<success_criteria>
SURF-01 closed: buildSystemPrompt refactored atomically across all 8 production call sites + 10 test files; ACCOUNTABILITY overload preserved; full test suite green at-or-above baseline; ChrisContextExtras interface exported for Plan 35-02's `operationalProfiles` consumption. HARD CO-LOC #M10-4 satisfied — no partial-refactor red-build leaked to main.
</success_criteria>

<output>
After completion, create `.planning/phases/35-surfaces/35-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Surface in the summary:
- The 10-test-file drift surfaced during Plan 35-01 (NOT 2 as D-06 originally claimed) and how Tasks 3+4 closed it
- Final commit count + git oneline trail
- Full Docker suite count (passed/skipped/failed) vs Plan 34-03 baseline (1504/12/29)
- The 84-92 ACCOUNTABILITY comment preserved verbatim (D-05 contract honored)
- The ChrisContextExtras interface surface (3 optional fields) ready for Plan 35-02 to consume via `extras.operationalProfiles`
- Update REQUIREMENTS.md traceability: SURF-01 → Plan 35-01 → status "Complete"
</output>
