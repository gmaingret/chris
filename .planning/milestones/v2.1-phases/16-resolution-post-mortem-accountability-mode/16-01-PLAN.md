---
phase: 16-resolution-post-mortem-accountability-mode
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/decisions/__tests__/resolution.test.ts
  - src/decisions/__tests__/engine-resolution.test.ts
  - src/proactive/__tests__/sweep-escalation.test.ts
autonomous: true
requirements:
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06

must_haves:
  truths:
    - "RED test scaffolds exist for resolution, engine routing, and sweep escalation"
    - "All new test files run and all tests are marked as failing (RED)"
  artifacts:
    - path: "src/decisions/__tests__/resolution.test.ts"
      provides: "Failing tests for handleResolution, handlePostmortem, classifyOutcome, getTemporalPensieve"
    - path: "src/decisions/__tests__/engine-resolution.test.ts"
      provides: "Failing tests for PP#0 AWAITING_RESOLUTION/AWAITING_POSTMORTEM routing"
    - path: "src/proactive/__tests__/sweep-escalation.test.ts"
      provides: "Failing tests for 48h escalation, stale transition, clearCapture on stale"
  key_links: []
---

<objective>
Create RED test scaffolds for all Phase 16 behaviors before any production code is written.

Purpose: Establish the Nyquist validation floor. Every behavior in RES-02 through RES-06 gets a failing test that later plans will turn GREEN. This ensures Wave 0 compliance per the validation strategy.

Output: Three new test files with all tests failing (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-resolution-post-mortem-accountability-mode/16-CONTEXT.md
@.planning/phases/16-resolution-post-mortem-accountability-mode/16-RESEARCH.md
@.planning/phases/16-resolution-post-mortem-accountability-mode/16-VALIDATION.md

<interfaces>
<!-- Key types the test scaffolds will import (stubs for now, implementations come in later plans) -->

From src/decisions/lifecycle.ts:
```typescript
export const LEGAL_TRANSITIONS: Readonly<Record<DecisionStatusLiteral, readonly DecisionStatusLiteral[]>>;
export async function transitionDecision(id: string, from: DecisionStatusLiteral, to: DecisionStatusLiteral, payload?: TransitionPayload): Promise<void>;
```

From src/decisions/capture-state.ts:
```typescript
export async function getActiveDecisionCapture(chatId: bigint): Promise<DecisionCaptureRow | null>;
export async function upsertAwaitingResolution(chatId: bigint, decisionId: string): Promise<void>;
export async function clearCapture(chatId: bigint): Promise<void>;
```

From src/decisions/errors.ts:
```typescript
export class InvalidTransitionError extends Error;
export class OptimisticConcurrencyError extends Error;
export class DecisionNotFoundError extends Error;
```

From src/proactive/state.ts:
```typescript
export async function getValue(key: string): Promise<unknown | null>;
export async function setValue(key: string, value: unknown): Promise<void>;
export async function deleteKey(key: string): Promise<void>;
```

Existing test patterns from src/decisions/__tests__/lifecycle.test.ts and src/decisions/__tests__/capture.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resolution.test.ts RED scaffolds</name>
  <files>src/decisions/__tests__/resolution.test.ts</files>
  <read_first>
    - src/decisions/__tests__/capture.test.ts (existing test pattern for decisions domain)
    - src/decisions/__tests__/lifecycle.test.ts (transitionDecision test patterns)
    - src/decisions/capture-state.ts (upsertAwaitingResolution, clearCapture signatures)
    - src/decisions/lifecycle.ts (LEGAL_TRANSITIONS map, transitionDecision signature)
    - src/decisions/capture.ts (writePensieveEntry pattern lines 224-236)
    - src/pensieve/retrieve.ts (searchPensieve signature, SearchResult type)
    - src/proactive/state.ts (getValue/setValue/deleteKey signatures)
  </read_first>
  <action>
Create `src/decisions/__tests__/resolution.test.ts` with the following failing test stubs. Each test should call a function that does not yet exist, ensuring RED status.

Import stubs (these functions will be created in Plan 03):
```typescript
import { handleResolution, handlePostmortem, classifyOutcome } from '../resolution.js';
import { getTemporalPensieve } from '../../pensieve/retrieve.js';
```

Test groups and cases:

**describe('handleResolution')**:
1. `it('transitions decision from due to resolved')` — call handleResolution with a chatId, resolution text, and decisionId. Assert the decision row's status is 'resolved' after the call.
2. `it('stores resolution text on the decision row')` — assert `decisions.resolution` contains the user's reply text after handleResolution returns.
3. `it('sets capture state to AWAITING_POSTMORTEM')` — assert getActiveDecisionCapture returns stage='AWAITING_POSTMORTEM' after handleResolution.
4. `it('writes two Pensieve entries with DECISION tag and sourceRefId')` — assert two new pensieve_entries rows exist with epistemicTag='DECISION' and metadata containing sourceRefId matching the decision's ID.
5. `it('returns acknowledgment concatenated with post-mortem question')` — assert the return value contains both the Sonnet acknowledgment and a class-specific post-mortem question (separated by newlines).
6. `it('catches OptimisticConcurrencyError gracefully on concurrent transition')` — set decision status to 'resolved' before calling handleResolution (simulating concurrent sweep). Assert no throw, and a graceful fallback message is returned.

**describe('handlePostmortem')**:
7. `it('stores resolution_notes on the decision row')` — after handlePostmortem, assert decisions.resolutionNotes contains the user's post-mortem text.
8. `it('transitions decision from resolved to reviewed')` — assert status is 'reviewed' after handlePostmortem.
9. `it('clears capture state')` — assert getActiveDecisionCapture returns null after handlePostmortem.
10. `it('returns one-line acknowledgment in user language')` — assert return value matches one of "Noted." / "Note." / "Принято." based on detected language.
11. `it('writes Pensieve entry for post-mortem answer')` — assert a new pensieve_entries row exists with DECISION tag and sourceRefId.

**describe('classifyOutcome')**:
12. `it('returns hit for confirmed prediction')` — mock Haiku to return `{"outcome":"hit"}`. Assert classifyOutcome returns 'hit'.
13. `it('returns miss for falsified prediction')` — mock Haiku to return `{"outcome":"miss"}`. Assert classifyOutcome returns 'miss'.
14. `it('falls back to ambiguous on parse failure')` — mock Haiku to return garbage. Assert classifyOutcome returns 'ambiguous'.

**describe('getTemporalPensieve')**:
15. `it('returns entries within +/-48h window only')` — insert entries at -72h, -24h, +24h, +72h relative to a center date. Assert only the -24h and +24h entries are returned.
16. `it('excludes soft-deleted entries')` — insert an entry within window but with deletedAt set. Assert it is excluded.

All tests should use `vi.mock` for the Anthropic client (Haiku/Sonnet calls). Database tests should use the real Docker Postgres (follow pattern from lifecycle.test.ts with beforeAll/afterAll setup).

Mark every test with `it.fails(...)` or simply import non-existent functions so the tests fail on import. The goal is RED — every test must fail.
  </action>
  <verify>
    <automated>cd /home/claude/chris && npx vitest run src/decisions/__tests__/resolution.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/decisions/__tests__/resolution.test.ts` exists
    - File contains `describe('handleResolution'` with at least 6 test cases
    - File contains `describe('handlePostmortem'` with at least 4 test cases
    - File contains `describe('classifyOutcome'` with at least 3 test cases
    - File contains `describe('getTemporalPensieve'` with at least 2 test cases
    - Running `npx vitest run src/decisions/__tests__/resolution.test.ts` exits with non-zero (all RED)
  </acceptance_criteria>
  <done>16 failing test cases exist in resolution.test.ts covering handleResolution, handlePostmortem, classifyOutcome, and getTemporalPensieve</done>
</task>

<task type="auto">
  <name>Task 2: Create engine-resolution.test.ts and sweep-escalation.test.ts RED scaffolds</name>
  <files>src/decisions/__tests__/engine-resolution.test.ts, src/proactive/__tests__/sweep-escalation.test.ts</files>
  <read_first>
    - src/decisions/__tests__/engine-capture.test.ts (existing PP#0 test pattern for engine integration)
    - src/chris/engine.ts (processMessage PP#0 lines 163-192)
    - src/proactive/sweep.ts (accountability channel lines 81-160)
    - src/proactive/state.ts (getValue/setValue/deleteKey)
    - src/proactive/__tests__/ (check for existing sweep test patterns)
  </read_first>
  <action>
**File 1: `src/decisions/__tests__/engine-resolution.test.ts`**

Import stubs that do not yet exist:
```typescript
import { handleResolution, handlePostmortem } from '../resolution.js';
```

Test cases:

**describe('PP#0 resolution routing')**:
1. `it('routes AWAITING_RESOLUTION message to handleResolution')` — set up an AWAITING_RESOLUTION row in decisionCaptureState, call processMessage with user text. Assert handleResolution was called (mock it) and normal mode detection was NOT called.
2. `it('routes AWAITING_POSTMORTEM message to handlePostmortem')` — set up an AWAITING_POSTMORTEM row, call processMessage. Assert handlePostmortem was called.
3. `it('skips abort-phrase check for AWAITING_RESOLUTION stage')` — AWAITING_RESOLUTION rows have draft={}, so language_at_capture is undefined. Assert that the abort-phrase code path does not crash with undefined language. (Per Pitfall 1: abort-phrase uses draft.language_at_capture which is empty for resolution stages — the resolution handler must handle this.)
4. `it('falls through to normal mode when no active capture exists')` — no capture row. Call processMessage. Assert normal mode detection runs.

**File 2: `src/proactive/__tests__/sweep-escalation.test.ts`**

Import stubs that do not yet exist:
```typescript
import { getEscalationSentAt, getEscalationCount, setEscalationSentAt, setEscalationCount, clearEscalationKeys } from '../../proactive/state.js';
```

Test cases:

**describe('sweep escalation')**:
1. `it('records first prompt timestamp in proactive_state')` — after sweep fires an accountability prompt, assert `getEscalationSentAt(decisionId)` returns a Date.
2. `it('fires second prompt after 48h of no reply')` — set escalation count=1 and sentAt=50h ago. Run sweep. Assert a second message is sent to Telegram and escalation count becomes 2.
3. `it('transitions to stale after 2 non-replies')` — set escalation count=2 and sentAt=50h ago. Run sweep. Assert transitionDecision was called with ('due', 'stale'). Assert clearCapture was called.
4. `it('does not fire escalation within 48h window')` — set escalation count=1 and sentAt=24h ago. Run sweep. Assert NO second message sent.
5. `it('second prompt text acknowledges follow-up')` — assert second prompt text contains "couple days ago" or equivalent follow-up acknowledgment (not a robotic repeat).
6. `it('stale transition is silent — no message sent')` — set count=2, sentAt=50h ago. Run sweep. Assert bot.api.sendMessage was NOT called for this decision after stale transition (per D-17).
7. `it('escalation bypasses daily accountability cap')` — set hasSentTodayAccountability=true, but escalation count=1 with sentAt=50h ago. Assert escalation still fires despite the daily cap (per Pitfall 4).
8. `it('clearCapture called on stale transition')` — assert clearCapture(chatId) is called when transition to stale occurs (per Pitfall 5).

All tests use `vi.mock` for Anthropic client and Telegram bot. Database-backed where needed (proactive_state KV operations).

Mark every test to fail (import non-existent modules). Goal is RED.
  </action>
  <verify>
    <automated>cd /home/claude/chris && npx vitest run src/decisions/__tests__/engine-resolution.test.ts src/proactive/__tests__/sweep-escalation.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/decisions/__tests__/engine-resolution.test.ts` exists with `describe('PP#0 resolution routing'` and at least 4 test cases
    - File `src/proactive/__tests__/sweep-escalation.test.ts` exists with `describe('sweep escalation'` and at least 7 test cases
    - Running both test files exits with non-zero (all RED)
  </acceptance_criteria>
  <done>12 additional failing test cases exist across engine-resolution.test.ts (4 tests) and sweep-escalation.test.ts (8 tests) covering PP#0 routing and sweep escalation behaviors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 creates test files only; no production code, no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00 | N/A | Test scaffolds | accept | Wave 0 is test-only; no attack surface introduced |
</threat_model>

<verification>
All three test files exist and all tests fail (RED). No production code is modified. `npx vitest run src/decisions/__tests__/resolution.test.ts src/decisions/__tests__/engine-resolution.test.ts src/proactive/__tests__/sweep-escalation.test.ts` exits non-zero with all tests failing.
</verification>

<success_criteria>
- 28+ failing test stubs across 3 files
- No production code changed
- Test files follow existing project patterns (vi.mock, Docker Postgres where needed)
- All imports reference functions that will be created in Plans 02-05
</success_criteria>

<output>
After completion, create `.planning/phases/16-resolution-post-mortem-accountability-mode/16-01-SUMMARY.md`
</output>
