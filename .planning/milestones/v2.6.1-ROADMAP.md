# v2.6.1 Code Review Cleanup — Archived Roadmap

**Status:** SHIPPED 2026-05-15
**Tag:** v2.6.1
**Phases:** 41-47 (7 phases, 17 plans, 39/39 requirements)
**Audit:** [.planning/milestones/v2.6.1-MILESTONE-AUDIT.md](./v2.6.1-MILESTONE-AUDIT.md) — PASSED

## Genesis

Triggered 2026-05-14 by a live UX defect at 17:00 Paris: the adjustment-dialogue prompt asserted "This daily daily_journal ritual isn't working" to Greg (FR locale). A 14-phase parallel code-review sweep surfaced 45 BLOCKERs + 97 WARNINGs across v2.3 → v2.6 phases. v2.6.1 organized 39 of those findings into 9 thematic categories (T1–T8 + display polish), planned across 7 phases, and shipped them in a wave-based execution model.

## Phases

- [x] **Phase 41: Adjustment-Dialogue Rework** — Live UX defect fix + skip_count reset + Haiku-whitelist privilege escalation closure (shipped 2026-05-15; 8 commits)
- [x] **Phase 42: Atomicity & Race Fixes** — Six independent production-correctness fixes sharing a concurrent-invocation test harness (shipped 2026-05-15; 9 commits)
- [x] **Phase 43: Inference Security & Contract Enforcement** — Prompt-injection escaping + "host computes, you don't emit" contract restoration across operational + psychological inference (shipped 2026-05-15; 11 commits)
- [x] **Phase 44: CI Milestone-Gate Hardening** — REQUIRE_FIXTURES env-gated hard-fail replaces silent skip across M009/M010/M011 gates (shipped 2026-05-15; 13 commits, 10 test files + scripts/test.sh + REQUIREMENTS.md)
- [x] **Phase 45: Schema Hygiene & Fixture-Pipeline Cleanup** — Phase 33 seed defaults backfill (root cause) + DB CHECK constraints (defense-in-depth) + operator-script + fixture-pipeline cleanup (shipped 2026-05-15; ~18 commits across 4 plans)
- [x] **Phase 46: FR/RU Localization Comprehensive** — Locale detection layer + 30+ EN-only sites localized across adjustment-dialogue, weekly-review, journal, /profile (shipped 2026-05-15; 11 commits)
- [x] **Phase 47: Display Polish** — Schwartz circumplex ordering + HEXACO × Schwartz cross-validation observations on /profile (shipped 2026-05-15; 4 commits)

## Phase Details

### Phase 41: Adjustment-Dialogue Rework
**Goal**: Stop the every-tick re-fire after threshold, eliminate the wrongful "isn't working" assertion + slug-leak copy from the live UX defect, and close the Haiku-whitelist privilege escalation around `mute_until`
**Depends on**: Nothing (top priority — live bug actively re-firing on Greg's account)
**Requirements**: ADJ-01, ADJ-02, ADJ-03, ADJ-04, ADJ-05, ADJ-06, ADJ-07
**Plans**: 2 (41-01 P0 live-fix ships ADJ-01/02/04; 41-02 closes ADJ-03/05/06/07)
**Outcome**: 7/7 requirements delivered. Observational copy replaces "isn't working"; `src/rituals/display-names.ts` constant map eliminates slug exposure; skip_count resets on yes/no/refusal completion paths (5 sites); `mute_until` removed from Haiku-controllable whitelist; per-field type validation on `confirmConfigPatch`; integration test asserts no re-fire next tick.

### Phase 42: Atomicity & Race Fixes
**Goal**: Eliminate six independent atomicity/race defects in the ritual-scheduling, wellbeing, and weekly-review code paths
**Depends on**: Nothing
**Requirements**: RACE-01, RACE-02, RACE-03, RACE-04, RACE-05, RACE-06
**Plans**: 3 (42-01 shared harness + RACE-01/02; 42-02 wellbeing RACE-03/04/05; 42-03 weekly-review RACE-06)
**Outcome**: 6/6 requirements delivered. M009 second-fire bug class permanently closed via `sql\`now()\`` SET clause; `ritualResponseWindowSweep` paired-insert transactional; wellbeing rapid-tap idempotent via completion-claim UPDATE; jsonb_set merge on skip path; DST-edge 24h cutoff on findOpenWellbeingRow; weekly-review transactional fire (Telegram failure rolls back state). Shared `src/__tests__/helpers/concurrent-harness.ts` helper consumed by 4 of 6 fixes.

### Phase 43: Inference Security & Contract Enforcement
**Goal**: Close prompt-injection surface on both operational + psychological inference prompts, restore the "host computes, Sonnet doesn't emit" contract, and persist Sonnet's data_consistency field
**Depends on**: Nothing
**Requirements**: INJ-01, INJ-02, CONTRACT-01, CONTRACT-02, CONTRACT-03
**Plans**: 2 (43-01 INJ-01/02 sanitizeSubstrateText; 43-02 CONTRACT-01/02/03 + migration 0014)
**Outcome**: 5/5 requirements delivered. `sanitizeSubstrateText` helper escapes `## ` line-start anchors + neutralizes backticks; injection-attack fixtures at `src/memory/__tests__/fixtures/injection-attacks.ts`; `stripMetadataColumns` removes `dataConsistency`; `extract<X>PrevState` returns null on first-fire (substrateHash === ''); migration `0014_psychological_data_consistency_column` adds `data_consistency real NOT NULL DEFAULT 0` (CHECK 0..1) to profile_hexaco/schwartz/attachment.

### Phase 44: CI Milestone-Gate Hardening
**Goal**: Replace silent-skip pattern across M009/M010/M011 milestone-gate tests with environment-gated hard-fail
**Depends on**: Phase 45 (FIX-02 path bug must ship first; planning ran parallel, execution gated)
**Requirements**: CI-01, CI-02, CI-03
**Plans**: 1 (44-01 — 12 tasks: 10 test files + scripts/test.sh + REQUIREMENTS.md)
**Outcome**: 3/3 requirements delivered. `REQUIRE_FIXTURES=1` env-gated single failing test per fixture; inline per-file (no helper); 12 `[CI-GATE] fixture present` describe blocks injected across 10 test files (M009 ×3, M010 ×4, M011 ×3); Family C orthogonality verified (no paid Anthropic call from CI gate).

### Phase 45: Schema Hygiene & Fixture-Pipeline Cleanup
**Goal**: Root-cause the M010 schema_mismatch warns via Phase 33 seed-defaults backfill, add defense-in-depth DB CHECK constraints, clean up the heterogeneous fixture-pipeline + operator-script defects
**Depends on**: Nothing (internal Wave A → B → C DAG)
**Requirements**: SCHEMA-01, SCHEMA-02, FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06, FIX-07, FIX-08
**Plans**: 4 (45-01 SCHEMA-01/migration 0015; 45-02 FIX-01..05/07/08; 45-03 SCHEMA-02/migration 0016; 45-04 FIX-06 fixture refresh)
**Outcome**: 10/10 requirements delivered. Migration `0015_psychological_check_constraints` (19 CHECK constraints, defense-in-depth); migration `0016_phase33_seed_defaults_backfill` (UPDATE seed rows + ALTER COLUMN SET DEFAULT for wellbeing_trend + parent_care_responsibilities); contradictions FK pre-filter at synthesize-delta:934; bias-prompt `phrasesClause` decoupled from `dimensionHint`; output-dir path bug fixed (`m011-1000words-5days`); migration list derived from glob; SSH `StrictHostKeyChecking=accept-new` + repo-vetted `scripts/.ssh-known-hosts`; pgvector(1024) coercion via staging-table CAST; M010 operational fixtures refreshed (FIX-06 ran `npx tsx scripts/regenerate-primed.ts --milestone m010 --force` in ~75s); HARN word-count uses calendar-month-windowed count; AbortController + `process.exitCode = 130` SIGINT pattern across 3 operator scripts.

### Phase 46: FR/RU Localization Comprehensive
**Goal**: Build the locale-strings layer once and apply it across all 30+ EN-only sites
**Depends on**: Phase 41 (display-names pattern precedent; runtime API is `src/chris/language.ts` which both reuse)
**Requirements**: L10N-01, L10N-02, L10N-03, L10N-04, L10N-05, L10N-06
**Plans**: 4 (46-01 shared infra; 46-02 /profile 21 sites; 46-03 weekly-review; 46-04 daily journal)
**Outcome**: 6/6 requirements delivered. New `src/chris/locale/strings.ts` exporting `qualifierFor(c, lang)` + `LANG_QUALIFIER_BANDS` + `normalizeForInterrogativeCheck`; `qualifierFor` consolidated from 2 duplicates to 1 canonical home; 21 EN-only sites in `bot/handlers/profile.ts` localized (qualifier + HEXACO 6 dims + Schwartz 10 dims + score template); `WEEKLY_REVIEW_HEADER` per-locale; FR regex curly-apostrophe + NFC normalize fix; `TEMPLATED_FALLBACK` per-locale; daily journal PROMPTS locale-aware.

### Phase 47: Display Polish
**Goal**: Add the two user-visible v2.6.1 surface improvements — Schwartz circumplex order + HEXACO × Schwartz cross-validation observations
**Depends on**: Phase 46 (locale-aware qualifierFor)
**Requirements**: DISP-01, DISP-02
**Plans**: 1 (47-01 — 6 tasks including Phase-46-dependency check, both DISP reqs, golden test, Phase 39 snapshot churn)
**Outcome**: 2/2 requirements delivered. `SCHWARTZ_CIRCUMPLEX_ORDER` canonical clockwise 10-element array (opposing pairs at ring distance ≥4); `CROSS_VALIDATION_RULES` hardcoded 16-rule table (preserves reader-never-throw — no Sonnet at /profile read path); `computeCrossValidationObservations` pure function; locale-aware (EN/FR/RU); 0.3 confidence floor; observational tone on negative observations.

## Key Decisions Locked Across the Milestone

- **Migration sequencing:** 0014 (Phase 43 CONTRACT-03) → 0015 (Phase 45 SCHEMA-01) → 0016 (Phase 45 SCHEMA-02). Journal `_journal.json` monotonic.
- **Reader never throws:** Phase 47 chose hardcoded rule table over Sonnet-at-call-time for cross-validation observations to preserve Phase 39 D-22 reader-never-throw contract; persist-at-inference was logically equivalent so the simpler option won.
- **No new locale-detection layer:** Phase 46 reuses existing `src/chris/language.ts` (Lang union + `langOf` + `getLastUserLanguageFromDb`); the new `src/chris/locale/strings.ts` is for cross-file shared registers only.
- **`MSG`-shape pattern:** Slug-keyed `Record<Lang, T>` co-located with consumer (mirrors Phase 39 `MSG` + Phase 41 `display-names.ts` + Phase 46 various). String storage convention codified.
- **Display-name discipline:** Phase 41's `RITUAL_DISPLAY_NAMES` + `CONFIG_FIELD_LABELS` maps replace slug exposure in user-facing copy; precedent for any future ritual-name-aware surface.

## Notable Inline Recoveries

- **Plan 45-03 mid-plan rate-limit:** Sibling `gsd-executor` agent hit API rate limit after writing only the migration SQL. Parent orchestrator completed Tasks 2-4 inline (snapshot, journal entry, test.sh wiring, regen sentinels, integration test) — 4/4 cases green.
- **Migration-number conflict:** Phase 43's discuss-phase agent (running first chronologically) explicitly claimed slot 0014 for `data_consistency` column; Phase 45's parallel discuss-phase agent unaware, originally claimed 0014+0015. Reconciled by parent to 0014/0015/0016.
- **Phase 46 plan-checker BLOCKER (L10N-03c regression false-positive):** Test passed under both old and new FR regex; replaced with direct `INTERROGATIVE_REGEX.match() === null` assertion + new L10N-03c2 multi-interrogative test that distinguishes the two regex versions via `stage1Check` behavior.
- **Phase 47 distance-5 invariant relaxed:** Original CONTEXT.md narrative claimed opposing Schwartz pairs at strict ring distance 5; geometry only matches 3/5 dipoles at strict distance 5 (the other 2 at distance 4). Relaxed to `≥ 4` + separate test locks the exact antipodal count.

## Deferred to v2.6.2 / v2.7+

- **PTEST-03 three-cycle date-window fragility** — test pins NOW assuming synthetic data range is strictly before NOW_C1; fresh regen breaks the assumption.
- **psychological-profiles.test.ts:171 test-injection** — deliberately injects `{score: 'not-a-number'}`; migration 0015 CHECK constraint now correctly rejects this at DB boundary. Test needs to use a different invalid value.
- **M011 HARN gates regen** — Plan 45-04 only ran `--milestone m010 --force`; M011 fixtures need separate `--milestone m011` regen run.
- **Plan 41 plan-check warnings** — minor signature/wording issues resolved inline during execution; no remaining work.
- **FR/RU translation review pass** — 20-row review table added to 46-02 SUMMARY for Greg's `/gsd-verify-work` pass when convenient.
- **CONS-01, CONS-02, ATT-POP-01, NARR-01, SAT-CAL-01** — original M011 carry-forwards still pending empirical data + downstream activation triggers (now unblocked by CONTRACT-03 column).

## Verdict

**v2.6.1 is COMPLETE.** All 39 requirements delivered with code-level evidence. Cross-phase integration verified (8/8 wiring checks WIRED; 2/2 E2E flows wired). Ready for archive + tag + deploy.
