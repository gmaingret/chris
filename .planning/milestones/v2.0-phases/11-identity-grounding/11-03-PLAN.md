---
phase: 11
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md
autonomous: false
requirements:
  - TEST-03
  - RETR-04
tags: [validation, live-integration, gate]

must_haves:
  truths:
    - "TEST-03 (the 3 JOURNAL grounding tests in live-integration.test.ts) passes 3-of-3 cases on three CONSECUTIVE clean runs of the full Docker test suite"
    - "TEST-07 (structured fact accuracy / INTERROGATE citations) does NOT regress — INTERROGATE still emits date-cited responses"
    - "No other live-integration test (TEST-01/02/04/05/06/08/09) regresses below its current pass rate"
    - "Run-by-run results are recorded in 11-TEST-03-RUNS.md so the gate is auditable, not just declared"
  artifacts:
    - path: ".planning/phases/11-identity-grounding/11-TEST-03-RUNS.md"
      provides: "Audit log of three consecutive ./scripts/test.sh runs with TEST-03 pass/fail per case"
      contains: "Run 1"
  key_links:
    - from: "11-TEST-03-RUNS.md"
      to: "ROADMAP.md Phase 11 success criterion 4"
      via: "documented 3-of-3-on-3-consecutive-runs evidence"
      pattern: "Run 3.*PASS"
---

<objective>
Validate the Phase 11 fix end-to-end against real Sonnet + real Haiku + real
Docker postgres. Run the full test suite three times consecutively and confirm
TEST-03 (JOURNAL grounding) passes all 3 cases on every run. Capture each run's
output as evidence so the M006 audit can verify the gate without re-running.

Purpose: ROADMAP success criterion 4 ("`live-integration.test.ts` TEST-03 passes
3-of-3 on three consecutive clean runs") is non-deterministic — Sonnet is
stochastic. A single green run is not sufficient. Three consecutive green runs
gives high confidence the identity-split regression is closed.

Output: 11-TEST-03-RUNS.md with three timestamped run records and a final
GREEN/RED gate status. This plan has one human-checkpoint task because (a) live
LLM runs cost money and the executor should pause for user permission before
spending, and (b) the executor must visually verify the run records were
captured correctly before declaring the gate satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-identity-grounding/11-RESEARCH.md
@.planning/phases/11-identity-grounding/11-VALIDATION.md
@.planning/phases/11-identity-grounding/11-01-SUMMARY.md
@.planning/phases/11-identity-grounding/11-02-SUMMARY.md

@src/chris/__tests__/live-integration.test.ts
@scripts/test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Docker test suite three consecutive times and capture per-run TEST-03 outcomes</name>
  <files>.planning/phases/11-identity-grounding/11-TEST-03-RUNS.md</files>
  <read_first>
    - src/chris/__tests__/live-integration.test.ts (lines 357-504 — the three TEST-03 it() blocks)
    - scripts/test.sh (full file — confirms Docker postgres lifecycle)
    - .planning/phases/11-identity-grounding/11-VALIDATION.md (Sampling Rate → "Phase gate" definition)
    - .planning/phases/11-identity-grounding/11-02-SUMMARY.md (confirm the rename + wiring landed)
  </read_first>
  <action>
    Pre-flight checks (do these before spending API budget):
    1. Confirm `ANTHROPIC_API_KEY` is exported in the shell (env requirement of test.sh).
    2. Confirm Docker daemon is running: `docker info >/dev/null 2>&1 && echo OK`.
    3. Confirm `docker-compose.local.yml` is present at repo root.
    4. Confirm Plan 11-02 landed: `grep -c '\bJohn\b' src/chris/personality.ts` returns 0 AND `grep -F 'includeDate: false' src/chris/modes/journal.ts` matches.

    Then execute three consecutive runs. Each run is the FULL Docker-backed
    suite — per user memory, no shortcuts:

    ```
    ./scripts/test.sh src/chris/__tests__/live-integration.test.ts -t "JOURNAL grounding"
    ```

    (Scoping the run to the JOURNAL grounding describe block keeps cycle time
    manageable while still spinning up real Docker postgres + real Anthropic
    calls. The full suite ran in Plan 11-02 Task 3 already; this gate is
    specifically about TEST-03 stability.)

    For EACH of the three runs, record in
    `.planning/phases/11-identity-grounding/11-TEST-03-RUNS.md`:
    - Timestamp (ISO).
    - Vitest summary line ("Test Files X passed, Tests Y passed").
    - Per-it-block status: 'grounds response in seeded nationality fact' / 'seeded location fact' / 'seeded business fact' — PASS or FAIL.
    - On any FAIL: capture the haikuJudge `reason` substring from the failure output.
    - Estimated cost (rough — Sonnet calls × tokens, can be a single line note).

    Use this file template for 11-TEST-03-RUNS.md:

    ```markdown
    # Phase 11 — TEST-03 Gate Run Log

    **Gate definition (ROADMAP success criterion 4):** TEST-03's three JOURNAL
    grounding cases must pass 3-of-3 on three CONSECUTIVE clean runs of the
    full Docker test suite.

    ## Run 1
    - Timestamp: <ISO>
    - Command: `./scripts/test.sh src/chris/__tests__/live-integration.test.ts -t "JOURNAL grounding"`
    - Vitest summary: <paste>
    - nationality: PASS|FAIL  (reason if fail: <...>)
    - location: PASS|FAIL
    - business: PASS|FAIL

    ## Run 2
    (same shape)

    ## Run 3
    (same shape)

    ## Gate Status
    - 3-of-3 on Run 1: yes|no
    - 3-of-3 on Run 2: yes|no
    - 3-of-3 on Run 3: yes|no
    - **GATE: GREEN | RED**
    ```

    Between runs, confirm Docker is fully torn down (test.sh's trap handles
    this) before starting the next run — clean state per gate definition.

    If any run is < 3-of-3:
    1. Record it faithfully in 11-TEST-03-RUNS.md (do NOT cherry-pick).
    2. Stop the gate sequence — DO NOT continue running until the regression is
       diagnosed.
    3. Diagnose via the haikuJudge reason and Pitfall 1 (residual "John" in any
       prompt surface) or Pitfall 4 (date leak from another source). Do NOT fix
       in this plan — surface to the user via the Task 2 checkpoint with a
       concrete remediation proposal.

    If all three runs are 3-of-3:
    - Set `GATE: GREEN` in the file.
    - Proceed to Task 2 for human verification.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md && grep -c "Run 3" .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-identity-grounding/11-TEST-03-RUNS.md` exists.
    - File contains exactly three `## Run N` sections (Run 1, Run 2, Run 3) — `grep -c '^## Run' 11-TEST-03-RUNS.md` returns 3.
    - Each run section records timestamp + per-test PASS/FAIL.
    - File contains a final `## Gate Status` section with `GATE: GREEN` or `GATE: RED`.
    - If GATE: GREEN, all three "3-of-3 on Run N" lines say "yes".
    - If GATE: RED, the file documents which run(s) failed, which case(s), and the haikuJudge reason — and Task 2 is invoked WITHOUT continuing to declare the phase green.
  </acceptance_criteria>
  <done>
    Three runs executed and faithfully recorded; gate status reflects reality.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies the run log and approves Phase 11 closure (or routes to gap-closure)</name>
  <files>.planning/phases/11-identity-grounding/11-TEST-03-RUNS.md (read-only verification)</files>
  <read_first>
    - .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md (full file produced by Task 1)
    - .planning/ROADMAP.md (Phase 11 success criteria — verify all 4 are now satisfiable)
  </read_first>
  <what-built>
    - 11-TEST-03-RUNS.md with three consecutive full-suite run records.
    - Renamed identity surfaces (Plans 11-01, 11-02 already landed) verified by 3-of-3 stability OR an honest RED gate with diagnosis.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/11-identity-grounding/11-TEST-03-RUNS.md` end to end.
    2. Verify each run's "Vitest summary" line is consistent with the per-test PASS/FAIL records (3 passed = nationality+location+business all PASS).
    3. Confirm timestamps are within a reasonable window of each other (runs were genuinely consecutive, not weeks apart).
    4. Confirm the GATE status line at the bottom reflects the three runs accurately.
    5. (Optional sanity) Re-run TEST-03 once yourself: `./scripts/test.sh src/chris/__tests__/live-integration.test.ts -t "JOURNAL grounding"`. Not part of the gate — just trust verification.
    6. If GATE: GREEN — approve Phase 11 closure.
    7. If GATE: RED — do NOT approve. Direct executor to open a new Phase 11 plan or run `/gsd-plan-phase 11 --gaps`.
  </how-to-verify>
  <action>
    Pause for user review per `<how-to-verify>` steps above. The executor MUST NOT
    update STATE.md, ROADMAP.md, or REQUIREMENTS.md until the user types "approved".
    On "approved" + GATE: GREEN, proceed to the `<output>` section's state updates.
    On any other response, treat the gate as not yet satisfied and follow the
    user's direction (new plan, gap-closure, etc.).
  </action>
  <verify>
    <automated>grep -F 'GATE: GREEN' .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md</automated>
  </verify>
  <acceptance_criteria>
    - User has read 11-TEST-03-RUNS.md.
    - `grep -F 'GATE: GREEN' 11-TEST-03-RUNS.md` matches one line (or the user explicitly approved a RED-gate remediation path).
    - User has typed "approved" (or equivalent) in response to the resume-signal prompt.
  </acceptance_criteria>
  <done>
    User has approved Phase 11 closure (GREEN gate) OR directed a remediation path (RED gate). Executor proceeds only on explicit approval.
  </done>
  <resume-signal>Type "approved" to close Phase 11, or describe the issue and how to proceed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API | TEST-03 sends real conversation snippets to Sonnet and judge prompts to Haiku. Existing trust relationship; no new data classes introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Repudiation | TEST-03 gate result | mitigate | Run results captured to a versioned file (11-TEST-03-RUNS.md) committed to git. Vitest summary lines included so claims are auditable. |

No other STRIDE categories activate.
</threat_model>

<verification>
Final phase-level check after Task 2 approval:

1. `cat .planning/phases/11-identity-grounding/11-TEST-03-RUNS.md | grep -F 'GATE: GREEN'` returns 1 match.
2. The full suite runs cleanly: `./scripts/test.sh` exits 0.
3. ROADMAP.md Phase 11 success criteria 1-4 all check out:
   - Crit 1: `grep -c '\bJohn\b' src/llm/prompts.ts | head` shows occurrences only in classifier prompts.
   - Crit 2: `grep -F 'Facts about you (Greg)' src/chris/personality.ts` matches.
   - Crit 3: `grep -F 'includeDate: false' src/chris/modes/journal.ts` matches.
   - Crit 4: 11-TEST-03-RUNS.md GATE: GREEN.
</verification>

<success_criteria>
- 11-TEST-03-RUNS.md exists with 3 honest run records.
- GATE: GREEN OR a RED gate with documented diagnosis routed to follow-up plan.
- No regression in TEST-07 (INTERROGATE citations) or other live-integration tests.
- Human checkpoint approved.
</success_criteria>

<output>
After approval, create `.planning/phases/11-identity-grounding/11-03-SUMMARY.md`
documenting:
- Final gate status (GREEN/RED).
- Reference to 11-TEST-03-RUNS.md as the audit trail.
- If GREEN: confirmation that all 4 ROADMAP success criteria are satisfied.
- If RED: link to the follow-up plan or gap-closure invocation.

Then update `.planning/STATE.md` and `.planning/ROADMAP.md`:
- ROADMAP: mark Phase 11 plans as `[x]`, mark Phase 11 status as Complete.
- ROADMAP: mark RETR-01, RETR-02, RETR-04, TEST-03 as Satisfied in the
  Traceability table.
- REQUIREMENTS.md: tick the four `- [ ]` boxes (RETR-01, RETR-02, RETR-04, TEST-03).
- STATE.md: bump completed_phases / completed_plans counters.
</output>
