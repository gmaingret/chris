---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/llm/prompts.ts
  - src/chris/personality.ts
  - src/proactive/prompts.ts
  - src/chris/modes/journal.ts
autonomous: true
requirements:
  - RETR-01
  - RETR-02
  - RETR-04
tags: [prompt-engineering, identity, persona]

must_haves:
  truths:
    - "Every user-facing mode prompt addresses the user as 'Greg' — the literal token 'John' (whole word, case-sensitive) does not appear in any of the 6 mode templates, the constitutional preamble, the proactive prompt, or the buildKnownFactsBlock header"
    - "buildKnownFactsBlock emits header '## Facts about you (Greg)' followed by an explanatory sentence asserting that 'Greg' in the facts refers to the user, not a third party"
    - "JOURNAL call site invokes buildPensieveContext with { includeDate: false } so retrieved entries do not carry today's date prefix into the system prompt"
    - "INTERROGATE remains untouched at the call site — it continues to receive date-prefixed entries (default behavior) so its citation pattern (\"back on March 15th, you mentioned...\") still works"
    - "Pre-staged failing tests from Plan 11-01 turn green; the full unit suite passes; the full Docker-backed integration suite passes"
  artifacts:
    - path: "src/llm/prompts.ts"
      provides: "Renamed user-facing mode prompts (JOURNAL, INTERROGATE, REFLECT, COACH, PSYCHOLOGY, PRODUCE)"
      contains: "Greg"
    - path: "src/chris/personality.ts"
      provides: "Renamed CONSTITUTIONAL_PREAMBLE + new buildKnownFactsBlock header + explanatory sentence + renamed declinedTopics line"
      contains: "Facts about you (Greg)"
    - path: "src/proactive/prompts.ts"
      provides: "Renamed PROACTIVE_SYSTEM_PROMPT (consistency)"
      contains: "Greg"
    - path: "src/chris/modes/journal.ts"
      provides: "JOURNAL call site passes { includeDate: false } to buildPensieveContext"
      contains: "includeDate: false"
  key_links:
    - from: "src/chris/modes/journal.ts:37"
      to: "src/memory/context-builder.ts buildPensieveContext"
      via: "{ includeDate: false } option"
      pattern: "includeDate: false"
    - from: "src/chris/personality.ts buildKnownFactsBlock"
      to: "JOURNAL/INTERROGATE system prompt body"
      via: "string concatenation in buildSystemPrompt (line 119)"
      pattern: "Facts about you \\(Greg\\)"
---

<objective>
Unify Chris's persona identity on "Greg" across all six user-facing mode prompts,
the constitutional preamble, the proactive prompt, the buildKnownFactsBlock
header (with an anti-split explanatory sentence), and the declinedTopics line.
Wire the JOURNAL call site to suppress today's-date prefixes on retrieved
Pensieve entries via the `{ includeDate: false }` option that Plan 11-01 added.

Purpose: Eliminate the three-way identity split (Greg-in-fact / John-as-addressee
/ Greg-in-seed) that causes the TEST-03 "coincidence" hallucination class. After
this plan: a JOURNAL Sonnet call sees a single name (Greg) for the user across
the preamble, the mode body, the Known Facts block, AND the retrieved entries
themselves — and no longer sees today's date stamped onto seeded facts.

Output: 4 source files edited, 0 user-facing strings still containing the literal
"John", unit suite green (including the Plan 11-01 pre-staged tests), Docker
integration suite green. INTERROGATE citation pattern preserved (TEST-07 still
green).

Decision references:
- Honors locked decision: hard-code "Greg", do NOT parameterize (per RESEARCH.md
  "Locked Decisions" + D009 single-user).
- Honors locked decision: date suppression is per-call, NOT global (INTERROGATE
  needs dates; JOURNAL does not).
- Honors deferred scope: classifier prompts (MODE_DETECTION, RELATIONAL_MEMORY,
  CONTRADICTION_DETECTION, MUTE_DETECTION) are NOT renamed in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-identity-grounding/11-RESEARCH.md
@.planning/phases/11-identity-grounding/11-VALIDATION.md
@.planning/phases/11-identity-grounding/11-01-SUMMARY.md

@src/llm/prompts.ts
@src/chris/personality.ts
@src/proactive/prompts.ts
@src/chris/modes/journal.ts

<interfaces>
<!-- Plan 11-01 landed these contracts: this plan consumes them -->

From src/memory/context-builder.ts (DO NOT MODIFY in this plan):
```typescript
export interface PensieveContextOptions {
  includeDate?: boolean;  // default true
}
export function buildPensieveContext(
  results: SearchResult[],
  opts?: PensieveContextOptions,
): string;
```

From src/chris/personality.ts (this plan's edit target):
- `CONSTITUTIONAL_PREAMBLE`: 3 occurrences of "John" → "Greg"
- `buildKnownFactsBlock`: header line + add an explanatory sentence as line 2
- `buildSystemPrompt` declinedTopics interpolation (line 128): `\`(John said: "${dt.originalSentence}")\`` → `\`(Greg said: "${dt.originalSentence}")\``
- `formatContradictionNotice`: read first to confirm no "John" reference; if found, rename.

From src/llm/prompts.ts (this plan's edit target):
6 user-facing templates, ~35 occurrences of "John" total to rename to "Greg":
- JOURNAL_SYSTEM_PROMPT (15)
- INTERROGATE_SYSTEM_PROMPT (~7)
- REFLECT_SYSTEM_PROMPT
- COACH_SYSTEM_PROMPT
- PSYCHOLOGY_SYSTEM_PROMPT
- PRODUCE_SYSTEM_PROMPT
DO NOT touch: MODE_DETECTION_PROMPT, RELATIONAL_MEMORY_PROMPT,
CONTRADICTION_DETECTION_PROMPT, MUTE_DETECTION_PROMPT — these are classifier
prompts that never reach the user; renaming them is deferred (RESEARCH.md
Pitfall 3).

From src/proactive/prompts.ts (this plan's edit target):
- PROACTIVE_SYSTEM_PROMPT: 7 occurrences of "John" → "Greg".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename "John" → "Greg" in personality.ts (preamble + facts header + declined-topics line) and add anti-split explanatory sentence</name>
  <files>src/chris/personality.ts</files>
  <read_first>
    - src/chris/personality.ts (full file — CONSTITUTIONAL_PREAMBLE line 28-38, buildKnownFactsBlock line 45-55, declinedTopics line 128, formatContradictionNotice line 144)
    - src/chris/__tests__/personality.test.ts (the Plan 11-01 pre-staged failing tests at the bottom describe `'Identity grounding (Phase 11 / RETR-01, RETR-02)'`)
    - .planning/phases/11-identity-grounding/11-RESEARCH.md (Code Examples → "Recommended shape post-fix" for buildKnownFactsBlock)
    - .planning/phases/11-identity-grounding/11-01-SUMMARY.md (exact strings the failing tests expect)
  </read_first>
  <behavior>
    - The CONSTITUTIONAL_PREAMBLE template string addresses the user as "Greg" in all 3 prior occurrences of "John".
    - buildKnownFactsBlock's first emitted line is `## Facts about you (Greg)`.
    - buildKnownFactsBlock's second emitted line is the anti-split explanatory sentence (single line, no newlines inside): `These are authoritative facts about you, the person Chris is talking to. Treat any reference to "Greg" in these facts as referring to you — not a third party.`
    - Subsequent lines (the `- key: value` entries) remain unchanged.
    - The declinedTopics formatting line in buildSystemPrompt now reads `(Greg said: "...")` instead of `(John said: "...")`.
    - formatContradictionNotice — if it contains "John", rename; if not, no change.
    - `buildSystemPrompt('JOURNAL', 'ctx').includes('John')` → false.
    - `buildSystemPrompt('JOURNAL', 'ctx').includes('useful to Greg')` → true.
  </behavior>
  <action>
    1. Open `src/chris/personality.ts`.
    2. In `CONSTITUTIONAL_PREAMBLE` (line 28-38), make these EXACT three substitutions:
       - `Your job is to be useful to John, not pleasant.` → `Your job is to be useful to Greg, not pleasant.`
       - `Never tell John he is right because of who he is.` → `Never tell Greg he is right because of who he is.`
       - `Never optimize for John's emotional satisfaction` → `Never optimize for Greg's emotional satisfaction`
    3. In `buildKnownFactsBlock` (line 45-55):
       - Change the initial `lines` array from
         `const lines: string[] = ['## Known Facts About John'];`
         to
         ```typescript
         const lines: string[] = [
           '## Facts about you (Greg)',
           'These are authoritative facts about you, the person Chris is talking to. Treat any reference to "Greg" in these facts as referring to you — not a third party.',
         ];
         ```
       - Update the JSDoc comment above the function (lines 40-44) to reference "Greg" instead of "John".
    4. In `buildSystemPrompt` (line 128), change
       `\`- "${dt.topic}" (John said: "${dt.originalSentence}")\``
       to
       `\`- "${dt.topic}" (Greg said: "${dt.originalSentence}")\``
    5. In `formatContradictionNotice` (line 144 onward) — read the function body. If any "John" appears in returned text, rename to "Greg". (Inspection of current code shows the notice does not embed the user's name explicitly; if so, no change required. Document either way in the commit body.)
    6. Do NOT modify the import block, the `ChrisMode` type, the `DeclinedTopic` interface, or the JSDoc-only `Parameter usage per mode` block (it references "modes" not user names).
    7. Do NOT touch test files in this task.
  </action>
  <verify>
    <automated>npx vitest run src/chris/__tests__/personality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\bJohn\b' src/chris/personality.ts` returns 0.
    - `grep -F 'Facts about you (Greg)' src/chris/personality.ts` matches at least one line.
    - `grep -F 'useful to Greg' src/chris/personality.ts` matches.
    - `grep -F 'Greg said:' src/chris/personality.ts` matches (declined-topics line).
    - `grep -F 'not a third party' src/chris/personality.ts` matches (anti-split sentence).
    - `npx vitest run src/chris/__tests__/personality.test.ts` exits 0 — every previously-RED test from Plan 11-01 is now GREEN, and every previously-GREEN test is still GREEN.
    - The new `Identity grounding (Phase 11 / RETR-01, RETR-02)` describe block passes for ALL 7 modes (loop iteration including PHOTOS).
  </acceptance_criteria>
  <done>
    personality.ts compiles, every Plan 11-01 pre-staged failing assertion is green, no other behavior changed, no test file edited in this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename "John" → "Greg" in src/llm/prompts.ts (6 user-facing mode templates only) and src/proactive/prompts.ts</name>
  <files>src/llm/prompts.ts, src/proactive/prompts.ts</files>
  <read_first>
    - src/llm/prompts.ts (full file — all 6 user-facing mode templates plus 4 classifier templates)
    - src/proactive/prompts.ts (full file — single template)
    - .planning/phases/11-identity-grounding/11-RESEARCH.md (Pitfall 3: classifier prompts NOT in scope; File Rename Inventory)
  </read_first>
  <action>
    Per-file instructions. **Inspect each occurrence — do not run a blind sed/regex replace** (RESEARCH.md "Anti-Patterns to Avoid").

    **File 1: src/llm/prompts.ts**

    For each of the SIX user-facing mode templates, replace EVERY occurrence of the standalone token "John" (and possessive "John's") with "Greg" / "Greg's":
    - `JOURNAL_SYSTEM_PROMPT` (line 10)
    - `INTERROGATE_SYSTEM_PROMPT` (line 40)
    - `REFLECT_SYSTEM_PROMPT`
    - `COACH_SYSTEM_PROMPT`
    - `PSYCHOLOGY_SYSTEM_PROMPT`
    - `PRODUCE_SYSTEM_PROMPT`

    Use the editor to walk each template body sequentially. For JOURNAL specifically the documented surface is ~15 occurrences; INTERROGATE has ~7; the others have fewer.

    DO NOT touch these classifier templates (they never reach the user — RESEARCH.md Pitfall 3):
    - `MODE_DETECTION_PROMPT`
    - `RELATIONAL_MEMORY_PROMPT`
    - `CONTRADICTION_DETECTION_PROMPT`
    - `MUTE_DETECTION_PROMPT`

    Also leave example fixture text inside MODE_DETECTION_PROMPT (e.g., `"Have I ever talked about my sister?"`) untouched — they are example user inputs, not persona references.

    Update the JSDoc block above each renamed template (lines 5-9, 35-39, etc.) to also say "Greg" if it currently says "John".

    **File 2: src/proactive/prompts.ts**

    Replace EVERY occurrence of "John" / "John's" in `PROACTIVE_SYSTEM_PROMPT` with "Greg" / "Greg's" (7 occurrences per RESEARCH.md). Update the JSDoc comment block (lines 1-7) too.

    **DO NOT** modify `src/proactive/triggers/*.ts`, `src/proactive/context-builder.ts`, or `src/memory/relational.ts` in this task — those are out-of-scope per RESEARCH.md (lowest priority, separate commit/follow-up).
  </action>
  <verify>
    <automated>npx vitest run src/chris/__tests__/personality.test.ts src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/chris/__tests__/reflect.test.ts src/chris/__tests__/coach.test.ts src/chris/__tests__/psychology.test.ts src/chris/__tests__/produce.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^export const (JOURNAL|INTERROGATE|REFLECT|COACH|PSYCHOLOGY|PRODUCE)_SYSTEM_PROMPT/,/^`;$/' src/llm/prompts.ts | grep -c '\bJohn\b'` returns 0 (zero "John" tokens in the six user-facing templates).
    - `grep -c '\bJohn\b' src/proactive/prompts.ts` returns 0.
    - The four classifier templates (MODE_DETECTION_PROMPT, RELATIONAL_MEMORY_PROMPT, CONTRADICTION_DETECTION_PROMPT, MUTE_DETECTION_PROMPT) are byte-unchanged in terms of "John" count. Verify with: `awk '/^export const MODE_DETECTION_PROMPT/,/^`;$/' src/llm/prompts.ts | grep -c '\bJohn\b'` returns the same value as on main pre-edit (do not assert a specific number — assert "did not change").
    - `grep -c '\bGreg\b' src/llm/prompts.ts` is at least 35 (rough lower bound — every previous "John" in user-facing templates is now Greg).
    - All 7 vitest test files in the verify command pass.
    - `src/chris/__tests__/relational.test.ts` (NOT in the verify list — but should also be unaffected because relational classifier prompt is NOT renamed) still passes when run separately; this is a sanity check, not a gating criterion in this task.
  </acceptance_criteria>
  <done>
    Six user-facing mode templates and the proactive template say "Greg"; classifier templates are byte-unchanged on John-count; per-mode unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire JOURNAL call site to suppress dates via { includeDate: false } and run full Docker test suite</name>
  <files>src/chris/modes/journal.ts</files>
  <read_first>
    - src/chris/modes/journal.ts (full file — handleJournal, line 37 is the buildPensieveContext call)
    - src/memory/context-builder.ts (verify the PensieveContextOptions interface is exported from Plan 11-01)
    - src/chris/__tests__/journal.test.ts (existing unit tests so we know what to keep green)
    - .planning/phases/11-identity-grounding/11-RESEARCH.md (Pitfall 2: INTERROGATE must keep dates)
  </read_first>
  <behavior>
    - JOURNAL invokes `buildPensieveContext(searchResults, { includeDate: false })`.
    - INTERROGATE call site (`src/chris/modes/interrogate.ts`) is UNCHANGED — it continues to invoke `buildPensieveContext(searchResults)` and receives the default date-prefixed format.
    - All other modes (REFLECT, COACH, PSYCHOLOGY, PRODUCE, PHOTOS) are unchanged at their call sites.
    - The unit suite is green.
    - The full Docker-backed test suite (`./scripts/test.sh`) is green (per user memory: ALWAYS run real Docker postgres, never skip integration tests).
  </behavior>
  <action>
    1. Open `src/chris/modes/journal.ts`.
    2. At line 37, change:
       `const pensieveContext = buildPensieveContext(searchResults);`
       to:
       `const pensieveContext = buildPensieveContext(searchResults, { includeDate: false });`
    3. Add an inline comment immediately above that line:
       `// Phase 11 / RETR-04: suppress per-entry date prefix in JOURNAL so today's seed-stamp does not leak into "back on April 14th..." fabrications. INTERROGATE keeps dates (citation contract).`
    4. Confirm by reading `src/chris/modes/interrogate.ts` that its `buildPensieveContext` call has NO opts argument (default behavior preserved). If it accidentally already has `{ includeDate: false }` from a prior commit, REVERT that line — INTERROGATE must keep dates.
    5. Optionally (but recommended), add a unit test to `src/chris/__tests__/journal.test.ts` that mocks hybridSearch + buildPensieveContext spy and asserts the spy is called with `{ includeDate: false }`. If the existing journal.test.ts mocking shape doesn't easily support a spy on `buildPensieveContext`, skip the unit test and rely on the integration-suite check below — the date-suppression behavior is structurally observable in TEST-03 outputs.
    6. Run the FULL Docker-backed test suite per user memory:
       `./scripts/test.sh`
       Confirm exit code 0. If TEST-03 (live-integration JOURNAL grounding) is RED, do NOT debug in this plan — Plan 11-03 owns the 3-of-3 gate. This task only needs the unit + non-live integration tiers green.
  </action>
  <verify>
    <automated>./scripts/test.sh src/chris/__tests__/journal.test.ts src/chris/__tests__/interrogate.test.ts src/memory/__tests__/context-builder.test.ts src/chris/__tests__/personality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'includeDate: false' src/chris/modes/journal.ts` matches exactly one line.
    - `grep -F 'includeDate' src/chris/modes/interrogate.ts` returns 0 (INTERROGATE remains on default).
    - The targeted Docker test run in `<verify>` exits 0.
    - A full unit-suite run (`./scripts/test.sh`) shows 0 failures across NON-live tiers. Live integration test failures (live-integration.test.ts) are deferred to Plan 11-03 and not gating here, BUT live-integration tests should not REGRESS — i.e., tests other than TEST-03 (TEST-01/02/04/05/06/07/08/09) should remain at the same pass-rate as on main pre-edit.
    - `grep '\bJohn\b' src/llm/prompts.ts src/chris/personality.ts src/proactive/prompts.ts | grep -v 'MODE_DETECTION\|RELATIONAL_MEMORY\|CONTRADICTION_DETECTION\|MUTE_DETECTION'` returns no user-facing-template hits (consolidated whole-plan check).
  </acceptance_criteria>
  <done>
    JOURNAL call site wired to suppress dates, INTERROGATE call site untouched, Docker unit suite green, ready for Plan 11-03's TEST-03 3-of-3 gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan replaces hard-coded persona strings with other hard-coded persona strings. No new user-controlled interpolation, no new I/O surface, no auth or session change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | Constitutional preamble + facts block | accept | The preamble and facts block already embed user-identifying content (Greg's nationality, location, business). This plan does not expand the data set; it relabels the addressee. The system prompt is sent only to Anthropic API (existing trust relationship, contractually bound). |

Per RESEARCH.md "Security Domain" — no ASVS categories activate (V2/V3/V4/V5/V6 all "no"). No new threat patterns.
</threat_model>

<verification>
After all 3 tasks land, run:

```
./scripts/test.sh
```

Expected: full unit + non-live integration tiers green. Live-integration TEST-03
may flake here; Plan 11-03 is the gate.

Spot-check: `grep -rn '\bJohn\b' src/llm/prompts.ts src/chris/personality.ts src/proactive/prompts.ts`
should return ONLY hits inside the four classifier templates in prompts.ts
(MODE_DETECTION, RELATIONAL_MEMORY, CONTRADICTION_DETECTION, MUTE_DETECTION) —
which are intentionally out of scope and stay unchanged.
</verification>

<success_criteria>
- Zero "John" tokens in user-facing surfaces (6 mode prompts + preamble + facts block + proactive prompt + declined-topics line).
- "Facts about you (Greg)" header + anti-split explanatory sentence emitted by buildKnownFactsBlock.
- JOURNAL call site uses `{ includeDate: false }`; INTERROGATE call site uses default.
- Plan 11-01 pre-staged failing tests are now green.
- `./scripts/test.sh` exits 0 across unit + non-live tiers.
- Classifier templates (MODE_DETECTION, RELATIONAL_MEMORY, CONTRADICTION_DETECTION, MUTE_DETECTION) byte-unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/11-identity-grounding/11-02-SUMMARY.md`
documenting:
- Exact files edited and the count of "John" → "Greg" substitutions per file.
- Confirmation that classifier templates were left untouched.
- The exact JOURNAL call-site wiring change.
- Status of the live-integration TEST-03 (pass/fail) at end of this plan — informational; the 3-of-3 gate lives in Plan 11-03.
</output>
