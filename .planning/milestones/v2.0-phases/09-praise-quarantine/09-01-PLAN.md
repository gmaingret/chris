---
phase: 09-praise-quarantine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chris/praise-quarantine.ts
  - src/chris/__tests__/praise-quarantine.test.ts
autonomous: true
requirements:
  - SYCO-04
  - SYCO-05
must_haves:
  truths:
    - "quarantinePraise rewrites reflexive opening flattery via Haiku"
    - "quarantinePraise returns original response when no flattery detected"
    - "quarantinePraise returns original response on any Haiku failure"
    - "COACH and PSYCHOLOGY modes bypass Haiku call entirely"
  artifacts:
    - path: "src/chris/praise-quarantine.ts"
      provides: "Self-contained praise quarantine module"
      exports: ["quarantinePraise"]
    - path: "src/chris/__tests__/praise-quarantine.test.ts"
      provides: "Unit tests for all quarantine behaviors"
      min_lines: 80
  key_links:
    - from: "src/chris/praise-quarantine.ts"
      to: "src/llm/client.ts"
      via: "import { anthropic, HAIKU_MODEL }"
      pattern: "anthropic\\.messages\\.create"
---

<objective>
Create the self-contained praise quarantine module that detects and rewrites reflexive opening flattery using Haiku, with full unit test coverage.

Purpose: Implements the core SYCO-04/SYCO-05 logic as a standalone module before wiring into the engine pipeline (Plan 02).
Output: `src/chris/praise-quarantine.ts` with `quarantinePraise()` export + comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-praise-quarantine/09-CONTEXT.md
@.planning/phases/09-praise-quarantine/09-RESEARCH.md

@src/chris/contradiction.ts
@src/llm/client.ts

<interfaces>
<!-- From src/llm/client.ts -->
```typescript
export const HAIKU_MODEL: string;  // config.haikuModel
export const anthropic: Anthropic;  // Anthropic client instance
```

<!-- From src/chris/engine.ts -->
```typescript
export type ChrisMode = 'JOURNAL' | 'INTERROGATE' | 'REFLECT' | 'COACH' | 'PSYCHOLOGY' | 'PRODUCE' | 'PHOTOS';
```

<!-- From src/utils/logger.js -->
```typescript
export const logger: { info: Function, warn: Function, error: Function, debug: Function };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create praise-quarantine module and tests</name>
  <files>src/chris/praise-quarantine.ts, src/chris/__tests__/praise-quarantine.test.ts</files>
  <read_first>
    - src/chris/contradiction.ts (reference pattern: Haiku call, stripFences, never-throw contract, JSON parsing)
    - src/llm/client.ts (imports: anthropic, HAIKU_MODEL)
    - src/chris/engine.ts (ChrisMode type export, line 22)
    - src/chris/__tests__/engine.test.ts (mock patterns for vi.mock, lines 60-70 for Anthropic mock)
  </read_first>
  <behavior>
    - quarantinePraise("Great question! Let me think about that.", "JOURNAL") returns rewritten text without the flattery opener
    - quarantinePraise("Here is my analysis of the situation.", "JOURNAL") returns the original text unchanged (no flattery detected)
    - quarantinePraise("Great question!", "COACH") returns original text without calling Haiku (mode bypass per D-06)
    - quarantinePraise("Great question!", "PSYCHOLOGY") returns original text without calling Haiku (mode bypass per D-06)
    - quarantinePraise returns original text when Haiku returns malformed JSON
    - quarantinePraise returns original text when Haiku API call throws an error
    - quarantinePraise logs { flattery_detected: true, mode } at info level when rewrite applied
    - quarantinePraise logs { flattery_detected: false, mode } at info level when no flattery found
    - quarantinePraise logs at warn level on any error, with error message
  </behavior>
  <action>
    **Create `src/chris/praise-quarantine.ts`:**

    1. Import `anthropic`, `HAIKU_MODEL` from `../llm/client.js`
    2. Import `logger` from `../utils/logger.js`
    3. Import `type { ChrisMode }` from `./engine.js`
    4. Define `stripFences` helper (same as contradiction.ts):
       ```typescript
       function stripFences(text: string): string {
         const match = text.match(/```(?:json)?\s*\n?([\s\S]*?)\n?\s*```/);
         return match ? match[1]!.trim() : text.trim();
       }
       ```
    5. Define the Haiku prompt as a module-level constant `PRAISE_QUARANTINE_PROMPT`:
       ```
       You are a response editor. Your only job is to detect and remove reflexive flattery from the opening of a response.

       Reflexive flattery means vacuous openers like:
       - "Great question!"
       - "That's a really insightful observation"
       - "What a thoughtful point"
       - "I love that you're thinking about this"
       - "That's so important that you're exploring this"

       Rules:
       - Look only at the FIRST 1-2 sentences.
       - If the response opens with reflexive flattery, rewrite that opening to remove it while preserving the rest of the response exactly.
       - Do NOT change anything after the opening.
       - If no reflexive flattery is found, return the original response unchanged.
       - Mid-response positive language ("that's worth exploring further") is NOT flattery — leave it alone.

       Respond with JSON only:
       { "flattery_detected": boolean, "rewritten": string }
       ```
    6. Export async function `quarantinePraise(response: string, mode: ChrisMode): Promise<string>`:
       - Early return `response` if `mode === 'COACH' || mode === 'PSYCHOLOGY'` (per D-06, SYCO-05)
       - Try block:
         - Call `anthropic.messages.create({ model: HAIKU_MODEL, max_tokens: 1500, system: PRAISE_QUARANTINE_PROMPT, messages: [{ role: 'user', content: response }] })`
         - Find text block: `result.content.find((b) => b.type === 'text')`
         - If no text block: `logger.warn({}, 'chris.praise_quarantine.no_text_block')` and return response
         - `stripFences(textBlock.text)` then `JSON.parse` as `{ flattery_detected: boolean; rewritten: string }`
         - `logger.info({ flattery_detected: parsed.flattery_detected, mode }, 'chris.praise_quarantine')`
         - Return `parsed.rewritten ?? response`
       - Catch block (per D-07):
         - `logger.warn({ error: error instanceof Error ? error.message : String(error) }, 'chris.praise_quarantine.error')`
         - Return `response` (never throw)

    **Create `src/chris/__tests__/praise-quarantine.test.ts`:**

    Mock setup (before imports, following engine.test.ts pattern):
    - `vi.mock('../../llm/client.js', ...)` with mockCreate, HAIKU_MODEL, SONNET_MODEL, OPUS_MODEL
    - `vi.mock('../../utils/logger.js', ...)` with mockLogInfo, mockLogWarn
    - `vi.mock('../engine.js', ...)` exporting ChrisMode type (needed for type-only import — mock the module to avoid pulling in engine dependencies)

    Test cases (9 total):
    1. "rewrites response when flattery detected in JOURNAL mode" — mockCreate returns `{ flattery_detected: true, rewritten: "Let me think about that." }`, assert result equals rewritten
    2. "returns original when no flattery detected" — mockCreate returns `{ flattery_detected: false, rewritten: "Here is my analysis." }`, assert result equals original
    3. "bypasses Haiku for COACH mode" — call with mode='COACH', assert mockCreate NOT called, result equals original
    4. "bypasses Haiku for PSYCHOLOGY mode" — call with mode='PSYCHOLOGY', assert mockCreate NOT called, result equals original
    5. "calls Haiku for JOURNAL mode" — assert mockCreate called once
    6. "calls Haiku for REFLECT mode" — assert mockCreate called once
    7. "calls Haiku for PRODUCE mode" — assert mockCreate called once
    8. "returns original on malformed JSON from Haiku" — mockCreate returns `{ content: [{ type: 'text', text: 'not json' }] }`, assert result equals original, logger.warn called
    9. "returns original when Haiku throws error" — mockCreate rejects, assert result equals original, logger.warn called
  </action>
  <verify>
    <automated>cd /home/claude/chris && npx vitest run src/chris/__tests__/praise-quarantine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/chris/praise-quarantine.ts contains `export async function quarantinePraise(`
    - src/chris/praise-quarantine.ts contains `import { anthropic, HAIKU_MODEL } from '../llm/client.js'`
    - src/chris/praise-quarantine.ts contains `if (mode === 'COACH' || mode === 'PSYCHOLOGY')`
    - src/chris/praise-quarantine.ts contains `chris.praise_quarantine.error` (warn log key)
    - src/chris/praise-quarantine.ts contains `chris.praise_quarantine` (info log key)
    - src/chris/praise-quarantine.ts contains `return response` in at least 3 places (mode bypass, no text block, catch)
    - src/chris/praise-quarantine.ts contains `PRAISE_QUARANTINE_PROMPT` constant
    - src/chris/praise-quarantine.ts contains `flattery_detected` in JSON type
    - src/chris/praise-quarantine.ts contains `max_tokens: 1500`
    - src/chris/__tests__/praise-quarantine.test.ts contains at least 9 `it(` test cases
    - src/chris/__tests__/praise-quarantine.test.ts contains `vi.mock('../../llm/client.js'`
    - All 9 tests pass: `npx vitest run src/chris/__tests__/praise-quarantine.test.ts` exits 0
  </acceptance_criteria>
  <done>
    quarantinePraise module exists with never-throw contract, mode bypass for COACH/PSYCHOLOGY, Haiku rewrite call, and all 9 unit tests passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine internal | Haiku processes Sonnet-generated text — no user-controlled injection path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | praise-quarantine.ts | accept | Haiku output is plain text returned to user, not executed; rewrite scope constrained by prompt to first 1-2 sentences |
| T-09-02 | Information Disclosure | praise-quarantine.ts | mitigate | Never log API key or full response content; use structured logger (pino) with field-level control — matches contradiction.ts pattern |
| T-09-03 | Denial of Service | praise-quarantine.ts | mitigate | Never-throw contract ensures Haiku failure passes through original response; timeout guard in engine (Plan 02) prevents latency blocking |
</threat_model>

<verification>
- `npx vitest run src/chris/__tests__/praise-quarantine.test.ts` — all 9 tests pass
- `grep -c 'return response' src/chris/praise-quarantine.ts` returns >= 3
- `grep 'export async function quarantinePraise' src/chris/praise-quarantine.ts` matches
</verification>

<success_criteria>
- quarantinePraise module is self-contained and importable
- Never-throw contract: every code path returns a string
- COACH and PSYCHOLOGY bypass without Haiku call
- JOURNAL, REFLECT, PRODUCE trigger Haiku rewrite
- 9 unit tests pass covering all behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/09-praise-quarantine/09-01-SUMMARY.md`
</output>
