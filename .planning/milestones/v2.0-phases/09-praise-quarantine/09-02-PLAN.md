---
phase: 09-praise-quarantine
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/chris/engine.ts
  - src/chris/__tests__/engine.test.ts
autonomous: true
requirements:
  - SYCO-04
  - SYCO-05
must_haves:
  truths:
    - "JOURNAL responses pass through praise quarantine before contradiction detection"
    - "REFLECT responses pass through praise quarantine before contradiction detection"
    - "PRODUCE responses pass through praise quarantine before contradiction detection"
    - "COACH responses do NOT pass through praise quarantine"
    - "PSYCHOLOGY responses do NOT pass through praise quarantine"
    - "Praise quarantine failure does not break the response pipeline"
  artifacts:
    - path: "src/chris/engine.ts"
      provides: "Praise quarantine pipeline step"
      contains: "quarantinePraise"
    - path: "src/chris/__tests__/engine.test.ts"
      provides: "Engine tests for praise quarantine wiring"
      contains: "praise quarantine"
  key_links:
    - from: "src/chris/engine.ts"
      to: "src/chris/praise-quarantine.ts"
      via: "import { quarantinePraise }"
      pattern: "quarantinePraise\\(response, mode\\)"
---

<objective>
Wire the praise quarantine module into the engine pipeline and add engine-level tests verifying correct mode routing and timeout behavior.

Purpose: Completes SYCO-04/SYCO-05 by integrating the quarantine step into processMessage() between mode handler and contradiction detection (per D-05).
Output: Updated engine.ts with praise quarantine step + new engine tests for the wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-praise-quarantine/09-CONTEXT.md
@.planning/phases/09-praise-quarantine/09-RESEARCH.md
@.planning/phases/09-praise-quarantine/09-01-SUMMARY.md

@src/chris/engine.ts
@src/chris/__tests__/engine.test.ts

<interfaces>
<!-- From src/chris/praise-quarantine.ts (created in Plan 01) -->
```typescript
export async function quarantinePraise(response: string, mode: ChrisMode): Promise<string>;
```

<!-- Existing engine.ts pipeline (lines 205-228): -->
```typescript
// Line 205: end of switch(mode) block
// Lines 207-228: contradiction detection (JOURNAL and PRODUCE only)
// Line 230: saveMessage(chatId, 'ASSISTANT', response, mode)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire praise quarantine into engine pipeline</name>
  <files>src/chris/engine.ts</files>
  <read_first>
    - src/chris/engine.ts (full file — must see current imports at top and pipeline at lines 205-230)
    - src/chris/praise-quarantine.ts (Plan 01 output — verify export name)
  </read_first>
  <action>
    **Add import (after line 12, the `detectContradictions` import):**
    ```typescript
    import { quarantinePraise } from './praise-quarantine.js';
    ```

    **Insert praise quarantine block between line 205 (end of switch) and line 207 (contradiction detection).**

    Insert this exact block after the closing `}` of the switch statement (currently line 205) and before the `// ── Contradiction detection` comment (currently line 207):

    ```typescript
    // ── Praise quarantine (JOURNAL, REFLECT, PRODUCE only) — SYCO-04 ──
    if (mode === 'JOURNAL' || mode === 'REFLECT' || mode === 'PRODUCE') {
      try {
        const QUARANTINE_TIMEOUT_MS = 3000;
        response = await Promise.race([
          quarantinePraise(response, mode),
          new Promise<string>((resolve) =>
            setTimeout(() => resolve(response), QUARANTINE_TIMEOUT_MS)
          ),
        ]);
      } catch (quarantineError) {
        logger.warn(
          {
            error: quarantineError instanceof Error
              ? quarantineError.message
              : String(quarantineError),
          },
          'chris.engine.praise_quarantine.error',
        );
      }
    }
    ```

    **Key details:**
    - Pipeline order per D-05: mode handler -> praise quarantine -> contradiction detection -> saveMessage -> relational memory
    - Timeout 3000ms per D-08 (matches contradiction detection DETECTION_TIMEOUT_MS)
    - Mode check includes JOURNAL, REFLECT, PRODUCE (NOT COACH, PSYCHOLOGY, INTERROGATE, PHOTOS per D-06/SYCO-05)
    - The `response` variable is reassigned with the (possibly rewritten) result
    - Catch block logs at warn level and swallows error (D-07 passthrough)
  </action>
  <verify>
    <automated>cd /home/claude/chris && grep -n 'quarantinePraise' src/chris/engine.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/chris/engine.ts contains `import { quarantinePraise } from './praise-quarantine.js'`
    - src/chris/engine.ts contains `if (mode === 'JOURNAL' || mode === 'REFLECT' || mode === 'PRODUCE')`
    - src/chris/engine.ts contains `quarantinePraise(response, mode)`
    - src/chris/engine.ts contains `QUARANTINE_TIMEOUT_MS = 3000`
    - src/chris/engine.ts contains `chris.engine.praise_quarantine.error` (warn log key)
    - src/chris/engine.ts contains `Promise.race` in the praise quarantine block
    - The praise quarantine block appears BEFORE the `// ── Contradiction detection` comment
    - The praise quarantine block appears AFTER the switch(mode) closing brace
  </acceptance_criteria>
  <done>
    engine.ts imports quarantinePraise and calls it in the pipeline between mode handler and contradiction detection, with 3s timeout and error swallowing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add engine tests for praise quarantine wiring</name>
  <files>src/chris/__tests__/engine.test.ts</files>
  <read_first>
    - src/chris/__tests__/engine.test.ts (full file — see mock setup at top, existing contradiction tests around line 975)
    - src/chris/engine.ts (updated in Task 1 — verify import and insertion)
  </read_first>
  <action>
    **Add mock for praise-quarantine module (after the contradiction mock at line 126):**
    ```typescript
    // ── Mock praise quarantine (SYCO-04/05) ───────────────────────────────────
    const mockQuarantinePraise = vi.fn();
    vi.mock('../praise-quarantine.js', () => ({
      quarantinePraise: mockQuarantinePraise,
    }));
    ```

    **In the `beforeEach` block, add default mock behavior:**
    ```typescript
    mockQuarantinePraise.mockImplementation((response: string) => Promise.resolve(response));
    ```
    This makes the mock a passthrough by default (returns whatever it receives), so existing tests are unaffected.

    **Add new describe block "praise quarantine integration (SYCO-04/05)" with these test cases:**

    1. **"calls quarantinePraise for JOURNAL mode"**
       - Set up: mockCreate for mode detection returns JOURNAL, mockCreate for handler returns "Great question! Here is my answer."
       - mockQuarantinePraise resolves with "Here is my answer."
       - Call processMessage, assert mockQuarantinePraise was called with ("Great question! Here is my answer.", "JOURNAL")

    2. **"calls quarantinePraise for REFLECT mode"**
       - Same setup but mode detection returns REFLECT, use mockHandleReflect
       - Assert mockQuarantinePraise called with response and "REFLECT"

    3. **"calls quarantinePraise for PRODUCE mode"**
       - Same setup but mode detection returns PRODUCE, use mockHandleProduce
       - Assert mockQuarantinePraise called with response and "PRODUCE"

    4. **"does NOT call quarantinePraise for COACH mode"**
       - Mode detection returns COACH, mockHandleCoach returns a response
       - Assert mockQuarantinePraise NOT called (the engine mode check excludes COACH)

    5. **"does NOT call quarantinePraise for PSYCHOLOGY mode"**
       - Mode detection returns PSYCHOLOGY
       - Assert mockQuarantinePraise NOT called

    6. **"uses rewritten response in saved message"**
       - mockQuarantinePraise resolves with "Rewritten response without flattery"
       - Mode detection returns JOURNAL
       - Assert mockSaveMessage called with 'ASSISTANT' and "Rewritten response without flattery" (not the original)

    7. **"passes through original on quarantine error"**
       - mockQuarantinePraise rejects with new Error("Haiku failed")
       - Assert processMessage still returns the original handler response (not throwing)
       - Assert mockLogWarn called with object containing 'chris.engine.praise_quarantine.error'

    **Test setup pattern (copy from existing contradiction tests around line 975):**
    - mockDetectMuteIntent returns { muted: false }
    - mockCreate first call returns mode JSON, subsequent calls handled by mode handler mock
    - mockLimit resolves with [] (empty conversation history)
    - mockReturning resolves with [{ id: 'test-id' }]
    - mockBuildMessageHistory resolves with []
    - mockBuildPensieveContext resolves with ''
    - mockDetectContradictions resolves with []
  </action>
  <verify>
    <automated>cd /home/claude/chris && npx vitest run src/chris/__tests__/engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/chris/__tests__/engine.test.ts contains `vi.mock('../praise-quarantine.js'`
    - src/chris/__tests__/engine.test.ts contains `mockQuarantinePraise`
    - src/chris/__tests__/engine.test.ts contains `praise quarantine` in a describe block
    - src/chris/__tests__/engine.test.ts contains at least 7 new `it(` blocks for praise quarantine
    - src/chris/__tests__/engine.test.ts contains assertion `mockQuarantinePraise` `.toHaveBeenCalledWith` for JOURNAL/REFLECT/PRODUCE
    - src/chris/__tests__/engine.test.ts contains assertion `mockQuarantinePraise` `.not.toHaveBeenCalled` for COACH/PSYCHOLOGY
    - `npx vitest run src/chris/__tests__/engine.test.ts` exits 0 (all tests pass, including existing ones)
  </acceptance_criteria>
  <done>
    Engine test suite includes 7 praise quarantine integration tests verifying mode routing, response rewriting, and error passthrough. All existing + new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files></files>
  <read_first>
    - src/chris/__tests__/praise-quarantine.test.ts (verify exists)
    - src/chris/__tests__/engine.test.ts (verify new tests added)
  </read_first>
  <action>
    Run the full test suite to verify no regressions:
    1. `npx vitest run` — runs all unit tests
    2. `npm test` — runs full Docker-based test suite (per project rule: always run Docker tests, never skip integration tests)
    3. If any test fails, diagnose and fix. Common issues:
       - Import path mismatch (`.js` extension required for ESM)
       - Missing mock in engine.test.ts causing real API calls
       - Type mismatch on ChrisMode
  </action>
  <verify>
    <automated>cd /home/claude/chris && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with 0 failures
    - `npm test` exits 0 with 0 failures
    - No test file references real Anthropic API (all mocked)
  </acceptance_criteria>
  <done>
    All unit and integration tests pass. Phase 9 code is fully integrated with no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine internal | Praise quarantine operates on Sonnet-generated text within the engine pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | engine.ts | mitigate | 3000ms Promise.race timeout prevents Haiku latency from blocking response delivery |
| T-09-05 | Tampering | engine.ts | accept | Rewritten text replaces original — acceptable since Haiku is a trusted internal service and prompt constrains changes to opening sentences only |
</threat_model>

<verification>
- `npx vitest run src/chris/__tests__/engine.test.ts` — all tests pass including 7 new praise quarantine tests
- `npx vitest run src/chris/__tests__/praise-quarantine.test.ts` — all 9 module tests pass
- `npm test` — full Docker suite passes with 0 failures
- `grep -c 'quarantinePraise' src/chris/engine.ts` returns >= 2 (import + usage)
- Pipeline order verified: praise quarantine comment appears before contradiction detection comment in engine.ts
</verification>

<success_criteria>
- engine.ts has praise quarantine step between mode handler and contradiction detection
- 3000ms timeout guard via Promise.race
- Mode check excludes COACH, PSYCHOLOGY, INTERROGATE, PHOTOS
- 7 new engine tests cover mode routing and error handling
- Full test suite (unit + Docker integration) passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-praise-quarantine/09-02-SUMMARY.md`
</output>
