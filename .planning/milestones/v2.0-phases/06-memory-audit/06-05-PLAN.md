---
phase: 06-memory-audit
plan: 05
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - .planning/phases/06-memory-audit/audit-report-production-dryrun.md
  - .planning/phases/06-memory-audit/audit-report-production.md
autonomous: false
gap_closure: true
requirements:
  - RETR-03
must_haves:
  truths:
    - "Production dry-run reviewed by user before any wet-run (D019 gate)"
    - "All Pensieve entries about Greg's location history, property, business entities, and key dates reviewed against ground truth (SC-1)"
    - "Incorrect or outdated entries are corrected or annotated so they no longer surface as current facts (SC-2)"
  artifacts:
    - path: ".planning/phases/06-memory-audit/audit-report-production-dryrun.md"
      provides: "Production dry-run report with real Pensieve entry UUIDs"
      contains: "would_correct"
    - path: ".planning/phases/06-memory-audit/audit-report-production.md"
      provides: "Production wet-run confirmation report"
      contains: "soft_deleted"
  key_links:
    - from: "src/scripts/audit-pensieve.ts"
      to: "Production Postgres at 192.168.1.50"
      via: "DATABASE_URL env var"
      pattern: "192.168.1.50"
---

<objective>
Run the audit script against the production Pensieve database: first a dry-run for user review (D019 gate), then a wet-run after explicit approval. This is the final step that actually satisfies RETR-03 -- reconciling real Pensieve entries against ground truth.

Purpose: The code is built and locally validated, but RETR-03 requires the reconciliation to have actually happened against production data, not merely the capability to do it. The D019 gate (PROJECT.md) requires explicit user approval before any production writes.

Output: Real production audit reports (dry-run + wet-run) with actual Pensieve entry UUIDs and confirmed correction actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-audit/06-CONTEXT.md
@.planning/phases/06-memory-audit/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production dry-run audit</name>
  <files>.planning/phases/06-memory-audit/audit-report-production-dryrun.md</files>
  <read_first>
    - src/scripts/audit-pensieve.ts
    - .planning/phases/06-memory-audit/audit-report-production-dryrun.md
    - .planning/phases/06-memory-audit/06-CONTEXT.md
  </read_first>
  <action>
Run the audit script in dry-run mode against the production database. The production DATABASE_URL should be loaded from the project `.env` file or provided explicitly.

```bash
npx tsx src/scripts/audit-pensieve.ts --dry-run --report-path .planning/phases/06-memory-audit/audit-report-production-dryrun.md
```

Note: The audit script has a localhost-only safety guard in seed mode, but the audit command itself should work against any DATABASE_URL. If the script refuses to connect to production, check if there is a `--force-production` flag or if the safety guard needs to be bypassed for audit-only (not seed) mode.

If the production database at 192.168.1.50 is not reachable from this environment, this task is BLOCKED and must be run from the main development environment where production network access is available. Report the block immediately.

The dry-run report should show:
- Real Pensieve entry UUIDs from the production database
- Each FACT/RELATIONSHIP-tagged entry reviewed
- Entries marked `correct`, `incorrect`, or `unmatched`
- For `incorrect` entries: the proposed correction (what `would_correct` to)
- NO mutations applied (dry-run only)
  </action>
  <verify>
    <automated>grep -c '[0-9a-f]\{8\}-[0-9a-f]\{4\}-' /home/claude/chris/.planning/phases/06-memory-audit/audit-report-production-dryrun.md 2>/dev/null || echo "No real UUIDs — production dry-run not executed"</automated>
  </verify>
  <acceptance_criteria>
    - audit-report-production-dryrun.md contains UUID-formatted entry IDs from production
    - audit-report-production-dryrun.md does NOT contain "NOT EXECUTED" or "placeholder"
    - Report shows entries reviewed with correctness status for each
    - No mutations were applied (dry-run mode)
  </acceptance_criteria>
  <done>Production dry-run report generated with real Pensieve entries; ready for user review under D019 gate</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D019 approval gate -- review production dry-run report</name>
  <files>.planning/phases/06-memory-audit/audit-report-production-dryrun.md</files>
  <action>
Human reviews the production dry-run report. This is the D019 approval gate -- no automated action. The report was generated by Task 1.

Review `.planning/phases/06-memory-audit/audit-report-production-dryrun.md` to confirm:
- Each entry marked `incorrect` is a genuine error
- Each proposed correction is accurate
- No false positives or false negatives
- Approve before any production writes proceed
  </action>
  <what-built>Production dry-run audit report generated. The audit script queried all FACT/RELATIONSHIP-tagged entries in the production Pensieve and compared them against the ground truth module. No changes were made -- this is report-only.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-memory-audit/audit-report-production-dryrun.md`
    2. Review each entry marked `incorrect`:
       - Is the identified issue a genuine error? (e.g., Cagnes-sur-Mer as rental location when it should be Golfe-Juan)
       - Is the proposed correction accurate?
    3. Review entries marked `correct` -- confirm no false negatives (entries that should be flagged but were not)
    4. Review entries marked `unmatched` -- these are FACT/RELATIONSHIP entries outside audit scope (D-04). Confirm none of them contain errors about the audited categories.
    5. This is the D019 approval gate: you must explicitly approve before any production writes.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: D019 human approval required"</automated>
  </verify>
  <done>User reviewed and approved the production dry-run report under D019 gate</done>
  <resume-signal>Type "approved" to proceed with production wet-run, or describe issues with the dry-run report</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run production wet-run audit (after D019 approval)</name>
  <files>.planning/phases/06-memory-audit/audit-report-production.md</files>
  <read_first>
    - .planning/phases/06-memory-audit/audit-report-production-dryrun.md
    - src/scripts/audit-pensieve.ts
  </read_first>
  <action>
After user approval of the dry-run report, execute the wet-run against production:

```bash
npx tsx src/scripts/audit-pensieve.ts --report-path .planning/phases/06-memory-audit/audit-report-production.md
```

This will:
1. Query all FACT/RELATIONSHIP-tagged entries (same as dry-run)
2. Soft-delete incorrect entries (set `deletedAt` timestamp)
3. Insert corrected replacement entries with `source: 'audit'` and `epistemicTag: 'FACT'`
4. Generate synchronous embeddings for corrected entries (D-02)
5. Write the final production audit report

The report should show:
- Same entries as dry-run, but with `soft_deleted` actions instead of `would_correct`
- New corrected entry UUIDs for each replaced entry
- Confirmation that embeddings were generated for corrected entries
  </action>
  <verify>
    <automated>grep -c 'soft_deleted\|corrected' /home/claude/chris/.planning/phases/06-memory-audit/audit-report-production.md 2>/dev/null || echo "No corrections found — wet-run may not have executed"</automated>
  </verify>
  <acceptance_criteria>
    - audit-report-production.md contains real UUIDs from production
    - audit-report-production.md does NOT contain "PENDING" or "placeholder"
    - Incorrect entries show action `soft_deleted` with their original UUIDs
    - Corrected replacement entries show new UUIDs with `source: 'audit'`
    - Existing retrieval (searchPensieve) now returns corrected facts, not old incorrect ones
  </acceptance_criteria>
  <done>Production Pensieve entries reconciled against ground truth; incorrect entries soft-deleted and replaced with corrected versions; RETR-03 fully satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script to production DB | Audit script connects to production Postgres at 192.168.1.50 via DATABASE_URL |
| D019 approval gate | Human must review dry-run report before wet-run proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-gc04 | Tampering | Production data modification | mitigate | D019 gate: dry-run report reviewed by human before any writes; soft-delete preserves originals |
| T-06-gc05 | Information Disclosure | Production DB credentials | mitigate | DATABASE_URL loaded from .env, not hardcoded in scripts or plan |
</threat_model>

<verification>
After wet-run completes:
1. `audit-report-production.md` has real UUIDs and correction actions
2. `audit-report-production-dryrun.md` has the pre-approval review
3. Production Pensieve returns corrected facts for audited categories
</verification>

<success_criteria>
- Production dry-run reviewed and approved by user (D019 gate passed)
- All incorrect/outdated entries in production Pensieve soft-deleted
- Corrected replacement entries inserted with source='audit' and immediate embeddings
- audit-report-production.md is a real run artifact (not placeholder)
- RETR-03 fully satisfied: memory audit completed against production data
</success_criteria>

<output>
After completion, create `.planning/phases/06-memory-audit/06-05-SUMMARY.md`
</output>
