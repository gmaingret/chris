---
phase: 06-memory-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pensieve/ground-truth.ts
  - src/pensieve/__tests__/ground-truth.test.ts
  - src/scripts/seed-audit-data.ts
  - src/scripts/__tests__/seed-audit-data.test.ts
autonomous: true
requirements:
  - RETR-03

must_haves:
  truths:
    - "A typed ground-truth module exports all M006 facts about Greg in categorized key-value form"
    - "A seed script inserts realistic Pensieve entries (correct + incorrect) into a local Docker Postgres DB"
    - "Seed entries include the known error patterns (Cagnes-sur-Mer/Golfe-Juan mix-up, wrong move direction)"
    - "All seed entries have epistemicTag set synchronously and embeddings generated"
  artifacts:
    - path: "src/pensieve/ground-truth.ts"
      provides: "Typed ground-truth const and map for Phase 8/10"
      exports: ["GROUND_TRUTH", "GROUND_TRUTH_MAP", "GroundTruthEntry", "FactCategory"]
    - path: "src/scripts/seed-audit-data.ts"
      provides: "Seed script inserting correct + incorrect Pensieve entries"
    - path: "src/pensieve/__tests__/ground-truth.test.ts"
      provides: "Unit tests for ground-truth module exports"
    - path: "src/scripts/__tests__/seed-audit-data.test.ts"
      provides: "Unit tests for seed data structure validation"
  key_links:
    - from: "src/scripts/seed-audit-data.ts"
      to: "src/db/schema.ts"
      via: "db.insert(pensieveEntries)"
      pattern: "db\\.insert\\(pensieveEntries\\)"
    - from: "src/scripts/seed-audit-data.ts"
      to: "src/pensieve/embeddings.ts"
      via: "embedAndStore() called synchronously for each seed entry"
      pattern: "await embedAndStore"
---

<objective>
Create the ground-truth reference module and seed script that establish the data foundation for the memory audit.

Purpose: The ground-truth module (D-03) is the shared contract imported by Phase 8 (structured fact injection) and Phase 10 (live tests). The seed script (D-06) populates a local test DB with realistic entries — some correct, some with the known errors from the M006 spec — so the audit script (Plan 02) has data to test against.

Output: `src/pensieve/ground-truth.ts`, `src/scripts/seed-audit-data.ts`, and tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-memory-audit/06-CONTEXT.md
@.planning/phases/06-memory-audit/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/schema.ts:
```typescript
export const epistemicTagEnum = pgEnum('epistemic_tag', [
  'FACT', 'EMOTION', 'BELIEF', 'INTENTION', 'EXPERIENCE',
  'PREFERENCE', 'RELATIONSHIP', 'DREAM', 'FEAR', 'VALUE',
  'CONTRADICTION', 'OTHER',
]);

export const pensieveEntries = pgTable('pensieve_entries', {
  id: uuid('id').primaryKey().default(sql`gen_random_uuid()`),
  content: text('content').notNull(),
  epistemicTag: epistemicTagEnum('epistemic_tag'),
  source: varchar('source', { length: 50 }).default('telegram'),
  contentHash: varchar('content_hash', { length: 64 }),
  metadata: jsonb('metadata'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
  deletedAt: timestamp('deleted_at', { withTimezone: true }),
});
```

From src/pensieve/embeddings.ts:
```typescript
export async function embedAndStore(entryId: string, content: string): Promise<void>;
export async function embedText(text: string): Promise<number[] | null>;
```

From src/db/connection.ts:
```typescript
export const db = drizzle(sql, { schema });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ground-truth module with tests</name>
  <files>src/pensieve/ground-truth.ts, src/pensieve/__tests__/ground-truth.test.ts</files>
  <read_first>
    - src/db/schema.ts (epistemicTag enum values, pensieveEntries column types)
    - .planning/phases/06-memory-audit/06-CONTEXT.md (D-03 decision, §Specific Ideas for exact facts)
    - .planning/phases/06-memory-audit/06-RESEARCH.md (Pattern 1: Ground-Truth Module for type structure)
  </read_first>
  <behavior>
    - Test 1: GROUND_TRUTH is an array with exactly 13 entries (identity: 3, location_history: 4, property: 2, business: 3, financial: 1)
    - Test 2: GROUND_TRUTH_MAP is a Record mapping all 13 keys to string values
    - Test 3: Every GroundTruthEntry has non-empty key, value, and category fields
    - Test 4: FactCategory type only allows 'identity' | 'location_history' | 'property' | 'business' | 'financial'
    - Test 5: GROUND_TRUTH_MAP['rental_property'] === 'Golfe-Juan, France'
    - Test 6: GROUND_TRUTH_MAP['birth_date'] === '1979-06-15'
    - Test 7: GROUND_TRUTH_MAP['fi_target'] === '$1,500,000'
  </behavior>
  <action>
    Create `src/pensieve/ground-truth.ts` per D-03 with these exact exports:

    1. `FactCategory` type: `'identity' | 'location_history' | 'property' | 'business' | 'financial'`

    2. `GroundTruthEntry` interface: `{ key: string; value: string; category: FactCategory }`

    3. `GROUND_TRUTH` const array with exactly these 13 entries (from CONTEXT.md §Specific Ideas):

    identity:
    - key: 'nationality', value: 'French'
    - key: 'birth_date', value: '1979-06-15'
    - key: 'birth_place', value: 'Cagnes-sur-Mer, France'

    location_history:
    - key: 'current_location', value: 'Saint Petersburg, Russia (until 2026-04-28)'
    - key: 'next_move', value: 'Batumi, Georgia (~1 month, from 2026-04-28)'
    - key: 'after_batumi', value: 'Antibes, France (June through end of August 2026)'
    - key: 'permanent_relocation', value: 'Batumi, Georgia (permanent, from ~September 2026)'

    property:
    - key: 'rental_property', value: 'Golfe-Juan, France'
    - key: 'rental_manager', value: 'Citya (since October 2022)'

    business:
    - key: 'business_us', value: 'MAINGRET LLC (New Mexico)'
    - key: 'business_georgia', value: 'Georgian Individual Entrepreneur'
    - key: 'residency_panama', value: 'Panama permanent residency'

    financial:
    - key: 'fi_target', value: '$1,500,000'

    4. `GROUND_TRUTH_MAP` const: `Record<string, string>` built via `Object.fromEntries(GROUND_TRUTH.map(e => [e.key, e.value]))`

    Then create the test file `src/pensieve/__tests__/ground-truth.test.ts` with the behaviors listed above. Use vitest (`describe`, `it`, `expect`). Import from `../ground-truth.js`.
  </action>
  <verify>
    <automated>npm test -- src/pensieve/__tests__/ground-truth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/pensieve/ground-truth.ts contains `export type FactCategory =`
    - src/pensieve/ground-truth.ts contains `export interface GroundTruthEntry`
    - src/pensieve/ground-truth.ts contains `export const GROUND_TRUTH: GroundTruthEntry[]`
    - src/pensieve/ground-truth.ts contains `export const GROUND_TRUTH_MAP: Record<string, string>`
    - src/pensieve/ground-truth.ts contains `key: 'rental_property', value: 'Golfe-Juan, France'`
    - src/pensieve/ground-truth.ts contains `key: 'birth_date', value: '1979-06-15'`
    - src/pensieve/__tests__/ground-truth.test.ts contains `GROUND_TRUTH.length`
    - npm test -- src/pensieve/__tests__/ground-truth.test.ts exits 0
  </acceptance_criteria>
  <done>Ground-truth module exports GROUND_TRUTH (13 entries), GROUND_TRUTH_MAP (13 keys), GroundTruthEntry interface, and FactCategory type. All 7 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create seed script with tests</name>
  <files>src/scripts/seed-audit-data.ts, src/scripts/__tests__/seed-audit-data.test.ts</files>
  <read_first>
    - src/pensieve/ground-truth.ts (created in Task 1 — the facts and known errors to seed)
    - src/db/schema.ts (pensieveEntries table, epistemicTag enum, column types)
    - src/pensieve/embeddings.ts (embedAndStore signature for synchronous embedding)
    - src/db/connection.ts (db export)
    - .planning/phases/06-memory-audit/06-RESEARCH.md (Pattern 3: Seed Script, Pitfalls 1-5)
    - docker-compose.local.yml (port 5433 local DB connection)
  </read_first>
  <behavior>
    - Test 1: SEED_ENTRIES array has entries for every ground-truth category (identity, location_history, property, business, financial)
    - Test 2: At least 2 entries have metadata.seedScenario = 'error' (the known error patterns)
    - Test 3: One error entry contains 'Cagnes-sur-Mer' in context of rental property
    - Test 4: One error entry contains wrong move direction (e.g., 'from Georgia to Saint Petersburg')
    - Test 5: Every entry in SEED_ENTRIES has a non-empty content string, epistemicTag of 'FACT' or 'RELATIONSHIP', and source of 'telegram'
  </behavior>
  <action>
    Create `src/scripts/seed-audit-data.ts` with:

    1. An exported `SEED_ENTRIES` array of objects with shape `{ content: string; epistemicTag: 'FACT' | 'RELATIONSHIP'; source: 'telegram'; metadata: { seedScenario: 'correct' | 'error'; groundTruthKey?: string; errorDescription?: string } }`.

    Include at minimum these entries:

    **Correct entries (seedScenario: 'correct'):**
    - "I was born on June 15, 1979 in Cagnes-sur-Mer." (identity/birth)
    - "I'm French, born and raised." (identity/nationality)
    - "My rental property in Golfe-Juan has been managed by Citya since October 2022." (property)
    - "I run MAINGRET LLC registered in New Mexico." (business)
    - "I also have a Georgian Individual Entrepreneur registration." (business)
    - "I have Panama permanent residency." (business)
    - "My FI target is $1.5 million." (financial)
    - "I'm currently living in Saint Petersburg, Russia. Planning to leave around April 28." (location)
    - "After a month in Batumi, I'll head to Antibes for the summer, June through August." (location)
    - "The plan is to permanently relocate to Batumi around September 2026." (location)

    **Error entries (seedScenario: 'error'):**
    - "My apartment in Cagnes-sur-Mer is rented out through Citya." (errorDescription: 'Rental property is in Golfe-Juan, not Cagnes-sur-Mer', groundTruthKey: 'rental_property')
    - "I'm planning to move from Georgia to Saint Petersburg next month." (errorDescription: 'Wrong direction — moving FROM Saint Petersburg, not TO it', groundTruthKey: 'current_location')

    2. A `main()` async function that:
    - Checks `DATABASE_URL` starts with `postgresql://` and contains `localhost` or `127.0.0.1` (safety guard per Pitfall 5 — refuse to run against non-local DB)
    - Logs "Loading bge-m3 embedding model..." before first embed call (Pitfall 3)
    - For each entry in SEED_ENTRIES: inserts via `db.insert(pensieveEntries).values(...)` directly (NOT `storePensieveEntry()` — it has no epistemicTag param, per Pitfall 1), then calls `await embedAndStore(entry.id, entry.content)` (Pitfall 2)
    - Logs count of inserted entries on completion
    - Calls `process.exit(0)` on success

    3. `main()` is called at module level with `.catch(err => { console.error(err); process.exit(1); })`

    Run command: `DATABASE_URL="postgresql://chris:localtest123@localhost:5433/chris" npx tsx src/scripts/seed-audit-data.ts`

    For the test file `src/scripts/__tests__/seed-audit-data.test.ts`: import `SEED_ENTRIES` from the seed script and validate the structure. These are pure unit tests on the data shape — no DB connection needed.
  </action>
  <verify>
    <automated>npm test -- src/scripts/__tests__/seed-audit-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/seed-audit-data.ts contains `export const SEED_ENTRIES`
    - src/scripts/seed-audit-data.ts contains `db.insert(pensieveEntries)` (direct insert, not storePensieveEntry)
    - src/scripts/seed-audit-data.ts contains `await embedAndStore(`
    - src/scripts/seed-audit-data.ts contains `epistemicTag:` in the values
    - src/scripts/seed-audit-data.ts contains `localhost` or `127.0.0.1` safety check
    - src/scripts/seed-audit-data.ts contains `Loading bge-m3`
    - src/scripts/__tests__/seed-audit-data.test.ts contains `SEED_ENTRIES`
    - At least one SEED_ENTRIES entry contains 'Cagnes-sur-Mer' with seedScenario 'error'
    - npm test -- src/scripts/__tests__/seed-audit-data.test.ts exits 0
  </acceptance_criteria>
  <done>Seed script exports SEED_ENTRIES (12+ entries covering all categories, 2+ error patterns), inserts via direct Drizzle insert with epistemicTag, generates embeddings synchronously, and has localhost safety guard. All 5 unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script -> local DB | Script writes to local Postgres; localhost guard prevents accidental production writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | seed-audit-data.ts | mitigate | Localhost-only guard: refuse to run if DATABASE_URL does not contain localhost/127.0.0.1 |
| T-06-02 | Information Disclosure | ground-truth.ts | accept | Contains Greg's personal facts but repo is private; no PII beyond what's already in the codebase |
</threat_model>

<verification>
1. `npm test -- src/pensieve/__tests__/ground-truth.test.ts` — all tests pass
2. `npm test -- src/scripts/__tests__/seed-audit-data.test.ts` — all tests pass
3. `src/pensieve/ground-truth.ts` exports GROUND_TRUTH with 13 entries and GROUND_TRUTH_MAP with 13 keys
4. `src/scripts/seed-audit-data.ts` has localhost safety guard and direct Drizzle insert pattern
</verification>

<success_criteria>
- Ground-truth module is importable and type-safe with all 13 M006 facts
- Seed script data covers all 5 fact categories with correct + error entries
- Both test files pass
- No production DB access possible from seed script
</success_criteria>

<output>
After completion, create `.planning/phases/06-memory-audit/06-01-SUMMARY.md`
</output>
