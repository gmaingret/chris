---
phase: 07-foundational-behavioral-fixes
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/chris/refusal.ts
  - src/chris/language.ts
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - LANG-01
  - LANG-02

must_haves:
  truths:
    - "detectRefusal() correctly identifies refusal phrases in EN, FR, and RU"
    - "detectRefusal() does NOT flag normal conversation or meta-references to refusals"
    - "Declined topics accumulate per chatId and are isolated between sessions"
    - "detectLanguage() identifies English, French, and Russian from text >= 4 words and >= 15 chars"
    - "Short messages (< 4 words or < 15 chars) inherit previous language, defaulting to English"
    - "Session language state tracks per chatId"
  artifacts:
    - path: "src/chris/refusal.ts"
      provides: "Refusal detection, topic extraction, session state"
      exports: ["detectRefusal", "addDeclinedTopic", "getDeclinedTopics", "clearDeclinedTopics", "generateRefusalAcknowledgment", "RefusalResult", "DeclinedTopicEntry"]
    - path: "src/chris/language.ts"
      provides: "Language detection via franc, session state"
      exports: ["detectLanguage", "getLastUserLanguage", "setLastUserLanguage", "clearLanguageState"]
  key_links:
    - from: "src/chris/language.ts"
      to: "franc"
      via: "import { franc } from 'franc'"
      pattern: "franc.*only.*eng.*fra.*rus"
    - from: "src/chris/refusal.ts"
      to: "session state Map"
      via: "module-level Map<string, DeclinedTopicEntry[]>"
      pattern: "new Map"
---

<objective>
Implement refusal detection (src/chris/refusal.ts) and language detection (src/chris/language.ts) as standalone modules with session state, passing all unit tests from Plan 01.

Purpose: These modules provide the pre-processing logic that engine.ts (Plan 04) will call. Keeping them separate enables unit testing without engine mocking.
Output: Two new modules, both tested and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundational-behavioral-fixes/07-CONTEXT.md
@.planning/phases/07-foundational-behavioral-fixes/07-RESEARCH.md
@.planning/phases/07-foundational-behavioral-fixes/07-01-SUMMARY.md

<interfaces>
<!-- From 07-RESEARCH.md — franc API: -->
```typescript
import { franc } from 'franc';
// franc(text, { only: ['eng', 'fra', 'rus'] }) returns ISO 639-3 code or 'und'
```

<!-- DeclinedTopic interface from personality.ts (Plan 02 creates this): -->
```typescript
export interface DeclinedTopic {
  topic: string;
  originalSentence: string;
}
```

<!-- mute.ts pattern for reference (synchronous detection + early return): -->
```typescript
export type MuteClassification =
  | { muted: false }
  | { muted: true; muteUntil: Date; durationDescription: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement refusal detection module</name>
  <files>src/chris/refusal.ts</files>
  <read_first>
    - src/chris/__tests__/refusal.test.ts (test expectations to satisfy)
    - src/proactive/mute.ts (pattern for detection module structure)
    - .planning/phases/07-foundational-behavioral-fixes/07-RESEARCH.md (refusal patterns and pitfalls)
    - .planning/phases/07-foundational-behavioral-fixes/07-CONTEXT.md (D-03 ephemeral, D-04 pipeline, D-05 fewer false positives)
  </read_first>
  <behavior>
    - detectRefusal("I don't want to talk about my father") -> { isRefusal: true, topic: "my father", originalSentence: "I don't want to talk about my father" }
    - detectRefusal("let's not discuss that") -> { isRefusal: true, ... }
    - detectRefusal("I'd rather not get into that right now") -> { isRefusal: true, ... }
    - detectRefusal("just drop it") -> { isRefusal: true, ... }
    - detectRefusal("please don't bring up my ex") -> { isRefusal: true, ... }
    - detectRefusal("I had a great day at work today") -> { isRefusal: false }
    - detectRefusal("I told my therapist I don't want to talk about my childhood") -> { isRefusal: false } (meta-reference)
    - detectRefusal("je ne veux pas en parler") -> { isRefusal: true, ... }
    - detectRefusal("changeons de sujet") -> { isRefusal: true, ... }
    - detectRefusal("laisse tomber") -> { isRefusal: true, ... }
    - detectRefusal("я не хочу об этом говорить") -> { isRefusal: true, ... }
    - detectRefusal("давай сменим тему") -> { isRefusal: true, ... }
    - addDeclinedTopic('chat-1', 'topic', 'sentence') -> getDeclinedTopics('chat-1') has length 1
    - getDeclinedTopics('chat-2') is empty (isolated by chatId)
    - clearDeclinedTopics('chat-1') -> getDeclinedTopics('chat-1') is empty
  </behavior>
  <action>
Create `src/chris/refusal.ts` with:

1. **Types:**
```typescript
export type RefusalResult =
  | { isRefusal: false }
  | { isRefusal: true; topic: string; originalSentence: string };

export interface DeclinedTopicEntry {
  topic: string;
  originalSentence: string;
}
```

2. **Session state** — module-level Maps keyed by chatId string (per D-03: ephemeral, resets on process restart):
```typescript
const sessionDeclinedTopics = new Map<string, DeclinedTopicEntry[]>();
```

3. **Refusal patterns** — 15-20 regex patterns per language (per TRUST-01). Err toward precision over recall (D-05). Each pattern has an optional capture group for the topic:

**English patterns (15-18):**
- `/^(?!.*(?:told|said|mentioned|explained).*(?:that|how))\s*i\s+don'?t\s+want\s+to\s+(?:talk|think)\s+about\s+(.+)/i` — "I don't want to talk about X" with meta-reference guard
- `/^(?!.*(?:told|said))\s*let'?s?\s+(?:not\s+(?:talk|discuss|get\s+into)|move\s+on|change\s+the\s+subject)/i` — "let's not discuss" / "let's move on"
- `/^(?!.*(?:told|said))\s*(?:please\s+)?don'?t\s+(?:bring\s+(?:up|that\s+up)|mention|ask\s+(?:about|me\s+about))\s*(.*)/i` — "don't bring up X"
- `/^(?!.*(?:told|said))\s*i'?d?\s+rather\s+not\s+(?:discuss|talk\s+about|get\s+into)\s*(.*)/i` — "I'd rather not discuss X"
- `/^(?!.*(?:told|said))\s*(?:just\s+)?drop\s+it/i` — "drop it"
- `/^(?!.*(?:told|said))\s*(?:just\s+)?leave\s+it/i` — "leave it"
- `/^(?!.*(?:told|said))\s*enough\s+about\s+(.*)/i` — "enough about X"
- `/^(?!.*(?:told|said))\s*i\s+(?:really\s+)?don'?t\s+(?:want|need)\s+to\s+(?:go\s+(?:there|into\s+that)|discuss\s+(?:this|that))/i` — "I don't want to go there"
- `/^(?!.*(?:told|said))\s*can\s+we\s+(?:not|please\s+not)\s+(?:talk|go)\s+(?:about|there)/i` — "can we not talk about"
- `/^(?!.*(?:told|said))\s*(?:I'm|i\s+am)\s+not\s+(?:going\s+to|gonna)\s+(?:talk|discuss|get\s+into)\s+(?:that|this)/i` — "I'm not going to talk about that"
- `/^(?!.*(?:told|said))\s*(?:skip|pass)\s+(?:that|this)/i` — "skip that"
- `/^(?!.*(?:told|said))\s*not\s+(?:now|today|right\s+now)/i` — "not now" / "not today"
- `/^(?!.*(?:told|said))\s*i\s+don'?t\s+(?:feel|want)\s+(?:like|to)\s+(?:talking|discussing)\s+(?:about\s+)?(?:that|this|it)/i` — "I don't feel like talking about that"

**French patterns (15):**
- `/je\s+ne\s+veux\s+pas\s+(?:en\s+)?parler\s*(?:de\s+)?(.*)/i`
- `/(?:on\s+)?(?:n'?en\s+)?parlons\s+plus/i`
- `/changeons\s+de\s+sujet/i`
- `/laisse(?:z)?\s+(?:tomber|ça|cela)/i`
- `/passons\s+à\s+autre\s+chose/i`
- `/je\s+(?:ne\s+)?(?:pr[eé]f[eè]re|voudrais)\s+(?:pas\s+)?(?:ne\s+pas\s+)?(?:en\s+)?(?:parler|discuter)/i`
- `/(?:arrête|arr[eê]tons)\s+(?:d'en\s+parler|avec\s+ça)/i`
- `/(?:pas|plus)\s+(?:maintenant|aujourd'?hui)/i`
- `/je\s+(?:ne\s+)?veux\s+(?:pas|plus)\s+(?:en\s+)?(?:discuter|aborder)/i`
- `/(?:on\s+peut|peut-on)\s+(?:changer|parler)\s+(?:de\s+)?(?:sujet|d'autre\s+chose)/i`
- `/ça\s+(?:ne\s+)?(?:me\s+)?(?:dit|tente)\s+(?:rien|pas)/i`
- `/(?:je\s+)?(?:ne\s+)?(?:souhaite|d[eé]sire)\s+pas\s+(?:en\s+)?(?:parler|discuter)/i`
- `/c'?est\s+(?:pas|plus)\s+(?:le\s+)?(?:moment|sujet)/i`
- `/(?:fiche|fous|fiche[z]?|foutez)\s*(?:moi|-)?\s*(?:la\s+)?paix\s+(?:avec|sur)\s+(.*)/i`
- `/j'?(?:ai|en\s+ai)\s+(?:assez|marre|ras\s+le\s+bol)\s+(?:de\s+(?:parler|discuter)\s+(?:de\s+)?)(.*)/i`

**Russian patterns (15):**
- `/я\s+не\s+хочу\s+(?:об?\s+этом\s+)?(?:говорить|разговаривать|обсуждать)/i`
- `/давай(?:те)?\s+(?:не\s+будем|сменим\s+тему|поговорим\s+о\s+другом)/i`
- `/(?:оставь(?:те)?|брось(?:те)?)\s+(?:это|эту\s+тему)/i`
- `/хватит\s+(?:об?\s+этом|на\s+эту\s+тему)/i`
- `/не\s+(?:сейчас|сегодня|надо)/i`
- `/(?:сменим|поменяем)\s+тему/i`
- `/(?:мне|я)\s+(?:не\s+)?(?:хочется|хочу|нравится)\s+(?:об?\s+этом\s+)?(?:говорить|думать)/i`
- `/(?:закроем|оставим)\s+эту\s+тему/i`
- `/(?:прекрати(?:те)?|перестань(?:те)?)\s+(?:об?\s+этом|спрашивать)/i`
- `/не\s+(?:хочу|буду|стану)\s+(?:это|об?\s+этом)\s+(?:обсуждать|трогать)/i`
- `/(?:пропустим|пропусти)\s+это/i`
- `/(?:можно|давай)\s+(?:без\s+этого|о\s+другом)/i`
- `/(?:достаточно|хватит)\s+(?:об?\s+этом|на\s+сегодня)/i`
- `/не\s+(?:трогай(?:те)?|поднимай(?:те)?)\s+эту\s+тему/i`
- `/мне\s+(?:не\s+)?(?:комфортно|приятно)\s+(?:об?\s+этом\s+)?(?:говорить|обсуждать)/i`

4. **Meta-reference guard:** English patterns use a negative lookahead `(?!.*(?:told|said|mentioned|explained))` to avoid matching sentences like "I told my therapist I don't want to talk about my childhood." This handles the false-positive case in the test. French/Russian patterns don't need this guard because the sentence structure makes meta-references structurally different.

5. **Topic extraction:** For patterns with capture groups, extract the captured text as the topic. For patterns without capture groups (e.g., "drop it", "changeons de sujet"), use the full original sentence as both topic and originalSentence. Always store `originalSentence` as the verbatim user input (per Pitfall 3 from research).

6. **detectRefusal(text: string): RefusalResult** — iterate all three language pattern arrays, return first match. Return `{ isRefusal: false }` if no match.

7. **Session state functions:**
```typescript
export function addDeclinedTopic(chatId: string, topic: string, originalSentence: string): void
export function getDeclinedTopics(chatId: string): DeclinedTopicEntry[]
export function clearDeclinedTopics(chatId: string): void
```

8. **generateRefusalAcknowledgment(language: string): string** — returns a template acknowledgment in the appropriate language:
```typescript
const ACKNOWLEDGMENTS: Record<string, string[]> = {
  English: ["Got it — moving on.", "Understood.", "No problem, we'll skip that."],
  French: ["Compris — on passe à autre chose.", "Pas de souci.", "D'accord, on laisse ça."],
  Russian: ["Понял — идём дальше.", "Хорошо.", "Без проблем, пропустим это."],
};
```
Pick a random acknowledgment from the matching language array. Default to English if language unknown.
  </action>
  <verify>
    <automated>cd /home/claude/chris && npm run test:unit -- --reporter=verbose src/chris/__tests__/refusal.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/chris/refusal.ts exists and exports detectRefusal, addDeclinedTopic, getDeclinedTopics, clearDeclinedTopics, generateRefusalAcknowledgment
    - detectRefusal("I don't want to talk about my father") returns { isRefusal: true, topic containing "my father" }
    - detectRefusal("I had a great day at work today") returns { isRefusal: false }
    - detectRefusal("I told my therapist I don't want to talk about my childhood") returns { isRefusal: false }
    - detectRefusal("je ne veux pas en parler") returns { isRefusal: true }
    - detectRefusal("changeons de sujet") returns { isRefusal: true }
    - detectRefusal("я не хочу об этом говорить") returns { isRefusal: true }
    - getDeclinedTopics('nonexistent') returns []
    - addDeclinedTopic + getDeclinedTopics round-trips correctly
    - clearDeclinedTopics empties the list
    - sessionDeclinedTopics is a module-level Map (not DB-backed) per D-03
    - No regex pattern uses catastrophic backtracking patterns like (a+)+ (T-07-01)
    - `npm run test:unit -- src/chris/__tests__/refusal.test.ts` passes
  </acceptance_criteria>
  <done>Refusal detection with 15+ patterns per language, session state, topic extraction, and acknowledgment generation — all refusal tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement language detection module</name>
  <files>src/chris/language.ts</files>
  <read_first>
    - src/chris/__tests__/language.test.ts (test expectations to satisfy)
    - .planning/phases/07-foundational-behavioral-fixes/07-RESEARCH.md (franc usage, Pitfall 1 and 2)
    - .planning/phases/07-foundational-behavioral-fixes/07-CONTEXT.md (D-09 franc preprocessing, D-10 injection method)
  </read_first>
  <behavior>
    - detectLanguage("I had a wonderful day at work today and I feel great", null) -> "English"
    - detectLanguage("Aujourd'hui j'ai passé une journée magnifique au bureau", null) -> "French"
    - detectLanguage("Сегодня у меня был замечательный день на работе", null) -> "Russian"
    - detectLanguage("oui merci", "French") -> "French" (short, inherits)
    - detectLanguage("да", "Russian") -> "Russian" (short, inherits)
    - detectLanguage("ok", null) -> "English" (short, no prior, defaults English)
    - detectLanguage("Aujourd'hui j'ai passé une journée magnifique au bureau et je suis content", "English") -> "French" (long enough, overrides)
    - getLastUserLanguage('new-chat') -> null
    - setLastUserLanguage('chat', 'French') + getLastUserLanguage('chat') -> "French"
  </behavior>
  <action>
Create `src/chris/language.ts` with:

1. **Import franc:**
```typescript
import { franc } from 'franc';
```

2. **Language name map** (per Pitfall 1 — franc returns ISO 639-3, LLM needs display names):
```typescript
const LANGUAGE_NAMES: Record<string, string> = {
  eng: 'English',
  fra: 'French',
  rus: 'Russian',
};
```

3. **Session state** — module-level Map keyed by chatId:
```typescript
const sessionLanguage = new Map<string, string>();
```

4. **detectLanguage(text: string, previousLanguage: string | null): string** — implements LANG-01 and LANG-02:
```typescript
export function detectLanguage(
  text: string,
  previousLanguage: string | null,
): string {
  // LANG-02: short message threshold
  const words = text.trim().split(/\s+/);
  if (words.length < 4 || text.trim().length < 15) {
    return previousLanguage ?? 'English';
  }

  // LANG-01: franc with restricted language set
  const detected = franc(text, { only: ['eng', 'fra', 'rus'] });
  const languageName = LANGUAGE_NAMES[detected];

  if (!languageName) {
    // 'und' or unrecognized — fall back to previous or English
    return previousLanguage ?? 'English';
  }

  return languageName;
}
```

5. **Session state accessors:**
```typescript
export function getLastUserLanguage(chatId: string): string | null {
  return sessionLanguage.get(chatId) ?? null;
}

export function setLastUserLanguage(chatId: string, language: string): void {
  sessionLanguage.set(chatId, language);
}

export function clearLanguageState(chatId: string): void {
  sessionLanguage.delete(chatId);
}
```
  </action>
  <verify>
    <automated>cd /home/claude/chris && npm run test:unit -- --reporter=verbose src/chris/__tests__/language.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/chris/language.ts exists and exports detectLanguage, getLastUserLanguage, setLastUserLanguage, clearLanguageState
    - detectLanguage imports franc via `import { franc } from 'franc'`
    - LANGUAGE_NAMES map has exactly { eng: 'English', fra: 'French', rus: 'Russian' }
    - Short message check uses `words.length < 4 || text.trim().length < 15`
    - franc is called with `{ only: ['eng', 'fra', 'rus'] }` restriction
    - Undetermined results ('und') fall back to previousLanguage or 'English'
    - sessionLanguage is a module-level Map (not DB-backed)
    - `npm run test:unit -- src/chris/__tests__/language.test.ts` passes
  </acceptance_criteria>
  <done>Language detection via franc with short-message inheritance and session state — all language tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text -> regex engine | Raw user text is matched against refusal patterns |
| User text -> franc | Raw user text is passed to franc for language detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | Refusal regex patterns | mitigate | All regex patterns use simple alternation and optional groups — no nested quantifiers, no catastrophic backtracking patterns like `(a+)+`. Patterns are anchored or bounded. Test with 10KB input string to verify <10ms execution. |
| T-07-04 | Tampering | Session state Maps | accept | Module-level Maps are in-process only. Single-user system (D009). No cross-user data leakage risk. Maps reset on process restart (D-03). |
| T-07-05 | Denial of Service | franc on large input | accept | franc is O(n) on input length. processMessage already limits input to 100KB. Worst case is a few milliseconds. |
</threat_model>

<verification>
1. `npm run test:unit -- src/chris/__tests__/refusal.test.ts` — all pass
2. `npm run test:unit -- src/chris/__tests__/language.test.ts` — all pass
3. `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- Refusal detection handles EN/FR/RU with 15+ patterns per language (TRUST-01)
- False positives avoided for meta-references and normal conversation (D-05)
- Session declined topics accumulate and isolate by chatId (TRUST-02)
- Language detection via franc with short-message inheritance (LANG-01, LANG-02)
- Both modules are pure, synchronous, and testable without mocking
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundational-behavioral-fixes/07-03-SUMMARY.md`
</output>
