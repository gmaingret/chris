---
phase: 22.1-wire-retrieve-context
plan: 01
subsystem: retrieval
tags: [routing, retrieveContext, journal, reflect, coach, psychology, produce, chat-modes, RETR-02, RETR-03, hybridOptions, summaryToSearchResult]

# Dependency graph
requires:
  - phase: 22-cron-retrieval-routing
    provides: "Plan 22-02: retrieveContext orchestrator + VERBATIM_KEYWORDS + HIGH_IMPORTANCE_THRESHOLD + RECENCY_BOUNDARY_DAYS constants + 22 unit tests in src/pensieve/__tests__/routing.test.ts. This plan extends retrieveContext with a hybridOptions passthrough + adds a summaryToSearchResult adapter so chat-mode callers can render summary-only/descent results through the same SearchResult[] → buildPensieveContext pipeline."
  - phase: 22-cron-retrieval-routing
    provides: "Plan 22-03: extractQueryDate(text, language?) with regex-first three-tier fast-path + Haiku fallback gated on DATE_HEURISTIC_KEYWORDS. Chat-mode handlers call this to derive the queryDate argument for retrieveContext — zero per-message Haiku cost on queries with no date heuristic."
provides:
  - "src/pensieve/routing.ts — extended with `hybridOptions?: SearchOptions` passthrough on `RetrieveContextOptions` (preserves per-mode tags/recencyBias/minScore/limit round-trip through routing into hybridSearch on every raw branch — verbatim, recent, no-summary-fallback, and catch-block fallback); new `summaryToSearchResult(summary): SearchResult` adapter export that synthesizes a single SearchResult with score=1.0 sentinel whose entry.content carries the labeled inline `[Episode Summary YYYY-MM-DD | importance=N/10 | topics=...]` block + summary text, type-compatible with pensieveEntries.\$inferSelect so buildPensieveContext renders it as a numbered citation."
  - "src/chris/modes/journal.ts, reflect.ts, coach.ts, psychology.ts, produce.ts — each 5-line retrieval block replaced with `extractQueryDate → retrieveContext({ query, queryDate, rawLimit, hybridOptions: MODE_SEARCH_OPTIONS }) → routing.summary != null ? [summaryToSearchResult(summary), ...routing.raw] : routing.raw`. Emits per-mode `chris.<mode>.routing` info log with `{ chatId, reason, hasSummary, rawCount }` for diagnostic visibility. The mode-specific *_SEARCH_OPTIONS identity (JOURNAL tag filter + recencyBias 0.3; REFLECT recencyBias 0.1; COACH recencyBias 0.5 + BELIEF/INTENTION/VALUE tags; PSYCHOLOGY recencyBias 0.2 + EMOTION/FEAR/BELIEF/DREAM tags; PRODUCE recencyBias 0.3) is preserved end-to-end via the hybridOptions passthrough."
  - "Per-mode regression tests in src/chris/__tests__/{journal,reflect,coach,psychology,produce}.test.ts — each has a new `RETR-02/03 routing wiring` describe block with 3 it() cases: recent queryDate (3d ago) → raw via hybridSearch + getEpisodicSummary NOT called; 30d-old queryDate → summary tier → buildPensieveContext first arg is the synthetic summary SearchResult with score=1.0 + content containing `[Episode Summary YYYY-MM-DD`; verbatim-keyword query 30d ago → raw regardless of age (getEpisodicSummary NOT called). Existing per-mode assertions relaxed from `expect(mockHybridSearch).toHaveBeenCalledWith(query, OPTIONS)` to `expect.objectContaining(OPTIONS)` to tolerate the merged hybridOptions shape (retrieveContext adds limit resolution)."
affects:
  - "v2.2 milestone audit — RETR-02/03 tech_debt items resolved. `retrieveContext` orchestrator now has 5 production callers (one per chat mode). A 30-day-old JOURNAL query escalates to the episodic-summary tier per the contract."
  - "INTERROGATE handler (src/chris/modes/interrogate.ts) — NOT touched, keeps its bespoke ad-hoc routing (Flow 3: extractQueryDate + getEpisodicSummary direct call + formatEpisodicBlock header form prepend). interrogate.test.ts passes byte-identically."
  - "/summary handler (src/bot/handlers/summary.ts) — NOT touched, keeps direct getEpisodicSummary call (Flow 5). summary.test.ts passes byte-identically."
  - "M009 weekly review (downstream) — can still read summary + raw via retrieveContext; the hybridOptions passthrough means any future M009 caller can supply its own tag filter / recencyBias without the orchestrator silently degrading the decision."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Opt-in hybridOptions passthrough for the mode-specific identity round-trip — retrieveContext accepts `hybridOptions?: SearchOptions` and merges with rawLimit (`limit: hybridOptions?.limit ?? rawLimit`) so mode-specific `tags`/`recencyBias`/`minScore` preserve through the routing decision tree into every raw hybridSearch call site. Callers without hybridOptions collapse to the pre-Plan-22.1 behavior (byte-identical) — existing routing.test.ts passes unchanged."
    - "summaryToSearchResult adapter for the summary-only branch — lives in routing.ts so the 5 mode handlers don't each duplicate the synthesis logic. Score=1.0 sentinel matches loadEntriesByIds' convention and survives buildPensieveContext's 0.3 threshold. The synthetic entry uses sentinel field values (id=`episodic-${summary.id}`, source='episodic-summary', epistemicTag=null) cast to `pensieveEntries.\$inferSelect` — load-bearing cast that is safe because only `content` and `createdAt` flow into the downstream prompt. Content carries the INLINE labeled block (NOT the header form — INTERROGATE keeps formatEpisodicBlock's `## Recent Episode Context (interpretation, not fact)` header because its citation contract differs)."
    - "Mode handlers wire retrieveContext via a uniform 5-line block: `extractQueryDate(text, language) → retrieveContext({ query, queryDate, rawLimit, hybridOptions }) → searchResults = summary != null ? [summaryToSearchResult(summary), ...raw] : raw → logger.info({ reason, hasSummary, rawCount }, 'chris.<mode>.routing') → buildPensieveContext(searchResults, ...)`. Near-identical block across 5 files makes the wiring auditable via grep — `grep -c 'retrieveContext' src/chris/modes/*.ts` returns 5, `grep -c 'hybridSearch' src/chris/modes/*.ts` returns 0 (interrogate also uses getEpisodicSummary direct, not hybridSearch)."
    - "Per-mode regression test shape — each test file mocks `../../pensieve/retrieve.js` with `{ hybridSearch, getEpisodicSummary, MODE_SEARCH_OPTIONS }`, adds `'../modes/date-extraction.js' → extractQueryDate` mock, and keeps the real retrieveContext. The 3-case describe block (recent / old / verbatim-override) drives the routing decision tree through the real orchestrator against mocked primitives — asserting on `mockGetEpisodicSummary.toHaveBeenCalled()` + `mockBuildPensieveContext.mock.calls[0][0]` first-arg structure proves the end-to-end wiring fires, not just that a function is imported."

key-files:
  created: []
  modified:
    - "src/pensieve/routing.ts (281 lines, +57 vs 224 baseline) — adds `hybridOptions?: SearchOptions` to RetrieveContextOptions + merges at top of retrieveContext with `{ ...opts.hybridOptions, limit: opts.hybridOptions?.limit ?? rawLimit }` replacing the 4 `{ limit: rawLimit }` call sites (verbatim, recent, no-summary-fallback, catch-block); adds `summaryToSearchResult(summary): SearchResult` export."
    - "src/chris/modes/journal.ts — replaces `const searchResults = await hybridSearch(text, JOURNAL_SEARCH_OPTIONS)` with the uniform retrieveContext block. Preserves `includeDate: false` invariant on buildPensieveContext (Phase 11 RETR-04 suppression of per-entry date prefix so today's seed-stamp does not leak into fabrications)."
    - "src/chris/modes/reflect.ts — same pattern with REFLECT_SEARCH_OPTIONS. Preserves `searchResults` variable name so rest of function unchanged."
    - "src/chris/modes/coach.ts — same pattern with COACH_SEARCH_OPTIONS."
    - "src/chris/modes/psychology.ts — same pattern with PSYCHOLOGY_SEARCH_OPTIONS."
    - "src/chris/modes/produce.ts — same pattern with PRODUCE_SEARCH_OPTIONS."
    - "src/chris/__tests__/journal.test.ts — extends retrieve.js mock with getEpisodicSummary, adds extractQueryDate mock, relaxes existing 'calls hybridSearch with JOURNAL_SEARCH_OPTIONS' assertion to objectContaining, adds 3-case `RETR-02/03 routing wiring` describe block."
    - "src/chris/__tests__/reflect.test.ts — same shape."
    - "src/chris/__tests__/coach.test.ts — same shape."
    - "src/chris/__tests__/psychology.test.ts — same shape."
    - "src/chris/__tests__/produce.test.ts — same shape."
    - "src/chris/__tests__/engine.test.ts — ONE defensive test-mock fix in the SYCO-04/05 beforeEach: `vi.resetAllMocks()` wipes the inline `vi.mock` factory default on mockHybridSearch. Pre-Phase-22.1 this was benign because handleJournal called hybridSearch directly and the undefined result was absorbed by a mocked buildPensieveContext. Post-22.1 the JOURNAL handler routes through real retrieveContext which surfaces `routing.raw.length` in a diagnostic log — `undefined.length` throws. Restored `mockHybridSearch.mockResolvedValue([])` at the end of the SYCO beforeEach. No production code change."

key-decisions:
  - "hybridOptions is OPT-IN — callers without hybridOptions collapse to the pre-Plan-22.1 `{ limit: rawLimit }` behavior, so the 22 existing routing.test.ts tests pass byte-identically. The mode handlers are the only current callers that supply hybridOptions; future M009 weekly review can opt in with its own tag/recencyBias preferences without the orchestrator silently degrading the decision."
  - "summaryToSearchResult uses INLINE form `[Episode Summary YYYY-MM-DD | importance=N/10 | topics=...]\\n<summary>` — NOT the header form (`## Recent Episode Context (interpretation, not fact)`) that INTERROGATE uses via formatEpisodicBlock. Chat modes embed the summary as a numbered citation alongside raw entries (the summary is ONE of the sources, not a dominant context block); INTERROGATE's header form treats the summary as a dominant context block because INTERROGATE's citation contract requires date-anchored interpretation to lead. Mixing the two would muddle both layers."
  - "Score=1.0 sentinel for the synthetic SearchResult matches loadEntriesByIds' convention — explicit lookup (not similarity match). Chosen because buildPensieveContext filters score < 0.3; we need the summary-only result to survive the threshold and render as the #1 citation. The inclusive `>= 0.3` threshold in context-builder is the constraint the sentinel satisfies."
  - "`as typeof pensieveEntries.\$inferSelect` cast in summaryToSearchResult is the load-bearing shape-compatibility trick — lets us synthesize a pensieve-entry-shaped object without instantiating one through Drizzle or padding unused fields (telegramChatId, telegramMessageId). Safe because only `content` + `createdAt` flow into the downstream buildPensieveContext consumer; nothing reflects on the synthetic entry's identity. Uses `id: 'episodic-${summary.id}'` namespace prefix so the synthetic id cannot collide with a real pensieve_entries UUID (T-22.1-03 spoofing disposition)."
  - "Mode handlers prepend summaryToSearchResult to routing.raw when `routing.summary != null` — covers BOTH the `summary-only` branch (raw=[], summary=row) AND the `high-importance-descent` branch (raw=loadEntriesByIds rows, summary=row). On descent, the synthetic summary leads the citation block and raw source entries follow — mirrors the interpretation-then-facts ordering INTERROGATE uses for its header form."
  - "extractQueryDate called from every chat mode — regex fast-path means queries with no date heuristic (e.g., 'I moved to Batumi') skip the LLM entirely. Gated by DATE_HEURISTIC_KEYWORDS 49-keyword EN/FR/RU set. Bounded API cost on the hot path — zero per-message Haiku cost for the 90%+ of queries with no date signal."
  - "engine.test.ts SYCO-04/05 mockHybridSearch restore is a TEST fix, not a production-code change — the defensive `routing.raw?.length ?? 0` alternative in journal.ts would silently mask real regressions where retrieveContext returns `{ raw: undefined, ... }`. Preferring the explicit test-fixture fix keeps the production contract strict: routing.raw is always an array."

patterns-established:
  - "Opt-in orchestrator options passthrough — retrieveContext's `hybridOptions?: SearchOptions` passthrough pattern can be reused anywhere a generic orchestrator needs to preserve downstream-callee options across a decision tree without forcing every caller to opt in. Default collapse to previous behavior keeps existing tests byte-identical."
  - "Adapter export alongside orchestrator — summaryToSearchResult lives in routing.ts (not a separate adapters.ts) so callers import exactly one module for the whole orchestrator contract. Future adapter patterns (e.g., decisions-to-SearchResult) should follow the same shape: co-located with the orchestrator that produces the raw input type."
  - "Per-mode diagnostic log key naming: `chris.<mode>.routing` — matches the existing `chris.<mode>.response` / `chris.<mode>.empty` / `chris.<mode>.error` convention. Log shape `{ chatId, reason, hasSummary, rawCount }` is uniform across all 5 modes for aggregatable observability."
  - "Defensive test-mock restore after vi.resetAllMocks() — when a describe block uses `vi.resetAllMocks()` AND transitively invokes a real orchestrator over a mocked module, any inline `vi.mock` factory default values must be re-set in beforeEach. Pattern reusable for any future orchestrator-of-mocks test scenario."

requirements-completed: []  # RETR-02 and RETR-03 were already [x] — this plan closes the wiring gap documented in v2.2-MILESTONE-AUDIT.md tech_debt.

# Metrics
duration: "2h 10m"
completed: "2026-04-19"
---

# Phase 22.1 Plan 01: Wire retrieveContext into JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE Chat-Mode Handlers Summary

**Close v2.2 milestone-audit tech debt: `retrieveContext` orchestrator goes from zero production callers to 5 (one per chat mode). A 30-day-old user query in JOURNAL mode now escalates to the episodic-summary tier per the RETR-02 contract. Mode-specific *_SEARCH_OPTIONS identity (tags/recencyBias/minScore/limit) preserved end-to-end via a new `hybridOptions?: SearchOptions` passthrough on `RetrieveContextOptions`. Summary-only branch routed through a new `summaryToSearchResult(summary)` adapter that surfaces the episodic summary as a numbered SearchResult citation alongside raw entries. INTERROGATE + `/summary` handlers byte-identical per their documented bypass. Per-mode regression tests (3 cases × 5 modes = 15 new tests) prove the routing decision tree actually fires: recent → raw, old → summary, verbatim-keyword → raw regardless of age. Full Docker excluded-suite gate: 996 passed / 15 environmental failed (net +15 vs 981 post-23-04 baseline — exactly the 15 new regression tests), zero regressions beyond the documented environmental baseline.**

## Performance

- **Duration:** ~2h 10m wall-time (first commit `1a5ff93` 2026-04-19 at plan drop → final commit projected at SUMMARY write time)
- **Started:** 2026-04-19 (plan-drop commit `1a5ff93`)
- **Completed:** 2026-04-19
- **Tasks:** 8 (per plan — 1 routing.ts extension + 5 mode handler wires + 1 per-mode test pass + 1 full Docker gate)
- **Files created:** 0
- **Files modified:** 12 (1 routing.ts + 5 mode handlers + 5 mode test files + 1 engine.test.ts defensive restore)
- **Tests added:** 15 (3 per mode × 5 modes — `RETR-02/03 routing wiring` describe block)

## Accomplishments

- `retrieveContext` orchestrator now has 5 production callers — one per JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE chat-mode handler. A 30-day-old user query in any of the 5 modes escalates to the episodic-summary tier (or high-importance-descent tier when importance ≥ 8) per the RETR-02/03 contract that was documented but unreachable at runtime pre-Plan-22.1.
- `hybridOptions?: SearchOptions` passthrough on `RetrieveContextOptions` preserves per-mode identity round-trip — JOURNAL's `tags: ['FACT', 'RELATIONSHIP', 'PREFERENCE', 'VALUE']` + `recencyBias: 0.3`, REFLECT's `recencyBias: 0.1`, COACH's `recencyBias: 0.5` + `tags: ['BELIEF', 'INTENTION', 'VALUE']`, PSYCHOLOGY's `recencyBias: 0.2` + `tags: ['EMOTION', 'FEAR', 'BELIEF', 'DREAM']`, PRODUCE's `recencyBias: 0.3` — preserved end-to-end through the routing decision into every hybridSearch call site (verbatim fast-path, recent branch, no-summary-fallback branch, catch-block fallback). The merge `{ ...opts.hybridOptions, limit: opts.hybridOptions?.limit ?? rawLimit }` ensures callers without hybridOptions collapse to the pre-Plan behavior.
- `summaryToSearchResult(summary)` adapter exported from routing.ts — synthesizes a single SearchResult with score=1.0 sentinel whose entry.content carries the labeled inline block `[Episode Summary YYYY-MM-DD | importance=N/10 | topics=...]\n<summary>`. The synthetic entry is cast to `pensieveEntries.$inferSelect` so it flows through buildPensieveContext as a numbered citation. ID uses the `episodic-` namespace prefix so it cannot collide with a real pensieve_entries UUID.
- Per-mode regression tests: each of the 5 mode test files has a new `RETR-02/03 routing wiring` describe block with 3 it() cases covering the three canonical routing decisions. Recent 3d-ago query asserts `mockGetEpisodicSummary).not.toHaveBeenCalled()` + `mockHybridSearch).toHaveBeenCalledWith(query, expect.objectContaining(OPTIONS))`. Old 30d-ago query asserts `mockBuildPensieveContext.mock.calls[0]![0][0].entry.content).toContain('[Episode Summary 2026-03-15')` AND `.score === 1.0` AND `mockGetEpisodicSummary).toHaveBeenCalled()`. Verbatim-keyword 30d-ago query asserts `mockGetEpisodicSummary).not.toHaveBeenCalled()` — the fast-path overrides recency.
- INTERROGATE handler + `/summary` handler byte-identical — verified by `git diff --stat 747134e..HEAD -- src/chris/modes/interrogate.ts src/bot/handlers/summary.ts src/chris/__tests__/interrogate.test.ts src/bot/handlers/__tests__/summary.test.ts` returning zero changes. Both handlers keep their documented bypass (Flow 3: ad-hoc routing in interrogate.ts with formatEpisodicBlock header form; Flow 5: direct getEpisodicSummary in summary.ts).
- engine.test.ts regression caught + fixed — the SYCO-04/05 describe block uses `vi.resetAllMocks()` which wipes the inline `vi.mock` factory default on mockHybridSearch. Pre-Plan-22.1 this was benign because handleJournal called hybridSearch directly and the undefined result was absorbed by a mocked buildPensieveContext. Post-22.1 the JOURNAL handler routes through real retrieveContext which surfaces `routing.raw.length` in a diagnostic log — `undefined.length` throws. Fix: restored `mockHybridSearch.mockResolvedValue([])` at the end of the SYCO beforeEach. No production code change. 3 failures → 0 failures in engine.test.ts.
- Full Docker excluded-suite gate: **996 passed / 15 failed / 1011 total / 29.99s** — net +15 vs 981 post-23-04 baseline (exactly the 15 new regression tests across the 5 modes). The 15 failing tests are the documented environmental baseline: 3 models-smoke API-gated 401s (`claude-haiku-4-5` / `claude-sonnet-4-6` / `claude-opus-4-6` on test-key) + 7 engine-mute mock-chain (`db.select(...).from(...).where(...).limit is not a function`) + 5 photos-memory mock-chain (same Drizzle query-chain mismatch). These 15 are the SAME 15 failures documented across Plans 22-02/03/05 and 23-01..04. Zero new regressions.
- `tsc --noEmit` exits 0 across the entire codebase.
- Targeted vitest on the 5 mode tests + interrogate + /summary + routing: **8 files / 159 tests passed / 3.54s**.

## Task Commits

Each task was committed atomically (13 total commits between plan-drop `1a5ff93` and SUMMARY):

1. **Plan drop** — `1a5ff93` docs(22.1-01): plan to wire retrieveContext into chat-mode handlers
2. **Task 1: routing.ts hybridOptions passthrough + summaryToSearchResult adapter** — `cbf9f15` feat(22.1-01): add hybridOptions passthrough + summaryToSearchResult
3. **Task 2: journal.ts wire** — `3d83c75` feat(22.1-01): wire journal handler to retrieveContext
4. **Task 3: reflect.ts wire** — `9f380ca` feat(22.1-01): wire reflect handler to retrieveContext
5. **Task 4: coach.ts wire** — `416ec59` feat(22.1-01): wire coach handler to retrieveContext
6. **Task 5: psychology.ts wire** — `74cdaa7` feat(22.1-01): wire psychology handler to retrieveContext
7. **Task 6: produce.ts wire** — `39a1011` feat(22.1-01): wire produce handler to retrieveContext
8. **Task 7 (journal regression tests)** — `e1e35be` test(22.1-01): add routing wiring regression tests to journal
9. **Task 7 (reflect regression tests)** — `c44c4db` test(22.1-01): add routing wiring regression tests to reflect
10. **Task 7 (coach regression tests)** — `7b170f5` test(22.1-01): add routing wiring regression tests to coach
11. **Task 7 (psychology regression tests)** — `9b399e7` test(22.1-01): add routing wiring regression tests to psychology
12. **Task 7 (produce regression tests)** — `3664102` test(22.1-01): add routing wiring regression tests to produce
13. **Task 8 — Docker gate caught engine.test.ts regression; fixed** — `ea25f54` test(22.1-01): restore hybridSearch mock default after vi.resetAllMocks in SYCO suite

Plan metadata commit pending — this SUMMARY + STATE/ROADMAP/REQUIREMENTS update + 22.1-VERIFICATION.md.

## Files Created/Modified

**Created:** 0

**Modified:**
- `src/pensieve/routing.ts` (+57 lines — 224→281) — `RetrieveContextOptions` extended with `hybridOptions?: SearchOptions`; `retrieveContext` computes `mergedHybridOptions` once at the top and passes it to all 4 hybridSearch call sites (verbatim, recent, no-summary-fallback, catch-block fallback); new `summaryToSearchResult(summary): SearchResult` export.
- `src/chris/modes/journal.ts` — replaces direct `hybridSearch` call with `extractQueryDate → retrieveContext → [summaryToSearchResult(summary), ...raw]` block + `chris.journal.routing` info log. Preserves `buildPensieveContext(combined, { includeDate: false })` invariant.
- `src/chris/modes/reflect.ts` — same uniform wire with REFLECT_SEARCH_OPTIONS + `chris.reflect.routing` log.
- `src/chris/modes/coach.ts` — same with COACH_SEARCH_OPTIONS + `chris.coach.routing` log.
- `src/chris/modes/psychology.ts` — same with PSYCHOLOGY_SEARCH_OPTIONS + `chris.psychology.routing` log.
- `src/chris/modes/produce.ts` — same with PRODUCE_SEARCH_OPTIONS + `chris.produce.routing` log.
- `src/chris/__tests__/journal.test.ts` — extends retrieve.js mock with `getEpisodicSummary`, adds `extractQueryDate` mock, relaxes `calls hybridSearch` assertion to `objectContaining`, adds 3-case `RETR-02/03 routing wiring` describe.
- `src/chris/__tests__/reflect.test.ts` — same shape.
- `src/chris/__tests__/coach.test.ts` — same shape.
- `src/chris/__tests__/psychology.test.ts` — same shape.
- `src/chris/__tests__/produce.test.ts` — same shape.
- `src/chris/__tests__/engine.test.ts` — single-line defensive restore: `mockHybridSearch.mockResolvedValue([])` appended to the SYCO-04/05 describe block's beforeEach (after `vi.resetAllMocks()`). No production code change. Resolves 3 transient engine test failures caused by `routing.raw.length` surfacing undefined in the journal routing log when the real retrieveContext runs over a reset hybridSearch mock.

## Decisions Made

All decisions carried forward verbatim from the plan's `<action>` blocks with one Rule 1 adaptation (the defensive engine.test.ts mock restore — the plan's test-writing tasks did not anticipate this side-effect until the full Docker gate caught it).

Key decisions also documented in the frontmatter `key-decisions` block:
- **hybridOptions is OPT-IN.** Callers without hybridOptions collapse to pre-Plan behavior. 22 existing routing.test.ts tests pass byte-identically.
- **summaryToSearchResult uses INLINE form, NOT the header form INTERROGATE uses.** Chat modes embed the summary as one citation among many; INTERROGATE's header form treats the summary as a dominant context block. Different citation contracts.
- **Score=1.0 sentinel matches loadEntriesByIds' convention.** Survives buildPensieveContext's 0.3 threshold.
- **`as typeof pensieveEntries.$inferSelect` cast is the load-bearing shape-compatibility trick** — only `content` + `createdAt` flow into downstream buildPensieveContext consumer. `id: 'episodic-${summary.id}'` namespace prefix is T-22.1-03's spoofing mitigation.
- **Mode handlers prepend summaryToSearchResult on both summary-only AND high-importance-descent branches.** On descent, synthetic summary leads and raw sources follow — mirrors INTERROGATE's interpretation-then-facts ordering for the header form.
- **extractQueryDate's regex fast-path means zero per-message API cost on queries with no date signal.** DATE_HEURISTIC_KEYWORDS gating enforced at function entry.
- **engine.test.ts restore is a TEST fix, NOT production-code defensive programming.** Preferred the explicit mock restore over silently masking real regressions.

## Deviations from Plan

### Deviation 1 (Rule 1): engine.test.ts SYCO-04/05 mockHybridSearch restore not anticipated by the plan

**What:** The plan's Task 7 `<action>` block covered the 5 mode test files but not `src/chris/__tests__/engine.test.ts`. Post-Plan-22.1 wiring of journal.ts made the JOURNAL handler call real retrieveContext over the mocked hybridSearch — which `vi.resetAllMocks()` in the SYCO-04/05 beforeEach wiped, causing 3 transient test failures: `Cannot read properties of undefined (reading 'length')` at journal.ts:57 (`rawCount: routing.raw.length`).

**Why the plan didn't catch this:** The plan's coverage model was "files named in `<files>` blocks plus test files for those same source files." engine.test.ts is a test file for `src/chris/engine.ts`, not for `src/chris/modes/journal.ts` — the journal dependency was transitive via processMessage's mode dispatch. The plan's <verification> block referenced `npx vitest run` against the 5 mode test files explicitly; the full Docker gate is what surfaced the regression.

**Fix:** Added `mockHybridSearch.mockResolvedValue([])` at the end of the SYCO describe's beforeEach. Single-line, localized, no production change. Commit `ea25f54`. Alternative considered and rejected: defensive `routing.raw?.length ?? 0` in journal.ts — would silently mask real regressions where retrieveContext returns undefined raw. Keeping the production contract strict is preferred.

**Impact:** 3 failing engine tests → 0 failing. Full Docker gate now at 996/15 (exactly +15 vs baseline from the 5 mode × 3 regression tests).

### Deviation 2 (Rule 1 adaptation): per-mode regression test first-arg assertion form

**What:** The plan's Task 7 Step 1 example for the JOURNAL "old query → summary tier" assertion uses `const createCall = mockCreate.mock.calls[0]![0]; const systemText = createCall.system[0].text; expect(systemText).toContain('[Episode Summary 2026-03-15');` — asserting on the final system prompt text passed to Sonnet. This shape works for JOURNAL where `buildSystemPrompt` is NOT mocked (journal.test.ts imports real buildSystemPrompt via personality.js passthrough).

For the other 4 modes (reflect/coach/psychology/produce), `buildSystemPrompt` IS mocked (returns `'interpolated'` placeholder), so asserting on the system-prompt text cannot catch the summary marker. The plan's Step 2 already anticipated this divergence: "adapt the 'old query' assertion to instead check the buildPensieveContext input." Adopted the suggested form.

**Why not a full deviation:** The plan explicitly telegraphed this in `<action> Step 2`: *"For reflect/coach/psychology/produce, the test files already mock buildSystemPrompt (returning a placeholder string) — that intercepts the system prompt assertion path. Adapt the 'old query' assertion to instead check the buildPensieveContext input"*. So this is a plan-anticipated adaptation, not an un-planned Rule 1 deviation.

**Implementation:** For REFLECT/COACH/PSYCHOLOGY/PRODUCE: `const buildCall = mockBuildPensieveContext.mock.calls[0]![0]; expect(buildCall[0].entry.content).toContain('[Episode Summary 2026-03-15'); expect(buildCall[0].score).toBe(1.0);`. Asserts on the buildPensieveContext first-arg's first element (the synthetic summary SearchResult).

### Deviation 3 (documentation-only): plan mentioned 4 hybridSearch call sites in routing.ts but the actual count was verified by grep before edits

**What:** The plan's Task 1 Step 4 asserts "Three call sites: verbatim-keyword branch, recent branch, no-summary-fallback branch, AND the catch-block fallback. (Four total — count carefully.)". Verified against the actual routing.ts source: 4 call sites at lines 151, 166, 184, 232 (post-merge). Plan instruction followed verbatim. No implementation deviation — documentation flag only to note the plan's caveat was precisely accurate.

**Total deviations:** 1 Rule 1 (engine.test.ts restore, discovered during Docker gate, fixed inline, commit `ea25f54`).

**Impact on plan:** Clean execution. All acceptance criteria pass.

## Issues Encountered

- **engine.test.ts SYCO-04/05 regression (fixed).** Described under Deviation 1. Caught by the full Docker gate; fixed via single-line mock restore in beforeEach; 3 failing → 0 failing; zero production code change.
- **Vitest 4 fork-mode IPC hang under HuggingFace cache EACCES — documented baseline, NOT a Plan 22.1 issue.** Same pattern as Plans 22-02/03/05 and 23-01..04 SUMMARYs: full `bash scripts/test.sh` without exclusions hits a fork-pool IPC hang when the worker tries to load @huggingface/transformers bge-m3 with root-owned `.cache/` directory. Mitigated via the documented 5-file exclusion list (`live-integration.test.ts`, `live-accountability.test.ts`, `vague-validator-live.test.ts`, `contradiction-false-positive.test.ts`, `live-anti-flattery.test.ts`). Produces deterministic 996/15 output in ~30s.
- **Postgres container state between runs.** First Docker gate invocation failed with `ERROR: type "contradiction_status" already exists` because postgres from a prior test run already had Phase 15 schema applied. Resolved by `docker compose -f docker-compose.local.yml down --volumes` before the gate run. Documented for operators running the gate locally after paused sessions.

## Threat Model

All four threats from the plan's `<threat_model>` block verified as accepted:

- **T-22.1-01 (Tampering on extractQueryDate Haiku fallback) — accepted.** Phase 22 Plan 03 already analyzed — regex fast-path deterministic; Haiku call gated on DATE_HEURISTIC_KEYWORDS; never-throw with logged JSON parse fallback returning null. No new exposure from this plan.
- **T-22.1-02 (Information Disclosure on summaryToSearchResult content in system prompt) — accepted.** Summary text + metadata are the SAME data INTERROGATE already injects via formatEpisodicBlock (Phase 22 Plan 03 shipped this). Chat modes now see the same data via the inline-form adapter. No new data class crossing a trust boundary; only the rendering form differs.
- **T-22.1-03 (Spoofing on synthetic entry id) — accepted.** The id sentinel uses `episodic-${summary.id}` namespace prefix so it cannot collide with a real pensieveEntries UUID. Downstream consumers don't query the id back to the database — it's a display-only field for buildPensieveContext's numbered citation.
- **T-22.1-04 (Elevation on mode handler signature) — accepted.** `handle<Mode>(chatId, text, language?, declinedTopics?)` signature unchanged. queryDate extracted internally from user text via the Phase 22 RETR-04 primitive. No new privileged params introduced to the engine layer.

## Next Phase Readiness

- **v2.2 milestone audit can flip `tech_debt` → `passed`** — the only tech_debt items cited in `.planning/v2.2-MILESTONE-AUDIT.md` frontmatter were RETR-02/03 wiring (now closed) and the environmental vitest-fork-hang (pre-existing, not a v2.2 regression). The env debt persists; the RETR debt does not. Plan 22.1 VERIFICATION.md + milestone-audit frontmatter flip to follow this SUMMARY.
- **M009 weekly review** — can now rely on the hybridOptions passthrough pattern; if M009 adds a new `WEEKLY_REVIEW_SEARCH_OPTIONS` const with its own tags/recencyBias, retrieveContext preserves them round-trip through the decision tree without silent degradation.
- **/resummary deferred command (EPI-FUTURE-01)** — if/when shipped, can reuse `summaryToSearchResult` directly without rebuilding the synthesis logic.
- **Proxmox deploy (pending explicit Greg approval per D019)** — RETR-02/03 are now runtime-reachable in production. Greg will see the episodic-summary tier fire for real 30-day+ queries in chat modes post-deploy.

## Self-Check: PASSED

Verified all claims:

- [x] `src/pensieve/routing.ts` exports `summaryToSearchResult` — verified via `grep -n "^export function summaryToSearchResult"`
- [x] `src/pensieve/routing.ts` `RetrieveContextOptions` has `hybridOptions?: SearchOptions` — verified via `grep -n "hybridOptions"`
- [x] `grep -c "retrieveContext\b" src/chris/modes/*.ts` returns ≥ 5 (one per mode)
- [x] `grep -c "hybridSearch\b" src/chris/modes/*.ts` (excluding interrogate.ts which was unchanged) returns 0 — verified via `Grep hybridSearch src/chris/modes`
- [x] `grep -c "extractQueryDate\b" src/chris/modes/*.ts` returns 10 (2 × 5 mode files: import + call site) + 2 in interrogate.ts (Phase 22 — unchanged) = 12 total
- [x] `npx tsc --noEmit` exits 0 with no output
- [x] Targeted vitest on 5 mode tests + interrogate + /summary + routing: **8 files / 159 tests passed / 3.54s**
- [x] Excluded-suite Docker gate: **996 passed / 15 failed / 1011 total / 29.99s** — net +15 vs 981 baseline (exact match: 5 modes × 3 new regression tests); the 15 failures are the documented environmental baseline (3 models-smoke API 401s + 7 engine-mute mock-chain + 5 photos-memory mock-chain — all pre-existing).
- [x] `git diff --stat 747134e..HEAD -- src/chris/modes/interrogate.ts src/bot/handlers/summary.ts src/chris/__tests__/interrogate.test.ts src/bot/handlers/__tests__/summary.test.ts` returns empty (zero changes) — INTERROGATE + /summary byte-identical
- [x] All 13 Phase 22.1 commits exist in `git log`: plan-drop `1a5ff93`, routing.ts `cbf9f15`, journal `3d83c75`, reflect `9f380ca`, coach `416ec59`, psychology `74cdaa7`, produce `39a1011`, journal-test `e1e35be`, reflect-test `c44c4db`, coach-test `7b170f5`, psychology-test `9b399e7`, produce-test `3664102`, engine-test-fix `ea25f54`

---
*Phase: 22.1-wire-retrieve-context*
*Completed: 2026-04-19*
