---
milestone: v2.2
milestone_name: M008 Episodic Consolidation
audited: 2026-04-19
status: tech_debt
scores:
  requirements: 33/35 fully wired (35/35 implemented + tested; 2 partial; 1 unreachable at runtime)
  phases: 4/4 verified passed
  integration: 5/6 flows complete (1 degraded — non-INTERROGATE retrieval routing)
  flows: 5/6 fully wired
  nyquist: not_evaluated (no VALIDATION.md files in milestone scope)
gaps:
  requirements: []
tech_debt:
  - phase: 22-cron-retrieval-routing
    items:
      - "RETR-02/03: `retrieveContext` orchestrator in `src/pensieve/routing.ts` is implemented + 22-test covered + boundary-audited, but has zero production callers. JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE mode handlers call `hybridSearch` directly with mode-specific options, bypassing the routing orchestrator. Net effect: a 30-day-old user query in JOURNAL mode does NOT escalate to the episodic-summary tier as RETR-02 specifies. INTERROGATE works (it has its own ad-hoc routing in Flow 3) and `/summary` works (Flow 5 directly calls `getEpisodicSummary`), so the user-visible feature surface is not broken — but the general recency-routing contract for chat modes is not live."
      - "RETR-01: `getEpisodicSummariesRange` is exported and tested, but has zero production callers in v2.2. Substrate for M009 weekly review (which will fan out across a 7-day window). Acceptable as-is — explicitly forward-looking."
  - phase: 22-cron-retrieval-routing (env)
    items:
      - "Vitest 4 fork-mode IPC hang under HuggingFace cache EACCES — full `bash scripts/test.sh` does not complete in Plans 22-02/03/05 and 23-01..04. Mitigated via excluded-suite + targeted vitest + HEAD~N regression cross-check. Documented in each plan SUMMARY. Underlying issue: root-owned `node_modules/@huggingface/transformers` cache directory + `live-integration.test.ts` 401-retry loop. Not a Phase 22/23 regression — predates this milestone — but worth a future fix-up phase."
---

# M008 v2.2 Episodic Consolidation — Milestone Audit

**Milestone:** v2.2 M008 Episodic Consolidation
**Audited:** 2026-04-19
**Status:** `tech_debt` — all requirements implemented and tested; one orchestrator (RETR-02/03 `retrieveContext`) is unreachable at runtime in chat-mode handlers.

## Phase Verification Roll-Up

| Phase | Plans | Reqs | Status | Score |
|-------|-------|------|--------|-------|
| 20 Schema + Tech Debt | 3/3 | TD-01, EPI-01..04 (5) | passed | 5/5 |
| 21 Consolidation Engine | 4/4 | CONS-01..12 (12) | passed | 12/12 |
| 22 Cron + Retrieval Routing | 5/5 | CRON-01..02, RETR-01..06 (8) | passed | 13/13 |
| 23 Test Suite + Backfill + /summary | 4/4 | TEST-15..22, OPS-01, CMD-01 (10) | passed | 14/14 |

**All 4 phase VERIFICATION.md files report `status: passed`. Total: 16 plans, 35/35 requirements checked off in REQUIREMENTS.md.**

## Cross-Phase Integration

Integration check report: `.planning/v2.2-INTEGRATION-CHECK.md`

| # | E2E Flow | Status |
|---|----------|--------|
| 1 | Daily cron consolidation (`cron → runConsolidateYesterday → runConsolidate → sources + prompt + Sonnet + INSERT → notify`) | ✅ COMPLETE |
| 2 | General retrieval routing (chat modes → `retrieveContext` → recency-based raw vs summary) | ⚠ DEGRADED — `retrieveContext` has no production caller |
| 3 | INTERROGATE date-anchored injection (`extractQueryDate` → `getEpisodicSummary` → context injection) | ✅ COMPLETE |
| 4 | Operator backfill (`scripts/backfill-episodic.ts --from --to → runConsolidate per day`) | ✅ COMPLETE |
| 5 | `/summary [YYYY-MM-DD]` Telegram command | ✅ COMPLETE |
| 6 | Boundary preservation (Known Facts + pensieve_embeddings never reference episodic_summaries) | ✅ COMPLETE — enforced by `boundary-audit.test.ts` |

## Requirements Coverage (35/35 implemented; 33/35 fully wired)

All 35 requirements have:
- Code implementation (verified via grep)
- Test coverage (unit and/or integration)
- Phase VERIFICATION.md attestation
- SUMMARY.md frontmatter listing
- REQUIREMENTS.md `[x]` checkbox

**Wiring nuance:**
- 32 reqs have at least one production caller in committed code.
- **RETR-02, RETR-03** are implemented + tested + audited but `retrieveContext` is not called from any chat-mode handler. The episodic tier IS reachable via INTERROGATE (Flow 3) and `/summary` (Flow 5), so user-visible feature is not broken — but the general recency-routing contract for JOURNAL/REFLECT/COACH/PSYCHOLOGY/PRODUCE is not active.
- **RETR-01 `getEpisodicSummariesRange`** is unconsumed in v2.2 (forward substrate for M009 weekly review). Acceptable.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant |
|-------|---------------|-----------|
| 20 | missing | n/a |
| 21 | missing | n/a |
| 22 | missing | n/a |
| 23 | missing | n/a |

No phases ran `/gsd-validate-phase`. Nyquist validation enabled in config but not invoked. Optional fix-up: run `/gsd-validate-phase 20` through `/gsd-validate-phase 23`.

## Recommended Action

The milestone is shippable as-is — all user-visible features (daily cron, INTERROGATE date queries, `/summary` command, operator backfill) work end-to-end. The RETR-02/03 wiring gap is real but does not block any user flow.

**Two options:**

**A. Ship as-is** — accept tech debt, log RETR-02/03 wiring as M008.1 follow-up or M009 prerequisite.
- `/gsd-complete-milestone v2.2`

**B. Plan a small wiring phase** — connect `retrieveContext` to chat-mode handlers before shipping.
- `/gsd-plan-milestone-gaps`
- Estimated scope: 1 plan, ~2-4 hours — modify mode handlers to call `retrieveContext` instead of `hybridSearch` directly. Includes regression tests for the 5 modes.

## Pause Gate Reminder (from ROADMAP)

≥7 days of real episodic summaries should accumulate before M009 starts. Operator action post-deploy:
```
tsx scripts/backfill-episodic.ts --from <M007-deploy-date> --to <yesterday>
```
